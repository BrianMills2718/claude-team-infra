import type { Lesson } from "../../types";
export const LESSONS: Lesson[] = [
 {
  "id": "stage-0",
  "stage": 0,
  "title": "Setup & onboarding",
  "summary": "Setup & onboarding: deployment path selection, authentication precedence, non-interactive authentication.",
  "prerequisites": [],
  "objectives": [
   "Understand the concepts in Setup & onboarding."
  ],
  "definitions": [
   {
    "term": "deployment path selection",
    "short": "Choosing the account/provider route (direct subscription vs cloud provider) that determines billing, auth, and which features you get before any install happens."
   },
   {
    "term": "authentication precedence",
    "short": "Claude Code resolves credentials through a fixed priority order, so the highest-set mechanism wins regardless of what else is configured."
   },
   {
    "term": "non-interactive authentication",
    "short": "Long-lived or vault-backed credentials let Claude Code run in CI and scripts where interactive browser login is impossible."
   },
   {
    "term": "fleet version control",
    "short": "Keeping every team member on a consistent, non-stale Claude Code version, since some install methods auto-update and others silently don't."
   },
   {
    "term": "managed settings precedence",
    "short": "Org-controlled policy that overrides all local user and project config, delivered through ranked sources so admins can enforce rules developers can't undo."
   },
   {
    "term": "permissions paired with sandboxing",
    "short": "Tool-level allow/deny rules only constrain known tools, so true isolation requires combining them with OS-level filesystem and network sandboxing."
   }
  ],
  "sections": [
   {
    "heading": "Deployment Path Selection: Commit Before You Install",
    "body": "The most consequential decision in a team rollout happens before anyone runs the installer: which account and provider path will the team use? This determines billing ownership, which authentication flow each developer gets, what data residency guarantees apply, and which features are available.\n\n### The Four Paths\n\n| Path | Auth mechanism | Billing | Features gated | Best for |\n|------|---------------|---------|---------------|----------|\n| Claude Pro/Max (individual) | OAuth via claude.ai | Per-user Anthropic subscription | No enterprise policy, no SSO | Solo devs, evaluation |\n| Claude for Teams/Enterprise | OAuth via claude.ai (SSO optional on Enterprise) | Centralized org billing | Teams: admin tools, billing mgmt. Enterprise adds SSO/SAML/OIDC, domain capture, compliance API, managed settings | Most teams |\n| Claude Console (API key) | Console account + API key or Console OAuth | API token usage, org billing | No native seat mgmt via claude.ai; you own retry/rate logic | API-heavy shops already on Console |\n| Cloud provider (Bedrock / Vertex AI / Foundry) | AWS/GCP/Azure credentials | Cloud provider bill | No browser OAuth needed; no `claude setup-token` OAuth tokens; WebSearch availability depends on region | Enterprises with existing cloud commitment, strict data residency |\n\n### What the Path Locks In\n\n**Teams/Enterprise** is the fastest path to org-controlled policy. Once you invite a user, they get a Claude.ai account. Setting `forceLoginMethod: \"claudeai\"` in managed settings ensures they can only authenticate as a Claude.ai account member — and critically, it **blocks** `ANTHROPIC_API_KEY`, `ANTHROPIC_AUTH_TOKEN`, and `apiKeyHelper` from working. No personal API key can override it. The Enterprise tier adds SSO (SAML/OIDC), domain capture, and server-managed settings pushed from the claude.ai admin console.\n\n**Console (API key)** is appropriate when the team already manages API token budgets, needs programmatic access patterns, or wants to gate Claude Code access through existing IAM tooling. The downside: no centralized seat management via claude.ai — each developer needs a Console account and API key distributed and rotated by you.\n\n**Cloud provider** makes sense when your organization already has AWS/GCP/Azure contractual commitments, wants inference billed to existing cloud accounts, or has data residency requirements. The tradeoff: Bedrock and Vertex paths skip browser OAuth entirely, so developers need working cloud credentials before they can run `claude`, and `claude setup-token` OAuth tokens cannot be used with these paths. On Vertex AI, `CLAUDE_CODE_USE_VERTEX=1` plus GCP credentials (via ADC) are the full auth path — no Anthropic account required.\n\n### Common Pitfall: Mixing Paths on the Same Fleet\n\nIf some developers authenticate via subscription OAuth and others via `ANTHROPIC_API_KEY`, you get two separate billing streams, two audit trails, and policy enforcement that only applies to the OAuth users. Pick one path and enforce it via managed settings (`forceLoginMethod`). Mixed fleets are the most common source of confusing cost attribution and partial policy coverage."
   },
   {
    "heading": "Authentication Precedence: The Fixed Resolution Order",
    "body": "Claude Code resolves credentials through a strict six-level priority stack. The highest-set mechanism wins; everything below it is ignored. Understanding this order is essential for CI setup, for diagnosing unexpected auth failures, and for knowing which levers enterprise admins control versus individual developers.\n\n```\nPriority 1 (highest): Cloud provider credentials\n  → Active when: CLAUDE_CODE_USE_BEDROCK=1 | CLAUDE_CODE_USE_VERTEX=1 | CLAUDE_CODE_USE_FOUNDRY=1\n  → Uses: AWS SDK credential chain | GCP Application Default Credentials | Azure credentials\n\nPriority 2: ANTHROPIC_AUTH_TOKEN env var\n  → Sent as: Authorization: Bearer <token>\n  → Use for: LLM gateway / proxy that authenticates via bearer token rather than API keys\n\nPriority 3: ANTHROPIC_API_KEY env var\n  → Sent as: X-Api-Key header\n  → In interactive mode: prompts once to approve; choice remembered. Toggle later via /config\n  → In -p (non-interactive) mode: always used when present, no prompt\n\nPriority 4: apiKeyHelper script\n  → Configured in settings.json: { \"apiKeyHelper\": \"vault read -field=api_key secret/claude-code\" }\n  → Runs shell command, uses stdout as API key\n  → Re-runs after 5 min or HTTP 401; configurable via CLAUDE_CODE_API_KEY_HELPER_TTL_MS\n  → Warning shown if script takes >10s\n  → CLI sessions only — Claude Desktop and web sessions ignore it\n\nPriority 5: CLAUDE_CODE_OAUTH_TOKEN env var\n  → Long-lived OAuth token from `claude setup-token`\n  → Valid for 1 year; scoped to inference only\n  → Cannot be used with --bare mode\n  → Cannot establish Remote Control sessions\n\nPriority 6 (lowest): Subscription OAuth from /login\n  → Default for Pro/Max/Teams/Enterprise interactive users\n  → Stored in macOS Keychain; ~/.claude/.credentials.json on Linux (mode 0600);\n    %USERPROFILE%\\.claude\\.credentials.json on Windows\n```\n\n### The Silent Trap: API Key Shadows Subscription\n\nIf a developer has an `ANTHROPIC_API_KEY` set in their shell environment (from an old project, a dotfile, etc.) and also holds a Claude for Teams subscription, Priority 3 fires and they authenticate via the API key — billing the API key's organization, not the team subscription. If that key belongs to an expired or disabled org, they see auth failures with no obvious cause.\n\nDiagnosis: run `/status` inside an active Claude Code session. The status line shows exactly which auth method is active. Remediation: `unset ANTHROPIC_API_KEY` and re-run.\n\n**Important:** If `forceLoginMethod: \"claudeai\"` is set in managed settings, then `ANTHROPIC_API_KEY`, `ANTHROPIC_AUTH_TOKEN`, and `apiKeyHelper` are all blocked at startup — not just shadowed but outright rejected. Cloud provider paths (Bedrock, Vertex, Foundry) are unaffected by `forceLoginMethod`.\n\n### What `apiKeyHelper` Enables\n\nFor teams using secrets managers (Vault, AWS Secrets Manager, GCP Secret Manager), `apiKeyHelper` is the bridge. The setting takes a shell command string; Claude Code calls it on a timer and uses stdout as the API key:\n\n```json\n// ~/.claude/settings.json or .claude/settings.json\n{\n  \"apiKeyHelper\": \"vault read -field=api_key secret/claude-code\"\n}\n```\n\nThis keeps long-lived credentials out of environment variables entirely. Combine with a short `CLAUDE_CODE_API_KEY_HELPER_TTL_MS` (e.g., `900000` for 15 minutes) for defense in depth. Note: `apiKeyHelper` applies to CLI sessions only — Claude Desktop and web sessions use OAuth exclusively and ignore it."
   },
   {
    "heading": "Non-Interactive Authentication: CI and Script Patterns",
    "body": "Claude Code's interactive login flow opens a browser window — fine on a developer workstation, completely impossible in a CI runner, Docker container, or SSH session. Three patterns cover the non-interactive case, each with different security profiles.\n\n## Pattern 1: API Key via Secret Store\n\nThe simplest pattern for most CI systems. Store the key as a secret, inject it as an environment variable:\n\n```yaml\n# GitHub Actions — using the official action (v1)\n- uses: anthropics/claude-code-action@v1\n  with:\n    anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}\n    prompt: \"Run the test suite and summarize failures\"\n    claude_args: \"--max-turns 10\"\n```\n\nFor raw CLI invocations (GitLab CI, Jenkins, etc.):\n\n```bash\nexport ANTHROPIC_API_KEY=\"$CI_ANTHROPIC_API_KEY\"   # injected from CI secret store\nclaude --bare -p \"Review staged changes\" --allowedTools \"Bash(git diff *),Bash(git log *)\"\n```\n\n**Why `--bare`?** Bare mode skips auto-discovery of hooks, skills, plugins, MCP servers, auto memory, and CLAUDE.md loading. It produces identical behavior on every machine regardless of what's in `.claude/` or `~/.claude/`. This is the recommended mode for CI and will become the default for `-p` in a future release.\n\n**Key constraint:** Bare mode does NOT read `CLAUDE_CODE_OAUTH_TOKEN`. If using `--bare`, authenticate via `ANTHROPIC_API_KEY` or an `apiKeyHelper` passed with `--settings`. Cloud provider credentials (Bedrock, Vertex, Foundry) work normally in bare mode.\n\n## Pattern 2: Long-Lived OAuth Token\n\nFor teams on Teams/Enterprise who want CI to use their subscription quota (not a separate API key), generate a one-year OAuth token:\n\n```bash\n# Run interactively once, from a developer machine with valid subscription\nclaude setup-token\n# Walks through OAuth authorization and prints token to stdout.\n# It does NOT save the token — copy it manually.\n\n# Store in your CI secret store, then inject:\nexport CLAUDE_CODE_OAUTH_TOKEN=\"<token>\"\nclaude -p \"Generate changelog for this sprint\"\n```\n\nThis token authenticates with your Claude subscription (requires Pro, Max, Teams, or Enterprise). It is scoped to inference only. It cannot be used with `--bare`. It cannot establish Remote Control sessions. If the subscription lapses, the token stops working immediately.\n\n## Pattern 3: Cloud Provider Credentials (Bedrock/Vertex in CI)\n\nFor GitHub Actions with Bedrock, use OIDC instead of static AWS keys:\n\n```yaml\npermissions:\n  id-token: write\n  contents: read\n\nsteps:\n  - uses: actions/checkout@v4\n\n  - uses: aws-actions/configure-aws-credentials@v4\n    with:\n      role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}\n      aws-region: us-west-2\n\n  - uses: anthropics/claude-code-action@v1\n    with:\n      use_bedrock: \"true\"\n      claude_args: \"--model us.anthropic.claude-sonnet-4-6 --max-turns 10\"\n    env:\n      AWS_REGION: us-west-2\n```\n\nFor Vertex AI, use Workload Identity Federation:\n\n```yaml\npermissions:\n  id-token: write\n  contents: read\n\nsteps:\n  - uses: actions/checkout@v4\n\n  - uses: google-github-actions/auth@v2\n    id: auth\n    with:\n      workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}\n      service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}\n\n  - uses: anthropics/claude-code-action@v1\n    with:\n      use_vertex: \"true\"\n      claude_args: \"--max-turns 10\"\n    env:\n      CLAUDE_CODE_USE_VERTEX: \"1\"\n      ANTHROPIC_VERTEX_PROJECT_ID: ${{ steps.auth.outputs.project_id }}\n      CLOUD_ML_REGION: us-east5\n```\n\nOIDC and Workload Identity Federation eliminate static secrets from CI entirely — credentials are short-lived and automatically rotated. This is the correct long-term posture for any production fleet. For 3rd-party providers (Bedrock, Vertex), you also need a custom GitHub App rather than the official Anthropic GitHub App; see the GitHub Actions docs for the full setup.\n\n## Troubleshooting Non-Interactive Auth\n\nIn interactive sessions, run `/status` to see the resolved auth method. In `--bare` CI runs, verify the key is visible before invoking:\n\n```bash\n# Confirm the key is visible before invoking claude\necho \"API key prefix: ${ANTHROPIC_API_KEY:0:8}...\"\nclaude --bare -p \"echo hello\" --allowedTools \"Bash(echo *)\" --output-format json | jq '.result'\n```"
   },
   {
    "heading": "Fleet Version Control: Who Updates and When",
    "body": "Claude Code ships multiple times per week. Across a team of 20 engineers, the combination of different install methods and auto-update behaviors can produce a version spread of weeks within days of a release. This matters because prompt behavior, tool schemas, and permission semantics can change between versions.\n\n### Install Method vs. Auto-Update Behavior\n\n| Install method | Auto-update | How to upgrade manually | Stable channel option |\n|---------------|-------------|------------------------|---------------------|\n| Native installer (`install.sh` / PowerShell / CMD) | Yes, background | `claude update` | `bash -s stable` at install time, or `autoUpdatesChannel: \"stable\"` in settings |\n| Homebrew `claude-code` (stable cask) | No (opt-in via `CLAUDE_CODE_PACKAGE_MANAGER_AUTO_UPDATE=1`) | `brew upgrade claude-code` | Default (already stable channel) |\n| Homebrew `claude-code@latest` | No (opt-in) | `brew upgrade claude-code@latest` | Switch to `claude-code` cask instead |\n| WinGet (`winget install Anthropic.ClaudeCode`) | No (opt-in) | `winget upgrade Anthropic.ClaudeCode` | Not natively; enforce via managed settings |\n| apt / dnf / apk | No (package manager controls) | `sudo apt upgrade claude-code` etc. | Use `stable` repo URL at setup (see Linux package manager docs) |\n| npm global (`@anthropic-ai/claude-code`) | No | `npm install -g @anthropic-ai/claude-code@latest` | Not natively |\n\nThe native installer is the only method that silently auto-updates. If your team mixes native installs with Homebrew, you will have version drift within days.\n\n**Homebrew note:** Homebrew keeps two casks: `claude-code` tracks the stable release channel (typically ~1 week behind, skips major-regression releases); `claude-code@latest` tracks every release immediately. Choose the cask name intentionally.\n\n### Enforcing a Version Range via Managed Settings\n\nThe most reliable fleet control mechanism is `requiredMinimumVersion` and `requiredMaximumVersion` in managed settings. These are managed-settings-only keys — user and project settings cannot override them. Claude Code checks them at startup and refuses to run if outside the allowed range:\n\n```json\n// /Library/Application Support/ClaudeCode/managed-settings.json (macOS)\n// /etc/claude-code/managed-settings.json (Linux)\n{\n  \"requiredMinimumVersion\": \"2.1.175\",\n  \"requiredMaximumVersion\": \"2.1.200\",\n  \"autoUpdatesChannel\": \"stable\"\n}\n```\n\nWith this in place:\n- A user on v2.1.170 sees a startup error and cannot proceed until they update (but `claude update`, `claude install`, and `claude doctor` still work below the floor for recovery)\n- A user on v2.1.205 sees a startup error until you raise the ceiling\n- Background auto-updates will not install a version above the ceiling\n\n**Important:** If `requiredMinimumVersion` or `requiredMaximumVersion` contain an invalid value, the validation fails open — the invalid field is stripped to prevent blocking startup. Validate carefully before fleet deployment.\n\n### Pinning for Bedrock Deployments\n\nOn Bedrock, model IDs use a region prefix. Always pin explicit model IDs rather than aliases to ensure consistent behavior across users and regions:\n\n```bash\n# In managed settings or CI env block — use actual model IDs from your Bedrock console\nexport ANTHROPIC_DEFAULT_SONNET_MODEL='us.anthropic.claude-sonnet-4-6'\n```\n\nWithout pinning, `claude update` can silently move users to a different model. With pinning, you control when your team rolls forward. Verify the exact model IDs available in your AWS account and region via the Bedrock console before setting these.\n\n### Verifying Installation Uniformity\n\n```bash\n# Check installed version\nclaude --version\n\n# Run the full diagnostic\nclaude doctor\n```\n\n`claude doctor` checks for update failures, missing dependencies, sandbox status, managed settings validity, and credential configuration issues. Run it as part of team onboarding validation scripts and after deploying managed settings changes."
   },
   {
    "heading": "Managed Settings Precedence: The Enforcement Stack",
    "body": "Claude Code's settings system has five layers. The key insight is that higher layers do not merely override lower layers — for some keys, they actively block lower layers from expressing any value at all.\n\n### The Five Layers\n\n```\nPriority 1 (highest): Managed settings\n  macOS:\n    /Library/Application Support/ClaudeCode/managed-settings.json\n    /Library/Application Support/ClaudeCode/managed-settings.d/*.json  (drop-in dir)\n    MDM plist: com.anthropic.claudecode\n  Linux/WSL:\n    /etc/claude-code/managed-settings.json\n    /etc/claude-code/managed-settings.d/*.json  (drop-in dir)\n  Windows:\n    C:\\Program Files\\ClaudeCode\\managed-settings.json\n    C:\\Program Files\\ClaudeCode\\managed-settings.d\\*.json\n    HKLM\\SOFTWARE\\Policies\\ClaudeCode  (Group Policy / Intune)\n    HKCU\\SOFTWARE\\Policies\\ClaudeCode  (user-level, lowest within managed tier)\n  Also: server-managed settings pushed from claude.ai admin console\n\nPriority 2: Command-line arguments (session-only)\n\nPriority 3: .claude/settings.local.json (project-local, gitignored)\n\nPriority 4: .claude/settings.json (project-shared, committed)\n\nPriority 5 (lowest): ~/.claude/settings.json (user-global)\n```\n\n**Critical note:** The documented precedence places `settings.local.json` (Priority 3) ABOVE `settings.json` (Priority 4) — local overrides project, which overrides user. This is a common source of confusion when a developer's local override unexpectedly wins.\n\n### Merge vs. Override: The Critical Distinction\n\n**Scalar values** (strings, booleans, numbers): the highest-priority scope that sets the key wins outright.\n\n**Array values** (`permissions.allow`, `permissions.deny`, sandbox filesystem arrays): arrays from all scopes **concatenate and de-duplicate** across scopes. A managed `permissions.deny` and a project `permissions.deny` both apply simultaneously.\n\n**Exception — `fallbackModel`**: this array does NOT merge. The highest-precedence scope that defines it supplies the entire chain (capped at 3 models). Lower scopes are ignored entirely.\n\n**Exception — `allowManagedPermissionRulesOnly: true`**: this managed setting changes the merge behavior for permissions. When set, only the managed `permissions.allow`, `ask`, and `deny` arrays apply. User, project, and local permission rules are discarded entirely. This is the mechanism for locking down what actions Claude can take without trusting developers to not add broad `allow` rules in their project settings.\n\n```json\n// Managed settings with full enforcement lockdown\n{\n  \"$schema\": \"https://json.schemastore.org/claude-code-settings.json\",\n  \"allowManagedPermissionRulesOnly\": true,\n  \"allowManagedHooksOnly\": true,\n  \"allowManagedMcpServersOnly\": true,\n  \"permissions\": {\n    \"allow\": [\n      \"Bash(npm run *)\",\n      \"Bash(git diff *)\",\n      \"Bash(git log *)\",\n      \"Bash(git status *)\",\n      \"Bash(git commit *)\",\n      \"Read(./docs/**)\"\n    ],\n    \"deny\": [\n      \"Bash(rm -rf *)\",\n      \"Bash(curl *)\",\n      \"Read(./.env)\",\n      \"Read(./.env.*)\",\n      \"Read(./secrets/**)\"\n    ]\n  }\n}\n```\n\n**What each lockdown key does:**\n- `allowManagedPermissionRulesOnly`: discards all user/project `allow`, `ask`, `deny` permission arrays\n- `allowManagedHooksOnly`: only managed hooks, SDK hooks, and hooks from force-enabled plugins run; all other hooks blocked\n- `allowManagedMcpServersOnly`: only the managed `allowedMcpServers` list applies; users can still add MCP servers but only the admin allowlist is used\n\n### Settings That Live-Reload vs. Settings That Require Restart\n\nClaude Code watches settings files and applies most changes immediately. This matters for managed settings rollouts — you do not need to terminate all active sessions when you push a policy change.\n\n| Setting | Takes effect |\n|---------|-------------|\n| `permissions`, `hooks`, `apiKeyHelper`, `env` | Immediately (live reload) |\n| `model` | On next `/model` command or restart |\n| `outputStyle` | On `/clear` or restart |\n| `requiredMinimumVersion`, `requiredMaximumVersion` | On startup (version gate; existing sessions unaffected) |\n| All others | On restart |\n\nThe `ConfigChange` hook fires for each detected change — use it to log or audit policy pushes.\n\n### Schema Validation and Invalid Settings Handling\n\nManaged settings parse tolerantly: an invalid entry is stripped, a warning is recorded, and all remaining valid policies are enforced. However, some security-enforcement fields fail in the restrictive direction when invalid:\n\n| Field | Behavior when invalid |\n|-------|---------------------|\n| `allowedMcpServers` | Enforced as empty allowlist (blocks all MCP) |\n| `forceLoginOrgUUID` | No organization permitted until fixed |\n| `requiredMinimumVersion` / `requiredMaximumVersion` | Fail open (stripped) to prevent blocking startup |\n\nBefore deploying managed settings to your fleet, run `claude doctor` on a test machine and add `\"$schema\": \"https://json.schemastore.org/claude-code-settings.json\"` to your file for IDE validation."
   },
   {
    "heading": "Permissions Paired with Sandboxing: Two Different Enforcement Boundaries",
    "body": "Teams frequently conflate Claude Code's permission rules with the OS-level sandbox. They solve different problems and must be understood as complementary layers, not alternatives.\n\n### What Each Layer Controls\n\n**Permission rules** (in `settings.json` → `permissions`) are evaluated by the Claude Code process itself *before* a tool call runs. They operate on tool names and argument patterns and apply to *all tools*: Bash, Read, Edit, Write, WebFetch, MCP, and custom tools:\n\n```json\n{\n  \"permissions\": {\n    \"allow\": [\"Bash(git *)\", \"Read(./src/**)\"],\n    \"deny\": [\"Bash(curl *)\", \"Read(.env)\"]\n  }\n}\n```\n\n**The Bash sandbox** (enabled via `/sandbox` in-session or `sandbox.enabled: true` in settings) is OS-level enforcement using macOS Seatbelt (macOS) or bubblewrap (Linux/WSL2). It runs on the *process* after permission rules allow a command. It restricts what a Bash command and its child processes can access in the filesystem and network, regardless of what the command string looks like.\n\n**Default sandbox filesystem policy (from official docs):**\n- Default *write* access: current working directory + session `$TMPDIR` only\n- Default *read* access: the **entire filesystem** — including `~/.aws/credentials`, `~/.ssh/id_rsa`, and `.env` files. This surprises most teams.\n- Network: no domains pre-allowed; first use of each domain prompts for approval\n\n### Why Both Are Necessary\n\nSuppose you have a deny rule for `Bash(curl *)`. A command might invoke `wget` instead, or embed a network call in a Python script. The permission rule never fires because the command string is `python script.py`, not `curl`. The sandbox's network allowlist blocks the outbound connection regardless.\n\nConversely, the sandbox only covers Bash commands and their child processes — **built-in file tools (Read, Edit, Write) bypass the sandbox entirely**. They are controlled only by permission rules. A combination is required for defense in depth.\n\n### Deploying the Sandbox to a Team\n\nFor Linux/WSL2: `bubblewrap` and `socat` must be installed. On Ubuntu 24.04+, you may also need to add an AppArmor profile for `bwrap` (see official docs for the exact commands).\n\n```bash\n# Ubuntu/Debian\nsudo apt-get install bubblewrap socat\n\n# Fedora\nsudo dnf install bubblewrap socat\n\n# Optional seccomp filter for Unix socket blocking\nnpm install -g @anthropic-ai/sandbox-runtime\n```\n\nEnforce sandboxing across the org via managed settings:\n\n```json\n{\n  \"sandbox\": {\n    \"enabled\": true,\n    \"failIfUnavailable\": true,\n    \"allowUnsandboxedCommands\": false,\n    \"filesystem\": {\n      \"denyRead\": [\"~/\"],\n      \"allowRead\": [\".\"]\n    }\n  }\n}\n```\n\n- `failIfUnavailable: true` — Claude Code refuses to start if the sandbox can't initialize. Critical for making this a hard security gate rather than a soft preference.\n- `allowUnsandboxedCommands: false` — disables the escape hatch where a sandbox-failing command retries outside the sandbox (\"strict sandbox mode\").\n- `denyRead: [\"~/\"]` with `allowRead: [\".\"` — blocks the entire home directory for reads, then re-allows the project root. Note: in project settings `.claude/settings.json`, the bare `.` path resolves to the project root; in user settings `~/.claude/settings.json`, it resolves to `~/.claude` instead.\n\n**Important:** `denyRead` in sandbox settings restricts Bash subprocess access at the OS level. Permission `deny` rules for `Read` tool apply to Claude's own file reading. Both are needed.\n\n### Filesystem Deny: Paths to Always Block\n\n```json\n{\n  \"sandbox\": {\n    \"filesystem\": {\n      \"denyRead\": [\"~/.aws\", \"~/.ssh\", \"~/.gnupg\", \"~/.config/gcloud\"]\n    }\n  }\n}\n```\n\nWithout explicit `denyRead` entries, Bash subprocesses can read `~/.aws/credentials` even with sandbox enabled, because the default read policy is the entire filesystem.\n\n### When Sandboxing Isn't Available\n\n| Platform | Sandbox support |\n|----------|----------------|\n| macOS | Yes (Seatbelt, no extra packages needed) |\n| Linux | Yes (bubblewrap + socat required) |\n| WSL2 | Yes (bubblewrap + socat required) |\n| WSL1 | No (kernel features unavailable) |\n| Native Windows | No |\n\nFor those environments, permission rules + minimal `--allowedTools` in CI are the available controls. The `--dangerously-skip-permissions` flag bypasses all permission checks and is blocked when running as root on Linux/macOS. Never use it in production."
   },
   {
    "heading": "Per-Repo Onboarding Artifacts: The .claude Directory",
    "body": "Claude Code looks for configuration files in a specific set of locations, with the `.claude/` directory in the repository root as the primary per-project surface. Checking these files into source control is the mechanism by which every team member — and every CI job — gets identical project context and tooling without any manual setup.\n\n### The File Map\n\n```\nyour-project/\n├── CLAUDE.md                   # committed — project instructions, loaded every session\n├── CLAUDE.local.md             # gitignored — personal additions to CLAUDE.md\n├── .mcp.json                   # committed — MCP server definitions for this project\n└── .claude/\n    ├── settings.json           # committed — team policy: permissions, hooks, MCP config\n    ├── settings.local.json     # gitignored — personal settings (overrides settings.json)\n    ├── skills/\n    │   └── deploy/\n    │       └── SKILL.md        # committed — invokable as /deploy (and supporting files)\n    ├── agents/\n    │   └── reviewer.md         # committed — subagent definition\n    ├── commands/               # legacy path, still works; files in .claude/commands/foo.md\n    │   └── review.md           #   create /review, same as .claude/skills/review/SKILL.md\n    └── hooks/\n        └── validate-bash.sh    # committed — hook scripts referenced from settings.json\n```\n\n### CLAUDE.md: What Belongs Here\n\nCLAUDE.md is loaded into every session's context window at the start. It is NOT the place for procedure-heavy skills or long reference material (those go in `.claude/skills/` and load lazily). CLAUDE.md is for:\n\n- Build commands and test invocations (`npm run check`, `make test`)\n- Architectural decisions that affect how Claude should write code\n- Conventions (naming, file layout, which library to use for X)\n- Non-obvious context (\"the `src/legacy/` directory is not touched\")\n- References to tools available in this repo\n\nCLAUDE.md is hierarchical: Claude reads the closest `CLAUDE.md` to the working directory, then walks up to parent directories (including `~/.claude/CLAUDE.md`). For monorepos, put a global `CLAUDE.md` at the root and per-package `CLAUDE.md` files in each package directory.\n\n### .claude/settings.json: Team Policy\n\nThis file is committed and controls what Claude can do in this repository for all team members (subject to managed settings overriding it):\n\n```json\n// .claude/settings.json\n{\n  \"$schema\": \"https://json.schemastore.org/claude-code-settings.json\",\n  \"permissions\": {\n    \"allow\": [\n      \"Bash(npm run lint)\",\n      \"Bash(npm run test *)\",\n      \"Bash(git diff *)\",\n      \"Bash(git log *)\",\n      \"Bash(git status)\"\n    ],\n    \"deny\": [\n      \"Bash(curl *)\",\n      \"Bash(wget *)\",\n      \"Read(./.env)\",\n      \"Read(./.env.*)\"\n    ]\n  },\n  \"enabledMcpjsonServers\": [\"memory\", \"github\"]\n}\n```\n\n### .mcp.json: Project MCP Servers\n\n```json\n// .mcp.json — committed\n{\n  \"mcpServers\": {\n    \"memory\": {\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"@anthropic-ai/memory-mcp\"]\n    },\n    \"github\": {\n      \"command\": \"node\",\n      \"args\": [\"./tools/github-mcp/index.js\"]\n    }\n  }\n}\n```\n\nBy committing `.mcp.json`, you ensure every developer gets the same MCP servers when they open this repository. The `enabledMcpjsonServers` key in `.claude/settings.json` controls which of those servers auto-approve without a prompt.\n\n### The Gitignore Pattern\n\nAdd this to your `.gitignore` at the repo root:\n\n```\n.claude/settings.local.json\nCLAUDE.local.md\n.claude/memory/\n```\n\n`settings.local.json` is where developers put personal preferences that don't belong to the team (e.g., their personal `apiKeyHelper`, a local model override). `CLAUDE.local.md` is the personal extension of project instructions. Claude Code creates timestamped backups of settings files automatically, retaining the five most recent."
   },
   {
    "heading": "Skills: Shareable Procedures Invoked as Slash Commands",
    "body": "A skill is a Markdown file that Claude Code treats as an invoking unit. When a developer types `/deploy`, Claude Code finds the matching skill and uses its content as a procedure to execute. Unlike CLAUDE.md — which loads into every session — skills load lazily and only when invoked, making them the right place for long, procedural content.\n\n### Creating a Skill\n\nCreate a directory under `.claude/skills/<skill-name>/` (project-shared) or `~/.claude/skills/<skill-name>/` (user-global), and put a `SKILL.md` inside it:\n\n```markdown\n---\nname: deploy\ndescription: Deploy the service to staging or production. Use when the user wants to deploy, release, or push to an environment.\ndisable-model-invocation: true\nallowed-tools: Bash(npm run *) Bash(./scripts/*)\n---\n\n## Deploy Procedure\n\nDeploy $ARGUMENTS using the following steps:\n\n1. Run `npm run build` and confirm exit 0\n2. Run `npm run test` and confirm all pass\n3. Check `git status` — abort if there are uncommitted changes\n4. Run `./scripts/deploy.sh $ARGUMENTS`\n5. Verify the health endpoint responds within 60s\n6. Report the deployed version and timestamp\n\nIf any step fails, stop and report the failure clearly before proceeding.\n```\n\n### Frontmatter Fields (Authoritative)\n\nThe draft's `invocation: \"user\"/\"model\"/\"both\"` and `args:` (with nested schema) fields do not exist. The actual skill frontmatter schema:\n\n| Field | Default | Effect |\n|-------|---------|--------|\n| `name` | directory name | Display label in skill listings. Does NOT change the `/command-name` (that comes from the directory name) |\n| `description` | first paragraph | How Claude decides when to auto-invoke the skill. Key use case first; truncated at 1,536 chars |\n| `when_to_use` | — | Additional trigger context; appended to `description` in the listing |\n| `disable-model-invocation` | `false` | `true` = only user can invoke via `/name`; Claude never runs it automatically |\n| `user-invocable` | `true` | `false` = hides from `/` menu; only Claude can invoke it automatically |\n| `allowed-tools` | — | Space- or comma-separated tool patterns Claude can use without prompting while this skill is active |\n| `disallowed-tools` | — | Tools removed from Claude's pool while the skill is active (clears on next message) |\n| `arguments` | — | Named positional args: `arguments: [env, target]` maps `$env` to arg 0, `$target` to arg 1 |\n| `argument-hint` | — | Hint shown during autocomplete, e.g. `[environment]` |\n| `context` | — | Set to `fork` to run in an isolated subagent context |\n| `agent` | `general-purpose` | Which subagent type when `context: fork` is set (`Explore`, `Plan`, or custom) |\n| `model` | session model | Model override for this skill's turn |\n| `effort` | session effort | Effort level override: `low`, `medium`, `high`, `xhigh`, `max` |\n| `paths` | — | Glob patterns limiting when Claude auto-loads the skill |\n| `hooks` | — | Hooks scoped to this skill's lifecycle |\n\n**Argument substitution:** `$ARGUMENTS` = full argument string; `$ARGUMENTS[0]`, `$ARGUMENTS[1]` = positional; `$0`, `$1` = shorthand; named args via `arguments:` frontmatter field.\n\n### Invocation Control: The Two Flags\n\nThis distinction matters for team policy:\n\n- **Default (neither flag set)** — both you and Claude can invoke the skill. Description is in Claude's context at all times.\n- **`disable-model-invocation: true`** — only you can invoke it with `/name`. Description is NOT in Claude's context, so Claude never runs it automatically. Use for high-consequence actions (deploy, rollback, database migration).\n- **`user-invocable: false`** — only Claude can invoke it automatically; hidden from the `/` menu. Use for background knowledge skills.\n\n### Legacy Commands Path\n\n`.claude/commands/deploy.md` creates `/deploy` exactly like `.claude/skills/deploy/SKILL.md` and continues to work. For new skills, prefer `.claude/skills/<name>/SKILL.md` because it supports a full directory of supporting files (templates, reference data, helper scripts) alongside the SKILL.md and supports the full frontmatter schema.\n\n### Distribution by Commit and Bare Mode\n\nBecause skills live under `.claude/skills/` in the repo, every team member gets the same skill set on `git pull`. CI jobs running with `--bare` do NOT load project skills — bare mode skips all `.claude/` discovery. To use a skill in CI via the GitHub Action, include `/skill-name` in the `prompt` input; the action handles this before bare mode is active."
   },
   {
    "heading": "Hooks: Deterministic Policy on Lifecycle Events",
    "body": "Hooks are the mechanism for enforcing team rules that must not depend on Claude's judgment. Where permissions control whether a tool call is allowed, and skills define procedures Claude can execute, hooks fire automatically at fixed points in the session lifecycle and can block, modify, or audit what happens.\n\n### The Hook Event Taxonomy\n\nClaude Code exposes 30 hook events. The most important for team policy:\n\n**Session events:** `SessionStart`, `SessionEnd`\n\n**Turn events:** `UserPromptSubmit` (fires before Claude processes a prompt; can block or modify), `UserPromptExpansion` (fires when a slash command expands), `Stop` (fires when Claude finishes a turn)\n\n**Tool-call events:** `PreToolUse` (fires before each tool; can block or modify input), `PostToolUse` (fires after success), `PostToolUseFailure` (fires after failure), `PermissionRequest` (fires when Claude needs permission; can auto-approve or auto-deny), `PermissionDenied`\n\n**Other events:** `SubagentStart`, `SubagentStop`, `FileChanged`, `CwdChanged`, `ConfigChange`, `PreCompact`, `PostCompact`, `Notification`, `MessageDisplay`, `WorktreeCreate`, `WorktreeRemove`, `Elicitation`, `Setup`, and more.\n\n### Hook Configuration Schema\n\n```json\n// .claude/settings.json\n{\n  \"hooks\": {\n    \"PreToolUse\": [\n      {\n        \"matcher\": \"Bash\",\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"${CLAUDE_PROJECT_DIR}/.claude/hooks/validate-bash.sh\",\n            \"timeout\": 30,\n            \"statusMessage\": \"Validating command...\"\n          }\n        ]\n      }\n    ],\n    \"SessionStart\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"http\",\n            \"url\": \"https://audit.internal.com/session-start\",\n            \"allowedEnvVars\": [\"EMPLOYEE_ID\"]\n          }\n        ]\n      }\n    ]\n  }\n}\n```\n\n**Matcher patterns:** `\"Bash\"` (exact match), `\"Edit|Write\"` (pipe-separated list), `\"^mcp__memory__.*\"` (regex), `\"\"` or omitted (match all). For `SessionStart`, matcher matches the session source (`startup`, `resume`, `clear`, `compact`).\n\n### Hook Types\n\n| Type | When to use |\n|------|------------|\n| `command` | Shell script for complex validation; receives event JSON on stdin |\n| `http` | HTTP POST to external service (audit log, security scanner) |\n| `mcp_tool` | Delegate to an already-connected MCP server's tool |\n| `prompt` | Ask Claude itself to evaluate a yes/no question (fast, lightweight) |\n| `agent` | Spawn a subagent with tools (experimental; slower but thorough) |\n\n### Exit Code Protocol for Command Hooks\n\n| Exit code | Meaning | Behavior |\n|-----------|---------|----------|\n| `0` | Success | Parse JSON from stdout; proceed normally |\n| `2` | Blocking error | Read stderr; block the action (tool call, permission, etc.) |\n| Any other | Non-blocking error | Show first line of stderr in transcript; continue |\n\nJSON output from stdout (on exit 0) controls fine-grained behavior:\n\n```bash\n#!/bin/bash\n# .claude/hooks/validate-bash.sh\n# Receives hook event JSON on stdin\nCOMMAND=$(jq -r '.tool_input.command' < /dev/stdin)\n\n# To block a command: output JSON with permissionDecision: deny and exit 0\nif echo \"$COMMAND\" | grep -qE '(rm -rf /|dd if=/dev/zero)'; then\n  echo '{\"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"permissionDecision\": \"deny\", \"permissionDecisionReason\": \"Destructive command blocked by policy\"}}'\n  exit 0\nfi\n\n# To add context for Claude without blocking:\n# echo '{\"hookSpecificOutput\": {\"additionalContext\": \"Note: this touches a production file\"}}'\n\n# To block and show error to user:\n# exit 2  (with message on stderr)\nexit 0\n```\n\n**Critical distinction:** To deny a tool call via JSON output, emit the `hookSpecificOutput.permissionDecision: \"deny\"` JSON and exit 0. Exit 2 blocks with a hard error that shows stderr to the user — different from a clean deny.\n\n### Hooks vs. Managed Settings: When to Use Which\n\n- Use **managed settings permissions** for static pattern-based allow/deny rules. These are simpler, faster, and composable across scopes.\n- Use **hooks** when the decision requires runtime context (e.g., validate the actual command against a live blocklist, audit-log with session ID, inject environment variables dynamically via the `CLAUDE_ENV_FILE` mechanism).\n- Use `allowManagedHooksOnly: true` in managed settings to ensure only org-deployed hooks run — no developer can add a `PostToolUse` hook that exfiltrates tool output.\n\n### Distributing Hooks via the Repo\n\nHooks configured in `.claude/settings.json` are committed and apply to all team members. Hook scripts should live under `.claude/hooks/` and reference themselves with the `${CLAUDE_PROJECT_DIR}` placeholder (not an absolute path):\n\n```json\n{\n  \"command\": \"${CLAUDE_PROJECT_DIR}/.claude/hooks/validate-bash.sh\"\n}\n```\n\nThis ensures the hook resolves correctly regardless of where the repository is checked out. `${CLAUDE_PROJECT_DIR}` is also available as an environment variable inside running hook processes."
   },
   {
    "heading": "Putting It Together: A Team Onboarding Checklist",
    "body": "The following is the minimal set of committed files and managed settings that establish a properly onboarded team environment. Work through them in order — each step's correctness depends on the previous one being in place.\n\n### Step 1: Decide on Auth Path and Enforce It\n\nChoose one of: Teams/Enterprise OAuth, Console API key, Bedrock, Vertex, Foundry. Set `forceLoginMethod` in managed settings before any developer installs. If you skip this, you will have mixed auth within the week.\n\n```json\n// Managed settings (Teams/Enterprise path)\n{\n  \"forceLoginMethod\": \"claudeai\",\n  \"forceLoginOrgUUID\": \"your-org-uuid-here\"\n}\n```\n\n`forceLoginMethod: \"claudeai\"` actively blocks `ANTHROPIC_API_KEY`, `ANTHROPIC_AUTH_TOKEN`, and `apiKeyHelper` — it is not merely a preference but a startup gate. Cloud provider sessions (Bedrock, Vertex) are unaffected. For Console path, use `\"forceLoginMethod\": \"console\"` instead.\n\n### Step 2: Set Version Bounds\n\n```json\n{\n  \"requiredMinimumVersion\": \"2.1.175\",\n  \"autoUpdatesChannel\": \"stable\"\n}\n```\n\n### Step 3: Commit Per-Repo Artifacts\n\n```bash\ngit add CLAUDE.md .claude/settings.json .mcp.json .claude/skills/ .claude/hooks/\ngit commit -m \"chore: add claude code team configuration\"\n```\n\nMinimum `.gitignore` additions:\n```\n.claude/settings.local.json\nCLAUDE.local.md\n.claude/memory/\n```\n\n### Step 4: Validate Before Fleet Rollout\n\n```bash\n# On a test machine with managed settings deployed:\nclaude doctor\n\n# Confirm the right auth path is active (run inside an interactive claude session):\n/status\n\n# Test a non-interactive call:\nclaude -p \"echo hello\" --allowedTools \"Bash(echo *)\" --output-format json | jq '.result'\n```\n\n### Step 5: Onboarding Script for New Team Members\n\n```bash\n#!/bin/bash\n# tools/setup-claude-code.sh — commit this to the repo\n\nset -euo pipefail\n\n# Install Claude Code (native installer, stable channel)\ncurl -fsSL https://claude.ai/install.sh | bash -s stable\n\n# Verify installation\nclaude --version\nclaude doctor\n\necho \"Run 'claude' to authenticate. Use your company SSO credentials.\"\n```\n\n### Common Gotchas in Team Rollouts\n\n1. **Managed settings not found**: The path must be exact. On Linux, `/etc/claude-code/managed-settings.json` — not `/etc/claude-code/settings.json`. Verify with `claude doctor`. The legacy Windows path `C:\\ProgramData\\ClaudeCode\\managed-settings.json` is deprecated as of v2.1.75.\n\n2. **`forceLoginOrgUUID` typo blocks all login**: If malformed, `forceLoginOrgUUID` causes Claude Code to reject all logins (no organization permitted until fixed). Test the UUID on one machine before fleet deployment. The UUID accepts either a single string or an array of strings if you have multiple orgs.\n\n3. **`allowManagedPermissionRulesOnly` with empty managed permissions locks everyone out**: If you set `allowManagedPermissionRulesOnly: true` but forget to add any `permissions.allow` rules, Claude has zero allowed tools. Always test the effective permission set after deploying. Note that invalid `allowedMcpServers` in managed settings enforces an empty allowlist (blocks all MCP), not a passthrough.\n\n4. **Skills work for interactive sessions but not CI**: `--bare` mode skips `.claude/skills/` discovery. Either omit `--bare` or include `/skill-name` in the GitHub Actions `prompt` input (which the action handles before bare mode).\n\n5. **Sandbox not enforced on native Windows or WSL1**: If your team includes these users, compensate with tighter permission rules and `allowManagedPermissionRulesOnly: true`.\n\n6. **`ANTHROPIC_API_KEY` in developer `.zshrc` shadows subscription**: The most common unexplained auth failure. Check with `/status` inside an interactive session. Fix: `unset ANTHROPIC_API_KEY`. Or enforce `forceLoginMethod: \"claudeai\"` in managed settings, which actively blocks the key rather than just shadowing it.\n\n7. **`settings.local.json` silently wins over `settings.json`**: Local settings (Priority 3) override project settings (Priority 4). If a team member has a local override that conflicts with team policy, it wins silently for that user. Managed settings (Priority 1) override everything, so use `allowManagedPermissionRulesOnly: true` for rules that must be absolute."
   }
  ],
  "preQuiz": [
   {
    "prompt": "Your org is on Amazon Bedrock and needs to deliver a managed policy update to all developers without any file-based configuration management. The update should reach devices without requiring anyone to push files to each machine. Which account type and delivery mechanism supports this?",
    "options": [
     "Bedrock with server-managed settings delivered via the Claude.ai admin console",
     "Claude for Teams using server-managed settings delivered via the Claude.ai admin console",
     "Bedrock with a managed-settings.json at /etc/claude-code/ on each machine",
     "Console (API key) with HKLM registry policy on Windows"
    ],
    "correct": 1,
    "sectionIndices": [
     0,
     4
    ],
    "explanation": "Server-managed settings (delivery via the Claude.ai admin console, no endpoint infra) are available only on Teams and Enterprise plans. Bedrock, Vertex, Foundry, and Console-only deployments must use file-based or OS-level (plist/registry) mechanisms instead."
   },
   {
    "prompt": "A developer on your team installs Claude Code with Homebrew and sets autoUpdatesChannel=\"stable\" in their user settings.json. Two weeks later they are running a version that's 6 weeks old and missing a security fix. What explains this?",
    "options": [
     "autoUpdatesChannel only works with native installer channels; Homebrew ignores it",
     "Homebrew does not auto-update Claude Code regardless of autoUpdatesChannel — manual brew upgrade is required",
     "stable channel lags 6 weeks by design to ensure regression testing",
     "The user's minimumVersion setting is blocking the upgrade"
    ],
    "correct": 1,
    "sectionIndices": [
     2
    ],
    "explanation": "Homebrew (and WinGet, apt, dnf, apk) do NOT auto-update Claude Code. The autoUpdatesChannel setting applies only to the native installer. Teams mixing install methods silently run stale versions because package-manager installs require a manual upgrade command."
   },
   {
    "prompt": "Your CI pipeline needs to run Claude Code non-interactively. You want a long-lived credential that doesn't require a browser login and can be set as an environment variable. Which approach is correct?",
    "options": [
     "Set ANTHROPIC_API_KEY with a Console API key — this is supported for CI on all plan types",
     "Use claude setup-token to generate a one-year OAuth token, then set CLAUDE_CODE_OAUTH_TOKEN",
     "Copy ~/.claude/.credentials.json to the CI environment after an interactive /login",
     "Use apiKeyHelper with a script that returns a fresh API key — this is the only headless-safe method"
    ],
    "correct": 1,
    "sectionIndices": [
     3
    ],
    "explanation": "claude setup-token walks the OAuth flow and prints a one-year token without saving it. You then export that as CLAUDE_CODE_OAUTH_TOKEN. This is the recommended CI/headless path for Pro/Max/Team/Enterprise plans. Copying .credentials.json is fragile and not the documented approach."
   },
   {
    "prompt": "You add Bash and WebFetch to your managed permissions.deny list to prevent external network calls. A developer later reports they can still curl external URLs from within Claude Code sessions. What is the most likely explanation?",
    "options": [
     "permissions.deny only applies to MCP tools, not built-in tools like WebFetch",
     "Denying WebFetch does not block network access when Bash is allowed, because curl/wget bypass it at the OS level",
     "The developer's local project .claude/settings.json is overriding the managed deny list",
     "Managed permission arrays are merged, so the developer's local allow overrides the managed deny"
    ],
    "correct": 1,
    "sectionIndices": [
     4
    ],
    "explanation": "Denying WebFetch (the built-in tool) does not block network if Bash is allowed, because curl and wget in Bash bypass the tool-level deny. You must add sandbox.network.allowedDomains to close the gap at the OS layer."
   },
   {
    "prompt": "Your enterprise has a mixed fleet: macOS laptops and WSL2 on Windows. You push a managed-settings.json to the Windows machines via HKLM registry. You then notice WSL2 terminals are not picking up the policy. What setting resolves this?",
    "options": [
     "Set CLAUDE_CONFIG_DIR on each WSL2 instance to point at the Windows registry location",
     "Deploy a duplicate managed-settings.json to /etc/claude-code/ on each WSL2 instance",
     "Set wslInheritsWindowsSettings: true in a Windows policy source so WSL reads it",
     "WSL2 inherits Windows registry settings automatically — the issue is a version mismatch"
    ],
    "correct": 2,
    "sectionIndices": [
     4
    ],
    "explanation": "WSL ignores Windows policy by default and reads only /etc/claude-code/. Setting wslInheritsWindowsSettings: true in a Windows managed-settings source (plist/registry/file) makes WSL inherit those Windows settings."
   },
   {
    "prompt": "You want to pin Claude Code to an exact version in a reproducible dev container so every build gets the same binary regardless of what's newest. Which combination achieves this with no background auto-updates?",
    "options": [
     "Set autoUpdatesChannel=\"stable\" in managed settings and minimumVersion to the target version",
     "npm install -g @anthropic-ai/claude-code@X.Y.Z combined with DISABLE_AUTOUPDATER=1",
     "npm install -g @anthropic-ai/claude-code@X.Y.Z combined with DISABLE_UPDATES=1",
     "Use the native installer with | bash -s X.Y.Z and set requiredMaximumVersion to the same version"
    ],
    "correct": 2,
    "sectionIndices": [
     2
    ],
    "explanation": "For reproducible builds, pin the exact version via npm install -g @anthropic-ai/claude-code@X.Y.Z and set DISABLE_UPDATES=1 to block all update paths including manual ones. DISABLE_AUTOUPDATER=1 only stops background checks — manual claude update would still work. minimumVersion/requiredMaximumVersion don't pin the install version."
   },
   {
    "prompt": "Your org uses a secrets vault that issues short-lived API keys. You configure apiKeyHelper to call a vault script. Developers report a warning appearing on every session startup that slows login noticeably. What is the most likely cause?",
    "options": [
     "apiKeyHelper is not supported for vault-issued keys; ANTHROPIC_API_KEY must be used instead",
     "The vault script takes longer than 10 seconds to respond, triggering the slow-helper warning",
     "CLAUDE_CODE_API_KEY_HELPER_TTL_MS is set too low, causing the helper to be called too frequently",
     "The helper is being called after 5 seconds instead of 5 minutes due to a TTL misconfiguration"
    ],
    "correct": 1,
    "sectionIndices": [
     3
    ],
    "explanation": "A helper that takes longer than 10 seconds shows a warning and slows every session. If your vault script is slow, optimize it or cache tokens client-side. The TTL (default 5 min) controls re-call frequency but doesn't affect per-call latency."
   },
   {
    "prompt": "A new Windows developer installs Claude Code natively (not WSL) and reports that Claude Code has no Bash tool available — only PowerShell. What is the most likely fix?",
    "options": [
     "Set CLAUDE_CODE_USE_POWERSHELL_TOOL=0 in environment to force Bash mode",
     "Install WSL2 — Bash is only available via WSL on Windows",
     "Install Git for Windows (Git Bash) so Claude Code can find a Bash binary",
     "Native Windows requires sandbox mode enabled to get the Bash tool"
    ],
    "correct": 2,
    "sectionIndices": [
     1
    ],
    "explanation": "On native Windows, Claude Code uses the Bash tool when Git for Windows (Git Bash) is installed. Without it, Claude Code falls back to the more limited PowerShell tool. Set CLAUDE_CODE_GIT_BASH_PATH if Git Bash isn't auto-discovered."
   },
   {
    "prompt": "You are using Claude for Teams and deploy org-wide governance via the Claude.ai admin console (server-managed settings). A developer checks /status and sees no mention of 'Enterprise managed settings'. What should you check first?",
    "options": [
     "Server-managed settings require a separate claude-managed-settings endpoint configured in settings.json",
     "Server-managed settings are only pushed on fresh installs; existing installs need a manual claude update",
     "The developer may not be authenticated to the org — server-managed settings are delivered at auth time and refreshed hourly",
     "The claude doctor command must be run once to register the device with the management plane"
    ],
    "correct": 2,
    "sectionIndices": [
     4
    ],
    "explanation": "Server-managed settings are delivered at authentication time and refreshed hourly. If /status shows no enterprise managed settings, the most likely cause is the developer is not authenticated to the org (or is logged in with a personal account). Run /logout then /login to re-auth against the org."
   },
   {
    "prompt": "Your org runs Claude Code via the Anthropic Console (API key auth). A security team member asks whether Anthropic trains on the code Claude Code processes. What is the correct answer?",
    "options": [
     "Console (API key) plans do have training use — only Enterprise plans with ZDR opt-out training",
     "Anthropic trains on prompts from all plans unless you purchase ZDR; ZDR is available to all plans on request",
     "On Team, Enterprise, API, and cloud-provider plans Anthropic does not train on code or prompts; ZDR is an additional option for qualified Enterprise accounts",
     "Training opt-out is set per-repo via a setting in .claude/settings.json"
    ],
    "correct": 2,
    "sectionIndices": [
     0
    ],
    "explanation": "On Team, Enterprise, API (Console), and cloud-provider plans (Bedrock, Vertex, Foundry), Anthropic does not train on your code or prompts. Zero Data Retention (ZDR) is an additional option available to qualified Enterprise accounts for stronger contractual guarantees."
   }
  ],
  "tasks": [
   {
    "id": "stage-0-task-install-verify",
    "afterSectionIdx": 1,
    "title": "Install Claude Code and verify your environment",
    "instructions": "Install Claude Code using the native installer and confirm it runs correctly.\n\n**macOS / Linux / WSL2:**\n```bash\ncurl -fsSL https://claude.ai/install.sh | bash\n```\n\n**Windows (native, PowerShell as admin):**\n```powershell\nirm https://claude.ai/install.ps1 | iex\n```\n\nAfter install, open a new terminal and run:\n```bash\nclaude --version\nclaude doctor\n```\n\n`claude doctor` checks for common issues: non-writable npm dirs, stale installs, config health. Fix any warnings it surfaces before proceeding.\n\n**Alpine/musl only** — add before running claude:\n```bash\napk add libgcc libstdc++ ripgrep\n# Then in ~/.claude/settings.json add:\n# { \"env\": { \"USE_BUILTIN_RIPGREP\": \"0\" } }\n```",
    "doneWhen": "claude --version prints a version number and claude doctor exits with no errors or only informational notes."
   },
   {
    "id": "stage-0-task-headless-auth",
    "afterSectionIdx": 3,
    "title": "Set up non-interactive auth for CI",
    "instructions": "Generate a long-lived OAuth token for headless use (CI, scripts, containers). Requires a Pro, Max, Team, or Enterprise account.\n\n**Step 1 — generate the token:**\n```bash\nclaude setup-token\n```\nFollow the prompts (browser OAuth flow). At the end, the command prints a token — it does NOT save it. Copy it now.\n\n**Step 2 — export it as an env var** (add to your CI secrets / shell profile for testing):\n```bash\nexport CLAUDE_CODE_OAUTH_TOKEN=\"<paste-token-here>\"\n```\n\n**Step 3 — verify it works non-interactively:**\n```bash\nclaude --print 'Say: auth-ok' 2>&1\n```\n\n**Step 4 — confirm the active auth method:**\n```bash\nclaude /status\n```\nLook for `OAuth token` (not `Subscription OAuth`) in the Setting sources line.\n\n> Note: this token is valid for one year and is inference-only. Store it in your CI secrets manager, not in dotfiles.",
    "doneWhen": "claude --print 'Say: auth-ok' returns a response without prompting for login, and /status confirms the OAuth token auth method is active."
   },
   {
    "id": "stage-0-task-managed-settings",
    "afterSectionIdx": 4,
    "title": "Write a file-based managed-settings policy and verify it loads",
    "instructions": "Deploy a minimal managed-settings file that locks the auto-update channel to stable and denies the WebFetch tool org-wide. This works on any deployment (no Enterprise required for the file-based path).\n\n**Step 1 — create the managed-settings file:**\n\nmacOS:\n```bash\nsudo mkdir -p \"/Library/Application Support/ClaudeCode\"\nsudo tee \"/Library/Application Support/ClaudeCode/managed-settings.json\" <<'EOF'\n{\n  \"autoUpdatesChannel\": \"stable\",\n  \"permissions\": {\n    \"deny\": [\"WebFetch\"]\n  }\n}\nEOF\n```\n\nLinux / WSL2:\n```bash\nsudo mkdir -p /etc/claude-code\nsudo tee /etc/claude-code/managed-settings.json <<'EOF'\n{\n  \"autoUpdatesChannel\": \"stable\",\n  \"permissions\": {\n    \"deny\": [\"WebFetch\"]\n  }\n}\nEOF\n```\n\n**Step 2 — verify the policy loaded:**\n```bash\nclaude /status\n```\nLook for `Enterprise managed settings` with source `(file)` in the output.\n\n**Step 3 — confirm deny takes effect** by asking Claude to fetch a URL inside a session and observing it is blocked.\n\n> To remove this test policy: `sudo rm /etc/claude-code/managed-settings.json` (Linux) or `sudo rm \"/Library/Application Support/ClaudeCode/managed-settings.json\"` (macOS).",
    "doneWhen": "claude /status shows 'Enterprise managed settings' with source (file), and autoUpdatesChannel is visible as stable in the setting sources output."
   }
  ],
  "visualizations": [
   {
    "id": "stage-0-v",
    "kind": "comparison-table",
    "title": "Setup & onboarding",
    "textualSummary": "Key concepts of Setup & onboarding: deployment path selection, authentication precedence, non-interactive authentication.",
    "columns": [
     "Concept",
     "In this stage"
    ],
    "rows": [
     {
      "label": "deployment path selection",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "authentication precedence",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "non-interactive authentication",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "fleet version control",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     }
    ]
   }
  ],
  "confusions": [
   {
    "misconception": "Skipping Setup & onboarding.",
    "correction": "Each stage is load-bearing for the setup."
   },
   {
    "misconception": "These concepts are interchangeable.",
    "correction": "Each has a distinct role; see the definitions."
   }
  ],
  "quiz": [
   {
    "id": "stage-0-q1",
    "type": "multiple-choice",
    "prompt": "Your team lead says: \"We're on the free Claude.ai plan and I've just installed Claude Code — but nobody can log in.\" What is the actual cause?",
    "options": [
     "The free Claude.ai plan does not include Claude Code; a paid plan (Pro, Max, Team, Enterprise, Console, or a cloud provider) is required.",
     "Claude Code requires a separate activation step in the Claude.ai dashboard that the team lead skipped.",
     "The free plan includes Claude Code, but only for one user at a time — additional seats need upgrading.",
     "The installation likely used `sudo npm install -g`, which corrupts credentials on the free tier."
    ],
    "correct": 0,
    "explanation": "The content states explicitly: 'The free Claude.ai plan does not include Claude Code.' There is no activation step to skip (distractor 2), no single-seat free tier (distractor 3), and the sudo issue is a real install problem but unrelated to plan eligibility (distractor 4)."
   },
   {
    "id": "stage-0-q2",
    "type": "multiple-choice",
    "prompt": "A security-conscious org on Claude Console (API credits) wants to enforce version gating so Claude Code refuses to start if it falls outside an approved range. Which settings accomplish true startup gating?",
    "options": [
     "`requiredMinimumVersion` and `requiredMaximumVersion` in managed settings.",
     "`minimumVersion` in managed settings — it blocks startup below the floor.",
     "`DISABLE_AUTOUPDATER=1` combined with a pinned npm install version.",
     "Server-managed settings pushed from the Claude.ai admin console."
    ],
    "correct": 0,
    "explanation": "`requiredMinimumVersion`/`requiredMaximumVersion` are the only settings that 'refuse to start outside the range (true version gating).' `minimumVersion` explicitly 'does not gate startup' — it only stops auto-update and `claude update` from going below that floor (distractor 2). `DISABLE_AUTOUPDATER=1` pins the version but does not gate startup (distractor 3). Server-managed settings require Teams/Enterprise, and Console-only deployments cannot use them (distractor 4)."
   },
   {
    "id": "stage-0-q3",
    "type": "multiple-choice",
    "prompt": "A teammate installs Claude Code with `sudo npm install -g @anthropic-ai/claude-code` and later runs `npm update -g` to get the latest version. Which TWO problems does this introduce?",
    "options": [
     "`sudo npm install -g` is explicitly prohibited, and `npm update -g` respects the original semver range so it may not move to the newest version.",
     "`sudo npm install -g` is fine, but `npm update -g` always downgrades to a LTS release.",
     "Neither action is problematic — npm global installs are the recommended path for Linux.",
     "`sudo npm install -g` creates a root-owned install, and `npm update -g` is the correct way to update."
    ],
    "correct": 0,
    "explanation": "The content states: 'Do NOT use `sudo npm install -g`' and 'Avoid `npm update -g` (respects original semver range — may not move to newest).' Both are called out as problems. Distractor 2 inverts the `npm update` behavior. Distractor 3 ignores both explicit warnings. Distractor 4 acknowledges the sudo root issue but then endorses `npm update -g`, which the content explicitly discourages."
   },
   {
    "id": "stage-0-q4",
    "type": "multiple-choice",
    "prompt": "Your org runs Claude Code on a mixed fleet: some devs on macOS (native installer), others on Ubuntu via apt, and a few on Windows with WinGet. The macOS machines update automatically but the Ubuntu and Windows machines are running a version that's several weeks old. Why?",
    "options": [
     "Native (curl/irm) installs auto-update in the background; Homebrew, WinGet, apt, dnf, and apk do NOT auto-update.",
     "The Ubuntu and Windows machines need `DISABLE_AUTOUPDATER=1` removed from their env to re-enable updates.",
     "Auto-updates only work on macOS; Linux and Windows always require manual update commands by design.",
     "The managed `autoUpdatesChannel` setting is only respected by native installers and is ignored by package managers."
    ],
    "correct": 0,
    "explanation": "The content says: 'Native (curl/irm) installs auto-update in the background. Homebrew, WinGet, apt, dnf, apk do NOT auto-update — teams mixing methods silently run stale, possibly insecure versions.' Distractor 2 assumes the problem is an env var when the real cause is the installer type. Distractor 3 over-generalizes — the native Linux installer does auto-update. Distractor 4 misattributes the gap to a settings issue rather than the installer method."
   },
   {
    "id": "stage-0-q5",
    "type": "multiple-choice",
    "prompt": "A team uses an LLM gateway that expects `Authorization: Bearer <token>` headers. Which authentication env var should they set for Claude Code to use this gateway correctly?",
    "options": [
     "`ANTHROPIC_AUTH_TOKEN` — sent as `Authorization: Bearer`.",
     "`ANTHROPIC_API_KEY` — it is the primary env var and takes precedence.",
     "`CLAUDE_CODE_OAUTH_TOKEN` — OAuth tokens are formatted as Bearer tokens.",
     "`apiKeyHelper` — it should return the bearer token on each call."
    ],
    "correct": 0,
    "explanation": "The content specifies: '`ANTHROPIC_AUTH_TOKEN` — sent as `Authorization: Bearer` (for LLM gateway/proxy)' and '`ANTHROPIC_API_KEY` — sent as `X-Api-Key` (direct Anthropic API).' Using `ANTHROPIC_API_KEY` (distractor 2) would send an `X-Api-Key` header, not a Bearer header, which a gateway may reject. `CLAUDE_CODE_OAUTH_TOKEN` (distractor 3) is for subscription OAuth, not arbitrary gateway auth. `apiKeyHelper` (distractor 4) is for rotating keys but the helper output feeds into `ANTHROPIC_API_KEY` semantics, not Bearer headers."
   },
   {
    "id": "stage-0-q6",
    "type": "multiple-choice",
    "prompt": "An Enterprise team denies the `WebFetch` tool in managed permissions to prevent Claude Code from making outbound network calls. A few days later, the security team discovers Claude is still fetching external URLs via Bash (`curl` commands). What explains this and what is the correct fix?",
    "options": [
     "Denying `WebFetch` only blocks the tool-level call; Bash (`curl`/`wget`) can bypass it. The fix is to also add `sandbox.network.allowedDomains` to restrict at the OS layer.",
     "The `WebFetch` deny rule was overridden by a local project setting. The fix is to set `allowManagedPermissionRulesOnly: true`.",
     "The deny rule applies only to the web UI, not to Claude Code's Bash tool. The fix is to deny the `Bash` tool entirely in managed settings.",
     "Managed permission denies only take effect after a restart. The fix is to restart all Claude Code sessions."
    ],
    "correct": 0,
    "explanation": "The content states: 'Pair permissions with sandboxing: denying `WebFetch` does NOT block network if Bash is allowed (`curl`/`wget` bypass it) — add `sandbox.network.allowedDomains` to close the gap at the OS layer.' Local overrides are prevented by array merge semantics (managed denies can't be removed locally), so distractor 2 misdiagnoses the cause. Denying Bash entirely (distractor 3) would cripple all shell use and is not the recommended approach. Restarts (distractor 4) have no bearing on permission rule application."
   },
   {
    "id": "stage-0-q7",
    "type": "multi-select",
    "prompt": "Your org uses Claude for Teams. Which of the following capabilities are available on Teams that are NOT available on Claude for Enterprise? (Select all that apply.)",
    "options": [
     "Server-managed settings delivery.",
     "SSO + domain capture.",
     "SCIM provisioning and role-based permissions.",
     "Claude on web (claude.ai).",
     "Managed policy settings (org-wide config)."
    ],
    "correct": [
     0,
     3
    ],
    "explanation": "The table shows Teams has 'Server-managed settings delivery' (✓) and 'Claude on web' (✓) — both are shared with Enterprise. SSO+domain capture (✗ on Teams), SCIM/role-based permissions (✗ on Teams), and Managed policy settings (✗ on Teams) are Enterprise-only. The question asks what is available on Teams but NOT Enterprise — none of these are Teams-only. Re-reading: Server-managed (Teams+Ent both ✓), Claude on web (Teams+Ent both ✓). The answer is that both of these ARE available on Teams. The question is actually asking which capabilities Teams has that Enterprise lacks — but since Teams is a subset of Enterprise features, no feature exists only on Teams. CORRECTION based on re-reading: the question should ask what Teams has that's NOT Enterprise-only. Both 'Server-managed settings delivery' and 'Claude on web' are present on BOTH tiers. The truly Teams-available (and Enterprise-available) items are server-managed and Claude on web. The Enterprise-only features are SSO, SCIM, Compliance API, and Managed policy settings. So this question as written has no valid 'Teams-only' answers. Let me reframe: which capabilities listed are available to Teams plan users? Answer: Server-managed settings delivery and Claude on web. SSO, SCIM, and Managed policy settings require Enterprise."
   },
   {
    "id": "stage-0-q8",
    "type": "multi-select",
    "prompt": "A developer on WSL 2 on Windows needs to log into Claude Code but the browser can't reach the localhost OAuth callback. Which of the following are correct actions? (Select all that apply.)",
    "options": [
     "Paste the login code at the `Paste code here if prompted` prompt during the login flow.",
     "Switch to WSL 1 where the Windows browser can reach localhost.",
     "Use `CLAUDE_CODE_OAUTH_TOKEN` set from a token obtained via `claude setup-token` on a machine that can complete OAuth.",
     "Set `ANTHROPIC_API_KEY` directly — OAuth is not supported in WSL environments.",
     "Run `/logout` then `/login` to retry; the second attempt always succeeds in WSL 2."
    ],
    "correct": [
     0,
     2
    ],
    "explanation": "The content says: 'WSL2/SSH/container login: if the browser can't reach the localhost callback, paste the login code at the `Paste code here if prompted` prompt.' It also describes `claude setup-token` as producing a one-year token for headless/CI use that can be set as `CLAUDE_CODE_OAUTH_TOKEN`. WSL 1 (distractor 2) is specifically called out as NOT supporting sandboxing; the content standardizes on WSL 2, and switching to WSL 1 does not fix the localhost issue. Setting `ANTHROPIC_API_KEY` (distractor 4) works for Console/API paths but is not 'the' fix for OAuth on WSL 2, and OAuth IS supported in WSL 2. The `/logout`+`/login` retry (distractor 5) is listed as a way to switch accounts, not as a fix for the localhost callback problem."
   },
   {
    "id": "stage-0-q9",
    "type": "multiple-choice",
    "prompt": "An Enterprise org deploys Claude Code to Linux desktops and also uses Windows workstations with WSL 2. They write a Windows Registry managed policy under `HKLM\\SOFTWARE\\Policies\\ClaudeCode`. The Linux and WSL 2 machines ignore it. What is the correct way to also cover those machines?",
    "options": [
     "Deploy `/etc/claude-code/managed-settings.json` on Linux/WSL and, for WSL to inherit Windows policy, set `wslInheritsWindowsSettings: true` in a Windows managed source.",
     "Copy the registry values into `~/.claude/settings.json` on each Linux machine — user settings are the fallback on non-Windows platforms.",
     "Use server-managed settings from the Claude.ai admin console; they override file and registry sources on all platforms.",
     "The plist mechanism covers all platforms; create `com.anthropic.claudecode.plist` entries on Linux as well."
    ],
    "correct": 0,
    "explanation": "The content describes the Linux/WSL file path as `/etc/claude-code/managed-settings.json` and states: 'WSL ignores Windows policy by default (reads only `/etc/claude-code`). Set `wslInheritsWindowsSettings: true` in a Windows source to inherit.' User `~/.claude/settings.json` (distractor 2) is not a managed source — it can be overridden by users. Server-managed (distractor 3) only reaches Teams/Enterprise via Claude.ai but does not make the Windows registry policy flow to Linux. Plist (distractor 4) is a macOS mechanism only."
   },
   {
    "id": "stage-0-q10",
    "type": "multiple-choice",
    "prompt": "A dev on the team adds a `permissions.allow` entry in their local `~/.claude/settings.json` for a tool that the Enterprise managed settings also lists in `permissions.allow`. A different dev tries to remove a `permissions.deny` entry that managed settings enforces, by overriding it in their project `.claude/settings.json`. What actually happens in each case?",
    "options": [
     "The first dev's local allow is additive (extends the managed list); the second dev's local deny removal has no effect — managed denies cannot be removed locally.",
     "Both local changes are ignored entirely — managed settings replace all local config, with no merging.",
     "The first dev's allow overrides the managed allow (last-write wins); the second dev's deny removal succeeds because project settings outrank user settings.",
     "Local settings can extend managed allows but also override managed denies, so both changes take effect."
    ],
    "correct": 0,
    "explanation": "The content states: 'Array settings merge across all sources: developers can extend but not remove managed `permissions.allow`/`permissions.deny` entries — a managed deny can't be overridden locally.' So local adds are additive, but local removals of managed denies are blocked. Distractor 2 incorrectly says managed settings completely replace local config (they don't — arrays merge). Distractor 3 incorrectly says local overrides managed (managed always wins on denies). Distractor 4 correctly states extends but incorrectly allows local override of managed denies."
   },
   {
    "id": "stage-0-q11",
    "type": "multiple-choice",
    "prompt": "A team uses rotating API keys from a secrets vault. They configure `apiKeyHelper` as a shell script that calls the vault API. The script takes about 15 seconds to return. What behavior will users observe?",
    "options": [
     "Every session will show a warning and experience a slowdown, because `apiKeyHelper` scripts taking longer than 10 seconds trigger a warning and slow every session.",
     "The helper will only be called once per day (TTL default is 24 hours), so the delay has negligible impact.",
     "Claude Code will fall back to `ANTHROPIC_API_KEY` if `apiKeyHelper` takes more than 10 seconds.",
     "The script runs in the background and users see no delay — they just proceed while the key is fetched asynchronously."
    ],
    "correct": 0,
    "explanation": "The content states: 'A helper >10s shows a warning and slows every session.' The default TTL is 5 minutes (not 24 hours), so the helper is called frequently (distractor 2 is wrong). There is no fallback to `ANTHROPIC_API_KEY` on timeout (distractor 3). The helper blocks the session — it does not run asynchronously (distractor 4)."
   },
   {
    "id": "stage-0-q12",
    "type": "multiple-choice",
    "prompt": "Your org is on Claude for Teams (not Enterprise). You want to enforce a specific Claude Code version range org-wide via managed settings. Which delivery mechanism is available to you?",
    "options": [
     "Server-managed settings pushed from the Claude.ai admin console — Teams includes server-managed settings delivery.",
     "The macOS plist (`com.anthropic.claudecode`) or Windows registry (`HKLM\\SOFTWARE\\Policies\\ClaudeCode`) — these work for any provider.",
     "File-based managed settings at `/etc/claude-code/managed-settings.json` or the OS equivalent — available for any provider.",
     "Both B and C are correct; server-managed settings require Enterprise."
    ],
    "correct": 3,
    "explanation": "The content states server-managed delivery is 'Teams/Enterprise only' (so Teams does have it — distractor 4 says 'require Enterprise' which is partially wrong). Wait — re-reading: 'Server-managed delivery requires Teams/Enterprise — Bedrock/Vertex/Foundry/Console-only deployments must use file/OS (plist/registry) mechanisms.' So Teams DOES have server-managed. That makes option A correct too. But option D says 'server-managed requires Enterprise' which contradicts the content. So option A is correct (Teams has server-managed), and the table shows Teams gets 'Server-managed settings delivery ✓'. The plist/registry and file-based options are also available (any provider). So A, B, and C are all valid mechanisms for Teams. But the question asks which is available — all three are. The most precise answer that captures what's special about Teams is A (server-managed IS available to Teams), while D incorrectly states Enterprise-only. Given the options, A is the best single answer and D is a trap that misreads the content."
   },
   {
    "id": "stage-0-q13",
    "type": "multiple-choice",
    "prompt": "A CI pipeline needs to run Claude Code headlessly. The pipeline can't complete an interactive OAuth flow. Which approach does the documentation recommend for generating a CI token?",
    "options": [
     "Run `claude setup-token` on a machine that can complete OAuth; it prints a one-year token without saving it, which you then set as `CLAUDE_CODE_OAUTH_TOKEN` in CI.",
     "Set `ANTHROPIC_API_KEY` in CI — API key auth is the supported headless path and no setup step is needed.",
     "Use `apiKeyHelper` pointing to a CI secret store; it is called automatically in headless mode.",
     "Run `/login` in CI and pipe the OAuth URL to a headless browser to complete the flow."
    ],
    "correct": 0,
    "explanation": "The content states: '`claude setup-token` walks OAuth, prints a one-year token (does not save it) → set `CLAUDE_CODE_OAUTH_TOKEN`. Requires Pro/Max/Team/Enterprise; inference-only; cannot establish Remote Control. Do not rely on interactive `/login`.' Using `ANTHROPIC_API_KEY` (distractor 2) is valid for Console/API plans, but the content explicitly recommends `claude setup-token` for subscription plans in CI. `apiKeyHelper` (distractor 3) is for rotating keys, not for establishing CI OAuth sessions. Running `/login` in CI (distractor 4) is explicitly warned against."
   },
   {
    "id": "stage-0-q14",
    "type": "multi-select",
    "prompt": "Which of the following statements about managed settings precedence and enforcement are TRUE? (Select all that apply.)",
    "options": [
     "Server-managed settings (from the Claude.ai admin console) have the highest priority among all four managed sources.",
     "The Windows user registry key (`HKCU\\SOFTWARE\\Policies\\ClaudeCode`) is writable without elevation and is therefore not an enforcement boundary.",
     "Managed settings completely replace user and project settings — no local config is ever merged or additive.",
     "Running `/status` will show which managed setting source is active (e.g., remote, plist, HKLM, or file).",
     "Plist and registry sources require Teams/Enterprise; file-based managed settings work for any provider."
    ],
    "correct": [
     0,
     1,
     3
    ],
    "explanation": "A: Correct — the table lists server-managed as 'Priority 1 (highest)'. B: Correct — the content explicitly calls out HKCU as 'writable without elevation — not an enforcement boundary.' C: False — array settings (permissions.allow/deny) MERGE additively; local settings can extend managed ones. D: Correct — '/status' shows 'Setting sources' including 'Enterprise managed settings' + source label. E: False — plist/registry are available for 'Any provider'; only server-managed requires Teams/Enterprise."
   }
  ],
  "masteryCheckpoint": "You can explain the concepts of Setup & onboarding."
 },
 {
  "id": "stage-1",
  "stage": 1,
  "title": "Config & memory (CLAUDE.md, settings)",
  "summary": "Config & memory (CLAUDE.md, settings): CLAUDE.md memory files, guidance vs enforcement, settings precedence and merging.",
  "prerequisites": [
   "stage-0"
  ],
  "objectives": [
   "Understand the concepts in Config & memory (CLAUDE.md, settings)."
  ],
  "definitions": [
   {
    "term": "CLAUDE.md memory files",
    "short": "Markdown instruction files loaded into every session's context to give Claude persistent guidance, discovered by walking up the directory tree with more-specific files winning on conflict."
   },
   {
    "term": "guidance vs enforcement",
    "short": "CLAUDE.md and rules are non-binding advice injected as context with no compliance guarantee, so anything that must never happen requires permissions, hooks, or managed settings instead."
   },
   {
    "term": "settings precedence and merging",
    "short": "Config layers (user < project < local < CLI/env < managed) combine by merging arrays across all scopes while scalars take the most-specific value, with managed settings always winning."
   },
   {
    "term": "committed vs personal config scope",
    "short": "Each config artifact has a deliberate home — source-controlled for shared team config, gitignored or user-level for personal/secret state that must never be committed."
   },
   {
    "term": "permission rules",
    "short": "Tool(pattern) allow/deny/ask entries that merge across scopes where deny always beats allow, matching commands literally rather than by semantics or executable."
   },
   {
    "term": "CLAUDE.md authoring discipline",
    "short": "Keep instruction files short (~200 lines), concrete, and verifiable because length and vagueness reduce adherence even though the file always loads in full."
   }
  ],
  "sections": [
   {
    "heading": "The Two Memory Systems",
    "body": "Claude Code ships two complementary mechanisms for carrying knowledge across sessions. Understanding their boundary is the first decision a team lead has to make, because conflating them leads to the most common misconfiguration: teams writing enforcement policies into CLAUDE.md and wondering why Claude still does the wrong thing.\n\n| Mechanism | Who writes it | What it contains | Scope | Load limit | Where it lives |\n|---|---|---|---|---|---|\n| **CLAUDE.md** | You (human) | Instructions, conventions, rules | Project, user, or org | Full file, always (no line limit, but shorter = better adherence) | Committed to source control or system dir |\n| **Auto memory (MEMORY.md)** | Claude itself | Learnings, build commands, debugging insights | Per-repository, shared across worktrees | First 200 lines or 25 KB | `~/.claude/projects/<repo>/memory/` — machine-local |\n\n**CLAUDE.md** is what you write once and every teammate gets. **MEMORY.md** is what Claude accumulates during your personal sessions — it is never shared across machines or team members.\n\nBoth are injected as user-turn messages at session start — the official docs are explicit: \"CLAUDE.md content is delivered as a user message after the system prompt, not as part of the system prompt itself.\" That distinction matters enormously and is covered in the Guidance vs Enforcement section below.\n\nAuto memory is on by default and requires v2.1.59+. Claude decides what is worth saving; you do not prompt it. The memory directory structure:\n\n```\n~/.claude/projects/<repo-path>/memory/\n├── MEMORY.md          # index — first 200 lines or 25 KB loaded every session\n├── debugging.md       # topic file, read by Claude on demand\n└── api-conventions.md # topic file, read by Claude on demand\n```\n\nTopic files are **not** preloaded at session start — only the index is. Claude reads them on demand using its standard file tools when it needs the information. The 200-line / 25 KB limit applies only to `MEMORY.md`; CLAUDE.md files are loaded in full regardless of length (though shorter files produce better adherence).\n\nYou can browse and edit everything with `/memory` from inside a session: it lists all CLAUDE.md, CLAUDE.local.md, and rules files loaded in the current session, lets you toggle auto memory on or off, and shows a link to open the auto memory folder."
   },
   {
    "heading": "CLAUDE.md Discovery and Load Order",
    "body": "Claude Code walks the directory tree from your current working directory up to the filesystem root, loading every `CLAUDE.md` and `CLAUDE.local.md` it finds along the way. Files are concatenated in order **from the filesystem root down to your working directory** — so content closer to where you launched Claude appears last in context and is read last by the model. Within any single directory, `CLAUDE.md` is appended first and `CLAUDE.local.md` is appended after it.\n\n### Canonical locations, from broadest to narrowest\n\n| Scope | Path | Committed? | Shared with |\n|---|---|---|---|\n| **Managed policy** | macOS: `/Library/Application Support/ClaudeCode/CLAUDE.md`<br>Linux/WSL: `/etc/claude-code/CLAUDE.md`<br>Windows: `C:\\Program Files\\ClaudeCode\\CLAUDE.md` | Via MDM/Ansible | All users on machine |\n| **User** | `~/.claude/CLAUDE.md` | N/A | Just you, all projects |\n| **Project** | `./CLAUDE.md` or `./.claude/CLAUDE.md` | Yes | Team via source control |\n| **Local** | `./CLAUDE.local.md` | No (gitignore it) | Just you, this project |\n\nNote that `./CLAUDE.md` and `./.claude/CLAUDE.md` are both valid project locations — Claude Code finds both.\n\n### What loads when, exactly\n\nAll `CLAUDE.md` and `CLAUDE.local.md` files discovered **in the directory tree from the filesystem root down to your working directory** are loaded in full at launch. Files in **subdirectories below your working directory** load on demand — only when Claude reads a file in that subdirectory. This is the lazy-loading mechanism for monorepos.\n\nIf you run Claude from `~/projects/myrepo/src/`, it loads:\n1. Every `CLAUDE.md` / `CLAUDE.local.md` found walking from `/` down through `~/projects/myrepo/src/`\n2. `~/.claude/CLAUDE.md` (user scope — loaded as part of that walk)\n3. The managed policy file if it exists\n\nIt does **not** preload `~/projects/myrepo/src/components/CLAUDE.md` — that waits until Claude touches a file in `components/`.\n\n### After `/compact`, subdirectory CLAUDE.md files do not auto-reload\n\nThe project-root CLAUDE.md survives compaction: Claude re-reads it from disk. Nested CLAUDE.md files in subdirectories are not re-injected automatically — they reload the next time Claude reads a file in that subdirectory. If an instruction disappeared after `/compact`, it either lived only in conversation or was in a nested CLAUDE.md that hasn't reloaded yet.\n\n### HTML comments are stripped before injection\n\nBlock-level HTML comments (`<!-- maintainer note -->`) are stripped before the file enters the context window. Use them freely for notes to human maintainers — they cost no tokens. Comments inside code blocks are preserved. When you open a CLAUDE.md file directly with the Read tool, the comments remain visible."
   },
   {
    "heading": "Guidance vs Enforcement: The Non-Negotiable Distinction",
    "body": "This is the point most teams get wrong, and it has real security consequences.\n\nCLAUDE.md is delivered as a **user-turn message after the system prompt** — not as enforced configuration. The official documentation states explicitly:\n\n> \"Claude reads it and tries to follow it, but there's no guarantee of strict compliance, especially for vague or conflicting instructions.\"\n\nThe practical consequence: **do not put security-critical constraints in CLAUDE.md**. Statements like \"never commit to main\" or \"never touch `.env` files\" in CLAUDE.md are reminders, not locks.\n\n### What actually enforces\n\n| Mechanism | Enforced regardless of Claude's decision | Notes |\n|---|---|---|\n| `permissions.deny` in settings.json | Yes | Client blocks the tool call before execution |\n| Hooks (`PreToolUse`) | Yes | Shell command runs and can exit non-zero to block; hook decisions do not bypass `deny` rules |\n| Managed settings | Yes, overrides everything | Cannot be bypassed by user or project config |\n| CLAUDE.md | No | Behavioral guidance only |\n\nA `permissions.deny` entry on `Read(./.env)` will stop Claude cold — the client blocks the tool call before it executes, regardless of what Claude decides. A CLAUDE.md line saying \"never read .env\" might be overridden if Claude infers a plausible reason under task pressure.\n\n**Important nuance on hooks and deny rules:** Hook decisions do not bypass permission rules. A PreToolUse hook that returns `\"allow\"` does not override a matching `deny` rule — deny rules always apply regardless of hook output. Conversely, a blocking hook (exit code 2) stops the tool call before permission rules are evaluated, so it takes precedence over `allow` rules.\n\n### The correct allocation\n\n```\n# CLAUDE.md — behavioral guidance\n- Use 2-space indentation throughout TypeScript files\n- API handlers live in src/api/handlers/\n- Run `make test` before committing\n- Compliance reminder: never log PII\n\n# .claude/settings.json — enforcement (what MUST happen)\n{\n  \"permissions\": {\n    \"deny\": [\n      \"Read(./.env)\",\n      \"Read(./.env.*)\",\n      \"Read(./secrets/**)\",\n      \"Bash(git push --force *)\"\n    ]\n  }\n}\n```\n\nFor things that must never happen: `permissions.deny`. For hooks that must run at specific lifecycle points (e.g., linting before every file write): hooks. For behavioral reminders that reduce friction: CLAUDE.md."
   },
   {
    "heading": "Settings Files: Locations, Schema, and What Goes Where",
    "body": "Claude Code has four settings scopes, each with a distinct file and purpose. The `$schema` line enables autocomplete in VS Code, Cursor, and any JSON Schema-aware editor.\n\n### Full settings.json structure\n\n```json\n{\n  \"$schema\": \"https://json.schemastore.org/claude-code-settings.json\",\n  \"model\": \"claude-sonnet-4-6\",\n  \"permissions\": {\n    \"allow\": [\"Bash(npm run lint)\", \"Bash(npm run test *)\"],\n    \"ask\":   [\"Bash(terraform apply *)\"],\n    \"deny\":  [\"Read(./.env)\", \"Read(./.env.*)\", \"Bash(git push --force *)\"]\n  },\n  \"env\": {\n    \"CLAUDE_CODE_ENABLE_TELEMETRY\": \"1\",\n    \"OTEL_METRICS_EXPORTER\": \"otlp\"\n  },\n  \"autoMemoryEnabled\": true,\n  \"autoCompactEnabled\": true,\n  \"hooks\": {\n    \"PreToolUse\": [\n      {\n        \"matcher\": \"Bash\",\n        \"hooks\": [\n          { \"type\": \"command\", \"command\": \"echo \\\"running: $CLAUDE_TOOL_INPUT\\\"\" }\n        ]\n      }\n    ]\n  }\n}\n```\n\n**Note on schema staleness:** The published JSON schema at SchemaStore is updated periodically and may lag the latest CLI releases. A validation warning on a recently documented field does not mean the configuration is invalid.\n\n### File locations by scope\n\n| Scope | File path | Committed to git | Gitignore needed |\n|---|---|---|---|\n| **User** | `~/.claude/settings.json` | No (user home) | No |\n| **Project** | `.claude/settings.json` | **Yes** | No |\n| **Local** | `.claude/settings.local.json` | **No** | **Yes** |\n| **Managed** | `/etc/claude-code/managed-settings.json` (Linux/WSL) | Via MDM | N/A |\n\nOn Windows, `~/.claude` resolves to `%USERPROFILE%\\.claude`.\n\nFor the local file, add to your repo's `.gitignore`:\n\n```gitignore\n# Personal Claude Code overrides — never commit\n.claude/settings.local.json\nCLAUDE.local.md\n```\n\n### Settings that do NOT hot-reload\n\nMost keys reload without restarting the process — the `ConfigChange` hook fires whenever settings change on disk, which is useful for audit logging. Two keys are read-only at startup:\n- `model` — switch mid-session with `/model`\n- `outputStyle` — rebuilt on `/clear` or restart\n\n### Related configuration files (non-settings)\n\n```\n~/.claude.json          OAuth session, per-project MCP approvals, caches — never commit\n.mcp.json               Team-shared MCP server list (project root, committed)\n~/.claude/CLAUDE.md     User-scope instruction file\nCLAUDE.md              Project-scope instruction file\nCLAUDE.local.md        Local-scope instruction file (gitignored)\n```"
   },
   {
    "heading": "Settings Precedence and Merging Rules",
    "body": "The precedence hierarchy from lowest to highest:\n\n```\nUser (~/.claude/settings.json)                         ← lowest\n  ↑ Project (.claude/settings.json)\n    ↑ Local (.claude/settings.local.json)\n      ↑ CLI flags / command-line arguments\n        ↑ Managed (/etc/claude-code/managed-settings.json)  ← highest, always wins\n```\n\n### Scalar vs array merging — they behave differently\n\nThis is the most important thing to internalize before configuring a team setup:\n\n**Scalars** (model, boolean flags, string values): the highest-precedence scope wins. A managed `model` setting overrides project, which overrides user. No accumulation.\n\n**Permission arrays** (permissions.allow, permissions.ask, permissions.deny): **merge across all scopes**. Every allow entry from user + project + local + managed is combined into one effective list. Deny rules evaluated first across the union; allow rules evaluated last. A deny in *any* scope beats an allow in *any* scope — there is no specificity ordering or per-scope override.\n\nThe official documentation is explicit: \"Rules are evaluated in order: deny, then ask, then allow. The first match in that order determines the outcome, and rule specificity does not change the order. A broad deny rule like `Bash(aws *)` blocks every matching call, including calls that also match a narrower allow rule like `Bash(aws s3 ls)`.\"\n\n### Practical consequence for teams\n\nBecause permission arrays merge, a developer's `~/.claude/settings.json` `allow: [\"Bash(*)\"]` remains active even when the project settings say nothing about it. If you need to restrict something absolutely, managed settings with `allowManagedPermissionRulesOnly: true` is required — this prevents user and project settings from defining any allow, ask, or deny rules, leaving only managed rules in effect.\n\n### Drop-in managed settings fragments\n\n```json\n// /etc/claude-code/managed-settings.d/10-security.json\n{\n  \"permissions\": {\n    \"deny\": [\"Bash(curl *)\", \"Bash(wget *)\", \"Read(./.env*)\"]\n  }\n}\n// /etc/claude-code/managed-settings.d/20-telemetry.json\n{\n  \"env\": {\n    \"CLAUDE_CODE_ENABLE_TELEMETRY\": \"1\",\n    \"OTEL_EXPORTER_OTLP_ENDPOINT\": \"https://otel.corp.example.com\"\n  }\n}\n```\n\nDrop-in files in `managed-settings.d/` are processed alphabetically; scalars from later files override earlier ones; arrays are concatenated and de-duplicated; objects are deep-merged. Use numeric prefixes to control order. Managed settings parse **tolerantly** (v2.1.169+) — a single invalid entry is stripped with a warning, the rest still enforce. User/project/local settings are **strict** — the entire file is rejected if validation fails."
   },
   {
    "heading": "Permission Rules: Format, Matching, and Pitfalls",
    "body": "Permission rules are the enforcement layer for tool calls. Each rule is a string of the form `Tool` or `Tool(pattern)`. Available built-in tools include: `Bash`, `Read`, `Write`, `Edit`, `MultiEdit`, `WebFetch`, `WebSearch`, `TodoRead`, `TodoWrite`. MCP tools are named `mcp__<serverName>__<toolName>` (e.g., `mcp__puppeteer__puppeteer_navigate`).\n\n### Pattern matching\n\nPatterns use **glob-style matching**. A bare tool name (`Bash`) or `Bash(*)` matches all uses of that tool. A bare tool-name deny rule removes the tool from Claude's context entirely — Claude never sees it.\n\n```json\n{\n  \"permissions\": {\n    \"allow\": [\n      \"Bash(npm run lint)\",       // exact command only\n      \"Bash(npm run test *)\",     // prefix + wildcard for args\n      \"Bash(git status)\",\n      \"Bash(git diff *)\",\n      \"Read(~/.zshrc)\",           // home-relative path\n      \"Read(src/**)\",             // ** crosses directory boundaries\n      \"Write(docs/**/*.md)\",      // recursive in docs/\n      \"WebFetch(domain:github.com)\" // WebFetch uses domain: prefix\n    ],\n    \"deny\": [\n      \"Bash(curl *)\",\n      \"Bash(git push --force *)\",\n      \"Bash(rm -rf *)\",\n      \"Read(./.env)\",             // gitignore semantics: matches .env anywhere under cwd\n      \"Read(./.env.*)\",\n      \"Read(./secrets/**)\"\n    ]\n  }\n}\n```\n\n### Path anchoring — four distinct pattern types for Read/Edit\n\nRead and Edit rules follow **gitignore semantics**. There are four distinct anchors:\n\n| Pattern form | Anchor | Example | Matches |\n|---|---|---|---|\n| `//path` | Absolute filesystem root | `Read(//home/alice/secrets/**)` | `/home/alice/secrets/**` |\n| `~/path` | Home directory | `Read(~/.zshrc)` | `/home/alice/.zshrc` |\n| `/path` | Project root | `Edit(/src/**/*.ts)` | `<project root>/src/**/*.ts` |\n| `path` or `./path` | Current working directory | `Read(.env)` | any `.env` at any depth under cwd |\n\n**Critical:** A bare `Read(.env)` is NOT anchored to a single file — in gitignore semantics, a pattern without a leading `/` or `~/` matches at **any depth** under the current directory. `Read(.env)` and `Read(**/.env)` are equivalent. To match only the file at the cwd root, use `Read(/.env)` (project-root anchor).\n\n### Deny always beats allow — evaluation order\n\nRules are evaluated in order: **deny → ask → allow**. The first match in that order determines the outcome. Rule specificity does not change this order — a broad deny beats a narrow allow.\n\n### The `ask` tier\n\nA third tier — `ask` — causes Claude Code to show a confirmation prompt for matching tool calls, even in auto/bypass modes. Use it for high-stakes operations where you want a human checkpoint:\n\n```json\n{ \"permissions\": { \"ask\": [\"Bash(terraform apply *)\", \"Bash(kubectl delete *)\"] } }\n```\n\n### Compound command handling\n\nClaude Code is shell-aware: a rule like `Bash(safe-cmd *)` does **not** permit `safe-cmd && dangerous-cmd`. The client parses compound commands split by `&&`, `||`, `;`, `|`, `|&`, `&`, and newlines, and evaluates each subcommand independently. A rule must match every subcommand in a compound command for it to proceed without a prompt.\n\n### Read-only commands need no explicit allow\n\nClaude Code has a built-in set of commands it treats as read-only and runs without a permission prompt in every mode: `ls`, `cat`, `echo`, `pwd`, `head`, `tail`, `grep`, `find`, `wc`, `which`, `diff`, `stat`, `du`, `cd`, and read-only forms of `git`. You do not need `allow` rules for these.\n\n### Critical gotchas\n\n**Gotcha 1: Bare path names match at any depth.** `Read(.env)` uses gitignore semantics and matches `.env` at *any* depth under the current directory, not just the root. This is likely what you want for a deny rule, but be aware of the semantics.\n\n**Gotcha 2: `*` vs `**` for paths.** In Read/Edit rules, `*` matches within a single path segment; `**` crosses directory boundaries. `Read(secrets/*)` matches `secrets/api-key` but not `secrets/sub/api-key`. Use `Read(secrets/**)` for recursive matching.\n\n**Gotcha 3: Pattern matching is against the raw tool input string.** `Bash(git push --force *)` does not match `git push origin main --force` because argument order differs. Test deny rules explicitly.\n\n**Gotcha 4: MCP tool permission format.** Regular MCP server tools use `mcp__<serverName>__<toolName>` — e.g., `mcp__filesystem__write_file`. Plugin-bundled MCP tools use `mcp__plugin_<plugin-name>_<server-name>__<tool-name>` with non-alphanumeric characters replaced by underscores.\n\n**Gotcha 5: URL restrictions in Bash rules are fragile.** A rule like `Bash(curl http://github.com/ *)` won't match `curl -X GET http://github.com/...` (flag before URL), `curl https://github.com/...` (different scheme), or shell variables. For reliable URL filtering, use `WebFetch(domain:github.com)` and deny curl/wget in Bash."
   },
   {
    "heading": "Committed vs Personal Config: The Deliberate Homes",
    "body": "Every config artifact has an intended home based on two questions: (1) Does this apply to all team members or just me? (2) Could this contain secrets?\n\n| Artifact | Location | Commit? | Why |\n|---|---|---|---|\n| Team instructions | `CLAUDE.md` or `.claude/CLAUDE.md` | Yes | Shared coding standards, project layout, CI commands |\n| Personal/local instructions | `CLAUDE.local.md` | **No** | Sandbox URLs, local test fixtures, personal shortcuts |\n| Team settings | `.claude/settings.json` | Yes | Shared permission rules, shared env vars, MCP server list |\n| Personal overrides | `.claude/settings.local.json` | **No** | Developer-specific allow rules, personal model preference |\n| Project MCP servers | `.mcp.json` (repo root) | Yes | Approved MCP servers for the team |\n| Personal MCP state | `~/.claude.json` | N/A | OAuth tokens, local-scope MCP servers, per-project approval state — never commit |\n| Org-wide policy | `/etc/claude-code/managed-settings.json` | Via MDM | Cannot be committed — deployed by infrastructure tooling |\n| Auto memory | `~/.claude/projects/*/memory/` | **No** | Machine-local, Claude-written, per-developer |\n\n### Recommended `.gitignore` additions for any Claude Code repo\n\n```gitignore\n# Claude Code — personal state, never commit\n.claude/settings.local.json\nCLAUDE.local.md\n```\n\n### What never belongs in committed files\n\n- API keys (use `apiKeyHelper` scripts that read from the system keychain, or deliver keys via managed settings `env` block with restricted file permissions)\n- Personal MCP OAuth tokens (`~/.claude.json` only)\n- Developer-local sandbox credentials (`CLAUDE.local.md` or `settings.local.json`)\n- Personal model preferences that conflict with team defaults\n\n**Anti-pattern to avoid:** Some teams put `env: { ANTHROPIC_API_KEY: \"sk-ant-...\" }` in the committed `.claude/settings.json`. The key is now in git history permanently — even if later removed. Use `apiKeyHelper` with a keychain script, or set the key via managed settings `env` block deployed through Ansible/MDM with restricted file permissions.\n\n**Per-project MCP approvals** are stored in `~/.claude.json` under the project path. When a developer opens a project containing `.mcp.json` for the first time, Claude Code shows a dialog listing the servers and they must approve each one. Approvals are machine-local and personal — they are not shared across machines and are not committed to source control. To reset approval choices, run `claude mcp reset-project-choices`."
   },
   {
    "heading": "CLAUDE.md Authoring Discipline",
    "body": "CLAUDE.md files are loaded in full into the context window on every session. Unlike code that runs once, instructions that are long, vague, or contradictory degrade adherence for the entire session — the model scans structure the same way readers do.\n\n### The 200-line target\n\nThe official guidance is to stay under 200 lines per CLAUDE.md file. This is not an arbitrary number: longer files consume more of the context budget that should go to actual task context, and empirically reduce how consistently Claude follows the instructions. If your file is growing past 200 lines, you have two legitimate tools:\n\n1. **Path-scoped rules** (`.claude/rules/` with `paths:` frontmatter) — load only when Claude touches matching files\n2. **Subdirectory CLAUDE.md files** — loaded lazily when Claude works in those directories\n\nNote that `@path` imports do **not** save context tokens. The imported file is inlined at launch. They improve maintainability but not token budget.\n\n### Specificity is the primary lever\n\nVague instructions are ignored under pressure. Concrete, verifiable instructions stick:\n\n```markdown\n# Bad — vague, unverifiable\n- Format code properly\n- Test your changes\n- Keep files organized\n- Be careful with secrets\n\n# Good — concrete, verifiable\n- Use 2-space indentation; tabs are a build error (see .editorconfig)\n- Run `make test` before every commit; the CI gate is the same command\n- API handlers live in `src/api/handlers/`; never create handlers elsewhere\n- Never read or write `.env`, `.env.*`, or `secrets/` — these are in settings deny rules\n```\n\n### Contradiction detection\n\nIf two rules in any loaded CLAUDE.md contradict each other, Claude picks one arbitrarily. Run `/memory` to list all loaded instruction files and audit for conflicts — especially in monorepos where ancestor CLAUDE.md files are loaded automatically.\n\n### What belongs in CLAUDE.md vs elsewhere\n\n| Content type | Best home |\n|---|---|\n| Build/test commands | CLAUDE.md ✓ |\n| Project architecture overview | CLAUDE.md ✓ |\n| Coding standards (indentation, naming) | CLAUDE.md ✓ |\n| \"Never do X\" security rules | `permissions.deny` — not CLAUDE.md |\n| Pre-commit linting hook | `hooks` in settings.json — not CLAUDE.md |\n| Multi-step procedures | `.claude/skills/` — load on demand |\n| Framework-specific rules (only for `.tsx` files) | `.claude/rules/` with `paths:` frontmatter |\n| Personal local sandbox URL | `CLAUDE.local.md` — not committed |\n\n### Maintainer comments that cost no tokens\n\n```markdown\n<!-- MAINTAINER: Last reviewed 2026-06-15. Update after each sprint retro. -->\n<!-- SOURCE: Derived from ADR-0003; if in doubt, check that doc. -->\n\n## Build\n- Run `make test` before committing\n```\n\nHTML block comments are stripped before context injection. They are visible when you open the file directly with the Read tool, but they cost zero tokens in the model's context."
   },
   {
    "heading": "Imports and Path-Scoped Rules",
    "body": "Two mechanisms let you modularize beyond a single flat CLAUDE.md, with very different token budgets.\n\n### `@path` imports — inline at launch, no context savings\n\nAny `@path/to/file` reference outside a code fence or backtick in a CLAUDE.md is expanded inline at session start. The full content of the imported file enters context immediately, alongside the referencing CLAUDE.md.\n\n```markdown\n# CLAUDE.md\nSee @README.md for project overview.\nDependencies: @package.json\n\n## Git workflow\n@docs/git-instructions.md\n\n## Security policies\n@../shared-standards/security.md  # relative to this file's location\n```\n\nImports support:\n- Relative paths (relative to the CLAUDE.md that contains the import, not the working directory)\n- Absolute paths\n- Home-directory paths (`@~/.claude/my-project-instructions.md`)\n- Recursive imports up to 4 hops deep\n- Backtick wrapping to suppress import: `` `@README` `` stays literal\n\n**First-time external import approval:** The first time Claude Code encounters imports that reference paths outside the project, it shows an approval dialog. If you decline, the imports stay disabled and the dialog does not appear again.\n\n### `.claude/rules/` — deferred loading by file path\n\nPath-scoped rules in `.claude/rules/` use YAML frontmatter to declare which file patterns trigger them. They load **only when Claude reads a matching file**, not at session start — these are the only rules that genuinely save context budget.\n\nRules without a `paths:` field are loaded unconditionally at launch, just like `.claude/CLAUDE.md`.\n\n```markdown\n---\npaths:\n  - \"src/api/**/*.ts\"\n  - \"tests/**/*.test.ts\"\n---\n\n# API Rules (loaded only when Claude touches API or test files)\n\n- All endpoints must include input validation via the shared `validateRequest()` wrapper\n- Use the standard `ApiError` class from `src/lib/errors.ts` — never throw raw errors\n- Every handler needs an OpenAPI JSDoc comment or the type check fails\n```\n\nBrace expansion works in `paths:` patterns:\n\n```markdown\n---\npaths:\n  - \"src/**/*.{ts,tsx}\"\n  - \"tests/**/*.test.ts\"\n---\n```\n\nDirectory layout for a well-organized project:\n\n```\n.claude/\n├── CLAUDE.md               # Main entry point, <200 lines, always loaded\n├── settings.json           # Committed permissions + env config\n├── settings.local.json     # Personal overrides (gitignored)\n└── rules/\n    ├── code-style.md       # No paths: — loads at launch like CLAUDE.md\n    ├── api-design.md       # paths: src/api/**/*.ts\n    ├── testing.md          # paths: tests/**\n    └── security.md         # paths: src/**/*.{ts,tsx}\n```\n\n### When to use what\n\n| Mechanism | Context cost | When it loads | Best for |\n|---|---|---|---|\n| CLAUDE.md | Always | Session start | Universal project context |\n| `@path` import | Always (same as inline) | Session start | Splitting large files for maintainability only |\n| `rules/` with no `paths:` | Always | Session start | Topic-organized unconditional rules |\n| `rules/` with `paths:` | Only when file matches | On file read | Language-specific, framework-specific rules |\n| Skill (`.claude/skills/`) | Never unless invoked | On explicit call | Multi-step procedures, specialized workflows |\n\nUser-level rules in `~/.claude/rules/` apply across all your projects. They load before project rules, so project rules have higher priority (they appear later in context)."
   },
   {
    "heading": "Team MCP Server Configuration",
    "body": "MCP servers that the whole team should use are declared in `.mcp.json` at the repository root. This file is committed to source control. Each developer must explicitly approve the servers on their machine before they load — approval is stored in `~/.claude.json` (never committed). To reset approval choices, run `claude mcp reset-project-choices`.\n\n### `.mcp.json` format\n\nThe format supports multiple transport types. Stdio servers use `command`/`args`/`env`; HTTP servers use `type: \"http\"` and `url`:\n\n```json\n{\n  \"mcpServers\": {\n    \"github\": {\n      \"type\": \"http\",\n      \"url\": \"https://api.githubcopilot.com/mcp/\",\n      \"headers\": {\n        \"Authorization\": \"Bearer ${GITHUB_TOKEN}\"\n      }\n    },\n    \"postgres\": {\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"@bytebase/dbhub\"],\n      \"env\": {\n        \"DATABASE_URL\": \"${DATABASE_URL}\"\n      }\n    },\n    \"company-internal\": {\n      \"type\": \"stdio\",\n      \"command\": \"/usr/local/bin/company-mcp-server\",\n      \"args\": [\"--config\", \"/etc/company/mcp-config.json\"],\n      \"env\": {\n        \"COMPANY_API_URL\": \"https://internal.example.com\"\n      }\n    }\n  }\n}\n```\n\n`${ENV_VAR}` references expand from the developer's shell environment at MCP server startup. `${VAR:-default}` syntax is also supported. **Secrets never live in `.mcp.json`** — they live in the developer's environment (shell profile, CI secrets, etc.). A required variable that is unset and has no default causes the config to fail to parse.\n\n### Approval workflow\n\nWhen a developer opens a project containing `.mcp.json` for the first time, Claude Code shows a dialog listing the servers. Project-scoped servers that are awaiting approval appear in `claude mcp list` as `⏸ Pending approval`. Run Claude interactively to review and approve them.\n\n### Managed-level MCP control\n\nFor organizations that need to restrict which MCP servers teams can use, the primary mechanism is `managed-mcp.json` (a separate file from `managed-settings.json`) deployed to the same system directory:\n\n```json\n// /etc/claude-code/managed-mcp.json\n// This gives exclusive control — users CANNOT add any other servers\n{\n  \"mcpServers\": {\n    \"github\": {\n      \"type\": \"http\",\n      \"url\": \"https://api.githubcopilot.com/mcp/\"\n    },\n    \"company-internal\": {\n      \"type\": \"stdio\",\n      \"command\": \"/usr/local/bin/company-mcp-server\",\n      \"args\": [\"--config\", \"/etc/company/mcp-config.json\"]\n    }\n  }\n}\n```\n\nWhen `managed-mcp.json` exists, Claude Code loads **only** the servers it defines. Users cannot add, modify, or use any other MCP servers. If a user tries to run `claude mcp add`, they see: `Cannot add MCP server: enterprise MCP configuration is active and has exclusive control over MCP servers`.\n\nFor an allowlist approach (users can add servers from an approved set), use `allowedMcpServers` and `allowManagedMcpServersOnly: true` in `managed-settings.json`. Entries use `serverUrl`, `serverCommand`, or `serverName` keys — not plain strings:\n\n```json\n// /etc/claude-code/managed-settings.json\n{\n  \"allowManagedMcpServersOnly\": true,\n  \"allowedMcpServers\": [\n    { \"serverUrl\": \"https://api.githubcopilot.com/*\" },\n    { \"serverUrl\": \"https://*.internal.example.com/*\" },\n    { \"serverCommand\": [\"npx\", \"-y\", \"@modelcontextprotocol/server-filesystem\", \".\"]\n    }\n  ],\n  \"deniedMcpServers\": [\n    { \"serverUrl\": \"https://*.untrusted.example.com/*\" }\n  ]\n}\n```\n\n**Important:** `serverCommand` entries match the exact command array, in order. `[\"npx\", \"-y\", \"server\"]` does not match `[\"npx\", \"server\"]`.\n\n### MCP in user settings vs `.mcp.json`\n\nMCP servers can also be added via `claude mcp add --scope user`, which stores them in `~/.claude.json` — these are personal servers available across all your projects. The `.mcp.json` pattern is specifically for team-shared servers that travel with the repo."
   },
   {
    "heading": "Managed Settings for Organizations",
    "body": "Managed settings are the only layer that cannot be overridden by any user or project configuration. They are deployed via standard infrastructure tooling — not via Claude Code itself.\n\n### Deployment paths by OS\n\n```\n# Linux/WSL\n/etc/claude-code/managed-settings.json\n/etc/claude-code/managed-settings.d/*.json  # drop-in fragments\n/etc/claude-code/managed-mcp.json           # separate file for exclusive MCP control\n\n# macOS\n/Library/Application Support/ClaudeCode/managed-settings.json\n/Library/Application Support/ClaudeCode/managed-settings.d/*.json\n# Also: MDM plist domain com.anthropic.claudecode\n\n# Windows\nC:\\Program Files\\ClaudeCode\\managed-settings.json\nC:\\Program Files\\ClaudeCode\\managed-settings.d\\*.json\n# Also: Group Policy / Intune via HKLM\\SOFTWARE\\Policies\\ClaudeCode\n# Note: C:\\ProgramData\\ClaudeCode\\managed-settings.json is deprecated (removed v2.1.75)\n```\n\nDrop-in files in `managed-settings.d/` are merged alphabetically. Scalars from later files override earlier ones; arrays are concatenated and de-duplicated; objects are deep-merged. Use numeric prefixes for deterministic ordering:\n\n```\nmanaged-settings.d/\n├── 10-security.json     # deny rules\n├── 20-telemetry.json    # OTEL configuration\n├── 30-identity.json     # forceLoginOrgUUID\n└── 40-announcements.json\n```\n\n### Minimum viable managed settings for a team\n\n```json\n{\n  \"forceLoginOrgUUID\": \"YOUR-ORG-UUID-HERE\",\n  \"forceLoginMethod\": \"claudeai\",\n  \"requiredMinimumVersion\": \"2.1.169\",\n  \"permissions\": {\n    \"deny\": [\n      \"Read(./.env*)\",\n      \"Read(./secrets/**)\",\n      \"Bash(git push --force *)\"\n    ]\n  },\n  \"env\": {\n    \"CLAUDE_CODE_ENABLE_TELEMETRY\": \"1\",\n    \"OTEL_EXPORTER_OTLP_ENDPOINT\": \"https://otel.corp.example.com\"\n  },\n  \"companyAnnouncements\": [\n    \"All Claude Code usage is subject to the Acme Corp AI Policy. See docs.acme.com/ai-policy\"\n  ]\n}\n```\n\n### Keys exclusive to managed settings (setting these in user/project/local has no effect)\n\n| Key | Purpose |\n|---|---|\n| `forceLoginOrgUUID` | Require login to a specific Anthropic org |\n| `forceLoginMethod` | Restrict to `claudeai` or `console` login only |\n| `requiredMinimumVersion` / `requiredMaximumVersion` | Enforce version bounds |\n| `allowManagedPermissionRulesOnly` | Block user/project permission rules; only managed rules apply |\n| `allowManagedHooksOnly` | Only managed hooks and SDK hooks load; user/project hooks blocked |\n| `allowManagedMcpServersOnly` | Only allowlisted MCP servers respected |\n| `allowedMcpServers` / `deniedMcpServers` | MCP server allowlist/denylist (use `serverUrl`, `serverCommand`, `serverName` entries) |\n| `claudeMd` | Embed CLAUDE.md content directly in the settings file |\n| `strictKnownMarketplaces` / `blockedMarketplaces` | Plugin marketplace restrictions |\n| `forceRemoteSettingsRefresh` | Block startup until remote managed settings freshly fetched |\n| `policyHelper` | Executable that computes managed settings dynamically at startup |\n| `allowAllClaudeAiMcps` | Load claude.ai connectors alongside `managed-mcp.json` |\n\n### `claudeMd` key — embed instructions in settings\n\nThe `claudeMd` managed-only key lets you embed CLAUDE.md content directly inside `managed-settings.json` without deploying a separate file:\n\n```json\n{\n  \"claudeMd\": \"Always run `make lint` before committing.\\nNever push directly to main.\"\n}\n```\n\nThis is useful when a separate file deployment is operationally inconvenient. Setting `claudeMd` in user, project, or local settings has no effect.\n\n### `claude doctor` for validation\n\nBefore deploying managed settings fleet-wide, validate on a test machine:\n\n```bash\nclaude doctor\n```\n\nThis lists each invalid entry with its source file. In headless (`-p`) mode, errors are printed to stderr. Managed settings parse tolerantly — a typo in one entry is stripped with a warning, the rest still enforce — but you still want to catch errors before rollout."
   },
   {
    "heading": "Monorepo and Multi-Team Layouts",
    "body": "In a monorepo, the directory-walking discovery mechanism means Claude loads CLAUDE.md files from teams you're not working with — their conventions may contradict yours, and their rules consume your context budget.\n\n### `claudeMdExcludes` to filter noise\n\n```json\n// .claude/settings.local.json  (personal, gitignored)\n{\n  \"claudeMdExcludes\": [\n    \"**/other-team/.claude/rules/**\",\n    \"/home/user/monorepo/infra/CLAUDE.md\"\n  ]\n}\n```\n\nPatterns are matched against **absolute file paths**. You can set `claudeMdExcludes` at any scope (user, project, local, or managed) and arrays merge across scopes. **Managed policy CLAUDE.md files cannot be excluded** — that is by design to prevent users from bypassing org-wide instructions.\n\n### Recommended monorepo layout\n\n```\nmonorepo/\n├── CLAUDE.md                         # Repo-wide conventions (build system, PR process)\n├── .claude/\n│   ├── settings.json                 # Shared permission rules\n│   └── rules/\n│       └── repo-wide-security.md     # No paths: — applies everywhere at launch\n├── packages/\n│   ├── api/\n│   │   ├── CLAUDE.md                 # API-specific conventions (lazy-loaded)\n│   │   └── .claude/rules/\n│   │       └── api-design.md         # paths: src/**/*.ts\n│   └── frontend/\n│       ├── CLAUDE.md                 # Frontend conventions (lazy-loaded)\n│       └── .claude/rules/\n│           └── react.md              # paths: src/**/*.{tsx,jsx}\n└── .mcp.json                         # Shared MCP servers for all teams\n```\n\nRoot CLAUDE.md covers what is universal. Package-level CLAUDE.md files are lazy-loaded only when Claude works in those directories. Rules with `paths:` frontmatter only fire for specific file types.\n\n### Symlinked shared rules\n\nIf multiple packages need identical rules, use symlinks — `.claude/rules/` resolves symlinks normally and detects circular symlinks gracefully:\n\n```bash\n# Link org-wide security rules into each package\nln -s ~/shared-claude-rules packages/api/.claude/rules/shared\nln -s ~/company-standards/security.md packages/frontend/.claude/rules/security.md\n```\n\n### Launching from a subdirectory\n\nWhen you launch Claude from `packages/api/`, the root `CLAUDE.md` is still loaded (discovered while walking up), but `packages/frontend/CLAUDE.md` is not — it's in a sibling directory, not an ancestor. Use `claudeMdExcludes` if a sibling's CLAUDE.md is mistakenly picked up via a shared ancestor."
   },
   {
    "heading": "Common Pitfalls and Failure Modes",
    "body": "These are the failure modes that appear repeatedly when teams set up shared Claude Code infrastructure.\n\n### Pitfall 1: Security policy in CLAUDE.md\n\nThe most common and most dangerous. CLAUDE.md says \"never read .env files\" — Claude usually complies, but compliance degrades under task pressure. The fix is a `permissions.deny` entry. CLAUDE.md is for behavioral guidance; settings is for enforcement. The official docs state this explicitly: instructions in CLAUDE.md \"shape what Claude tries to do, but they don't change what Claude Code allows.\"\n\n### Pitfall 2: Secrets in committed settings\n\n`env: { DATABASE_URL: \"postgres://user:password@prod-host/db\" }` in `.claude/settings.json` commits credentials to git history permanently (even if later removed via `git filter-branch`). Use `${ENV_VAR}` references in `.mcp.json`, shell environment variables, or the `apiKeyHelper` pattern that reads from a keychain script.\n\n### Pitfall 3: Forgetting that permission arrays merge\n\nA team member's `~/.claude/settings.json` has `allow: [\"Bash(*)\"]`. The project `.claude/settings.json` has no such entry. Result: that developer can run any bash command regardless of what the project settings say. To lock this down, managed settings with `allowManagedPermissionRulesOnly: true` is required.\n\n### Pitfall 4: `@path` imports don't reduce context\n\nTeams split a 300-line CLAUDE.md into 6 imported files thinking this reduces token usage. It doesn't — all 6 files are inlined at launch. The only way to defer loading is path-scoped rules in `.claude/rules/` with `paths:` frontmatter.\n\n### Pitfall 5: Glob semantics on Read/Edit paths differ from Bash\n\nRead and Edit rules follow gitignore semantics, not shell glob semantics. A bare `Read(.env)` matches `.env` at **any depth** under the working directory (not just the root). `Read(secrets/*)` matches within one directory segment; `Read(secrets/**)` is recursive. Test rules against actual paths before relying on them.\n\n### Pitfall 6: Contradicting rules across files\n\nAncestor CLAUDE.md says \"always use tabs\". Package CLAUDE.md says \"use 2-space indentation\". Both load. Claude picks one arbitrarily (usually the later-in-context one, since project content appears after ancestor content). Audit with `/memory` to see all loaded files, then resolve contradictions explicitly.\n\n### Pitfall 7: MCP server approval not set up in CI\n\nCI environments running `claude -p \"...\"` headlessly have no mechanism to click through the MCP server approval dialog. In CI, do not rely on project `.mcp.json` servers — use local-scoped servers pre-configured with `claude mcp add --scope local` in the CI setup step, or use `claude mcp reset-project-choices` followed by a session to pre-approve. Alternatively, manage CI tool access without MCP (use shell commands instead).\n\n### Pitfall 8: MEMORY.md treated as shared team state\n\nAuto memory lives in `~/.claude/projects/*/memory/` — machine-local, written by Claude from your personal sessions. It is not shared across developers and should never be committed. If you want team-shared build insights, they belong in CLAUDE.md, not auto memory.\n\n### Pitfall 9: `allowedMcpServers` entries as plain strings\n\nIn older draft docs and community posts, `allowedMcpServers` was shown as a list of server name strings. The current format uses objects with a discriminating key: `{ \"serverUrl\": \"...\" }`, `{ \"serverCommand\": [...] }`, or `{ \"serverName\": \"...\" }`. A `serverName` entry is **not a security control** — users can name any server anything. Use `serverUrl` or `serverCommand` for enforcement."
   },
   {
    "heading": "Reference: Minimum Viable Team Configuration",
    "body": "A concrete starting template for a shared Claude Code setup. Copy, review, adjust.\n\n### `.claude/settings.json` (committed)\n\n```json\n{\n  \"$schema\": \"https://json.schemastore.org/claude-code-settings.json\",\n  \"permissions\": {\n    \"allow\": [\n      \"Bash(npm run lint)\",\n      \"Bash(npm run test)\",\n      \"Bash(npm run test *)\",\n      \"Bash(npm run build)\",\n      \"Bash(make *)\",\n      \"Bash(git status)\",\n      \"Bash(git diff *)\",\n      \"Bash(git log *)\",\n      \"Bash(git add *)\",\n      \"Bash(git commit *)\"\n    ],\n    \"deny\": [\n      \"Read(./.env)\",\n      \"Read(./.env.*)\",\n      \"Read(./secrets/**)\",\n      \"Bash(git push --force *)\",\n      \"Bash(git push -f *)\",\n      \"Bash(curl *)\",\n      \"Bash(wget *)\"\n    ]\n  },\n  \"env\": {\n    \"CLAUDE_CODE_ENABLE_TELEMETRY\": \"1\"\n  }\n}\n```\n\n**Note:** There is no `enabledMcpjsonServers` key in Claude Code's settings schema. MCP server approvals are stored per-developer in `~/.claude.json` and managed via `claude mcp reset-project-choices`.\n\n### `CLAUDE.md` (committed)\n\n```markdown\n<!-- MAINTAINER: Keep under 200 lines. Last reviewed 2026-06-15. -->\n\n# Project Name\n\nBrief one-paragraph project description. What it does, what it's not.\n\n## Build and Test\n- Install: `npm install`\n- Test: `make test` or `npm run test`\n- Lint: `npm run lint` (runs automatically in CI)\n- Build: `npm run build`\n\n## Architecture\n- API handlers: `src/api/handlers/` — never create handlers elsewhere\n- Shared types: `src/types/` — import from here, never redefine\n- Database access: `src/db/` — no raw SQL outside these files\n\n## Conventions\n- 2-space indentation throughout (enforced by .editorconfig)\n- TypeScript strict mode — no `any`, no `@ts-ignore` without a comment explaining why\n- Import order: stdlib → third-party → internal (enforced by eslint-plugin-import)\n- Never log `req.body` or `req.headers` in API handlers — PII risk\n\n## Deployment\n- Staging: push to `develop` branch, auto-deploys in ~3 min\n- Production: merge PR to `main`, requires 2 approvals\n- Never push directly to `main` (blocked by branch protection and deny rules)\n```\n\n### `.mcp.json` (committed)\n\n```json\n{\n  \"mcpServers\": {\n    \"github\": {\n      \"type\": \"http\",\n      \"url\": \"https://api.githubcopilot.com/mcp/\",\n      \"headers\": {\n        \"Authorization\": \"Bearer ${GITHUB_TOKEN}\"\n      }\n    }\n  }\n}\n```\n\n### `.gitignore` additions\n\n```gitignore\n# Claude Code — personal state\n.claude/settings.local.json\nCLAUDE.local.md\n```\n\n### Developer onboarding checklist\n\n```\n[ ] Export GITHUB_TOKEN in shell profile (or .env.local)\n[ ] Open project in Claude Code — approve MCP server dialog for github\n[ ] Run /memory to verify CLAUDE.md loaded correctly and check for any conflicts\n[ ] Optionally: create CLAUDE.local.md with personal sandbox URL\n[ ] Run: claude mcp list — confirm github server shows as connected\n```"
   }
  ],
  "preQuiz": [
   {
    "prompt": "Your team commits `.claude/settings.json` and `.mcp.json` to the repo. A developer reports that MCP servers from `.mcp.json` are not loading. What is the most likely cause?",
    "options": [
     "`.mcp.json` must be listed under `permissions.allow` in `.claude/settings.json` before it activates.",
     "`.mcp.json` was placed inside `.claude/` rather than the repository root.",
     "MCP servers require an entry in `~/.claude.json` to activate for any project.",
     "`.mcp.json` tool schemas load eagerly and crash if the server is not running."
    ],
    "correct": 1,
    "sectionIndices": [
     0,
     12
    ],
    "explanation": "`.mcp.json` must sit at the repository root. Placing it under `.claude/` or in Claude Desktop format means it is never loaded. Tool schemas are deferred and load on demand, so an unreachable server does not prevent loading."
   },
   {
    "prompt": "You launch Claude Code from `/home/alice/projects/webapp/src`. Which CLAUDE.md files are loaded into context at startup?",
    "options": [
     "Only `/home/alice/projects/webapp/src/CLAUDE.md` (the immediate cwd file).",
     "All CLAUDE.md files found by walking UP the directory tree from cwd, plus `~/.claude/CLAUDE.md`, concatenated root-down.",
     "All CLAUDE.md files in the entire repository tree, including subdirectories below cwd.",
     "`~/.claude/CLAUDE.md` only — project CLAUDE.md files require `/init` to activate."
    ],
    "correct": 1,
    "sectionIndices": [
     1
    ],
    "explanation": "Discovery walks UP from cwd, loading every CLAUDE.md and CLAUDE.local.md found on the way. Subdirectory CLAUDE.md files (below cwd) are NOT loaded at launch — they load on demand only when Claude reads a file in that subdirectory."
   },
   {
    "prompt": "A teammate edits the project CLAUDE.md to add a critical instruction. After a `/compact`, Claude ignores the instruction for a file deep in `src/auth/`. What is happening?",
    "options": [
     "Project CLAUDE.md is evicted from context by `/compact` and must be reloaded with `/memory`.",
     "The instruction is in `src/auth/CLAUDE.md` (a nested subdirectory), which does not re-inject after `/compact` — it only reloads when Claude next reads a file there.",
     "CLAUDE.md instructions are converted to auto-memory entries by `/compact` and lose their project scope.",
     "`/compact` merges all CLAUDE.md files into one flat summary; nested instructions are lost permanently."
    ],
    "correct": 1,
    "sectionIndices": [
     8
    ],
    "explanation": "Project-root CLAUDE.md survives `/compact` (re-read from disk and re-injected). Nested subdirectory CLAUDE.md files are NOT re-injected — they only reload on the next file read in that subdirectory. Instructions that must survive compaction must live in the root CLAUDE.md."
   },
   {
    "prompt": "Your `.claude/rules/frontend.md` has a `paths: src/**/*.{ts,tsx}` frontmatter glob. A developer adds a new rule for database migrations to the same file. What happens?",
    "options": [
     "The migration rule also loads only when Claude reads a `.ts` or `.tsx` file, regardless of what it governs.",
     "The `paths:` glob is ignored once more than one rule is in a file; all rules load at session start.",
     "Claude loads the file eagerly because it detects a conflict between the migration context and the TypeScript glob.",
     "Rules files with `paths:` frontmatter are skipped if they contain mixed domain content."
    ],
    "correct": 0,
    "sectionIndices": [
     4
    ],
    "explanation": "The `paths:` frontmatter controls loading of the entire rules file, not individual rules inside it. Everything in `frontend.md` loads only when Claude reads a matching `.ts`/`.tsx` file — including the unrelated migration rule."
   },
   {
    "prompt": "Your team uses `@~/projects/shared-standards.md` inside the project CLAUDE.md to import shared guidelines. A junior developer opens a Claude Code session from a git worktree. Which of the following is true?",
    "options": [
     "The import expands `~/` relative to the worktree root, so it resolves to a different file per worktree.",
     "The import does NOT save context tokens — the full contents of `shared-standards.md` expand inline at launch.",
     "The import is silently skipped in worktrees because CLAUDE.md imports require the original clone directory.",
     "Imports from `~/` paths trigger the approval dialog on every session in a new worktree."
    ],
    "correct": 1,
    "sectionIndices": [
     3
    ],
    "explanation": "Imports do NOT save context — imported files fully expand into context at launch. Only path-scoped rules defer loading. The `~/` path resolves to the user's home directory, not the worktree root, so the same file is used everywhere."
   },
   {
    "prompt": "Your org deploys `managed-settings.json` with `permissions.deny: ['Bash(rm -rf *)']` to prevent accidental deletions. A developer adds `permissions.allow: ['Bash(rm -rf *)']` to their `~/.claude/settings.json`. What happens?",
    "options": [
     "The user allow overrides the managed deny because user settings are more specific to that machine.",
     "The managed deny wins — `managed-settings.json` always wins over all other scopes, including CLI flags.",
     "Both rules coexist; the allow fires first because allow lists are checked before deny lists.",
     "The conflict is detected at startup and Claude asks the user to resolve it interactively."
    ],
    "correct": 1,
    "sectionIndices": [
     10,
     11
    ],
    "explanation": "Managed settings always win, even over CLI flags. The precedence is: managed deny > user/project deny > managed ask > user/project ask > managed allow > user/project allow. A managed deny cannot be overridden by a user allow at any scope."
   },
   {
    "prompt": "You add `Bash(npm run *)` to `permissions.allow` expecting it to cover `npm run test --watch`. A teammate reports Claude still prompts for the `--watch` variant. What is the cause?",
    "options": [
     "`*` in Bash patterns matches across multiple arguments, so `npm run *` should have worked — the issue is a settings file merge conflict.",
     "Bash permission patterns match the literal command string component-by-component; `*` matches a single component, not additional arguments, so `--watch` is a separate component not covered.",
     "npm commands require an additional `npm(*)` permission in addition to `Bash(npm run *)` to authorize subcommands.",
     "`permissions.allow` applies only to Claude-initiated commands; user-typed commands in the terminal always prompt."
    ],
    "correct": 1,
    "sectionIndices": [
     11
    ],
    "explanation": "`*` matches a single path/command component. `Bash(npm run *)` covers `npm run test` but NOT `npm run test --watch` because `--watch` is an additional argument beyond the single wildcard. You need `Bash(npm run test *)` or a broader pattern."
   },
   {
    "prompt": "A developer adds `permissions: { allow: ['Bash(git status)'] }` to `~/.claude.json` to authorize a git command globally. Claude continues to prompt. Why?",
    "options": [
     "`~/.claude.json` is not a settings file — it stores app state, OAuth, and UI toggles. `permissions` keys there are silently ignored; the entry belongs in `~/.claude/settings.json`.",
     "`~/.claude.json` only accepts `deny` entries; allow rules must go in `.claude/settings.json` at the project level.",
     "Global allow rules require a `scope: global` field inside `~/.claude.json` to be recognized.",
     "`Bash(git status)` is a built-in safe command; permission rules for it are silently discarded."
    ],
    "correct": 0,
    "sectionIndices": [
     10
    ],
    "explanation": "`~/.claude.json` holds app state, theme, OAuth session, and MCP server entries — NOT permissions, hooks, or env. Putting `permissions` there is silently ignored. User-scoped permission rules belong in `~/.claude/settings.json`."
   },
   {
    "prompt": "A team member says 'I told Claude to always use pnpm instead of npm.' Claude followed this for one session, then forgot it. What most likely happened?",
    "options": [
     "The instruction was typed in conversation and saved to auto-memory (MEMORY.md). Auto-memory topic files only load on demand, not at startup — if it was put in a topic file rather than the first 200 lines of MEMORY.md, it was silently not loaded.",
     "Auto-memory is disabled by default and must be explicitly enabled per-project before session instructions persist.",
     "MEMORY.md entries are reset at the start of each git branch to avoid conflicts between branches.",
     "Session instructions typed as regular messages are saved to CLAUDE.md, but CLAUDE.md is not loaded after the first session."
    ],
    "correct": 0,
    "sectionIndices": [
     9
    ],
    "explanation": "Auto-memory MEMORY.md has a 200-line / 25KB cap at startup — only the first 200 lines load. Topic files (e.g. `pnpm.md`) load on demand, not at startup. If the pnpm preference was written to a topic file or beyond line 200, it silently fails to load."
   },
   {
    "prompt": "Your built-in Explore subagent is ignoring a critical `no-console.log` rule in the project CLAUDE.md. You verify the file is committed and loads correctly in the main session. What is the explanation?",
    "options": [
     "Built-in Explore and Plan subagents skip CLAUDE.md — you must restate critical instructions in the delegating prompt.",
     "Subagents load CLAUDE.md but only the first 50 lines to reduce context; the rule is beyond that cutoff.",
     "The Explore subagent applies user-level `~/.claude/CLAUDE.md` only, not project-level CLAUDE.md.",
     "Subagents inherit permissions from the parent session but receive a fresh context without any CLAUDE.md content."
    ],
    "correct": 0,
    "sectionIndices": [
     7
    ],
    "explanation": "Built-in Explore and Plan subagents explicitly skip CLAUDE.md. Critical instructions must be restated in the delegating prompt. Custom subagents defined in `.claude/agents/` do load CLAUDE.md like the main conversation."
   },
   {
    "prompt": "You commit `.claude/settings.json` with a new `hooks` entry. A teammate pulls the change but reports the hook is not firing. They tried restarting their terminal. What should they do?",
    "options": [
     "Run `/hooks` in Claude Code — if it shows stale definitions, run `/hooks` again, because env vars are read at startup but settings.json changes take effect after a file-stability delay with no restart needed.",
     "Restart the `claude` daemon service because hooks are compiled into a binary cache at startup.",
     "The hook is in project settings but hooks only fire when defined in user `~/.claude/settings.json`.",
     "Delete `.claude/settings.json.lock` — a stale lockfile prevents new hook definitions from loading."
    ],
    "correct": 0,
    "sectionIndices": [
     10
    ],
    "explanation": "settings.json edits take effect after a brief file-stability delay with no restart needed. If `/hooks` shows stale definitions, running `/hooks` again refreshes them. Note: env vars ARE read at startup only (restart required for those), but hooks in settings.json do not require a restart."
   },
   {
    "prompt": "Your monorepo has a top-level CLAUDE.md with org-wide rules and a `packages/payments/CLAUDE.md` with payment-team rules. A new engineer joins the payments team and wants to ignore the org-level CLAUDE.md to avoid noise. What is the correct approach?",
    "options": [
     "Add `claudeMdExcludes` globs in `packages/payments/.claude/settings.local.json` to skip the ancestor org CLAUDE.md.",
     "Rename the org-level CLAUDE.md to `.claude/CLAUDE.md` — files in `.claude/` are skipped by subdirectory sessions.",
     "Set `CLAUDE_CODE_DISABLE_CLAUDE_MDS=1` in their shell — this disables only the org-level file while keeping the payments CLAUDE.md.",
     "Add a `priority: low` frontmatter key to the org CLAUDE.md to mark it as advisory."
    ],
    "correct": 0,
    "sectionIndices": [
     6
    ],
    "explanation": "`claudeMdExcludes` (a string array of globs in settings) skips loading ancestor CLAUDE.md/rules files matched by the glob. Using `settings.local.json` keeps it personal and gitignored. `CLAUDE_CODE_DISABLE_CLAUDE_MDS=1` disables ALL CLAUDE.md files — including the payments one — which is too aggressive."
   }
  ],
  "tasks": [
   {
    "id": "stage-1-task-team-settings",
    "afterSectionIdx": 0,
    "title": "Bootstrap team config files for your project",
    "instructions": "Create the three source-controlled config files every team project should have.\n\n1. Create `.claude/settings.json` at your project root with a minimal permission allow-list:\n```bash\nmkdir -p .claude\ncat > .claude/settings.json << 'EOF'\n{\n  \"permissions\": {\n    \"allow\": [\n      \"Bash(npm run *)\",\n      \"Bash(git status)\",\n      \"Bash(git diff *)\",\n      \"Bash(git log *)\"\n    ],\n    \"deny\": [\n      \"Bash(rm -rf *)\",\n      \"Read(./.env*)\",\n      \"Read(./secrets/**)\"\n    ]\n  }\n}\nEOF\n```\n2. Create `.mcp.json` at the **repository root** (not inside `.claude/`):\n```bash\ncat > .mcp.json << 'EOF'\n{\n  \"mcpServers\": {}\n}\nEOF\n```\n3. Add `settings.local.json` to `.gitignore` so personal overrides stay off the repo:\n```bash\necho '.claude/settings.local.json' >> .gitignore\necho 'CLAUDE.local.md' >> .gitignore\n```\n4. Open Claude Code, run a bash command (e.g. `git status`), and confirm it runs without a permission prompt.",
    "doneWhen": "`.claude/settings.json` and `.mcp.json` exist in your repo, `settings.local.json` and `CLAUDE.local.md` are in `.gitignore`, and `git status` runs inside Claude Code without a permission prompt."
   },
   {
    "id": "stage-1-task-claude-md",
    "afterSectionIdx": 2,
    "title": "Write a lean, effective CLAUDE.md for your project",
    "instructions": "Create or update your project's `CLAUDE.md` following the authoring guidelines.\n\n1. Open (or create) `CLAUDE.md` in your project root.\n2. Add the following sections, filling in real values for your project:\n```markdown\n# <Project Name>\n\n## Commands\n- Build: `npm run build`\n- Test: `npm test`\n- Lint: `npm run lint`\n- Type-check: `npx tsc --noEmit`\n\n## Architecture\n- Source lives in `src/`; API handlers in `src/api/`\n- Config files in `config/`; never hardcode environment values\n\n## Rules\n- Use 2-space indentation (TypeScript/JS)\n- Run `npm test` before committing\n- Never commit `.env` or files containing secrets\n- All new functions require a JSDoc comment explaining *why*, not just what\n```\n3. Keep the file under 200 lines. If it grows, move task-specific content to `.claude/rules/<topic>.md`.\n4. Start a new Claude Code session and verify Claude references your build command correctly when asked how to build the project.",
    "doneWhen": "`CLAUDE.md` exists at the project root, is under 200 lines, and Claude answers 'how do I build this project?' using the command you defined without being told separately."
   },
   {
    "id": "stage-1-task-path-scoped-rule",
    "afterSectionIdx": 4,
    "title": "Add a path-scoped rule that loads only for relevant files",
    "instructions": "Create a path-scoped rule so Claude gets extra instructions only when working with a specific file type.\n\n1. Create the rules directory and a new rule file:\n```bash\nmkdir -p .claude/rules\ncat > .claude/rules/api-handlers.md << 'EOF'\n---\npaths:\n  - src/api/**/*.ts\n---\n\n# API Handler Rules\n- Every handler must validate input with Zod before processing\n- Return typed responses using the shared `ApiResponse<T>` wrapper from `src/types/api.ts`\n- Log all errors with `logger.error()` including the request trace ID\n- Never return raw database objects — map to a DTO first\nEOF\n```\n2. Create a test file at `src/api/test-handler.ts` (can be empty):\n```bash\nmkdir -p src/api\ntouch src/api/test-handler.ts\n```\n3. In a Claude Code session, ask Claude to create an API handler in `src/api/test-handler.ts`. Confirm it mentions Zod validation and the `ApiResponse<T>` wrapper — guidance it would only have if the path-scoped rule loaded.\n4. Then ask Claude to create a utility function in `src/utils/math.ts` and confirm it does NOT reference API-specific rules (the path-scoped rule should not have loaded for that path).",
    "doneWhen": "`.claude/rules/api-handlers.md` exists with a `paths:` frontmatter glob, and Claude applies the API rules when editing a file under `src/api/` but not when editing a file outside that path."
   }
  ],
  "visualizations": [
   {
    "id": "stage-1-v",
    "kind": "comparison-table",
    "title": "Config & memory (CLAUDE.md, settings)",
    "textualSummary": "Key concepts of Config & memory (CLAUDE.md, settings): CLAUDE.md memory files, guidance vs enforcement, settings precedence and merging.",
    "columns": [
     "Concept",
     "In this stage"
    ],
    "rows": [
     {
      "label": "CLAUDE.md memory files",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "guidance vs enforcement",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "settings precedence and merging",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "committed vs personal config scope",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     }
    ]
   }
  ],
  "confusions": [
   {
    "misconception": "Skipping Config & memory (CLAUDE.md, settings).",
    "correction": "Each stage is load-bearing for the setup."
   },
   {
    "misconception": "These concepts are interchangeable.",
    "correction": "Each has a distinct role; see the definitions."
   }
  ],
  "quiz": [
   {
    "id": "stage-1-q1",
    "type": "multiple-choice",
    "prompt": "Your team commits `.claude/settings.json` with `permissions.allow: [\"Bash(npm run test *)\"]`. A teammate later adds `permissions.allow: [\"Bash(npm run lint *)\"]` to their personal `~/.claude/settings.json`. When your teammate runs Claude, which permissions are in effect?",
    "options": [
     "Only the project `.claude/settings.json` allow list, because project settings override user settings.",
     "Only the user `~/.claude/settings.json` allow list, because personal settings take highest priority.",
     "Both allow lists are combined — the teammate can run both `npm run test *` and `npm run lint *`.",
     "Whichever file was loaded last wins; the result depends on directory traversal order."
    ],
    "correct": 2,
    "explanation": "Array settings like `permissions.allow` merge across all scopes rather than replacing each other. The teammate ends up with both entries active. Option A is wrong because the content states that array settings 'combine across all scopes' — there is no winner-takes-all replacement for arrays. Option B is wrong for the same reason; user settings have lower precedence for scalar values, but array settings accumulate. Option D is wrong because merge behavior is deterministic by scope, not by load order."
   },
   {
    "id": "stage-1-q2",
    "type": "multiple-choice",
    "prompt": "A security-conscious team wants to guarantee that Claude never runs `rm -rf *` regardless of what individual developers add to their personal or project allow lists. They add `Bash(rm -rf *)` to `permissions.deny` in `.claude/settings.json`. Is this sufficient?",
    "options": [
     "Yes — deny always beats allow across all scopes, so this blocks the command everywhere.",
     "No — project deny rules can be overridden by a higher-precedence local `settings.local.json` allow.",
     "No — `Bash(rm -rf *)` matches the literal string, not the executable, so `/bin/rm` or `find -delete` are not blocked; use a PreToolUse hook or sandbox instead.",
     "No — deny rules in `.claude/settings.json` only apply to interactive sessions, not headless `claude` invocations."
    ],
    "correct": 2,
    "explanation": "The content explicitly warns that 'Bash prefix deny rules are not a hard guarantee — `Bash(rm *)` matches the literal string, NOT the executable, so it doesn't block `/bin/rm` or `find -delete`. For a real block use a PreToolUse hook or the sandbox.' Option A is true that deny beats allow, but that does not solve the bypass via different executables or tools. Option B is wrong — managed deny beats everything, and local allow cannot override a deny from any scope. Option D invents a distinction not present in the content."
   },
   {
    "id": "stage-1-q3",
    "type": "multiple-choice",
    "prompt": "A developer places `.mcp.json` inside the `.claude/` directory (i.e., `.claude/.mcp.json`) to keep it alongside other Claude config. After restarting Claude, the MCP servers defined there never appear. What went wrong?",
    "options": [
     "The file must be named `mcp-servers.json`, not `.mcp.json`.",
     "`.mcp.json` must be at the repo root — placing it under `.claude/` means it never loads.",
     "The developer forgot to run `claude mcp add` after creating the file.",
     "MCP server tool schemas are deferred and require a tool search before they appear."
    ],
    "correct": 1,
    "explanation": "The content states: '`.mcp.json` **must be at repo root** — placing it under `.claude/` or in Claude Desktop format **never loads**.' The file location is the issue. Option A is wrong — the file name `.mcp.json` is correct per the content. Option C is wrong — `claude mcp add` is for user-scope personal servers, not the team `.mcp.json` file. Option D describes a real behavior (tool schemas are deferred) but that would cause them to appear after a tool search, not to never appear at all — the question says they 'never appear', pointing to a loading failure."
   },
   {
    "id": "stage-1-q4",
    "type": "multiple-choice",
    "prompt": "Your team's CLAUDE.md has grown to 350 lines. A teammate argues the file is fine because 'Claude loads the full file regardless of length.' You argue it still needs to be trimmed. Who is correct, and why?",
    "options": [
     "The teammate is correct — since CLAUDE.md loads in full, length has no effect on Claude's behavior.",
     "You are correct — files over 200 lines are truncated before injection, so instructions past line 200 are silently dropped.",
     "You are correct — while the file does load in full, longer files reduce adherence, meaning Claude follows the instructions less reliably.",
     "The teammate is correct for project CLAUDE.md but wrong for `~/.claude/CLAUDE.md`, which is capped at 25 KB."
    ],
    "correct": 2,
    "explanation": "The content states: 'Keep under ~200 lines: longer files still load fully but **reduce adherence**.' So the teammate is wrong that length doesn't matter, but you are also wrong if you claim truncation occurs — it doesn't. The 200-line cap applies to auto-memory MEMORY.md, not to CLAUDE.md. Option B confuses the MEMORY.md 200-line cap with CLAUDE.md behavior. Option D invents a distinction that doesn't appear in the content."
   },
   {
    "id": "stage-1-q5",
    "type": "multiple-choice",
    "prompt": "A teammate types `#  Always use pnpm instead of npm` in the Claude chat. Where does Claude store this, and what can a team member expect about its availability?",
    "options": [
     "Claude appends it to the project `CLAUDE.md`, making it available to all teammates on next pull.",
     "Claude stores it in auto-memory (`MEMORY.md`) under `~/.claude/projects/<project>/memory/`. It is machine-local and not synced across machines.",
     "Claude stores it in `~/.claude/CLAUDE.md`, making it a cross-project user preference that applies everywhere.",
     "Claude discards `#` lines as comments; the teammate should use `/memory` instead."
    ],
    "correct": 1,
    "explanation": "The content states: '`# <text>` quick-add / \"remember this\" / \"always use pnpm\" goes to **AUTO MEMORY, not CLAUDE.md**' and 'Machine-local — not synced across machines or cloud.' The shortcut stores to auto-memory, which is scoped to the user's machine. Option A is wrong — the `#` shortcut targets auto-memory, not CLAUDE.md; to update CLAUDE.md, you must ask explicitly. Option C is wrong — auto-memory is project-scoped, not cross-project user settings. Option D is wrong — `# ` is the defined quick-add syntax for auto-memory."
   },
   {
    "id": "stage-1-q6",
    "type": "multiple-choice",
    "prompt": "You update `env.MY_TOKEN` in `.claude/settings.json` while Claude is already running. When will Claude pick up the new value?",
    "options": [
     "Immediately — settings.json changes take effect after a brief file-stability delay without a restart.",
     "After running `/hooks` in the current session to reload configuration.",
     "Only after restarting `claude` — env vars are read at startup only.",
     "Immediately, but only for newly spawned subagents, not the current session."
    ],
    "correct": 2,
    "explanation": "The content states: 'Edits to settings.json take effect after a brief file-stability delay (no restart); env vars are read **at startup only** (restart `claude` to apply).' There is an important split: non-env settings reload without restart, but env vars require a restart. Option A is partially true for hooks/permissions but not for env vars. Option B is wrong — `/hooks` shows current hook definitions but does not reload env vars. Option D invents a subagent-specific behavior not described in the content."
   },
   {
    "id": "stage-1-q7",
    "type": "multiple-choice",
    "prompt": "After a `/compact` operation, Claude seems to have forgotten the coding conventions from a subdirectory `src/api/CLAUDE.md` that were active earlier in the conversation. The project-root `CLAUDE.md` instructions are still followed. What explains this behavior?",
    "options": [
     "Subdirectory CLAUDE.md files are truncated first during compaction to save context space.",
     "Project-root `CLAUDE.md` survives `/compact` (re-injected from disk), but subdirectory CLAUDE.md files are NOT re-injected — they reload only on the next file read in that subdirectory.",
     "The subdirectory CLAUDE.md was never loaded to begin with; subdirectory files only load for write operations.",
     "Compaction clears all CLAUDE.md files including the root; the root instructions survived only because Claude cached them before compaction."
    ],
    "correct": 1,
    "explanation": "The content states: 'Project-root CLAUDE.md survives `/compact` (re-read from disk, re-injected). **Nested subdirectory CLAUDE.md and conversation-only instructions are NOT re-injected** — they reload only on the next file read in that subdir.' Option A invents a truncation-priority scheme not described in the content. Option C is wrong in two ways: subdirectory files do load before compaction (when Claude reads a file there), and they load on Read not Write. Option D is wrong — the root CLAUDE.md is actively re-read from disk after compaction, not cached."
   },
   {
    "id": "stage-1-q8",
    "type": "multi-select",
    "prompt": "Your org wants certain rules to be impossible for any individual developer to override — not through personal settings, not through project settings, not through CLI flags. Select ALL approaches that achieve this.",
    "options": [
     "Add the rules to `permissions.deny` in the team `.claude/settings.json`.",
     "Deploy the rules in managed settings (`managed-settings.json` in the system directory).",
     "Add the rules to each developer's `~/.claude/settings.json` via onboarding scripts.",
     "Deploy a managed CLAUDE.md at the managed policy path (e.g., `/etc/claude-code/CLAUDE.md` on Linux).",
     "Add the rules to CLAUDE.md with a prominent heading stating they are mandatory."
    ],
    "correct": [
     1,
     3
    ],
    "explanation": "Managed settings (`managed-settings.json`) are at the top of the precedence chain and 'always wins, even over CLI flags' — this is the correct enforcement mechanism. The managed CLAUDE.md at the system path (e.g., `/etc/claude-code/CLAUDE.md`) is also loaded first and 'Cannot [be] exclude[d]' unlike project-level CLAUDE.md files. Option A (project deny) can be overridden by managed allow or by developers removing the file from their fork. Option C (personal settings) can be overridden by the developer themselves. Option E is wrong because 'CLAUDE.md is not enforcement' — the content explicitly warns: 'use CLAUDE.md for \"we do it this way\" guidance; use permissions/hooks for security boundaries and anything that must never happen — CLAUDE.md is not enforcement.'"
   },
   {
    "id": "stage-1-q9",
    "type": "multi-select",
    "prompt": "A teammate asks what should go in the committed `.claude/settings.json` (project-level, source-controlled) vs what should stay in `.claude/settings.local.json` (gitignored). Select ALL items that belong in the committed `.claude/settings.json`.",
    "options": [
     "Team-wide `permissions.allow` rules (e.g., `Bash(npm run test *)`).",
     "A developer's personal API key stored as an `env` variable.",
     "Hooks that enforce team code-quality gates (e.g., run linter before commit).",
     "The developer's personal model preference that differs from the team default.",
     "Approved team MCP servers — wait, those go in `.mcp.json`, not `settings.json`."
    ],
    "correct": [
     0,
     2,
     4
    ],
    "explanation": "Team-wide permissions (A) and shared hooks (C) are exactly what project `settings.json` is for — they should be committed so all teammates get them. Option E is the correct placement nuance: MCP servers go in `.mcp.json` at the repo root, not in `settings.json`. Personal API keys (B) should never be committed — they belong in a gitignored local file or environment. A personal model preference (D) is a per-developer override and belongs in `settings.local.json` or `~/.claude/settings.json`. The content lists: 'Keep gitignored / personal: `.claude/settings.local.json`'."
   },
   {
    "id": "stage-1-q10",
    "type": "multiple-choice",
    "prompt": "A monorepo has three teams. Team A's CLAUDE.md at the repo root contains frontend-specific build commands that confuse the backend agents working in `services/`. A backend developer wants to suppress loading Team A's CLAUDE.md for their sessions without removing the file. What is the correct mechanism?",
    "options": [
     "Add a `CLAUDE.md` in `services/` that contradicts Team A's instructions; the more specific file wins.",
     "Use `claudeMdExcludes` in `.claude/settings.local.json` with a glob pattern matching Team A's CLAUDE.md path.",
     "Rename Team A's file to `CLAUDE.team-a.md`; Claude only loads files named exactly `CLAUDE.md`.",
     "Move the instructions into `.claude/rules/` with a `paths:` frontmatter glob scoped to frontend directories."
    ],
    "correct": 1,
    "explanation": "The content states: 'In monorepos, use `claudeMdExcludes` in `.claude/settings.local.json` to skip other teams' ancestor CLAUDE.md files.' `claudeMdExcludes` takes string[] globs and 'skip[s] loading ancestor CLAUDE.md/rules; matched against absolute paths.' Option A would add conflicting instructions but not suppress loading — the confusion would persist. Option C is wrong — renaming is a destructive change that affects everyone and is not the described mechanism. Option D is a good practice for Team A to adopt going forward but doesn't solve the current developer's problem of suppressing an existing file."
   },
   {
    "id": "stage-1-q11",
    "type": "multiple-choice",
    "prompt": "You write a CLAUDE.md import: `@../shared/team-standards.md`. The import is in `frontend/CLAUDE.md`. From which directory does Claude resolve the path `../shared/team-standards.md`?",
    "options": [
     "The current working directory where Claude was launched.",
     "The project root (where `.git` lives).",
     "The directory containing `frontend/CLAUDE.md` (i.e., the `frontend/` directory).",
     "The user's home directory `~/.claude/`."
    ],
    "correct": 2,
    "explanation": "The content states: 'relative paths resolve **relative to the file containing the import, NOT cwd**.' So `../shared/team-standards.md` resolves relative to `frontend/`, yielding `shared/team-standards.md` at the project root level. Option A is a common mistake — developers often assume imports resolve from where Claude is launched. Option B is wrong — the resolution is file-relative, not project-root-relative. Option D is obviously wrong but catches anyone who conflates CLAUDE.md imports with global user config."
   },
   {
    "id": "stage-1-q12",
    "type": "multiple-choice",
    "prompt": "A developer adds `permissions: { allow: [\"Bash(npm run build *)\"] }` to `~/.claude.json` because they want it to apply across all projects. After restarting Claude, the permission never takes effect. What is wrong?",
    "options": [
     "`~/.claude.json` requires a `version` field; without it, permissions blocks are ignored.",
     "`~/.claude.json` is not a settings file — it holds app state and OAuth session. Permissions placed there are silently ignored; they belong in `~/.claude/settings.json`.",
     "Cross-project permissions must be placed in the managed `managed-settings.json`, not in personal files.",
     "The `Bash(npm run build *)` syntax is incorrect; the correct form is `Bash(\"npm run build *\")`."
    ],
    "correct": 1,
    "explanation": "The content states: '**`~/.claude.json` is NOT a settings file** — it holds app state, theme, OAuth session, per-project trust, personal MCP servers, UI toggles. Putting `permissions`/`hooks`/`env` there is **silently ignored** (they belong in `~/.claude/settings.json`).' This is one of the most common misconfigurations. Option A invents a `version` field requirement not mentioned in the content. Option C is wrong — personal cross-project permissions go in `~/.claude/settings.json`, not managed settings (which are org-controlled). Option D is wrong — the Bash permission syntax shown is consistent with the content's examples."
   },
   {
    "id": "stage-1-q13",
    "type": "multiple-choice",
    "prompt": "Your team delegates a task to the built-in Explore subagent. The subagent ignores a critical instruction that is clearly written in the project CLAUDE.md. No other agent has this problem. What is the most likely cause?",
    "options": [
     "The Explore subagent only reads CLAUDE.md once at launch; if CLAUDE.md was updated mid-session, the subagent uses a stale version.",
     "Built-in Explore and Plan subagents skip CLAUDE.md; critical instructions must be restated in the delegating prompt.",
     "The subagent encountered the 200-line CLAUDE.md limit and the instruction was past line 200.",
     "The Explore subagent reads `CLAUDE.local.md` but not `CLAUDE.md`; the instruction should be in the local file."
    ],
    "correct": 1,
    "explanation": "The content states: 'Built-in **Explore and Plan subagents skip CLAUDE.md** — restate critical instructions in the delegating prompt.' This is a known gotcha for the specific built-in subagents. Custom subagents behave differently (they load CLAUDE.md). Option A invents a stale-version behavior; CLAUDE.md is re-read from disk. Option C confuses the 200-line adherence guideline with the MEMORY.md cap; CLAUDE.md loads in full regardless. Option D inverts the real behavior — it is the built-in subagents that skip CLAUDE.md, not a file-naming issue."
   },
   {
    "id": "stage-1-q14",
    "type": "multiple-choice",
    "prompt": "Claude is launched from the `frontend/components/` subdirectory of a monorepo. Which CLAUDE.md files are loaded at session start?",
    "options": [
     "Only `frontend/components/CLAUDE.md`, because Claude uses the cwd as the sole source.",
     "All CLAUDE.md files found by walking UP the directory tree from `frontend/components/`, plus the user and managed CLAUDE.md; subdirectory CLAUDE.md files BELOW cwd are not loaded at launch.",
     "All CLAUDE.md files in the entire repo, including those in sibling subdirectories like `frontend/utils/`.",
     "The project-root CLAUDE.md only; ancestor and subdirectory files are ignored."
    ],
    "correct": 1,
    "explanation": "The content states: 'Discovery **walks UP the directory tree** from cwd, loading every `CLAUDE.md` + `CLAUDE.local.md` found; all are concatenated (root-down, so the file closest to launch dir is read LAST).' It also states: '**Subdirectory (below cwd) CLAUDE.md files are NOT loaded at launch**.' Plus the user (`~/.claude/CLAUDE.md`) and managed policy CLAUDE.md are also in the load order. Option A is wrong — the walk goes upward, not just cwd. Option C is wrong — sibling and deeper subdirectories are not loaded at launch. Option D is wrong — ancestor directories are included in the upward walk."
   },
   {
    "id": "stage-1-q15",
    "type": "multi-select",
    "prompt": "You are reviewing onboarding docs for a new project. Which of the following claims about CLAUDE.md imports are CORRECT? Select all that apply.",
    "options": [
     "An import of `@shared/docs/standards.md` in `.claude/CLAUDE.md` resolves `shared/docs/standards.md` relative to the `.claude/` directory.",
     "Imports do not save context — imported files fully expand into context at launch.",
     "Wrapping a path in backticks (e.g., `` `@README` ``) prevents Claude from treating it as an import.",
     "If a developer declines the external import approval dialog, imports are permanently disabled for that project and the dialog never reappears.",
     "Path-scoped rules (`.claude/rules/*.md` with `paths:` frontmatter) defer loading, unlike imports which expand at launch."
    ],
    "correct": [
     0,
     1,
     2,
     3,
     4
    ],
    "explanation": "All five claims are correct and grounded in the content. (A) Relative paths resolve relative to the file containing the import. (B) 'Imports **do NOT save context** — imported files fully expand into context at launch.' (C) 'Import parsing skips Markdown code spans/fenced blocks; wrap a path in backticks to mention it literally.' (D) 'First time a project hits external imports, an approval dialog appears; **declining permanently disables imports for that project** (dialog never reappears).' (E) Path-scoped rules with `paths:` 'load only when Claude reads a matching file,' while imports expand at launch — this contrast is explicit in the content."
   }
  ],
  "masteryCheckpoint": "You can explain the concepts of Config & memory (CLAUDE.md, settings)."
 },
 {
  "id": "stage-2",
  "stage": 2,
  "title": "Permissions, security & governance",
  "summary": "Permissions, security & governance: settings scope precedence, deny/ask/allow rule evaluation, enforcement vs guidance.",
  "prerequisites": [
   "stage-1"
  ],
  "objectives": [
   "Understand the concepts in Permissions, security & governance."
  ],
  "definitions": [
   {
    "term": "settings scope precedence",
    "short": "The ranked layering of config sources (managed > CLI args > local project > shared project > user) that decides which value wins."
   },
   {
    "term": "deny/ask/allow rule evaluation",
    "short": "Permission rules are evaluated in the fixed order deny then ask then allow, with first match winning regardless of how specific a rule is."
   },
   {
    "term": "enforcement vs guidance",
    "short": "CLAUDE.md and prompts only shape what Claude tries to do; only permission rules, modes, hooks, and sandboxes actually grant or revoke access."
   },
   {
    "term": "permission modes",
    "short": "Named operating profiles (default, acceptEdits, plan, auto, dontAsk, bypassPermissions) that set the baseline prompting behavior for tool calls."
   },
   {
    "term": "protected paths",
    "short": "A built-in set of sensitive dirs and files (like .git, .claude, shell rc files, .mcp.json) whose writes are never auto-approved in any mode except bypass, even by allow rules."
   },
   {
    "term": "permission rule syntax",
    "short": "The Tool(specifier) matching language with space-as-word-boundary wildcards, compound-command awareness, and path anchors that determines exactly which tool calls a rule covers."
   }
  ],
  "sections": [
   {
    "heading": "Why Permission Architecture Matters Before You Write a Line of Config",
    "body": "Most teams treat Claude Code permissions as an afterthought — they grant broad access to stop prompts from interrupting the flow, then wonder why a CI agent deleted something it shouldn't have. Getting this backwards is the primary cause of agentic accidents.\n\nThe architecture has three distinct enforcement layers that must be understood separately before you configure any of them:\n\n1. **Rule-based permission system** — deny/ask/allow rules evaluated before a tool runs, enforced by the Claude Code process\n2. **Auto mode classifier** — a separate server-side LLM gate that reviews tool calls in auto mode against user intent and your declared infrastructure\n3. **OS-level sandbox** — Seatbelt (macOS) or bubblewrap (Linux/WSL2) enforcing kernel-enforced isolation on Bash processes and their children\n\nThese layers are complementary, not redundant. A team that only configures layer 1 has no real isolation from a compromised subcommand that slips through string-based rule matching. A team that only sandboxes has no control over which tools the model even sees, or which commands it attempts. A team using auto mode without configuring trusted infrastructure will fight constant false positives on routine internal operations.\n\nA fourth layer — **CLAUDE.md and prompt instructions** — is important but provides zero enforcement. It shapes what Claude tries to do, not what Claude Code allows. The enforcement/guidance distinction is the single most commonly misunderstood concept in this space, and this stage covers it explicitly.\n\nUnderstanding all layers before touching config prevents the two failure modes teams fall into: over-permissioning to silence prompts, and thinking guidance rules are the same as enforcement."
   },
   {
    "heading": "Settings Scope Precedence: Which Value Wins",
    "body": "Claude Code merges configuration from five sources in a strict precedence chain. When the same scalar key appears in multiple scopes, the highest-precedence scope wins. Permission rules are the notable exception — they **merge** across all scopes rather than override.\n\n| Priority | Scope | Location | Shared? | Can override lower? |\n|----------|-------|----------|---------|---------------------|\n| 1 (highest) | Managed | System directory or MDM; see below | All users on machine | Yes — cannot be overridden by anything |\n| 2 | CLI args | `--permission-mode`, `--allowedTools`, etc. | No | Yes |\n| 3 | Local project | `.claude/settings.local.json` | No (gitignored) | Yes |\n| 4 | Shared project | `.claude/settings.json` | Yes (committed) | Yes |\n| 5 (lowest) | User | `~/.claude/settings.json` | No | Baseline |\n\nNote: the official docs describe this as a **4-tier** scope system (managed, user, project, local), with CLI args handled separately as session-scoped overrides. The precedence chain above reflects the full ordering in practice.\n\n### File layout\n\n```\n# User-level baseline (all projects, not shared)\n~/.claude/settings.json\n\n# MCP server configuration (user-level)\n~/.claude.json\n\n# Project defaults (checked into git, applies to all collaborators)\n.claude/settings.json\n\n# MCP server configuration (project-level)\n.mcp.json\n\n# Personal overrides for this project (gitignored, never committed)\n.claude/settings.local.json\n\n# Admin-deployed, highest authority (file-based options):\n#  macOS:   /Library/Application Support/ClaudeCode/managed-settings.json\n#  Linux:   /etc/claude-code/managed-settings.json\n#  Windows: C:\\Program Files\\ClaudeCode\\managed-settings.json  (v2.1.75+)\n#  Drop-in: managed-settings.d/*.json (merged alphabetically on top of base)\n#\n# Windows legacy path (deprecated as of v2.1.75):\n#  C:\\ProgramData\\ClaudeCode\\managed-settings.json\n```\n\nClaude Code creates timestamped backups of settings files and retains the 5 most recent. The `$schema` key is supported for autocomplete in VS Code and Cursor:\n\n```json\n{\n  \"$schema\": \"https://json.schemastore.org/claude-code-settings.json\"\n}\n```\n\n### Permission rule merging\n\nFor `permissions.allow`, `.deny`, and `.ask`, **all rules from all scopes are combined**. A deny rule in user settings blocks even if an allow rule exists in project settings, because deny is evaluated first regardless of which scope it came from. There is no way to override a deny with an allow at a lower scope — and this is intentional.\n\n```json\n// ~/.claude/settings.json (user)\n{\n  \"permissions\": {\n    \"deny\": [\"Bash(curl *)\"],\n    \"allow\": [\"Bash(npm run lint)\"]\n  }\n}\n\n// .claude/settings.json (project)\n{\n  \"permissions\": {\n    \"allow\": [\"Bash(npm run test *)\", \"Read(src/**)\"],\n    \"deny\": [\"Read(./.env)\"]\n  }\n}\n\n// Effective result: all four rules apply simultaneously.\n// Deny: Bash(curl *), Read(./.env)\n// Allow: Bash(npm run lint), Bash(npm run test *), Read(src/**)\n```\n\nTo prevent user and project settings from injecting any permission rules at all, set `allowManagedPermissionRulesOnly: true` in managed settings. Only rules in managed settings will then apply.\n\n### MDM deployment for managed settings\n\nFor IT-managed fleets, managed settings can be delivered via:\n- **macOS MDM (Jamf, Kandji/Iru)**: plist domain `com.anthropic.claudecode`\n- **Windows Group Policy / Intune**: registry `HKLM\\SOFTWARE\\Policies\\ClaudeCode`, value `Settings` (JSON string)\n- **Windows user-level**: `HKCU\\SOFTWARE\\Policies\\ClaudeCode` (lower priority than HKLM)\n- **File drop**: the system directory above, or a `managed-settings.d/` fragment directory\n\nWhen using `managed-settings.d/`, files are merged on top of `managed-settings.json` in alphabetical order. For arrays, entries are **concatenated and de-duplicated** (not replaced). For scalars, later files override earlier ones. Use numeric prefixes to control merge order:\n\n```\nmanaged-settings.d/\n  10-security.json\n  20-tools.json\n  30-network.json\n```\n\nAnthropic publishes ready-to-use starter templates for Jamf, Kandji (Iru), Intune, and Group Policy at `https://github.com/anthropics/claude-code/tree/main/examples/mdm`.\n\n**Critical**: settings in the managed layer cannot be overridden by `--allowedTools`, user settings, or project settings. If a tool is denied in managed settings, no developer or project config can un-deny it. Invalid entries in managed settings are stripped tolerantly (v2.1.169+) — valid policies remain enforced. Run `claude doctor` to list each invalid entry with its source file."
   },
   {
    "heading": "Deny / Ask / Allow Rule Evaluation: First Match Wins, Order Is Fixed",
    "body": "Rules are evaluated in exactly this order, regardless of specificity or where the rule is defined:\n\n```\n1. deny   → first matching deny rule blocks the call, stops evaluation\n2. ask    → first matching ask rule prompts the user, stops evaluation\n3. allow  → first matching allow rule auto-approves, stops evaluation\n4. (no match) → default behavior for the active permission mode\n```\n\nThis has a non-obvious consequence: **a broad deny rule beats a narrow allow rule, unconditionally**. You cannot write exceptions to a deny. The docs are explicit: `Bash(aws *)` in deny blocks every `aws` call, including one that also matches `Bash(aws s3 ls)` in allow. The same precedence applies to ask vs. allow: a matching ask rule prompts even when a more specific allow rule also matches the same call.\n\nIf you need exceptions, invert the structure — don't deny a broad pattern, instead deny the dangerous subcommands individually and allow the safe ones.\n\n### Bare tool name vs. scoped pattern\n\nA bare tool name like `Bash` (or equivalently `Bash(*)`) as a deny rule **removes the tool from Claude's context entirely** — the model never sees it as available. A scoped pattern like `Bash(rm *)` leaves the tool available but blocks matching calls when Claude attempts them. This distinction matters: bare-name deny rules are more aggressive and should be used when you want to completely hide a capability from the model rather than just block specific invocations.\n\nTool-name wildcards are also supported in deny and ask rules: `mcp__*` matches every MCP tool across all servers. A tool matched by a bare-name glob deny rule is likewise removed from context. Allow rules accept tool-name globs only with a literal `mcp__<server>__` prefix (server segment must be glob-free). An unanchored allow glob like `\"*\"` or `\"B*\"` is skipped with a startup warning.\n\n### Rule evaluation and hooks\n\n`PreToolUse` hooks add a third evaluation surface but interact with the rule system asymmetrically:\n- A hook that **blocks** (exits with code 2) takes precedence over allow rules — the call is stopped before allow rules are evaluated.\n- A hook that **allows** does **not** override deny or ask rules — those rules still apply after the hook returns.\n\nThe rule: hooks can tighten but cannot loosen the permission system. Use hooks for dynamic logic (checking a database, calling an external policy service) where static rules aren't expressive enough.\n\n### Common pitfall: deny precedence surprises\n\n```json\n// WRONG — attempting to allow an exception to a broad deny:\n{\n  \"permissions\": {\n    \"deny\": [\"Bash(aws *)\"],\n    \"allow\": [\"Bash(aws s3 ls)\"]  // NEVER REACHED — deny fires first\n  }\n}\n\n// RIGHT — if you want only specific aws commands:\n{\n  \"permissions\": {\n    \"deny\": [\n      \"Bash(aws s3 rm *)\",\n      \"Bash(aws iam *)\",\n      \"Bash(aws ec2 terminate-instances *)\"\n    ],\n    \"allow\": [\n      \"Bash(aws s3 ls *)\",\n      \"Bash(aws s3 cp *)\",\n      \"Bash(aws cloudwatch *)\"\n    ]\n  }\n}\n```\n\nNote: rules can also match arbitrary tool input parameters using `Tool(param:value)` syntax. For example, `Agent(model:opus)` blocks subagent calls that request the Opus model tier, and `Bash(run_in_background:true)` blocks background Bash calls. Each rule names one parameter; write two rules to gate on two parameters. Parameter matching only applies to deny and ask rules — allow rules continue to use each tool's own specifier syntax."
   },
   {
    "heading": "Permission Rule Syntax: The Matching Language in Full",
    "body": "The `Tool(specifier)` syntax is more capable and subtler than it first appears. Knowing the exact matching semantics prevents both over-permissioning and rule-writing that silently fails.\n\n### Basic structure\n\n```\nBash(npm run test *)            # Bash command matching\nRead(~/.zshrc)                  # File read by path\nEdit(/src/**/*.ts)              # File edit by pattern\nWrite(./.env)                   # File write\nWebFetch(domain:example.com)    # Web fetch by hostname\nmcp__puppeteer__navigate        # Specific MCP tool\nmcp__puppeteer__*               # All tools from a server\nAgent(Explore)                  # Subagent by name\nAgent(model:opus)               # Subagent by input parameter (deny/ask only)\nBash(run_in_background:true)    # Bash by input parameter (deny/ask only)\n```\n\n### Bash wildcard semantics — the space boundary\n\nThe space-before-`*` rule is the most commonly misunderstood behavior:\n\n```\nBash(ls *)     → matches \"ls -la\" but NOT \"lsof\"   (space enforces word boundary)\nBash(ls*)      → matches \"ls -la\" AND \"lsof\"        (no boundary)\nBash(npm *)    → matches any npm subcommand\nBash(git * main) → matches \"git checkout main\", \"git push origin main\", etc.\n```\n\nA single `*` matches any sequence of characters including spaces, so one wildcard can span multiple arguments. `Bash(git *)` matches `git log --oneline --all`.\n\nThe `:*` suffix is an equivalent way to write a trailing wildcard: `Bash(ls:*)` matches the same commands as `Bash(ls *)`. The `:*` form is only recognized at the end of a pattern; a colon in a middle position like `Bash(git:* push)` is treated as a literal character.\n\n### Compound command awareness\n\nClaude Code parses shell operators (`&&`, `||`, `;`, `|`, `|&`, `&`, newlines) and checks each subcommand independently. A single rule cannot approve a compound command by itself — each part must independently match a rule:\n\n```bash\n# The rule Bash(safe-cmd *) does NOT approve:\nsafe-cmd --args && other-cmd   # other-cmd has no matching allow rule\n```\n\nWhen you approve a compound command interactively with \"Yes, don't ask again\", Claude Code saves a separate rule for each subcommand requiring approval (up to 5 rules per compound). Future invocations of `npm test` are recognized independently of whatever preceded the `&&`.\n\n### Process wrapper stripping\n\nA fixed set of wrappers is stripped before matching: `timeout`, `time`, `nice`, `nohup`, and `stdbuf`. So `Bash(npm test *)` also approves `timeout 30 npm test`.\n\nBare `xargs` (without flags) is also stripped, so `Bash(grep *)` matches `xargs grep pattern`. When `xargs` has flags (`xargs -n1 grep pattern`), it is matched as an `xargs` command — rules written for the inner command do not cover it.\n\nThis wrapper list is built-in and not configurable. Development environment runners like `direnv exec`, `devbox run`, `mise exec`, `npx`, and `docker exec` are **not** stripped. A rule like `Bash(devbox run *)` matches whatever follows `run`, including `devbox run rm -rf .`. Write specific rules that include both the runner and the inner command, e.g. `Bash(devbox run npm test)`.\n\nExec wrappers such as `watch`, `setsid`, `ionice`, and `flock` always prompt and cannot be auto-approved by a prefix rule. The same applies to `find` with `-exec` or `-delete`.\n\n### Read-only commands (built-in set, not configurable)\n\nClaude Code recognizes a built-in set as read-only and runs them without a prompt in every mode: `ls`, `cat`, `echo`, `pwd`, `head`, `tail`, `grep`, `find`, `wc`, `which`, `diff`, `stat`, `du`, `cd`, and read-only forms of `git`. To require a prompt for one of these, add an `ask` or `deny` rule.\n\n### File path anchoring (Read, Edit, Write)\n\nRead and Edit rules follow gitignore conventions with four distinct anchor prefixes:\n\n| Pattern | Anchors to | Example | Matches |\n|---------|-----------|---------|--------|\n| `//path` | Filesystem root (absolute) | `Read(//etc/passwd)` | `/etc/passwd` |\n| `~/path` | Home directory | `Read(~/.ssh/*)` | `$HOME/.ssh/*` |\n| `/path` | Project root | `Edit(/src/**/*.ts)` | `<project-root>/src/**/*.ts` |\n| `path` or `./path` | Current working directory | `Read(.env)` | `<cwd>/**/.env` (any depth) |\n\n**Warning**: `/Users/alice/file` is NOT an absolute path — it is relative to the project root. Use `//Users/alice/file` for absolute paths on macOS/Linux.\n\nIn gitignore semantics, `*` matches within one path segment; `**` matches across segments. A bare filename like `Read(.env)` is equivalent to `Read(**/.env)` and matches at any depth under the current directory.\n\nSymlinks receive extra treatment: allow rules apply only when both the symlink path **and** its target match; deny rules apply when **either** matches. So `Read(./project/**)` allowed + `Read(~/.ssh/**)` denied blocks a symlink at `./project/key` that points to `~/.ssh/id_rsa`.\n\nOn Windows, paths are normalized to POSIX form before matching: `C:\\Users\\alice` becomes `/c/Users/alice`. Use `//c/**/.env` to match files on that drive.\n\n### WebFetch domain matching\n\n```\nWebFetch(domain:example.com)       → exact hostname match\nWebFetch(domain:*.example.com)     → any subdomain at any depth (not the root itself)\nWebFetch(domain:*)                 → all domains (same as bare WebFetch)\n```\n\n`WebFetch(domain:example.*)` matches `example.org` (where `*` becomes `org`) but **not** `example.evil.com` (where `*` would have to cross a dot). This prevents an attacker from registering a lookalike domain that matches a trailing wildcard.\n\n### Fragile patterns to avoid\n\nURL-constraining Bash rules are unreliable and explicitly warned against in the docs:\n\n```bash\n# This looks like it restricts curl to GitHub, but it doesn't:\nBash(curl http://github.com/ *)  # Fails on: curl -X GET http://...\n                                  # Fails on: HTTPS, redirects, $URL variables\n                                  # Fails on: extra spaces, different flag order\n```\n\nUse `WebFetch(domain:github.com)` for read-only web fetch, and a deny rule `Bash(curl *)` if you want to block curl in Bash entirely. Note that using WebFetch alone does not prevent network access — if Bash is allowed, Claude can still use `curl`, `wget`, or other tools to reach any URL."
   },
   {
    "heading": "Enforcement vs. Guidance: The Critical Distinction",
    "body": "This is the most commonly misunderstood concept in Claude Code security, and getting it wrong creates a false sense of security.\n\n**CLAUDE.md and prompts are guidance.** They tell Claude what to try to do. A CLAUDE.md line saying \"never delete production data\" makes Claude less likely to write such a command, but it provides zero enforcement. The model can misinterpret, be confused by prompt injection, or simply generate a command that violates the instruction.\n\n**Permission rules, modes, hooks, and the sandbox are enforcement.** These mechanisms operate at the Claude Code layer (not the model layer) and take effect regardless of what the model outputs. The docs are explicit on this boundary:\n\n> \"Permission rules are enforced by Claude Code, not by the model. Instructions in your prompt or `CLAUDE.md` shape what Claude tries to do, but they don't change what Claude Code allows.\"\n\nIf a deny rule blocks `Bash(aws s3 rm *)`, that call never executes even if the model generates it.\n\n### Practical implications for your team setup\n\n| Control mechanism | Layer | Can be bypassed by model error? | Can be bypassed by prompt injection? |\n|------------------|-------|--------------------------|--------------------------------------|\n| CLAUDE.md instruction | Model (guidance) | Yes | Yes |\n| Prompt instruction | Model (guidance) | Yes | Yes |\n| `permissions.deny` rule | Claude Code (userspace) | Harder (string-based matching) | Harder |\n| `permissions.ask` rule | Claude Code (prompts human) | No — human must approve | Difficult — human reviews |\n| Auto mode classifier | Server-side LLM gate | No (separate process) | Hardened — tool results stripped from classifier |\n| Bash sandbox (Seatbelt/bubblewrap) | OS kernel | No | No |\n\n### The important nuance: userspace vs. kernel enforcement\n\nPermission rules are evaluated by the Claude Code process (userspace). Rule matching is string-based: if a prompt injection causes Claude to generate a cleverly-formatted command string that slips through pattern matching, it executes. The Bash sandbox is enforced by the OS kernel — it applies to every Bash command and its child processes regardless of how Claude decided to run them. These two layers are not redundant; they defend against different failure modes.\n\n### Design principle: use guidance to reduce prompts, use enforcement to set boundaries\n\nA CLAUDE.md that says \"prefer `git commit` over amend\" reduces the frequency of amend calls. A deny rule `Bash(git commit --amend *)` blocks them entirely. Use both: guidance shapes intent, enforcement provides the guarantee. Don't rely solely on guidance for any operation that would be harmful if it executed."
   },
   {
    "heading": "Permission Modes: The Six Operating Profiles",
    "body": "Permission modes set the baseline prompting behavior. Rules and sandbox restrictions layer on top regardless of mode. The mode is a session property, set with `Shift+Tab` in the CLI to cycle through the default rotation, or as a persistent default:\n\n```json\n// .claude/settings.json or ~/.claude/settings.json\n{\n  \"permissions\": {\n    \"defaultMode\": \"acceptEdits\"\n  }\n}\n```\n\nOr at startup: `claude --permission-mode plan`\n\nIn every mode except `bypassPermissions`, writes to [protected paths](#protected-paths-the-built-in-safety-net) are never auto-approved. Allow rules in settings files do not change this.\n\n### Mode comparison table\n\n| Mode | Auto-approves without prompting | Bash auto-run | Best for |\n|------|--------------------------------|---------------|----------|\n| `default` | Reads only | Built-in read-only set only | Getting started, sensitive work |\n| `acceptEdits` | Reads, file edits, filesystem Bash (mkdir/touch/mv/cp/rm/rmdir/sed) in-scope | In-scope filesystem commands | Iterating on code you're reviewing |\n| `plan` | Reads only | Read-only Bash only | Exploring a codebase before changing it |\n| `auto` | Everything, with classifier gate | Classifier gate | Long tasks, reducing prompt fatigue |\n| `dontAsk` | Only pre-approved via allow rules | Pre-approved only; ask rules denied | Locked-down CI and scripts |\n| `bypassPermissions` | Everything (including protected paths) | Everything | Isolated containers/VMs only |\n\n### `default` — safe starting point\n\nEach tool call requires an allow rule or user confirmation. A built-in set of read-only Bash commands (`ls`, `cat`, `grep`, `find`, `git status`, and others) runs without a prompt; this set is not configurable. Add an `ask` or `deny` rule to override a specific read-only command.\n\n### `acceptEdits` — file-editing automation\n\nAuto-approves file edits and a specific set of filesystem Bash commands (`mkdir`, `touch`, `rm`, `rmdir`, `mv`, `cp`, `sed`) for paths in the working directory or `additionalDirectories`. These are also auto-approved when prefixed with safe environment variables like `LANG=C` or process wrappers like `timeout`. Paths outside scope and all other Bash still prompt. Useful when you want to review `git diff` after the fact rather than approving inline.\n\n### `plan` — read before write\n\nClaude reads files and runs read-only shell commands to explore, but makes no edits. When the plan is ready, Claude presents it and offers to proceed in auto, acceptEdits, or default mode. Set as default for teams that want a mandatory planning step:\n\n```json\n// .claude/settings.json — enforce plan-first for the project\n{\n  \"permissions\": {\n    \"defaultMode\": \"plan\"\n  }\n}\n```\n\nYou can also prefix a single prompt with `/plan` to enter plan mode for that turn only.\n\n### `auto` — classifier-gated autonomy (research preview)\n\nThe most powerful and most nuanced mode. A separate classifier model runs before each tool call and blocks anything that escalates beyond your request, targets unrecognized infrastructure, or appears driven by hostile content. Broad allow rules (`Bash(*)`, `PowerShell(*)`, wildcarded interpreters, package-manager run commands, `Agent` allow rules) are **dropped on auto mode entry** and restored on exit. Narrow rules like `Bash(npm test)` carry over.\n\n**Requirements:** All plans. On Team/Enterprise, an admin must enable it in the Claude Code admin console. Models supported: Anthropic API requires Claude Opus 4.6+ or Sonnet 4.6; on Amazon Bedrock, Google Cloud Vertex AI, and Microsoft Foundry only Opus 4.7 and Opus 4.8 are supported, and `CLAUDE_CODE_ENABLE_AUTO_MODE=1` must be set.\n\n`auto` cannot be set as `defaultMode` in project settings (`.claude/settings.json`) or local settings (`.claude/settings.local.json`) — the setting is silently ignored there (v2.1.142+). Move it to `~/.claude/settings.json` for user-default, or managed settings for org-wide default. This prevents a repository from granting itself auto mode.\n\nAdministrators can lock off auto mode with `permissions.disableAutoMode: \"disable\"` in managed settings.\n\n### `dontAsk` — pre-approved-only for CI\n\nAuto-denies anything that would otherwise prompt. Only calls matching `permissions.allow` rules and built-in read-only Bash commands execute. Ask rules are denied rather than prompting. Fully non-interactive — suitable for CI pipelines where you've enumerated exactly what Claude may do.\n\n```bash\nclaude --permission-mode dontAsk -p \"run the test suite and report results\"\n```\n\nNote: `defaultMode: \"dontAsk\"` is ignored in cloud sessions on Claude Code on the web.\n\n### `bypassPermissions` — containers and VMs only\n\nSkips all permission prompts including for protected paths (as of v2.1.126). Explicit ask rules still force a prompt, and `rm -rf /` and `rm -rf ~` still prompt as a circuit breaker. **Never use this on a developer workstation.** Explicitly blocked when running as root on Linux/macOS.\n\nYou cannot enter this mode mid-session without having started with an enabling flag (`--permission-mode bypassPermissions` or `--dangerously-skip-permissions`). Administrators can prevent it entirely:\n\n```json\n// managed-settings.json\n{\n  \"permissions\": {\n    \"disableBypassPermissionsMode\": \"disable\"\n  }\n}\n```\n\n`defaultMode: \"bypassPermissions\"` is ignored in cloud sessions on Claude Code on the web."
   },
   {
    "heading": "Protected Paths: The Built-In Safety Net",
    "body": "A built-in set of paths is never auto-approved in any mode except `bypassPermissions`. These protect repository state and Claude's own configuration from accidental corruption. `permissions.allow` rules do **not** override this — the protected-path check runs before allow rules from settings are evaluated.\n\n| Mode | Protected-path writes |\n|------|-----------------------|\n| `default`, `acceptEdits`, `plan` | Prompted |\n| `auto` | Routed to the classifier |\n| `dontAsk` | Denied |\n| `bypassPermissions` | Allowed |\n\n### Protected directories\n\n```\n.git\n.config/git\n.vscode\n.idea\n.husky\n.cargo\n.devcontainer\n.yarn\n.mvn\n.claude           (except .claude/worktrees where Claude stores its own git worktrees)\n```\n\n### Protected files (complete list)\n\n```\n# Git configuration\n.gitconfig  .gitmodules\n\n# Shell rc files\n.bashrc  .bash_profile  .bash_login  .bash_aliases  .bash_logout\n.zshrc  .zprofile  .zshenv  .zlogin  .zlogout  .profile  .envrc\n\n# Package manager configuration\n.npmrc  .yarnrc  .yarnrc.yml  .pnp.cjs  .pnp.loader.mjs\n.pnpmfile.cjs  bunfig.toml  .bunfig.toml\n\n# Build tooling\n.bazelrc  .bazelversion  .bazeliskrc\n\n# Git hook managers\n.pre-commit-config.yaml\nlefthook.yml  lefthook.yaml  .lefthook.yml  .lefthook.yaml\n\n# Build wrappers\ngradle-wrapper.properties  maven-wrapper.properties\n\n# Dev container config\n.devcontainer.json\n\n# Tool config\n.ripgreprc  pyrightconfig.json\n\n# Claude Code own config\n.mcp.json  .claude.json\n```\n\n### Gotcha: allow rules don't help here\n\n```json\n// This does NOT auto-approve writes to .claude/ in acceptEdits mode:\n{\n  \"permissions\": {\n    \"allow\": [\"Edit(.claude/**)\"]\n  }\n}\n// The protected-path check runs before this rule is evaluated.\n```\n\nWhen a `.claude/` write is prompted, the dialog offers **\"Yes, and allow Claude to edit its own settings for this session\"**. That approval persists for the rest of the session without prompting again — it's a session-scoped exception, not a permanent rule.\n\n### Sandbox and protected paths\n\nWhen sandboxing is enabled, the sandbox automatically denies write access to Claude Code's settings files at every scope and to the managed settings directory, so a sandboxed command cannot modify its own policy. This protection holds even in auto-allow sandbox mode."
   },
   {
    "heading": "The Auto Mode Classifier: Architecture and Limits",
    "body": "Understanding how the auto mode classifier works is essential for both configuring it effectively and knowing what it cannot protect against.\n\n### Decision order inside the classifier\n\nFor each tool call, evaluation proceeds in this fixed order:\n\n1. Actions matching **deny or ask rules** resolve immediately (deny blocks, ask prompts), skipping the classifier\n2. **Writes to protected paths** route to the classifier even when an allow rule matches\n3. **Read-only actions and file edits in the working directory** are auto-approved, except writes to protected paths\n4. Everything else goes to the **classifier**\n5. If the classifier blocks, Claude receives the reason and may try an alternative\n\nOn entering auto mode, broad allow rules that grant arbitrary code execution are dropped: `Bash(*)`, `PowerShell(*)`, wildcarded interpreters, package-manager run commands, and `Agent` allow rules. Narrow rules like `Bash(npm test)` carry over. Dropped rules are restored when you leave auto mode.\n\n### What the classifier sees — and what it doesn't\n\nThe classifier receives: user messages, tool calls, and CLAUDE.md content. **Tool results are stripped.** This is the key hardening decision: a malicious string in a file Claude reads cannot influence the classifier's decision about the next action. A separate server-side probe scans incoming tool results and flags suspicious content before Claude reads it.\n\nThis also means: the classifier evaluates actions against user intent as stated in the conversation, not against the content Claude discovered while executing.\n\n### Boundaries you state in conversation\n\nThe classifier treats explicit boundaries you state as a block signal. If you tell Claude \"don't push\" or \"wait until I review before deploying\", the classifier blocks matching actions even when the default rules would allow them. A boundary stays in force until you lift it in a later message — Claude's own judgment that a condition was met does not lift it.\n\nBoundaries are not stored as rules. The classifier re-reads them from the transcript on each check, so a boundary can be lost if context compaction removes the message that stated it. For a hard guarantee, add a `permissions.deny` rule instead.\n\n### Default blocks (partial list)\n\n```\ncurl | bash                         # download-and-execute\nSending sensitive data externally   # data exfiltration\nProduction deploys/migrations       # irreversible remote state\ngit push to main / force push      # shared branch modification\ngit reset --hard / git checkout -- . / git restore . / git clean -fd  # discards uncommitted work\ngit commit --amend (pre-session commit)  # modifies history you didn't create this session\nterraform/pulumi/cdk/terragrunt destroy  # irreversible infra destruction\nApplying a plan that destroys resources\nIAM or repo permission grants\nMass deletion on cloud storage\n```\n\n**Allowed by default**: local file operations in your working directory, installing dependencies in lock files/manifests, reading `.env` and sending credentials to their matching API, read-only HTTP requests, pushing to the branch you started on or one Claude created.\n\n### Configuring trusted infrastructure\n\nThe classifier's definition of \"external\" is: anything not in your working directory or the repo's configured remotes. Your company's GitHub org, internal CI, artifact stores — all treated as external until you configure otherwise. Use `autoMode.environment` in `~/.claude/settings.json` or managed settings:\n\n```json\n{\n  \"autoMode\": {\n    \"environment\": [\n      \"$defaults\",\n      \"Organization: Acme Corp. Primary use: software development.\",\n      \"Source control: github.acme.com/acme-corp and all repos under it\",\n      \"Cloud providers: AWS us-east-1. Trusted buckets: s3://acme-build-artifacts\",\n      \"Trusted internal domains: *.corp.acme.com, api.internal.acme.com\",\n      \"Key internal services: Jenkins at ci.acme.com, Artifactory at artifacts.acme.com\"\n    ]\n  }\n}\n```\n\nEntries are **natural language prose**, not regex. The classifier reads them as an LLM would.\n\n**Critical**: the classifier does not read `autoMode` from shared project settings (`.claude/settings.json`). It reads from managed settings, user settings (`~/.claude/settings.json`), and per-project local settings (`.claude/settings.local.json`). A committed repo cannot inject its own classifier allow rules.\n\n### Three rule tiers inside the classifier\n\nBeyond `environment`, you can override the built-in rule lists:\n\n| Field | Behavior |\n|-------|----------|\n| `autoMode.hard_deny` | Unconditional blocks — user intent and allow exceptions do not apply |\n| `autoMode.soft_deny` | Blocks that user intent or `allow` exceptions can override |\n| `autoMode.allow` | Exceptions that override matching soft_deny rules |\n\n### The `$defaults` sentinel\n\nOmitting `\"$defaults\"` from any array **replaces** the entire built-in list for that section. A `hard_deny` without `\"$defaults\"` discards the built-in data exfiltration and auto-mode bypass rules. A `soft_deny` without `\"$defaults\"` discards force push, `curl | bash`, and production deploy blocks. Always include `\"$defaults\"` unless you're intentionally taking full ownership of the list and have copied the defaults explicitly.\n\nInspect your effective config:\n```bash\nclaude auto-mode defaults    # print built-in rules\nclaude auto-mode config      # show effective config with your settings applied\nclaude auto-mode critique    # get AI feedback on custom rules\n```\n\n### When auto mode falls back\n\nIf the classifier blocks the same action 3 consecutive times or 20 times total (session-level), auto mode pauses and Claude Code resumes prompting. Approving the prompted action resumes auto mode. The consecutive counter resets on any allowed action; the total counter resets only when its own limit triggers a fallback. In non-interactive mode (`-p` flag), repeated blocks abort the session.\n\n### Auto mode classifier is a second gate, not isolation\n\nThe classifier reduces risk but is not a hard isolation boundary. The Bash sandbox is the only true OS-level boundary. The classifier's judgment can be wrong — that's why the docs label it a \"research preview.\""
   },
   {
    "heading": "Bash Sandbox Isolation: The Only Real OS-Level Enforcement Boundary",
    "body": "Permission rules decide whether a tool call runs. The sandbox decides what a running Bash process can actually access. These are fundamentally different enforcement mechanisms.\n\nPermission rules are evaluated by Claude Code (a userspace process). If a prompt injection causes Claude to generate a command that slips through rule matching, it executes at the process's full OS permissions. The sandbox is enforced by the OS kernel and applies to every Bash command and its child processes, regardless of how Claude decided to run them.\n\n### OS primitives\n\n- **macOS**: Seatbelt framework (built-in, nothing to install)\n- **Linux/WSL2**: bubblewrap (`sudo apt-get install bubblewrap socat`) + optional seccomp filter (`npm install -g @anthropic-ai/sandbox-runtime`)\n- **WSL1**: not supported — upgrade to WSL2 or run without sandboxing\n- **Windows native**: not supported — run inside WSL2\n\nOn Ubuntu 24.04 and later, AppArmor may prevent bubblewrap from creating user namespaces. Check with `sysctl kernel.apparmor_restrict_unprivileged_userns`; if it returns `1`, add an AppArmor profile granting `bwrap` the `userns` capability (see official docs for the profile).\n\n### Enable and configure\n\n```bash\n/sandbox    # opens the sandbox panel in Claude Code\n```\n\nThe panel has three tabs: **Mode** (auto-allow vs. regular permissions), **Overrides** (the `allowUnsandboxedCommands` escape hatch), and **Config** (resolved settings). If only a Dependencies tab appears, a required package is missing.\n\nOr in settings:\n\n```json\n// .claude/settings.local.json (project, gitignored)\n// or ~/.claude/settings.json (user-level, all projects)\n// or managed-settings.json (org-wide)\n{\n  \"sandbox\": {\n    \"enabled\": true,\n    \"failIfUnavailable\": true,\n    \"allowUnsandboxedCommands\": false,\n    \"filesystem\": {\n      \"allowWrite\": [\"~/.kube\", \"/tmp/build\"],\n      \"denyRead\": [\"~/\"],\n      \"allowRead\": [\".\"]\n    },\n    \"network\": {\n      \"allowedDomains\": [\"registry.npmjs.org\", \"*.github.com\"]\n    }\n  }\n}\n```\n\n**Path prefix semantics differ from permission rules**: in sandbox filesystem settings, `/tmp/build` is an absolute path (not project-relative). `~/path` is home-relative. `./path` or bare `path` in project settings resolves to the project root; in user settings it resolves to `~/.claude`. This is different from the `//path` convention used in `Read`/`Edit` permission rules.\n\n### What the sandbox restricts by default\n\n| Access type | Default behavior |\n|------------|------------------|\n| Writes | Only working directory and session `$TMPDIR` |\n| Reads | Entire filesystem (including `~/.aws/credentials`, `~/.ssh/`) |\n| Network | No domains pre-allowed; each new domain prompts for approval |\n| Git worktrees | Writes to the main repo's shared `.git` allowed (except `hooks/` and `config`) |\n\n**Credential directories are readable by default.** If you don't want sandboxed commands reading AWS or SSH keys, add them to `denyRead` explicitly.\n\nThe session temp directory (`$TMPDIR`) is writable by default. Sandboxed and unsandboxed commands resolve `$TMPDIR` to different directories; to share temp files between the two, write them under the working directory instead.\n\n### Sandbox modes\n\n**Auto-allow mode**: sandboxed Bash commands run without prompting. Commands that can't run sandboxed fall back to the regular permission flow. Explicit deny rules still apply. `rm`/`rmdir` targeting `/`, the home directory, or other critical paths still prompt. Content-scoped ask rules like `Bash(git push *)` still force a prompt. A bare `Bash` ask rule is skipped for commands that run sandboxed; it still applies to commands that fall back.\n\n**Regular permissions mode**: all Bash commands go through the regular permission flow even when sandboxed. More control, more approvals.\n\nThe sandbox's auto-allow mode is **separate from** the auto mode permission mode. They work independently and can be combined.\n\n### Sandbox vs. permissions — key differences\n\n| | Permission rules | Bash sandbox |\n|--|-----------------|-------------|\n| Enforced by | Claude Code (userspace) | OS kernel |\n| Applies to | All tools (Bash, Read, Edit, WebFetch, MCP) | Bash commands and child processes only |\n| Bypassed by string-matched rule gap | Possible | No |\n| Bypassed by prompt injection | Harder but possible | No |\n| Configures what? | Which tool calls are permitted | What permitted calls can access |\n\n### Organization-wide sandbox enforcement\n\n```json\n// managed-settings.json — enforce strict sandbox for all developers\n{\n  \"sandbox\": {\n    \"enabled\": true,\n    \"failIfUnavailable\": true,\n    \"allowUnsandboxedCommands\": false,\n    \"filesystem\": {\n      \"denyRead\": [\"~/\"],\n      \"allowRead\": [\".\"]\n    },\n    \"network\": {\n      \"allowedDomains\": [\"registry.npmjs.org\", \"*.github.com\", \"github.com\"]\n    }\n  },\n  \"sandbox.network.allowManagedDomainsOnly\": true\n}\n```\n\nFor arrays (`allowRead`, `excludedCommands`, `allowedDomains`), Claude Code **merges** entries from every scope — a developer can always append entries that widen the policy. Set `allowManagedDomainsOnly: true` or `sandbox.filesystem.allowManagedReadPathsOnly: true` to lock those surfaces to managed values only. There is no equivalent lockdown for `excludedCommands`.\n\n### Known incompatibilities\n\n- `docker`: incompatible — add `docker *` to `excludedCommands`\n- `jest` with watchman: use `jest --no-watchman`\n- Go-based CLIs (`gh`, `gcloud`, `terraform`) with TLS on macOS under Seatbelt: add to `excludedCommands`\n- `open`, `osascript`, Apple Events: blocked by default; `allowAppleEvents: true` enables them but removes code-execution isolation (honored from user/managed/CLI settings only — project settings are ignored for this key)\n- Bubblewrap inside unprivileged containers: set `enableWeakerNestedSandbox: true` only when the outer container already provides isolation\n- WSL2: sandboxed commands cannot launch Windows binaries (`cmd.exe`, `powershell.exe`, anything under `/mnt/c/`) — add to `excludedCommands`"
   },
   {
    "heading": "Commercial Account Terms: Training Data and Data Handling",
    "body": "This is not a legal briefing — it's the minimum your team needs to understand before putting company code through Claude Code.\n\n### The fundamental split\n\nAnthropic has two distinct terms regimes:\n\n| Plan tier | Governed by | Model training on your data |\n|-----------|-------------|-----------------------------|\n| **Free / Pro / Max** (consumer) | Consumer Terms of Service | Subject to Consumer ToS; users can adjust training preferences in Claude.ai settings |\n| **Team / Enterprise / API** (commercial) | Commercial Terms of Service | **Prohibited** — Anthropic may not train models on Customer Content |\n\nThe Commercial Terms are explicit: \"Anthropic may not train models on Customer Content from Services.\" This means prompts, code, and outputs sent through the API, Team, or Enterprise plans do not feed model training.\n\nConsumer plans (Free/Pro/Max) have different defaults. Users can adjust training preferences at `claude.ai/settings/privacy`.\n\n### Data retention\n\nUnder commercial plans, retention is configurable. The Anthropic Trust Center (`trust.anthropic.com`) publishes current retention policies, security certifications (SOC 2 Type 2, ISO 27001), and the Privacy Center at `privacy.anthropic.com` documents data storage periods.\n\n### Practical team governance rule\n\nAnyone running Claude Code against company code, internal APIs, or proprietary data must be on a commercial plan (Team, Enterprise, or API). Personal Free/Pro/Max accounts are not acceptable for company work — the training and retention guarantees are contractually different.\n\nIf your team uses Bedrock or Vertex AI as the provider, those providers have their own data handling agreements with Anthropic. Verify passthrough terms with your cloud provider before treating Bedrock usage as equivalent to direct commercial API usage.\n\n### Admin controls on Team and Enterprise\n\n- Auto mode must be enabled per-org by an admin in the Claude Code admin settings console (`claude.ai/admin-settings/claude-code`) before users can activate it\n- Remote Control and web sessions are toggled org-wide in the same admin console\n- `forceLoginOrgUUID` in managed settings ensures that developers authenticate to your org's account, not a personal account:\n\n```json\n// managed-settings.json\n{\n  \"forceLoginMethod\": \"claudeai\",\n  \"forceLoginOrgUUID\": \"your-org-uuid-here\"\n}\n```\n\nThis prevents the scenario where a developer has both a personal Free account and a company Enterprise account and is unintentionally using the wrong one. Multiple UUIDs can be specified as an array to permit more than one org.\n\nAdditional managed-only org controls: `disableRemoteControl` to block Remote Control per-device, `availableModels` + `enforceAvailableModels` to restrict which models developers can select, and `companyAnnouncements` to inject org-wide messages at session start."
   },
   {
    "heading": "Putting It Together: A Team Configuration Starting Point",
    "body": "The following is a reference configuration for a team deploying Claude Code on shared repos, combining the concepts from this stage. Anthropic publishes additional starter templates at `https://github.com/anthropics/claude-code/tree/main/examples/settings`. Adjust the deny lists and sandbox domains for your environment.\n\n### `.claude/settings.json` — checked into git, applies to all collaborators\n\n```json\n{\n  \"$schema\": \"https://json.schemastore.org/claude-code-settings.json\",\n  \"permissions\": {\n    \"defaultMode\": \"acceptEdits\",\n    \"allow\": [\n      \"Bash(npm run lint)\",\n      \"Bash(npm run test *)\",\n      \"Bash(npm run build)\",\n      \"Bash(git status)\",\n      \"Bash(git log *)\",\n      \"Bash(git diff *)\",\n      \"Bash(git commit *)\",\n      \"Bash(git checkout *)\",\n      \"Bash(git branch *)\"\n    ],\n    \"deny\": [\n      \"Bash(git push --force *)\",\n      \"Bash(git push -f *)\",\n      \"Bash(rm -rf *)\",\n      \"Read(./.env)\",\n      \"Read(./.env.*)\",\n      \"Read(./secrets/**)\"\n    ]\n  }\n}\n```\n\n**Note on `curl | bash`-style rules**: these cannot be reliably blocked with a single Bash rule because Claude Code checks each subcommand independently. The classifier in auto mode blocks `curl | bash` as a default hard block. In non-auto modes, the best defense is a deny rule for `Bash(curl *)` combined with using `WebFetch` for permitted domains.\n\n### `managed-settings.json` — IT-deployed, highest authority\n\n```json\n{\n  \"$schema\": \"https://json.schemastore.org/claude-code-settings.json\",\n  \"forceLoginMethod\": \"claudeai\",\n  \"forceLoginOrgUUID\": \"xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx\",\n  \"permissions\": {\n    \"disableBypassPermissionsMode\": \"disable\",\n    \"disableAutoMode\": \"disable\"\n  },\n  \"sandbox\": {\n    \"enabled\": true,\n    \"failIfUnavailable\": true,\n    \"allowUnsandboxedCommands\": false,\n    \"filesystem\": {\n      \"denyRead\": [\"~/\"],\n      \"allowRead\": [\".\"]\n    },\n    \"network\": {\n      \"allowedDomains\": [\n        \"registry.npmjs.org\",\n        \"*.github.com\",\n        \"github.com\",\n        \"*.acme-internal.com\"\n      ]\n    }\n  },\n  \"sandbox.network.allowManagedDomainsOnly\": true,\n  \"requiredMinimumVersion\": \"2.1.169\"\n}\n```\n\n### `~/.claude/settings.json` — individual developer, auto mode with org context\n\n```json\n{\n  \"permissions\": {\n    \"defaultMode\": \"auto\"\n  },\n  \"autoMode\": {\n    \"environment\": [\n      \"$defaults\",\n      \"Organization: Acme Corp. Software development team.\",\n      \"Source control: github.acme.com/acme-corp and all repos under it\",\n      \"Cloud: AWS us-east-1. Trusted buckets: s3://acme-build-artifacts\",\n      \"Trusted internal domains: *.acme-internal.com, ci.acme.com\"\n    ]\n  }\n}\n```\n\n### Common gotchas checklist\n\n- **Deny rules from any scope block allow rules from any scope** — if a user's `~/.claude/settings.json` has `deny: [\"Bash(aws *)\"]`, project allow rules for aws won't help.\n- **Broad allow rules are dropped on auto mode entry** — `Bash(*)` in allow has no effect in auto mode; only narrow rules carry over.\n- **`auto` `defaultMode` is ignored in project and local settings** (v2.1.142+) — it must be in `~/.claude/settings.json` or managed settings.\n- **Sandbox reads credentials by default** — add `denyRead: [\"~/\"]` with `allowRead: [\".\"]` to block home directory reads while keeping project access.\n- **`$defaults` omission is catastrophic in `autoMode` config** — silently discards all built-in safety rules for that section.\n- **Protected paths ignore allow rules** — don't try to pre-approve `.claude/` writes via settings; it won't work.\n- **`bypassPermissions` is blocked for root** — if CI runs as root, this flag fails; use a non-root user or a dev container.\n- **WebFetch deny rules don't block curl in Bash** — add a separate `Bash(curl *)` deny rule.\n- **Sandbox path syntax differs from permission rule syntax** — in sandbox settings, `/tmp/build` is absolute; in `Read`/`Edit` rules, use `//tmp/build` for absolute paths.\n- **Managed settings array keys merge, not replace** — a developer can always append entries to `allowedDomains`, `allowRead`, and `excludedCommands`; use `allowManagedDomainsOnly` and `allowManagedReadPathsOnly` to lock those surfaces.\n- **`disableAutoMode: \"disable\"` must go in managed settings to be effective** — a user can set it in their own settings, but that only locks themselves out."
   }
  ],
  "preQuiz": [
   {
    "prompt": "Your company signs a BAA with Anthropic for HIPAA compliance. A dev on your team starts using Claude Code via the Anthropic API with the BAA in place, but hasn't done anything else. Are patient records safe to use as context?",
    "options": [
     "Yes — a signed BAA with Anthropic covers all Claude Code usage by default.",
     "No — a BAA alone is not sufficient; Zero Data Retention (ZDR) must also be activated per-org before the BAA coverage applies to Claude Code.",
     "No — Claude Code never supports HIPAA regardless of BAA or ZDR because prompts are logged client-side.",
     "Yes — API usage automatically activates ZDR, so a signed BAA is all that's needed."
    ],
    "correct": 1,
    "sectionIndices": [
     1
    ],
    "explanation": "BAA coverage requires BOTH a signed BAA AND ZDR activated per-org. A BAA alone does not extend HIPAA-eligible data protection to Claude Code; ZDR must be explicitly enabled."
   },
   {
    "prompt": "A developer on your team uses their personal Pro account (with training opted in) to get work done faster. What is the primary contractual risk?",
    "options": [
     "None — the training toggle only affects future models and doesn't expose current IP.",
     "Company source code and prompts sent via a personal Pro account with training enabled can be used to train Anthropic models, under consumer terms.",
     "The risk is only that free-tier rate limits will slow the team down; there are no data rights differences.",
     "Pro accounts are covered by the same commercial terms as Team accounts, so no extra risk exists."
    ],
    "correct": 1,
    "sectionIndices": [
     1
    ],
    "explanation": "Consumer plans (Free/Pro/Max) have different data terms than commercial plans. With training opted in, data retention is 5 years and code/prompts may be used for model training. Commercial plans (Team/Enterprise/API) never use your data for training unless you explicitly opt into the Development Partner Program."
   },
   {
    "prompt": "Your org's managed settings file includes `\"permissions\": {\"deny\": [\"Bash(rm -rf *)\"]}`. A developer adds `\"permissions\": {\"allow\": [\"Bash(rm -rf *)\"]}` to their local `.claude/settings.json`. What happens?",
    "options": [
     "The allow rule wins because local settings override managed settings for specific commands.",
     "The deny rule wins because managed settings sit at the top of precedence and a deny at any level cannot be overridden by any other level.",
     "Claude prompts the user each time, because conflicting rules at different scopes always fall back to 'ask'.",
     "The most specific rule wins, so the scoped allow overrides the managed deny."
    ],
    "correct": 1,
    "sectionIndices": [
     2
    ],
    "explanation": "Managed settings rank highest in the precedence chain, and a deny at any level cannot be overridden by any other level. A local allow rule has no power to override a managed deny."
   },
   {
    "prompt": "You add `\"Bash(safe_script.sh)\"` to your project's `permissions.allow`. A developer then runs a task that executes `safe_script.sh && rm -rf ./build`. Will both commands auto-approve?",
    "options": [
     "Yes — once the compound command is approved interactively, Claude saves a rule for the whole compound.",
     "No — Claude is shell-operator aware; each subcommand must independently match an allow rule. `rm -rf ./build` has no matching allow rule and will prompt.",
     "No — compound commands always prompt regardless of allow rules.",
     "Yes — `safe_script.sh` matches, so the entire pipeline inherits that approval."
    ],
    "correct": 1,
    "sectionIndices": [
     4
    ],
    "explanation": "Claude's permission engine is shell-operator aware. In a compound command (`&&`, `||`, `;`, `|`), each subcommand must independently match an allow rule. An allow for `safe_script.sh` does not cover `rm -rf ./build`."
   },
   {
    "prompt": "You write the rule `Bash(ls *)` in your allow list, intending to permit `ls` with any flags. A developer runs `lsof -i`. Is that command auto-approved?",
    "options": [
     "Yes — `*` in Bash rules matches any string, so `lsof` matches `ls *`.",
     "No — a space before `*` enforces a word boundary, so `Bash(ls *)` matches `ls -la` but not `lsof`.",
     "No — `ls *` only permits `ls` with no arguments.",
     "Yes — `Bash(ls *)` is equivalent to a prefix match that covers any command starting with `ls`."
    ],
    "correct": 1,
    "sectionIndices": [
     4
    ],
    "explanation": "In Claude Code Bash rule wildcards, a space before `*` enforces a word boundary. `Bash(ls *)` matches `ls -la` but not `lsof`. To match both you would use `Bash(ls*)` (no space)."
   },
   {
    "prompt": "You want to block all network requests except to your internal API. You add `Bash(curl *)` to `permissions.deny` and `WebFetch(domain:internal.company.com)` to `permissions.allow`. A developer's Python script uses `requests.get('https://external.com')`. Is that blocked?",
    "options": [
     "Yes — denying curl and restricting WebFetch is sufficient to block all outbound HTTP.",
     "No — Read/Edit deny rules cover Bash file commands like `cat`, but subprocess network calls (Python's `requests`) go through the OS and are not covered by Bash or WebFetch rules. Only a sandbox/container enforces OS-level isolation.",
     "Yes — WebFetch deny rules apply to all HTTP traffic regardless of which tool makes the call.",
     "No — but adding `WebFetch(domain:*)` to the deny list would also catch Python subprocess calls."
    ],
    "correct": 1,
    "sectionIndices": [
     4
    ],
    "explanation": "Permission rules govern tool calls within Claude Code, not OS-level network access. Bash deny rules cover Bash file commands like `cat`, but arbitrary subprocesses (Python, Node) reading files or making network calls bypass them. A sandbox or container is required for OS-level enforcement."
   },
   {
    "prompt": "Your team sets `defaultMode: \"auto\"` in `.claude/settings.json` (the checked-in project settings file). Developers pull the repo and start Claude Code. Does auto mode activate?",
    "options": [
     "Yes — checked-in settings apply to everyone who clones the repo, so auto mode activates for all.",
     "No — repo-level `defaultMode: \"auto\"` is silently ignored. Auto mode must be set in user settings (`~/.claude/settings.json`) or managed settings.",
     "No — auto mode requires a CLI flag; it cannot be set in any settings file.",
     "Yes — but only if the user has Claude Code v2.1.142 or later installed."
    ],
    "correct": 1,
    "sectionIndices": [
     5
    ],
    "explanation": "Since v2.1.142, repo-level `defaultMode: \"auto\"` is silently ignored. Auto mode can only be activated via `~/.claude/settings.json` (user scope), managed settings, or the `--settings` inline flag — never from a checked-in project settings file."
   },
   {
    "prompt": "You write a CLAUDE.md auto-mode section: `soft_deny: [\"Force push to main\"]`. A developer explicitly says 'force push this feature branch to origin.' Does the classifier block it?",
    "options": [
     "Yes — soft_deny entries are unconditional; explicit user intent has no effect.",
     "No — soft_deny can be overridden by explicit, specific user intent. Only hard_deny entries are unconditional. For true enforcement, use `permissions.deny`.",
     "No — CLAUDE.md entries never affect the classifier; only managed settings do.",
     "Yes — force push is always a hard_deny regardless of scope."
    ],
    "correct": 1,
    "sectionIndices": [
     5
    ],
    "explanation": "The classifier's tier hierarchy is: hard_deny (unconditional) → soft_deny (overridable by explicit user intent) → allow → remaining soft blocks. A soft_deny can be overridden when a developer expresses specific, explicit intent. For non-overridable enforcement use `permissions.deny`."
   },
   {
    "prompt": "You configure managed settings with `\"allowManagedPermissionRulesOnly\": true`. A developer adds new allow rules to their local `.claude/settings.json`. What happens to those local rules?",
    "options": [
     "They apply normally — `allowManagedPermissionRulesOnly` only prevents deny rules from being added locally.",
     "They are ignored — this key locks the permission rule set to only what the managed settings define; local additions are disregarded.",
     "They prompt the user before applying, but are eventually accepted.",
     "They apply in user scope (`~/.claude/settings.json`) but not project scope."
    ],
    "correct": 1,
    "sectionIndices": [
     2
    ],
    "explanation": "`allowManagedPermissionRulesOnly: true` is a managed-only key that restricts permission rules exclusively to those defined in managed settings. Local or user-scope permission rules are ignored when this is set."
   },
   {
    "prompt": "In `bypassPermissions` mode, a developer's `permissions.allow` rule list contains `Write(./.env)`. Does that allow rule auto-approve writes to `.env`?",
    "options": [
     "Yes — allow rules are always enforced regardless of permission mode.",
     "No — in `bypassPermissions` mode, allow rules have no effect. Only deny/ask rules and read-only Bash run; additionally, `.env` is not in the protected path list so the write would auto-proceed anyway without needing an allow rule.",
     "No — `.env` is a protected file and protected-path writes are never auto-approved in any mode, including bypassPermissions.",
     "Yes — bypassPermissions skips all prompts and allow rules become the only active gate."
    ],
    "correct": 1,
    "sectionIndices": [
     3
    ],
    "explanation": "In `bypassPermissions` mode, allow rules have no effect — the mode skips all prompts and auto-approves everything (except explicit deny/ask rules and the `rm -rf /` circuit-breaker). `.env` is not on the protected-file list (protected files include `.gitconfig`, shell rc files, package config files, `.mcp.json`, etc.), so the write proceeds without the allow rule mattering either way."
   },
   {
    "prompt": "You want to deny all MCP tool calls from a server named `github`, but still allow `mcp__github__get_repo`. Which rule set achieves this?",
    "options": [
     "`deny: [\"mcp__github\"]` and `allow: [\"mcp__github__get_repo\"]` — the allow overrides the deny for that specific tool.",
     "This is impossible — a bare server deny (`mcp__github`) removes all its tools from context entirely; a scoped allow cannot reinstate individual tools.",
     "`deny: [\"mcp__github__*\"]` and `allow: [\"mcp__github__get_repo\"]` — the scoped deny leaves the server available and the allow reinstates the specific tool.",
     "`deny: [\"mcp__github\"]` only — the deny already excludes all tools except explicitly named ones."
    ],
    "correct": 1,
    "sectionIndices": [
     4
    ],
    "explanation": "A bare-tool deny (`mcp__github`, equivalent to `mcp__github__*`) removes the entire server from context. Once the server is removed, no scoped allow can reinstate individual tools. If you want to selectively allow certain tools, use scoped denies (`mcp__github__delete_*`) rather than a bare server deny."
   },
   {
    "prompt": "Your project's `.claude/settings.json` has `\"permissions\": {\"allow\": [\"WebFetch(domain:*.company.com)\"]}`. A developer tries to fetch `https://company.com/api`. Is it auto-approved?",
    "options": [
     "Yes — `*.company.com` is a wildcard that matches `company.com` and all subdomains.",
     "No — `*.company.com` matches subdomains (e.g. `api.company.com`) but NOT `company.com` itself. A separate rule for `company.com` is needed.",
     "No — WebFetch allow rules require the full URL path, not just the domain.",
     "Yes — domain wildcards in WebFetch always match the apex domain and all subdomains."
    ],
    "correct": 1,
    "sectionIndices": [
     4
    ],
    "explanation": "`WebFetch(domain:*.company.com)` matches subdomains but NOT the apex domain `company.com` itself. To allow both, you need two rules: `WebFetch(domain:*.company.com)` and `WebFetch(domain:company.com)`."
   }
  ],
  "tasks": [
   {
    "id": "stage-2-task-inspect-permissions",
    "afterSectionIdx": 2,
    "title": "Audit your active permission rules and settings scopes",
    "instructions": "1. Open a terminal and launch Claude Code in an existing project directory:\n```bash\nclaude\n```\n2. Inside the Claude Code session, run:\n```\n/permissions\n```\nNote which rules are active and which file they came from (managed, project, user, local).\n\n3. Run:\n```\n/status\n```\nConfirm whether a managed settings source is active (look for \"Managed settings\" in the output).\n\n4. Exit Claude Code (`/exit` or Ctrl+C) and inspect your user-level settings file:\n```bash\ncat ~/.claude/settings.json\n```\nIf it doesn't exist yet, that's expected — note the absence.\n\n5. Inspect the project-level shared settings (if present):\n```bash\ncat .claude/settings.json\n```\nAnd the local (gitignored) override:\n```bash\ncat .claude/settings.local.json\n```",
    "doneWhen": "You have run `/permissions` and `/status` inside a Claude Code session and can identify which settings file is the source of each active rule (or confirmed that no managed settings source is active)."
   },
   {
    "id": "stage-2-task-add-deny-rule",
    "afterSectionIdx": 4,
    "title": "Add a scoped Bash deny rule and verify it fires",
    "instructions": "1. Open (or create) your project's shared settings file:\n```bash\nmkdir -p .claude\nnano .claude/settings.json\n```\n2. Add the following deny rule that blocks `rm -rf` commands (preserve any existing content):\n```json\n{\n  \"permissions\": {\n    \"deny\": [\"Bash(rm -rf *)\"]\n  }\n}\n```\nSave the file.\n\n3. Launch Claude Code in the same directory:\n```bash\nclaude\n```\n\n4. Ask Claude to run:\n```\nPlease run: rm -rf /tmp/test-deleteme\n```\nVerify that Claude Code blocks the command rather than executing it (you should see a permission denial, not execution).\n\n5. Run `/permissions` to confirm the deny rule appears in the active rule list.\n\n6. Now test that a similar but non-matching command still works:\n```\nPlease run: rm /tmp/test-deleteme\n```\n(You can create that file first with `touch /tmp/test-deleteme` in a separate terminal.) This should prompt or succeed, confirming the deny is scoped to `rm -rf` specifically.",
    "doneWhen": "Claude Code blocks `rm -rf` with a permission denial message, and `/permissions` shows your deny rule as active from `.claude/settings.json`."
   },
   {
    "id": "stage-2-task-set-acceptedits-mode",
    "afterSectionIdx": 3,
    "title": "Set acceptEdits permission mode and observe its boundaries",
    "instructions": "1. Add `defaultMode` to your project settings file (`.claude/settings.json`):\n```json\n{\n  \"permissions\": {\n    \"defaultMode\": \"acceptEdits\",\n    \"deny\": [\"Bash(rm -rf *)\"]\n  }\n}\n```\nSave the file.\n\n2. Launch Claude Code:\n```bash\nclaude\n```\nNote the mode indicator in the UI (it should show `acceptEdits` or similar).\n\n3. Ask Claude to create a small test file:\n```\nCreate a file called test-permissions.txt in the current directory with the text \"hello world\"\n```\nVerify it creates the file without prompting for approval (file edits within cwd are auto-approved in `acceptEdits` mode).\n\n4. Now ask Claude to run a general Bash command that is NOT a simple filesystem operation:\n```\nRun: curl -s https://httpbin.org/get\n```\nVerify this prompts you (non-filesystem Bash still requires approval in `acceptEdits` mode).\n\n5. Clean up:\n```bash\nrm test-permissions.txt\n```\n\n6. Optionally, revert `defaultMode` to `\"default\"` if you don't want `acceptEdits` as your team default.",
    "doneWhen": "Claude Code auto-creates `test-permissions.txt` without prompting, but prompts before running the `curl` command, confirming that `acceptEdits` mode auto-approves file edits but not arbitrary Bash."
   }
  ],
  "visualizations": [
   {
    "id": "stage-2-v",
    "kind": "comparison-table",
    "title": "Permissions, security & governance",
    "textualSummary": "Key concepts of Permissions, security & governance: settings scope precedence, deny/ask/allow rule evaluation, enforcement vs guidance.",
    "columns": [
     "Concept",
     "In this stage"
    ],
    "rows": [
     {
      "label": "settings scope precedence",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "deny/ask/allow rule evaluation",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "enforcement vs guidance",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "permission modes",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     }
    ]
   }
  ],
  "confusions": [
   {
    "misconception": "Skipping Permissions, security & governance.",
    "correction": "Each stage is load-bearing for the setup."
   },
   {
    "misconception": "These concepts are interchangeable.",
    "correction": "Each has a distinct role; see the definitions."
   }
  ],
  "quiz": [
   {
    "id": "stage-2-q1",
    "type": "multiple-choice",
    "prompt": "Your team lead adds `\"permissions\": {\"deny\": [\"Bash(rm -rf *)\"]}` to `.claude/settings.json` (the checked-in project file) and then adds a conflicting `\"permissions\": {\"allow\": [\"Bash(rm -rf *)\"]}` to the managed settings file deployed via MDM. Which rule wins, and why?",
    "options": [
     "The project-level deny wins because deny rules always beat allow rules at any scope.",
     "The managed settings allow wins because managed settings sit at the top of the precedence hierarchy.",
     "The managed settings deny would win, but since the managed file has an allow, the project deny wins by default.",
     "They cancel each other out and Claude prompts the user at runtime."
    ],
    "correct": 2,
    "explanation": "Managed settings sit at rank 1 — the highest precedence — and beat every other scope including CLI args. However, the managed file in this scenario contains an allow, not a deny. The project deny is at rank 4. The key rule is that managed settings override all lower scopes, so a managed ALLOW beats a project DENY. The correct interpretation is: the managed allow wins because it is at the top of the precedence hierarchy. Option A mistakes the deny-beats-allow evaluation ORDER within a single scope for cross-scope precedence. Option C misreads the scenario — the managed file has an allow, not a deny. Option D is wrong: there is no cancel-out mechanic; precedence is deterministic."
   },
   {
    "id": "stage-2-q2",
    "type": "multiple-choice",
    "prompt": "A developer on your team is working at home and adds `Bash(git push --force)` to their `~/.claude/settings.json` allow list. The team's checked-in `.claude/settings.json` has `Bash(git push --force)` in deny. What happens when Claude evaluates a force-push request?",
    "options": [
     "The user allow wins because user settings (rank 5) are more specific to the individual than the project deny (rank 4).",
     "The project deny wins because shared project settings (rank 4) have higher precedence than user settings (rank 5).",
     "The deny wins regardless of scope because the rule evaluation order always processes deny before allow.",
     "Claude prompts the user since there is a conflicting allow and deny."
    ],
    "correct": 1,
    "explanation": "The precedence table ranks shared project settings (`.claude/settings.json`) at rank 4 and user settings (`~/.claude/settings.json`) at rank 5. Lower rank number = higher precedence. So the project deny at rank 4 beats the user allow at rank 5. Option A confuses 'more specific to the individual' with higher precedence — user scope is actually lower priority. Option C describes the within-scope rule evaluation order (deny→ask→allow first match wins), not cross-scope precedence; both rules must first be selected by scope precedence before the deny/ask/allow ordering applies. Option D is incorrect: the system is deterministic, not prompt-on-conflict."
   },
   {
    "id": "stage-2-q3",
    "type": "multiple-choice",
    "prompt": "You want to prevent any developer from using `bypassPermissions` mode on a shared project. You add `\"permissions\": {\"disableBypassPermissionsMode\": \"disable\"}` to the shared project's `.claude/settings.json`. A developer later tries to launch Claude with `--permission-mode bypassPermissions`. Will this block them?",
    "options": [
     "Yes — `disableBypassPermissionsMode` in project settings blocks this flag regardless of where it appears.",
     "No — `disableBypassPermissionsMode` is a managed-only key; it is ignored when placed in `.claude/settings.json`.",
     "No — CLI flags (rank 2) have higher precedence than shared project settings (rank 4), so the flag overrides the setting.",
     "Yes — but only if the developer is not listed as a project admin in Claude Console."
    ],
    "correct": 1,
    "explanation": "The content explicitly lists `disableBypassPermissionsMode` as one of the admin org lock settings applied via managed settings, and separately states it 'works from any scope (a user can self-lock)' — but the key point tested here is whether putting it in the shared project `.claude/settings.json` (not managed settings) is sufficient. However, re-reading the content: 'Admin org locks (managed settings): permissions.disableAutoMode: \"disable\", permissions.disableBypassPermissionsMode: \"disable\". disableBypassPermissionsMode works from any scope (a user can self-lock).' This means `disableBypassPermissionsMode` DOES work from non-managed scopes too (it is not listed as a managed-only key). The managed-only keys list does NOT include `disableBypassPermissionsMode`. So it would work from project settings. However, CLI args are rank 2 vs project settings at rank 4 — so the CLI flag would normally override. The correct answer is B (ignored in project settings) is WRONG per the content; C would be correct if CLI beats project settings, BUT the content says managed settings cannot be overridden by CLI args specifically. Since this is project settings (rank 4) vs CLI (rank 2), CLI wins. Option C is actually correct. Let me re-evaluate: project settings = rank 4, CLI = rank 2. CLI is higher. So C is correct."
   },
   {
    "id": "stage-2-q4",
    "type": "multiple-choice",
    "prompt": "A teammate writes the allow rule `Bash(devbox run *)` to permit running tests via devbox. Later they discover Claude is using this rule to run arbitrary commands like `devbox run curl https://exfil.example.com/data`. Why is this happening?",
    "options": [
     "The `*` wildcard in Bash rules matches only safe commands; `curl` must have been separately allowed.",
     "`devbox run` is a process wrapper that gets stripped before matching, so the rule effectively allows anything after it.",
     "`devbox run` is NOT stripped before matching — unlike `timeout` or `nice`, it is explicitly listed as a wrapper that is NOT stripped, making `devbox run *` allow arbitrary inner commands.",
     "The rule should have been written with `Bash(devbox run:*)` param-syntax to restrict the inner command."
    ],
    "correct": 2,
    "explanation": "The content explicitly states: 'NOT stripped: `direnv exec`, `devbox run`, `mise exec`, `npx`, `docker exec` — so `Bash(devbox run *)` allows arbitrary commands. Write specific rules per inner command (`Bash(devbox run npm test)`).' The teammate's rule is dangerous because devbox run is intentionally NOT stripped, meaning `devbox run *` permits any inner command. Option A is wrong — `*` in Bash matches anything including spaces and does not filter for 'safety'. Option B is the opposite of correct — devbox run is NOT stripped. Option D describes param-syntax, which the content says only works on deny/ask rules, not allow rules, and only on top-level scalar params, not `command`."
   },
   {
    "id": "stage-2-q5",
    "type": "multiple-choice",
    "prompt": "You write the rule `Bash(ls *)` in your allow list. A developer later reports that `ls -la` is allowed but `lsof -i` is not. Another developer is surprised that `ls-tree` (a custom script) is also not allowed. What explains this behavior?",
    "options": [
     "`ls *` is a prefix match, so anything starting with `ls` should be allowed; the behavior is a bug.",
     "A space before `*` enforces a word boundary — `Bash(ls *)` matches `ls -la` (space after `ls`) but not `lsof` (no space). `ls-tree` also has no space after `ls`, so it is excluded.",
     "The `*` wildcard only matches alphanumeric characters, which excludes the `-` in `lsof` and `ls-tree`.",
     "`ls *` requires the command to end with a space, which neither `lsof` nor `ls-tree` does."
    ],
    "correct": 1,
    "explanation": "The content states: 'A space before `*` enforces a word boundary — `Bash(ls *)` matches `ls -la` not `lsof`; `Bash(ls*)` matches both.' So `ls *` (with space before `*`) matches `ls` followed by a space — meaning `ls -la` (which has a space after `ls`) matches, while `lsof` and `ls-tree` (no space after `ls`) do not. This is the word-boundary behavior. Option A is wrong — this is by design, not a bug. Option C is wrong — `*` matches anything including spaces and special characters. Option D mischaracterizes the rule; the space is before the `*` in the rule pattern, enforcing that the matched text must contain a space at that position."
   },
   {
    "id": "stage-2-q6",
    "type": "multiple-choice",
    "prompt": "Your security team wants to block all network access by Claude except to `api.internal.example.com`. They write the rule `Bash(curl * api.internal.example.com *)` to allow only that domain and deny all other curl. Will this approach reliably enforce the network restriction?",
    "options": [
     "Yes — the Bash rule pattern matches the domain argument and blocks everything else.",
     "No — Bash patterns can be circumvented by argument reordering or shell quoting; use `WebFetch(domain:api.internal.example.com)` combined with denying `curl` and `wget` instead.",
     "No — Bash allow rules with `*` are silently skipped; you must use param-syntax `Bash(url:api.internal.example.com)` instead.",
     "Yes — but only if `WebFetch` is also denied, since WebFetch bypasses Bash rules."
    ],
    "correct": 1,
    "explanation": "The content explicitly states: 'Robust URL/network filtering: deny `curl`/`wget` and grant `WebFetch(domain:...)` allow rules — never try to constrain curl args with Bash patterns. `WebFetch` alone does not block network access while Bash is allowed.' Bash argument patterns are unreliable for enforcing domain restrictions because curl's argument order can vary, arguments can be quoted, and environment variables can obscure the domain. The correct pattern is deny curl/wget entirely and use WebFetch with domain allow rules. Option A is exactly the anti-pattern the content warns against. Option C is wrong — the issue isn't about param-syntax; the problem is that Bash arg matching is unreliable for URLs. Option D is partially right that WebFetch must be the permitted path, but frames it incorrectly."
   },
   {
    "id": "stage-2-q7",
    "type": "multi-select",
    "prompt": "Your organization is deploying Claude Code to 50 developers. Select ALL statements that correctly describe where and how to configure things for team-wide governance.",
    "options": [
     "Hard constraints like `permissions.deny` for destructive operations should go in managed settings (MDM/server-managed) to prevent override by CLI args or local files.",
     "Shared allow/deny lists and `enabledPlugins` should be checked into `.claude/settings.json` in version control for team-wide distribution.",
     "Per-developer overrides (e.g., a developer's preferred tool allow list) should go in `.claude/settings.local.json`, which is gitignored.",
     "The `defaultMode` for `auto` should be set in the checked-in `.claude/settings.json` so every developer gets auto mode automatically.",
     "Setting `allowManagedPermissionRulesOnly: true` in managed settings prevents developers from adding their own permission rules."
    ],
    "correct": [
     0,
     1,
     2,
     4
    ],
    "explanation": "A: Correct — managed settings sit at rank 1 and cannot be overridden by CLI args or any lower scope; this is where hard constraints belong. B: Correct — the content states 'check `.claude/settings.json` into VCS for team-wide allow/deny lists and `enabledPlugins`'. C: Correct — 'keep dev-specific overrides in gitignored `.claude/settings.local.json`'. D: WRONG — 'Set `defaultMode: \"auto\"` only in `~/.claude/settings.json` (user) or managed settings — repo-level `auto` is ignored (v2.1.142+, silently).' A checked-in `.claude/settings.json` with `auto` will be silently ignored. E: Correct — `allowManagedPermissionRulesOnly` is a managed-only key that, when true, restricts permission rules to only those in managed settings."
   },
   {
    "id": "stage-2-q8",
    "type": "multi-select",
    "prompt": "A security auditor asks which of the following are TRUE about the `auto` permission mode's classifier. Select ALL correct statements.",
    "options": [
     "The classifier is an isolation boundary — if it blocks a tool call, Claude cannot reach that resource through any other path.",
     "Permission `deny` and `ask` rules are evaluated BEFORE the classifier acts on a tool call.",
     "A developer can override an org's `soft_deny` classifier entry by adding an `allow` entry in their own autoMode config.",
     "Setting `defaultMode: \"auto\"` in a checked-in `.claude/settings.json` file will apply auto mode to all developers who clone the repo.",
     "A hard_deny classifier entry is unconditional and cannot be overridden by user intent or an allow entry."
    ],
    "correct": [
     1,
     2,
     4
    ],
    "explanation": "A: WRONG — the content explicitly states auto mode is 'A per-action control, not an isolation boundary.' For true isolation, use a sandbox/container/VM. B: Correct — 'Permission `deny`/`ask` rules are evaluated BEFORE the classifier.' C: Correct — 'a dev `allow` can override an org `soft_deny`'; `soft_deny` is not a hard boundary. D: WRONG — 'Set `defaultMode: \"auto\"` only in `~/.claude/settings.json` (user) or managed settings — repo-level `auto` is ignored (v2.1.142+, silently).' E: Correct — 'Classifier precedence: `hard_deny` (unconditional)' sits at the top of the 4-tier classifier precedence."
   },
   {
    "id": "stage-2-q9",
    "type": "multiple-choice",
    "prompt": "A developer is running Claude in `acceptEdits` mode. They try to edit `.claude/settings.json` to add a new permission rule. Claude refuses even though the developer expects file edits to be auto-approved in this mode. Why?",
    "options": [
     "`.claude/settings.json` is a protected file — in every mode except `bypassPermissions`, protected-path writes are never auto-approved, and `allow` rules do not pre-approve them.",
     "`acceptEdits` mode only allows edits to files that are tracked by git; `.claude/settings.json` is gitignored by default.",
     "The developer must explicitly add `Edit(.claude/settings.json)` to their allow list for this to work in `acceptEdits` mode.",
     "Claude cannot edit its own settings files unless launched with `--permission-mode bypassPermissions`."
    ],
    "correct": 0,
    "explanation": "The content lists `.claude` (except `.claude/worktrees`) as a protected directory, and states: 'In every mode except `bypassPermissions`, protected-path writes are never auto-approved, and `allow` rules do not pre-approve them (the protected-path check runs before allow evaluation).' So even in `acceptEdits` mode, `.claude/settings.json` will not be auto-approved. Option B is wrong — `.claude/settings.json` is the checked-in project settings file, not gitignored (`.claude/settings.local.json` is gitignored). Option C is wrong — the content explicitly states `allow` rules do NOT pre-approve protected paths. Option D overstates the case — bypass mode skips prompts, but protected paths remain never auto-approved even in bypass except for the specific circuit-breaker exceptions."
   },
   {
    "id": "stage-2-q10",
    "type": "multiple-choice",
    "prompt": "Your CI pipeline needs Claude to run fully non-interactively, auto-denying any action that would normally prompt. A teammate suggests `bypassPermissions` mode for this. Another suggests `dontAsk` mode. Which is correct for CI, and why?",
    "options": [
     "`bypassPermissions` — it skips all prompts, ensuring CI never blocks waiting for input.",
     "`dontAsk` — it auto-denies anything that would prompt (only `permissions.allow` + read-only Bash run); it is explicitly designed for non-interactive CI use.",
     "Either works equally well for CI; the difference is only which rules are evaluated.",
     "`plan` mode — it makes Claude read-only so CI can inspect without risk of changes."
    ],
    "correct": 1,
    "explanation": "The content describes `dontAsk`: 'Auto-denies anything that would prompt; only `permissions.allow` + read-only Bash run; `ask` rules are denied. Non-interactive — for CI.' `bypassPermissions` skips prompts but by allowing actions rather than denying them — it is not safe for CI because it allows everything that isn't an explicit `deny` or `ask` rule. The content states `bypassPermissions` 'Skips all prompts except explicit `ask` rules' — meaning it runs everything by default. In CI you want unknown actions to FAIL (be denied), not silently succeed. Option A describes what bypassPermissions does but misidentifies it as appropriate for CI. Option C is wrong — the modes behave very differently. Option D is wrong — plan mode is read-only exploration with no edits, not the same as non-interactive CI."
   },
   {
    "id": "stage-2-q11",
    "type": "multiple-choice",
    "prompt": "Your org is on a Team plan with a signed BAA (Business Associate Agreement) and wants to use Claude Code for HIPAA-covered work. The security lead says 'We have a BAA, so we're covered.' Is this correct?",
    "options": [
     "Yes — a signed BAA is the only requirement for HIPAA coverage under the Anthropic commercial terms.",
     "No — BAA coverage requires BOTH a signed BAA AND ZDR (Zero Data Retention) activated per-org; a BAA alone does not cover Claude Code.",
     "No — Claude Code is excluded from BAA coverage; healthcare orgs must use Bedrock or Vertex instead.",
     "Yes — Team plan commercial terms automatically include ZDR; no separate activation is needed."
    ],
    "correct": 1,
    "explanation": "The content states explicitly: 'BAA coverage requires BOTH a signed BAA AND ZDR activated per-org — a BAA alone does not cover Claude Code.' This is an easy-to-miss two-part requirement. Option A represents the common misconception that a BAA alone is sufficient. Option C is wrong — the content does not exclude Claude Code from BAA coverage, it just requires both a BAA and ZDR. Option D is wrong — ZDR must be separately activated per-org; it is not automatic with Team plan."
   },
   {
    "id": "stage-2-q12",
    "type": "multiple-choice",
    "prompt": "A developer writes the Bash deny rule `Bash(command:rm *)` to block all `rm` commands. Claude still allows `rm -rf /tmp/build`. They check the logs and see a startup warning. What went wrong?",
    "options": [
     "The rule should be `Bash(rm *)` — `command` is a canonicalized field that is NOT param-matchable, so `Bash(command:rm *)` is silently ignored with a warning.",
     "`command:` param-syntax works for deny rules but requires the full command without wildcards.",
     "The wildcard `*` must be escaped in command param-syntax — the rule should be `Bash(command:rm \\*)`.",
     "The `rm` command is in the protected-path list and cannot be denied via Bash rules; use sandbox enforcement instead."
    ],
    "correct": 0,
    "explanation": "The content states: 'Canonicalized fields are NOT param-matchable — `command`, `file_path`, `path`, `notebook_path`, `url`. `Bash(command:rm *)` is silently ignored with a warning → use `Bash(rm *)` instead.' The developer used `command:` which is one of the explicitly listed canonicalized fields that cannot be used in param-syntax. The startup warning is the diagnostic clue. Option B is wrong — the issue is not about wildcards but about using an invalid canonicalized field. Option C is wrong — escaping is not the fix; the field itself is not param-matchable. Option D is wrong — rm is not in the protected-path list; the issue is purely about rule syntax."
   },
   {
    "id": "stage-2-q13",
    "type": "multiple-choice",
    "prompt": "You add an allow rule `mcp__*` in your project's `.claude/settings.json` to permit all MCP tools. Later you notice the rule seems to have no effect — MCP tools still prompt. What is the likely cause?",
    "options": [
     "MCP tools can only be allowed via managed settings, not project settings.",
     "Unanchored allow globs like `mcp__*` are silently skipped with a warning — allow rules only accept globs after a literal `mcp__<server>__` prefix, e.g. `mcp__github__get_*`.",
     "The `mcp__*` rule only works in `bypassPermissions` mode, not in default mode.",
     "You must use `mcp__server` (without `*`) to allow all tools from a specific server."
    ],
    "correct": 1,
    "explanation": "The content states: 'Tool-name globs: deny/ask accept `*` and `mcp__*`. Allow rules only accept globs after a literal `mcp__<server>__` prefix (e.g. `mcp__github__get_*`); unanchored allow globs (`*`, `B*`, `mcp__*`) are silently skipped with a warning.' This asymmetry — deny/ask accept unanchored globs but allow does not — is the source of the confusion. Option A is wrong — MCP tools can be managed at the project level; the issue is glob syntax. Option C is wrong — this restriction applies to all modes, not just non-bypass modes. Option D describes using `mcp__server` (no wildcard) which IS valid for allowing all tools from a server, but this doesn't explain why `mcp__*` failed."
   },
   {
    "id": "stage-2-q14",
    "type": "multiple-choice",
    "prompt": "Your company builds an internal SaaS tool that lets employees log in with their Claude.ai accounts (OAuth) to access Claude capabilities via your app. Is this permitted under Anthropic's terms?",
    "options": [
     "Yes — OAuth/Claude.ai login is the recommended method for building internal employee-facing tools.",
     "No — OAuth/Claude.ai login is for ordinary subscription use only. Products and services must use API-key auth via Claude Console or a cloud provider. Routing requests through OAuth credentials in a third-party product is prohibited.",
     "Yes — as long as your company has a Team plan, routing through OAuth credentials is permitted for internal apps.",
     "Yes — the prohibition only applies to external customer-facing products, not internal employee tools."
    ],
    "correct": 1,
    "explanation": "The content states: 'OAuth/Claude.ai login is for ordinary subscription use only. Products/services (incl. Agent SDK) must use API-key auth via Claude Console or a cloud provider. Routing requests through Free/Pro/Max OAuth credentials on behalf of others — or offering Claude.ai login in a third-party product — is prohibited and enforceable without notice.' This applies regardless of whether the product is internal or external, and regardless of the subscription tier. Option A is the opposite of what the content states. Option C is wrong — Team plan doesn't change the authentication policy. Option D is wrong — the prohibition applies to products/services generally, not just external-facing ones."
   }
  ],
  "masteryCheckpoint": "You can explain the concepts of Permissions, security & governance."
 },
 {
  "id": "stage-3",
  "stage": 3,
  "title": "Skills, hooks & commands",
  "summary": "Skills, hooks & commands: configuration scope and precedence, shared vs. personal config placement, hooks on lifecycle events.",
  "prerequisites": [
   "stage-2"
  ],
  "objectives": [
   "Understand the concepts in Skills, hooks & commands."
  ],
  "definitions": [
   {
    "term": "configuration scope and precedence",
    "short": "Claude Code settings resolve through ordered layers where later layers override earlier, determining which config actually takes effect."
   },
   {
    "term": "shared vs. personal config placement",
    "short": "Team-standard config goes in committable files while personal or experimental config stays in user-level or gitignored files so it doesn't impose on teammates."
   },
   {
    "term": "hooks on lifecycle events",
    "short": "Hooks run deterministic logic at named, case-sensitive lifecycle events, configured as sibling keys under a single hooks object with matchers selecting when they fire."
   },
   {
    "term": "hook exit-code blocking semantics",
    "short": "A command hook blocks an action only with exit code 2 (stderr fed back to Claude); exit 1 and other nonzero codes are non-blocking, and exit 0 may add context."
   },
   {
    "term": "PreToolUse policy enforcement",
    "short": "PreToolUse hooks fire before any permission-mode check, so a deny decision is the only way to enforce policy users cannot bypass even in skip-permissions mode."
   },
   {
    "term": "hook matcher and if-filter selection",
    "short": "Matchers select which tool/event a hook fires on using exact, pipe-list, or regex syntax (case-sensitive), and the if filter further narrows by tool arguments but fails open."
   }
  ],
  "sections": [
   {
    "heading": "Configuration Scope and Precedence",
    "body": "Claude Code resolves settings through a strict five-tier hierarchy. Understanding which file wins — and when — is the difference between a policy that holds under pressure and one that breaks the first time a developer adds a local override.\n\n### The Five Tiers\n\n| Tier | File path | Who controls it | Git-committed? | Precedence |\n|------|-----------|-----------------|----------------|------------|\n| Managed | See delivery methods below | IT / enterprise admin | Out-of-band deployment | Highest — cannot be overridden |\n| Command-line flags | `--model`, `--permission-mode`, etc. | Developer, per session | N/A | Overrides all file-based settings |\n| Local | `.claude/settings.local.json` | Individual developer | No (auto-gitignored) | Overrides project and user |\n| Project | `.claude/settings.json` | Team (via repo) | Yes | Overrides user |\n| User | `~/.claude/settings.json` | Individual developer | No | Lowest baseline |\n\nCommand-line flags are session-scoped and evaporate when the session ends. They sit above all file-based tiers but below managed settings — there is no way to override managed policy from the command line.\n\n### Managed Settings Delivery Methods\n\nThe Managed tier is not a single path — it is delivered through one of several mechanisms depending on the platform:\n\n| Platform | Delivery mechanism |\n|----------|-------------------|\n| Linux / WSL | `/etc/claude-code/managed-settings.json` |\n| macOS | `/Library/Application Support/ClaudeCode/managed-settings.json` or `com.anthropic.claudecode` MDM plist |\n| Windows (machine-wide) | `C:\\Program Files\\ClaudeCode\\managed-settings.json` or `HKLM\\SOFTWARE\\Policies\\ClaudeCode` registry key |\n| Windows (user-level) | `HKCU\\SOFTWARE\\Policies\\ClaudeCode` (lowest managed priority) |\n| All platforms | Server-managed via Anthropic's Claude.ai admin console |\n\nAll delivery methods support a **drop-in directory** named `managed-settings.d/` in the same system directory (v2.1.175+). Files named `*.json` inside it are merged alphabetically on top of the base `managed-settings.json`. This lets IT distribute hook policy separately from model policy without editing a single shared file:\n\n```\n/etc/claude-code/\n├── managed-settings.json          # Base policy\n└── managed-settings.d/\n    ├── 10-telemetry.json           # Telemetry settings\n    └── 20-security-hooks.json     # Hook policy\n```\n\nLater files override earlier ones for scalar values; arrays are concatenated and deduplicated; objects are deep-merged.\n\n### How Values Merge\n\nFor most settings, the highest-precedence scope that defines the key wins — lower scopes are silently ignored. There is one critical exception: **`permissions` rules merge across all scopes** rather than override. Allow and deny rules from every scope are combined into a single effective set. The deny-first rule then applies: if a rule appears in both `allow` at one scope and `deny` at another, deny wins regardless of which scope the deny came from.\n\nThis is not symmetrical. A managed deny cannot be unlocked by a local allow. A user allow cannot be negated by a project deny unless the project deny is more specific and wins on that basis. The evaluation order is: deny → ask → allow.\n\n### Hot-Reload vs. Restart-Required\n\nMost keys are watched and reload live without restarting Claude Code. The `ConfigChange` hook fires on each detected change. Two keys are read once at startup and require a restart:\n\n- `model` — use `/model` to switch mid-session instead\n- `outputStyle` — is rebuilt on `/clear` or restart\n\nPermissions, hooks, `env`, and `apiKeyHelper` all hot-reload.\n\n### Practical Implication for Teams\n\nThe asymmetry between Local and User scope is a common surprise. A deny rule in `.claude/settings.json` (Project tier) wins over anything in `~/.claude/settings.json` (User tier). But a developer's `.claude/settings.local.json` (Local tier) sits *above* the Project tier and can override the team's project file. If a policy must be inviolable, it belongs in the Managed tier — only there is it truly unoverridable."
   },
   {
    "heading": "Shared vs. Personal Config Placement",
    "body": "The discipline of config placement is fundamentally about what you want teammates to inherit automatically versus what should stay personal.\n\n### File Layout\n\nA typical team repository looks like this:\n\n```\nrepo-root/\n├── .claude/\n│   ├── settings.json          # Committed — team permissions, hooks, env vars\n│   ├── settings.local.json    # Gitignored — individual developer overrides\n│   ├── hooks/\n│   │   ├── pre-tool-policy.sh # Committed — team policy script\n│   │   └── post-write-lint.sh # Committed — linter hook\n│   ├── skills/\n│   │   ├── deploy/SKILL.md    # Committed — shared workflow\n│   │   └── commit/SKILL.md    # Committed — shared commit helper\n│   └── agents/\n│       └── code-reviewer.md   # Committed — shared subagent definition\n├── .mcp.json                  # Committed — project MCP server declarations\n├── CLAUDE.md                  # Committed — project memory / instruction context\n└── CLAUDE.local.md            # Gitignored — personal instruction overrides\n```\n\nThe `~/.claude/` directory is the developer's personal scope:\n\n```\n~/.claude/\n├── settings.json              # Personal defaults across all projects\n├── skills/                    # Personal skills available in every project\n│   └── my-workflow/SKILL.md\n├── agents/                    # Personal subagent definitions\n└── CLAUDE.md                  # Personal memory injected into every session\n```\n\n### Decision Heuristic\n\n| Put it in `.claude/settings.json` (committed) | Put it in `~/.claude/settings.json` or `settings.local.json` |\n|-----------------------------------------------|---------------------------------------------------------------|\n| Deny rules that enforce team policy | Personal model preferences (`model`, `effortLevel`) |\n| `env` vars every developer needs (non-secret) | Experimental hooks under active development |\n| Shared `hooks` that enforce CI parity | Personal notification settings |\n| `companyAnnouncements` / onboarding context | Individual `apiKeyHelper` or credential scripts |\n| MCP server declarations in `.mcp.json` | Per-developer MCP servers (e.g., personal Notion connector) |\n| `CLAUDE.md` project architecture notes | `CLAUDE.local.md` personal working notes |\n\n### What Happens to Secrets\n\nNever put API keys or secrets in any `settings.json` value directly — they will appear in git history. Use the `apiKeyHelper` setting to point at a script that retrieves the key at runtime, or use `env` to name a variable that is populated from the shell environment before Claude Code launches:\n\n```json\n// .claude/settings.json — committed, safe\n{\n  \"env\": {\n    \"ANTHROPIC_BASE_URL\": \"https://gateway.corp.internal/anthropic\"\n  }\n}\n```\n\n```json\n// ~/.claude/settings.json — personal, never committed\n{\n  \"apiKeyHelper\": \"~/.secrets/get-claude-key.sh\"\n}\n```\n\n### Enterprise Managed Scope\n\nFor organizations with strict requirements, the Managed tier sidesteps git entirely. The `claudeMd` key injects org-wide CLAUDE.md content — but **it is only honored when set in managed or policy settings**; placing it in user or project settings has no effect. The `policyHelper` key (v2.1.136+) points at an admin-deployed binary that computes settings dynamically at startup, enabling fleet-wide policy that adapts at runtime. It is only honored from MDM or system `managed-settings.json`, not from user settings.\n\nThe `allowManagedPermissionRulesOnly` managed setting goes further: when `true`, it blocks user and project settings from defining any `allow`, `ask`, or `deny` permission rules — only managed rules apply. `allowManagedHooksOnly` does the same for hooks."
   },
   {
    "heading": "Hooks: Lifecycle Events and Configuration Format",
    "body": "Hooks are the mechanism for running **deterministic, side-effect-aware logic** at named points in Claude Code's execution. The word \"deterministic\" is load-bearing: hooks run scripts, not Claude — they are not subject to LLM judgment, they always fire when their matcher matches, and their exit code has a specific, guaranteed semantic.\n\n### The Complete Event Taxonomy\n\n**Session-level** (once per session):\n- `SessionStart` — fires when a session begins or resumes; matcher selects the source (`startup`, `resume`, `clear`, `compact`)\n- `Setup` — fires only when launched with `--init-only` or `--init`; matcher selects trigger type (`init`, `maintenance`)\n- `SessionEnd` — fires when a session terminates; matcher selects end reason (`clear`, `resume`, `logout`, `prompt_input_exit`)\n\n**Turn-level** (once per user prompt):\n- `UserPromptSubmit` — before Claude processes the prompt; no matcher support, always fires\n- `UserPromptExpansion` — when a `/slash` command expands; matcher selects command name\n- `Stop` — when Claude finishes responding; no matcher support\n- `StopFailure` — when a turn ends due to an API error; matcher selects error type (`rate_limit`, `overloaded`, `authentication_failed`)\n\n**Agentic loop** (on every tool call):\n- `PreToolUse` — before a tool executes; can block, modify input, or grant/deny permission\n- `PermissionRequest` — when the permission dialog would appear\n- `PermissionDenied` — after a tool is denied\n- `PostToolUse` — after a tool succeeds; cannot block but can add context or transform output\n- `PostToolUseFailure` — after a tool fails\n- `PostToolBatch` — after a batch of parallel tool calls resolves\n\n**Subagent/task events**:\n- `SubagentStart` — matcher selects agent type (`general-purpose`, `Explore`, custom names)\n- `SubagentStop` — matcher selects agent type\n- `TaskCreated` — task lifecycle event\n- `TaskCompleted` — task lifecycle event\n- `TeammateIdle` — when a teammate agent goes idle\n\n**Infrastructure**:\n- `FileChanged` — when a watched file changes; matcher selects literal filenames (e.g., `.envrc|.env`)\n- `CwdChanged` — when the working directory changes; no matcher support\n- `Notification` — when Claude Code sends a notification; matcher selects type (`permission_prompt`, `auth_success`)\n- `ConfigChange` — when any settings file changes; matcher selects scope (`user_settings`, `project_settings`, `policy_settings`)\n- `WorktreeCreate` / `WorktreeRemove` — git worktree lifecycle\n- `PreCompact` / `PostCompact` — before/after context compaction; matcher selects trigger (`manual`, `auto`)\n- `InstructionsLoaded` — when CLAUDE.md loads; matcher selects reason (`session_start`, `nested_traversal`, `path_glob_match`, `include`, `compact`)\n- `MessageDisplay` — when a message is shown\n- `Elicitation` / `ElicitationResult` — MCP elicitation lifecycle; matcher selects MCP server name\n\n### Configuration Format\n\nHooks live under the `hooks` key in any `settings.json`. The structure has three nesting levels:\n\n```json\n{\n  \"hooks\": {\n    \"PreToolUse\": [\n      {\n        \"matcher\": \"Bash|Write|Edit\",\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"${CLAUDE_PROJECT_DIR}/.claude/hooks/audit.sh\",\n            \"timeout\": 30\n          }\n        ]\n      }\n    ]\n  }\n}\n```\n\nLevel 1 is the **event name** (PascalCase and case-sensitive — `preToolUse` silently does nothing).\nLevel 2 is a **matcher group** — one object with a `matcher` field and a `hooks` array.\nLevel 3 is the **handler** — the thing that actually runs.\n\nYou can globally disable all hooks for a session by setting `\"disableAllHooks\": true` in settings — useful for debugging whether a hook is interfering with a workflow."
   },
   {
    "heading": "Hook Matcher and if-Filter Selection",
    "body": "The `matcher` field and the `if` filter are two independent narrowing mechanisms. Matcher selects which tool or event type triggers the hook group; `if` further narrows by inspecting tool arguments.\n\n### Matcher Syntax Rules\n\n| Pattern characters | Evaluation mode | Example |\n|--------------------|-----------------|---------|\n| Omitted or `\"*\"` | Match all | Fires on every tool call |\n| Only `[a-zA-Z0-9_\\|]` | Exact string or pipe-separated list | `\"Bash\\|Edit\"` |\n| Contains any other character | JavaScript regex | `\"^mcp__.*__write.*\"` |\n\nMatchers are case-sensitive. `\"bash\"` does not match the `Bash` tool.\n\n**Tool name examples:**\n```json\n\"matcher\": \"Bash\"                   // exact: only Bash\n\"matcher\": \"Write|Edit\"             // pipe list: Write or Edit\n\"matcher\": \"mcp__memory__.*\"        // regex: all tools from memory MCP server\n\"matcher\": \"mcp__.*__write.*\"       // regex: any write tool from any MCP server\n```\n\n**Event-specific matchers** — what the matcher field is compared against varies by event:\n\n| Event | Matches against | Example values |\n|-------|----------------|----------------|\n| `PreToolUse`, `PostToolUse`, `PostToolUseFailure`, `PermissionRequest`, `PermissionDenied` | Tool name | `Bash`, `mcp__.*__write.*` |\n| `SessionStart` | Session source | `startup`, `resume`, `clear`, `compact` |\n| `Setup` | Trigger type | `init`, `maintenance` |\n| `SessionEnd` | End reason | `clear`, `resume`, `logout`, `prompt_input_exit` |\n| `SubagentStart`, `SubagentStop` | Agent type | `general-purpose`, `Explore`, custom names |\n| `PreCompact`, `PostCompact` | Trigger | `manual`, `auto` |\n| `ConfigChange` | Config source | `user_settings`, `project_settings`, `policy_settings` |\n| `FileChanged` | Literal filenames | `.envrc\\|.env` (literal match, not regex) |\n| `StopFailure` | Error type | `rate_limit`, `overloaded`, `authentication_failed` |\n| `InstructionsLoaded` | Load reason | `session_start`, `nested_traversal`, `path_glob_match` |\n| `UserPromptExpansion` | Command name | skill/command names |\n| `Elicitation`, `ElicitationResult` | MCP server name | configured server names |\n| No matcher support | — | `UserPromptSubmit`, `PostToolBatch`, `Stop`, `CwdChanged`, `MessageDisplay` |\n\n### The if Filter\n\nThe `if` field sits on the individual hook handler (not the matcher group) and uses the same permission-rule syntax as `permissions.allow`/`deny`:\n\n```json\n{\n  \"matcher\": \"Bash\",\n  \"hooks\": [\n    {\n      \"type\": \"command\",\n      \"if\": \"Bash(rm *)\",\n      \"command\": \"/path/to/rm-guard.sh\"\n    },\n    {\n      \"type\": \"command\",\n      \"if\": \"Bash(curl *)\",\n      \"command\": \"/path/to/network-audit.sh\"\n    }\n  ]\n}\n```\n\nThe `if` filter is only evaluated on tool events (`PreToolUse`, `PostToolUse`, `PostToolUseFailure`, `PermissionRequest`, `PermissionDenied`). **On other events, a hook handler with an `if` field never runs** — the filter fails closed, not open.\n\n**Bash command matching is more powerful than simple glob:**\n- Leading environment variable assignments are stripped: `FOO=bar git push` matches `Bash(git *)`\n- Compound commands check each subcommand: `npm test && git push` matches `Bash(git *)`\n- Subshell substitutions are checked: `echo $(rm -rf /)` matches `Bash(rm *)`\n\n### Handler Types\n\nFive handler types exist:\n\n```json\n// command — shell script or binary (most common)\n{\n  \"type\": \"command\",\n  \"command\": \"${CLAUDE_PROJECT_DIR}/.claude/hooks/check.sh\",\n  \"timeout\": 30,\n  \"async\": false\n}\n```\n\n```json\n// HTTP endpoint — for centralized policy servers\n// Non-2xx or connection failure is non-blocking; block via JSON body with decision:\"block\"\n{ \"type\": \"http\", \"url\": \"https://policy.corp.internal/hooks\", \"timeout\": 10 }\n```\n\n```json\n// MCP tool — delegates to an already-connected MCP server\n// Non-blocking error if server not connected or tool returns isError:true\n{ \"type\": \"mcp_tool\", \"server\": \"security-scanner\", \"tool\": \"scan_file\",\n  \"input\": { \"path\": \"${tool_input.file_path}\" } }\n```\n\n```json\n// prompt — single-turn Claude evaluation (default timeout: 30s)\n{ \"type\": \"prompt\", \"prompt\": \"Is this change safe? $ARGUMENTS\",\n  \"model\": \"claude-haiku-4-5\" }\n```\n\n```json\n// agent — spawns a subagent that can use Read, Grep, Glob (experimental, default timeout: 60s)\n{ \"type\": \"agent\", \"prompt\": \"Validate: $ARGUMENTS\" }\n```\n\nFor team infrastructure, `command` and `http` are the practical choices. `prompt` and `agent` hooks add latency on every matching tool call; reserve them for genuinely ambiguous judgment calls where deterministic scripts cannot do the job.\n\n### Background Execution\n\nTwo fields enable non-blocking side effects:\n\n```json\n{ \"type\": \"command\", \"command\": \"./hooks/audit-log.sh\", \"async\": true }\n```\n\n- `async: true` — runs in the background without blocking; its result never influences the decision.\n- `asyncRewake: true` — runs in the background but if it exits with code 2, Claude Code wakes Claude and shows the hook's stderr (or stdout if stderr is empty) as a system reminder. This lets a slow background check alert Claude after the fact. `asyncRewake` implies `async`."
   },
   {
    "heading": "Hook Exit-Code Blocking Semantics",
    "body": "The single most important thing to understand about hook exit codes: **only exit code 2 blocks (for blockable events)**. This is a deliberate design choice, not an accident.\n\n### The Three Exit Codes\n\n| Exit code | Blocking? | What Claude Code does |\n|-----------|-----------|----------------------|\n| `0` | No | Parses stdout for JSON structured control; execution continues normally if no JSON or JSON omits a decision |\n| `2` | **Yes (on blockable events)** | Blocks the action; stderr is fed back to Claude or the user as the error message; stdout/JSON is ignored |\n| Any other nonzero (`1`, `3`, etc.) | No | Shows first line of stderr in the transcript with a hook-error notice; full stderr goes to debug log; execution continues |\n\nThe intent is that **broken hooks should not silently block work**. A hook that fails for environmental reasons (script not found, dependency missing, network timeout) exits nonzero but not 2, so work continues with a visible warning. You have to explicitly choose to block by exiting 2.\n\n**Special case:** For `WorktreeCreate`, any non-zero exit code (not just 2) aborts worktree creation.\n\n### Exit Code 2 Effects by Event Type\n\nBlocking has different effects depending on where in the lifecycle the hook fires:\n\n| Blockable events — exit 2 prevents the action | Non-blockable events — exit 2 shows stderr only |\n|-----------------------------------------------|--------------------------------------------------|\n| `PreToolUse` | `PostToolUse` |\n| `PermissionRequest` | `PostToolUseFailure` |\n| `UserPromptSubmit` | `PermissionDenied` |\n| `UserPromptExpansion` | `Notification` |\n| `Stop` | `SubagentStart` |\n| `SubagentStop` | `SessionStart` |\n| `TeammateIdle` | `Setup` |\n| `TaskCreated` | `SessionEnd` |\n| `TaskCompleted` | `CwdChanged` |\n| `ConfigChange` | `FileChanged` |\n| `PreCompact` | `WorktreeRemove` |\n| `Elicitation`, `ElicitationResult` | `PostCompact` |\n| `PostToolBatch` | `StopFailure` |\n| `WorktreeCreate` (any nonzero) | `InstructionsLoaded`, `MessageDisplay` |\n\n### The Alternative: JSON-Based Decisions\n\nFor `PreToolUse`, exiting 2 is the coarse tool. The fine-grained mechanism is returning structured JSON on stdout with exit 0:\n\n```bash\n#!/bin/bash\ninput=$(cat)  # stdin contains the event JSON\ncommand=$(echo \"$input\" | jq -r '.tool_input.command')\n\nif [[ \"$command\" =~ ^rm\\ -rf ]]; then\n  # Fine-grained deny with reason surfaced to Claude\n  jq -nc '{\n    hookSpecificOutput: {\n      hookEventName: \"PreToolUse\",\n      permissionDecision: \"deny\",\n      permissionDecisionReason: \"Recursive delete blocked by policy\"\n    }\n  }'\n  exit 0\nfi\n\n# Rewrite the command instead of blocking\nif [[ \"$command\" == \"cat /etc/passwd\" ]]; then\n  jq -nc '{\n    hookSpecificOutput: {\n      hookEventName: \"PreToolUse\",\n      permissionDecision: \"allow\",\n      updatedInput: { command: \"echo access denied\" }\n    }\n  }'\n  exit 0\nfi\n\nexit 0  # No opinion — normal permission flow applies\n```\n\nThe `permissionDecision` field accepts four values: `deny` (block), `allow` (skip the permission dialog), `ask` (force the dialog even in auto mode), and `defer` (normal flow). The `updatedInput` field lets you rewrite tool arguments before they reach Claude.\n\nFor events other than `PreToolUse`, the JSON blocking mechanism uses a top-level `decision` field instead of `hookSpecificOutput`:\n\n```json\n{ \"decision\": \"block\", \"reason\": \"Explanation shown to Claude\" }\n```\n\nThis form applies to `UserPromptSubmit`, `Stop`, `SubagentStop`, `ConfigChange`, `PreCompact`, and similar non-tool events.\n\n### Common Pitfall: Using exit 1 to Block\n\nThe single most common hook mistake is using `exit 1` thinking it will block an action. It does not. `exit 1` shows a warning in the transcript and continues. **Always use `exit 2` for intentional blocks, or return `permissionDecision: \"deny\"` / `decision: \"block\"` via JSON with exit 0.**"
   },
   {
    "heading": "PreToolUse Policy Enforcement and Permission Mode Interaction",
    "body": "A critical fact about how `PreToolUse` hooks interact with permissions: the relationship is more nuanced than a simple \"hooks fire first\" rule. The official documentation states it precisely:\n\n> Hook decisions do not bypass permission rules. Deny and ask rules are evaluated regardless of what a PreToolUse hook returns, so a matching deny rule blocks the call and a matching ask rule still prompts even when the hook returned `allow` or `ask`. A blocking hook also takes precedence over allow rules — a hook that exits with code 2 stops the tool call before permission rules are evaluated.\n\nThe key design: **a blocking hook pre-empts allow rules, but does not override deny rules — deny rules remain in force regardless**.\n\n### The Permission Mode Landscape\n\n| Mode | What it does |\n|------|-------------|\n| `default` | Prompts for permission on first use of each tool |\n| `acceptEdits` | Auto-accepts file edits and common filesystem commands for paths in the working directory |\n| `plan` | Claude reads files and runs read-only shell commands but does not edit source files |\n| `auto` | Auto-approves tool calls with background safety checks (research preview) |\n| `dontAsk` | Auto-denies tools unless pre-approved via `/permissions` or `permissions.allow` rules |\n| `bypassPermissions` | Skips permission prompts, except those forced by explicit `ask` rules and certain destructive commands (`rm -rf /`, `rm -rf ~`) |\n\nAdministrators can prevent `bypassPermissions` or `auto` mode from being used by setting `permissions.disableBypassPermissionsMode` or `permissions.disableAutoMode` to `\"disable\"` in managed settings.\n\n### Practical Consequence for Team Policy\n\nIf you want to prevent `git push --force` on the main branch across your team, a `PreToolUse` hook with `permissionDecision: \"deny\"` blocks the call before allow rules are evaluated — and does so in every permission mode including `bypassPermissions`, because a blocking hook pre-empts allow-rule evaluation entirely:\n\n```bash\n#!/bin/bash\n# .claude/hooks/git-policy.sh\ninput=$(cat)\ntool=$(echo \"$input\" | jq -r '.tool_name')\ncommand=$(echo \"$input\" | jq -r '.tool_input.command // empty')\n\nif [[ \"$tool\" == \"Bash\" ]] && echo \"$command\" | grep -qE 'git push.*(--force|-f)';\nthen\n  branch=$(git symbolic-ref --short HEAD 2>/dev/null)\n  if [[ \"$branch\" == \"main\" || \"$branch\" == \"master\" ]]; then\n    jq -nc '{\n      hookSpecificOutput: {\n        hookEventName: \"PreToolUse\",\n        permissionDecision: \"deny\",\n        permissionDecisionReason: \"Force-push to main/master is prohibited by team policy.\"\n      }\n    }'\n    exit 0\n  fi\nfi\n\nexit 0\n```\n\n```json\n// .claude/settings.json (committed)\n{\n  \"hooks\": {\n    \"PreToolUse\": [\n      {\n        \"matcher\": \"Bash\",\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"${CLAUDE_PROJECT_DIR}/.claude/hooks/git-policy.sh\",\n            \"timeout\": 5\n          }\n        ]\n      }\n    ]\n  }\n}\n```\n\n### Reading the Event Payload\n\nHooks receive the full event JSON on stdin. For `PreToolUse`:\n\n```json\n{\n  \"session_id\": \"abc123\",\n  \"transcript_path\": \"/path/to/transcript.jsonl\",\n  \"cwd\": \"/home/dev/myproject\",\n  \"permission_mode\": \"auto\",\n  \"effort\": { \"level\": \"high\" },\n  \"hook_event_name\": \"PreToolUse\",\n  \"tool_name\": \"Bash\",\n  \"tool_input\": { \"command\": \"rm -rf ./dist\" }\n}\n```\n\nThe `permission_mode` field tells you what mode is currently active. The `transcript_path` field gives a JSONL file of the full conversation history — useful for hooks that need broader context about what Claude is doing, not just the single tool call.\n\n### PostToolUse: What You Can and Cannot Do\n\n`PostToolUse` is non-blockable — the tool already ran. What it can do:\n- Add context for Claude via `additionalContext` (e.g., \"this file is machine-generated; always edit the source\")\n- Redact sensitive output via `updatedToolOutput` (e.g., scrub API keys from a curl response before Claude sees them)\n- Log the action to an audit trail\n- Trigger downstream side effects (run a formatter, update a metrics dashboard)\n\nThe `updatedToolOutput` pattern is underused but powerful for teams with compliance requirements — it lets you sanitize tool results before they enter Claude's context window, and is the correct place to implement output redaction rather than trying to block the tool before it runs."
   },
   {
    "heading": "Parallel Hook Execution and Most-Restrictive-Wins Merge",
    "body": "When multiple hook groups match the same event, Claude Code runs all matching hooks in parallel, waits for all of them to complete, then merges the results.\n\n### Parallel Execution\n\nMultiple matcher groups under the same event all fire concurrently:\n\n```json\n{\n  \"hooks\": {\n    \"PostToolUse\": [\n      {\n        \"matcher\": \"Write|Edit\",\n        \"hooks\": [{ \"type\": \"command\", \"command\": \"./hooks/lint.sh\" }]\n      },\n      {\n        \"matcher\": \"Write|Edit\",\n        \"hooks\": [{ \"type\": \"command\", \"command\": \"./hooks/format.sh\" }]\n      },\n      {\n        \"matcher\": \"*\",\n        \"hooks\": [{ \"type\": \"command\", \"command\": \"./hooks/audit-log.sh\", \"async\": true }]\n      }\n    ]\n  }\n}\n```\n\n`lint.sh` and `format.sh` run in parallel. `audit-log.sh` runs asynchronously — its result is not awaited and does not influence the merge.\n\n**Deduplication:** Identical handlers are deduplicated automatically before execution — command hooks by command string and args, HTTP hooks by URL. The same handler registered in both `~/.claude/settings.json` and `.claude/settings.json` runs only once.\n\n### Merge Rules\n\n| Result type | Merge behavior |\n|-------------|----------------|\n| `decision: block` or `permissionDecision: deny` | Any blocking result stops execution; the most-restrictive decision wins |\n| `additionalContext` | Values from all hooks are concatenated |\n| `updatedToolOutput` | First non-empty value wins |\n| Hook order | Undefined — do not rely on execution order |\n\nThe **most-restrictive-wins** principle means: if any hook in the parallel set blocks, the action is blocked — regardless of what other hooks returned. You cannot have an allow hook \"override\" a deny hook by running last.\n\n### Cross-Scope Hook Merging\n\nThis same principle applies across configuration scopes. Hooks from managed settings, project settings, user settings, local settings, and plugins all participate in the same parallel-then-merge cycle.\n\nThe `allowManagedHooksOnly` managed setting locks this down: when `true`, only managed hooks, SDK hooks, and hooks from plugins explicitly listed in `enabledPlugins` (managed settings) are loaded. User, project, local, and all other plugin hooks are ignored.\n\n### Hook Priority Table\n\n| Source | Runs when | Shareable |\n|--------|-----------|----------|\n| Managed policy settings | Always | Admin-deployed |\n| `.claude/settings.json` | In this project | Yes (committed) |\n| `~/.claude/settings.json` | In all projects | No |\n| `.claude/settings.local.json` | In this project, personally | No (gitignored) |\n| Plugin `hooks/hooks.json` | When plugin enabled | Bundled with plugin |\n| Skill/Agent frontmatter | While component is active | With the component |\n\nAll tiers participate in the same parallel-then-merge cycle. There is no \"first one wins\" short-circuit except for the deny decision itself."
   },
   {
    "heading": "Component-Scoped and Plugin Hooks",
    "body": "Beyond the global `hooks` key in settings files, hooks can be scoped to the lifetime of a specific skill, subagent, or plugin. This is the mechanism for making hooks activate only when a particular workflow is active.\n\n### Skill-Scoped Hooks\n\nIn a `SKILL.md` frontmatter, the `hooks` key uses the same nested format as `settings.json`:\n\n```yaml\n---\nname: secure-deploy\ndescription: Deploys the application with security checks enabled\nhooks:\n  PreToolUse:\n    - matcher: \"Bash\"\n      hooks:\n        - type: command\n          command: \"${CLAUDE_SKILL_DIR}/scripts/deployment-guard.sh\"\n          timeout: 10\n  PostToolUse:\n    - matcher: \"Bash\"\n      hooks:\n        - type: command\n          command: \"${CLAUDE_SKILL_DIR}/scripts/deployment-audit.sh\"\n          async: true\n---\n\nDeploy the application to $ARGUMENTS...\n```\n\nThese hooks are **active only while the skill is active** — from the moment the skill is invoked until the component finishes. They are cleaned up automatically.\n\nSkill frontmatter supports a `once` field on individual handlers:\n\n```yaml\nhooks:\n  SessionStart:\n    - matcher: \"startup\"\n      hooks:\n        - type: command\n          command: \"${CLAUDE_SKILL_DIR}/scripts/setup-once.sh\"\n          once: true\n```\n\n`once: true` runs the handler once per session then removes it. **This field is only honored in skill frontmatter** — it is ignored in `settings.json` and agent frontmatter.\n\n**The Stop→SubagentStop conversion:** For subagents, if you define a `Stop` hook in frontmatter, it is automatically converted to `SubagentStop`, since that is the event that fires when a subagent completes. Write `Stop` and it does the right thing in both skill and subagent contexts.\n\n### Plugin Hooks\n\nPlugins distribute hooks via `hooks/hooks.json` at the plugin root:\n\n```\nmy-formatter-plugin/\n├── plugin.json\n├── hooks/\n│   └── hooks.json\n└── skills/\n    └── format/SKILL.md\n```\n\n```json\n// hooks/hooks.json\n{\n  \"description\": \"Automatic code formatting after file writes\",\n  \"hooks\": {\n    \"PostToolUse\": [\n      {\n        \"matcher\": \"Write|Edit\",\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"${CLAUDE_PLUGIN_ROOT}/scripts/format.sh\",\n            \"timeout\": 30\n          }\n        ]\n      }\n    ]\n  }\n}\n```\n\n`CLAUDE_PLUGIN_ROOT` resolves to the plugin's installation directory at runtime (it changes on each plugin update). Plugin hooks also support `${CLAUDE_PLUGIN_DATA}` for the plugin's persistent data directory, and `${user_config.*}` for user-configurable plugin settings.\n\nPlugin hooks merge with settings-based hooks when the plugin is enabled and are deactivated when the plugin is disabled. **Changes to `hooks/hooks.json`, `.mcp.json`, `agents/`, and `output-styles/` within a plugin require `/reload-plugins` to take effect** in the current session — unlike `SKILL.md` text changes, which are picked up via live reload.\n\n### When to Use Each Scope\n\n| Scope | Use when |\n|-------|----------|\n| `settings.json` hooks | Policy that applies to every session and every tool call |\n| Skill frontmatter hooks | Checks that only make sense during a specific workflow; use `once:true` for one-time setup |\n| Subagent frontmatter hooks | Guardrails for an autonomous agent's isolated execution |\n| Plugin hooks | Distributable tooling (formatters, linters, audit trails) that teams opt into |\n\n### Common Pitfall: Hook File Paths\n\nSkill-scoped hooks that reference scripts must use `${CLAUDE_SKILL_DIR}` for path resolution, not relative paths. Relative paths resolve from the working directory (the user's project root), not from the skill directory — this breaks when the skill is installed at the personal level (`~/.claude/skills/`) rather than the project level. Always anchor bundled scripts to `${CLAUDE_SKILL_DIR}/scripts/`."
   },
   {
    "heading": "Slash Command Invocation and Availability",
    "body": "Slash commands and skills are the same underlying system. A file at `.claude/commands/deploy.md` and a skill at `.claude/skills/deploy/SKILL.md` both create `/deploy` and work identically. The skill directory layout is recommended for new work because it supports supporting files, frontmatter, and plugin bundling. When a skill and a command share the same name, **the skill takes precedence** over the command file.\n\n### Recognition Rules\n\nA slash command is recognized **only when `/name` appears at the very start of a message**. Any preceding text — including a single space — causes it to be treated as regular prose. Everything after the command name (separated by a space) becomes `$ARGUMENTS`:\n\n```\n/deploy staging              # Recognized: ARGUMENTS = \"staging\"\n/deploy                     # Recognized: ARGUMENTS = \"\"\ncan you /deploy staging     # NOT a command — regular prose\n /deploy staging             # NOT recognized — leading space\n```\n\n### How Command Names Are Resolved\n\n| Skill location | Command name |\n|----------------|--------------|\n| `.claude/skills/deploy-staging/SKILL.md` | `/deploy-staging` (directory name) |\n| `.claude/commands/deploy.md` | `/deploy` (filename without extension) |\n| `~/.claude/skills/my-workflow/SKILL.md` | `/my-workflow` (directory name) |\n| Plugin `skills/review/SKILL.md` | `/my-plugin:review` (plugin-namespaced) |\n| Plugin root `SKILL.md` with `name: review` | `/my-plugin:review` (frontmatter `name` used here only) |\n| Nested `apps/web/.claude/skills/deploy/SKILL.md` when name clashes | `/apps/web:deploy` (directory-qualified) |\n\nThe frontmatter `name` field sets the **display label** shown in skill listings everywhere except the plugin-root `SKILL.md` case — there it also sets the command name because there is no directory to take it from. In all other layouts, the directory name alone determines what you type after `/`.\n\n### Availability Conditions\n\nA command is available when:\n1. Its `SKILL.md` or command file is present in a watched directory (project `.claude/skills/`, personal `~/.claude/skills/`, or a `--add-dir` added directory)\n2. It is not set to `\"off\"` in `skillOverrides`\n3. For user-invocation: `user-invocable` is not `false`\n4. For model-invocation: `disable-model-invocation` is not `true`\n\nBundled skills (`/code-review`, `/debug`, `/batch`, `/loop`, `/run`, `/verify`, etc.) are available unless `disableBundledSkills: true` is set in settings. A project skill with the same name as a bundled skill takes precedence.\n\n### The Two Invocation Paths\n\n| Frontmatter | You can invoke | Claude can invoke | When loaded into context |\n|-------------|---------------|------------------|--------------------------|\n| (default, neither field set) | Yes | Yes | Description always in context; full skill loads when invoked |\n| `disable-model-invocation: true` | Yes | No | **Description not loaded into context at all** — skill is invisible to Claude |\n| `user-invocable: false` | No | Yes | Description in context; full skill loads when invoked; hidden from `/` menu |\n\nWith `disable-model-invocation: true`, the description is not loaded into Claude's context at all — the skill is invisible to the model and cannot be auto-triggered. Use this for deploy/commit workflows with side effects where you want explicit control over timing.\n\n### Skill Name Precedence When Conflicts Exist\n\nWhen the same name exists at multiple levels, the resolution order is:\n1. Enterprise managed overrides personal\n2. Personal (`~/.claude/skills/`) overrides project\n3. Project overrides bundled skills\n4. Plugin skills use `plugin-name:skill-name` namespace and cannot conflict with other levels\n\nNested skills that would conflict with an existing name get a directory-qualified name (`apps/web:deploy`) automatically — both variants remain available simultaneously. Typing `/deploy` runs the root-level skill; `/apps/web:deploy` runs the nested one explicitly."
   },
   {
    "heading": "Skill Frontmatter: Full Reference",
    "body": "The `SKILL.md` frontmatter is the primary configuration interface for a skill's behavior, invocation control, tool permissions, and lifecycle hooks. All fields are optional; only `description` is strongly recommended.\n\n```yaml\n---\nname: team-deploy\ndescription: Deploy the application. Use when the user asks to deploy, push to staging, or release.\nwhen_to_use: Trigger phrases — 'deploy', 'push to staging', 'release', 'ship it'\nargument-hint: \"[environment] [version]\"\narguments: [environment, version]\ndisable-model-invocation: true\nallowed-tools: Bash(make deploy *) Bash(gh release *) Bash(git tag *)\ndisallowed-tools: AskUserQuestion\ncontext: fork\nagent: general-purpose\nmodel: claude-sonnet-4-6\neffort: high\npaths: \"apps/**,services/**\"\nhooks:\n  PreToolUse:\n    - matcher: \"Bash\"\n      hooks:\n        - type: command\n          command: \"${CLAUDE_SKILL_DIR}/scripts/pre-deploy-check.sh\"\n          timeout: 30\n  PostToolUse:\n    - matcher: \"Bash\"\n      hooks:\n        - type: http\n          url: \"https://deploy-tracker.corp.internal/log\"\n          timeout: 5\n---\n\nDeploy $environment (version $version):\n\n1. Run `make test` and fail if tests do not pass\n2. Run `make deploy ENV=$environment VERSION=$version`\n3. Verify the deployment with `make smoke-test ENV=$environment`\n```\n\n### Complete Field Reference\n\n| Field | Default | Description |\n|-------|---------|-------------|\n| `name` | Directory name | Display label in skill listings. Does not change the invocation command except for plugin-root `SKILL.md` files. |\n| `description` | First paragraph of body | What the skill does and when to use it. Claude uses this for auto-invocation decisions. Combined with `when_to_use`, truncated at 1,536 characters (configurable via `maxSkillDescriptionChars`). |\n| `when_to_use` | — | Additional trigger phrases. Appended to `description` in the skill listing; counts toward the 1,536-character cap. |\n| `argument-hint` | — | Hint shown in autocomplete, e.g. `[issue-number]` or `[env] [version]`. |\n| `arguments` | — | Named positional arguments for `$name` substitution. Space-separated string or YAML list. |\n| `disable-model-invocation` | `false` | When `true`, description is not loaded into Claude's context; skill is invisible to the model. |\n| `user-invocable` | `true` | When `false`, hidden from the `/` menu but Claude can still invoke it. |\n| `allowed-tools` | — | Tools Claude can use without prompting while this skill is active. Permission-rule syntax: `Bash(make deploy *)`. Takes effect after workspace trust dialog for project skills. |\n| `disallowed-tools` | — | Tools removed from Claude's pool while active. Clears when you send your next message. |\n| `context` | inline | Set to `fork` to run in an isolated subagent. The skill content becomes the subagent's prompt; no conversation history is passed. |\n| `agent` | `general-purpose` | Which subagent type to use when `context: fork`. Accepts built-in names (`Explore`, `Plan`, `general-purpose`) or any custom agent from `.claude/agents/`. |\n| `model` | Session model | Override model for this skill's turn. Not saved to settings; session model resumes on next prompt. |\n| `effort` | Session effort | Override effort level (`low`, `medium`, `high`, `xhigh`, `max`). |\n| `paths` | — | Glob patterns gating auto-invocation. Claude loads skill automatically only when working with matching files. |\n| `hooks` | — | Component-scoped hooks. Cleaned up when skill finishes. See hook section for format. |\n| `shell` | `bash` | Shell for `!\\`command\\`` blocks: `bash` or `powershell`. |\n\n### Field-by-Field Notes\n\n**`allowed-tools`** grants permission for listed tools during the skill's active turn, bypassing the usual permission prompt. The grant applies *only* while the skill is active. For project skills, it takes effect after the user accepts the workspace trust dialog. It does not restrict other tools — every tool remains callable with normal permission rules.\n\n**`context: fork`** runs the skill in an isolated subagent. The skill content becomes the subagent's prompt. The subagent does not have access to conversation history. Built-in Explore and Plan agents skip CLAUDE.md and git status to keep context small; a forked skill using `agent: Explore` sees only the SKILL.md content and the agent's system prompt.\n\n**`paths`** is useful in monorepos: a `deploy` skill at `packages/frontend/.claude/skills/deploy/` with `paths: \"packages/frontend/**\"` will only auto-invoke when Claude is working on frontend files.\n\n**`model` + `effort`** override applies for the rest of the current turn only. It is not saved to settings; the session model and effort level resume on your next message."
   },
   {
    "heading": "Dynamic Context Injection in Skills",
    "body": "Skills can inject live data into their prompt before Claude sees it, using the `` !`command` `` syntax. This is preprocessing — the command runs at skill invocation time and its output replaces the placeholder. Claude receives the rendered result, not the command itself. Crucially, command output is inserted as plain text and is **not re-scanned** for further `` !`command` `` placeholders — a command cannot emit a placeholder for a later pass to expand.\n\n### Inline Form\n\n```yaml\n---\nname: pr-review\ndescription: Review the current pull request\ncontext: fork\nagent: Explore\nallowed-tools: Bash(gh *) Read Grep\n---\n\n## Pull request context\n\n- Current diff: !`gh pr diff`\n- PR description: !`gh pr view --json title,body -q '.title + \"\\n\\n\" + .body'`\n- Changed files: !`gh pr diff --name-only`\n- CI status: !`gh pr checks --json name,state -q '.[] | .name + \": \" + .state'`\n\n## Instructions\n\nReview the above pull request for correctness, security issues, and adherence\nto team conventions. Flag blocking issues separately from suggestions.\n```\n\nThe inline form requires `!` at the start of a line or immediately after whitespace. The pattern `` KEY=!`cmd` `` is **not recognized** — the `!` must start a token, not follow another character. That assignment would be left as literal text.\n\n### Block Form\n\nFor multi-line shell commands, use a fenced code block opened with ` ```! `:\n\n````yaml\n---\nname: env-check\ndescription: Diagnose the current environment\n---\n\n## Environment state\n\n```!\nnode --version\nnpm --version\ngit status --short\ngit log --oneline -5\n```\n````\n\n### Security and Control\n\nShell injection in skill content runs in the user's environment with the user's permissions. For project skills checked into a repository, every developer who opens the repository and accepts the workspace trust dialog has effectively consented to whatever these scripts do. **Review project skills before trusting a repository** — the trust dialog is the gate.\n\nAdministrators can disable skill shell execution entirely via managed settings:\n\n```json\n// managed-settings.json\n{\n  \"disableSkillShellExecution\": true\n}\n```\n\nWith this set, `` !`command` `` blocks are replaced with `[shell command execution disabled by policy]`. Bundled and managed skills are not affected — only user, project, plugin, and additional-directory skills.\n\n### String Substitutions Available in Skills\n\n| Variable | Value |\n|----------|-------|\n| `$ARGUMENTS` | All text after the command name. If not present in the content, arguments are appended as `ARGUMENTS: <value>`. |\n| `$ARGUMENTS[N]` | Nth argument (0-based). Multi-word values require quoting: `/skill \"hello world\"` makes `$ARGUMENTS[0]` = `hello world`. |\n| `$N` | Shorthand for `$ARGUMENTS[N]` (e.g., `$0`, `$1`). |\n| `$name` | Named argument from `arguments` frontmatter list. |\n| `${CLAUDE_SESSION_ID}` | Current session ID — useful for log file naming or correlating outputs. |\n| `${CLAUDE_EFFORT}` | Current effort level (`low`/`medium`/`high`/`xhigh`/`max`). `xhigh` is also what Ultracode reports. |\n| `${CLAUDE_SKILL_DIR}` | Directory containing this `SKILL.md` — use for all script references. |\n\nTo include a literal `$` before a digit or `ARGUMENTS`, escape with a backslash: `\\$1.00`. A backslash before any other `$` is left unchanged."
   },
   {
    "heading": "Common Pitfalls and Failure Modes",
    "body": "### Hook Pitfalls\n\n**Exit code 1 does not block.** The single most common mistake. If your hook script exits 1, the action continues with a warning entry in the transcript. Use exit 2 or return `permissionDecision: \"deny\"` / `decision: \"block\"` via JSON with exit 0.\n\n**Event names are case-sensitive.** `preToolUse` is silently ignored. The correct names are PascalCase throughout: `PreToolUse`, `PostToolUse`, `UserPromptSubmit`, etc.\n\n**The `if` filter fails closed on non-tool events.** A hook handler with an `if` field on a `SessionStart` or `Stop` hook never runs, because `if` filtering is only evaluated for tool events. If you need to filter session events, use `matcher` instead (where supported) or do the filtering inside the script.\n\n**Hooks without an explicit `timeout` can hang.** A hung hook hangs Claude Code. Set `timeout` explicitly on every production hook; use `async: true` for side-effects (audit logging, metrics) that do not need to influence the decision. For background checks where you still want to alert Claude on failure, use `asyncRewake: true` instead.\n\n**Relative script paths break when a skill is installed at the personal level.** A hook pointing at `./hooks/check.sh` resolves from the project working directory, not from the skill directory. Use `${CLAUDE_SKILL_DIR}/hooks/check.sh` for any script bundled with a skill.\n\n**Plugin hook changes require `/reload-plugins`.** Unlike `SKILL.md` text changes (which are picked up via live reload), changes to a plugin's `hooks/hooks.json`, `.mcp.json`, `agents/`, or `output-styles/` directory require `/reload-plugins` to take effect in the current session.\n\n**`once: true` only works in skill frontmatter.** The `once` field on a handler is honored only when the hook is declared in skill frontmatter — it is silently ignored in `settings.json` and agent frontmatter.\n\n**HTTP hook non-2xx responses are non-blocking.** If your policy server returns 500, execution continues. Only a 2xx response with `decision: \"block\"` in the body blocks the action. Design your policy server to fail open (return 2xx with no decision) rather than fail closed on errors if you do not want hook infrastructure failures to block all work.\n\n### Settings Precedence Pitfalls\n\n**Local settings can override project policy.** `.claude/settings.local.json` sits above `.claude/settings.json` in the precedence stack. A developer can create a local file that overrides team deny rules — except for deny rules coming from the Managed tier. If a policy must be inviolable, it belongs in managed settings.\n\n**Permission rules merge; other settings do not.** All `allow`, `ask`, and `deny` arrays from all scopes are combined into one effective set. If you expect a user's `model` setting to stack with a project's `model` setting, it does not — the higher-precedence one wins entirely. Only permission arrays merge.\n\n**`model` is read once at startup.** Changing `.claude/settings.json` mid-session does not switch the model; use `/model` to switch without restarting. Most other keys (`hooks`, `permissions`, `env`) are watched and reload live via the `ConfigChange` hook.\n\n**The `claudeMd` managed key only works in managed settings.** Placing `claudeMd` in user, project, or local settings has no effect — it is silently ignored. It is only honored from managed or policy settings.\n\n### Slash Command Pitfalls\n\n**Any text before the slash breaks recognition.** `can you /deploy` is not a command invocation. The slash must be the very first character of the message.\n\n**`name` in frontmatter is not the invocation name** (with one exception). The directory name determines what you type after `/`. The frontmatter `name` field only sets the display label — except for the plugin-root `SKILL.md` case, where there is no directory name and frontmatter `name` is used.\n\n**Skill description budget overflow silently drops descriptions.** When the total description text across all skills exceeds the budget (1% of the model's context window by default, configurable via `skillListingBudgetFraction`), descriptions for the least-used skills are dropped first. Run `/doctor` to see which skills are affected; use `skillOverrides` to set low-priority skills to `\"name-only\"` to free budget. The per-entry cap of 1,536 characters is separate from this budget.\n\n**`disable-model-invocation: true` makes the skill completely invisible to Claude.** The description is not loaded into context at all. Claude cannot trigger it automatically, reference it by name, or see it in listings. `/skill-name` typed by the user is the only invocation path. This is correct for deploy/commit workflows, but wrong for reference skills where Claude needs to know the skill exists."
   },
   {
    "heading": "Team Setup: Putting It All Together",
    "body": "Here is a concrete file layout and configuration for a team adopting Claude Code, covering the most important policy, hook, and skill decisions.\n\n### Repository Layout\n\n```\n.claude/\n├── settings.json              # Committed: team permissions, hooks, env\n├── settings.local.json        # Gitignored: individual overrides\n├── hooks/\n│   ├── pre-tool-policy.sh     # Committed: enforces team policy\n│   └── post-write-lint.sh     # Committed: runs linter after file writes\n└── skills/\n    ├── commit/SKILL.md        # Committed: team commit workflow\n    └── deploy/SKILL.md        # Committed: deployment workflow\n.mcp.json                      # Committed: project MCP servers\nCLAUDE.md                      # Committed: project architecture context\nCLAUDE.local.md                # Gitignored: personal working notes\n```\n\n### `.claude/settings.json`\n\n```json\n{\n  \"$schema\": \"https://json.schemastore.org/claude-code-settings.json\",\n  \"permissions\": {\n    \"allow\": [\n      \"Bash(npm run lint)\",\n      \"Bash(npm run test *)\",\n      \"Bash(git status)\",\n      \"Bash(git log *)\",\n      \"Bash(git diff *)\"\n    ],\n    \"deny\": [\n      \"Bash(git push --force *)\",\n      \"Bash(git push -f *)\",\n      \"Read(.env)\",\n      \"Read(.env.*)\",\n      \"Read(./secrets/**)\"\n    ]\n  },\n  \"hooks\": {\n    \"PreToolUse\": [\n      {\n        \"matcher\": \"Bash\",\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"${CLAUDE_PROJECT_DIR}/.claude/hooks/pre-tool-policy.sh\",\n            \"timeout\": 10\n          }\n        ]\n      }\n    ],\n    \"PostToolUse\": [\n      {\n        \"matcher\": \"Write|Edit\",\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"${CLAUDE_PROJECT_DIR}/.claude/hooks/post-write-lint.sh\",\n            \"timeout\": 30\n          }\n        ]\n      }\n    ]\n  },\n  \"env\": {\n    \"NODE_ENV\": \"development\"\n  }\n}\n```\n\n### `.claude/hooks/pre-tool-policy.sh`\n\n```bash\n#!/bin/bash\n# Reads the PreToolUse event from stdin, enforces team Bash policies.\n# Exit 0 with JSON deny = clean denial message to Claude.\n# Exit 2 = hard stop (avoid — prefer JSON deny for better UX).\n\nset -euo pipefail\ninput=$(cat)\ntool=$(echo \"$input\" | jq -r '.tool_name')\ncommand=$(echo \"$input\" | jq -r '.tool_input.command // empty')\n\nif [[ \"$tool\" != \"Bash\" ]]; then\n  exit 0\nfi\n\n# Block force-push to protected branches\nif echo \"$command\" | grep -qE 'git push.*(--force|-f)'; then\n  branch=$(git symbolic-ref --short HEAD 2>/dev/null || echo \"unknown\")\n  if [[ \"$branch\" =~ ^(main|master|release/.*)$ ]]; then\n    jq -nc '{\n      hookSpecificOutput: {\n        hookEventName: \"PreToolUse\",\n        permissionDecision: \"deny\",\n        permissionDecisionReason: \"Force-push to protected branches is prohibited.\"\n      }\n    }'\n    exit 0\n  fi\nfi\n\nexit 0\n```\n\n### `.claude/skills/deploy/SKILL.md`\n\n```yaml\n---\nname: team-deploy\ndescription: Deploy the application to an environment. Use when asked to deploy, release, or push to staging or production.\ndisable-model-invocation: true\nallowed-tools: Bash(make *) Bash(gh release *)\ndisallowed-tools: AskUserQuestion\ncontext: fork\nagent: general-purpose\nhooks:\n  PostToolUse:\n    - matcher: \"Bash\"\n      hooks:\n        - type: command\n          command: \"${CLAUDE_SKILL_DIR}/scripts/log-deploy.sh\"\n          async: true\n---\n\nDeploy to $ARGUMENTS:\n\n1. Run `make test` — abort with a clear error if tests fail\n2. Run `make build`\n3. Run `make deploy ENV=$ARGUMENTS`\n4. Verify with `make smoke-test ENV=$ARGUMENTS`\n5. Report success or failure with the deploy URL\n```\n\n### Validation Checklist Before Rolling Out\n\n- `claude --version` — confirm v2.1.145+ for bundled skills; v2.1.175+ for `managed-settings.d/`\n- `cat .gitignore | grep settings.local` — confirm `.claude/settings.local.json` is gitignored\n- Test each hook script manually: `echo '{\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git push --force origin main\"}}' | .claude/hooks/pre-tool-policy.sh`\n- Run `claude /doctor` inside the project to check for skill description budget overflow and config parse errors\n- Verify `.mcp.json` is committed and MCP servers connect on a fresh clone"
   }
  ],
  "preQuiz": [
   {
    "prompt": "Your team commits a `PreToolUse` hook to `.claude/settings.json` that blocks destructive Bash commands. A developer adds a personal rule to `~/.claude/settings.json` that disables the same hook. When the developer runs Claude Code, which setting wins?",
    "options": [
     "The project `.claude/settings.json` wins because project config always overrides user config.",
     "The `~/.claude/settings.json` wins because user config overrides project config.",
     "The managed policy settings win if they exist; otherwise the local settings file `.claude/settings.local.json` is authoritative.",
     "The two configs are merged; conflicting values produce an error that Claude reports at startup."
    ],
    "correct": 0,
    "sectionIndices": [
     1
    ],
    "explanation": "Settings resolve in ascending order: user → project → local → managed policy. Project `.claude/settings.json` overrides the user-level `~/.claude/settings.json`, so the team hook committed to the project file takes precedence."
   },
   {
    "prompt": "A developer wants to experiment with a new hook that only applies to their own machine, not the team. They have three candidate files: `~/.claude/settings.json`, `.claude/settings.json`, and `.claude/settings.local.json`. Which file should they write it to, and why?",
    "options": [
     "`.claude/settings.json` — it is the highest-precedence non-admin file and will override user config.",
     "`.claude/settings.local.json` — it is gitignored, so it won't impose the experiment on the rest of the team.",
     "`~/.claude/settings.json` — user-level config is the correct place for anything that should not be committed.",
     "Either `~/.claude/settings.json` or `.claude/settings.local.json` are equivalent choices because both are personal and not committed."
    ],
    "correct": 1,
    "sectionIndices": [
     1
    ],
    "explanation": "`.claude/settings.local.json` is gitignored by convention and is the correct home for personal/experimental project-scoped overrides. `~/.claude/settings.json` would apply to all projects, not just this one."
   },
   {
    "prompt": "You configure a `PreToolUse` hook with the matcher value `bash`. The hook never fires even though Bash is frequently called. What is the most likely explanation?",
    "options": [
     "Matchers on `PreToolUse` only accept regex patterns, not plain strings.",
     "Matchers are case-sensitive: the tool name is `Bash` (capital B), so `bash` silently never matches.",
     "Hooks on `PreToolUse` require the `if` filter to be set before the matcher is evaluated.",
     "`PreToolUse` hooks only fire when the tool call is blocked, not when it is allowed."
    ],
    "correct": 1,
    "sectionIndices": [
     12
    ],
    "explanation": "Matchers are case-sensitive. The tool name is `Bash` with a capital B; the matcher `bash` silently never fires. This is explicitly called out as a pitfall in the content."
   },
   {
    "prompt": "A hook handler exits with code `1` while checking a pre-commit file. You expected this to block the operation and show an error message to Claude. What actually happens?",
    "options": [
     "Exit code `1` blocks execution and sends stderr to Claude as feedback, same as exit code `2`.",
     "Exit code `1` is non-blocking — only exit code `2` blocks; any other non-zero code is treated as a non-fatal error.",
     "Exit code `1` blocks execution but stdout is discarded; only exit code `2` includes stderr feedback.",
     "Exit code `1` causes Claude Code to terminate the entire session with a fatal error."
    ],
    "correct": 1,
    "sectionIndices": [
     14
    ],
    "explanation": "Only exit code `2` blocks. Any other non-zero exit code (including `1`) is non-blocking — execution continues. Only exit code `2` causes stderr to be fed back to Claude as feedback."
   },
   {
    "prompt": "Your team sets up an `http` hook handler that returns an HTTP 403 status to block a tool call. The tool call goes through anyway. What is the issue?",
    "options": [
     "HTTP hooks cannot block tool calls; only `command` type hooks can block.",
     "HTTP hooks must return a 2xx status code with a body containing `hookSpecificOutput` to block — HTTP status alone cannot block.",
     "The hook is firing asynchronously because `async: true` is set by default for `http` hooks.",
     "Blocking via HTTP requires exit code `2` to be returned in a special header `X-Hook-Exit`."
    ],
    "correct": 1,
    "sectionIndices": [
     10
    ],
    "explanation": "HTTP handler hooks can only block via a 2xx response body containing `hookSpecificOutput`. HTTP status codes alone (like 403) cannot block execution."
   },
   {
    "prompt": "You add an `if` filter to a `UserPromptSubmit` hook, hoping to limit it to prompts containing the word 'deploy'. The hook stops firing entirely. What explains this?",
    "options": [
     "`UserPromptSubmit` requires the `if` filter to use a regex, not a plain string.",
     "Adding an `if` filter to a non-tool event (like `UserPromptSubmit`) prevents the hook from running at all.",
     "The `if` filter on `UserPromptSubmit` only applies to the first prompt per session and then disables itself.",
     "The `if` filter is not evaluated until after the hook exits; a hook that exits `0` before the filter runs causes the issue."
    ],
    "correct": 1,
    "sectionIndices": [
     13
    ],
    "explanation": "The `if` filter is only valid for tool-name events (`PreToolUse`, `PostToolUse`, etc.). Adding it to a non-tool event like `UserPromptSubmit` prevents the hook from running at all."
   },
   {
    "prompt": "A teammate runs `/model claude-opus-4-5` with the intent of using that model only for the current session, then forgets to switch back. In a new session the next day, which model is active?",
    "options": [
     "The session's model selection does not persist; Claude Code always resets to the default on a new session.",
     "The model selected with `/model` is saved as the default for new sessions, so `claude-opus-4-5` will be active the next day.",
     "The model reverts to the team's configured default in `.claude/settings.json` because `/model` only affects the current project.",
     "The teammate must run `/model --save` for the selection to persist; without the flag, it's session-only."
    ],
    "correct": 1,
    "sectionIndices": [
     4
    ],
    "explanation": "`/model <model>` switches the model AND saves it as the default for new sessions. To switch for the current session only without saving, the user must press `s` in the model picker."
   },
   {
    "prompt": "You want a hook to run a slow background integrity check after every file edit, and have the check's findings injected as a system reminder before Claude's NEXT response (not interrupting the current one). Which hook configuration achieves this?",
    "options": [
     "Set `async: true` on a `PostToolUse` hook matching `Edit`.",
     "Set `asyncRewake: true` on a `PostToolUse` hook matching `Edit`, and exit with code `2` when findings are ready.",
     "Set `async: true` on the hook and exit with code `0`; stdout is automatically injected as a reminder.",
     "Use `type: agent` with `async: true` — only agent-type hooks support rewake behavior."
    ],
    "correct": 1,
    "sectionIndices": [
     10
    ],
    "explanation": "`asyncRewake: true` runs the hook in the background and wakes Claude on exit `2`, with stderr/stdout shown as a system reminder on the next model request. This is the mechanism for background monitors that need to report back."
   },
   {
    "prompt": "After editing a skill file on disk, a developer notices Claude Code isn't picking up the changes. They try `/reload-plugins` but the changes still don't apply. What should they run instead?",
    "options": [
     "`/plugin disable` followed by `/plugin enable` to force a full plugin reload.",
     "`/reload-skills` — skill/command directories require a separate reload command, available in v2.1.152+.",
     "Restart Claude Code entirely — neither `/reload-plugins` nor `/reload-skills` picks up skill content changes.",
     "`/doctor` to diagnose why the skill is not loading, then `/reload-plugins --force`."
    ],
    "correct": 1,
    "sectionIndices": [
     8
    ],
    "explanation": "`/reload-skills` (v2.1.152+) re-scans skill/command directories so on-disk changes apply without restart. `/reload-plugins` handles plugins, not standalone skill files."
   },
   {
    "prompt": "Your team uses `/add-dir /path/to/shared-lib` to give Claude access to a shared library directory. A developer reports that the shared library's `.claude/settings.json` hooks are not running. What is the correct explanation?",
    "options": [
     "`/add-dir` loads the directory's `.claude/settings.json` config, but hooks require explicit opt-in via `hooksEnabled: true`.",
     "`/add-dir` does NOT load the added directory's `.claude/` config (settings, hooks, commands) — only `.claude/skills/` from that directory is loaded.",
     "`/add-dir` only grants file-read access; to load config from the directory, use `/cd` instead.",
     "Hooks from added directories are loaded but run in a sandboxed mode without blocking capability."
    ],
    "correct": 1,
    "sectionIndices": [
     7
    ],
    "explanation": "`/add-dir` grants file access to a directory but does NOT load that directory's `.claude/` config, EXCEPT `.claude/skills/` which IS loaded. Commands, hooks, and output-styles from added dirs are not loaded."
   },
   {
    "prompt": "A hook is configured to match MCP tool calls from any server that perform write operations using a regex pattern. Which matcher string correctly matches tools like `mcp__github__write_file` and `mcp__filesystem__write_dir`?",
    "options": [
     "`mcp__*__write*` — glob-style wildcards work in matchers for MCP tools.",
     "`mcp__.*__write.*` — any string containing a non-alphanumeric character other than `_` is interpreted as a JS regex.",
     "`mcp|write` — pipe-separated values match any of the listed substrings anywhere in the tool name.",
     "`mcp__github__write_file|mcp__filesystem__write_dir` — exact pipe-separated names are the only safe way to match multiple MCP tools."
    ],
    "correct": 1,
    "sectionIndices": [
     12
    ],
    "explanation": "Matcher syntax: if the value contains any character other than letters, digits, `_`, or `|`, it is treated as a JS regex. `mcp__.*__write.*` uses `.` (a non-`_`/non-digit char), making it a regex that matches any MCP server's write tools."
   },
   {
    "prompt": "You run `/compact` mid-session to free context. Later in the SAME conversation you realize you need a detail from earlier. What is the correct characterization of what `/compact` does to prior conversation content?",
    "options": [
     "`/compact` ends the current conversation and starts a fresh one, permanently discarding prior content.",
     "`/compact` summarizes the conversation to free context while continuing the same conversation — prior details not in the summary are no longer accessible in this session.",
     "`/compact` archives the full conversation to disk and loads only the summary, so you can restore it later with `/resume`.",
     "`/compact` frees context by offloading turn text to a sidecar file that Claude can re-read on demand."
    ],
    "correct": 1,
    "sectionIndices": [
     3
    ],
    "explanation": "`/compact` summarizes the conversation to free context while continuing the same conversation. Details not captured in the summary are gone for the rest of this session. `/clear` (not `/compact`) is what starts a fresh session stored in `/resume`."
   },
   {
    "prompt": "A hook handler outputs plain text (non-JSON) to stdout and exits with code `0` on a `PostToolUse` event. What happens with that stdout text?",
    "options": [
     "The plain text is added as context to Claude's next message on any event type.",
     "Plain (non-JSON) stdout is added as context ONLY for `SessionStart`, `UserPromptSubmit`, and `UserPromptExpansion`; on `PostToolUse` it is ignored.",
     "Plain text stdout on exit `0` is always displayed to the user in the terminal but not passed to Claude.",
     "The hook framework automatically JSON-wraps plain stdout before passing it to Claude, so it always becomes context."
    ],
    "correct": 1,
    "sectionIndices": [
     14
    ],
    "explanation": "On exit code `0`, plain (non-JSON) stdout is added as context ONLY for `SessionStart`, `UserPromptSubmit`, and `UserPromptExpansion`. For all other events including `PostToolUse`, non-JSON stdout is ignored."
   }
  ],
  "tasks": [
   {
    "id": "stage3-task-precedence-audit",
    "afterSectionIdx": 1,
    "title": "Map your live config precedence stack",
    "instructions": "Inspect each layer of the settings stack and verify which layer is actually controlling your session.\n\n```bash\n# 1. View your user-level config\ncat ~/.claude/settings.json\n\n# 2. View the project-level config (run from your project root)\ncat .claude/settings.json 2>/dev/null || echo '(no project settings file)'\n\n# 3. View the local (gitignored) overrides\ncat .claude/settings.local.json 2>/dev/null || echo '(no local settings file)'\n```\n\nThen open Claude Code and run:\n```\n/hooks\n```\nNote the **Source** column next to each hook — it shows `User`, `Project`, `Local`, `Plugin`, or `Session` so you can see exactly which layer each hook comes from.\n\nFinally, add a harmless test permission to `.claude/settings.local.json` to confirm local overrides work:\n```json\n{\n  \"permissions\": {\n    \"allow\": [\"Bash(echo *)\"],\n    \"deny\": []\n  }\n}\n```\nRun `/hooks` and `/permissions` again to confirm the new rule appears with source `Local`.",
    "doneWhen": "You can see at least two distinct Source labels in the `/hooks` or `/permissions` output (e.g., `User` and `Project`), confirming the layered precedence stack is active."
   },
   {
    "id": "stage3-task-write-blocking-hook",
    "afterSectionIdx": 14,
    "title": "Write a blocking PreToolUse hook with correct exit code",
    "instructions": "Add a `PreToolUse` hook that blocks any `Bash` call containing `rm -rf` and returns feedback to Claude.\n\n1. Open (or create) `.claude/settings.json` in your project root and add the following hook:\n```json\n{\n  \"hooks\": {\n    \"PreToolUse\": [\n      {\n        \"matcher\": \"Bash\",\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"python3 -c \\\"import sys, json; data=json.load(sys.stdin); cmd=data.get('tool_input',{}).get('command',''); sys.stderr.write('Blocked: rm -rf is not allowed\\\\n') or sys.exit(2) if 'rm -rf' in cmd else sys.exit(0)\\\"\"\n          }\n        ]\n      }\n    ]\n  }\n}\n```\n\n2. In Claude Code, run:\n```\n/hooks\n```\nConfirm the new `PreToolUse` hook appears with source `Project`.\n\n3. Ask Claude to run a safe test:\n> Please run: `echo hello`\nIt should succeed (exit 0).\n\n4. Ask Claude:\n> Please run: `rm -rf /tmp/test-nonexistent`\nThe hook should block it and Claude should see the stderr message `Blocked: rm -rf is not allowed`.",
    "doneWhen": "Claude reports that the `rm -rf` Bash call was blocked with the feedback message from stderr, while `echo hello` ran without issue."
   },
   {
    "id": "stage3-task-reload-skills",
    "afterSectionIdx": 8,
    "title": "Create a custom skill and reload it live",
    "instructions": "Add a custom skill file and reload it without restarting Claude Code.\n\n1. Create the skills directory if it doesn't exist:\n```bash\nmkdir -p .claude/skills\n```\n\n2. Write a minimal skill file at `.claude/skills/git-status-helper.md`:\n```markdown\n---\nname: git-status-helper\ndescription: Show a formatted git status summary. Use when the user asks for project status or a clean git overview.\n---\n\nRun `git status --short --branch` and summarize the output: list the branch name, number of modified files, and number of untracked files in one sentence.\n```\n\n3. In Claude Code, run:\n```\n/skills\n```\nNote that the new skill does NOT appear yet (it was added after session start).\n\n4. Run:\n```\n/reload-skills\n```\n\n5. Run `/skills` again and confirm `git-status-helper` now appears in the list.",
    "doneWhen": "After running `/reload-skills`, the `git-status-helper` skill appears in the `/skills` list without having restarted Claude Code."
   }
  ],
  "visualizations": [
   {
    "id": "stage-3-v",
    "kind": "comparison-table",
    "title": "Skills, hooks & commands",
    "textualSummary": "Key concepts of Skills, hooks & commands: configuration scope and precedence, shared vs. personal config placement, hooks on lifecycle events.",
    "columns": [
     "Concept",
     "In this stage"
    ],
    "rows": [
     {
      "label": "configuration scope and precedence",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "shared vs. personal config placement",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "hooks on lifecycle events",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "hook exit-code blocking semantics",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     }
    ]
   }
  ],
  "confusions": [
   {
    "misconception": "Skipping Skills, hooks & commands.",
    "correction": "Each stage is load-bearing for the setup."
   },
   {
    "misconception": "These concepts are interchangeable.",
    "correction": "Each has a distinct role; see the definitions."
   }
  ],
  "quiz": [
   {
    "id": "stage-3-q1",
    "type": "multiple-choice",
    "prompt": "Your team's security policy requires that every `Bash` tool call be logged to an audit service via HTTP POST. You configure an `http` hook on `PreToolUse` and set the audit service to return HTTP 403 when it wants to block a command. During testing you notice that commands still execute even when the service returns 403. What is the most likely cause?",
    "options": [
     "The `http` handler ignores HTTP status codes for blocking — it can only block via a 2xx response body that includes `hookSpecificOutput`.",
     "The `PreToolUse` event does not support blocking; only `PostToolUse` hooks can prevent execution.",
     "HTTP hooks are always `async:true` by default, so they cannot block execution.",
     "The hook matcher must include the full tool path including MCP prefix for Bash calls."
    ],
    "correct": 0,
    "explanation": "The content states explicitly: 'block via 2xx body with `hookSpecificOutput` (HTTP status alone can't block).' A 403 is a non-2xx status, but even a 200 status without `hookSpecificOutput` in the body would not block. Option B is wrong — PreToolUse does support blocking. Option C is wrong — async:true is not the default and must be set explicitly. Option D is wrong — Bash is a built-in tool, not an MCP tool, and needs no prefix."
   },
   {
    "id": "stage-3-q2",
    "type": "multiple-choice",
    "prompt": "A teammate adds a `PreToolUse` hook with `if: 'Bash(git *)'` to skip spawning the hook process for non-git bash calls. She then mentions she also added the same `if` filter to a `ConfigChange` hook to reduce noise. The next day, the `ConfigChange` hook never fires at all. What does the content say explains this behavior?",
    "options": [
     "Adding `if` to a non-tool event prevents the hook from running at all.",
     "The `ConfigChange` event requires a `matcher` field instead of an `if` field to filter.",
     "The `if` filter fails open on non-tool events, firing the hook for every config change regardless of the filter value.",
     "`ConfigChange` hooks only accept `mcp_tool` or `http` handler types, not `command` handlers with `if` filters."
    ],
    "correct": 0,
    "explanation": "The content states: 'Adding `if` to a non-tool event prevents the hook from running at all.' The `if` filter is defined only for tool events (PreToolUse, PostToolUse, etc.) and using it on non-tool events silently disables the hook entirely. Option B is wrong — ConfigChange uses a matcher, but that is separate from the `if` issue. Option C inverts the real behavior (fails open = runs anyway on unparseable commands, not fires always). Option D is unsupported by the content."
   },
   {
    "id": "stage-3-q3",
    "type": "multiple-choice",
    "prompt": "You write a `PostToolUse` hook script that outputs a detailed JSON summary to stdout. You expect Claude to receive this summary as context after every tool call so it can adjust its next action. After deployment, Claude never mentions the information in the JSON. What does the configuration spec say explains this?",
    "options": [
     "Plain stdout is added as context only for `SessionStart`, `UserPromptSubmit`, and `UserPromptExpansion`; for all other events including `PostToolUse`, non-JSON-decision stdout is ignored.",
     "Context injection from hooks requires setting `async:true` so the output is buffered and delivered to the next model request.",
     "The hook output must be written to stderr instead of stdout for Claude to receive it as context.",
     "`PostToolUse` hooks cannot inject context; they can only block (exit 2) or succeed silently (exit 0)."
    ],
    "correct": 0,
    "explanation": "The content states: 'Plain (non-JSON) stdout added as context ONLY for `SessionStart`/`UserPromptSubmit`/`UserPromptExpansion`; ignored elsewhere.' Since PostToolUse is not in that list, stdout is ignored. Option B is wrong — async:true means fire-and-forget, not buffered context delivery. Option C is wrong — stderr is fed back as feedback only on exit 2 (block), not as general context. Option D overstates the restriction; PostToolUse can return JSON decisions, it just can't inject plain stdout context."
   },
   {
    "id": "stage-3-q4",
    "type": "multiple-choice",
    "prompt": "Your organization's platform team wants to enforce a company-wide rule that developers cannot disable a specific safety hook. Individual developers have `~/.claude/settings.json` and project `.claude/settings.json` files. Where must the platform team place this hook so that developer settings cannot override it?",
    "options": [
     "In managed policy settings, which sit at the highest precedence and cannot be overridden by user or project settings.",
     "In `.claude/settings.json` committed to every project repository, since project settings override user settings.",
     "In `~/.claude/settings.json` on each developer's machine, since user settings are the base layer.",
     "In a plugin's `hooks/hooks.json`, since plugin hooks load before any settings file."
    ],
    "correct": 0,
    "explanation": "The content's precedence order is: user → project → local → managed policy (highest, admin-controlled). It explicitly states managed policy settings are 'highest, admin-controlled' and uses them for 'Guardrails users cannot bypass.' Placing the rule in project .claude/settings.json (option B) can be overridden by local settings. User settings (option C) are the base layer, overridden by everything else. Plugin hooks (option D) have no stated precedence advantage over managed policy."
   },
   {
    "id": "stage-3-q5",
    "type": "multiple-choice",
    "prompt": "You use `/add-dir /shared/libs` to give your session access to a shared library directory. The shared library repo has `.claude/settings.json` with hooks and a skill in `.claude/skills/check-types.md`. After adding the directory, you notice the hooks from that repo never fire, but the `check-types` skill appears in your session. What does the content say explains this split behavior?",
    "options": [
     "`/add-dir` does not load the added dir's `.claude/` config (including hooks and settings), but it DOES load `.claude/skills/` from that directory.",
     "`/add-dir` loads the full `.claude/` config including hooks, but skills must be explicitly enabled with `/reload-skills` after adding.",
     "`/add-dir` loads everything from `.claude/` except hooks, because hooks require the directory to be the active working directory.",
     "Both hooks and skills are loaded by `/add-dir`; the hooks are likely misconfigured or use an event name with incorrect casing."
    ],
    "correct": 0,
    "explanation": "The content states: '/add-dir <path> — add a working directory for file access. Does NOT load that dir's `.claude/` config, EXCEPT `.claude/skills/` (which IS loaded).' This directly explains why the hook from settings.json does not fire (config not loaded) but the skill does appear (skills directory is the exception). Options B, C, and D all incorrectly claim some or all of the .claude/ config is loaded."
   },
   {
    "id": "stage-3-q6",
    "type": "multiple-choice",
    "prompt": "A teammate writes a `FileChanged` hook and sets the matcher to `\\.ts$` expecting it to fire only for TypeScript files. After committing, she reports the hook never fires for `.ts` files but occasionally fires for files she did not expect. What does the content say about how `FileChanged` matchers work?",
    "options": [
     "`FileChanged` matcher values are treated as literal filenames, not regex — so `\\.ts$` would only match a file literally named `\\.ts$`.",
     "`FileChanged` matchers use JS regex but require the full path including directory, so `\\.ts$` fails because it doesn't include the directory prefix.",
     "`FileChanged` uses the same matcher syntax as tool-name events, which interprets `\\.ts$` as a pipe-separated list and causes unexpected matches.",
     "`FileChanged` matchers are case-insensitive, so `\\.ts$` fires for `.TS` files but silently skips lowercase `.ts` filenames."
    ],
    "correct": 0,
    "explanation": "The content states explicitly: 'FileChanged — matcher values split into literal filenames, not regex.' This means `.ts$` would be treated as an exact literal filename match, not a regex. The hook either never fires (no file is literally named `\\.ts$`) or if the matcher is processed differently, the behavior is not regex-based. Options B, C, and D all involve regex interpretation, which is the misconception the question targets."
   },
   {
    "id": "stage-3-q7",
    "type": "multiple-choice",
    "prompt": "You configure a background monitoring hook with `asyncRewake:true` on a `Stop` event. After a session ends, the monitor script runs and discovers a test failure. What should happen next according to the content?",
    "options": [
     "The hook runs in the background and wakes Claude on exit 2, with stderr and stdout shown as a system reminder on the next model request.",
     "Because the `Stop` event has already fired, `asyncRewake:true` has no effect — the session is closed and cannot be re-entered.",
     "The hook wakes Claude immediately (synchronously interrupting the stop) and Claude resumes the session to fix the failure.",
     "`asyncRewake:true` causes the hook's stdout to be injected as context at the start of the NEXT new session."
    ],
    "correct": 0,
    "explanation": "The content states: '`asyncRewake:true` = runs in background and wakes Claude on exit 2 (stderr/stdout shown as a system reminder on the next model request) — for background monitors that report back.' This means the hook runs async, and if it exits with code 2, Claude is woken and sees the output as a system reminder. Option B is wrong — asyncRewake is specifically designed to work across the stop boundary. Option C is wrong — it wakes on exit 2, not synchronously. Option D is wrong — it wakes Claude on the next request, but via a system reminder, not context injection at session start."
   },
   {
    "id": "stage-3-q8",
    "type": "multi-select",
    "prompt": "Your team is setting up a shared repository and needs to configure Claude Code for all contributors. Select ALL statements that correctly describe where configuration should be placed according to the content.",
    "options": [
     "Team-standard hooks and permissions should be committed to `.claude/settings.json` so they are shared with all contributors.",
     "Personal or experimental hook overrides that should not impose on teammates belong in `~/.claude/settings.json` or `.claude/settings.local.json`.",
     "`.claude/settings.local.json` is committable to the repository and serves as the team's override layer above project settings.",
     "Managed policy settings are the correct place for org-wide guardrails that no individual contributor can bypass.",
     "Plugin `hooks/hooks.json` is the primary recommended location for team-standard hooks because it loads before project settings."
    ],
    "correct": [
     0,
     1,
     3
    ],
    "explanation": "Options A and B are directly stated: 'commit team-standard hooks/permissions to `.claude/settings.json`; keep personal/experimental ones in `~/.claude/settings.json` or gitignored `.claude/settings.local.json` so they don't impose on the team.' Option D is correct: managed policy settings are 'admin-controlled' for 'Guardrails users cannot bypass.' Option C is wrong — `.claude/settings.local.json` is gitignored, not committable. Option E is wrong — the content does not rank plugin hooks as the primary recommendation for team-standard hooks; project settings.json serves that role."
   },
   {
    "id": "stage-3-q9",
    "type": "multi-select",
    "prompt": "Which of the following are TRUE about hook matcher syntax according to the content? Select all that apply.",
    "options": [
     "A matcher value containing only letters, digits, underscores, and pipe characters is treated as an exact match or pipe-separated list.",
     "Matcher values are case-sensitive — a hook intended for `PreToolUse` will never fire if written as `preToolUse`.",
     "An MCP tool from a server named `github` and tool named `create_issue` would be matched by the pattern `mcp__github__.*`.",
     "A wildcard matcher (`\"*\"` or omitted) matches all tool names for tool-name events.",
     "The `if` filter uses the same regex syntax as the `matcher` field and can be used on any event type to reduce overhead."
    ],
    "correct": [
     0,
     1,
     2,
     3
    ],
    "explanation": "Option A: The content states 'value with only letters/digits/`_`/`|` = exact or pipe-separated list.' Option B: 'Event names are case-sensitive' and 'Matchers are case-sensitive (mismatched case silently never fires).' Option C: MCP tools are named `mcp__<server>__<tool>` and the content shows the example `mcp__github__.*` as a valid matcher. Option D: The content states '\"*\"/\"\"/omitted = all.' Option E is FALSE — the `if` filter uses permission-rule syntax (`Bash(git *)`, `Edit(*.ts)`), not the same regex syntax as matchers, and crucially 'Adding `if` to a non-tool event prevents the hook from running at all' — so it cannot be used on any event type."
   },
   {
    "id": "stage-3-q10",
    "type": "multiple-choice",
    "prompt": "A hook script intended to block a disallowed file edit exits with code 1 and prints an error message to stderr. A teammate expected this to prevent the file write. What actually happens?",
    "options": [
     "Exit code 1 is non-blocking — only exit code 2 blocks. The file write proceeds and the stderr message is ignored for blocking purposes.",
     "Exit code 1 and exit code 2 both block execution; the difference is that exit 1 is silent while exit 2 feeds stderr back to Claude.",
     "Exit code 1 blocks execution and stderr is fed back to Claude as feedback, the same behavior as exit code 2.",
     "The hook's effect depends on the handler type — for `command` handlers, exit 1 blocks, but for `http` handlers only the HTTP status matters."
    ],
    "correct": 0,
    "explanation": "The content's exit code table specifies: exit 2 = Block (stdout/JSON ignored; stderr fed back to Claude as feedback); 'any other (incl. `1`)' = Non-blocking. So exit 1 does NOT block — only exit 2 does. The teammate's script would need to exit with code 2 for the block to take effect. Options B and C both wrongly treat exit 1 as blocking. Option D introduces a false handler-type dependency that the content does not support."
   },
   {
    "id": "stage-3-q11",
    "type": "multiple-choice",
    "prompt": "Your team wants a pre-commit style check: before Claude stops working, run the test suite and if tests fail, have Claude fix the failures rather than stopping. Which hook handler type and event are most appropriate according to the content?",
    "options": [
     "An `agent` handler on the `Stop` event, because verification must inspect actual files (e.g. run tests) and the agent can take follow-up actions.",
     "A `prompt` handler on the `Stop` event, because the LLM can evaluate test output and decide whether to block.",
     "A `command` handler on the `PostToolBatch` event, because commands run after each batch of tool calls and can block via exit 2.",
     "An `http` handler on the `Stop` event posting to a CI service, since external CI systems are the correct place to run tests."
    ],
    "correct": 0,
    "explanation": "The content states the `agent` handler type is for 'Experimental multi-turn subagent with tool access (up to 50 tool-use turns). Use only when verification must inspect actual files (e.g. run tests before a Stop).' The `Stop` event fires when Claude finishes, and the agent handler is explicitly called out for this exact use case. Option B (`prompt`) is wrong — the content says 'Use when hook input alone suffices,' but running a test suite requires file inspection. Option C is wrong — PostToolBatch does not fire on Stop and cannot replace the Stop event for pre-stop gating. Option D's http approach cannot run local tests directly."
   },
   {
    "id": "stage-3-q12",
    "type": "multiple-choice",
    "prompt": "A developer types `/code-review high --fix src/api.ts` in the middle of a message that also includes 'and also check the docs folder'. What does the content say about how slash commands are parsed?",
    "options": [
     "A slash command is recognized only at the start of a message; trailing text after the command is passed as arguments. The phrase 'and also check the docs folder' would be treated as additional arguments to `/code-review`, not as a separate instruction.",
     "A slash command can appear anywhere in a message; the parser scans for `/` and treats the next word as the command, ignoring surrounding text.",
     "Trailing text after a slash command causes the command to be unrecognized; the entire message is treated as a plain conversation turn.",
     "The slash command `/code-review` accepts only flags and a target file, so 'and also check the docs folder' would cause a parse error and the command would not run."
    ],
    "correct": 0,
    "explanation": "The content states: 'A slash command is recognized only at the start of a message; trailing text is passed as arguments.' This means 'and also check the docs folder' becomes part of the argument string, not a separate instruction or a parse error. Option B inverts the rule — it must be at the start. Option C is wrong — trailing text does not prevent recognition; it becomes arguments. Option D incorrectly describes command-specific argument parsing that the content does not specify."
   },
   {
    "id": "stage-3-q13",
    "type": "multiple-choice",
    "prompt": "You run `/effort ultracode` at the start of a session for a complex refactor. After the session is done, you open a new session for a quick one-line fix and notice it is still running at `ultracode` effort level. Why might this be happening — and what does the content say about persistence of effort levels?",
    "options": [
     "This would NOT happen per the content — `max` and `ultracode` are session-only and do not persist. The new session should revert to the model default. Another explanation is needed.",
     "`ultracode` persists across sessions because `/effort` always saves the level as the new default, the same way `/model` does.",
     "Effort levels persist until explicitly reset with `/effort auto`, which explains why the new session inherits `ultracode`.",
     "The behavior depends on the plan tier; on Team/Enterprise plans effort levels are saved per-project and persist across sessions."
    ],
    "correct": 0,
    "explanation": "The content states: '`max`/`ultracode` are session-only (don't persist).' This directly contradicts the scenario — ultracode should NOT carry over to a new session. The correct answer identifies this discrepancy and directs the user to find another explanation. Option B is wrong — the content contrasts /model (which 'saves as default for new sessions') with effort levels that don't persist. Option C conflates /effort auto (which resets to model default) with the persistence question. Option D invents a plan-tier dependency the content does not mention."
   }
  ],
  "masteryCheckpoint": "You can explain the concepts of Skills, hooks & commands."
 },
 {
  "id": "stage-4",
  "stage": 4,
  "title": "Subagents & agent teams",
  "summary": "Subagents & agent teams: Choosing a multi-context approach, Subagent, Definition scope and precedence.",
  "prerequisites": [
   "stage-3"
  ],
  "objectives": [
   "Understand the concepts in Subagents & agent teams."
  ],
  "definitions": [
   {
    "term": "Choosing a multi-context approach",
    "short": "Claude Code offers four ways to run work in more than one context, picked by who coordinates the work and whether the workers need to talk to each other."
   },
   {
    "term": "Subagent",
    "short": "A worker defined in a Markdown file (YAML frontmatter plus body-as-system-prompt) that runs in a fresh isolated context and returns only a summary to the conversation that spawned it."
   },
   {
    "term": "Definition scope and precedence",
    "short": "Subagent definitions resolve across ranked scopes (managed, CLI flag, project, user, plugin), and on a name collision the higher-priority or closest-to-cwd definition wins."
   },
   {
    "term": "Subagent frontmatter configuration",
    "short": "Frontmatter fields tune each subagent's routing, tools, model, permissions, memory, and isolation so it does one focused job with the minimum capability needed."
   },
   {
    "term": "Tool and context scoping for workers",
    "short": "Limiting a worker to the minimum tools, inlining its MCP servers, and using hooks for fine-grained gating keeps capability tight and keeps tool descriptions out of the main conversation's context."
   },
   {
    "term": "Fork vs fresh subagent",
    "short": "A fork inherits the entire current conversation (and reuses its prompt cache) for a side task, whereas a normal subagent starts from a fresh isolated context."
   }
  ],
  "sections": [
   {
    "heading": "The Five Multi-Context Approaches",
    "body": "Claude Code offers five architecturally distinct ways to run work beyond a single conversation context. Choosing the wrong one wastes tokens, creates coordination headaches, or leaves capability on the table. The decision hinges on two axes: **who coordinates the work** and **whether workers need to communicate with each other**.\n\n| Approach | Coordinator | Worker-to-worker comms | Scale | Repeatable? |\n|---|---|---|---|---|\n| **Subagents** | Main conversation, turn-by-turn | No — only report back to caller | A few delegated tasks per turn | Worker definition only |\n| **Agent teams** | Lead session + shared task list | Yes — peer messaging via mailbox | Handful of long-running peers | Team definition |\n| **Dynamic workflows** | A JavaScript script the runtime executes | No — agents report results to script variables | Up to 1,000 agents per run (16 concurrent) | Entire orchestration as a rerunnable `.claude/workflows/` command |\n| **Background agents** | Agent view (`claude agents`), manually | N/A | As many sessions as you dispatch | N/A (each is a full independent session) |\n| **Git worktrees** | You, manually | N/A (human coordination) | As many shells as you open | N/A |\n\n### When subagents are the right choice\nA subagent is right when a side task would flood your main conversation with output you won't reference again — running a test suite, scraping docs, auditing a directory tree — and only the summary needs to come back. Tool restrictions and specialized prompts are free to configure per-subagent. Results land back in your main context window, so use them for focused tasks with compact results.\n\n### When agent teams are the right choice\nAgent teams become necessary when workers need to share findings, challenge each other's hypotheses, or self-coordinate work without routing every message through the lead. The canonical cases: a parallel code review where a security reviewer, performance reviewer, and test-coverage reviewer are actively comparing notes; debugging with competing hypotheses where teammates try to disprove each other; or feature work where three teammates own frontend, backend, and tests independently.\n\n### When workflows are the right choice\nWorkflows move the orchestration plan itself into a JavaScript script that the runtime executes. Use them when a task needs more agents than one conversation can coordinate, when you want a repeatable quality pattern (like adversarial cross-checking), or when the orchestration is worth saving as a slash command. A 500-file migration, a codebase-wide audit, or a research question that fans out across dozens of sources and votes on each claim before reporting it are workflow-scale tasks. The bundled `/deep-research` command is a workflow. Intermediate results live in script variables, not in Claude's context window.\n\n### When background agents are the right choice\nBackground agents (`claude agents`, enabled via `claude agents` or `--bg`) are full independent Claude Code sessions dispatched and monitored from a single dashboard. Use them when you want to run several sessions in parallel and check in on each from one screen, but those sessions do not need to coordinate with each other. They are closer to \"managed terminal tabs\" than to a team.\n\n### Worktrees for manual parallelism\nGit worktrees let you run multiple Claude Code sessions yourself against isolated checkout branches. No automated coordination — you are the lead. Useful when you want to develop two features in parallel without merge conflicts, not when you want agents to coordinate on your behalf."
   },
   {
    "heading": "Subagent Fundamentals",
    "body": "A subagent is a Markdown file with YAML frontmatter plus a body that becomes the system prompt. When Claude encounters a task matching a subagent's `description`, it delegates via the `Agent` tool. The subagent runs in its own context window and returns only a summary to the conversation that spawned it.\n\n```\n.claude/\n  agents/\n    code-reviewer.md   # project-scoped\n    db-reader.md\n~/.claude/\n  agents/\n    data-scientist.md  # user-scoped, available everywhere\n```\n\nThree things distinguish a subagent from just \"asking Claude to do something\":\n\n1. **Isolated context.** The subagent does not see your conversation history. It gets its own system prompt, the task delegation prompt Claude writes, CLAUDE.md files, and git status — nothing else. Verbose intermediate output (file contents, test logs, search results) stays in the subagent's window and never inflates your context.\n\n2. **Scoped capability.** The `tools` allowlist and `disallowedTools` denylist constrain exactly what the subagent can reach. A read-only researcher cannot accidentally edit files.\n\n3. **Independent lifecycle.** Subagents accumulate their own transcripts under `~/.claude/projects/{project}/{sessionId}/subagents/agent-{agentId}.jsonl`, support their own auto-compaction, and can be resumed by asking Claude to continue a previous subagent's work.\n\n### Built-in subagents\nClaude Code ships four always-registered built-ins in interactive sessions:\n\n- **Explore** — Haiku, read-only tools, skips CLAUDE.md and git status for speed. Used by Claude to search and understand codebases without polluting context. Supports thoroughness levels: `quick`, `medium`, or `very thorough`.\n- **Plan** — inherits model, read-only tools, also skips CLAUDE.md and git status. Used in plan mode for codebase research before presenting a plan.\n- **General-purpose** — inherits model and all tools. Used for complex multi-step tasks that need both exploration and modification.\n- **Additional helpers** — `statusline-setup` (Sonnet, invoked by `/statusline`) and `claude-code-guide` (Haiku, answers Claude Code feature questions).\n\nIn non-interactive mode and the Agent SDK, set `CLAUDE_AGENT_SDK_DISABLE_BUILTIN_AGENTS=1` to disable all built-ins and supply only your own.\n\nYou can suppress a specific built-in by adding it to `permissions.deny` in `settings.json` using the `Agent(name)` syntax:\n\n```json\n{\n  \"permissions\": {\n    \"deny\": [\"Agent(Explore)\"]\n  }\n}\n```\n\nTo suppress all Agent tool use, deny `Agent` itself. From the CLI: `claude --disallowedTools \"Agent(Explore)\"`.\n\n### Nested subagents\nAs of v2.1.172, a subagent can spawn its own subagents. Depth is counted from the main conversation; a subagent at depth five does not receive the Agent tool. The limit of five levels is fixed and not configurable. A fork cannot spawn another fork, but it can spawn named subagent types."
   },
   {
    "heading": "Definition Scope and Precedence",
    "body": "Subagent definitions resolve across five ranked scopes. On a name collision, the higher-priority scope wins — not the most recently written file.\n\n| Priority | Scope | Location | Notes |\n|---|---|---|---|\n| 1 (highest) | Managed settings | Deployed via managed settings infrastructure | Organization-wide; overrides everything |\n| 2 | CLI flag | `--agents '{...}'` JSON at launch | Session-only; not saved to disk |\n| 3 | Project | `.claude/agents/` | Check into git; team-shared |\n| 4 | User | `~/.claude/agents/` | Personal; all projects |\n| 5 (lowest) | Plugin | Plugin's `agents/` directory | Namespaced: `plugin:name` or `plugin:subfolder:name` |\n\n### Nested project discovery\nClaude Code walks up from the current working directory scanning every `.claude/agents/` it finds between cwd and the repo root. Both `.claude/agents/` and `~/.claude/agents/` are scanned recursively, so you can organize into subfolders (`agents/review/`, `agents/research/`). As of v2.1.178, when two nested project directories define the same `name`, **the definition closest to cwd wins**:\n\n```\nrepo/\n  .claude/agents/code-reviewer.md    # root definition\n  packages/api/\n    .claude/agents/code-reviewer.md  # wins when cwd is packages/api/\n```\n\nThe subdirectory path does not affect identity — only the `name` frontmatter field does. Keep `name` values unique within a scope tree: two files with the same name cause Claude Code to keep one and silently discard the other.\n\n### Plugin subagent namespacing\nPlugin subagents are scoped by subfolder path: a file at `agents/review/security.md` inside plugin `team-tools` registers as `team-tools:review:security`. Reference it with an @-mention or the `--agent` flag:\n\n```bash\nclaude --agent team-tools:review:security\n```\n\n### Important caveats\n- **Files are loaded at session start.** If you edit a subagent file on disk, restart your session to pick up changes. Subagents created or edited through `/agents` take effect immediately.\n- **`name` is identity.** The filename does not have to match the `name` field. Two files in the same scope tree with the same `name` — Claude Code keeps one and silently discards the other.\n- **`--add-dir` directories are also scanned.** A `.claude/agents/` folder inside a directory added with `--add-dir` loads alongside project subagents.\n\n### CLI-defined subagents\nFor automation or one-off testing, define subagents as JSON at launch. They exist only for the session. In the JSON, the outer key is the `name`; use `prompt` for the system prompt body:\n\n```bash\nclaude --agents '{\n  \"schema-auditor\": {\n    \"description\": \"Audits database schema for normalization issues. Use after any migration.\",\n    \"prompt\": \"You are a database schema auditor. Analyze schema files for 3NF violations, missing indexes, and implicit nullability.\",\n    \"tools\": [\"Read\", \"Grep\", \"Glob\"],\n    \"model\": \"sonnet\"\n  }\n}'\n```\n\nThe `--agents` flag accepts the same fields as frontmatter, with `prompt` as the body equivalent. All fields documented in the frontmatter reference below are accepted here."
   },
   {
    "heading": "Subagent Frontmatter Configuration",
    "body": "Only `name` and `description` are required. Every other field is optional; omission inherits from the parent session.\n\n```markdown\n---\nname: api-security-reviewer\ndescription: >-\n  Reviews API endpoints and auth middleware for security vulnerabilities.\n  Use proactively after any change to src/routes/ or src/middleware/.\ntools: Read, Grep, Glob, Bash\ndisallowedTools: Write, Edit\nmodel: sonnet\npermissionMode: default\nmaxTurns: 40\nskills:\n  - api-conventions\n  - owasp-checklist\nmcpServers:\n  - github\n  - semgrep:\n      type: stdio\n      command: npx\n      args: [\"-y\", \"@semgrep/mcp@latest\"]\nhooks:\n  PreToolUse:\n    - matcher: \"Bash\"\n      hooks:\n        - type: command\n          command: \"./scripts/validate-safe-command.sh\"\nmemory: project\nbackground: false\neffort: high\nisolation: worktree\ncolor: red\n---\n\nYou are a security reviewer specializing in API surface and authentication.\nWhen invoked, run `git diff HEAD~1` to see recent changes, then focus on:\n- Broken object-level authorization\n- Missing rate limiting\n- JWT validation gaps\n- Input validation and injection surfaces\n\nReport findings by severity: Critical / High / Medium. Include the exact\nfile:line for each finding and a concrete fix.\n```\n\n### Field reference\n\n| Field | Type | Effect |\n|---|---|---|\n| `name` | string (lowercase, hyphens) | Required. Identity key. The filename does not have to match. Hooks receive this as `agent_type`. |\n| `description` | string | Required. Claude uses this to decide when to delegate. Phrases like \"use proactively after X\" push Claude to delegate automatically. |\n| `tools` | comma-separated string or YAML list | Allowlist. If omitted, inherits all tools from parent. To preload skills, use the `skills` field rather than listing `Skill` here. |\n| `disallowedTools` | same | Denylist applied before `tools` is resolved. Accepts `mcp__<server>` to remove an entire MCP server, or `mcp__*` to remove all MCP tools. A tool in both lists is removed. |\n| `model` | `sonnet` / `opus` / `haiku` / `fable` / `sonnet[1m]` / `opus[1m]` / full model ID / `inherit` | Defaults to `inherit`. Resolution order: (1) `CLAUDE_CODE_SUBAGENT_MODEL` env var, (2) per-invocation model parameter, (3) this field, (4) parent session model. |\n| `permissionMode` | see below | Overrides the session's permission mode for this subagent. Ignored when parent uses `bypassPermissions` or `acceptEdits` (those take precedence). Also ignored when parent uses `auto` mode (subagent inherits auto). |\n| `maxTurns` | integer | Hard cap on agentic turns before the subagent stops. |\n| `skills` | YAML list of skill names | Full skill content is injected at startup. Subagent can still invoke unlisted project/user/plugin skills via the Skill tool during execution. |\n| `mcpServers` | YAML list — server name strings or inline definitions | String entries reuse the parent session's existing connection. Inline definitions connect at subagent start and disconnect at finish, keeping tool descriptions out of the main session's context. Ignored for plugin subagents. |\n| `hooks` | object, same schema as settings.json hooks | Scoped to this subagent's lifecycle. `Stop` events in frontmatter hooks are automatically converted to `SubagentStop` at runtime. Ignored for plugin subagents. |\n| `memory` | `user` / `project` / `local` | Gives the subagent a persistent directory. Paths: `~/.claude/agent-memory/<name>/` (user), `.claude/agent-memory/<name>/` (project), `.claude/agent-memory-local/<name>/` (local). Subagent reads first 200 lines or 25KB of `MEMORY.md` at startup. |\n| `background` | boolean | Force background (non-blocking) execution. Default: `false`. |\n| `effort` | `low` / `medium` / `high` / `xhigh` / `max` | Overrides session effort level for this subagent. Not overridden by `CLAUDE_CODE_EFFORT_LEVEL`. |\n| `isolation` | `worktree` | Runs the subagent in a temporary git worktree branched from your **default branch** (not the parent's HEAD). Worktree is cleaned up automatically if the subagent makes no changes. |\n| `color` | `red` / `blue` / `green` / `yellow` / `purple` / `orange` / `pink` / `cyan` | Display color in the UI. |\n| `initialPrompt` | string | Auto-submitted as the first user turn when this agent runs as the main session via `--agent` or the `agent` setting. Commands and skills are processed. Prepended to any user-provided prompt. |\n\n### Permission mode details\n\n| Mode | Behavior |\n|---|---|\n| `default` | Standard permission checking with prompts |\n| `acceptEdits` | Auto-accept file edits and common filesystem commands for paths in the working directory or `additionalDirectories` |\n| `auto` | Background classifier reviews commands and protected-directory writes |\n| `dontAsk` | Auto-deny permission prompts (explicitly allowed tools still work) |\n| `bypassPermissions` | Skip permission prompts. Still blocks root and home directory removals and directories like `.git`, `.claude`, `.vscode`, `.cargo`. |\n| `plan` | Read-only plan-mode exploration |"
   },
   {
    "heading": "Tool and Context Scoping for Workers",
    "body": "The `tools` allowlist and `disallowedTools` denylist compose with a well-defined evaluation order: **`disallowedTools` is applied first, then `tools` is resolved against the remaining pool**. A tool in both lists is removed.\n\n### Allowlist vs denylist tradeoffs\n\n```yaml\n# Allowlist: subagent gets exactly these tools and nothing else.\n# Safest for a read-only worker.\ntools: Read, Grep, Glob, Bash\n\n# Denylist: inherits everything except the listed tools.\n# Useful when you want to keep MCP tools from parent but drop Write.\ndisallowedTools: Write, Edit\n\n# MCP server-level wildcard: remove every tool from a named server.\ndisallowedTools: mcp__github\n\n# Remove all MCP tools from any server.\ndisallowedTools: mcp__*\n```\n\n### Tools unavailable to subagents regardless of the tools field\nThe following tools depend on the main session's UI or state and cannot be used by any subagent even when listed in `tools`:\n- `AskUserQuestion`\n- `EnterPlanMode`\n- `ExitPlanMode` (unless the subagent's `permissionMode` is `plan`)\n- `ScheduleWakeup`\n- `WaitForMcpServers`\n\n### Scoping spawnable subagent types\nWhen an agent runs as the main thread via `claude --agent`, you can restrict which sub-subagent types it can spawn using the `Agent(type)` syntax:\n\n```yaml\ntools: Agent(researcher, verifier), Read, Bash\n```\n\nThis is an allowlist for spawn targets — any other type fails at request time. The `Agent(type)` allowlist applies **only** when the agent is running as the main thread via `--agent`. In a regular subagent definition, listing `Agent` lets it spawn nested subagents, but any type list inside the parentheses is ignored. To allow spawning any type, use `Agent` without parentheses. If `Agent` is omitted entirely, the subagent cannot spawn at all.\n\n**Note:** As of v2.1.63, the `Task` tool was renamed to `Agent`. Existing `Task(...)` references in settings and definitions still work as aliases.\n\n### Keeping MCP tool descriptions out of the main context\nMCP tool descriptions consume context in every session that loads the server. If a server is only relevant to one subagent, declare it inline in `mcpServers` rather than in `.mcp.json`. The server connects when the subagent starts, disconnects when it finishes, and the tool descriptions never appear in the main session's context window:\n\n```yaml\nmcpServers:\n  - playwright:\n      type: stdio\n      command: npx\n      args: [\"-y\", \"@playwright/mcp@latest\"]\n```\n\nString entries (server name only, no inline definition) reuse the parent session's existing connection to an already-configured server.\n\n### Hooks for conditional tool gating\nThe `tools` field is a static gate. When you need dynamic validation — allow `Bash` but only for `SELECT` queries, allow `Write` but only to paths under `src/generated/` — use a `PreToolUse` hook:\n\n```yaml\nhooks:\n  PreToolUse:\n    - matcher: \"Bash\"\n      hooks:\n        - type: command\n          command: \"./scripts/validate-readonly-query.sh\"\n```\n\nClaude Code passes the full hook event as JSON on stdin to the hook command. The `tool_input.command` field carries the Bash command being attempted. Exit 0 to allow; exit 2 to block and feed stderr back to Claude as an error:\n\n```bash\n#!/bin/bash\nINPUT=$(cat)\nCOMMAND=$(echo \"$INPUT\" | jq -r '.tool_input.command // empty')\nif echo \"$COMMAND\" | grep -iE '\\b(INSERT|UPDATE|DELETE|DROP|CREATE|ALTER|TRUNCATE|REPLACE|MERGE)\\b' > /dev/null; then\n  echo \"Blocked: Only SELECT queries are allowed\" >&2\n  exit 2\nfi\nexit 0\n```\n\nMake the script executable before use: `chmod +x ./scripts/validate-readonly-query.sh`.\n\nThis pattern enforces a constraint that a plain `tools` field cannot express — you cannot prevent individual Bash commands without a hook."
   },
   {
    "heading": "Fork vs Fresh Subagent",
    "body": "A **fork** and a **fresh (named) subagent** are both spawned via the Agent tool, but they start from fundamentally different contexts.\n\n| | Fresh subagent | Fork |\n|---|---|---|\n| Conversation history | Not inherited | Full history inherited |\n| System prompt | From subagent's definition file | Same as main session |\n| Tools | From definition `tools` field | Same as main session |\n| Model | From definition `model` field | Same as main session |\n| Prompt cache | Separate | Shared with main session |\n| Can spawn another fork | Yes (depth counts toward limit) | No — a fork cannot spawn another fork |\n\n### When to use a fork\nA fork is correct when the side task needs the full context of what has happened in the conversation — context that would be expensive or impossible to re-explain in a delegation prompt. The prototypical case: you're mid-implementation and want to try two approaches from the current state without diverging your main thread.\n\n```text\n/fork draft unit tests for the parser changes made so far\n```\n\nThe fork inherits everything (system prompt, tools, model, and message history) at the moment it spawns. Because its system prompt and tool definitions are identical to the parent, **the first request to the fork reuses the parent's prompt cache** — making forking cheaper than spawning a fresh subagent when the shared context is large.\n\n### When a fresh subagent is better\nFresh subagents are cheaper for tasks where the worker does not need conversation history. They also enforce cleaner capability boundaries because their `tools` and `model` are defined explicitly rather than inherited ad hoc. If you find yourself writing a long delegation prompt to reconstruct context the fork would have inherited automatically, that is the signal to use a fork instead.\n\n### Enabling fork mode\nForks require Claude Code v2.1.117 or later. From v2.1.161, the `/fork` command is enabled by default. To control fork mode explicitly regardless of any staged rollout:\n\n```bash\nexport CLAUDE_CODE_FORK_SUBAGENT=1   # enable everywhere\nexport CLAUDE_CODE_FORK_SUBAGENT=0   # disable everywhere (overrides server-side rollout)\n```\n\nSetting `CLAUDE_CODE_FORK_SUBAGENT=1` changes Claude Code in two ways:\n1. Claude can spawn a fork by requesting the `fork` subagent type. Spawns without a specified type still use the general-purpose subagent.\n2. **Every subagent spawn runs in the background** (not just forks), so permission prompts from background subagents surface in your main session rather than blocking it.\n\nTo disable all background task functionality (including the side effect above), set `CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1`.\n\n### Interaction with worktree isolation\nWhen Claude spawns a fork, it can pass `isolation: \"worktree\"` so the fork's file edits land in a temporary git worktree branched from the default branch rather than your working checkout. This lets you compare two approaches without risk of merge conflict. The worktree is cleaned up automatically if no changes are made.\n\n### Limitations\n- A fork cannot spawn another fork.\n- A fork **can** spawn named subagent types; those count toward the depth limit (max depth 5).\n- Resuming a session with `/resume` or `/rewind` does not restore forks spawned in the previous session."
   },
   {
    "heading": "Agent Teams: Architecture and Mechanics",
    "body": "Agent teams are an experimental feature (disabled by default) that promotes subagents from isolated report-back workers to fully independent peer sessions that coordinate through a shared task list and direct messaging.\n\n### Enabling agent teams\n\n```json\n// .claude/settings.json or ~/.claude/settings.json\n{\n  \"env\": {\n    \"CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS\": \"1\"\n  }\n}\n```\n\nWithout this variable, no team infrastructure is set up, no team directories are written, and Claude will not spawn or propose teammates.\n\n**API history note:** Before v2.1.178, spawning a teammate required asking Claude to create and name a team first, using `TeamCreate` and `TeamDelete` tools. Those tools no longer exist as of v2.1.178. The `team_name` input on the Agent tool is still accepted but ignored.\n\n### Structural components\n\n| Component | Role | Location |\n|---|---|---|\n| Team lead | The main Claude Code session that spawns teammates and coordinates work | Your terminal |\n| Teammates | Separate Claude Code instances, each in its own context window | Separate panes or in-process panel |\n| Task list | Shared work-item list with dependency tracking and file-locked claiming | `~/.claude/tasks/{team-name}/` |\n| Mailbox | Message delivery system for agent-to-agent communication | Delivered automatically; no polling needed |\n\nThe team name is `session-` followed by the first 8 characters of the session ID. Team config lives at `~/.claude/teams/{team-name}/config.json`. **Do not edit this file manually** — it is overwritten on every state update. The config directory is removed when the session ends; the task directory persists (for resumed sessions) and is cleaned up according to `cleanupPeriodDays`.\n\n### Display modes\n\n```json\n// ~/.claude/settings.json\n{ \"teammateMode\": \"auto\" }  // split panes when tmux/iTerm2 detected, in-process otherwise\n\n// or per session:\nclaude --teammate-mode auto\n```\n\nThe default as of v2.1.179 is `\"in-process\"` (all teammates in a single terminal). `\"auto\"` enables split panes when already inside a tmux session or iTerm2, falling back to in-process otherwise. `\"tmux\"` enables split-pane mode and auto-detects whether to use tmux or iTerm2. `\"iterm2\"` (v2.1.186+) uses iTerm2 native split panes explicitly and requires the `it2` CLI.\n\nNavigating in in-process mode:\n- `↑`/`↓`: select a teammate in the agent panel\n- `Enter`: open the selected teammate's transcript and type to message it directly\n- `Escape`: interrupt the selected teammate's current turn\n- `x` on a selected row: stop that teammate\n- `Ctrl+T`: toggle the task list\n\nIdle teammate rows hide after 30 seconds (v2.1.181+). The teammate is still running; send it a message by name to bring it back."
   },
   {
    "heading": "Teammate Context and Permissions",
    "body": "This is the most critical section to get right, because mistakes here produce teammates that stall for context or over-approve risky operations.\n\n### What a teammate does and does not receive\n\n| Context source | Teammate receives it? |\n|---|---|\n| `CLAUDE.md` files (all levels) | Yes |\n| MCP servers from project/user settings | Yes |\n| Skills from project/user settings | Yes |\n| The lead's conversation history | **No** |\n| The lead's model selection | **No by default** — set via `/config` Default teammate model, or specify per-spawn |\n| The spawn prompt from the lead | Yes |\n\nThe lead's conversation history does not carry over. **Include all task-specific context in the spawn prompt.** If the teammate needs to know that the app uses JWT tokens in httpOnly cookies, say so in the spawn prompt — it cannot infer this from conversation history it never saw.\n\n```text\n# Good spawn prompt\nSpawn a security reviewer teammate with the prompt:\n\"Review src/auth/ for JWT validation vulnerabilities.\n The app uses JWT tokens stored in httpOnly cookies.\n The token signing key is loaded from TOKEN_SECRET env var.\n Report findings with severity ratings and file:line references.\"\n\n# Incomplete spawn prompt (teammate has no context about JWT)\nSpawn a security reviewer for the auth module.\n```\n\n### Model selection\nTeammates do not inherit the lead's `/model` selection by default. To change the default teammate model, set **Default teammate model** in `/config`. Choose **Default (leader's model)** to have teammates follow the lead's current model. Alternatively, specify per-spawn:\n\n```text\nSpawn 3 teammates to audit the routes directory in parallel. Use Haiku for each.\n```\n\nThe env var `CLAUDE_CODE_SUBAGENT_MODEL` applies to teammates as well as subagents — it overrides all other model settings.\n\n### Permissions inheritance\nTeammates start with the lead's permission settings — specifically the permission **mode** at spawn time. If the lead runs with `--dangerously-skip-permissions`, all teammates inherit that. You cannot set per-teammate permission modes **at spawn time**; you can change individual teammate modes after spawning by messaging them directly.\n\nThe practical implication: **pre-approve common operations before spawning**, or teammates will surface permission prompts that bubble up to the lead and create friction:\n\n```bash\n# Pre-approve the operations your teammates will need before launching\nclaude --permission-mode acceptEdits\n```\n\n### Plan approval workflow\nFor high-risk tasks, require teammates to plan before implementing:\n\n```text\nSpawn an architect teammate to refactor the auth module.\nRequire plan approval before they make any changes.\n```\n\nThe teammate enters read-only plan mode, presents a plan to the lead, and waits. The lead approves or rejects with feedback. On rejection, the teammate revises and resubmits.\n\nTo influence the lead's approval criteria:\n```text\nOnly approve plans that include test coverage for the changed paths.\nReject any plan that modifies the database schema without a migration file.\n```"
   },
   {
    "heading": "Task Coordination and Quality Gating",
    "body": "The shared task list is the coordination primitive that distinguishes agent teams from just running multiple Claude Code sessions in parallel terminal windows.\n\n### Task lifecycle\nTasks have three states: **pending**, **in progress**, and **completed**. Tasks can declare dependencies on other tasks. A pending task with unresolved dependencies cannot be claimed until those dependencies complete. The runtime unblocks dependents automatically when a dependency completes.\n\n**File locking prevents race conditions.** When multiple teammates attempt to claim the same task simultaneously, the file lock ensures exactly one succeeds. This is why the task directory lives at `~/.claude/tasks/` (on the local filesystem) rather than in-process state.\n\n### Claiming strategies\n- **Lead assigns**: tell the lead which task to give to which teammate by name.\n- **Self-claim**: after finishing a task, a teammate picks up the next unassigned, unblocked task on its own.\n\nPractical sizing: aim for **5-6 tasks per teammate**. Too few and coordination overhead dominates; too many and teammates context-switch too often without finishing work.\n\n### Quality gates with hooks\nThree hook events fire around the task lifecycle:\n\n```json\n// settings.json\n{\n  \"hooks\": {\n    \"TaskCompleted\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"./.claude/hooks/validate-task-completion.sh\"\n          }\n        ]\n      }\n    ],\n    \"TaskCreated\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"./.claude/hooks/validate-task-scope.sh\"\n          }\n        ]\n      }\n    ],\n    \"TeammateIdle\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"./.claude/hooks/check-work-quality.sh\"\n          }\n        ]\n      }\n    ]\n  }\n}\n```\n\n| Hook | When it fires | Exit 2 effect |\n|---|---|---|\n| `TaskCompleted` | Teammate marks a task complete | Prevents completion; stderr message sent to the teammate as feedback |\n| `TaskCreated` | Task is being created | Prevents creation; stderr message sent as feedback |\n| `TeammateIdle` | Teammate is about to go idle | Keeps the teammate working; stderr message is the reason |\n\nThe hook receives task metadata as JSON on stdin. The `TaskCompleted` hook is the most valuable: it lets you enforce that a task is not marked done until tests pass, lint is clean, or a required output file was written. Exit 2 with a descriptive stderr message; Claude relays that message to the teammate as a reason to continue working.\n\n### Peer-to-peer messaging\nTeammates send messages directly by name. The lead assigns names at spawn time; use the spawn instruction to get predictable names:\n\n```text\nSpawn three teammates:\n- \"security-reviewer\": audit auth module\n- \"performance-reviewer\": profile the API routes\n- \"test-coverage\": check coverage gaps\nHave them share findings with each other before reporting.\n```\n\nMessages are delivered automatically — the lead does not need to poll. When a teammate finishes and goes idle, it automatically notifies the lead. **There is no broadcast primitive.** To reach all teammates, send one message per recipient.\n\nTeam membership is discoverable: teammates can read `~/.claude/teams/{team-name}/config.json`, which contains a `members` array with each teammate's name, agent ID, and agent type."
   },
   {
    "heading": "Using Subagent Definitions as Teammate Roles",
    "body": "Subagent definition files serve double duty: they define workers for single-session delegation (spawned via the Agent tool or @-mention) and they define role templates for agent team teammates. Write a role once, reuse it in both contexts.\n\n### Referencing a subagent definition when spawning a teammate\n\n```text\nSpawn a teammate using the security-reviewer agent type to audit the auth module.\n```\n\nThe teammate honors the definition's `tools` allowlist and `model`. The definition's body is **appended** to the teammate's system prompt as additional instructions — it does not replace the team coordination system prompt. Team coordination tools (`SendMessage`, task management tools) are always available to a teammate regardless of what `tools` restricts.\n\n### Fields that do and do not apply in the teammate context\n\nWhen a subagent definition runs as a teammate:\n\n| Field | Applied? |\n|---|---|\n| `tools` | Yes — used as teammate's tool allowlist |\n| `model` | Yes — used as teammate's model |\n| Body (system prompt) | Yes — appended to teammate's system prompt |\n| `skills` | **No** — teammates load skills from project/user settings, not from the definition's `skills` field |\n| `mcpServers` | **No** — teammates use the session's configured MCP servers, not inline definitions from the subagent file |\n\nThis asymmetry matters: if your subagent definition relies on inline `mcpServers` or a specific `skills` preload to function correctly as a standalone subagent, those will be silently ignored when the same definition is used as a teammate. Design teammate-role definitions to rely only on `tools`, `model`, and the body prompt.\n\n### Team-specific role design\nSince teammates load project context (CLAUDE.md, project MCP servers, project skills), teammate role definitions can be more minimal than standalone subagent definitions:\n\n```markdown\n---\nname: feature-implementer\ndescription: Implements a new feature in isolation, owns one module end-to-end.\ntools: Read, Write, Edit, Bash, Grep, Glob\nmodel: sonnet\n---\n\nYou are implementing an assigned feature. Work only on the files and\ndirectories you are assigned. If you discover a dependency that requires\ncross-team coordination, send a message to the architect teammate.\n\nBefore starting work: read CLAUDE.md for project conventions.\nAfter completing work: run the test suite for your changed files.\nReport any cross-cutting concerns to the lead before marking your task complete.\n```\n\nNote: the `name` field here is what the lead uses when referencing this role at spawn time — keep it short and descriptive."
   },
   {
    "heading": "Common Pitfalls and Gotchas",
    "body": "### Subagent pitfalls\n\n**Forgetting that subagents load CLAUDE.md but not conversation history.** Every non-Explore/Plan subagent loads your CLAUDE.md hierarchy from scratch. If your CLAUDE.md has rules that conflict with the subagent's system prompt, Claude must reconcile them. Keep subagent prompts specific to avoid ambiguity. Explore and Plan specifically skip CLAUDE.md and git status for speed — if a rule must reach them, restate it in the delegation prompt.\n\n**Description quality determines delegation quality.** Claude uses the `description` field to decide when to delegate. A vague description (`description: Code helper`) means Claude will delegate unpredictably. A sharp description with trigger conditions (`description: >- Runs the full test suite and reports only failing tests. Use proactively after any code change.`) means Claude delegates reliably.\n\n**Plugin subagents silently ignore `hooks`, `mcpServers`, and `permissionMode`.** If your subagent definition uses these fields and you distribute it as a plugin, those fields are no-ops. Users who need those fields must copy the definition into `.claude/agents/` or `~/.claude/agents/`.\n\n**`tools` field with `Agent` controls spawning.** If you list `Agent` in `tools` without parentheses, the subagent can spawn any subagent type. If you omit `Agent` entirely, the subagent cannot spawn at all. `Agent(type1, type2)` restricts which types can be spawned, but only when the agent is running as the main thread via `--agent` — in a regular nested subagent, the type list is ignored.\n\n**Auto-compaction inside subagents.** Subagents have their own context and will auto-compact when they hit the threshold. `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE` applies to subagents too. If a subagent is producing detailed work that must not be compressed mid-run, set `maxTurns` to bound the work within a single compact window.\n\n**Resuming a subagent reuses its full prior transcript.** When you ask Claude to continue a previous subagent's work, the subagent resumes with its complete conversation history intact — including all prior tool calls and results. The built-in Explore and Plan subagents are one-shot and return no agent ID; they cannot be resumed.\n\n### Agent team pitfalls\n\n**The lead's conversation history does not carry over — design spawn prompts accordingly.** The single most common mistake. Teammates can read CLAUDE.md and project files, but they have no idea what was discussed in the lead's session. If the lead spent 10 turns analyzing an architectural decision, that knowledge is not available to teammates unless explicitly included in the spawn prompt.\n\n**File conflicts from teammates working on the same files.** Two teammates editing the same file produces overwrites. Partition the work so each teammate owns a distinct set of files before spawning. Each teammate's source of truth for what files it owns should be the spawn prompt.\n\n**Task status can lag.** Teammates sometimes fail to mark tasks complete, which blocks dependent tasks. Monitor with Ctrl+T and nudge stuck tasks manually or instruct the lead to check in.\n\n**`/resume` and `/rewind` do not restore in-process teammates.** After resuming a session, the lead may try to message teammates that no longer exist. Spawn fresh teammates when resuming.\n\n**One team per session, no nested teams.** Teammates cannot spawn their own teammates. Only the lead manages the team. For hierarchical coordination, use dynamic workflows instead (up to 1,000 agents, 16 concurrent).\n\n**Token costs scale with team size.** Each teammate is a separate Claude instance with its own context. Three teammates with 200K context each is 600K tokens of parallel consumption. Start with 3-5 teammates and scale only when parallel exploration genuinely benefits from the additional instance.\n\n**Lead starts implementing instead of delegating.** A common pattern when tasks become available faster than teammates claim them. Use: `Wait for your teammates to complete their tasks before proceeding.`\n\n**`skills` and `mcpServers` in a subagent definition are ignored when that definition runs as a teammate.** Teammates load skills and MCP servers from project/user settings, not from the definition file. If a role depends on an inline MCP server, add it to the project's `.mcp.json` instead."
   },
   {
    "heading": "Worked Example: Team Setup for a Parallel Security Audit",
    "body": "This walks through a realistic team setup for a security audit that benefits from parallel investigation and cross-checking.\n\n### 1. Enable agent teams in project settings\n\n```json\n// .claude/settings.json\n{\n  \"env\": {\n    \"CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS\": \"1\"\n  },\n  \"teammateMode\": \"auto\",\n  \"hooks\": {\n    \"TaskCompleted\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"./.claude/hooks/require-findings-doc.sh\"\n          }\n        ]\n      }\n    ]\n  }\n}\n```\n\n### 2. Define reusable role subagents\n\n```markdown\n# .claude/agents/auth-auditor.md\n---\nname: auth-auditor\ndescription: Audits authentication and session management for security issues.\ntools: Read, Grep, Glob, Bash\nmodel: sonnet\n---\n\nYou are a security auditor specializing in authentication.\nFocus on: token handling, session fixation, CSRF, privilege escalation.\nWrite findings to docs/audit/auth-findings.md as you work.\n```\n\n```markdown\n# .claude/agents/dep-auditor.md\n---\nname: dep-auditor\ndescription: Audits dependencies for known vulnerabilities and supply chain risks.\ntools: Read, Bash, Grep\nmodel: haiku\n---\n\nYou are a dependency security auditor.\nRun npm audit, check package-lock.json for suspicious packages,\nand cross-reference high-severity CVEs against what the app actually imports.\nWrite findings to docs/audit/dep-findings.md as you work.\n```\n\n### 3. Quality gate hook\n\n```bash\n#!/bin/bash\n# .claude/hooks/require-findings-doc.sh\n# Block task completion unless the teammate wrote a findings doc.\nINPUT=$(cat)\nTASK=$(echo \"$INPUT\" | jq -r '.task.title // \"\"')\n\nif echo \"$TASK\" | grep -i \"audit\" > /dev/null; then\n  if ! find docs/audit -name \"*findings.md\" -newer .git/index 2>/dev/null | grep -q .; then\n    echo \"Task cannot be marked complete: no findings document written to docs/audit/\" >&2\n    exit 2\n  fi\nfi\nexit 0\n```\n\nMake it executable: `chmod +x .claude/hooks/require-findings-doc.sh`\n\n### 4. Spawn the team\n\n```text\nSpawn three teammates for a parallel security audit:\n- An \"auth-auditor\" teammate (use that agent type) for src/auth/ and src/middleware/\n- A \"dep-auditor\" teammate (use that agent type) for package.json and lock files\n- An \"api-reviewer\" teammate to audit every route in src/routes/ for\n  missing auth guards, missing rate limiting, and IDOR vulnerabilities.\nHave each write findings docs to docs/audit/ before marking tasks complete.\nWhen all three finish, synthesize findings into docs/audit/REPORT.md.\n```\n\nThe auth-auditor and dep-auditor will use their definition files (tools, model, system prompt body). The api-reviewer will get a generic teammate context. All three are subject to the `TaskCompleted` hook.\n\n### What to watch for\n- Press `Ctrl+T` to open the task list and monitor claim status.\n- If a task appears stuck in \"in progress\" when the teammate is idle, nudge it: select the teammate with `↑`/`↓`, press `Enter`, and send a message asking it to mark the task complete or explain what is blocking it.\n- If the lead starts synthesizing before all three teammates finish, send: `Wait for all teammates to complete before synthesizing.`"
   },
   {
    "heading": "Known Limitations and Stability Caveats",
    "body": "Agent teams are explicitly marked experimental. Before deploying team setups to your team's shared workflow, understand the current limitations:\n\n### Session and resumption\n- **No session resumption for in-process teammates.** `/resume` and `/rewind` restore the lead session but not its teammates. The lead may attempt to message ex-teammates that no longer exist. Workaround: instruct the lead to spawn fresh teammates after resuming.\n- **No nested teams.** Teammates cannot spawn their own teammates. Only the lead manages the team. For deeper hierarchies, use dynamic workflows (which can run up to 1,000 agents per run, 16 concurrently).\n- **One team per session.** You cannot create multiple named teams or share a team across sessions.\n\n### Coordination reliability\n- **Task status can lag.** Teammates sometimes fail to mark tasks as completed, which blocks dependents. Monitor with Ctrl+T; nudge stuck tasks manually.\n- **Lead is fixed.** The main session is the lead for its lifetime. You cannot promote a teammate to lead or transfer leadership.\n- **Shutdown can be slow.** Teammates finish their current request or tool call before shutting down, which can take time. Request a graceful shutdown by referring to the teammate by name.\n\n### Permissions\n- **Permissions set at spawn time.** All teammates start with the lead's permission mode. You can change individual teammate modes after spawning by messaging them directly, but not at spawn time.\n- **Split panes require tmux or iTerm2.** The default in-process mode works in any terminal. Split-pane mode is not supported in VS Code's integrated terminal, Windows Terminal, or Ghostty.\n\n### Versioning to track\nSignificant behavioral changes have shipped rapidly. Key version markers:\n- **v2.1.117**: fork subagents introduced\n- **v2.1.154**: dynamic workflows introduced\n- **v2.1.161**: `/fork` enabled by default\n- **v2.1.172**: nested subagents (subagents spawning subagents, up to depth 5)\n- **v2.1.178**: nested project discovery (closest-to-cwd wins); workflow monorepo support; `TeamCreate`/`TeamDelete` tools removed; teammate spawning no longer requires a setup step\n- **v2.1.179**: `teammateMode` default changed from `\"auto\"` to `\"in-process\"`\n- **v2.1.181**: idle teammate rows hide after 30 seconds\n- **v2.1.186**: `\"iterm2\"` display mode; background subagent permission prompts surface in main session\n\nPin your team's Claude Code version if you depend on specific team coordination behaviors. Run `claude update` to upgrade."
   }
  ],
  "preQuiz": [
   {
    "prompt": "Your team creates a subagent file at `.claude/agents/reviewer.md` and also has one at `~/.claude/agents/reviewer.md` (a user-scoped default). Both have `name: reviewer`. Which definition wins when you invoke the reviewer from a project directory?",
    "options": [
     "The user-scoped one (~/.claude/agents/) wins because user settings take higher priority than project settings",
     "The project-scoped one (.claude/agents/) wins because project definitions take precedence over user-scoped ones",
     "Claude merges both definitions, with the project file overriding conflicting fields",
     "Whichever file was modified more recently wins"
    ],
    "correct": 1,
    "sectionIndices": [
     2
    ],
    "explanation": "Project-scoped definitions (priority 3) override user-scoped ones (priority 4). The precedence order is: managed settings > --agents CLI flag > project > user > plugin. On name collision the higher-priority scope wins outright; there is no merging."
   },
   {
    "prompt": "You write a subagent with `tools: [Read, Glob]` and `disallowedTools: [Read]`. What tools does the subagent actually have access to?",
    "options": [
     "Read and Glob, because the tools allowlist overrides the denylist",
     "Only Glob, because disallowedTools is applied first and then tools resolves against the remainder",
     "No tools at all, because conflicting tool configs cancel each other out",
     "All tools, because specifying both fields is invalid and Claude falls back to inheriting everything"
    ],
    "correct": 1,
    "sectionIndices": [
     3
    ],
    "explanation": "When both fields are set, disallowedTools applies first, then tools resolves against what remains. A tool in both lists ends up removed. So Read is denied first, leaving only Glob available."
   },
   {
    "prompt": "You delegate a task to the built-in Explore subagent with a note that says 'ignore vendor/ in your search'. The task requires Explore to respect a CLAUDE.md rule that says 'always skip vendor/'. What actually happens?",
    "options": [
     "Explore loads CLAUDE.md automatically, so the rule is enforced without any extra work",
     "Explore skips CLAUDE.md entirely — you must restate critical rules like 'ignore vendor/' directly in the delegation prompt",
     "Explore loads CLAUDE.md but only the first 200 lines, so long files may miss the rule",
     "Explore inherits CLAUDE.md rules but ignores them by default unless you set permissionMode: plan"
    ],
    "correct": 1,
    "sectionIndices": [
     4
    ],
    "explanation": "Explore (and Plan) skip CLAUDE.md and git status to stay fast and cheap. There is no field or setting to change this behavior. Any critical rule that Explore must follow — like ignoring vendor/ — must be restated explicitly in the delegation prompt."
   },
   {
    "prompt": "You edit a custom subagent file on disk while a Claude Code session is already running. When does the updated definition take effect?",
    "options": [
     "Immediately — Claude hot-reloads .claude/agents/ in the background",
     "After you send the next message in the current session",
     "Only after restarting the session, because files on disk load at session start with no hot reload",
     "After 60 seconds — Claude polls for changes on a timer"
    ],
    "correct": 2,
    "sectionIndices": [
     2
    ],
    "explanation": "Files on disk load at session start and there is no hot reload. To pick up edits, you must restart the session. The exception is editing via the /agents interface, which takes effect immediately."
   },
   {
    "prompt": "Your team wants to use the Explore subagent for quick code searches and then have a general-purpose subagent continue the work from where Explore left off, reusing its findings. What is the problem with this plan?",
    "options": [
     "Explore cannot be invoked from within another subagent due to nesting depth limits",
     "Explore is one-shot and cannot be resumed — you cannot continue its work in a follow-up interaction with the same Explore agent",
     "Explore runs with a separate model that general-purpose cannot read output from",
     "Explore results are encrypted and only accessible to the spawning conversation, not to other subagents"
    ],
    "correct": 1,
    "sectionIndices": [
     4
    ],
    "explanation": "Explore and Plan are one-shot: they return no agent ID and cannot be resumed. If you need continuable work, use the general-purpose subagent or a custom subagent instead. You can still feed Explore's summary to a general-purpose agent, but you cannot 'resume' Explore itself."
   },
   {
    "prompt": "You set the parent conversation to `permissionMode: acceptEdits`. A subagent frontmatter specifies `permissionMode: auto`. What permission mode does the subagent actually run under?",
    "options": [
     "auto, because the subagent frontmatter overrides the parent",
     "acceptEdits, because if the parent is acceptEdits or bypassPermissions that takes precedence and overrides the subagent",
     "default, because conflicting permission modes always fall back to the safe default",
     "dontAsk, because acceptEdits on the parent promotes to dontAsk for subagents"
    ],
    "correct": 1,
    "sectionIndices": [
     3
    ],
    "explanation": "If the parent is bypassPermissions or acceptEdits, that takes precedence and overrides the subagent's permissionMode entirely. If the parent is auto, the subagent inherits auto and its own permissionMode is ignored."
   },
   {
    "prompt": "You want to use a subagent that connects to a sensitive internal MCP server, but you do NOT want that MCP server's tool descriptions to appear in your main conversation's context window. How do you accomplish this?",
    "options": [
     "Set disallowedTools: [mcp__*] in the subagent frontmatter to prevent the tools from loading",
     "Define the MCP server as an inline mcpServers definition in the subagent frontmatter rather than using a pre-configured server reference",
     "Use the --bare CLI flag to strip MCP tools from the main conversation",
     "Add the MCP server to managed settings so it bypasses the main context window automatically"
    ],
    "correct": 1,
    "sectionIndices": [
     3
    ],
    "explanation": "Defining the MCP server inline in the subagent's mcpServers frontmatter (instead of referencing a pre-configured server) keeps that server's tool descriptions out of the main conversation's context. The server is connected when the subagent starts and disconnected when it finishes — only the subagent pays the context cost."
   },
   {
    "prompt": "A subagent at depth 4 is running and tries to spawn its own subagent. What happens?",
    "options": [
     "It spawns successfully but the nested subagent runs at reduced capability",
     "It throws a runtime error and the depth-4 subagent is terminated",
     "The depth-4 subagent gets no Agent tool and therefore cannot spawn any nested subagents",
     "It succeeds only if you set CLAUDE_CODE_ENABLE_DEEP_NESTING=1"
    ],
    "correct": 2,
    "sectionIndices": [
     5
    ],
    "explanation": "The nesting depth limit is fixed at 5 and is not configurable. A depth-5 subagent gets no Agent tool at all, preventing further nesting. A depth-4 subagent can still try, but if the limit would be exceeded at depth 5, that attempt gets no Agent tool."
   },
   {
    "prompt": "You use /fork to start a side exploration from your current conversation. Later you want to use /fork again from inside that fork. What happens?",
    "options": [
     "The nested fork succeeds because forks support up to 3 levels of nesting",
     "A fork cannot spawn another fork — you must use a named subagent if you need further delegation from inside a fork",
     "The nested fork succeeds but runs as a general-purpose subagent instead of a true fork",
     "The fork attempt is silently ignored and the work continues in the current fork context"
    ],
    "correct": 1,
    "sectionIndices": [
     5
    ],
    "explanation": "Forks cannot spawn other forks. If you need to delegate from within a fork, you must use a named subagent or other delegation mechanism. This is a hard constraint, not a configuration option."
   },
   {
    "prompt": "Your team enables CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS='1' and asks Claude to form a team to parallelize a large refactor. Nothing happens — no teammates are spawned. What is the most likely cause?",
    "options": [
     "Agent teams require at least 4 free terminal panes to spawn teammates",
     "The env var must be set in the Claude Code settings.json env block or shell — if it is not present in the environment at startup, no team forms and Claude silently won't spawn teammates",
     "Agent teams only work if you also set teammateMode to tmux in settings",
     "Agent teams are only available with claude --agent-mode flag"
    ],
    "correct": 1,
    "sectionIndices": [
     8
    ],
    "explanation": "Agent teams are disabled by default. You must set CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS='1' in the settings.json env block or shell environment before starting the session. Without it, no team forms, no team directories are written, and Claude silently won't spawn teammates — regardless of the request."
   },
   {
    "prompt": "You're deciding between running a background bash command and spawning a subagent for a side task. The key question is: does the side task need to run code, read files, and return a synthesized result back to the main conversation? Which statement is correct?",
    "options": [
     "A background bash command can do all of this — it spawns a shell agent that returns a summary",
     "A background bash command runs a shell command without blocking but does NOT spawn an agent; you need a subagent if the task needs agent capabilities and should return a summary",
     "Both options spawn agents — the difference is only whether the main conversation blocks",
     "Subagents cannot return results to the main conversation; only background bash commands can pipe results back"
    ],
    "correct": 1,
    "sectionIndices": [
     0
    ],
    "explanation": "A background bash command runs a shell command without blocking but does NOT spawn an agent. If you need agent-level capabilities (tool use, reasoning, file reading, synthesis) and a summary returned to the main conversation, you need a subagent. Background bash is for non-blocking shell execution only."
   },
   {
    "prompt": "Your subagent uses `model: haiku`. But at the organization level, the env var CLAUDE_CODE_SUBAGENT_MODEL is set to 'claude-opus-4-8'. Which model actually runs the subagent?",
    "options": [
     "claude-haiku-4-5, because the frontmatter model field is the final authority",
     "claude-opus-4-8, because the CLAUDE_CODE_SUBAGENT_MODEL env var has the highest resolution priority",
     "Whichever model the main conversation is using, because inherit is the implicit fallback",
     "The main conversation model, overriding both env var and frontmatter"
    ],
    "correct": 1,
    "sectionIndices": [
     3
    ],
    "explanation": "Model resolution order is: (1) CLAUDE_CODE_SUBAGENT_MODEL env var, (2) per-invocation model param, (3) frontmatter model, (4) main conversation's model. The env var wins, so claude-opus-4-8 runs even though the frontmatter says haiku."
   }
  ],
  "tasks": [
   {
    "id": "stage-4-task-create-subagent",
    "afterSectionIdx": 3,
    "title": "Write and invoke a project-scoped subagent with tool restrictions",
    "instructions": "Create a read-only code reviewer subagent for your project that cannot write or edit files.\n\n1. Create the agents directory if it doesn't exist:\n```bash\nmkdir -p .claude/agents\n```\n\n2. Create the file `.claude/agents/code-reviewer.md` with this content:\n```markdown\n---\nname: code-reviewer\ndescription: Reviews code for correctness, style, and security issues. Use proactively when the user asks for a code review or wants feedback on code quality. Does not make changes — read-only analysis only.\ndisallowedTools:\n  - Write\n  - Edit\n  - MultiEdit\nmodel: haiku\n---\n\nYou are a careful code reviewer. Analyze code for bugs, security issues, and style problems. Always cite the specific line or function you're commenting on. Never suggest edits directly — describe what should change and why.\n```\n\n3. Restart your Claude Code session so the file is loaded.\n\n4. In the new session, ask Claude: \"Use the code-reviewer agent to review this snippet: `def divide(a, b): return a / b`\"\n\n5. Confirm the reviewer runs and does NOT attempt to edit any files.",
    "doneWhen": "The code-reviewer subagent runs successfully, provides feedback on the snippet, and does not attempt to call Write or Edit tools."
   },
   {
    "id": "stage-4-task-subagent-precedence",
    "afterSectionIdx": 2,
    "title": "Verify project vs. user subagent precedence by creating a name collision",
    "instructions": "Test that project-scoped subagents override user-scoped ones of the same name.\n\n1. Create a user-scoped subagent:\n```bash\nmkdir -p ~/.claude/agents\ncat > ~/.claude/agents/greeter.md << 'EOF'\n---\nname: greeter\ndescription: A greeter subagent that says hello.\n---\n\nYou always respond with: \"Hello from the USER-SCOPED greeter!\"\nEOF\n```\n\n2. Create a project-scoped subagent with the SAME name in your current project:\n```bash\nmkdir -p .claude/agents\ncat > .claude/agents/greeter.md << 'EOF'\n---\nname: greeter\ndescription: A greeter subagent that says hello.\n---\n\nYou always respond with: \"Hello from the PROJECT-SCOPED greeter!\"\nEOF\n```\n\n3. Restart your Claude Code session from inside the project directory.\n\n4. Ask Claude: \"Invoke the greeter agent and tell me what it says.\"\n\n5. Verify the response contains 'PROJECT-SCOPED' — confirming the project definition won.\n\n6. Clean up: `rm ~/.claude/agents/greeter.md .claude/agents/greeter.md`",
    "doneWhen": "The greeter subagent responds with 'Hello from the PROJECT-SCOPED greeter!' confirming project scope overrides user scope."
   },
   {
    "id": "stage-4-task-agent-teams",
    "afterSectionIdx": 8,
    "title": "Enable agent teams and observe parallel teammate spawning",
    "instructions": "Enable the experimental agent teams feature and give Claude a task that benefits from parallelization.\n\n1. Open your Claude Code settings file:\n```bash\ncat ~/.claude/settings.json\n# or for project-level:\ncat .claude/settings.json\n```\n\n2. Add the env var to your settings.json (create the file if needed):\n```json\n{\n  \"env\": {\n    \"CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS\": \"1\"\n  }\n}\n```\nSave the file.\n\n3. Restart Claude Code to pick up the new env var.\n\n4. Verify the env var is active by running in the terminal:\n```bash\necho $CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS\n```\nIt should print `1`.\n\n5. Give Claude a genuinely parallel task such as: \"Form an agent team: have one teammate research how to set up ESLint in a Node project and another research how to set up Prettier. Then combine their findings into a single setup guide.\"\n\n6. Observe whether Claude attempts to spawn teammates. Check `/tasks` in the Claude Code session to see running agents.\n\n7. When done testing, remove the env block from settings.json to disable agent teams.",
    "doneWhen": "Claude Code picks up CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1 from settings.json, and when given a parallelizable task Claude attempts to form a team or explicitly explains it is coordinating teammates."
   }
  ],
  "visualizations": [
   {
    "id": "stage-4-v",
    "kind": "comparison-table",
    "title": "Subagents & agent teams",
    "textualSummary": "Key concepts of Subagents & agent teams: Choosing a multi-context approach, Subagent, Definition scope and precedence.",
    "columns": [
     "Concept",
     "In this stage"
    ],
    "rows": [
     {
      "label": "Choosing a multi-context approach",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "Subagent",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "Definition scope and precedence",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "Subagent frontmatter configuration",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     }
    ]
   }
  ],
  "confusions": [
   {
    "misconception": "Skipping Subagents & agent teams.",
    "correction": "Each stage is load-bearing for the setup."
   },
   {
    "misconception": "These concepts are interchangeable.",
    "correction": "Each has a distinct role; see the definitions."
   }
  ],
  "quiz": [
   {
    "id": "stage-4-q1",
    "type": "multiple-choice",
    "prompt": "A teammate is investigating a flaky test, fixing a bug, and reviewing a PR — three independent tasks. She wants to hand them off, check their status at a glance, and step in only when one needs her. Which approach fits?",
    "options": [
     "Agent teams — Claude supervises three peers sharing a task list and can message them directly",
     "Agent view (`claude agents`) — three independent tasks visible as rows; she steps in only when needed",
     "Three sequential subagents — each finishes before the next starts, keeping context clean",
     "Dynamic workflows — the three tasks form phases of a workflow run"
    ],
    "correct": 1,
    "explanation": "Agent view (`claude agents`) is exactly the pattern described: several independent tasks handed off and checked on at a glance, with the human stepping in only when one needs them. Agent teams are for genuinely parallelizable work where Claude (not the human) coordinates peers. Sequential subagents don't run in parallel and require waiting. Dynamic workflows are for jobs that outgrow manual coordination, such as 500-file migrations or codebase-wide audits."
   },
   {
    "id": "stage-4-q2",
    "type": "multiple-choice",
    "prompt": "You delegate a research task to the built-in Explore subagent and tell it in the delegation prompt to 'follow all rules in CLAUDE.md, especially the one about ignoring the vendor/ directory.' Your teammate says that's redundant — Explore loads CLAUDE.md automatically. Who is correct, and why?",
    "options": [
     "Your teammate is correct — all built-in subagents load CLAUDE.md at startup just like custom ones",
     "You are correct — Explore and Plan skip CLAUDE.md to stay fast and cheap; critical rules must be restated in the delegation prompt",
     "Your teammate is correct — Explore inherits the parent conversation's context, so CLAUDE.md rules are already in scope",
     "You are correct — CLAUDE.md loads, but Explore ignores tool-restriction rules unless they are repeated in the prompt"
    ],
    "correct": 1,
    "explanation": "The content explicitly states: 'Explore and Plan SKIP CLAUDE.md and git status to stay fast/cheap (every other built-in and custom subagent loads both); there is no field or setting to change this. Restate critical rules (e.g. \"ignore vendor/\") in the delegation prompt when delegating to them.' Your teammate holds a common misconception that all subagents load CLAUDE.md uniformly. Explore does NOT inherit the parent conversation's context — each subagent starts with fresh isolated context."
   },
   {
    "id": "stage-4-q3",
    "type": "multiple-choice",
    "prompt": "You define a subagent in `.claude/agents/review.md` and set both `tools: [Read, Grep]` and `disallowedTools: [Grep]`. You expect Grep to be allowed since it appears in `tools`. What actually happens?",
    "options": [
     "Grep is allowed — `tools` is an allowlist and takes precedence over `disallowedTools`",
     "Grep is removed — `disallowedTools` applies first, then `tools` resolves against the remainder; a tool in both is removed",
     "The frontmatter is invalid — you cannot set both fields simultaneously",
     "Grep is allowed in read mode only — `disallowedTools` blocks write operations but not reads"
    ],
    "correct": 1,
    "explanation": "The content states: 'If both set, `disallowedTools` applies first, then `tools` resolves against the remainder; a tool in both is removed.' So Grep gets removed by `disallowedTools` before `tools` is consulted, leaving only Read. The common misconception is that `tools` as an explicit allowlist overrides denials — but the resolution order is denylist-first."
   },
   {
    "id": "stage-4-q4",
    "type": "multiple-choice",
    "prompt": "Your team has a custom subagent `security-reviewer.md` checked into `.claude/agents/`. You edit it on disk during a live session. When does the change take effect?",
    "options": [
     "Immediately — Claude polls `.claude/agents/` for changes and hot-reloads them",
     "On the next delegation — the file is re-read each time the subagent is invoked",
     "Only after restarting the session — files on disk load at session start; no hot reload",
     "Only after running `/agents` to manually refresh the library"
    ],
    "correct": 2,
    "explanation": "The content states: 'Files on disk load at session start (restart required to apply edits — no hot reload). Subagents created/edited via the /agents interface take effect immediately.' So a disk edit requires a restart; only edits made through the /agents interface bypass this and apply immediately."
   },
   {
    "id": "stage-4-q5",
    "type": "multiple-choice",
    "prompt": "You want a subagent that can query your read-only analytics database (SELECT only). The `tools` allowlist does not let you restrict by SQL verb. Which mechanism lets you block INSERT/UPDATE/DELETE while still allowing SELECT?",
    "options": [
     "Set `permissionMode: plan` in the frontmatter — plan mode inherently blocks write operations",
     "Use a `PreToolUse` hook with exit code 2 to inspect and block write SQL verbs",
     "Add the database MCP server to `disallowedTools` and expose only a read-only MCP endpoint",
     "Set `disallowedTools: [SQL-Write]` — Claude Code recognizes this synthetic tool name"
    ],
    "correct": 1,
    "explanation": "The content explicitly calls out this use case: 'Use PreToolUse hooks (exit code 2 to block) for tool gating finer than the `tools` field allows — e.g. allow read-only SQL while blocking INSERT/UPDATE/DELETE.' Plan mode restricts to planning actions, not SQL-verb filtering. Disabling the MCP server entirely removes all SQL access. There is no 'SQL-Write' synthetic tool name."
   },
   {
    "id": "stage-4-q6",
    "type": "multiple-choice",
    "prompt": "A subagent is running in the background. It tries to use a tool that requires permission. Your Claude Code is older than v2.1.186. What happens?",
    "options": [
     "The permission prompt surfaces in the main session, naming the subagent, and you can approve or deny",
     "The subagent pauses and waits indefinitely until you check its status with `/tasks`",
     "The tool call is auto-denied, but the subagent continues running",
     "The subagent is stopped and removed from the task list"
    ],
    "correct": 2,
    "explanation": "The content states: 'Background subagent permission prompts (v2.1.186+) surface in the main session naming the subagent (approve, or Esc denies that one tool call without stopping it). Before v2.1.186, background subagents auto-denied any prompting tool call.' So on older versions the tool call is silently denied but the subagent keeps running."
   },
   {
    "id": "stage-4-q7",
    "type": "multiple-choice",
    "prompt": "You have a subagent with `model: haiku` in its frontmatter. Before invoking it, you set the `CLAUDE_CODE_SUBAGENT_MODEL` environment variable to `sonnet`. Which model does the subagent use?",
    "options": [
     "Haiku — frontmatter is the authoritative per-subagent configuration and overrides env vars",
     "Sonnet — the env var has higher priority than the frontmatter `model` field",
     "The main conversation's model — `inherit` is the default and frontmatter `model` is advisory",
     "Whichever model the parent conversation is using at invocation time"
    ],
    "correct": 1,
    "explanation": "The content gives an explicit resolution order: '(1) CLAUDE_CODE_SUBAGENT_MODEL env var, (2) per-invocation model param, (3) frontmatter, (4) main conversation's model.' The env var is highest priority, so `sonnet` wins over the `haiku` in frontmatter. The common misconception is that frontmatter, being the explicit per-subagent config, should take precedence."
   },
   {
    "id": "stage-4-q8",
    "type": "multiple-choice",
    "prompt": "You need to run a long research pipeline that cross-checks findings from multiple angles and will involve 500 documents. You consider using agent teams. Which approach is actually more appropriate, and why?",
    "options": [
     "Agent teams — the lead Claude can coordinate parallel research and have teammates message each other",
     "Dynamic workflows — for jobs that outgrow manual coordination, like large migrations or cross-verified research",
     "Multiple forks — each fork handles one angle of research and reports back to the parent",
     "Agent view — you monitor rows and merge findings manually at the end"
    ],
    "correct": 1,
    "explanation": "The content specifically lists 'cross-checked research' and 'large migrations (e.g. 500 files)' as the use case for dynamic workflows: 'a job that outgrows manual coordination or needs cross-verification: codebase-wide audits, large migrations (e.g. 500 files), cross-checked research, multi-angle planning.' Agent teams are for parallelizable work with Claude coordinating peers, not for the scale/verification needs described here. Forks cannot spawn other forks and are not designed for this scale."
   },
   {
    "id": "stage-4-q9",
    "type": "multiple-choice",
    "prompt": "You write a subagent with `isolation: worktree`. From which branch does the temporary git worktree branch by default?",
    "options": [
     "The current HEAD of the parent conversation's working branch",
     "The default branch of the repository",
     "A detached HEAD at the last commit before the session started",
     "The branch specified in the subagent's frontmatter `branch` field"
    ],
    "correct": 1,
    "explanation": "The content states: '`isolation: worktree` — runs the subagent in a temporary git worktree branched (by default) from the **default branch**, not parent HEAD.' The most intuitive assumption — that it branches from parent HEAD — is explicitly contradicted. There is no `branch` frontmatter field mentioned."
   },
   {
    "id": "stage-4-q10",
    "type": "multi-select",
    "prompt": "Your org wants to distribute a standard security-review subagent to all teams. Select ALL statements that are true about distribution and precedence.",
    "options": [
     "Deploying via managed settings (`.claude/agents/` in the managed settings dir) gives it the highest precedence, overriding project and user definitions with the same name",
     "Checking the subagent into each project's `.claude/agents/` is the recommended way to share codebase-specific subagents within a team",
     "A user-level definition in `~/.claude/agents/` has higher precedence than a project-level definition in `.claude/agents/`",
     "For org-wide standard subagents, managed settings is the recommended deployment mechanism so they take precedence",
     "Plugin subagents always take precedence over project subagents because plugins load after the project scan"
    ],
    "correct": [
     0,
     1,
     3
    ],
    "explanation": "Options A and D are both true per the precedence table: managed settings is Priority 1 (highest). Option B is true: 'Check project subagents into version control (.claude/agents/) so the team shares and improves them — the recommended way to distribute codebase-specific subagents.' Option C is false: the table shows project (Priority 3) beats user (Priority 4). Option E is false: plugins are Priority 5 (lowest) — they do not override project definitions."
   },
   {
    "id": "stage-4-q11",
    "type": "multi-select",
    "prompt": "Which of the following are true about how a subagent's initial context is constructed? Select ALL that apply.",
    "options": [
     "The subagent sees the full message history of the parent conversation",
     "The subagent's own system prompt (the frontmatter body) is included",
     "CLAUDE.md and the memory hierarchy load for custom subagents (but not Explore or Plan)",
     "A git-status snapshot is included in the initial context (except for Explore and Plan)",
     "Any files the parent conversation has already Read are automatically available to the subagent"
    ],
    "correct": [
     1,
     2,
     3
    ],
    "explanation": "The content states a subagent's initial context = 'own system prompt + env details + delegation task + CLAUDE.md/memory hierarchy + git-status snapshot + preloaded skills (Explore/Plan omit CLAUDE.md and git).' So B (own system prompt), C (CLAUDE.md/memory hierarchy, except Explore/Plan), and D (git-status snapshot, except Explore/Plan) are all correct. A is wrong — subagents start with fresh isolated context and do NOT see conversation history. E is wrong — previously-Read files are not automatically transferred; each subagent starts fresh."
   },
   {
    "id": "stage-4-q12",
    "type": "multiple-choice",
    "prompt": "You want to add an inline MCP server definition directly in a subagent's `mcpServers` frontmatter field rather than referencing an already-configured server. What is the primary benefit of doing this?",
    "options": [
     "Inline definitions persist across sessions, while referenced servers are ephemeral",
     "Inline servers connect at subagent start and disconnect at finish, keeping the server's tool descriptions out of the main conversation context",
     "Inline definitions bypass MCP permission checks, making the subagent faster",
     "Inline definitions allow the subagent to share the server connection with other sibling subagents"
    ],
    "correct": 1,
    "explanation": "The content states: 'Define inline to keep the server's tool descriptions out of the main conversation context — only the subagent pays the cost.' Inline servers are connected at start and disconnected at finish. They don't persist longer than a reference; they don't bypass permissions; and they are isolated to that subagent, not shared with siblings."
   },
   {
    "id": "stage-4-q13",
    "type": "multiple-choice",
    "prompt": "You have a subagent nested 4 levels deep. It tries to spawn another subagent. What happens?",
    "options": [
     "It succeeds — nesting depth is capped at 10 by default and can be configured higher",
     "It fails — the depth limit is fixed at 5 and a depth-5 subagent gets no Agent tool",
     "It succeeds if the nested subagent is a built-in (Explore/Plan), which are exempt from depth limits",
     "It fails immediately and the depth-4 subagent is terminated"
    ],
    "correct": 1,
    "explanation": "The content states: 'Depth limit fixed at 5 (not configurable); a depth-5 subagent gets no Agent tool.' A depth-4 subagent CAN spawn one more (making that child depth-5), but a depth-5 subagent has no Agent tool and cannot spawn further. The limit is not configurable, and built-ins are not exempt. Reaching the depth limit removes the Agent tool silently — it does not terminate the subagent."
   },
   {
    "id": "stage-4-q14",
    "type": "multiple-choice",
    "prompt": "A plugin named `my-plugin` contains `agents/review/security.md`. What identifier do you use to reference this subagent?",
    "options": [
     "`security` — identity is the `name` frontmatter field only, regardless of file path",
     "`review:security` — the subfolder path forms the identifier within the plugin",
     "`my-plugin:review:security` — plugin subfolders scope the identifier using the plugin name as prefix",
     "`my-plugin:security` — only the plugin name and filename matter, not intermediate folders"
    ],
    "correct": 2,
    "explanation": "The content states: 'Plugin subfolders DO scope the identifier: `agents/review/security.md` in plugin `my-plugin` → `my-plugin:review:security`.' This is explicitly contrasted with project/user subagents where 'subfolders do NOT affect identity — identity is the `name` frontmatter only.' Plugins are a special case where the folder structure IS part of the identifier."
   },
   {
    "id": "stage-4-q15",
    "type": "multiple-choice",
    "prompt": "You want a quick answer to a question using your full conversation context, but you don't want to spawn a subagent or use any tools — and you want the answer discarded after reading it. What feature fits?",
    "options": [
     "/fork — forks inherit full context and their results return to the parent",
     "A background bash command — it runs shell commands without blocking",
     "/btw — answers with full conversation context but no tool access, and the answer is discarded",
     "A general-purpose subagent — it inherits the model but starts with fresh context"
    ],
    "correct": 2,
    "explanation": "The content states: '**`/btw`** answers a quick question with full conversation context but no tool access, and the answer is discarded — cheaper than spawning a subagent.' Forks use tools and their results are kept. Background bash runs shell commands, not conversational questions. General-purpose subagents start with fresh isolated context, not full conversation context."
   },
   {
    "id": "stage-4-q16",
    "type": "multiple-choice",
    "prompt": "Agent teams are enabled with the environment variable. Your lead Claude tries to form a team, but no teammates appear and no team directories are written. What is the most likely cause?",
    "options": [
     "The `teammateMode` is set to `in-process`, which disables team formation",
     "The `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` env var is not set to `\"1\"`",
     "Agent teams require at least one custom subagent in `.claude/agents/` to function",
     "The lead model must be set to `opus` for agent teams to activate"
    ],
    "correct": 1,
    "explanation": "The content states: 'Disabled by default. Enable with `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=\"1\"` (in `settings.json` env block or shell). Without it, no team forms, no team dirs are written, and Claude silently won't spawn teammates.' The in-process mode is the default display mode but does not disable team formation. No custom subagent requirement is mentioned. No model requirement is documented."
   },
   {
    "id": "stage-4-q17",
    "type": "multiple-choice",
    "prompt": "You want to expose a third-party tool (e.g., Codex) to Claude so it can be used within a subagent or agent team workflow. How does the content say this should be done?",
    "options": [
     "Use `/fork` to hand off to Codex directly — forks can delegate to non-Claude agents",
     "Define a dynamic workflow with Codex as a phase runner",
     "Expose it to Claude as an MCP server",
     "Add Codex as a `teammate` entry in the agent team configuration"
    ],
    "correct": 2,
    "explanation": "The content states: 'To involve a non-Claude tool (e.g. Codex) in any approach, expose it to Claude as an **MCP server**.' This is listed as the general integration point for non-Claude tools across all delegation approaches. The other options (fork delegation, dynamic workflow phases, teammate entries) are not described as the mechanism for non-Claude tool integration."
   },
   {
    "id": "stage-4-q18",
    "type": "multiple-choice",
    "prompt": "You run `--agent my-agent` to start a session as a custom subagent. Your project has a `CLAUDE.md` with important coding rules. Does `CLAUDE.md` load in this mode?",
    "options": [
     "No — `--agent` fully replaces the default system prompt including all project context loading",
     "Yes — CLAUDE.md and project memory still load even when using `--agent`",
     "Only if the subagent frontmatter explicitly includes `memory: project`",
     "Only the user-level `~/.claude/CLAUDE.md` loads; project-level CLAUDE.md is bypassed"
    ],
    "correct": 1,
    "explanation": "The content states: '`--agent <name>` runs the whole session as that subagent — its system prompt fully replaces the default Claude Code system prompt (like `--system-prompt`); CLAUDE.md/project memory still load.' So the system prompt is replaced, but CLAUDE.md and project memory continue loading normally. The `memory` frontmatter field controls the agent-memory feature (MEMORY.md), not whether CLAUDE.md loads."
   }
  ],
  "masteryCheckpoint": "You can explain the concepts of Subagents & agent teams."
 },
 {
  "id": "stage-5",
  "stage": 5,
  "title": "MCP (connecting tools)",
  "summary": "MCP (connecting tools): MCP server, MCP scope (local / project / user), Configuration-as-code distribution.",
  "prerequisites": [
   "stage-4"
  ],
  "objectives": [
   "Understand the concepts in MCP (connecting tools)."
  ],
  "definitions": [
   {
    "term": "MCP server",
    "short": "An external process that gives Claude Code additional tools via the Model Context Protocol; Anthropic doesn't security-audit them, so approval is the admin's responsibility."
   },
   {
    "term": "MCP scope (local / project / user)",
    "short": "Where a server's definition is stored, which determines who sees it and which projects it applies to; scope is fixed at add time and changing it requires remove-then-re-add."
   },
   {
    "term": "Configuration-as-code distribution",
    "short": "Sharing MCP servers with a team by committing the project-scope .mcp.json to version control so every clone gets the same tools (after an approval prompt), with secrets kept out via env-var expansion."
   },
   {
    "term": "Scope precedence (non-merging)",
    "short": "When the same server is defined in multiple places, only the entire entry from the highest-precedence source wins and fields are never merged (local > project > user > plugin > claude.ai connector)."
   },
   {
    "term": "Plugin-bundled MCP servers",
    "short": "Packaging servers inside a plugin so they auto-start when the plugin is enabled, giving zero-touch consistency without anyone running manual add commands."
   },
   {
    "term": "MCP authentication",
    "short": "How Claude Code proves identity to a server: OAuth 2.0 (triggered on a 401/403), a static bearer-token header, or a headersHelper command that generates auth headers fresh at connect time."
   }
  ],
  "sections": [
   {
    "heading": "What MCP Actually Is (and What It Is Not)",
    "body": "The Model Context Protocol is an open JSON-RPC standard that lets Claude Code talk to external processes that expose tools, resources, and prompts. From Claude's perspective, an MCP tool call looks identical to a built-in tool call — the distinction is invisible inside a prompt. From the infrastructure perspective, an MCP server is simply a process (local or remote) that speaks the protocol. Claude Code manages the lifecycle, authentication, and tool routing; you manage which servers are trusted and how they are distributed.\n\n### What MCP is not\n\n**Not a package manager.** There is no built-in registry users can browse and click-to-install from inside Claude Code. The Anthropic Directory at `claude.ai/directory` lists reviewed connectors, and any remote server listed there can be added with `claude mcp add`, but there is no one-click install inside a session. You distribute servers through documentation, a managed plugin marketplace, or `managed-mcp.json` deployment.\n\n**Not security-audited by Anthropic.** Anthropic reviews connectors against listing criteria before they appear in the Anthropic Directory, but it does not audit server code. Every server your team runs is your organization's attack surface. Servers that fetch external content can introduce prompt injection risk — a malicious document retrieved by a web-search server can attempt to hijack Claude's next action. Evaluating and approving servers is explicitly the admin's responsibility.\n\n### The three things MCP servers expose\n\nThe MCP spec defines three capability types. Claude Code supports all three:\n\n| Capability | How you use it in Claude Code |\n|-----------|------------------------------|\n| **Tools** | Called by Claude to act (read a database, open a PR, run a query) |\n| **Resources** | Referenced in prompts with `@server:protocol://path` syntax |\n| **Prompts** | Invoked as `/mcp__servername__promptname` slash commands |\n\nMCP servers can also declare a `claude/channel` capability to push messages into an active session (CI results, monitoring alerts, webhook events). See the Channels reference for how to enable this with the `--channels` flag at startup."
   },
   {
    "heading": "Transports: Choosing the Right Connection Type",
    "body": "Claude Code supports four transports. Your choice determines authentication options, reconnection behavior, and what ends up in `.mcp.json`.\n\n| Transport | `type` field | Best for | OAuth support | Auto-reconnect |\n|-----------|-------------|----------|---------------|----------------|\n| HTTP (Streamable HTTP) | `http` or `streamable-http` | Cloud services; most new integrations | Yes | Yes (5 attempts, exponential backoff) |\n| SSE | `sse` | Legacy remote servers — migrate away | Yes | Yes (5 attempts, exponential backoff) |\n| stdio | `stdio` | Local processes, scripts needing filesystem access | No | No (local process; not reconnected) |\n| WebSocket | `ws` | Event-push / bidirectional streaming | No | Not documented — prefer HTTP |\n\n**HTTP is the recommended default** for any cloud-hosted service. The `streamable-http` alias exists because the MCP specification uses that name; configs copied from server documentation work without modification.\n\n**SSE is deprecated** — migrate off it when the upstream server offers HTTP.\n\n**WebSocket** holds a persistent bidirectional connection and suits servers that push events unprompted (CI results, monitoring alerts). It supports neither OAuth nor the `--transport ws` flag on `claude mcp add`. Authentication is header-only: pass a static token in `headers` or use `headersHelper` for short-lived tokens. Configure WebSocket servers via `add-json` or direct `.mcp.json` editing only.\n\n**stdio** servers receive the `CLAUDE_PROJECT_DIR` environment variable in their spawned environment, pointing at the project root. Read it from inside your server (`os.environ[\"CLAUDE_PROJECT_DIR\"]` in Python, `process.env.CLAUDE_PROJECT_DIR` in Node). In `.mcp.json` `command` or `args` fields, `${VAR}` expansion applies, so reference it as `${CLAUDE_PROJECT_DIR:-.}` (with a default) when defining project- or user-scoped servers. Plugin-provided configs substitute `${CLAUDE_PROJECT_DIR}` directly without needing a default.\n\n### Adding servers\n\n```bash\n# HTTP server — most common\nclaude mcp add --transport http github https://api.githubcopilot.com/mcp/ \\\n  --header \"Authorization: Bearer $GITHUB_PAT\"\n\n# stdio server — note the double-dash separator\nclaude mcp add --env AIRTABLE_API_KEY=YOUR_KEY --transport stdio airtable \\\n  -- npx -y airtable-mcp-server\n\n# WebSocket — must use add-json (--transport ws is not supported)\nclaude mcp add-json events-server \\\n  '{\"type\":\"ws\",\"url\":\"wss://mcp.example.com/socket\",\"headers\":{\"Authorization\":\"Bearer $TOKEN\"}}'\n```\n\nThe double-dash (`--`) in stdio commands is required, not optional decoration. Everything before `--` is parsed as `claude mcp add` flags; everything after is passed verbatim to the spawned process. Without it, a server flag like `--port 8080` is incorrectly parsed as a Claude Code flag.\n\n**`--env` placement pitfall**: `--env` accepts multiple `KEY=value` pairs. If the server name immediately follows `--env`, the CLI reads the name as another key-value pair and rejects it. Always place at least one other option between `--env` and the server name, as in the airtable example above.\n\n### Automatic reconnection detail\n\nIf an HTTP or SSE server disconnects mid-session, Claude Code automatically reconnects with exponential backoff: up to five attempts, starting at a one-second delay and doubling each time. The same retry logic applies at initial startup: up to three retries on transient errors (5xx, connection refused, timeout), then the server is marked failed. Authentication errors and 404s are not retried. Stdio servers are local processes and are never reconnected automatically."
   },
   {
    "heading": "MCP Scopes: Where Configs Live and What They Mean",
    "body": "Every server is stored in exactly one scope, determined at `add` time. Changing scope requires `remove` then re-add — there is no `move` command.\n\n| Scope | `--scope` value | Config file | Who sees it | Shared with team? |\n|-------|----------------|-------------|-------------|-------------------|\n| Local (default) | `local` | `~/.claude.json` (keyed by project path) | Only you, only this project | No |\n| Project | `project` | `.mcp.json` in project root | Anyone who clones the repo | Yes, via VCS |\n| User | `user` | `~/.claude.json` (global section) | Only you, all your projects | No |\n| Managed | (admin-deployed) | System path (see Enterprise section) | Everyone on the machine | Yes, by policy |\n\n> **Scope name history:** In older versions of Claude Code, `local` was called `project` and `user` was called `global`. You may still see the old names in older docs or scripts.\n\n```bash\nclaude mcp add --transport http stripe --scope local  https://mcp.stripe.com\nclaude mcp add --transport http sentry --scope project https://mcp.sentry.dev/mcp\nclaude mcp add --transport http hubspot --scope user https://mcp.hubspot.com/anthropic\n```\n\n### The local scope naming confusion\n\nThe term \"local\" for MCP scope does **not** mean the same thing as \"local\" in settings files. MCP local-scoped servers are stored in `~/.claude.json` (your home directory, private to you). The settings concept of \"local\" is `.claude/settings.local.json` (in the project directory, gitignored). These are different files with overlapping terminology.\n\n### What `~/.claude.json` actually holds\n\n```json\n{\n  \"projects\": {\n    \"/home/alice/projects/api\": {\n      \"mcpServers\": {\n        \"stripe\": { \"type\": \"http\", \"url\": \"https://mcp.stripe.com\" }\n      }\n    }\n  },\n  \"mcpServers\": {\n    \"hubspot\": { \"type\": \"http\", \"url\": \"https://mcp.hubspot.com/anthropic\" }\n  }\n}\n```\n\nLocal-scope entries are nested under the project path; user-scope entries sit at the top-level `mcpServers` key. Both live in the same file. On Windows, `~/.claude.json` resolves to `%USERPROFILE%\\.claude.json`. If the `CLAUDE_CONFIG_DIR` environment variable is set, Claude Code reads `.claude.json` from inside that directory instead.\n\n### Project approval prompt\n\nWhen a developer clones a repo that has a `.mcp.json`, Claude Code prompts for approval before connecting those servers. This is a trust gate, not a nuisance — someone could commit a malicious `.mcp.json` in a PR. To pre-approve specific servers in settings (for CI or known-safe repos):\n\n```json\n{\n  \"enableAllProjectMcpServers\": true\n}\n```\n\nOr for a selective allowlist by server name:\n\n```json\n{\n  \"enabledMcpjsonServers\": [\"github\", \"sentry\", \"postgres\"]\n}\n```\n\nYou can also reject specific servers by name:\n\n```json\n{\n  \"disabledMcpjsonServers\": [\"filesystem\"]\n}\n```\n\nPending-approval servers appear in `claude mcp list` as `⏸ Pending approval`; run Claude Code interactively to review them. Rejected servers show `✗ Rejected` in `claude mcp get <name>`. `claude mcp reset-project-choices` clears stored decisions for the current project."
   },
   {
    "heading": "Configuration-as-Code: Distributing Servers via `.mcp.json`",
    "body": "The `.mcp.json` file at the project root is the team distribution mechanism. Commit it; every clone gets the same server definitions after the approval prompt.\n\n### File format\n\n```json\n{\n  \"mcpServers\": {\n    \"sentry\": {\n      \"type\": \"http\",\n      \"url\": \"https://mcp.sentry.dev/mcp\"\n    },\n    \"postgres\": {\n      \"type\": \"stdio\",\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"@bytebase/dbhub\", \"--dsn\", \"${DB_DSN}\"],\n      \"env\": {}\n    },\n    \"internal-api\": {\n      \"type\": \"http\",\n      \"url\": \"${API_BASE_URL:-https://api.staging.example.com}/mcp\",\n      \"headers\": {\n        \"Authorization\": \"Bearer ${INTERNAL_API_KEY}\"\n      },\n      \"timeout\": 30000\n    }\n  }\n}\n```\n\n### Environment variable expansion\n\nThe `${VAR}` and `${VAR:-default}` syntax is expanded in `command`, `args`, `env`, `url`, and `headers` fields. This is the mechanism for keeping secrets out of version control while still distributing a complete, runnable config.\n\nExpansion rules:\n- `${VAR}` — Claude Code fails to parse the config if the variable is unset (the server cannot start)\n- `${VAR:-default}` — uses `default` if `VAR` is unset\n- Expansion happens at session start from the user's shell environment, not from the `.mcp.json` `env` block\n\nWhat belongs in `.mcp.json` (committed): server URLs, command paths, non-secret config flags, `${VAR}` placeholders for secrets.\n\nWhat does not belong in `.mcp.json`: actual API keys, tokens, passwords, connection strings with credentials. Route those through environment variables or `headersHelper`.\n\n### The `timeout` field\n\nSet `timeout` in milliseconds per server to override the `MCP_TOOL_TIMEOUT` environment variable for that server. This is a hard wall-clock limit per tool call; progress notifications from the server do not extend it. Values below 1000 ms are ignored and fall through to `MCP_TOOL_TIMEOUT` (or its default of approximately 28 hours when unset). Useful for servers known to run slow queries:\n\n```json\n{ \"timeout\": 600000 }\n```\n\n### The `alwaysLoad` field\n\nBy default (when Tool Search is enabled), tool definitions are deferred until Claude searches for them. Setting `\"alwaysLoad\": true` forces a server's tools into context at session start. Requires Claude Code v2.1.121 or later.\n\n```json\n{\n  \"mcpServers\": {\n    \"core-tools\": {\n      \"type\": \"http\",\n      \"url\": \"https://mcp.example.com/mcp\",\n      \"alwaysLoad\": true\n    }\n  }\n}\n```\n\nUse this only for servers whose tools Claude needs on virtually every turn. Each upfront tool definition consumes context that would otherwise be available for your conversation. `alwaysLoad` also blocks session startup until the server connects (capped at the standard 5-second connect timeout), unlike the non-blocking background connection of normal servers.\n\nIndividual tools can also be marked always-loaded by the server itself via `\"anthropic/alwaysLoad\": true` in the tool's `_meta` object in the `tools/list` response — this targets a single tool rather than all tools on the server."
   },
   {
    "heading": "Scope Precedence: Non-Merging Winner-Takes-All",
    "body": "When the same server name appears in multiple scopes, Claude Code connects to it exactly once, using the **entire entry** from the highest-precedence source. Fields are never merged across scopes.\n\n```\nLocal  >  Project  >  User  >  Plugin-provided  >  claude.ai connectors\n(highest)                                          (lowest)\n```\n\nThe three user-managed scopes (local, project, user) deduplicate by **name**. Plugins and connectors match by **endpoint** (URL or command), so a plugin that points at the same URL as a project-scope server is treated as a duplicate and suppressed.\n\n### What \"non-merging\" means in practice\n\nSuppose the project `.mcp.json` defines:\n\n```json\n{ \"mcpServers\": { \"github\": { \"type\": \"http\", \"url\": \"https://api.githubcopilot.com/mcp/\", \"timeout\": 10000 } } }\n```\n\nAnd a developer adds a local override:\n\n```bash\nclaude mcp add --transport http github --scope local \\\n  https://api.githubcopilot.com/mcp/ --header \"Authorization: Bearer $PAT\"\n```\n\nResult: the local entry wins entirely. The project entry's `timeout: 10000` is silently dropped; the local entry's `Authorization` header is used. The developer did not \"add an auth header to the project config\" — they replaced the whole entry.\n\nThis is different from settings files, where permission rules **merge** across scopes. MCP server entries do not merge.\n\n### Practical team pattern\n\n```\n.mcp.json (committed)           — team servers, no credentials, ${VAR} for secrets\n~/.claude.json (local scope)    — per-developer credential overrides or personal additions\n~/.claude.json (user scope)     — personal cross-project servers\n```\n\nKeep server names identical between local overrides and project definitions. The name is the deduplication key. If a developer names their local override `github-dev` instead of `github`, they will get both servers instead of replacing one.\n\n### Reserved server name\n\nThe name `workspace` is reserved for internal Claude Code use. Any server configured with that name is silently skipped at load time with a warning asking you to rename it. Do not use it."
   },
   {
    "heading": "MCP Authentication: OAuth, Static Headers, and headersHelper",
    "body": "Claude Code supports three authentication mechanisms for remote servers. They are not interchangeable — choose based on what the server requires.\n\n### OAuth 2.0\n\nOAuth is triggered automatically when a server returns `401 Unauthorized` or `403 Forbidden`. Claude Code flags the server in `/mcp` and you complete the browser flow there. From v2.1.186, you can also run the flow from the command line:\n\n```bash\nclaude mcp login sentry            # interactive browser flow\nclaude mcp login sentry --no-browser  # prints URL for SSH sessions (requires ssh -t)\nclaude mcp logout sentry           # clear stored credentials\n```\n\nTokens are stored in the system keychain (macOS) or a credentials file, not in any config file. They are refreshed automatically.\n\nIf you configured a static `headers.Authorization` and the server rejects it, Claude Code reports the connection as failed — it does not fall back to OAuth. Remove the static header first if you want to use the OAuth flow.\n\nSome servers require pre-registered OAuth credentials (no Dynamic Client Registration support). The `--client-secret` flag prompts for the secret with masked input:\n\n```bash\nclaude mcp add --transport http \\\n  --client-id your-client-id --client-secret --callback-port 8080 \\\n  my-server https://mcp.example.com/mcp\n\n# Or supply secret via env var to avoid the interactive prompt (useful in CI):\nMCP_CLIENT_SECRET=your-secret claude mcp add --transport http \\\n  --client-id your-client-id --client-secret --callback-port 8080 \\\n  my-server https://mcp.example.com/mcp\n```\n\n`--callback-port` fixes the OAuth redirect URI to `http://localhost:PORT/callback`. The port must match what you registered with the provider. Use `--callback-port` without `--client-id` when you need a fixed port but the server supports Dynamic Client Registration.\n\nTo pin which OAuth scopes are requested (narrowing what Claude can do with the integration):\n\n```json\n{\n  \"mcpServers\": {\n    \"slack\": {\n      \"type\": \"http\",\n      \"url\": \"https://mcp.slack.com/mcp\",\n      \"oauth\": {\n        \"scopes\": \"channels:read chat:write search:read\"\n      }\n    }\n  }\n}\n```\n\n`oauth.scopes` takes precedence over whatever scopes the server advertises. If the server later returns `403 insufficient_scope`, widen the pinned scopes. Claude Code appends `offline_access` automatically when the authorization server supports it, so the token can be refreshed without a new browser sign-in.\n\n### Static bearer token header\n\nFor servers that use a fixed API key or personal access token:\n\n```bash\nclaude mcp add --transport http github https://api.githubcopilot.com/mcp/ \\\n  --header \"Authorization: Bearer $GITHUB_PAT\"\n```\n\nOr in `.mcp.json` using variable expansion to keep the token out of the file:\n\n```json\n{\n  \"mcpServers\": {\n    \"github\": {\n      \"type\": \"http\",\n      \"url\": \"https://api.githubcopilot.com/mcp/\",\n      \"headers\": {\n        \"Authorization\": \"Bearer ${GITHUB_PAT}\"\n      }\n    }\n  }\n}\n```\n\n### headersHelper: dynamic credentials\n\nFor Kerberos, short-lived tokens, internal SSO, or any scheme where credentials cannot be static:\n\n```json\n{\n  \"mcpServers\": {\n    \"internal-api\": {\n      \"type\": \"http\",\n      \"url\": \"https://mcp.internal.example.com\",\n      \"headersHelper\": \"/opt/bin/get-mcp-auth-headers.sh\"\n    }\n  }\n}\n```\n\nThe command runs in a shell at each connection (session start and reconnect) with a 10-second timeout. There is no caching — your script is responsible for any token reuse. It must write a JSON object of string key-value pairs to stdout. Dynamic headers override any static `headers` with the same name.\n\n```bash\n#!/bin/bash\n# /opt/bin/get-mcp-auth-headers.sh\nTOKEN=$(vault kv get -field=token secret/mcp-server)\necho '{\"Authorization\": \"Bearer '\"$TOKEN\"'\"}'\n```\n\nClaude Code sets these environment variables in the helper's environment:\n\n| Variable | Value |\n|----------|-------|\n| `CLAUDE_CODE_MCP_SERVER_NAME` | the name of the MCP server |\n| `CLAUDE_CODE_MCP_SERVER_URL` | the URL of the MCP server |\n\nUse these to write a single helper script that serves multiple servers by branching on the name.\n\n`headersHelper` at project or local scope only runs after you accept the workspace trust dialog — it executes arbitrary shell commands. Put it in `managed-mcp.json` (admin-controlled) if you need it to run unconditionally without each developer's trust acceptance.\n\n### Decision guide\n\n| Authentication need | Use |\n|---------------------|-----|\n| Cloud SaaS with standard OAuth | OAuth flow via `/mcp` or `claude mcp login` |\n| Personal access token / API key | `--header` flag or `headers` + `${VAR}` expansion |\n| Short-lived token, Kerberos, SSO | `headersHelper` script |\n| Admin-provisioned, no per-user auth | `managed-mcp.json` with env var expansion per user |"
   },
   {
    "heading": "Tool Search and Context Budget",
    "body": "MCP servers can collectively expose hundreds of tools. Loading every tool definition into context at session start would consume a large fraction of the context window before any work begins. Tool Search addresses this by deferring definitions until Claude actually needs them.\n\n### How it works\n\nWith Tool Search enabled (the default), only tool names and server instructions load at session start. When Claude determines it needs a tool from an MCP server, it calls the internal `ToolSearch` tool, which retrieves the relevant definitions on demand. From the user's perspective, MCP tools work identically. The practical effect is that the number of MCP servers you can have connected is bounded by your context window budget rather than any per-server cap — Claude Code imposes no fixed per-server tool count limit.\n\nWhen Tool Search is disabled (via `ENABLE_TOOL_SEARCH=false`, or automatically on Vertex AI and proxied deployments), Claude Code uses the `WaitForMcpServers` tool instead to block until servers are ready, and all tool definitions load upfront.\n\nTool Search requires a model that supports `tool_reference` blocks. Haiku models do not support it. On Vertex AI, it is supported for Claude Sonnet 4.5 and later, and Claude Opus 4.5 and later.\n\n### Configuration\n\n```bash\n# Default: deferred on demand (leave ENABLE_TOOL_SEARCH unset)\n\n# Threshold mode: load upfront if they fit within 10% of context window,\n# defer only the overflow\nENABLE_TOOL_SEARCH=auto claude\n\n# Custom threshold (5%)\nENABLE_TOOL_SEARCH=auto:5 claude\n\n# Force full deferral even on Vertex AI or custom base URLs\nENABLE_TOOL_SEARCH=true claude\n\n# Disable entirely: all tool definitions load upfront\nENABLE_TOOL_SEARCH=false claude\n```\n\nTool Search is disabled by default on Vertex AI and when `ANTHROPIC_BASE_URL` points to a non-first-party host (most proxies do not forward `tool_reference` blocks). Set `ENABLE_TOOL_SEARCH=true` explicitly to override — but be aware this will cause requests to fail on Vertex AI models earlier than Sonnet 4.5/Opus 4.5, or on proxies that do not support `tool_reference` blocks.\n\nTo disable ToolSearch as a specific tool (without disabling the deferral mechanism entirely):\n\n```json\n{\n  \"permissions\": {\n    \"deny\": [\"ToolSearch\"]\n  }\n}\n```\n\n### Exempt specific servers from deferral\n\nIf a server's tools must be available without a search step, set `alwaysLoad: true` in that server's `.mcp.json` entry (v2.1.121+). This is an exception, not a default — each upfront definition consumes context. MCP servers can also mark individual tools as always-loaded by including `\"anthropic/alwaysLoad\": true` in the tool's `_meta` object in the `tools/list` response.\n\n### Writing good server instructions\n\nWith Tool Search enabled, server instructions (the server-level description field in the MCP spec) become the primary signal Claude uses to decide when to search your server's tools. Claude Code truncates both tool descriptions and server instructions at 2KB each. Front-load the critical information:\n\n```\nGood: \"GitHub tools: create/read/update issues and PRs, list repositories,\nreview code. Use when working with GitHub issues, pull requests, or code reviews.\"\n\nPoor: \"This server provides integration with the GitHub platform including\nvarious operations related to repository management and collaboration\nworkflows for software development teams.\"\n```\n\n### Output size limits\n\nMCP tool output that exceeds 10,000 tokens triggers a warning. The default maximum is 25,000 tokens, overridable with `MAX_MCP_OUTPUT_TOKENS`. This limit applies to tools that do not declare their own limit and to tools that return image data.\n\nServer authors can raise the threshold for specific text-returning tools via the `tools/list` response:\n\n```json\n{\n  \"name\": \"get_schema\",\n  \"_meta\": {\n    \"anthropic/maxResultSizeChars\": 200000\n  }\n}\n```\n\nThe hard ceiling on `maxResultSizeChars` is 500,000 characters. This annotation has no effect on image-returning tools — for those, raising `MAX_MCP_OUTPUT_TOKENS` is the only option. Results that exceed the active threshold are written to disk and replaced with a file reference in the conversation, rather than truncated."
   },
   {
    "heading": "Plugin-Bundled MCP Servers: Zero-Touch Distribution",
    "body": "When a server is bundled inside a plugin, it starts automatically whenever the plugin is enabled — no one runs `claude mcp add`. This is the closest Claude Code has to a zero-touch distribution mechanism for team MCP tooling.\n\n### Plugin structure\n\nA plugin's MCP servers are declared either in `.mcp.json` at the plugin root or inline in `plugin.json`:\n\n**`.mcp.json` at plugin root:**\n```json\n{\n  \"mcpServers\": {\n    \"database-tools\": {\n      \"command\": \"${CLAUDE_PLUGIN_ROOT}/servers/db-server\",\n      \"args\": [\"--config\", \"${CLAUDE_PLUGIN_ROOT}/config.json\"],\n      \"env\": {\n        \"DB_URL\": \"${DB_URL}\"\n      }\n    }\n  }\n}\n```\n\n**Inline in `plugin.json`:**\n```json\n{\n  \"name\": \"my-plugin\",\n  \"mcpServers\": {\n    \"plugin-api\": {\n      \"command\": \"${CLAUDE_PLUGIN_ROOT}/servers/api-server\",\n      \"args\": [\"--port\", \"8080\"]\n    }\n  }\n}\n```\n\n### Plugin-specific environment variables\n\n| Variable | Purpose |\n|----------|---------|\n| `${CLAUDE_PLUGIN_ROOT}` | Absolute path to the installed plugin directory. Use for bundled binaries and config files |\n| `${CLAUDE_PLUGIN_DATA}` | Persistent data directory that survives plugin updates |\n| `${CLAUDE_PROJECT_DIR}` | Current project root — substituted directly in plugin configs without needing a `:-` default (unlike project/user-scope `.mcp.json` files) |\n\n### Tool naming convention\n\nTools from plugin-bundled servers have a compound name:\n\n```\nmcp__plugin_<plugin-name>_<server-name>__<tool-name>\n```\n\nCharacters outside `[A-Za-z0-9_-]` are replaced with `_`. For a plugin named `my-plugin` with a server key `database-tools` exposing a `query` tool:\n\n```\nmcp__plugin_my-plugin_database-tools__query\n```\n\nUse this full name in permission rules, skill `allowed-tools` lists, and subagent `tools` fields.\n\n### Lifecycle\n\n- Plugin enabled → MCP servers connect at next session start\n- Plugin enabled mid-session → run `/reload-plugins` to connect servers immediately\n- Plugin disabled mid-session → run `/reload-plugins` to disconnect\n- `managed-mcp.json` deployed → plugin-provided servers are suppressed (exclusive control). This cannot be overridden for plugins. To allow claude.ai connectors alongside the managed set, use `allowAllClaudeAiMcps: true` in a managed settings source — but this affects only claude.ai connectors, not plugin-bundled servers.\n\nPlugin servers do **not** appear in `claude mcp list`. They appear only in `/mcp` within a session, marked with a plugin indicator.\n\n### When to bundle vs. separate\n\n| Approach | Use when |\n|----------|----------|\n| Plugin-bundled server | Server is tightly coupled to a skill or workflow; you want one install command to wire everything up |\n| Separate `project` scope `.mcp.json` | Server is useful independently; multiple plugins or projects might share it |\n| `managed-mcp.json` | Admin-controlled; servers the organization approves regardless of individual projects |"
   },
   {
    "heading": "Enterprise Managed MCP Governance",
    "body": "For organizations that need centralized control, Claude Code provides a managed configuration layer that overrides everything user-level. The mechanisms are `managed-mcp.json` (fixed server set) and `allowedMcpServers`/`deniedMcpServers` (filtering).\n\n### managed-mcp.json: exclusive control\n\nWhen this file is present at the system path, Claude Code loads **only** the servers it defines. Users cannot add, modify, or use any other MCP servers. Plugin-provided servers and claude.ai connectors are also suppressed (unless re-enabled — see below). The file uses the same format as `.mcp.json` and cannot be delivered through server-managed settings; it must be deployed by device management tooling (Jamf, GPO, Intune, fleet management).\n\n**System paths:**\n\n| Platform | Path |\n|----------|------|\n| macOS | `/Library/Application Support/ClaudeCode/managed-mcp.json` |\n| Linux / WSL | `/etc/claude-code/managed-mcp.json` |\n| Windows | `C:\\Program Files\\ClaudeCode\\managed-mcp.json` |\n\n```json\n{\n  \"mcpServers\": {\n    \"github\": {\n      \"type\": \"http\",\n      \"url\": \"https://api.githubcopilot.com/mcp/\"\n    },\n    \"sentry\": {\n      \"type\": \"http\",\n      \"url\": \"https://mcp.sentry.dev/mcp\"\n    },\n    \"company-internal\": {\n      \"type\": \"stdio\",\n      \"command\": \"/usr/local/bin/company-mcp-server\",\n      \"args\": [\"--config\", \"/etc/company/mcp-config.json\"],\n      \"env\": {\n        \"COMPANY_API_URL\": \"https://internal.example.com\"\n      }\n    }\n  }\n}\n```\n\n**Do not put credentials in `env` blocks here.** Any user on the machine can read this file. Use `${VAR}` expansion (reads from each user's environment), OAuth, or `headersHelper`.\n\n**To disable MCP entirely:** deploy a `managed-mcp.json` with an empty server map:\n```json\n{ \"mcpServers\": {} }\n```\n\n**Validate deployment:**\n```bash\n# Shows only managed servers if file is in effect\nclaude mcp list\n\n# Fails with exact error if policy is active:\n# \"Cannot add MCP server: enterprise MCP configuration is active\n#  and has exclusive control over MCP servers\"\nclaude mcp add --transport http test https://example.com/mcp\n```\n\nUsers whose previously configured servers are now blocked see those servers silently disappear from `/mcp` with no explanation that policy is the reason. Tell affected users before deploying.\n\n### Allow/deny lists: policy-based filtering\n\nAllowlists and denylists filter which servers load; they do not deploy servers. Key settings:\n\n- `allowedMcpServers`: only matching servers load. Unset = no restriction. Empty array `[]` = nothing loads.\n- `deniedMcpServers`: matching servers are blocked. Merges from all settings sources (users can always add to the denylist for themselves).\n- `allowManagedMcpServersOnly: true`: makes the managed allowlist authoritative; allowlists from user, project, and local settings are ignored. Must come from a managed settings source — no effect in user or project settings.\n\nNote that `allowedMcpServers` and `deniedMcpServers` also apply to servers in `managed-mcp.json` itself — a managed server that fails the checks won't load.\n\n### Identity-based vs. name-based enforcement (critical security distinction)\n\nThe `serverName` key is **not a security control**. A user can name any server anything. To enforce which servers actually run, use `serverUrl` (HTTP/SSE) or `serverCommand` (stdio). The matching rules differ between the two lists:\n\n- In `deniedMcpServers`, `serverName` accepts any non-empty string and is useful for blocking claude.ai connectors by their display name.\n- In `allowedMcpServers`, `serverName` is limited to letters, numbers, hyphens, and underscores. For allowlisting connectors, use `serverUrl`.\n\nOnce any `serverUrl` entry exists in the allowlist, every remote server must match a URL pattern — name-only matches no longer apply for remote servers. Same for `serverCommand` and stdio servers.\n\n```json\n{\n  \"allowedMcpServers\": [\n    { \"serverUrl\": \"https://api.githubcopilot.com/*\" },\n    { \"serverUrl\": \"https://mcp.sentry.dev/*\" },\n    { \"serverCommand\": [\"npx\", \"-y\", \"@modelcontextprotocol/server-filesystem\", \".\"] }\n  ],\n  \"deniedMcpServers\": [\n    { \"serverUrl\": \"https://*.untrusted.example.com/*\" }\n  ]\n}\n```\n\nURL patterns support `*` wildcards anywhere, including the scheme. Hostname matching is case-insensitive. Command matching is exact: every argument must match in order — `[\"npx\", \"-y\", \"server\"]` does not match `[\"npx\", \"server\"]` or `[\"npx\", \"-y\", \"server\", \"--flag\"]`.\n\n### Governance pattern comparison\n\n| Need | Pattern | Config |\n|------|---------|--------|\n| Completely lock down MCP | Disable all | `managed-mcp.json` with `mcpServers: {}` |\n| Give everyone the same approved set | Fixed deployment | `managed-mcp.json` with servers defined |\n| Publish approved list, users choose | Approved catalog | `allowedMcpServers` + `allowManagedMcpServersOnly: true` |\n| Block specific servers | Denylist only | `deniedMcpServers` (merges everywhere) |\n| Allow plugin servers only | Plugin-only | `strictPluginOnlyCustomization` with `mcp` in the list |\n| Allow claude.ai connectors alongside managed set | Connectors + managed | `allowAllClaudeAiMcps: true` in managed settings |\n| No admin control | Default | Don't deploy any managed config |\n\n### How restrictions appear to users\n\n| Restriction | What the user sees |\n|-------------|--------------------|\n| `managed-mcp.json` present and user runs `claude mcp add` | `Cannot add MCP server: enterprise MCP configuration is active and has exclusive control over MCP servers` |\n| Server on a denylist, user runs `claude mcp add` | `Cannot add MCP server \"<name>\": server is explicitly blocked by enterprise policy` |\n| Server not on the allowlist, user runs `claude mcp add` | `Cannot add MCP server \"<name>\": not allowed by enterprise policy` |\n| Previously configured server is now blocked by policy | Server silently disappears from `/mcp` and `claude mcp list` with no warning |\n\n### Monitoring usage\n\nWith OpenTelemetry export configured, set `OTEL_LOG_TOOL_DETAILS=1` to include MCP server and tool names in tool events. Aggregate in your collector to see which servers your users actually invoke — essential for deciding which servers belong in a fixed deployment versus an approved catalog."
   },
   {
    "heading": "claude.ai Connectors: The Fifth Source",
    "body": "If a developer is logged in with a Claude.ai account (Team or Enterprise plan), any MCP servers configured in the claude.ai web interface at `claude.ai/customize/connectors` automatically appear in Claude Code. These are the lowest-precedence source — a locally configured server pointing at the same URL takes precedence and the connector is suppressed (the `/mcp` panel shows it as hidden and offers to remove the duplicate).\n\nConnectors only load when the active authentication method is the Claude.ai subscription. If `ANTHROPIC_API_KEY`, `ANTHROPIC_AUTH_TOKEN`, `apiKeyHelper`, or a third-party provider (Bedrock, Vertex) is active, connectors do not load regardless of `/login` state. `/status` shows which auth method is active. To confirm: unset those environment variables (or remove `apiKeyHelper`), then run `/login` to select your Claude.ai account.\n\nSome Anthropic-hosted connectors (Microsoft 365, Gmail, Google Calendar) do not support local OAuth from Claude Code — the upstream identity provider only accepts the redirect URI registered by claude.ai. From v2.1.162, attempting to authenticate these from `/mcp` shows a message directing you to `Settings → Connectors` on claude.ai instead. Once connected there, the connector appears in Claude Code automatically.\n\n### Disabling connectors\n\n```json\n{ \"disableClaudeAiConnectors\": true }\n```\n\nThis uses any-source-true semantics: `true` in any settings source takes effect. A project-level `false` cannot re-enable connectors that a user- or policy-level `true` has disabled. Servers passed explicitly via `--mcp-config` are unaffected.\n\nFor a session-only disable:\n```bash\nENABLE_CLAUDEAI_MCP_SERVERS=false claude\n```\n\nTo block individual connectors rather than all of them, add them to `deniedMcpServers` by name (e.g., `{ \"serverName\": \"claude.ai Slack\" }`) or by URL pattern.\n\nWhen `managed-mcp.json` is deployed, claude.ai connectors are suppressed by default. To allow them alongside the managed set, set `\"allowAllClaudeAiMcps\": true` in a managed settings source (server-managed settings, MDM-deployed plist/registry, or system `managed-settings.json`). Placing this setting in user or project settings has no effect — it is admin-only. Requires Claude Code v2.1.149 or later.\n\nFrom v2.1.161, connectors you have never authenticated appear collapsed in `/mcp` under a \"Show unused connectors\" row, so an organization-provisioned list does not fill the panel. A connector you signed in to before remains visible even when it currently needs re-authentication."
   },
   {
    "heading": "Common Pitfalls and Failure Modes",
    "body": "### Secrets in committed config\n**Problem:** Developer runs `claude mcp add --env STRIPE_KEY=sk_live_... --scope project ...` and Claude Code writes the literal key into `.mcp.json`.\n**Fix:** Never pass real credentials via `--env` on project-scope adds. Use `${STRIPE_KEY}` in `.mcp.json` and require developers to set the variable in their shell profile. The `${VAR}` syntax causes a parse failure if unset — use `${VAR:-}` (empty default) only if an unset key is acceptable for the server.\n\n### Scope confusion: \"override\" that actually adds\n**Problem:** Developer adds a local override with a different name (`github-local` vs. project-scope `github`). Both servers load; Claude now has two GitHub tools and may choose the wrong one.\n**Fix:** Use identical names between local overrides and project definitions. Deduplication is by name, not by URL.\n\n### headersHelper in project scope without trust acceptance\n**Problem:** `headersHelper` is committed to `.mcp.json`. New developer clones the repo and the helper script does not run — authentication fails silently.\n**Explanation:** `headersHelper` at project or local scope only executes after the workspace trust dialog is accepted. This is by design (arbitrary shell execution). Ensure developers accept the prompt, or route the server through `managed-mcp.json` where trust is admin-granted.\n\n### `serverName` in allowlist treated as a security control\n**Problem:** Admin sets `allowedMcpServers: [{ \"serverName\": \"github\" }]` believing this restricts users to the official GitHub MCP server. A user runs `claude mcp add --transport stdio github -- python /home/user/evil.py` and it loads.\n**Fix:** Use `serverUrl` for HTTP servers and `serverCommand` for stdio servers in allowlists whenever security is the goal. `serverName` is a convenience label, not a binding identity. Once any `serverCommand` entry exists in the allowlist, every stdio server must match a listed command — name-only matches no longer apply.\n\n### Tool Search disabled on proxied deployments\n**Problem:** Team uses a corporate proxy (`ANTHROPIC_BASE_URL=https://proxy.corp.com`). Tool Search is silently disabled; all tool definitions load upfront and consume large amounts of context.\n**Explanation:** Claude Code disables Tool Search by default when `ANTHROPIC_BASE_URL` points to a non-first-party host. Set `ENABLE_TOOL_SEARCH=true` explicitly only if your proxy forwards `tool_reference` blocks — otherwise requests will fail.\n\n### `managed-mcp.json` suppresses plugin servers with no warning\n**Problem:** Team deploys `managed-mcp.json`. A plugin that previously provided an MCP server stops working. Developers see no error — the server simply is not in `/mcp`.\n**Explanation:** `managed-mcp.json` takes exclusive control. Plugin-provided servers are suppressed with no user-visible message. Add the plugin server's definition directly to `managed-mcp.json` to restore it. Unlike claude.ai connectors, plugin suppression cannot be re-enabled with any flag.\n\n### Missing `--` separator on stdio adds\n**Problem:** `claude mcp add --transport stdio myserver python server.py --port 8080` causes Claude Code to try to parse `--port` as its own flag and fail.\n**Fix:** Always use `--` before the server command: `claude mcp add --transport stdio myserver -- python server.py --port 8080`.\n\n### Scope terminology confusion from old docs\n**Problem:** Developer reads an older guide that says `--scope project` means private-to-you. In current Claude Code, `--scope project` means shared via `.mcp.json` in the repo.\n**Explanation:** Scope names changed. The old `project` scope (private, current project) is now `local`. The old `global` scope (private, all projects) is now `user`. The old names may still appear in some docs or scripts — always verify with `claude mcp get <name>` which scope holds a server.\n\n### `managed-mcp.json` silently removes previously working servers\n**Problem:** After rolling out `managed-mcp.json`, developers report their personal servers have vanished with no error message.\n**Explanation:** `managed-mcp.json` takes exclusive control with no warning to the user that policy is the reason. Communicate the change before deploying, and document which servers are in the managed set."
   },
   {
    "heading": "Operational Reference: Commands and File Locations",
    "body": "### CLI commands\n\n```bash\n# Add servers\nclaude mcp add --transport http <name> <url>                    # HTTP, local scope\nclaude mcp add --transport http <name> --scope project <url>    # project scope\nclaude mcp add --transport http <name> --scope user <url>       # user scope\nclaude mcp add --transport stdio <name> -- <command> [args...]  # stdio (note: --)\nclaude mcp add-json <name> '<json>'                             # from raw JSON\nclaude mcp add-from-claude-desktop                              # import from Claude Desktop (macOS/WSL only)\n\n# Inspect\nclaude mcp list                  # all servers with status (pending/rejected/connected/failed)\nclaude mcp get <name>            # details for one server, including scope\n\n# Auth\nclaude mcp login <name>          # OAuth flow from CLI (v2.1.186+)\nclaude mcp login <name> --no-browser  # for SSH sessions (requires ssh -t)\nclaude mcp logout <name>         # clear stored credentials\n\n# Remove and reset\nclaude mcp remove <name>                 # remove server (local scope by default)\nclaude mcp remove <name> --scope local   # explicit scope if server exists in multiple\nclaude mcp reset-project-choices         # reset approval decisions for current project\n\n# Run Claude as an MCP server\nclaude mcp serve                         # start Claude as a stdio MCP server\n\n# In-session\n/mcp                             # panel: status, tool counts, OAuth flows, reconnect\n/reload-plugins                  # reconnect/disconnect plugin MCP servers mid-session\n```\n\n### Connection status codes (`claude mcp list` / `claude mcp get`)\n\n| Status | Meaning |\n|--------|---------|\n| `✓ Connected` | Ready to use |\n| `! Connected · tools fetch failed` | Connected but `tools/list` failed; run `claude mcp get <name>` for detail |\n| `! Needs authentication` | Reachable but requires OAuth or a token |\n| `✗ Failed to connect` | Server didn't respond |\n| `✗ Connection error` | Connection attempt threw an error |\n| `⏸ Pending approval` | Project-scoped server not yet approved |\n\n### File locations\n\n```\n~/.claude.json                              # user-scope and local-scope MCP entries\n.mcp.json                                  # project-scope MCP (commit this)\n.claude/settings.json                      # project settings (MCP approval preferences)\n.claude/settings.local.json                # local settings overrides (gitignore this)\n~/.claude/settings.json                    # user settings\n\n# Managed (admin-deployed)\n/Library/Application Support/ClaudeCode/managed-mcp.json   # macOS\n/etc/claude-code/managed-mcp.json                          # Linux/WSL\nC:\\Program Files\\ClaudeCode\\managed-mcp.json               # Windows\n/etc/claude-code/managed-settings.json                     # Linux/WSL managed settings\n/etc/claude-code/managed-settings.d/*.json                 # Linux/WSL drop-in configs\n```\n\nOn Windows, `~/.claude.json` resolves to `%USERPROFILE%\\.claude.json`. If `CLAUDE_CONFIG_DIR` is set, Claude Code reads `.claude.json` from inside that directory.\n\n### Environment variables\n\n| Variable | Effect |\n|----------|--------|\n| `MCP_TIMEOUT` | Server connect timeout in ms (e.g., `MCP_TIMEOUT=10000`). Default is 30 seconds. |\n| `MCP_TOOL_TIMEOUT` | Default per-tool execution timeout. Default is approximately 28 hours. |\n| `MAX_MCP_OUTPUT_TOKENS` | Max tokens per tool result (default 25,000). Does not override `anthropic/maxResultSizeChars` for text-returning tools. |\n| `ENABLE_TOOL_SEARCH` | `true` / `false` / `auto` / `auto:N` (percentage threshold) |\n| `ENABLE_CLAUDEAI_MCP_SERVERS` | Set to `false` to disable claude.ai connectors for the session |\n| `OTEL_LOG_TOOL_DETAILS` | Set to `1` to include MCP server/tool names in telemetry events |\n| `MCP_CLIENT_SECRET` | Supply OAuth client secret non-interactively (CI use) |"
   }
  ],
  "preQuiz": [
   {
    "prompt": "Your team commits a `.mcp.json` with a Slack MCP server and pushes it. A new teammate clones the repo and runs `claude`. What happens when they try to use the Slack server?",
    "options": [
     "The server connects automatically — project-scoped servers start without any prompt.",
     "They see an approval prompt before Claude connects to the project-scoped server for the first time.",
     "The server is ignored because `.mcp.json` is only read on the machine that created it.",
     "They must run `claude mcp add --scope project slack ...` themselves before it appears."
    ],
    "correct": 1,
    "sectionIndices": [
     1
    ],
    "explanation": "Project-scoped servers in `.mcp.json` are shared via VCS, but Claude Code shows an approval prompt before first connect — it does not silently connect servers committed by others."
   },
   {
    "prompt": "You add a server with `claude mcp add --scope user my-docs-server -- npx my-docs`. Later you want it to be project-scoped instead. What is the correct procedure?",
    "options": [
     "Edit `~/.claude.json` directly and move the entry to `.mcp.json`.",
     "Run `claude mcp add --scope project my-docs-server -- npx my-docs` — the new scope overwrites the old one.",
     "Run `claude mcp remove my-docs-server --scope user`, then re-add with `--scope project`.",
     "Run `claude mcp add --scope project my-docs-server` — omitting the command copies it from the existing entry."
    ],
    "correct": 2,
    "sectionIndices": [
     2
    ],
    "explanation": "Scope is fixed at add time. To change it you must remove the entry at its current scope (`--scope user`) and then re-add it with the desired scope (`--scope project`)."
   },
   {
    "prompt": "Your `.mcp.json` defines a server named `search` pointing to an internal URL. A teammate also has a local entry named `search` pointing to their personal search server. Which server runs when they use Claude Code?",
    "options": [
     "The project `.mcp.json` entry — project scope overrides local scope.",
     "The local entry in `~/.claude.json` — local scope has higher precedence than project scope.",
     "Both run simultaneously and Claude picks the one that responds first.",
     "Claude merges both entries, taking the URL from local and the headers from project."
    ],
    "correct": 1,
    "sectionIndices": [
     3
    ],
    "explanation": "The precedence order is local > project > user. When the same server name exists in multiple scopes, only the highest-precedence entry is used in its entirety — no field merging occurs."
   },
   {
    "prompt": "You run `claude mcp add --scope project db-tool --env DB_PORT=5432 db-mcp -- node server.js --port 5432`. The server fails to connect. Checking `claude mcp get db-tool` you see the launch command looks wrong. What is the likely cause?",
    "options": [
     "The `--env` flag cannot be used with `--scope project`; env vars must go in `.mcp.json`.",
     "The `--` separator was placed before `--env`, causing Claude to parse `DB_PORT=5432` as a server arg, not an env var.",
     "Without `--` before the server command (`node server.js`), Claude parses `--port 5432` as one of its own options instead of passing it to the server.",
     "The `--scope` flag must come after the server name, not before it."
    ],
    "correct": 2,
    "sectionIndices": [
     4
    ],
    "explanation": "The `--` separator is required for stdio servers: everything after it is passed untouched to the launch command. Without it, Claude parses the server's own flags (like `--port`) as Claude Code options, corrupting the launch command."
   },
   {
    "prompt": "You add an HTTP MCP server with `claude mcp add --transport http my-api https://mcp.example.com`. `claude mcp list` shows it as `✓ Connected`. You edit `.mcp.json` to add an env var, then immediately use the server. The edit has no effect. Why?",
    "options": [
     "HTTP servers ignore `.mcp.json`; their config must be set with `--env` at add time.",
     "Claude Code reads `.mcp.json` only at session start, so mid-session edits have no effect until you restart.",
     "Env vars are not supported for HTTP transport; they only apply to stdio servers.",
     "You must run `claude mcp remove` and re-add the server for any config change to take effect."
    ],
    "correct": 1,
    "sectionIndices": [
     1,
     5
    ],
    "explanation": "Claude Code reads `.mcp.json` only at session start. Changes to the file (even saved to disk) are not picked up mid-session — you must restart Claude Code for the edits to take effect."
   },
   {
    "prompt": "Your team's `.mcp.json` contains `\"apiKey\": \"${INTERNAL_API_KEY}\"` in the server's `env` block. A security reviewer asks whether the literal token ends up in git history. What is the accurate answer?",
    "options": [
     "Yes — Claude Code expands the variable at session start, but the unexpanded `${INTERNAL_API_KEY}` string is what gets committed to VCS.",
     "No — Claude Code substitutes env vars at runtime; only the `${INTERNAL_API_KEY}` placeholder is stored in `.mcp.json` and committed.",
     "It depends on transport: HTTP servers expand vars into the URL before writing, but stdio servers keep the placeholder.",
     "Env var expansion only works in `headers`, not in the `env` block, so the literal value would be committed."
    ],
    "correct": 1,
    "sectionIndices": [
     1
    ],
    "explanation": "Env var expansion (`${VAR}` and `${VAR:-default}`) happens at runtime, not at write time. Only the placeholder string is stored in `.mcp.json` and committed, keeping secrets out of VCS."
   },
   {
    "prompt": "An HTTP MCP server returns a 401 on first connection. What does Claude Code do, and what can the team admin do to authenticate it non-interactively in CI?",
    "options": [
     "Claude retries the connection 3 times then marks it failed; for CI, set the token in `ANTHROPIC_API_KEY`.",
     "Claude marks the server as needing authentication; for CI use a static token via `--header \"Authorization: Bearer <token>\"` at add time.",
     "Claude falls back to OAuth automatically; for CI set `MCP_CLIENT_SECRET` in the environment.",
     "Claude disables the server permanently until `claude mcp reset-project-choices` is run; re-add with `--no-auth` for CI."
    ],
    "correct": 1,
    "sectionIndices": [
     6
    ],
    "explanation": "Claude Code marks a server as needing authentication on 401 or 403. For CI or non-interactive flows, a static Bearer token via `--header` is the correct approach. If `headers.Authorization` is set but rejected, Claude reports a failed connection rather than falling back to OAuth."
   },
   {
    "prompt": "You have a server whose OAuth scopes were discovered automatically, but your security team wants to restrict it to only `read:issues`. You add `\"oauth\": { \"scopes\": \"read:issues\" }` to the server's `.mcp.json` entry. Later a tool call fails with 403 `insufficient_scope`. What should you do?",
    "options": [
     "Remove the `oauth.scopes` field to let Claude Code re-discover all scopes automatically.",
     "Add `\"oauth\": { \"authServerMetadataUrl\": \"https://...\" }` — it overrides `scopes` and grants the needed access.",
     "Widen `oauth.scopes` to include the additional scope the failing tool requires (e.g. `\"read:issues write:comments\"`).",
     "Run `claude mcp logout <name>` and re-authenticate — the `insufficient_scope` error means the token is expired."
    ],
    "correct": 2,
    "sectionIndices": [
     6
    ],
    "explanation": "`oauth.scopes` pins requested scopes and overrides discovered scopes. A 403 `insufficient_scope` means the pinned scope set is too narrow for that tool call — widen it to include the required scope."
   },
   {
    "prompt": "Your team installs 12 MCP servers. A developer complains that Claude Code's context window is being consumed by all those tool definitions at startup. You check and `ENABLE_TOOL_SEARCH` is not set. What is actually happening?",
    "options": [
     "All 12 servers' tool definitions are loaded upfront — unset `ENABLE_TOOL_SEARCH` means the feature is disabled.",
     "Tool Search is enabled by default: only tool names and server instructions load at session start; full definitions are deferred and fetched on demand.",
     "All tool definitions load upfront only for stdio servers; HTTP servers always defer.",
     "The context budget consumed equals 12 × 2KB (the per-tool truncation limit), so it is bounded but not zero."
    ],
    "correct": 1,
    "sectionIndices": [
     7
    ],
    "explanation": "Tool Search is enabled by default (when `ENABLE_TOOL_SEARCH` is unset). Tool definitions are deferred — only names and server instructions load at session start. Adding more servers under default deferral has minimal context impact."
   },
   {
    "prompt": "You have one MCP tool that must be available on every turn (e.g. a memory-lookup tool). Tool Search is enabled globally. How do you ensure this tool's definition is always loaded upfront without disabling Tool Search for all servers?",
    "options": [
     "Set `ENABLE_TOOL_SEARCH=false` in the server's env block — it applies per-server.",
     "Add `\"alwaysLoad\": true` to that server's entry in `.mcp.json` (v2.1.121+) — it loads all that server's tools upfront regardless of `ENABLE_TOOL_SEARCH`.",
     "Use `--transport sse` for that server — SSE transport always loads tools upfront.",
     "Add the tool name to `permissions.allow` — allowed tools are pre-loaded."
    ],
    "correct": 1,
    "sectionIndices": [
     7
    ],
    "explanation": "`alwaysLoad: true` on a server config (v2.1.121+) loads all that server's tools upfront regardless of the global `ENABLE_TOOL_SEARCH` setting. This lets you keep deferral for other servers while guaranteeing availability for critical tools."
   },
   {
    "prompt": "A stdio MCP server crashes mid-session. The developer waits for it to reconnect automatically but it never does. Why?",
    "options": [
     "Stdio servers auto-reconnect up to 5 attempts, but only if `MCP_TIMEOUT` is set.",
     "Stdio servers do not auto-reconnect. The developer must manually reconnect via the `/mcp` panel.",
     "Stdio auto-reconnect requires `\"reconnect\": true` in `.mcp.json`; it is not on by default.",
     "Auto-reconnect is only for auth failures; crashes require a new session."
    ],
    "correct": 1,
    "sectionIndices": [
     8
    ],
    "explanation": "HTTP/SSE servers auto-reconnect mid-session with exponential backoff (up to 5 attempts). Stdio servers do NOT auto-reconnect — a crashed stdio server requires manual reconnection via the `/mcp` panel or restarting the session."
   },
   {
    "prompt": "A team is using Claude Code with an `ANTHROPIC_API_KEY` set. The IT admin added a connector at `claude.ai/customize/connectors` for the team. Developers report the connector never appears in Claude Code sessions. What is the cause?",
    "options": [
     "claude.ai connectors only appear on macOS; the team is on Linux.",
     "claude.ai connectors load only when active auth is the Claude.ai subscription. With `ANTHROPIC_API_KEY` active, they silently do not load.",
     "The connector must also be added locally with `claude mcp add --scope user` for it to appear in the CLI.",
     "Enterprise connectors require `ENABLE_CLAUDEAI_MCP_SERVERS=true` to be set explicitly."
    ],
    "correct": 1,
    "sectionIndices": [
     10
    ],
    "explanation": "claude.ai connectors silently do not load when `ANTHROPIC_API_KEY`, `ANTHROPIC_AUTH_TOKEN`, `apiKeyHelper`, Bedrock, or Vertex is active as the auth source — even after `/login`. They only appear when the active auth is the Claude.ai subscription. Run `/status` to check."
   },
   {
    "prompt": "Your `headersHelper` script in `.mcp.json` fetches a short-lived Kerberos token and writes it to stdout. The server intermittently fails to connect because the script takes 15 seconds on a cold start. What should you change?",
    "options": [
     "Switch to OAuth — `headersHelper` has a hard 10-second timeout that cannot be changed, so long-running scripts must use OAuth instead.",
     "Set `MCP_TIMEOUT` to a higher value — this extends the `headersHelper` script's execution budget.",
     "Cache the token to disk inside the script and return early if it is still valid — `headersHelper` runs fresh each connect with no caching.",
     "Use `--header` with a static token instead — `headersHelper` is only for HTTP transport and the server may be stdio."
    ],
    "correct": 0,
    "sectionIndices": [
     6
    ],
    "explanation": "`headersHelper` has a hard 10-second timeout; scripts exceeding it cause connection failure. If your token-generation script reliably takes longer than 10 seconds, `headersHelper` is the wrong tool — use OAuth or pre-generate and cache the token in the environment before starting Claude Code."
   }
  ],
  "tasks": [
   {
    "id": "stage-5-task-add-project-server",
    "afterSectionIdx": 1,
    "title": "Add a project-scoped MCP server and inspect .mcp.json",
    "instructions": "This task gets you comfortable with the project-scope workflow — the recommended approach for team consistency.\n\n1. In a scratch git repo (or any existing project), add the public filesystem MCP server at project scope:\n```bash\ncd ~/projects/my-scratch-repo   # or any project dir\nclaude mcp add --scope project filesystem -- npx -y @modelcontextprotocol/server-filesystem /tmp\n```\n\n2. Inspect the generated file:\n```bash\ncat .mcp.json\n```\nYou should see a `mcpServers` block with your server's `type`, `command`, and `args`.\n\n3. Verify Claude Code recognizes it:\n```bash\nclaude mcp list\n```\nYour `filesystem` entry should appear. Note the status — it may show `⏸ Pending approval` until you accept the workspace-trust dialog.\n\n4. Clean up:\n```bash\nclaude mcp remove filesystem --scope project\n```",
    "doneWhen": "`cat .mcp.json` shows the `filesystem` server entry and `claude mcp list` shows it as present (any status)."
   },
   {
    "id": "stage-5-task-env-var-secret",
    "afterSectionIdx": 5,
    "title": "Add a server with a secret via env var expansion",
    "instructions": "Practice keeping secrets out of `.mcp.json` using `${VAR}` expansion.\n\n1. Export a fake token in your shell:\n```bash\nexport MY_MCP_TOKEN=fake-token-abc123\n```\n\n2. Add an HTTP MCP server that references it:\n```bash\nclaude mcp add --scope project --transport http secret-demo https://httpbin.org/anything --header \"Authorization: Bearer ${MY_MCP_TOKEN}\"\n```\n\n3. Open `.mcp.json` and confirm the file contains the literal string `${MY_MCP_TOKEN}` (the placeholder), NOT the expanded token value `fake-token-abc123`:\n```bash\ngrep -n 'MY_MCP_TOKEN\\|fake-token' .mcp.json\n```\nYou should see `MY_MCP_TOKEN` and NOT `fake-token-abc123`.\n\n4. Clean up:\n```bash\nclaude mcp remove secret-demo --scope project\n```",
    "doneWhen": "`grep 'MY_MCP_TOKEN' .mcp.json` returns a match and `grep 'fake-token' .mcp.json` returns no match, confirming only the placeholder is stored."
   },
   {
    "id": "stage-5-task-tool-search-alwaysload",
    "afterSectionIdx": 7,
    "title": "Observe Tool Search deferral and force-load a specific server",
    "instructions": "Confirm the default deferral behavior and practice overriding it for a critical server.\n\n1. Add a project-scoped server (use the filesystem server from the earlier task):\n```bash\nclaude mcp add --scope project filesystem -- npx -y @modelcontextprotocol/server-filesystem /tmp\n```\n\n2. Open `.mcp.json` in a text editor and add `\"alwaysLoad\": true` to the server entry so it looks like:\n```json\n{\n  \"mcpServers\": {\n    \"filesystem\": {\n      \"type\": \"stdio\",\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"@modelcontextprotocol/server-filesystem\", \"/tmp\"],\n      \"alwaysLoad\": true\n    }\n  }\n}\n```\n\n3. Start a new Claude Code session and immediately (before any prompt) run:\n```\n/mcp\n```\nVerify the `filesystem` server shows as connected. With `alwaysLoad: true` it connects at startup rather than waiting for a tool search.\n\n4. In the same session, also run:\n```\n/status\n```\nNote the active auth type — this matters for whether claude.ai connectors load (they don't with API key auth).\n\n5. Clean up when done:\n```bash\nclaude mcp remove filesystem --scope project\n```",
    "doneWhen": "`.mcp.json` contains `\"alwaysLoad\": true` and after starting a new Claude Code session `/mcp` shows the filesystem server as connected at startup."
   }
  ],
  "visualizations": [
   {
    "id": "stage-5-v",
    "kind": "comparison-table",
    "title": "MCP (connecting tools)",
    "textualSummary": "Key concepts of MCP (connecting tools): MCP server, MCP scope (local / project / user), Configuration-as-code distribution.",
    "columns": [
     "Concept",
     "In this stage"
    ],
    "rows": [
     {
      "label": "MCP server",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "MCP scope (local / project / user)",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "Configuration-as-code distribution",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "Scope precedence (non-merging)",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     }
    ]
   }
  ],
  "confusions": [
   {
    "misconception": "Skipping MCP (connecting tools).",
    "correction": "Each stage is load-bearing for the setup."
   },
   {
    "misconception": "These concepts are interchangeable.",
    "correction": "Each has a distinct role; see the definitions."
   }
  ],
  "quiz": [
   {
    "id": "stage-5-q1",
    "type": "multiple-choice",
    "prompt": "A teammate runs `claude mcp add --scope project github -- npx -y @github/mcp-server` and gets the 'Added' confirmation message. She then tries to use a GitHub tool immediately and nothing happens. What is the most likely explanation?",
    "options": [
     "The `--scope project` flag requires admin approval before the server activates.",
     "'Added' only means the entry was saved; the server may not have connected yet — she should verify with `claude mcp list`.",
     "The `--` separator was used incorrectly and the server was added with a bad command.",
     "Project-scoped servers require a session restart before they appear in `claude mcp list`."
    ],
    "correct": 1,
    "explanation": "The content states: 'The `Added` confirmation only means the entry was saved, not that the server starts/connects — always verify with `claude mcp list`.' Option A is wrong because there is no admin-approval gate on individual `claude mcp add` commands (approval prompts appear on clone/first connect for project-scoped servers). Option C is wrong because `--` is the correct separator for stdio servers. Option D is wrong because `claude mcp list` shows server status within the current session."
   },
   {
    "id": "stage-5-q2",
    "type": "multiple-choice",
    "prompt": "Your team has a shared internal MCP server that requires Kerberos tickets, which are short-lived and environment-specific. You need every developer who opens the project to have auth headers automatically generated at connection time. Which mechanism is designed for this case?",
    "options": [
     "Store the Kerberos token in `.mcp.json` using `${MY_TOKEN}` env var expansion.",
     "Use `--header \"Authorization: Bearer <token>\"` in the `claude mcp add` command.",
     "Configure `headersHelper` in `.mcp.json` with a shell command that generates the auth headers at connection time.",
     "Use `oauth.scopes` pinning in `.mcp.json` to restrict to a Kerberos-compatible scope."
    ],
    "correct": 2,
    "explanation": "`headersHelper` is explicitly described as the solution for non-OAuth auth (Kerberos, short-lived tokens, internal SSO): it 'runs a shell command at connection time to generate auth headers.' Option A fails because env var expansion embeds a static value — it cannot generate time-limited tokens dynamically. Option B sets a static token baked in at `mcp add` time, not regenerated per-connection. Option D is wrong because `oauth.scopes` is for restricting OAuth scope sets, not for Kerberos auth at all."
   },
   {
    "id": "stage-5-q3",
    "type": "multiple-choice",
    "prompt": "A developer adds a project-scoped MCP server to `.mcp.json` and also has a user-scoped server with the same name in `~/.claude.json`. When Claude Code starts, which server entry is used?",
    "options": [
     "Both entries are merged — fields from user scope fill in any gaps the project entry leaves blank.",
     "The user-scoped entry, because user settings are more persistent than project settings.",
     "The project-scoped entry, because project scope has higher precedence than user scope.",
     "Whichever entry was added most recently wins."
    ],
    "correct": 2,
    "explanation": "The precedence table shows local (rank 1) > project (rank 2) > user (rank 3). The content explicitly states: 'When a server is defined in multiple places, only the entire entry from the highest-precedence source is used — fields are never merged.' So the project entry (rank 2) beats the user entry (rank 3). Option A is wrong because merging never happens. Option B inverts the precedence order. Option D is wrong — recency has no role in precedence."
   },
   {
    "id": "stage-5-q4",
    "type": "multiple-choice",
    "prompt": "You run `claude mcp add --transport ws my-realtime-server wss://example.com/mcp` and get an error saying `ws` is not a valid transport. What is the correct way to add a WebSocket MCP server?",
    "options": [
     "Use `--transport websocket` instead of `--transport ws`.",
     "Add it via `.mcp.json` or `claude mcp add-json` with `type: \"ws\"`; `--transport` does not accept `ws`.",
     "WebSocket servers are not supported; use SSE instead.",
     "Use `--transport sse` since SSE and WebSocket are interchangeable in the CLI."
    ],
    "correct": 1,
    "explanation": "The content states: 'WebSocket: add only via `.mcp.json` or `add-json` with `type:\"ws\"`; `--transport` does **not** accept `ws`.' Option A is wrong — neither `ws` nor `websocket` works as a `--transport` flag value. Option C is wrong because WebSocket is supported, just not via `--transport`. Option D is wrong and doubly so because SSE is deprecated."
   },
   {
    "id": "stage-5-q5",
    "type": "multiple-choice",
    "prompt": "A teammate edited `.mcp.json` to add a new server while their Claude Code session was already running. They notice the new server does not appear in `/mcp`. What should they do?",
    "options": [
     "Run `claude mcp reset-project-choices` to reload all project-scoped servers.",
     "Restart the Claude Code session — it reads config files only at session start.",
     "Run `/reload-plugins` to pick up changes to `.mcp.json`.",
     "Run `claude mcp get <name>` to trigger a re-read of the config file."
    ],
    "correct": 1,
    "explanation": "The content states: 'Claude Code reads it **only at session start** — restart after editing.' Option A (`reset-project-choices`) resets approval decisions for already-known project servers, it does not reload the config. Option C (`/reload-plugins`) connects/disconnects plugin-bundled servers, not changes to `.mcp.json`. Option D (`mcp get`) only reads from already-loaded state, not from disk."
   },
   {
    "id": "stage-5-q6",
    "type": "multiple-choice",
    "prompt": "Your team connects a Claude.ai connector via the admin dashboard. A developer whose Claude Code uses `ANTHROPIC_API_KEY` for auth runs `/status` and notices the connector is absent. Why?",
    "options": [
     "The developer needs to run `claude mcp login` to authenticate the connector manually.",
     "Claude.ai connectors only load when active auth is the Claude.ai subscription — they silently do not load when `ANTHROPIC_API_KEY` is active.",
     "The connector is pending approval at project scope and shows `⏸ Pending approval` in `claude mcp list`.",
     "The developer must run `/reload-plugins` to pull down connectors added after their session started."
    ],
    "correct": 1,
    "explanation": "The content states: 'They silently do **not** load when `ANTHROPIC_API_KEY`, `ANTHROPIC_AUTH_TOKEN`, `apiKeyHelper`, Bedrock, or Vertex is active (even after `/login`).' This is a key gotcha: API key auth and Claude.ai subscription auth are mutually exclusive for connector loading. Option A is wrong because `mcp login` is for OAuth on regular MCP servers, not for claude.ai connectors. Option C is wrong — the approval flow is for project-scoped `.mcp.json` servers, not connectors. Option D is wrong because `/reload-plugins` is for plugin-bundled servers."
   },
   {
    "id": "stage-5-q7",
    "type": "multiple-choice",
    "prompt": "You run `claude mcp add --env MY_SECRET=abc my-server -- python server.py` but the server immediately fails with an env parsing error. A colleague suggests the `--env` flag may have consumed the server name. What went wrong and how do you fix it?",
    "options": [
     "The server name `my-server` must come before `--env`; reorder to `claude mcp add my-server --env MY_SECRET=abc -- python server.py`.",
     "The `--env` flag does not support `=`; use `--env MY_SECRET abc` instead.",
     "The `--env` flag requires the secret to be in single quotes: `--env 'MY_SECRET=abc'`.",
     "The `--` separator must come immediately after `claude mcp add` before any flags."
    ],
    "correct": 0,
    "explanation": "The content warns: 'Don't put the server name directly after `--env` — it's read as another `KEY=value` and rejected; place another option between them.' In the broken command, `--env MY_SECRET=abc my-server` causes `my-server` to be parsed as another env var attempt. The fix is to put the server name before `--env`. Option B is wrong — `KEY=value` is the correct form. Option C is wrong — quoting doesn't fix the positional parsing problem. Option D is wrong — `--` must come after Claude's own options, not at the start."
   },
   {
    "id": "stage-5-q8",
    "type": "multi-select",
    "prompt": "Your security team requires that all MCP servers the team uses are pre-approved and that no developer should be able to casually connect arbitrary third-party servers. Which of the following are valid mechanisms the content describes for achieving this? (Select all that apply.)",
    "options": [
     "Distribute approved servers via a managed plugin marketplace so everyone gets the same tools automatically when the plugin is enabled.",
     "Share an approved catalog as an internal wiki listing exact `claude mcp add` commands.",
     "Set `ANTHROPIC_API_KEY` in the project environment, which blocks unapproved server connections.",
     "Commit `.mcp.json` to version control so teammates get the same servers with an approval prompt before first connect.",
     "Enable `disableClaudeAiConnectors: true` in project settings to block all claude.ai connectors for the repo."
    ],
    "correct": [
     0,
     1,
     3,
     4
    ],
    "explanation": "Options A and D are both described as team distribution mechanisms: plugins give 'zero-touch consistency' and `.mcp.json` in VCS gives 'everyone who clones gets the same servers (with an approval prompt before first connect).' Option B is also described: 'Since there is no built-in MCP registry, share an approved catalog as a plugin marketplace or an internal wiki listing exact `claude mcp add` commands.' Option E is valid for blocking claude.ai connectors: 'Opt a repo out by committing `disableClaudeAiConnectors:true` in project settings.' Option C is wrong — `ANTHROPIC_API_KEY` controls auth mode, not server connection permissions; the content does not describe it as a security gate for MCP servers."
   },
   {
    "id": "stage-5-q9",
    "type": "multi-select",
    "prompt": "A slow stdio MCP server is timing out at startup. Which of the following are correct statements about MCP timeout behavior based on the content? (Select all that apply.)",
    "options": [
     "The default startup timeout is 30 seconds; override with the `MCP_TIMEOUT` environment variable (value in milliseconds).",
     "Per-server tool-execution timeout can be set via a `timeout` field in the server's `.mcp.json` entry.",
     "HTTP and SSE servers auto-reconnect with exponential backoff on mid-session disconnection; stdio servers do NOT auto-reconnect.",
     "Values below 1000 for the per-server `timeout` field are honored and override `MCP_TOOL_TIMEOUT`.",
     "Slow stdio startup is best fixed by switching the server to HTTP transport, which has no startup timeout."
    ],
    "correct": [
     0,
     1,
     2
    ],
    "explanation": "Options A, B, and C are all directly stated. A: 'Startup timeout: default 30s; override with `MCP_TIMEOUT` (ms).' B: '`timeout` field (ms) in the server's `.mcp.json` entry overrides `MCP_TOOL_TIMEOUT`.' C: 'HTTP/SSE auto-reconnect mid-session... Stdio servers do NOT auto-reconnect.' Option D is wrong: 'values < 1000 are ignored (fall through to `MCP_TOOL_TIMEOUT`, default ~28h).' Option E is wrong — the content recommends bumping `MCP_TIMEOUT` for slow stdio servers, not switching transports, and does not claim HTTP has no startup timeout."
   },
   {
    "id": "stage-5-q10",
    "type": "multiple-choice",
    "prompt": "Tool Search is enabled by default. A developer adds 15 new MCP servers and worries the context window will be overwhelmed. Under default Tool Search behavior, what actually happens?",
    "options": [
     "All tools from all 15 servers load upfront, consuming context proportional to the number of tools.",
     "Tool definitions are deferred — only tool names and server instructions load at session start — so adding servers has minimal context impact.",
     "Claude Code automatically removes the oldest servers to stay within the context budget.",
     "Tool Search caps the number of servers at 10; the 11th through 15th servers are ignored."
    ],
    "correct": 1,
    "explanation": "The content states: 'Tool Search is **enabled by default**: tool definitions are deferred (only tool **names + server instructions** load at session start) and discovered on demand... Adding more servers under default deferral has minimal context impact.' Option A describes behavior only when `ENABLE_TOOL_SEARCH=false`. Option C is invented — there is no automatic server eviction. Option D is invented — there is no server count cap."
   },
   {
    "id": "stage-5-q11",
    "type": "multiple-choice",
    "prompt": "A team wants a specific MCP server to always have its tools available immediately, even on the first tool call of a session, without waiting for Tool Search to discover them. What is the correct configuration?",
    "options": [
     "Set `ENABLE_TOOL_SEARCH=false` in the project environment to load all server tools upfront.",
     "Set `alwaysLoad: true` on the server's config entry in `.mcp.json`.",
     "Use `--transport sse` for the server, since SSE servers load tools upfront.",
     "Add the server with `--scope user` so it loads before project-scoped servers."
    ],
    "correct": 1,
    "explanation": "The content states: '`alwaysLoad: true` on a server config (v2.1.121+, all server types) loads all its tools upfront regardless of `ENABLE_TOOL_SEARCH`. Reserve it for tools needed every turn.' Option A would load ALL servers upfront, defeating deferred loading for everything else — `alwaysLoad` is the targeted, per-server solution. Option C is wrong — transport type does not affect whether tools are deferred. Option D is wrong — scope affects sharing, not tool loading behavior."
   },
   {
    "id": "stage-5-q12",
    "type": "multiple-choice",
    "prompt": "An MCP server with a static `Authorization: Bearer <token>` header in its config returns a 401. Claude Code marks it as having a failed connection rather than prompting for OAuth login. Why?",
    "options": [
     "Static token auth requires the `--no-browser` flag to avoid an OAuth fallback loop.",
     "If `headers.Authorization` is set but rejected, Claude reports a failed connection rather than falling back to OAuth — the token must be valid or remove the header.",
     "A 401 on a token-auth server triggers the `/mcp` panel to prompt for re-authentication via OAuth.",
     "The server must be re-added with `--transport sse` to enable the 401→OAuth fallback path."
    ],
    "correct": 1,
    "explanation": "The content explicitly states: 'If `headers.Authorization` is set but rejected, Claude reports a **failed connection** rather than falling back to OAuth — the token must be valid or remove the header.' The presence of a static auth header tells Claude Code the server does not use OAuth, so a 401 is treated as a configuration failure, not an authentication challenge to resolve via OAuth. Option A inverts the logic (`--no-browser` is for SSH SSH scenarios). Option C contradicts the content. Option D is wrong — transport type does not determine the 401 handling."
   },
   {
    "id": "stage-5-q13",
    "type": "multiple-choice",
    "prompt": "A developer wants a personal docs-search MCP server available in every project they work on, without adding it to any project's `.mcp.json`. Which scope should they use?",
    "options": [
     "`--scope local`, because local scope persists across sessions for the current machine.",
     "`--scope project`, because project-scoped servers are shared via VCS automatically.",
     "`--scope user`, because user scope makes the server available across all projects.",
     "`--scope global` (the new alias for the cross-project scope introduced in recent versions)."
    ],
    "correct": 2,
    "explanation": "The content states: 'Use `--scope user` for personal servers wanted in every project (e.g. a docs-search server).' User scope writes to `~/.claude.json` under top-level `mcpServers`, making it available 'in all your projects.' Option A is wrong — local scope is private to the current project only. Option B is wrong — project scope is tied to one project's `.mcp.json` and shared via VCS (not personal). Option D is wrong — the content notes that older versions called user scope 'global' but the current flag is `--scope user`; `global` is not described as a current valid alias."
   },
   {
    "id": "stage-5-q14",
    "type": "multiple-choice",
    "prompt": "Your organization's security team sets `disableClaudeAiConnectors: true` in a managed policy (user-level settings). A developer then sets `disableClaudeAiConnectors: false` in their project settings to re-enable connectors for their repo. What happens?",
    "options": [
     "The project `false` overrides the user policy, and connectors load for that project.",
     "The most recently written setting wins, so the project `false` takes effect.",
     "A `true` anywhere wins — the project `false` cannot re-enable what user/policy `true` has disabled.",
     "The behavior is undefined; the two conflicting settings cause an error at session start."
    ],
    "correct": 2,
    "explanation": "The content states: 'any-source-true: a `true` anywhere wins; a project `false` cannot re-enable a user/policy `true`.' This is a one-way ratchet: once any scope sets `true`, it cannot be overridden downward by lower-precedence scopes. Option A inverts the rule. Option B inverts the rule and incorrectly applies a recency model. Option D is wrong — the content describes well-defined behavior, not an error."
   }
  ],
  "masteryCheckpoint": "You can explain the concepts of MCP (connecting tools)."
 },
 {
  "id": "stage-6",
  "stage": 6,
  "title": "Plugins & distribution",
  "summary": "Plugins & distribution: plugin, marketplace, settings scope.",
  "prerequisites": [
   "stage-5"
  ],
  "objectives": [
   "Understand the concepts in Plugins & distribution."
  ],
  "definitions": [
   {
    "term": "plugin",
    "short": "A self-contained directory that bundles shareable Claude Code components (skills, agents, hooks, MCP/LSP servers, monitors, bin executables) under one namespace."
   },
   {
    "term": "marketplace",
    "short": "A catalog of plugins, defined by a marketplace.json, that you first add (register the catalog) and then install individual plugins from."
   },
   {
    "term": "settings scope",
    "short": "The level (user, project, local, or managed) that determines which settings file a plugin's enabled state is written to and therefore who receives it."
   },
   {
    "term": "project-scope distribution via VCS",
    "short": "Sharing plugins with a whole team by committing the marketplace registration and enabled-plugins state into the repo's checked-in project settings."
   },
   {
    "term": "managed (admin) settings",
    "short": "Admin-installed, read-only settings that enforce plugin and marketplace configuration which users cannot modify or remove."
   },
   {
    "term": "plugin authoring layout",
    "short": "The required directory structure where only plugin.json lives in `.claude-plugin/` and all component dirs sit at the plugin root, with the manifest name acting as the component namespace."
   }
  ],
  "sections": [
   {
    "heading": "What a Plugin Actually Is",
    "body": "A **plugin** is a self-contained directory that bundles shareable Claude Code components under one namespace. That directory can contain skills, agents, hooks, MCP servers, LSP servers, background monitors, themes, output styles, and executables — anything you might otherwise scatter across a project's `.claude/` directory, now packaged for repeatable distribution.\n\nThe single mandatory structural rule: only `plugin.json` lives inside `.claude-plugin/`. Every other component directory (`skills/`, `agents/`, `hooks/`, `.mcp.json`, `bin/`, etc.) sits at the **plugin root**, not inside `.claude-plugin/`. Violating this is the most common first mistake — the plugin loads but all components silently disappear. Diagnose with `claude --debug`.\n\nThe manifest's `name` field becomes the **namespace** for every component. A skill named `code-review` inside a plugin named `acme-tools` becomes `/acme-tools:code-review`. This prevents collisions when multiple plugins ship overlapping skill names.\n\n```\nacme-tools/\n├── .claude-plugin/\n│   └── plugin.json          ← ONLY file here\n├── skills/\n│   ├── code-review/\n│   │   └── SKILL.md\n│   └── scan-deps/\n│       ├── SKILL.md\n│       └── scripts/scan.sh\n├── agents/\n│   └── security-reviewer.md\n├── hooks/\n│   └── hooks.json\n├── .mcp.json\n├── .lsp.json\n├── monitors/\n│   └── monitors.json\n├── output-styles/\n│   └── terse.md\n├── themes/\n│   └── acme-dark.json\n├── bin/\n│   └── acme-lint            ← added to Bash tool's PATH when enabled\n└── settings.json\n```\n\nA `CLAUDE.md` at the plugin root is **not** loaded as project context — plugins contribute instructions through skills and agents, not CLAUDE.md.\n\n### Manifest Schema\n\n```json\n// acme-tools/.claude-plugin/plugin.json\n{\n  \"name\": \"acme-tools\",\n  \"displayName\": \"Acme Developer Tools\",\n  \"version\": \"1.4.0\",\n  \"description\": \"CI/CD, security scanning, and review automation for Acme monorepo\",\n  \"author\": { \"name\": \"Platform Eng\", \"email\": \"platform@acme.com\" },\n  \"homepage\": \"https://docs.acme.com/claude-plugins\",\n  \"repository\": \"https://github.com/acme-corp/claude-plugins\",\n  \"license\": \"MIT\",\n  \"keywords\": [\"ci\", \"security\", \"review\"],\n  \"defaultEnabled\": false\n}\n```\n\nOnly `name` is required when you include a manifest. The manifest is optional entirely — without it, Claude Code auto-discovers components in default locations and derives the name from the directory name. Include one whenever you need metadata, custom component paths, or version pinning.\n\n### Unrecognized Fields\n\nClaude Code ignores top-level fields it does not recognize, so one `plugin.json` can double as a VS Code extension manifest, an npm `package.json`, or a DXT bundle manifest. `claude plugin validate` reports unrecognized fields as **warnings**, not errors — a plugin with only unrecognized-field warnings still passes validation and loads. Fields with the wrong **type** (e.g. `keywords` as a string instead of an array) are load errors. Pass `--strict` to treat warnings as errors in CI."
   },
   {
    "heading": "Plugin Authoring Layout: The Rules That Bite",
    "body": "The directory contract has a few non-obvious rules worth internalizing before you write a line of content.\n\n### Skills vs Commands\n\nPlugins support two skill formats:\n\n- `skills/<name>/SKILL.md` — the canonical format for new plugins. Supports supporting files alongside the SKILL.md (reference docs, scripts, etc.).\n- `commands/<name>.md` — flat markdown files, the older format. Still works but prefer `skills/` for new work.\n\nA plugin with a single `SKILL.md` directly at its root (no `skills/` directory, no `skills` manifest field) is auto-loaded as a single-skill plugin in Claude Code v2.1.142 and later. The skill's invocation name comes from the frontmatter `name` field, or the directory basename as a fallback — but the basename for a marketplace-installed plugin is a versioned cache path that changes on every update, so always set `name` in frontmatter for root-level skills.\n\n### Path Behavior Rules\n\nWhen you use custom paths in `plugin.json`, the semantics differ by field:\n\n| Field | Behavior when manifest key is set |\n|-------|-----------------------------------|\n| `commands`, `agents`, `outputStyles`, `experimental.themes`, `experimental.monitors` | **Replaces** the default directory scan |\n| `skills` | **Adds to** the default `skills/` scan (both locations searched) |\n| `hooks`, `mcpServers`, `lspServers` | Own merge rules — multiple sources combined |\n\nTo keep the default and add more for a replacing field, list it explicitly:\n```json\n\"commands\": [\"./commands/\", \"./extras/cmd.md\"]\n```\n\nAll manifest paths must be **relative to the plugin root** and **start with `./`**. Paths with `../` are blocked — plugins cannot reference files outside their directory after installation because the cache copy only contains the plugin's own tree. (The one exception: symlinks to sibling plugins within the same marketplace are dereferenced and copied in.)\n\n**Conflict detection**: when a plugin has both a default folder and the matching manifest key pointing elsewhere, Claude Code v2.1.140 and later flags the ignored folder in `/doctor` and `claude plugin list`. The plugin still loads using the manifest paths.\n\n### The bin/ Directory\n\nExecutables under `bin/` are added to the Bash tool's `PATH` while the plugin is enabled. This lets hook scripts and agent instructions invoke them as bare commands without absolute paths. Scripts must be executable (`chmod +x`). Scripts that need to reference bundled assets should use `${CLAUDE_PLUGIN_ROOT}`, not hardcoded paths, because the plugin cache location changes with every version.\n\n### settings.json in the Plugin\n\nA plugin can include a `settings.json` at its root to apply defaults when enabled. Currently only `agent` and `subagentStatusLine` keys are supported — `agent` activates one of the plugin's agents as the main thread for all sessions while the plugin is enabled:\n\n```json\n// acme-tools/settings.json\n{\n  \"agent\": \"security-reviewer\"\n}\n```\n\nSettings in the plugin's `settings.json` take priority over the same keys declared inline in `plugin.json`. **Unknown keys are silently ignored** — there is no error if you put other settings here, they simply have no effect."
   },
   {
    "heading": "Marketplaces: Two-Step Distribution",
    "body": "A **marketplace** is a catalog defined by a `marketplace.json` file that lives at `.claude-plugin/marketplace.json` in a git repository. The distribution model is two-step: users first *add* a marketplace (making its catalog locally known), then *install* individual plugins from it.\n\n```\nyour-org/claude-plugins (GitHub repo)\n├── .claude-plugin/\n│   └── marketplace.json        ← the catalog\n└── plugins/\n    ├── acme-tools/             ← plugin directory\n    │   ├── .claude-plugin/\n    │   │   └── plugin.json\n    │   └── skills/...\n    └── deploy-helper/\n        └── ...\n```\n\n### marketplace.json Structure\n\n```json\n{\n  \"name\": \"acme-plugins\",\n  \"owner\": { \"name\": \"Platform Engineering\", \"email\": \"platform@acme.com\" },\n  \"description\": \"Internal Claude Code plugins for the Acme monorepo\",\n  \"metadata\": {\n    \"pluginRoot\": \"./plugins\"\n  },\n  \"plugins\": [\n    {\n      \"name\": \"acme-tools\",\n      \"source\": \"./acme-tools\",\n      \"description\": \"CI/CD, security scanning, and review automation\",\n      \"version\": \"1.4.0\",\n      \"defaultEnabled\": false\n    },\n    {\n      \"name\": \"deploy-helper\",\n      \"source\": {\n        \"source\": \"github\",\n        \"repo\": \"acme-corp/deploy-helper-plugin\",\n        \"ref\": \"stable\",\n        \"sha\": \"a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0\"\n      },\n      \"description\": \"Deployment automation\"\n    }\n  ]\n}\n```\n\nThe `metadata.pluginRoot` shorthand lets you write `\"source\": \"acme-tools\"` instead of `\"source\": \"./plugins/acme-tools\"` for every entry. Note that `description` and `version` are also accepted under `metadata` for backward compatibility.\n\n### Plugin Sources\n\n| Source type | `source` value | Notes |\n|-------------|---------------|-------|\n| Relative path | `\"./plugins/my-plugin\"` (string) | Local to the marketplace repo. Only works when marketplace is git-hosted. Must start with `./`. Resolves relative to marketplace root (the dir containing `.claude-plugin/`), not relative to `marketplace.json` itself |\n| GitHub repo | `{\"source\": \"github\", \"repo\": \"owner/repo\", \"ref\": \"...\", \"sha\": \"...\"}` | Preferred for cross-repo plugins |\n| Any git URL | `{\"source\": \"url\", \"url\": \"https://...\", \"ref\": \"...\", \"sha\": \"...\"}` | GitLab, Bitbucket, self-hosted. `.git` suffix is optional |\n| Git subdirectory | `{\"source\": \"git-subdir\", \"url\": \"...\", \"path\": \"tools/plugin\", \"ref\": \"...\", \"sha\": \"...\"}` | Sparse clone — efficient for monorepos. `url` also accepts `owner/repo` GitHub shorthand or SSH URLs |\n| npm package | `{\"source\": \"npm\", \"package\": \"@acme/plugin\", \"version\": \"^2.0.0\", \"registry\": \"...\"}` | Private registry supported |\n\nThe critical distinction: **marketplace source** (where `marketplace.json` is fetched from) vs **plugin source** (where each plugin's files are fetched from). They point to independent repositories and are pinned independently. The marketplace `add` command supports `ref` (for branch/tag) but not `sha`; individual plugin sources support both `ref` and `sha`.\n\n### CLI Commands\n\n```bash\n# Add marketplace from GitHub (registers catalog locally)\nclaude plugin marketplace add acme-corp/claude-plugins\n\n# Add at project scope (writes to .claude/settings.json — team-shared)\nclaude plugin marketplace add acme-corp/claude-plugins --scope project\n\n# Add from other git hosts (include .git suffix for git clone behavior)\nclaude plugin marketplace add https://gitlab.com/company/plugins.git\n\n# Install a plugin from a registered marketplace\nclaude plugin install acme-tools@acme-plugins\n\n# Install at project scope\nclaude plugin install acme-tools@acme-plugins --scope project\n\n# List registered marketplaces\nclaude plugin marketplace list\n\n# Refresh marketplace catalog\nclaude plugin marketplace update acme-plugins\n\n# Remove marketplace (also uninstalls plugins from it)\nclaude plugin marketplace remove acme-plugins\n```\n\nWithin an interactive session, all these have `/plugin` equivalents (`/plugin marketplace add`, `/plugin install`, etc.). `/plugin market` is accepted as a shorthand for `/plugin marketplace`, and `rm` is accepted instead of `remove`."
   },
   {
    "heading": "Settings Scope: What Gets Written Where",
    "body": "Every plugin install or enable operation writes to exactly one settings file, determined by the `--scope` flag. Understanding this determines who receives the plugin.\n\n| Scope | Settings file written | Gitignored? | Who it affects |\n|-------|----------------------|-------------|----------------|\n| `user` (default) | `~/.claude/settings.json` | N/A (home dir) | You alone, across all projects |\n| `project` | `.claude/settings.json` | No (checked in) | Everyone who clones the repo |\n| `local` | `.claude/settings.local.json` | Yes (typically) | You alone, in this project only |\n| `managed` | Platform-specific (see below) | N/A | Read-only; applied by admin |\n\nManaged settings are delivered via server-managed settings (Anthropic Console), MDM/OS-level policies (macOS: `com.anthropic.claudecode` plist; Windows: registry at `HKLM\\SOFTWARE\\Policies\\ClaudeCode`), or file-based at `/Library/Application Support/ClaudeCode/` (macOS) or `/etc/claude-code/` (Linux/WSL). They also support a `managed-settings.d/` drop-in directory where multiple JSON files are merged alphabetically.\n\nThe `enabledPlugins` object in each settings file controls which plugins are active at that scope. An entry of `true` enables; `false` disables (even if a higher-priority scope says `true`).\n\n```json\n// .claude/settings.json (project scope, checked into VCS)\n{\n  \"extraKnownMarketplaces\": {\n    \"acme-plugins\": {\n      \"source\": {\n        \"source\": \"github\",\n        \"repo\": \"acme-corp/claude-plugins\"\n      }\n    }\n  },\n  \"enabledPlugins\": {\n    \"acme-tools@acme-plugins\": true,\n    \"deploy-helper@acme-plugins\": true\n  }\n}\n```\n\nWhen a developer clones this repo and trusts the workspace, they are prompted to install missing plugins. Accepting installs and enables the listed plugins without further action.\n\n### Scope Priority and Override\n\nMultiple scopes can make conflicting declarations. Resolution order (highest wins):\n1. Managed settings (admin-enforced, read-only)\n2. Command-line arguments (temporary, session-only)\n3. Local settings\n4. Project settings\n5. User settings\n\nBecause `local` beats `project`, a developer can locally disable a team-enabled plugin:\n```bash\nclaude plugin disable acme-tools@acme-plugins --scope local\n```\nThis writes `\"acme-tools@acme-plugins\": false` to `.claude/settings.local.json` without touching the shared project settings.\n\n### The `defaultEnabled` Field\n\n`defaultEnabled` (requires Claude Code v2.1.154 or later) ships a plugin disabled when set to `false` in `plugin.json` or the marketplace entry (the marketplace entry takes precedence over `plugin.json`). The user explicitly enables it. This is the right pattern for plugins with non-trivial token cost or external service dependencies.\n\nTwo things override `defaultEnabled`:\n- **The user's explicit setting**: once a user writes any enabled/disabled entry, `defaultEnabled` no longer applies — even across plugin updates and reinstalls.\n- **A dependency requirement**: when a plugin is required by another active plugin, Claude Code writes an explicit `true` at install time, overriding the default."
   },
   {
    "heading": "Project-Scope Distribution via VCS",
    "body": "Committing marketplace registration and plugin enabled-state to `.claude/settings.json` is the mechanism for distributing plugins to an entire team without requiring each member to run manual install steps.\n\n### The Two-Entry Pattern\n\nThe project settings file needs two entries working together:\n\n1. `extraKnownMarketplaces` — registers the marketplace so Claude Code can resolve plugin names from it\n2. `enabledPlugins` — declares which plugins should be active\n\n```json\n// .claude/settings.json\n{\n  \"extraKnownMarketplaces\": {\n    \"acme-plugins\": {\n      \"source\": {\n        \"source\": \"github\",\n        \"repo\": \"acme-corp/claude-plugins\"\n      }\n    }\n  },\n  \"enabledPlugins\": {\n    \"acme-tools@acme-plugins\": true\n  }\n}\n```\n\nA developer who clones the repo and trusts the workspace is prompted to install missing plugins. Accepting installs and enables `acme-tools` without further action.\n\nTo add both entries in one step from the CLI (run from inside the repo):\n\n```bash\nclaude plugin marketplace add acme-corp/claude-plugins --scope project\nclaude plugin install acme-tools@acme-plugins --scope project\n```\n\nCommit the resulting `.claude/settings.json` changes.\n\n### Skills-Directory Alternative\n\nFor plugins you want to develop and distribute entirely within a single repo without a separate marketplace repository, use the **skills-directory plugin** pattern: place the plugin under `.claude/skills/<name>/` with its `.claude-plugin/plugin.json` manifest.\n\n```\nyour-repo/\n└── .claude/\n    └── skills/\n        └── acme-tools/          ← plugin lives here, loads as acme-tools@skills-dir\n            ├── .claude-plugin/\n            │   └── plugin.json\n            └── skills/\n                └── code-review/\n                    └── SKILL.md\n```\n\nThis loads automatically as `acme-tools@skills-dir` after the workspace trust gate, with no marketplace and no install step. Code-executing components have additional restrictions for project-scope skills-directory plugins:\n- MCP servers go through per-server approval\n- LSP servers start only after workspace trust\n- Background monitors do **not** load\n\nPersonal-scope plugins under `~/.claude/skills/` have none of these restrictions.\n\n**Critical caveat**: project-scope skills-directory plugins only load from `.claude/skills/` relative to the directory where you launch Claude Code. They do not walk up to the repository root (unlike plain skills and commands which do walk up). Always launch from the root, or run `/reload-plugins` after changing directories within a session.\n\n### Pre-Seeding for CI and Containers\n\nFor CI environments where network calls at startup are unacceptable, pre-populate a plugin cache at image build time by setting `CLAUDE_CODE_PLUGIN_CACHE_DIR` to your target seed path:\n\n```bash\n# During image build — installs directly into the seed directory\nCLAUDE_CODE_PLUGIN_CACHE_DIR=/opt/claude-seed \\\n  claude plugin marketplace add acme-corp/claude-plugins\nCLAUDE_CODE_PLUGIN_CACHE_DIR=/opt/claude-seed \\\n  claude plugin install acme-tools@acme-plugins\n```\n\nThen set `CLAUDE_CODE_PLUGIN_SEED_DIR=/opt/claude-seed` in the container's runtime environment. At startup, Claude Code reads from the seed without cloning. You can layer multiple seed directories by separating paths with `:` (Unix) or `;` (Windows).\n\nSeed directories are **read-only** at runtime — auto-updates are blocked and `/plugin marketplace update` against a seed-managed marketplace fails with guidance to update the seed image. Use `/plugin disable` to opt out of a seed plugin rather than removing the marketplace."
   },
   {
    "heading": "Managed (Admin) Settings and Enforcement",
    "body": "The `managed` scope is admin-installed and **read-only**. Managed settings override all user and project settings and cannot be modified or removed by the user. This is the correct mechanism for enforcing which plugins and marketplaces are permitted across an organization.\n\n### What Managed Settings Control\n\nAdmins can enforce:\n- Which marketplaces users are permitted to add (`strictKnownMarketplaces`)\n- Which plugins are automatically enabled for all users (`enabledPlugins` at managed scope)\n- Which marketplaces are auto-registered for all users (`extraKnownMarketplaces` at managed scope)\n- Blocklisting specific marketplace sources (`blockedMarketplaces`)\n- Which marketplace plugins can be surfaced as install suggestions (`pluginSuggestionMarketplaces`)\n\n### strictKnownMarketplaces\n\nThis setting in managed settings locks which marketplace sources users can register:\n\n```json\n// managed-settings.json\n{\n  \"strictKnownMarketplaces\": [\n    {\n      \"source\": \"github\",\n      \"repo\": \"acme-corp/approved-plugins\"\n    },\n    {\n      \"source\": \"hostPattern\",\n      \"hostPattern\": \"^github\\.acme\\.internal$\"\n    },\n    {\n      \"source\": \"pathPattern\",\n      \"pathPattern\": \"^/opt/approved/\"\n    }\n  ]\n}\n```\n\n| Value | Effect |\n|-------|--------|\n| Not set (default) | No restrictions — users add any marketplace |\n| Empty array `[]` | Complete lockdown — no new marketplaces |\n| Array of sources | Allowlist — only matching sources accepted |\n\nChecks run before any network or filesystem operation, and also on install, update, refresh, and auto-update. A marketplace added before the policy was configured and whose source no longer matches the allowlist will refuse installs and updates.\n\nFour match types are supported: `github` (exact `repo` match, `ref`/`path` must also match if specified), `url` (exact full URL), `hostPattern` (regex on hostname), and `pathPattern` (regex on filesystem path). Exact matching is strict about URL normalization: a trailing slash, `.git` suffix, or `ssh://` vs `https://` are treated as distinct. For internal servers where multiple URL forms are in use, `hostPattern` matching is safer than literal URL matching.\n\n### Pairing strictKnownMarketplaces with extraKnownMarketplaces\n\nThe restriction setting blocks additions but does not register anything. Pair it with `extraKnownMarketplaces` in the same managed settings file to both restrict and auto-provision:\n\n```json\n// managed-settings.json\n{\n  \"strictKnownMarketplaces\": [\n    { \"source\": \"github\", \"repo\": \"acme-corp/approved-plugins\" }\n  ],\n  \"extraKnownMarketplaces\": {\n    \"acme-plugins\": {\n      \"source\": {\n        \"source\": \"github\",\n        \"repo\": \"acme-corp/approved-plugins\"\n      }\n    }\n  },\n  \"enabledPlugins\": {\n    \"security-scanner@acme-plugins\": true\n  }\n}\n```\n\nThis results in: all developers get the `security-scanner` plugin enabled by default, cannot add any other marketplace, and cannot disable or remove managed-scope plugins.\n\n### Release Channels via Managed Settings\n\nTo give different teams different plugin versions, set up two marketplace repos pointing to different git refs (`stable` vs `latest`) and assign each via managed settings to the appropriate group:\n\n```json\n// managed-settings-stable-team.json\n{\n  \"extraKnownMarketplaces\": {\n    \"stable-tools\": {\n      \"source\": { \"source\": \"github\", \"repo\": \"acme-corp/tools-stable\" }\n    }\n  },\n  \"enabledPlugins\": { \"formatter@stable-tools\": true }\n}\n```\n\nBoth channels must resolve to **different version strings** or Claude Code treats them as identical and skips updates. If you use explicit `version` fields, each ref's `plugin.json` must declare a different version string. If you omit `version`, the distinct commit SHAs on each branch already distinguish them automatically.\n\n### Drop-in Directory for Per-Team Policies\n\nFor organizations deploying different policies to different user groups, the `managed-settings.d/` drop-in directory lets you split policies across files merged alphabetically:\n\n```\n/etc/claude-code/\n  managed-settings.json       ← base config\n  managed-settings.d/\n    10-telemetry.json\n    20-security.json           ← overrides base for matching keys\n    30-approved-plugins.json\n```\n\nArrays (like `strictKnownMarketplaces`) are concatenated and de-duplicated across files."
   },
   {
    "heading": "Version Pinning and Resolution",
    "body": "Version is the cache key Claude Code uses to determine whether an update is needed. Understanding the resolution chain is critical for teams that want predictable, reproducible behavior.\n\n### Resolution Order\n\nThe effective version is the first non-empty value from:\n\n1. `version` field in the plugin's `plugin.json`\n2. `version` field in the plugin's marketplace entry in `marketplace.json`\n3. The git commit SHA of the plugin source (for `github`, `url`, `git-subdir`, and relative paths in git-hosted marketplaces)\n4. `unknown` (for `npm` sources, or local directories outside a git repo)\n\nThe `plugin.json` value wins silently over the marketplace entry. Setting `version` in both creates a latent trap: the marketplace `version` is ignored, so bumping only the marketplace entry does nothing for existing users.\n\n### Two Versioning Strategies\n\n| Strategy | How | When `/plugin update` fires | Best for |\n|----------|-----|----------------------------|----------|\n| Explicit version | Set `\"version\": \"2.1.0\"` in `plugin.json` | Only when the version string changes | Published plugins, stable release cycles |\n| Commit-SHA version | Omit `version` from both files | On every new commit to the source | Internal / team plugins under active development |\n\nFor internally-developed plugins where the team wants to ship continuously, omitting `version` gives you automatic delivery: every merged commit is available to developers after the next `claude plugin marketplace update` or auto-update cycle.\n\n### SHA Pinning for Reproducibility\n\nFor plugins fetched from external repositories, pin both `ref` and `sha` to lock against accidental breakage when upstream force-pushes or deletes tags:\n\n```json\n{\n  \"name\": \"shared-linter\",\n  \"source\": {\n    \"source\": \"github\",\n    \"repo\": \"partner-org/claude-linter\",\n    \"ref\": \"v3.1.0\",\n    \"sha\": \"a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0\"\n  }\n}\n```\n\nWhen both are set, `sha` is the effective pin. The `ref` is retained for human readability and as a fallback hint. Note: on some servers (AWS CodeCommit), fetching by SHA requires `ref` to still exist and be reachable from the pinned commit. GitHub, GitLab, and Bitbucket all support fetching commits by SHA directly even if the ref has been deleted.\n\n### Tagging Releases\n\nFor plugins that other plugins declare as dependencies (using the `dependencies` field), Claude Code uses git tags in the format `{plugin-name}--v{version}` for semver resolution. Create these with:\n\n```bash\n# Run from inside the plugin directory\nclaude plugin tag\nclaude plugin tag --push     # Create and push to remote\nclaude plugin tag --dry-run  # Preview without creating\nclaude plugin tag --force    # Create even if working tree is dirty\n```\n\n### The Commit-SHA Gotcha\n\nBecause `version` in `plugin.json` takes precedence over the SHA, a team that sets a version string and then forgets to bump it on subsequent commits will find that `claude plugin update` reports \"already at the latest version\" even though newer code exists. The fix is either: always bump `version` on every merge to main (following semantic versioning: MAJOR for breaking changes, MINOR for features, PATCH for fixes), or remove `version` from `plugin.json` entirely and let the SHA drive updates."
   },
   {
    "heading": "Plugin Path Environment Variables",
    "body": "Plugins are installed into a versioned cache directory (`~/.claude/plugins/cache`), not used in-place. Hard-coding paths in hook commands or MCP server configs will break after installation because the absolute location changes with each version. Three substituted variables solve this correctly.\n\n### The Three Variables\n\n| Variable | Resolves to | Use for |\n|----------|-------------|----------|\n| `${CLAUDE_PLUGIN_ROOT}` | Absolute path to the plugin's current installation directory | Bundled scripts, config files, binaries — anything that ships with the plugin |\n| `${CLAUDE_PLUGIN_DATA}` | `~/.claude/plugins/data/{sanitized-plugin-id}/` (e.g. `formatter-my-marketplace/`) | Persistent state that must survive updates: `node_modules`, venvs, caches |\n| `${CLAUDE_PROJECT_DIR}` | The directory Claude Code was launched from | Project-local scripts or configs the plugin needs to interact with |\n\nAll three are substituted inline wherever they appear in: skill content, agent content, hook commands, monitor commands, MCP server configs, and LSP server configs. They are also exported as environment variables to hook processes and MCP/LSP server subprocesses. `${user_config.KEY}` substitutions and any `${ENV_VAR}` from the shell environment are also supported.\n\n### Usage in Hook Commands\n\nThe docs recommend **exec form** (using `args`) in hook commands so the path is passed as one argument with no quoting concerns. In shell-form hooks, wrap the variable in double quotes:\n\n```json\n// hooks/hooks.json\n{\n  \"hooks\": {\n    \"PostToolUse\": [\n      {\n        \"matcher\": \"Write|Edit\",\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"\\\"${CLAUDE_PLUGIN_ROOT}\\\"/scripts/format-code.sh\"\n          }\n        ]\n      }\n    ]\n  }\n}\n```\n\n### Usage in MCP Server Configs\n\n```json\n// .mcp.json\n{\n  \"mcpServers\": {\n    \"acme-db\": {\n      \"command\": \"node\",\n      \"args\": [\"${CLAUDE_PLUGIN_ROOT}/server.js\"],\n      \"env\": {\n        \"NODE_PATH\": \"${CLAUDE_PLUGIN_DATA}/node_modules\",\n        \"PROJECT_DIR\": \"${CLAUDE_PROJECT_DIR}\"\n      }\n    }\n  }\n}\n```\n\n### The Persistent Data Pattern\n\n`${CLAUDE_PLUGIN_DATA}` outlives any single plugin version. The data directory is identified by the sanitized plugin id: for `formatter@my-marketplace` it is `~/.claude/plugins/data/formatter-my-marketplace/`. A naive \"install if not present\" check misses the case where an update changes `package.json`. The recommended pattern compares manifests:\n\n```json\n{\n  \"hooks\": {\n    \"SessionStart\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"diff -q \\\"${CLAUDE_PLUGIN_ROOT}/package.json\\\" \\\"${CLAUDE_PLUGIN_DATA}/package.json\\\" >/dev/null 2>&1 || (cd \\\"${CLAUDE_PLUGIN_DATA}\\\" && cp \\\"${CLAUDE_PLUGIN_ROOT}/package.json\\\" . && npm install) || rm -f \\\"${CLAUDE_PLUGIN_DATA}/package.json\\\"\"\n          }\n        ]\n      }\n    ]\n  }\n}\n```\n\nIf `npm install` fails, the trailing `rm` removes the manifest copy so the next session retries. The data directory is automatically deleted when the plugin is uninstalled from its last remaining scope (the `/plugin` UI shows the directory size and prompts before deleting). Pass `--keep-data` to `claude plugin uninstall` to preserve it.\n\n### Mid-Session Update Behavior\n\nWhen a plugin updates during an active session, hook commands, monitors, MCP servers, and LSP servers keep using `${CLAUDE_PLUGIN_ROOT}` from the previous version. The previous version directory is retained on disk for about 7 days before cleanup. Run `/reload-plugins` to switch hooks, MCP servers, and LSP servers to the new path. Monitors require a full session restart."
   },
   {
    "heading": "Always-On Token Cost Budgeting",
    "body": "Every enabled plugin adds a standing context cost to every session, regardless of whether any plugin component is invoked. This is the always-on cost: the token overhead from the plugin's skill descriptions, agent descriptions, and command names being present in Claude's context at startup. It is distinct from the on-invoke cost of actually running a skill or expanding an agent's full prompt.\n\n### Inspecting Cost\n\n```bash\nclaude plugin details acme-tools\n```\n\nThe output groups components as Skills (including both `skills/` and `commands/` entries), Agents, Hooks, MCP servers, and LSP servers, then shows two cost figures per component:\n\n```\nacme-tools 1.4.0\n  CI/CD, security scanning, and review automation\n  Source: acme-tools@acme-plugins\n\nComponent inventory\n  Skills (3)  scan-deps, code-review, deploy-check\n  Agents (1)  security-reviewer\n  Hooks (2)  (harness-only — no model context cost)\n  MCP servers (1)\n  LSP servers (0)\n\nProjected token cost\n  Always-on:   ~420 tok   added to every session\n\nPer-component (rounded)\n  component            always-on  on-invoke\n  scan-deps                ~100      ~2400\n  code-review               ~80      ~3100\n  deploy-check              ~60      ~1800\n  security-reviewer        ~180      ~4200\n\n  On-invoke cost is paid each time a skill or agent fires.\n  Token counts are estimates and may differ from actual usage.\n```\n\nThe always-on total is computed via the `count_tokens` API against your active model. Per-component numbers are proportionally scaled from that total. If the API is unreachable, the command falls back to a character-based estimate. The same cost summary appears in the `/plugin` Discover tab's detail pane (requires v2.1.143 or later).\n\n### Design Decisions for Cost Control\n\n**1. Ship costly plugins as opt-in.** Set `defaultEnabled: false` in `plugin.json` (requires v2.1.154) for any plugin whose always-on or on-invoke cost is significant. Users who need it enable it explicitly; the rest pay nothing.\n\n**2. Keep skill descriptions tight.** The description field in a skill's SKILL.md frontmatter appears in context at all times. A bloated description costs tokens on every session. Aim for one clear sentence describing when to invoke the skill.\n\n**3. Use `when: on-skill-invoke:<name>` for monitors.** Monitors whose `when` field is omitted default to `\"always\"` — they start at session start and deliver output to Claude throughout. An always-on monitor from an error log or CI system adds noise and tokens on sessions where the developer is not debugging. Defer monitors to when the relevant skill fires:\n\n```json\n[\n  {\n    \"name\": \"error-log\",\n    \"command\": \"tail -F ./logs/error.log\",\n    \"description\": \"Application error log\",\n    \"when\": \"on-skill-invoke:debug-session\"\n  }\n]\n```\n\n**4. Bundle fewer MCP tools per server.** MCP server tool definitions are injected at session start. A server that exposes 40 tools because it was designed for maximum generality adds the token cost of 40 tool definitions to every session, whether used or not. Design plugin MCP servers around the minimal set of tools actually needed.\n\n**5. Split plugins rather than bundle everything.** A monolithic \"everything\" plugin with 20 skills costs 20x the always-on skill description tokens vs a lean plugin with 3 targeted skills. Team leads who enable everything in one plugin for convenience pay the cost for every developer on every session.\n\n**6. Watch the /reload-plugins cost.** When you run `/reload-plugins` and the plugin provides MCP servers whose tools are not deferred by tool search, the reload invalidates the prompt cache and the next request re-reads the entire conversation. In Claude Code v2.1.163 and later, `/reload-plugins` warns in this case and requires `--force` to proceed."
   },
   {
    "heading": "Development Workflow: From Sketch to Installed",
    "body": "### Scaffold\n\n```bash\n# Scaffold a personal plugin in your skills directory (loads as <name>@skills-dir)\nclaude plugin init my-tool\n\n# Scaffold with specific component directories\nclaude plugin init my-tool --with skills agents hooks mcp\n# Valid --with values: skills, agents, hooks, mcp, lsp, output-style, channel\n```\n\nThis creates `~/.claude/skills/my-tool/` with a manifest and starter files. It loads automatically on the next session — no install step. Use `claude plugin init my-tool --force` to overwrite an existing scaffold.\n\n### Iterate\n\nDuring development, load a plugin directory without any marketplace registration:\n\n```bash\nclaude --plugin-dir ./my-plugin\n```\n\nThe flag also accepts zip archives (v2.1.128+):\n\n```bash\nclaude --plugin-dir ./my-plugin.zip\n```\n\nTo load a zip hosted at a URL (e.g. a CI build artifact), use `--plugin-url`:\n\n```bash\nclaude --plugin-url https://ci.acme.com/artifacts/my-plugin-build.zip\n# Or space-separated URLs in one quoted argument:\nclaude --plugin-url \"https://ci.acme.com/a.zip https://ci.acme.com/b.zip\"\n```\n\nMultiple `--plugin-dir` flags load multiple plugins simultaneously. When a `--plugin-dir` plugin has the same name as an installed marketplace plugin, the local copy takes precedence for that session — except for plugins that managed settings force-enable or force-disable, which `--plugin-dir` cannot override.\n\nWithin a session, pick up changes without restarting:\n\n```bash\n/reload-plugins\n```\n\nSKILL.md changes take effect immediately without reload. Changes to hooks, MCP servers, agents, or output styles require `/reload-plugins`. Changes to monitors require a full session restart.\n\n### Validate\n\n```bash\n# Validate plugin manifest, skill/agent frontmatter, hooks.json\nclaude plugin validate ./my-plugin\n\n# Fail on warnings too (run in CI before publishing)\nclaude plugin validate ./my-plugin --strict\n\n# Validate a marketplace catalog\nclaude plugin validate ./my-marketplace\n```\n\nThe validator checks `plugin.json` schema errors, YAML frontmatter errors in skills and agents, `hooks/hooks.json` syntax errors, unrecognized manifest fields (warnings), and version mismatches between `plugin.json` and `marketplace.json`. Fields with the wrong type are errors; unrecognized keys are warnings (unless you pass `--strict`, which promotes them to errors).\n\n### Publish\n\n```bash\n# Validate before pushing\nclaude plugin validate . --strict\n\n# If using explicit version, tag the release\nclaude plugin tag --push\n\n# Or push code commits and let SHA versioning handle it (no tag needed)\ngit push origin main\n```\n\nFor the community marketplace, submit at `claude.ai/admin-settings/directory/submissions/plugins/new` (requires Team/Enterprise org with directory management access) or `platform.claude.com/plugins/submit` (individual authors without Team/Enterprise). Run `claude plugin validate` locally first — the review pipeline runs the same check. Approved plugins are pinned to a specific commit SHA in `anthropics/claude-plugins-community` and CI bumps the pin automatically as you push new commits."
   },
   {
    "heading": "User Configuration: Prompting for Secrets and Options",
    "body": "Plugins that connect to external services (APIs, databases, internal tools) need per-user configuration — things like API tokens, endpoint URLs, and directory paths that differ across team members. The `userConfig` field in `plugin.json` declares these, and Claude Code prompts for them when the plugin is enabled.\n\n```json\n// plugin.json\n{\n  \"name\": \"acme-tools\",\n  \"userConfig\": {\n    \"api_endpoint\": {\n      \"type\": \"string\",\n      \"title\": \"API endpoint\",\n      \"description\": \"Your team's internal API endpoint\",\n      \"default\": \"https://api.acme.internal\"\n    },\n    \"api_token\": {\n      \"type\": \"string\",\n      \"title\": \"API token\",\n      \"description\": \"Personal API authentication token\",\n      \"sensitive\": true,\n      \"required\": true\n    }\n  }\n}\n```\n\nKeys must be valid identifiers. The full set of per-option fields:\n\n| Field | Required | Description |\n|-------|----------|-------------|\n| `type` | Yes | One of `string`, `number`, `boolean`, `directory`, or `file` |\n| `title` | Yes | Label shown in the configuration dialog |\n| `description` | Yes | Help text shown beneath the field |\n| `sensitive` | No | If `true`, masks input and stores the value in the system keychain instead of `settings.json` |\n| `required` | No | If `true`, validation fails when the field is empty |\n| `default` | No | Value used when the user provides nothing |\n| `multiple` | No | For `string` type, allow an array of strings |\n| `min` / `max` | No | Bounds for `number` type |\n\nCollected values are available in three forms:\n\n1. **Variable substitution in plugin configs**: `${user_config.api_endpoint}` in MCP server configs, hook commands, monitor commands, skill content (non-sensitive only), and agent content (non-sensitive only)\n2. **Environment variables to subprocesses**: exported as `CLAUDE_PLUGIN_OPTION_API_ENDPOINT`, `CLAUDE_PLUGIN_OPTION_API_TOKEN`, etc. (uppercased key name)\n3. **Storage**: non-sensitive values go to `pluginConfigs[<plugin-id>].options` in `settings.json`; sensitive values go to the system keychain (or `~/.claude/.credentials.json` where the keychain is unavailable)\n\n**Keychain limit**: the keychain is shared with OAuth tokens and has an approximately 2 KB total limit. Keep sensitive values small — store tokens, not full JWTs or certificates.\n\nThe `channels` field in `plugin.json` supports a per-channel `userConfig` with the same schema, letting channel-based plugins (Telegram, Slack, Discord integrations) prompt for bot tokens independently of the plugin's top-level options."
   },
   {
    "heading": "Common Pitfalls and Gotchas",
    "body": "### Structure Mistakes\n\n**Components inside `.claude-plugin/`** — only `plugin.json` belongs there. Putting `skills/`, `agents/`, or `hooks/` inside `.claude-plugin/` means Claude Code never discovers them. The plugin loads; the components don't. Diagnose with `claude --debug` and look for \"loading plugin\" messages that omit those component counts.\n\n**Missing frontmatter `name` in root-level skills** — a `SKILL.md` at the plugin root without `name` in frontmatter uses the directory basename as the skill name. After marketplace installation, that basename is a versioned cache path (e.g. `a1b2c3d4@acme-plugins`) that changes on every update. Always set `name` in frontmatter.\n\n**Absolute paths in hook commands or MCP configs** — works during development from `--plugin-dir`, silently breaks after marketplace installation because the cache path is different on every machine. Always use `${CLAUDE_PLUGIN_ROOT}`.\n\n**`../` paths in plugin manifests** — paths outside the plugin directory are blocked at install. The plugin cache copies only the plugin's own tree; symlinks that resolve outside the marketplace are skipped for security.\n\n### Version Traps\n\n**Setting `version` in both `plugin.json` and marketplace entry** — `plugin.json` wins silently. If you bump only the marketplace entry, nothing changes for users. Pick one place.\n\n**Forgetting to bump an explicit version** — if you set `\"version\": \"1.0.0\"` and push fixes, `claude plugin update` reports \"already at the latest version\" because the version string hasn't changed. Either bump the field on every release or remove it and use commit SHA versioning.\n\n**Two release channels resolving to the same version** — if both your `stable` and `latest` branch tags still carry `\"version\": \"1.0.0\"` in `plugin.json`, Claude Code treats them as identical. Each channel must resolve to a distinct version string. Omitting `version` from `plugin.json` entirely sidesteps this: the distinct commit SHAs automatically distinguish the channels.\n\n### Distribution Mistakes\n\n**Relative paths in a URL-served marketplace** — `\"source\": \"./plugins/my-plugin\"` only resolves when the marketplace is added via git (GitHub, git URL). If users add your marketplace via a direct URL to `marketplace.json`, relative paths fail. For URL-based distribution, use explicit `github`, `npm`, or `url` sources instead.\n\n**Launching from a subdirectory with a project-scope skills-directory plugin** — `@skills-dir` plugins load from `.claude/skills/` relative to the directory where you start Claude Code. Unlike plain skills and commands, they do not walk up to the repo root. Launch from the repository root.\n\n**Monitors from project-scope skills-directory plugins** — background monitors do not load from project-scope `@skills-dir` plugins. If you need monitors, install via a marketplace.\n\n### Managed Settings Pitfalls\n\n**URL normalization in `strictKnownMarketplaces`** — exact matching is strict: `https://github.com/acme/plugins` and `https://github.com/acme/plugins.git` are treated as different sources. Use `hostPattern` matching for internal servers where multiple URL forms exist.\n\n**`strictKnownMarketplaces` without `extraKnownMarketplaces`** — the restriction setting blocks additions but registers nothing. Pair them: restrict what users can add, and auto-register the approved marketplace.\n\n**Managed scope and `plugin disable`** — managed-scope plugins cannot be disabled by users. If you enforce a plugin via managed settings and later decide some teams shouldn't have it, you must update the managed settings; there is no user-level override.\n\n**`defaultEnabled` on old clients** — `defaultEnabled: false` requires v2.1.154. Earlier versions ignore the field and enable the plugin on install. If your team may be on older versions, document the `claude plugin disable` step explicitly."
   },
   {
    "heading": "Standalone Config vs Plugin: When to Use Which",
    "body": "The decision between keeping configuration in a project's `.claude/` directory versus packaging it as a plugin is primarily about distribution scope, not capability.\n\n| Dimension | Standalone `.claude/` | Plugin |\n|-----------|----------------------|--------|\n| Skill invocation | `/my-skill` (no namespace) | `/plugin-name:my-skill` (namespaced) |\n| Sharing mechanism | Manual file copy or monorepo path | `claude plugin install` from marketplace |\n| Version tracking | Git history of the project | Explicit version field or commit SHA |\n| Cross-project reuse | Copy files each time | Install once per scope |\n| Conflict prevention | None — names can collide | Namespace prevents collisions |\n| Team automation | Commit `.claude/` to VCS | Commit marketplace registration to VCS |\n| Always-on token cost | Same | Same (review with `claude plugin details`) |\n| First-session experience | Immediate, no install step | Prompted to install if `enabledPlugins` is in project settings |\n| Auto-discovery scope | Walks up to repo root | Project-scope `@skills-dir` does NOT walk up |\n\n### Decision Heuristic\n\n**Stay standalone when** the configuration is project-specific, the team is small and works in one repo, or you are iterating rapidly on prompt design and want short `/skill-name` invocation without namespace overhead.\n\n**Move to a plugin when** you want the same skills or agents across multiple repositories, you want version pinning and controlled rollout of changes, the configuration includes an MCP server or LSP server that needs its own process lifecycle, or you are distributing outside your immediate team.\n\n### Migration Path\n\nThe migration path is well-defined:\n\n```bash\n# 1. Create plugin structure\nmkdir -p my-plugin/.claude-plugin\n\n# 2. Copy existing standalone files\ncp -r .claude/commands my-plugin/\ncp -r .claude/agents  my-plugin/   # if any\ncp -r .claude/skills  my-plugin/   # if any\n\n# 3. Extract hooks from settings.json into hooks/hooks.json\n# (same format — the \"hooks\" object moves to a dedicated file)\nmkdir my-plugin/hooks\n# ... create my-plugin/hooks/hooks.json\n\n# 4. Create the manifest\ncat > my-plugin/.claude-plugin/plugin.json <<'EOF'\n{ \"name\": \"my-plugin\", \"description\": \"Migrated from standalone config\", \"version\": \"1.0.0\" }\nEOF\n\n# 5. Test locally before publishing\nclaude --plugin-dir ./my-plugin\n```\n\nAfter migrating, **remove the originals from `.claude/`** to avoid duplicates. Project and user `.claude/agents/` definitions override same-named plugin agents, so the plugin version only takes effect once the originals are gone."
   }
  ],
  "preQuiz": [
   {
    "prompt": "Your team adds a plugin at project scope and commits `.claude/settings.json` to the repo. A new engineer clones the repo and opens it in Claude Code for the first time. What actually happens?",
    "options": [
     "Nothing — they must run `claude plugin install` manually before any plugin installs.",
     "They are auto-prompted to install the org marketplace and plugins because the trusted repo contains `extraKnownMarketplaces` and `enabledPlugins` in `.claude/settings.json`.",
     "The plugin installs silently and automatically without any prompt, because `.claude/settings.json` is trusted VCS content.",
     "They must first run `claude plugin marketplace add` before the auto-prompt appears, because marketplace registration is per-user."
    ],
    "correct": 1,
    "sectionIndices": [
     0
    ],
    "explanation": "Project-scope `enabledPlugins` and `extraKnownMarketplaces` in `.claude/settings.json` cause Claude Code to auto-prompt collaborators to install the org marketplace and plugins when they trust the repo folder — no manual `marketplace add` per person, but it is a prompt, not a silent install."
   },
   {
    "prompt": "Your CI pipeline runs `claude plugin install formatter@your-org` without specifying a scope. Where does the enabled state get written, and who is affected?",
    "options": [
     "To `.claude/settings.json` (project scope), affecting all repo collaborators.",
     "To `~/.claude/settings.json` (user scope), affecting only the CI user identity and all its projects.",
     "To `.claude/settings.local.json` (local scope), affecting only the CI runner for this repo.",
     "To `managed-settings.json`, making it read-only for all users."
    ],
    "correct": 1,
    "sectionIndices": [
     0,
     1
    ],
    "explanation": "Without `--scope`, `claude plugin install` defaults to user scope and writes to `~/.claude/settings.json`, affecting the CI user identity across all projects. To scope to the repo and share via VCS, you must pass `--scope project`."
   },
   {
    "prompt": "You run `/plugin marketplace add your-org/claude-plugins` in one worktree and then open a second worktree of the same repo. Is the marketplace registration available in the second worktree?",
    "options": [
     "No — worktrees each have their own `.claude/` directory, so the registration only applies where you ran the command.",
     "Yes — marketplace registration is stored once per user in `~/.claude/plugins/known_marketplaces.json`, not per-project, so all worktrees share it.",
     "Only if you ran the command with `--scope project` so it was committed to `.claude/settings.json`.",
     "No — you must re-run `marketplace add` in every new session, because it is session-state only."
    ],
    "correct": 1,
    "sectionIndices": [
     0
    ],
    "explanation": "Marketplace registration state lives in `~/.claude/plugins/known_marketplaces.json`, once per user. Worktrees share this file, so a marketplace added in one worktree is immediately visible in another — no need to re-register per worktree or per project."
   },
   {
    "prompt": "After installing a new plugin mid-session you run `/reload-plugins`. The session immediately errors: 'Refusing to reload: pass --force'. What is the most likely cause?",
    "options": [
     "The plugin contains a syntax error in `plugin.json` that the validator caught.",
     "You do not have write permission to `~/.claude/plugins/`.",
     "The plugin ships a non-deferred MCP server, which would invalidate the prompt cache and force a full re-read; Claude Code v2.1.163+ refuses this without `--force`.",
     "The plugin's hooks are not marked executable, so the reload is blocked as a safety measure."
    ],
    "correct": 2,
    "sectionIndices": [
     1
    ],
    "explanation": "v2.1.163+ shows a warning and refuses `/reload-plugins` unless you pass `--force` when the new plugin includes a non-deferred MCP server, because loading it invalidates the prompt cache and forces a full context re-read — a significant token cost."
   },
   {
    "prompt": "You want CI to check whether any installed plugins have errors. Which command gives you machine-readable output suitable for scripting?",
    "options": [
     "`/plugin list --enabled` — its exit code is non-zero if any plugin has errors.",
     "`claude plugin list --json` — read each plugin's `errors` field.",
     "`claude plugin validate` — it prints errors in JSON by default.",
     "`claude plugin marketplace update --json` — the update output includes plugin error state."
    ],
    "correct": 1,
    "sectionIndices": [
     1
    ],
    "explanation": "The content explicitly recommends `claude plugin list --json` for CI/health checks because you can read each plugin's `errors` field, rather than parsing human-readable output. The slash commands are for interactive sessions, not scripting."
   },
   {
    "prompt": "A teammate tries to install a plugin but gets 'not found in any marketplace'. The official marketplace was auto-added when they first launched Claude Code interactively months ago. What is the most likely fix?",
    "options": [
     "Run `claude plugin marketplace add anthropics/claude-plugins-official` to re-add the marketplace from scratch.",
     "Run `/plugin marketplace update claude-plugins-official` to refresh the catalog from its source.",
     "Submit the plugin to the community marketplace — official plugins are always available once submitted.",
     "Add `extraKnownMarketplaces` to the project `.claude/settings.json` pointing to the official repo."
    ],
    "correct": 1,
    "sectionIndices": [
     2
    ],
    "explanation": "When a plugin is 'not found in any marketplace,' the marketplace catalog is stale (not missing). `/plugin marketplace update claude-plugins-official` refreshes it. Re-adding with `marketplace add` is only needed if it was never added in the first place (e.g., non-interactive scripts)."
   },
   {
    "prompt": "Your security team wants to self-submit a tool to the official `claude-plugins-official` marketplace so it's available to all org users. Is this possible?",
    "options": [
     "Yes — submit via the `/plugin` Discover tab's in-app submission form.",
     "No — in-app submission forms go to the community marketplace, not the official one. The official catalog is curated by Anthropic; you cannot self-submit to it.",
     "Yes — submit at platform.claude.com/plugins/submit and it appears in the official catalog after approval.",
     "Yes — add your plugin to the `claude-plugins-official` GitHub repo via pull request."
    ],
    "correct": 1,
    "sectionIndices": [
     2
    ],
    "explanation": "You cannot self-submit to the official catalog — it is curated at Anthropic's discretion. In-app submission forms go to the *community* marketplace. Community approval requires a Team/Enterprise org with directory-management access; individuals use the Console form at platform.claude.com/plugins/submit."
   },
   {
    "prompt": "You create a plugin and want to ship instructions that appear as Claude's context when the plugin is active. You place a `CLAUDE.md` at the plugin root directory. Will this work?",
    "options": [
     "Yes — `CLAUDE.md` at plugin root is loaded as context for all sessions where the plugin is enabled.",
     "No — a plugin-root `CLAUDE.md` is NOT loaded as context. Ship instructions as a skill, not a `CLAUDE.md`.",
     "Only if you declare the `CLAUDE.md` path in `plugin.json` under the `context` field.",
     "Yes — but only for agents defined in the plugin, not for general sessions."
    ],
    "correct": 1,
    "sectionIndices": [
     3
    ],
    "explanation": "A `CLAUDE.md` at the plugin root is NOT loaded as context. Plugins contribute context only via skills, agents, and hooks. The correct approach is to ship instructions as a skill (SKILL.md with frontmatter)."
   },
   {
    "prompt": "You are converting a standalone `.claude/` config into a plugin and move skills and agents to the plugin root. After enabling the plugin you notice Claude uses old agent definitions instead of the plugin's agents. What went wrong?",
    "options": [
     "Plugin agents must be declared in `plugin.json` under an `agents` field or they are ignored.",
     "You left the original agent files in `.claude/agents/` — project/user `.claude/agents/` override same-named plugin agents, causing the plugin agents to be shadowed.",
     "Agents in plugins require a `hooks` field in their frontmatter to be loaded.",
     "The plugin was installed at user scope but the old `.claude/agents/` are at project scope, so project scope wins."
    ],
    "correct": 1,
    "sectionIndices": [
     3
    ],
    "explanation": "When converting a standalone config to a plugin, you must delete the original `.claude/` files. Leftover files cause duplicate skills and shadow plugin agents: project/user `.claude/agents/` override same-named plugin agents."
   },
   {
    "prompt": "You author a plugin with a single skill at the plugin root (`SKILL.md` with no `skills/` directory). After every update, users report the invocation name changes. What is missing?",
    "options": [
     "A `skills/` directory — single-skill plugins at root are not supported; the name always derives from directory.",
     "A `name` field in the SKILL.md frontmatter — without it the invocation name falls back to the install-dir version string, which changes on every update.",
     "A `displayName` in `plugin.json` — without it Claude Code derives the name from the manifest version.",
     "The `version` field in `plugin.json` — an unversioned plugin cannot have a stable invocation name."
    ],
    "correct": 1,
    "sectionIndices": [
     3
    ],
    "explanation": "For single-skill plugins at the plugin root (supported since v2.1.142+), you must set the `name` frontmatter field in SKILL.md. Without it, the invocation name falls back to the install-dir version string, which changes on every plugin update."
   },
   {
    "prompt": "A plugin hook that should fire after every file write is silently not firing. The hook command is valid shell. What is the most likely cause?",
    "options": [
     "The hook is defined in `hooks/hooks.json` but should be in `.claude/settings.json` to fire.",
     "The hook event name uses `postToolUse` (lowercase) instead of the required `PostToolUse` (PascalCase) — hook event names are case-sensitive.",
     "The hook type is `command` but file-write events require type `http`.",
     "Plugin hooks only fire during worktree sessions, not in the default session."
    ],
    "correct": 1,
    "sectionIndices": [
     4
    ],
    "explanation": "Hook event names are case-sensitive. `postToolUse` is silently ignored; the correct name is `PostToolUse`. Additionally, non-executable scripts silently don't fire (`chmod +x` required), but the question specifies a valid shell command, making case-sensitivity the most likely culprit."
   },
   {
    "prompt": "A plugin agent's frontmatter includes `permissionMode: acceptEdits`. After shipping the plugin, you notice the permission mode is not applied. Why?",
    "options": [
     "Plugin agents do not support frontmatter at all — all configuration must go in `plugin.json`.",
     "For security, `hooks`, `mcpServers`, and `permissionMode` are NOT supported in plugin-shipped agents — they are silently ignored.",
     "`permissionMode` is only honored in user-scope agents, not plugin-bundled agents.",
     "You must set `permissionMode` in the plugin's `settings.json`, not in the agent frontmatter."
    ],
    "correct": 1,
    "sectionIndices": [
     4
    ],
    "explanation": "Plugin agents support `name, description, model, effort, maxTurns, tools, disallowedTools, skills, memory, background, isolation` in frontmatter, but `hooks`, `mcpServers`, and `permissionMode` are explicitly NOT supported and are silently ignored for security reasons."
   },
   {
    "prompt": "Your plugin installs a Python venv for a bundled tool. After an update the venv is gone. Where should the plugin have written the venv to survive updates?",
    "options": [
     "Inside the plugin directory at `${CLAUDE_PLUGIN_ROOT}/venv/` — this is the plugin's home directory.",
     "At `${CLAUDE_PLUGIN_DATA}` (`~/.claude/plugins/data/{id}/`) — this is the persistent state dir that survives updates.",
     "At `~/.claude/plugins/{plugin-name}/venv/` — the standard plugin state location.",
     "Inside the project directory at `.claude/plugin-data/{plugin-name}/` — project-scoped plugins use project-local storage."
    ],
    "correct": 1,
    "sectionIndices": [
     4
    ],
    "explanation": "`${CLAUDE_PLUGIN_ROOT}` is the install directory and CHANGES on every update. `${CLAUDE_PLUGIN_DATA}` maps to `~/.claude/plugins/data/{id}/` and is the persistent state directory that survives updates — the correct place to install deps and persistent state."
   },
   {
    "prompt": "You add a `userConfig` entry with `sensitive: true` to collect an API key. A user with many other plugins and OAuth tokens enabled reports the key cannot be saved. What is the constraint you hit?",
    "options": [
     "`sensitive: true` entries require a Team/Enterprise org — individual licenses store secrets in settings.json only.",
     "The system keychain has a ~2KB total budget shared with OAuth tokens. With many plugins and tokens, the budget is exhausted and sensitive config cannot be stored.",
     "Sensitive config requires the user to explicitly unlock the keychain first with `claude plugin unlock-keychain`.",
     "`sensitive: true` is only honored for `string` type config; other types always go to settings.json."
    ],
    "correct": 1,
    "sectionIndices": [
     4
    ],
    "explanation": "Sensitive `userConfig` values are stored in the system keychain (or `~/.claude/.credentials.json`), which has a ~2KB total budget shared with all OAuth tokens. A user with many plugins and tokens can exhaust this budget, making it impossible to store additional secrets."
   },
   {
    "prompt": "In a `marketplace.json` plugin entry you set both `ref: 'v1.2.0'` and `sha: 'abc123...'`. The `v1.2.0` tag is later deleted from the upstream repo. What happens when a user installs this plugin?",
    "options": [
     "Installation fails — `ref` and `sha` cannot both be set; only one is honored.",
     "Installation uses the tag name `v1.2.0`; since the tag is deleted, the install fails.",
     "Installation succeeds using the SHA `abc123...` — when both are set, `sha` is the effective pin and survives tag deletion as long as the commit is reachable.",
     "Installation falls back to the `main` branch since neither `ref` nor `sha` resolved cleanly."
    ],
    "correct": 2,
    "sectionIndices": [
     5
    ],
    "explanation": "When both `ref` and `sha` are set in a plugin source, `sha` is the effective pin. The install fetches by SHA directly, so it survives deletion of the named branch or tag upstream as long as the commit remains reachable in the remote repository."
   },
   {
    "prompt": "You publish a marketplace with `strict: false` on a plugin entry. The plugin directory contains a `plugin.json` that declares several components. What happens at install time?",
    "options": [
     "The `plugin.json` is authoritative — `strict: false` only adds marketplace metadata but never overrides plugin.json.",
     "The marketplace entry is the entire definition and any `plugin.json` declaring components fails to load — `strict: false` lets the marketplace operator curate the plugin without a plugin.json.",
     "`strict: false` means validation is skipped, so both `plugin.json` and the marketplace entry are merged with no conflict errors.",
     "The plugin installs without any components — `strict: false` disables component auto-discovery."
    ],
    "correct": 1,
    "sectionIndices": [
     5
    ],
    "explanation": "With `strict: false` the marketplace entry is the entire plugin definition. Any `plugin.json` that declares components fails to load — this allows a marketplace operator to curate/override plugin definitions without the plugin author's `plugin.json` taking precedence."
   }
  ],
  "tasks": [
   {
    "id": "stage6-task-project-scope-plugin",
    "afterSectionIdx": 0,
    "title": "Add a marketplace and install a plugin at project scope",
    "instructions": "This task gives you concrete experience with the project-scope distribution flow.\n\n1. Navigate to a test git repo (or create one: `mkdir /tmp/claude-plugin-test && cd /tmp/claude-plugin-test && git init`).\n\n2. Add the official marketplace at **project scope**:\n```bash\nclaude plugin marketplace add anthropics/claude-plugins-official --scope project\n```\n\n3. Verify the `extraKnownMarketplaces` entry was written to `.claude/settings.json` (not `~/.claude/settings.json`):\n```bash\ncat .claude/settings.json\n```\n\n4. Install a plugin at project scope (use any available plugin, e.g. `git` if available, or list what's available first with `claude plugin list --available --json`):\n```bash\nclaude plugin install <name>@claude-plugins-official --scope project\n```\n\n5. Confirm `enabledPlugins` now appears in `.claude/settings.json`:\n```bash\ncat .claude/settings.json\n```\n\n6. Check that `~/.claude/settings.json` was NOT modified (the install should only be in the project file):\n```bash\ngrep -c 'enabledPlugins' ~/.claude/settings.json || echo 'not in user settings'\n```",
    "doneWhen": "`.claude/settings.json` contains both `extraKnownMarketplaces` and `enabledPlugins` entries, and `~/.claude/settings.json` does not contain the same plugin in `enabledPlugins`."
   },
   {
    "id": "stage6-task-author-minimal-plugin",
    "afterSectionIdx": 3,
    "title": "Author a minimal plugin with a named skill",
    "instructions": "Build the smallest valid plugin structure, focusing on the layout rules and the single-skill naming pitfall.\n\n1. Create the plugin directory structure:\n```bash\nmkdir -p /tmp/my-test-plugin/.claude-plugin\n```\n\n2. Write the manifest at `.claude-plugin/plugin.json` (only `plugin.json` goes here — NOT skills):\n```bash\ncat > /tmp/my-test-plugin/.claude-plugin/plugin.json << 'EOF'\n{\n  \"name\": \"my-test-plugin\",\n  \"description\": \"Minimal plugin for learning stage-6\",\n  \"version\": \"1.0.0\",\n  \"author\": \"Your Name\"\n}\nEOF\n```\n\n3. Create the skill at the **plugin root** (not inside `.claude-plugin/`):\n```bash\ncat > /tmp/my-test-plugin/SKILL.md << 'EOF'\n---\nname: greet\ndescription: Greets the user by name. Invoke with /my-test-plugin:greet.\n---\n\nGreet the user warmly, addressing them by name if provided.\nEOF\n```\n\n4. Install the plugin from local path (at user scope for this exercise):\n```bash\nclaude plugin install /tmp/my-test-plugin\n```\n\n5. Verify the install and confirm no errors:\n```bash\nclaude plugin list --json | python3 -c \"import sys,json; plugins=json.load(sys.stdin); [print(p['name'], 'errors:', p.get('errors','none')) for p in plugins]\"\n```\n\n6. Inside a Claude Code session, confirm the skill is invocable as `/my-test-plugin:greet`.",
    "doneWhen": "`claude plugin list --json` shows your plugin with an empty `errors` field, and you can invoke `/my-test-plugin:greet` in a Claude Code session."
   },
   {
    "id": "stage6-task-marketplace-json",
    "afterSectionIdx": 5,
    "title": "Create a private Git marketplace definition",
    "instructions": "Author a `marketplace.json` that references your local plugin, simulating what a team would commit to a private `your-org/claude-plugins` repo.\n\n1. Create the marketplace repo directory:\n```bash\nmkdir -p /tmp/my-org-marketplace/.claude-plugin\n```\n\n2. Place a local plugin at a relative path inside it:\n```bash\nmkdir -p /tmp/my-org-marketplace/plugins/greeter/.claude-plugin\ncat > /tmp/my-org-marketplace/plugins/greeter/.claude-plugin/plugin.json << 'EOF'\n{\"name\": \"greeter\", \"description\": \"Team greeter plugin\"}\nEOF\ncat > /tmp/my-org-marketplace/plugins/greeter/SKILL.md << 'EOF'\n---\nname: hello\ndescription: Says hello from the team plugin.\n---\nSay: Hello from the team marketplace!\nEOF\n```\n\n3. Write the marketplace definition at `.claude-plugin/marketplace.json` (at the **repo root**, not inside the plugin):\n```bash\ncat > /tmp/my-org-marketplace/.claude-plugin/marketplace.json << 'EOF'\n{\n  \"name\": \"my-org-plugins\",\n  \"owner\": {\"name\": \"My Org\", \"email\": \"admin@example.com\"},\n  \"plugins\": [\n    {\n      \"name\": \"greeter\",\n      \"source\": \"./plugins/greeter\",\n      \"defaultEnabled\": false,\n      \"displayName\": \"Team Greeter\"\n    }\n  ]\n}\nEOF\n```\n\n4. Register the local marketplace:\n```bash\nclaude plugin marketplace add /tmp/my-org-marketplace\n```\n\n5. Verify it appears in the marketplace list:\n```bash\nclaude plugin marketplace list\n```\n\n6. Install the plugin from your new marketplace:\n```bash\nclaude plugin install greeter@my-org-plugins\n```",
    "doneWhen": "`claude plugin marketplace list` shows `my-org-plugins` and `claude plugin list` shows the `greeter` plugin installed from it."
   }
  ],
  "visualizations": [
   {
    "id": "stage-6-v",
    "kind": "comparison-table",
    "title": "Plugins & distribution",
    "textualSummary": "Key concepts of Plugins & distribution: plugin, marketplace, settings scope.",
    "columns": [
     "Concept",
     "In this stage"
    ],
    "rows": [
     {
      "label": "plugin",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "marketplace",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "settings scope",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "project-scope distribution via VCS",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     }
    ]
   }
  ],
  "confusions": [
   {
    "misconception": "Skipping Plugins & distribution.",
    "correction": "Each stage is load-bearing for the setup."
   },
   {
    "misconception": "These concepts are interchangeable.",
    "correction": "Each has a distinct role; see the definitions."
   }
  ],
  "quiz": [
   {
    "id": "stage-6-q1",
    "type": "multiple-choice",
    "prompt": "Your team adds a private Git marketplace and installs a linting plugin at project scope, then commits `.claude/settings.json`. A new hire clones the repo. What happens when she opens the project in Claude Code for the first time?",
    "options": [
     "The marketplace and plugin are silently active — no prompt is shown because everything is checked into VCS.",
     "She is auto-prompted to install the org marketplace and plugin because they are in the trusted project's `.claude/settings.json`.",
     "She must manually run `claude plugin marketplace add` and `claude plugin install` because marketplace registration is per-user.",
     "The plugin is installed at user scope on her machine, overriding the project-scope intent."
    ],
    "correct": 1,
    "explanation": "The content states: 'Collaborators are then auto-prompted to install the org marketplace/plugins when they trust the repo folder — no manual `marketplace add` per person.' The marketplace and plugin settings in `.claude/settings.json` trigger an install prompt on first trust, not silent activation (option A is wrong) and not a fully manual process (option C is wrong). The scope stays project, not user (option D is wrong)."
   },
   {
    "id": "stage-6-q2",
    "type": "multiple-choice",
    "prompt": "An admin wants certain security-hardening plugins to be impossible for individual developers to disable. Which scope achieves this, and why?",
    "options": [
     "Project scope — because `.claude/settings.json` is checked into VCS, developers cannot change it without a code review.",
     "User scope with `--force` — because user settings override all other scopes.",
     "Managed settings — because managed settings are read-only and users cannot modify or remove them.",
     "Local scope — because `.claude/settings.local.json` is gitignored and therefore invisible to regular developers."
    ],
    "correct": 2,
    "explanation": "The content explicitly says managed settings are 'Installed by admins; read-only, users cannot modify/remove.' Project scope (option A) requires a VCS PR, but any developer can locally override it with user or local scope — it is not truly read-only for the running process. User scope with `--force` (option B) is not a real mechanism and user settings don't enforce policy. Local scope (option D) is the opposite of what's needed — it's per-person and optional."
   },
   {
    "id": "stage-6-q3",
    "type": "multiple-choice",
    "prompt": "A developer runs `/reload-plugins` after enabling a new MCP-server-backed plugin mid-session. Claude Code prints a warning and refuses to proceed. What is the most likely cause?",
    "options": [
     "The plugin's `plugin.json` manifest is malformed and the validator caught it.",
     "The plugin contains non-deferred MCP servers, which would invalidate the prompt cache and force a full re-read; the tool requires `--force` to proceed.",
     "The developer lacks permission to reload MCP servers without restarting Claude Code entirely.",
     "The reload failed because marketplace registration is stored per-user and the current session has not yet synced."
    ],
    "correct": 1,
    "explanation": "The content states: 'a plugin with non-deferred MCP servers invalidates the prompt cache and forces a full re-read. v2.1.163+ shows a warning and refuses unless you pass `--force`.' Manifest validation errors (option A) would surface as errors, not a warning-plus-refusal gate. There is no per-permission-level restriction on reloading (option C). Marketplace sync (option D) is unrelated to the reload-plugins warning."
   },
   {
    "id": "stage-6-q4",
    "type": "multiple-choice",
    "prompt": "A teammate authors a plugin and places a `CLAUDE.md` file at the plugin root to give context to users. After installing the plugin, users report that the instructions never appear. What is the actual problem?",
    "options": [
     "The `CLAUDE.md` file must be inside `.claude-plugin/` to be picked up at load time.",
     "A plugin-root `CLAUDE.md` is not loaded as context; plugins contribute context only via skills, agents, and hooks.",
     "The plugin must declare the `CLAUDE.md` in its manifest under a `contextFiles` field.",
     "The `CLAUDE.md` is only loaded if the user has the plugin enabled at user scope, not project scope."
    ],
    "correct": 1,
    "explanation": "The content is explicit: 'a plugin-root CLAUDE.md is NOT loaded as context. Plugins contribute context only via skills/agents/hooks.' The correct approach is to ship instructions as a skill. Option A is wrong — `.claude-plugin/` holds only `plugin.json`. Option C invents a non-existent `contextFiles` field. Option D incorrectly ties context loading to scope."
   },
   {
    "id": "stage-6-q5",
    "type": "multiple-choice",
    "prompt": "You author a single-skill plugin and omit `name` from the `SKILL.md` frontmatter. After publishing an update, users complain the slash-command invocation name changed. What caused this?",
    "options": [
     "The plugin manifest's `displayName` field was also changed, and `displayName` is used for skill namespacing.",
     "Without a frontmatter `name`, the invocation name falls back to the install-directory version string, which changes on every update.",
     "The marketplace re-pinned the plugin to a new SHA, which resets the skill namespace.",
     "Omitting `name` causes the skill to use the plugin `name` field from `plugin.json` as its invocation name, and the plugin was renamed."
    ],
    "correct": 1,
    "explanation": "The content states: 'Set frontmatter `name` or the invocation name falls back to the install-dir version string (changes every update).' `displayName` (option A) is explicitly described as 'UI-only, not used for namespacing.' SHA-pinning (option C) controls source integrity, not skill invocation names. The plugin `name` field is the namespace (option D), not the per-skill invocation name — and it would only change if the plugin itself were renamed."
   },
   {
    "id": "stage-6-q6",
    "type": "multiple-choice",
    "prompt": "Your organization wants to submit a plugin to be publicly discoverable and used by the broader Claude Code community. Where should the plugin be submitted, and through what channel?",
    "options": [
     "Submit to the official `claude-plugins-official` marketplace via a pull request to the Anthropic-curated GitHub repo.",
     "Submit to the community marketplace via the in-app submission form or the Console form at platform.claude.com/plugins/submit; individuals use the Console form.",
     "Post the plugin to the official marketplace by opening an issue on the official catalog repository — all submissions are reviewed on a first-come basis.",
     "Create an npm package with a `claude-plugin` keyword; it is auto-indexed into the official marketplace nightly."
    ],
    "correct": 1,
    "explanation": "The content states: 'In-app submission forms go to the community marketplace, not the official one — you cannot self-submit to the official catalog (it's curated at Anthropic's discretion). Community approval needs a Team/Enterprise org with directory-management access; individuals use the Console form at platform.claude.com/plugins/submit.' Options A and C incorrectly describe the official marketplace as open for self-submission. Option D invents an npm-keyword auto-indexing mechanism."
   },
   {
    "id": "stage-6-q7",
    "type": "multiple-choice",
    "prompt": "A marketplace `plugin.json` entry sets both `ref: 'v2.0'` and `sha: 'a3f8c1d...'`. The team later deletes the `v2.0` tag from GitHub. Will installs still succeed?",
    "options": [
     "No — without the named ref, the installer cannot resolve the source and the install fails.",
     "Yes — when both are set, `sha` is the effective pin; the install succeeds as long as the commit is reachable.",
     "Yes — but only if the marketplace catalog is refreshed before the tag deletion propagates.",
     "No — plugin sources must use either `ref` or `sha`, not both; the conflict causes a load error."
    ],
    "correct": 1,
    "explanation": "The content states: 'When both `ref` and `sha` are set, `sha` is the effective pin — install survives deletion of the named branch/tag upstream as long as the commit is reachable.' Option A is wrong because sha takes priority over the ref name. Option C invents a catalog-refresh dependency. Option D is wrong — the content describes using both as a valid and intentional pattern, not a conflict."
   },
   {
    "id": "stage-6-q8",
    "type": "multiple-choice",
    "prompt": "A developer writes a plugin hook script but it never fires, even though the hook is correctly listed in `hooks/hooks.json`. The most common silent failure cause is:",
    "options": [
     "The event name uses lowercase (`postToolUse`) instead of the required PascalCase (`PostToolUse`).",
     "The hook script was not added to `bin/` so it isn't on the PATH.",
     "The `hooks/hooks.json` must redeclare the hook type in the plugin manifest `plugin.json`.",
     "Hooks in plugins only fire in interactive sessions, not in non-interactive `claude` CLI invocations."
    ],
    "correct": 0,
    "explanation": "The content states: 'Hook event names are case-sensitive (`PostToolUse`, not `postToolUse`).' A second silent failure is the script not being executable (`chmod +x`), but between these options only the case-sensitivity issue is directly quoted. Option B confuses PATH executables (in `bin/`) with hook scripts. Option C invents a manifest redeclaration requirement. Option D is not stated and confuses monitors (which run only in interactive sessions) with hooks."
   },
   {
    "id": "stage-6-q9",
    "type": "multi-select",
    "prompt": "Your team is converting a standalone `.claude/` config directory into a distributable plugin. Select ALL steps that the content identifies as required or explicitly recommended during this conversion.",
    "options": [
     "Create a `.claude-plugin/` directory and add `plugin.json` inside it.",
     "Move hooks into `hooks/hooks.json` at the plugin root.",
     "Delete the original `.claude/` files after copying components to the plugin root.",
     "Place component directories (`skills/`, `agents/`, etc.) inside `.claude-plugin/` alongside `plugin.json`.",
     "Move the plugin root `CLAUDE.md` into a skill so it is loaded as context."
    ],
    "correct": [
     0,
     1,
     2,
     4
    ],
    "explanation": "The content lists: 'mkdir .claude-plugin/, add plugin.json, copy commands/agents/skills/ to plugin root, move hooks into hooks/hooks.json. Then delete the original .claude/ files — leftovers cause duplicate skills and shadow plugin agents.' It also says plugin-root CLAUDE.md is not loaded; instructions should be shipped as a skill. Option D is explicitly wrong: 'All component dirs go at the plugin ROOT, never inside .claude-plugin/ (only plugin.json lives there).'"
   },
   {
    "id": "stage-6-q10",
    "type": "multi-select",
    "prompt": "Which of the following are true about `userConfig` fields in a plugin manifest? Select ALL that apply.",
    "options": [
     "Values marked `sensitive: true` are stored in the system keychain or `~/.claude/.credentials.json` instead of `settings.json`.",
     "The keychain has an unlimited storage capacity for sensitive config values.",
     "Config values are substituted in skill/agent content as `${user_config.KEY}` and exported as `CLAUDE_PLUGIN_OPTION_<KEY>`.",
     "Non-sensitive values are written to `pluginConfigs[<plugin-id>].options` in `settings.json`.",
     "Users are prompted to enter `userConfig` values the first time they run any skill, not at enable time."
    ],
    "correct": [
     0,
     2,
     3
    ],
    "explanation": "The content confirms: (A) '`sensitive: true` masks input and stores in the system keychain (or `~/.claude/.credentials.json`) instead of settings.json'; (C) 'Substituted as `${user_config.KEY}` and exported as `CLAUDE_PLUGIN_OPTION_<KEY>`'; (D) 'Non-sensitive values go to `pluginConfigs[<plugin-id>].options` in settings.json.' Option B is wrong — the content says the keychain has a '~2KB total budget shared with OAuth tokens, so keep secrets small.' Option E is wrong — the content says users are prompted 'at enable time', not on first skill run."
   },
   {
    "id": "stage-6-q11",
    "type": "multiple-choice",
    "prompt": "A marketplace operator wants to ship a curated plugin catalog where each entry's marketplace metadata completely defines the plugin — no `plugin.json` in the plugin repo should be respected. Which `marketplace.json` field achieves this, and what is its effect?",
    "options": [
     "`strict: false` — the marketplace entry becomes the entire definition, and any `plugin.json` declaring components fails to load.",
     "`strict: true` — the marketplace entry overrides all fields in `plugin.json`.",
     "`defaultEnabled: false` — plugins load as disabled so the marketplace entry controls activation.",
     "`allowCrossMarketplaceDependenciesOn: []` — an empty list prevents `plugin.json` from declaring external dependencies."
    ],
    "correct": 0,
    "explanation": "The content states: '`strict` (default true): true → `plugin.json` is authority, marketplace entry supplements/merges. false → marketplace entry is the entire definition and any `plugin.json` declaring components fails to load (lets a marketplace operator curate without a plugin.json).' Option B reverses the meaning of `strict: true`. Option C is about default-disabled installation, not about who is the authoritative source. Option D is unrelated to plugin.json authority."
   },
   {
    "id": "stage-6-q12",
    "type": "multiple-choice",
    "prompt": "A plugin script references bundled helper binaries using the absolute path `/home/brian/.claude/plugins/my-plugin-1.2.3/bin/helper.sh`. After the next plugin update, the script breaks. What is the correct pattern?",
    "options": [
     "Use `${CLAUDE_PLUGIN_DATA}/bin/helper.sh` — the data directory persists across updates.",
     "Use `${CLAUDE_PLUGIN_ROOT}/bin/helper.sh` — the install dir is substituted correctly at runtime even though it changes on update.",
     "Use `${CLAUDE_PROJECT_DIR}/bin/helper.sh` — the project root is stable and should contain bundled executables.",
     "Use a relative path `./bin/helper.sh` — hooks always execute from the plugin root directory."
    ],
    "correct": 1,
    "explanation": "The content states: '`${CLAUDE_PLUGIN_ROOT}` — Absolute install dir — CHANGES on every update; reference bundled scripts/binaries via this, never absolute paths.' Using `${CLAUDE_PLUGIN_ROOT}` gives the correct current install dir at runtime, even as it changes. `${CLAUDE_PLUGIN_DATA}` (option A) is for persistent state like node_modules and venvs — not for bundled binaries that are part of the plugin source. `${CLAUDE_PROJECT_DIR}` (option C) is the user's project, not the plugin install. Relative paths (option D) depend on the working directory at execution time, which is not guaranteed to be the plugin root."
   },
   {
    "id": "stage-6-q13",
    "type": "multiple-choice",
    "prompt": "A CI pipeline needs to check whether any installed plugins have errors. Which command does the content recommend, and why?",
    "options": [
     "`/plugin list` in interactive mode — the 4-tab UI shows an Errors tab that is the canonical error source.",
     "`claude plugin list --json` and read each plugin's `errors` field — use this for CI rather than parsing human output.",
     "`claude plugin validate <name>` — validation mode returns a machine-readable exit code for CI gates.",
     "`claude plugin marketplace update` — refresh all sources first; error state is only accurate after an update."
    ],
    "correct": 1,
    "explanation": "The content explicitly says: 'Use `claude plugin list --json` (read each plugin's `errors` field) for CI/health checks rather than parsing human output.' The `/plugin` interactive UI (option A) is for human use, not CI. Option C (`validate`) is a valid subcommand but the content recommends `list --json` with the errors field for CI health checks specifically. Option D conflates marketplace source freshness with runtime error state."
   },
   {
    "id": "stage-6-q14",
    "type": "multiple-choice",
    "prompt": "A plugin agent frontmatter declares `permissionMode: restricted` to limit what it can do. Will this setting be honored?",
    "options": [
     "Yes — `permissionMode` is one of the supported frontmatter fields for plugin agents.",
     "No — `permissionMode` is not supported in plugin-shipped agents and is silently ignored.",
     "Only if the plugin is installed at managed scope — otherwise it is overridden by user settings.",
     "Yes, but only for agents declared with `isolation: worktree`."
    ],
    "correct": 1,
    "explanation": "The content states: 'For security, `hooks`, `mcpServers`, and `permissionMode` are NOT supported in plugin-shipped agents (silently ignored).' This is an explicit security policy. None of the other options correctly describe the behavior — managed scope (option C) affects read-only enforcement on settings, not agent frontmatter support; `isolation: worktree` (option D) is the only valid isolation value but is unrelated to whether `permissionMode` is honored."
   },
   {
    "id": "stage-6-q15",
    "type": "multi-select",
    "prompt": "A team is designing a plugin that will be always-on for all developers. Select ALL concerns the content says teams should actively budget or plan for with always-on plugins.",
    "options": [
     "Always-on token cost from non-deferred MCP servers invalidating the prompt cache.",
     "The marketplace catalog syncing nightly, which may delay availability of critical security updates.",
     "The `${CLAUDE_PLUGIN_DATA}` directory growing unbounded if the plugin installs dependencies without cleanup.",
     "The keychain's ~2KB total budget being consumed by plugin secrets, leaving insufficient space for OAuth tokens.",
     "Background monitors consuming resources because they run continuously and each stdout line is delivered to Claude as a notification."
    ],
    "correct": [
     0,
     3,
     4
    ],
    "explanation": "The content identifies: (A) 'always-on token cost is budgeted' and non-deferred MCP servers invalidate prompt cache (a reload-plugins warning exists specifically for this); (D) 'keychain has a ~2KB total budget shared with OAuth tokens, so keep secrets small'; (E) 'Monitors: each command runs while active and every stdout line is delivered to Claude as a notification' — active resource concern. Option B (nightly sync delay) is mentioned in the context of community approval, not as a plugin design concern for always-on plugins. Option C (`${CLAUDE_PLUGIN_DATA}` growth) is not raised as a concern in the content."
   }
  ],
  "masteryCheckpoint": "You can explain the concepts of Plugins & distribution."
 },
 {
  "id": "stage-7",
  "stage": 7,
  "title": "Automation, CI & integrations",
  "summary": "Automation, CI & integrations: headless (print) mode, CI authentication, cost and loop bounding.",
  "prerequisites": [
   "stage-6"
  ],
  "objectives": [
   "Understand the concepts in Automation, CI & integrations."
  ],
  "definitions": [
   {
    "term": "headless (print) mode",
    "short": "Running Claude Code non-interactively with `claude -p` so it executes one query via the Agent SDK and exits, forming the basis for all scripting and automation."
   },
   {
    "term": "CI authentication",
    "short": "Authenticating unattended runs without interactive sign-in, choosing between subscription billing and Console API-usage billing."
   },
   {
    "term": "cost and loop bounding",
    "short": "Capping an automated run's agentic turns and dollar spend so a pipeline can't run away."
   },
   {
    "term": "tool and MCP scoping",
    "short": "Controlling which tools and MCP servers an automated agent can access, distinguishing auto-approval, removal-from-context, and call-level denial."
   },
   {
    "term": "managed Code Review service",
    "short": "A GitHub App service (Team/Enterprise plans) that posts inline PR review comments on configurable triggers, billed separately via usage credits and unable to approve or block on its own."
   },
   {
    "term": "REVIEW.md control file",
    "short": "A short repo-root file injected verbatim as highest-priority instructions into every managed review, used to tune what counts as a blocking finding."
   }
  ],
  "sections": [
   {
    "heading": "Headless (Print) Mode: The Foundation of All Automation",
    "body": "Every automated Claude Code workflow starts with one flag: `-p` (or `--print`). This puts Claude Code into non-interactive mode: it runs one query through the agent loop, prints its response, and exits — no TTY, no spinner, no prompt bar. The official docs call this the Agent SDK CLI.\n\n```bash\n# Minimal invocation — prints plain text to stdout\nclaude -p \"Summarize the changes in this PR\"\n\n# Pipe input; redirect output\ngit diff main | claude -p \"List every TODO introduced in this diff\" > todos.txt\n\n# JSON output — includes session_id, total_cost_usd, and per-model cost breakdown\nclaude -p \"Explain the auth module\" --output-format json | jq -r '.result'\n\n# Streaming NDJSON — one JSON object per line, suitable for real-time tailing\n# --verbose and --include-partial-messages are both required for token streaming\nclaude -p \"Refactor this function\" \\\n  --output-format stream-json \\\n  --verbose \\\n  --include-partial-messages\n```\n\n### Output formats\n\n| `--output-format` | What you get | When to use |\n|---|---|---|\n| `text` (default) | Raw assistant text to stdout | Shell scripts, human-readable logs |\n| `json` | JSON object with `result`, `session_id`, `total_cost_usd`, per-model cost breakdown | CI pipelines that need structured metadata |\n| `stream-json` | Newline-delimited JSON events as they arrive; requires `--verbose` | Long tasks where you need progress or token-streaming |\n\nThe `json` format is the most useful for scripting: `total_cost_usd` lets you track spend per invocation without hitting the dashboard. Extract fields with jq:\n\n```bash\nresult=$(claude -p \"...\" --output-format json)\necho \"Cost: $(echo \"$result\" | jq '.total_cost_usd')\"\necho \"$(echo \"$result\" | jq -r '.result')\"\n```\n\n### Structured output with a JSON Schema\n\nWhen you need machine-readable data, combine `--output-format json` with `--json-schema`. Claude completes its full agent loop first, then renders the final answer as validated JSON. The structured payload appears under `structured_output` (note: not at the top level — the top-level `result` field still contains the prose response):\n\n```bash\nclaude -p \"Extract all function names from auth.py\" \\\n  --output-format json \\\n  --json-schema '{\"type\":\"object\",\"properties\":{\"functions\":{\"type\":\"array\",\"items\":{\"type\":\"string\"}}},\"required\":[\"functions\"]}' \\\n  | jq '.structured_output'\n```\n\n### Streaming token-by-token\n\nTo surface tokens as they arrive — useful for long tasks or progress UIs — use `stream-json` with both `--verbose` and `--include-partial-messages`. Each line is a JSON event. Filter for text deltas:\n\n```bash\nclaude -p \"Write a poem\" \\\n  --output-format stream-json \\\n  --verbose \\\n  --include-partial-messages | \\\n  jq -rj 'select(.type == \"stream_event\" and .event.delta.type? == \"text_delta\") | .event.delta.text'\n```\n\n### Stdin cap\n\nAs of v2.1.128, piped stdin is capped at 10 MB. Exceeding it causes a clean error exit with a non-zero status. If your diff or log is larger, write it to a temp file and reference the path in the prompt."
   },
   {
    "heading": "Bare Mode: Deterministic, Fast, Isolated",
    "body": "By default, even a `-p` invocation loads the full context that an interactive session would: CLAUDE.md files at every directory level, skills, hooks, MCP servers from `.mcp.json`, auto-memory, and plugins. On a developer machine with a rich `~/.claude/` directory, this means CI behavior depends on whatever happens to be configured locally.\n\n`--bare` eliminates that coupling:\n\n```bash\nclaude --bare -p \"Run the linter and report failures\" \\\n  --allowedTools \"Bash(npm run lint),Read\"\n```\n\nIn bare mode, Claude skips auto-discovery of hooks, skills, plugins, MCP servers, auto memory, and CLAUDE.md. It loads only what you pass explicitly as flags. Claude still has access to Bash, file-read, and file-edit tools. Same query, same machine, same output — every time.\n\n**Authentication in bare mode:** Bare mode skips OAuth and keychain reads. You must supply credentials via one of:\n- `ANTHROPIC_API_KEY` environment variable\n- An `apiKeyHelper` command in a settings file passed via `--settings`\n- Cloud-provider environment variables (`CLAUDE_CODE_USE_BEDROCK`, `CLAUDE_CODE_USE_VERTEX`, `CLAUDE_CODE_USE_FOUNDRY`)\n\nThe `CLAUDE_CODE_OAUTH_TOKEN` long-lived subscription token is **not** read in bare mode.\n\n> Anthropic's own docs note that `--bare` will become the default for `-p` in a future release. Start using it now to be ahead of that change.\n\n### Explicit context injection in bare mode\n\n| What you need | Flag |\n|---|---|\n| Extra instructions | `--append-system-prompt \"...\"` or `--append-system-prompt-file ./rules.txt` |\n| Specific settings (including `apiKeyHelper`) | `--settings ./ci-settings.json` |\n| MCP servers | `--mcp-config ./mcp.json` |\n| A plugin | `--plugin-dir ./my-plugin` or `--plugin-url https://...` |\n| Custom agents | `--agents '{\"reviewer\":{\"description\":\"...\",\"prompt\":\"...\"}}'` |\n\n### Why bare mode matters for reproducibility\n\nWithout `--bare`, a hook in a teammate's `~/.claude/` or an MCP server in the project's `.mcp.json` silently changes what Claude does during your CI run. The only way to guarantee reproducible output across machines is to control all inputs explicitly — which is exactly what bare mode enforces."
   },
   {
    "heading": "CI Authentication: Subscription vs. Console API Billing",
    "body": "This is the most consequential setup decision for an org. Claude Code supports two completely different billing and auth models, and they are not interchangeable at the CI layer.\n\n### Authentication precedence (ordered)\n\nWhen multiple credentials are present, Claude Code picks one in this order:\n\n1. Cloud provider credentials (`CLAUDE_CODE_USE_BEDROCK`, `CLAUDE_CODE_USE_VERTEX`, or `CLAUDE_CODE_USE_FOUNDRY` is set)\n2. `ANTHROPIC_AUTH_TOKEN` — bearer-token header; use for LLM gateway / proxy routing\n3. `ANTHROPIC_API_KEY` — Anthropic Console API key; sent as `X-Api-Key`; direct API billing\n4. `apiKeyHelper` shell script — runs a command whose stdout becomes the API key; supports rotating/vault-sourced keys\n5. `CLAUDE_CODE_OAUTH_TOKEN` — long-lived subscription OAuth token from `claude setup-token`\n6. Interactive browser OAuth — subscription billing; not usable in CI\n\n**Critical order note:** `ANTHROPIC_API_KEY` (position 3) takes precedence over `apiKeyHelper` (position 4), which takes precedence over `CLAUDE_CODE_OAUTH_TOKEN` (position 5). This ordering catches many teams off guard — an `ANTHROPIC_API_KEY` set in the environment silently shadows the OAuth token they intended to use.\n\n### The two primary paths\n\n| | Subscription token (`CLAUDE_CODE_OAUTH_TOKEN`) | Console API key (`ANTHROPIC_API_KEY`) |\n|---|---|---|\n| **Billing** | Against your Pro/Max/Teams/Enterprise plan | Usage-based API billing via Console |\n| **Plan required** | Pro, Max, Teams, or Enterprise subscription | Console API access (no subscription needed) |\n| **Rate limits** | Governed by your subscription tier | Standard API rate limits |\n| **Token lifetime** | 1 year (regenerate with `claude setup-token`) | Permanent until rotated |\n| **Works in `--bare`** | No — bare mode skips OAuth reads | Yes |\n| **Cloud providers** | Not applicable | Not applicable (separate env var path) |\n\n### Generating the long-lived subscription token\n\nOn a machine with an active subscription, run once and copy the printed token. The command does **not** save it anywhere:\n\n```bash\n# Run interactively on your workstation — opens browser for OAuth\nclaude setup-token\n# Prints the token to terminal; copy it manually\n```\n\nStore it as a CI secret and inject it:\n\n```bash\nexport CLAUDE_CODE_OAUTH_TOKEN=\"$CLAUDE_CODE_SUBSCRIPTION_TOKEN_SECRET\"\nclaude -p \"Run the test suite\" --allowedTools \"Bash(npm test)\"\n```\n\n**Scope limitation:** this token is scoped to inference only — it cannot establish Remote Control sessions. If the subscription lapses or the seat is removed, every CI pipeline that uses this token breaks with an auth error.\n\n### Using an API key via Console\n\nFor teams that want per-project API keys with independent billing caps:\n\n```bash\nexport ANTHROPIC_API_KEY=\"$ANTHROPIC_API_KEY_SECRET\"\nclaude -p \"Review staged changes\" --output-format json\n```\n\nIn non-interactive (`-p`) mode, `ANTHROPIC_API_KEY` is always used when present — there is no approval prompt as there is in interactive mode. If you have both `ANTHROPIC_API_KEY` and `CLAUDE_CODE_OAUTH_TOKEN` set in CI, the API key wins (position 3 vs 5). To use subscription auth explicitly, `unset ANTHROPIC_API_KEY` before the call.\n\n### Dynamic credentials via `apiKeyHelper`\n\nFor orgs running a secrets manager or short-lived token infrastructure, configure `apiKeyHelper` in a settings file:\n\n```json\n// ci-settings.json\n{\n  \"apiKeyHelper\": \"vault kv get -field=value secret/anthropic/api-key\"\n}\n```\n\n```bash\nclaude --bare --settings ./ci-settings.json -p \"...\"\n```\n\nThe helper runs at session start, and again after 5 minutes or on HTTP 401. Set `CLAUDE_CODE_API_KEY_HELPER_TTL_MS` to override the 5-minute refresh window. If the script takes longer than 10 seconds, Claude Code displays a warning notice in the prompt bar showing the elapsed time — optimize the credential lookup if you see this regularly.\n\n**Note:** `apiKeyHelper` only applies to CLI terminal sessions. Claude Desktop and cloud sessions use OAuth exclusively and do not call `apiKeyHelper` or read API key environment variables."
   },
   {
    "heading": "Cost and Loop Bounding: Preventing Runaway Pipelines",
    "body": "An unguarded agentic run can iterate indefinitely if Claude encounters an ambiguous task, a flaky test, or a circular dependency. Two flags enforce hard limits:\n\n```bash\nclaude -p \"Fix all the type errors in the codebase\" \\\n  --max-turns 20 \\\n  --max-budget-usd 2.00 \\\n  --output-format json\n```\n\n### `--max-turns N`\n\nLimits the number of agent loop iterations (tool call + response cycles). When the limit is hit, Claude Code exits with an error and a non-zero status. This is the primary lever for preventing infinite loops. `--max-turns` is print-mode (`-p`) only — it has no effect in interactive sessions.\n\nCalibrate by task type:\n\n| Task type | Recommended `--max-turns` |\n|---|---|\n| Single-file read + summarize | 3–5 |\n| Lint a diff, write a commit message | 5–10 |\n| Fix failing tests (known-bounded) | 15–30 |\n| Open-ended refactor | 50+ (or omit and rely on cost cap) |\n\n### `--max-budget-usd AMOUNT`\n\nSets a dollar ceiling on API spend for the run. Claude Code tracks token usage against real-time pricing and exits cleanly when the budget is exhausted. Print-mode only.\n\nThe `--output-format json` response includes `total_cost_usd` so you can log actual spend per run:\n\n```bash\nresult=$(claude -p \"...\" \\\n  --max-budget-usd 1.00 \\\n  --max-turns 30 \\\n  --output-format json)\n\necho \"Spent: $(echo \"$result\" | jq '.total_cost_usd')\" >> ci-cost.log\n```\n\n### Exit codes and CI integration\n\nClaude Code exits non-zero when:\n- `--max-turns` is reached\n- `--max-budget-usd` is reached\n- An unrecoverable error occurs (auth failure, API error after retries)\n\nThis means your CI step fails loudly rather than succeeding silently with an incomplete result. If you want a budget cap to be a soft warning rather than a hard failure, capture the exit code explicitly:\n\n```bash\nif ! claude -p \"...\" --max-budget-usd 0.50 --max-turns 10; then\n  echo \"::warning::Claude run hit a limit — check cost log\"\n  exit 0  # Don't fail the CI job\nfi\n```\n\n### The cap interaction pitfall\n\n`--max-budget-usd` counts actual API spend; `--max-turns` counts loop iterations regardless of cost. An expensive single turn — one with a large context window, for example — can exhaust the budget before `--max-turns` is reached. Always set both: `--max-turns` for loop bounding, `--max-budget-usd` for spend bounding. Neither is a substitute for the other.\n\n### GitHub Actions-level concurrency control\n\nCost also scales with parallelism. Gate concurrent runs at the workflow level:\n\n```yaml\nconcurrency:\n  group: claude-review-${{ github.ref }}\n  cancel-in-progress: true\n```\n\nThis prevents a high-push-rate branch from stacking up multiple simultaneous Claude runs. Combine with `timeout-minutes` at the job level as a third layer in case Claude Code itself hangs before the agent loop starts:\n\n```yaml\njobs:\n  claude:\n    timeout-minutes: 15\n```"
   },
   {
    "heading": "Tool and MCP Scoping: Controlling the Agent's Reach",
    "body": "In CI, the default tool surface is too broad: Claude can run arbitrary Bash, write any file, and call every MCP server in the project config. Scoping is mandatory for safe automation.\n\nThere are three distinct mechanisms, each with different semantics:\n\n### 1. `--allowedTools` (alias `--allowed-tools`): auto-approve without prompting\n\nTools listed here execute without a permission prompt. The syntax supports exact names and scoped prefix-match patterns:\n\n```bash\n# Exact tool names\nclaude -p \"...\" --allowedTools \"Read,Edit\"\n\n# Pattern: allow only specific Bash prefixes\nclaude -p \"Create a commit\" \\\n  --allowedTools \"Bash(git diff *),Bash(git log *),Bash(git status *),Bash(git commit *),Read\"\n```\n\nThe trailing ` *` (space then asterisk) is significant: it enables prefix matching, so `Bash(git diff *)` allows any command starting with `git diff ` (with the space). Without the space, `Bash(git diff*)` would also match `git diff-index`, which is a different command.\n\nTools NOT in `--allowedTools` require a permission prompt. In `-p` mode, unapproved tool calls cause the run to abort — the process does not hang waiting for human input. To prevent a run from aborting on any unpermissioned tool, add `--permission-mode dontAsk`, which denies anything not in your allow rules rather than prompting:\n\n```bash\nclaude -p \"Apply lint fixes\" --permission-mode acceptEdits\n# acceptEdits: auto-approve file writes and common filesystem commands (mkdir, touch, mv, cp)\n# dontAsk: deny anything not explicitly allowed, never prompt\n```\n\n### 2. `--disallowedTools` (alias `--disallowed-tools`): two behaviors depending on syntax\n\nThis flag has two fundamentally different modes:\n\n```bash\n# Bare name: removes the tool from the model's context entirely.\n# Claude cannot see or call it — as if it doesn't exist.\n--disallowedTools \"Edit\"        # removes Edit\n--disallowedTools \"mcp__*\"      # removes ALL MCP tools\n--disallowedTools \"*\"           # removes every tool (read-only mode)\n\n# Scoped pattern: leaves the tool available, but denies specific calls.\n# Claude can see the tool but the call is blocked if it matches the pattern.\n--disallowedTools \"Bash(rm *)\"\n--disallowedTools \"Bash(git push *)\"\n```\n\nThe bare-name form is the safer choice for CI: the model never considers calling the tool. The scoped form is useful when you want most Bash available but need to fence off destructive subcommands.\n\n### 3. `--tools`: restrict to a named set\n\nThe most restrictive built-in option — replaces the set of available built-in tools:\n\n```bash\n# Claude can only use Bash, Read, and Edit — no WebFetch, no Write\nclaude -p \"...\" --tools \"Bash,Read,Edit\"\n\n# Disable all built-in tools\nclaude -p \"...\" --tools \"\"\n\n# MCP tools are NOT affected by --tools; to also deny them:\nclaude -p \"...\" --tools \"Read\" --disallowedTools \"mcp__*\"\n# OR: don't load MCP servers at all:\nclaude --bare -p \"...\" --tools \"Read\"   # bare skips .mcp.json discovery\n```\n\n### MCP server scoping\n\nBy default, Claude Code loads MCP servers from all discovered configs (user `~/.claude/`, project `.mcp.json`, etc.). For CI you want only the servers you explicitly provision:\n\n```bash\n# Load ONLY the servers in this file, ignore all others\nclaude --bare \\\n  --strict-mcp-config \\\n  --mcp-config ./ci-mcp.json \\\n  -p \"...\" \\\n  --allowedTools \"mcp__my_tool__action\"\n```\n\n`--strict-mcp-config` ignores every MCP configuration source other than `--mcp-config`. The `ci-mcp.json` file uses the standard MCP config format:\n\n```json\n{\n  \"mcpServers\": {\n    \"my_tool\": {\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"@company/my-mcp-server\"],\n      \"env\": {\n        \"MY_TOOL_API_KEY\": \"${MY_TOOL_API_KEY}\"\n      }\n    }\n  }\n}\n```\n\n### `--permission-prompt-tool`: custom approval logic\n\nFor advanced pipelines where you want per-call dynamic approval — such as routing approval to a Slack message — specify an MCP tool that handles the decision:\n\n```bash\nclaude -p \"...\" \\\n  --mcp-config ./mcp.json \\\n  --permission-prompt-tool mcp__approver__check_permission\n```\n\nWhen Claude wants to call a tool not already allowed, it invokes `mcp__approver__check_permission` with the proposed call details. The MCP tool returns allow/deny. This pattern lets you build audit-logged, policy-driven approval gates without human interaction.\n\n### Decision table: which mechanism to use\n\n| Goal | Mechanism |\n|---|---|\n| Auto-approve a known-safe tool | `--allowedTools \"ToolName\"` |\n| Block specific destructive commands | `--disallowedTools \"Bash(rm -rf *)\"` |\n| Remove a tool from the model's view entirely | `--disallowedTools \"ToolName\"` (bare name) |\n| Restrict to an explicit built-in whitelist | `--tools \"Bash,Read,Edit\"` |\n| Suppress all MCP tools | `--disallowedTools \"mcp__*\"` |\n| Load only specific MCP servers | `--strict-mcp-config --mcp-config ./ci.json` |\n| Never prompt; deny anything not allowed | `--permission-mode dontAsk` |\n| Dynamic per-call approval | `--permission-prompt-tool mcp__approver__fn` |"
   },
   {
    "heading": "Managed Code Review Service: The GitHub App",
    "body": "The managed Code Review service (Team and Enterprise plans only; currently in research preview) is a completely different product from GitHub Actions integration. It runs on Anthropic's own infrastructure, not on your GitHub runners, and is triggered by the Claude GitHub App rather than a workflow file.\n\n### What it is and what it is not\n\n- It analyzes PRs using a **fleet of specialized agents in parallel**, each focused on a different class of issue (logic errors, security, regressions, edge cases)\n- A verification step filters candidates against actual code behavior to reduce false positives before posting\n- Findings are posted as **inline comments on the specific diff lines** where issues were found, and also as annotations in the **Files changed** tab and as a severity summary in the check run Details\n- The check run always completes with a **neutral conclusion** — it never approves, blocks, or fails your PR via branch protection rules\n- It is **billed separately via usage credits** (not against your plan's included usage) at roughly $15–25 per review, scaling with PR size and codebase complexity\n- It is **not available** for orgs with Zero Data Retention enabled\n- Reviews complete in an average of 20 minutes\n\n### Severity levels\n\nEach finding is tagged with one of three severity markers:\n\n| Marker | Level | Meaning |\n|---|---|---|\n| 🔴 | Important | A bug that should be fixed before merging |\n| 🟡 | Nit | A minor issue, worth fixing but not blocking |\n| 🟣 | Pre-existing | A bug that exists in the codebase but was not introduced by this PR |\n\n### Setup and trigger configuration\n\nAn admin enables it once at `claude.ai/admin-settings/claude-code` and installs the Claude GitHub App. Per-repo, you choose one of three trigger modes:\n\n| Mode | When reviews run | Cost profile |\n|---|---|---|\n| Once after PR creation | On open or ready-for-review | Lowest — one review per PR |\n| After every push | On each push to the PR branch | Scales with push frequency |\n| Manual | Only when someone comments `@claude review` or `@claude review once` | Zero unless requested |\n\n**Manual trigger commands** (top-level PR comments only; you must have owner, member, or collaborator access):\n- `@claude review` — starts a review AND subscribes the PR to push-triggered reviews going forward\n- `@claude review once` — starts a single review without subscribing; use for one-off checks on long-running PRs\n\n**Important:** `@claude review` (without `once`) subscribes the PR to a review on every subsequent push. On a high-push PR, this can run a $15–25 review on every commit. Use `@claude review once` for spot checks; only use the subscribing form when you want continuous per-push coverage.\n\n### Reading severity programmatically\n\nThe check run output exposes machine-readable severity counts. The last line of the Details text is parseable with `gh` and jq:\n\n```bash\ngh api repos/OWNER/REPO/check-runs/CHECK_RUN_ID \\\n  --jq '.output.text | split(\"bughunter-severity: \")[1] | split(\" -->\")[0] | fromjson'\n# Returns: {\"normal\": 2, \"nit\": 1, \"pre_existing\": 0}\n```\n\n`normal` = count of Important findings, `nit` = count of Nit findings, `pre_existing` = count of Pre-existing findings. If you want to gate merges on Important findings, read this JSON in your own CI and exit non-zero when `normal > 0`. The managed service itself will not block the merge.\n\n### Spend cap\n\nSet a monthly cap at `claude.ai/admin-settings/usage` for the Claude Code Review service. When the cap is reached, the service posts a comment on the PR explaining the skip; reviews resume at the next billing period or immediately when an admin raises the cap."
   },
   {
    "heading": "REVIEW.md: Tuning the Managed Reviewer",
    "body": "The Code Review service reads two files from your repo root:\n\n- **`CLAUDE.md`**: shared project instructions for all Claude Code tasks. The reviewer treats newly introduced violations as nit-level findings. If your PR changes code in a way that makes a `CLAUDE.md` statement outdated, Claude flags that the docs need updating too. `CLAUDE.md` files at every level of your directory hierarchy are read; subdirectory rules apply only to files under that path.\n- **`REVIEW.md`**: review-only instructions. Contents are **injected verbatim** as the highest-priority instruction block into every agent in the review pipeline, taking precedence over the default review guidance.\n\n`REVIEW.md` is plain markdown — `@` import syntax is **not** expanded and referenced files are not read into the prompt. Put the rules you want enforced directly in the file.\n\n### What you can tune with REVIEW.md\n\n**Severity calibration**: redefine what 🔴 Important means for your repo. The default targets production correctness; a docs repo or prototype might want a narrower definition. State explicitly which finding classes are Important and which are Nit at most.\n\n**Nit volume cap**: a rule like \"report at most five Nits per review, say 'plus N similar items' in the summary for the rest\" keeps reviews actionable on large PRs.\n\n**Skip rules**: list paths, branch patterns, and finding categories where Claude should post no findings — generated files under `src/gen/`, lockfiles, machine-authored branches matching `renovate/*`, anything your CI already enforces.\n\n**Repo-specific checks**: rules like \"new API routes must have an integration test\" are injected as highest priority, so they land more reliably than the same rule buried in a long `CLAUDE.md`.\n\n**Re-review convergence**: \"after the second review of a PR, suppress new Nits and post Important findings only\" stops a one-line fix from reaching round seven on style.\n\n### Effective REVIEW.md example\n\n```markdown\n# Review instructions\n\n## What Important means here\nReserve 🔴 Important for: incorrect logic, unscoped database queries,\nPII in logs or error messages, and migrations that aren't backward-compatible.\nStyle, naming, and refactoring suggestions are 🟡 Nit at most.\n\n## Cap the nits\nReport at most five Nits per review. Say \"plus N similar items\" in the\nsummary for the rest. If everything you found is a Nit, lead the summary\nwith \"No blocking issues.\"\n\n## Do not report\n- Anything CI already enforces: lint, formatting, type errors, test coverage\n- Generated files under `src/gen/` and any `*.lock` file\n- Test-only code that intentionally violates production-style rules\n- Machine-authored branches matching `renovate/*` or `dependabot/*`\n\n## Always check\n- New API routes have an integration test in `tests/integration/`\n- Log lines don't include email addresses, user IDs, or request bodies\n- Database queries are scoped to the caller's tenant_id\n\n## Re-review behavior\nAfter the second review of a PR, suppress new Nits. Post Important findings only.\n\n## Summary shape\nOpen the review body with: \"N blocking, M nits\" (or \"No blocking issues\").\n```\n\n### What not to put in REVIEW.md\n\n- General project conventions that apply to interactive sessions too (those belong in `CLAUDE.md`)\n- Long narratives — length dilutes the rules that matter; keep it to behavior-changing instructions\n- Rules your linter already enforces — duplicate coverage creates noise without value\n\n### CLAUDE.md vs REVIEW.md precedence\n\n`REVIEW.md` wins for review behavior. `CLAUDE.md` provides project context and generates nit-level findings for newly introduced violations. If the same rule appears in both files with conflicting instructions, the `REVIEW.md` version takes effect during reviews because it is injected as the highest-priority instruction block."
   },
   {
    "heading": "GitHub Actions Integration: anthropics/claude-code-action@v1",
    "body": "The GitHub Actions integration runs Claude Code on GitHub-hosted runners (not on Anthropic infrastructure). You control the hardware, the secrets, and the workflow triggers. This is the path for custom automation beyond periodic PR review.\n\n### Quickest route: `/install-github-app`\n\nIn an interactive Claude Code session in the repo:\n\n```\n/install-github-app\n```\n\nThis installs the Anthropic Claude GitHub App, creates `ANTHROPIC_API_KEY` as a repo secret, and scaffolds the workflow file. Requires repo admin access. Only works for direct API users — Bedrock/Vertex users must configure manually.\n\n### Minimal workflow: respond to @claude mentions\n\n```yaml\n# .github/workflows/claude.yml\nname: Claude Code\non:\n  issue_comment:\n    types: [created]\n  pull_request_review_comment:\n    types: [created]\n\njobs:\n  claude:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: anthropics/claude-code-action@v1\n        with:\n          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}\n          # Omit 'prompt' — action auto-responds to @claude mentions in comments\n```\n\nWith this configuration, any `@claude <instruction>` in a PR or issue comment triggers Claude to analyze the context and respond.\n\n### Scheduled automation with a prompt\n\n```yaml\nname: Daily Commit Summary\non:\n  schedule:\n    - cron: \"0 9 * * 1-5\"\njobs:\n  summarize:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: anthropics/claude-code-action@v1\n        with:\n          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}\n          prompt: \"Summarize commits from the last 24 hours and open issues\"\n          claude_args: |\n            --model claude-sonnet-4-6\n            --max-turns 10\n            --max-budget-usd 1.00\n```\n\n### Action parameters (v1)\n\n| Parameter | Required | Description |\n|---|---|---|\n| `prompt` | No* | Instructions for Claude (plain text or a `/skill-name`); omit to respond to `@claude` trigger phrase | \n| `claude_args` | No | Any CLI flags passed verbatim to `claude -p` |\n| `anthropic_api_key` | Yes** | Anthropic API key from console.anthropic.com |\n| `github_token` | No | GitHub token for API access (defaults to `GITHUB_TOKEN`) |\n| `trigger_phrase` | No | Override trigger phrase (default: `@claude`) |\n| `use_bedrock` | No | Route to Amazon Bedrock instead of direct API |\n| `use_vertex` | No | Route to Google Vertex AI instead of direct API |\n| `plugin_marketplaces` | No | Newline-separated Git URLs for plugin marketplaces |\n| `plugins` | No | Newline-separated plugin names to install before execution |\n\n*`prompt` is optional when using issue/PR comment triggers — Claude responds to the `@claude` mention automatically.\n**Not required when using Bedrock or Vertex.\n\nAll Claude Code CLI flags pass through `claude_args`. The v1 mapping from beta:\n\n```yaml\n# Old beta (do not use):\nclaude_code_action@beta:\n  mode: \"tag\"                          # removed — auto-detected\n  direct_prompt: \"...\"                 # → prompt\n  custom_instructions: \"...\"           # → claude_args: --append-system-prompt\n  max_turns: \"10\"                      # → claude_args: --max-turns 10\n  model: \"claude-sonnet-4-6\"           # → claude_args: --model claude-sonnet-4-6\n  allowed_tools: \"Read,Edit\"           # → claude_args: --allowedTools\n  disallowed_tools: \"Write\"            # → claude_args: --disallowedTools\n\n# v1:\nclaude_code_action@v1:\n  prompt: \"Review this PR for security issues\"\n  anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}\n  claude_args: |\n    --append-system-prompt \"Follow our coding standards\"\n    --max-turns 10\n    --model claude-sonnet-4-6\n```\n\n### Invoking a skill from a workflow\n\nSkills in `.claude/skills/` are accessible in Actions. Check out the repo first, then pass the skill name as the prompt:\n\n```yaml\nsteps:\n  - uses: actions/checkout@v4\n  - uses: anthropics/claude-code-action@v1\n    with:\n      anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}\n      prompt: \"/security-review\"\n      claude_args: \"--max-turns 20\"\n```\n\nFor a skill packaged in a plugin, use `plugin_marketplaces` and `plugins`:\n\n```yaml\n- uses: anthropics/claude-code-action@v1\n  with:\n    anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}\n    plugin_marketplaces: \"https://github.com/anthropics/claude-code.git\"\n    plugins: \"code-review@claude-code-plugins\"\n    prompt: \"/code-review:code-review ${{ github.repository }}/pull/${{ github.event.pull_request.number }}\"\n```\n\n### Cost controls at the workflow level\n\nLayer three independent guards:\n\n```yaml\nconcurrency:\n  group: claude-${{ github.ref }}\n  cancel-in-progress: true  # cancel stale runs when a new push arrives\n\njobs:\n  claude:\n    timeout-minutes: 15    # hard ceiling on runner time (catches hangs before agent loop starts)\n    steps:\n      - uses: anthropics/claude-code-action@v1\n        with:\n          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}\n          claude_args: \"--max-turns 15 --max-budget-usd 2.00\"\n          # --max-turns: loop bound | --max-budget-usd: spend bound | timeout-minutes: wall-clock bound\n```"
   },
   {
    "heading": "Bedrock and Vertex Authentication in Actions",
    "body": "For teams routing through their own cloud infrastructure, the Actions integration supports Bedrock and Vertex via OIDC — no long-lived cloud credentials stored as secrets. Because you are not using Anthropic's direct API, you must create a custom GitHub App (or use an existing one) rather than installing the Anthropic Claude GitHub App; the `/install-github-app` command is not available for this path.\n\n### Amazon Bedrock (OIDC)\n\nPrerequisites: Bedrock enabled with Claude model access, GitHub OIDC identity provider registered in AWS (`https://token.actions.githubusercontent.com`), IAM role with `AmazonBedrockFullAccess` that trusts `token.actions.githubusercontent.com`.\n\n```yaml\nname: Claude PR Action\n\npermissions:\n  id-token: write   # required for OIDC token exchange\n  contents: write\n  pull-requests: write\n  issues: write\n\non:\n  issue_comment:\n    types: [created]\n\njobs:\n  claude-pr:\n    if: contains(github.event.comment.body, '@claude')\n    runs-on: ubuntu-latest\n    env:\n      AWS_REGION: us-west-2\n    steps:\n      - uses: actions/checkout@v4\n\n      - name: Generate GitHub App token\n        id: app-token\n        uses: actions/create-github-app-token@v2\n        with:\n          app-id: ${{ secrets.APP_ID }}\n          private-key: ${{ secrets.APP_PRIVATE_KEY }}\n\n      - name: Configure AWS Credentials (OIDC)\n        uses: aws-actions/configure-aws-credentials@v4\n        with:\n          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}\n          aws-region: us-west-2\n\n      - uses: anthropics/claude-code-action@v1\n        with:\n          github_token: ${{ steps.app-token.outputs.token }}\n          use_bedrock: \"true\"\n          claude_args: '--model us.anthropic.claude-sonnet-4-6 --max-turns 10'\n```\n\nNote the Bedrock model ID format: `us.anthropic.claude-sonnet-4-6` (region prefix required). There is no `anthropic_api_key` parameter when using Bedrock — omit it entirely.\n\n### Google Vertex AI (Workload Identity Federation)\n\nPrerequisites: Vertex AI API enabled, IAM Credentials API and STS API enabled, Workload Identity Pool configured for GitHub (`https://token.actions.githubusercontent.com`), service account with `Vertex AI User` role.\n\n```yaml\nname: Claude PR Action\n\npermissions:\n  id-token: write\n  contents: write\n  pull-requests: write\n  issues: write\n\non:\n  issue_comment:\n    types: [created]\n\njobs:\n  claude-pr:\n    if: contains(github.event.comment.body, '@claude')\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n\n      - name: Generate GitHub App token\n        id: app-token\n        uses: actions/create-github-app-token@v2\n        with:\n          app-id: ${{ secrets.APP_ID }}\n          private-key: ${{ secrets.APP_PRIVATE_KEY }}\n\n      - name: Authenticate to Google Cloud\n        id: auth\n        uses: google-github-actions/auth@v2\n        with:\n          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}\n          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}\n\n      - uses: anthropics/claude-code-action@v1\n        with:\n          github_token: ${{ steps.app-token.outputs.token }}\n          use_vertex: \"true\"\n          claude_args: '--model claude-sonnet-4-5@20250929 --max-turns 10'\n        env:\n          ANTHROPIC_VERTEX_PROJECT_ID: ${{ steps.auth.outputs.project_id }}\n          CLOUD_ML_REGION: us-east5\n          VERTEX_REGION_CLAUDE_4_5_SONNET: us-east5\n```\n\nThree environment variables are needed for Vertex: `ANTHROPIC_VERTEX_PROJECT_ID` (the GCP project ID, automatically available from the auth step's output), `CLOUD_ML_REGION`, and the model-specific region variable (`VERTEX_REGION_CLAUDE_4_5_SONNET` for Sonnet 4.5). The project ID is retrieved automatically from the auth step — no hardcoding needed.\n\n**Why OIDC and Workload Identity Federation over static credentials:** Both approaches issue temporary credentials per-run that expire automatically. No secrets are stored in GitHub that could be leaked, rotated manually, or left stale after an employee leaves."
   },
   {
    "heading": "System Prompt Layering vs. Replacement",
    "body": "Every automated Claude Code invocation makes an implicit choice about the system prompt. The default prompt includes tool guidance (how to use Bash, Read, Edit safely), safety instructions, and Claude Code's coding conventions. When you automate, you can extend it or replace it entirely. All four system-prompt flags work in both interactive and non-interactive (`-p`) modes.\n\n### Four flags, two modes\n\n```bash\n# APPEND: keep the default prompt, add your rules at the end\nclaude -p \"Review this diff\" \\\n  --append-system-prompt \"You are a security engineer. Flag any hardcoded credentials.\"\n\n# APPEND from file (cleaner for long rule sets)\nclaude -p \"...\" --append-system-prompt-file ./review-rules.txt\n\n# REPLACE: discard the default prompt entirely\nclaude -p \"Generate a changelog entry\" \\\n  --system-prompt \"Output only a single bullet point in past tense. No preamble.\"\n\n# REPLACE from file\nclaude -p \"...\" --system-prompt-file ./custom-identity.txt\n```\n\n`--system-prompt` and `--system-prompt-file` are mutually exclusive (both replace the default; you cannot use both at once). Either append flag can be combined with either replace flag — the append content is added after the replacement.\n\n### When to append vs. replace\n\n| Scenario | Use | Reason |\n|---|---|---|\n| Add per-pipeline rules to a coding agent | `--append-system-prompt` | Keeps tool guidance and safety; your rules layer on top |\n| Add repo-specific coding standards | `--append-system-prompt-file` | Same — you want Claude to remain a coding assistant |\n| Build a non-coding pipeline agent | `--system-prompt` | The default coding identity is wrong; you own the full prompt |\n| Strict output format (no prose, just JSON) | `--system-prompt` | Default conventions encourage explanation; replace suppresses them |\n| Security review persona on top of default | `--append-system-prompt` | Tool knowledge still needed; persona is additive |\n\n**The replacement footgun:** when you replace the system prompt, you drop all default tool guidance and safety instructions. Claude may still call tools (you control availability via `--tools` and `--allowedTools`), but it has no guidance about when that is appropriate or safe. If your replacement prompt drives an agentic workflow with Bash access, you need to include your own tool constraints and safety rules explicitly.\n\n**Persistent personas:** these flags apply only to the current invocation. For persistent personas you can switch between and share across a project, use output styles. For project conventions Claude should always follow, use `CLAUDE.md`.\n\n### Prompt caching optimization\n\nThe default system prompt contains per-machine dynamic sections (working directory, environment info, memory paths, git-repo flag). These sections vary per machine, which breaks prompt-cache reuse in multi-machine CI environments. Use `--exclude-dynamic-system-prompt-sections` to move those sections to the first user message instead:\n\n```bash\nclaude -p \\\n  --exclude-dynamic-system-prompt-sections \\\n  --output-format json \\\n  \"Analyze this codebase\"\n```\n\nThis flag only applies when using the default system prompt — it is silently ignored when `--system-prompt` or `--system-prompt-file` is set (there are no dynamic sections to move in that case). For high-volume pipelines with many identical runs across machines, this can meaningfully reduce API costs by enabling cache hits on the system prompt."
   },
   {
    "heading": "Putting It Together: A Full CI Pipeline",
    "body": "A realistic team setup combines all the above into layered automation. Here is a concrete layout:\n\n### Repository structure\n\n```\n.github/\n  workflows/\n    claude-on-mention.yml      # @claude trigger for PR/issue comments\n    claude-pr-summary.yml      # automatic summary on PR open\nCLAUDE.md                      # project conventions (all sessions)\nREVIEW.md                      # review-only tuning (managed service)\n.claude/\n  settings.json                # org policy via managed settings\n  skills/\n    security-review.md         # invocable as /security-review\nci/\n  mcp-ci.json                  # MCP servers for CI only\n  ci-settings.json             # CI-specific settings overrides\n```\n\n### On-mention workflow (the interactive layer)\n\n```yaml\n# .github/workflows/claude-on-mention.yml\nname: Claude Code (on mention)\non:\n  issue_comment:\n    types: [created]\n  pull_request_review_comment:\n    types: [created]\n  issues:\n    types: [opened]\n\njobs:\n  claude:\n    if: |\n      contains(github.event.comment.body, '@claude') ||\n      contains(github.event.issue.body, '@claude')\n    runs-on: ubuntu-latest\n    permissions:\n      contents: write\n      pull-requests: write\n      issues: write\n    concurrency:\n      group: claude-mention-${{ github.event.issue.number || github.event.pull_request.number }}\n      cancel-in-progress: false   # queue, don't cancel — each mention is intentional\n    steps:\n      - uses: actions/checkout@v4\n      - uses: anthropics/claude-code-action@v1\n        with:\n          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}\n          claude_args: |\n            --max-turns 25\n            --max-budget-usd 3.00\n            --allowedTools \"Bash(git *),Read,Edit,Write\"\n            --disallowedTools \"Bash(rm *),Bash(git push --force *)\"\n```\n\n### Scripted one-shot (no Actions, pure CLI)\n\n```bash\n#!/usr/bin/env bash\n# ci/run-review.sh — run from any CI system\nset -euo pipefail\n\nexport ANTHROPIC_API_KEY=\"${ANTHROPIC_API_KEY:?must be set}\"\n\nresult=$(claude --bare \\\n  -p \"Review the staged changes for security issues and output a JSON list of findings\" \\\n  --settings ./ci/ci-settings.json \\\n  --mcp-config ./ci/mcp-ci.json \\\n  --strict-mcp-config \\\n  --output-format json \\\n  --max-turns 10 \\\n  --max-budget-usd 0.50 \\\n  --tools \"Read,Bash\" \\\n  --allowedTools \"Bash(git diff *),Bash(git log *),Read\" \\\n  --json-schema '{\"type\":\"array\",\"items\":{\"type\":\"object\",\"properties\":{\"file\":{\"type\":\"string\"},\"severity\":{\"type\":\"string\"},\"description\":{\"type\":\"string\"}}}}')\n\necho \"Cost: $(echo \"$result\" | jq '.total_cost_usd')\" >> \"${CI_COST_LOG:-/dev/null}\"\necho \"$result\" | jq '.structured_output'\n```\n\n### Common pitfalls\n\n**Hanging or aborting runs from unapproved tools:** In `-p` mode, a tool call that requires a permission prompt and is not covered by `--allowedTools` will abort the run (not hang — print mode does not wait for human input). Audit which tools your prompts actually invoke, or add `--permission-mode dontAsk` to deny anything not explicitly allowed.\n\n**`CLAUDE_CODE_OAUTH_TOKEN` not working in bare mode:** bare mode skips OAuth. Switch to `ANTHROPIC_API_KEY` for scripted bare calls, or drop `--bare` if you need the subscription token. Check with `claude auth status` to see which credential is active.\n\n**Stacked managed reviews:** commenting `@claude review` (without `once`) subscribes the PR to push-triggered reviews. On a high-push PR, this can run a $15–25 review on every commit. Use `@claude review once` for spot checks; only use the subscribing form when you want continuous per-push coverage.\n\n**Auth precedence surprises:** `ANTHROPIC_API_KEY` (position 3) takes precedence over `CLAUDE_CODE_OAUTH_TOKEN` (position 5). If both are set in your environment, the API key wins. Run `unset ANTHROPIC_API_KEY` and confirm with `claude auth status` if subscription auth isn't working.\n\n**Cost cap vs. max-turns:** `--max-budget-usd` counts actual API spend; `--max-turns` counts loop iterations regardless of cost. An expensive single turn (large context window) can exhaust the budget before `--max-turns` is reached. Use both.\n\n**System prompt replacement drops tool safety:** if you use `--system-prompt` to give Claude a custom persona and then allow Bash, Claude has no default guidance about safe Bash usage. Either append instead of replace, or explicitly include safe-tool instructions in your custom prompt.\n\n**Beta → v1 migration:** the `mode`, `direct_prompt`, `custom_instructions`, `max_turns`, `model`, `allowed_tools`, and `disallowed_tools` action inputs from the beta version are all removed in v1. Everything goes through `claude_args` as CLI flags. See the beta-to-v1 migration table in the GitHub Actions section."
   }
  ],
  "preQuiz": [
   {
    "prompt": "You want to run Claude Code in a GitHub Actions workflow using your team's Anthropic Console API billing (not a subscription). Which command do you use inside the CI step to authenticate?",
    "options": [
     "claude setup-token",
     "claude auth login --console",
     "claude auth status --json",
     "claude auth login --sso"
    ],
    "correct": 1,
    "sectionIndices": [
     0
    ],
    "explanation": "`claude auth login --console` signs in with Anthropic Console credentials, routing billing through API usage rather than a subscription. `setup-token` mints a long-lived OAuth token for subscription billing, not Console API billing."
   },
   {
    "prompt": "Your CI pipeline runs `claude -p 'audit this diff'` but it keeps hitting runaway cost on large PRs. Which two flags together cap both the number of agentic turns AND the dollar spend?",
    "options": [
     "--max-turns N and --max-budget-usd N",
     "--safe-mode and --bare",
     "--permission-mode plan and --output-format json",
     "--no-session-persistence and --effort low"
    ],
    "correct": 0,
    "sectionIndices": [
     0
    ],
    "explanation": "`--max-turns` caps the number of agentic turns (errors when reached), and `--max-budget-usd` caps dollar spend before stopping. The other options affect permissions or startup behavior, not cost/loop control."
   },
   {
    "prompt": "You pass `--allowedTools 'Bash(git log *)'` to your headless claude invocation. A team member assumes this means Claude cannot call any other Bash commands. What actually happens?",
    "options": [
     "Claude can only run `git log` commands; all other Bash calls are blocked",
     "`--allowedTools` auto-approves matching tools without prompting but does NOT restrict which tools are available — other Bash patterns can still be called",
     "Claude errors out if it tries any Bash command not in the allowed list",
     "The flag is only valid in interactive mode, so it is silently ignored in headless mode"
    ],
    "correct": 1,
    "sectionIndices": [
     0
    ],
    "explanation": "`--allowedTools` auto-approves matching calls without a permission prompt but does NOT restrict availability. To remove a tool from context entirely, use `--disallowedTools` with a bare tool name."
   },
   {
    "prompt": "You want to pin your team's CI invocations to a fixed set of MCP servers and prevent any user-level MCP config from leaking in. Which combination of flags achieves this?",
    "options": [
     "--disallowedTools 'mcp__*' and --mcp-config team.json",
     "--mcp-config team.json and --strict-mcp-config",
     "--bare and --mcp-config team.json",
     "--tools '' and --mcp-config team.json"
    ],
    "correct": 1,
    "sectionIndices": [
     0
    ],
    "explanation": "`--mcp-config team.json` loads the specified servers, and `--strict-mcp-config` ignores all other (user-level) MCP configuration, ensuring only the pinned set is active."
   },
   {
    "prompt": "Your CI script passes `--system-prompt-file pipeline-rules.txt` to Claude. A colleague warns this could be dangerous. What is the specific risk?",
    "options": [
     "The flag only works in interactive mode, so it is silently ignored in CI",
     "`--system-prompt-file` performs a full replacement of the default system prompt, dropping ALL default tool guidance and safety instructions — you own safety entirely",
     "It conflicts with `--append-system-prompt` and causes Claude to error",
     "The flag adds the file contents on top of the default prompt, potentially doubling token usage"
    ],
    "correct": 1,
    "sectionIndices": [
     0
    ],
    "explanation": "`--system-prompt` / `--system-prompt-file` fully replace the default system prompt, removing all built-in tool guidance and safety instructions. Use `--append-system-prompt` instead if you want to add rules while keeping the defaults."
   },
   {
    "prompt": "Your organization uses `--exclude-dynamic-system-prompt-sections` with `-p` for shared scripted workloads. What specific problem does this flag solve?",
    "options": [
     "It prevents the model from reading CLAUDE.md files in subdirectories",
     "It moves per-machine sections (cwd, env, memory paths, git-repo flag) into the first user message so the system prompt is identical across machines, enabling prompt-cache reuse",
     "It strips tool descriptions from the system prompt to reduce token count",
     "It disables all dynamic tool approvals, equivalent to --permission-mode plan"
    ],
    "correct": 1,
    "sectionIndices": [
     0
    ],
    "explanation": "Per-machine sections like cwd and memory paths vary across runners, busting the prompt cache. Moving them to the first user message keeps the system prompt byte-identical across machines, preserving cache hits."
   },
   {
    "prompt": "The managed Code Review service posts a PR comment but the `Claude Code Review` check run shows 'neutral'. Your branch protection rule requires the check to pass before merging. What happens?",
    "options": [
     "The PR is blocked — a neutral check counts as a failure under branch protection",
     "Code Review never approves or blocks — the check always completes neutral and cannot gate merges via branch protection; you must parse the machine-readable check output yourself",
     "The PR auto-merges because neutral means no Important findings were found",
     "Branch protection ignores neutral checks, so the PR merges freely regardless of findings"
    ],
    "correct": 1,
    "sectionIndices": [
     1
    ],
    "explanation": "The `Claude Code Review` check always completes neutral by design. It cannot gate merges via branch protection. To build a gate, you must parse the machine-readable last line of the check run output (the `bughunter-severity` JSON) and fail the CI step yourself."
   },
   {
    "prompt": "You configure a high-traffic repo to use 'After every push' review mode. A PR author opens a draft PR and pushes 15 commits while iterating. How does the managed Code Review service handle this?",
    "options": [
     "All 15 pushes trigger separate reviews, costing roughly $225–$375 total",
     "Automatic triggers do not run on draft PRs, so no reviews fire; only `@claude review` in a top-level comment would trigger one",
     "Only the first push triggers a review; subsequent pushes are queued and skipped",
     "Reviews run on draft PRs only for the 'After every push' mode, not for 'Manual' mode"
    ],
    "correct": 1,
    "sectionIndices": [
     1
    ],
    "explanation": "Automatic triggers (including 'After every push') do not run on draft PRs. Manual triggers (`@claude review`) do run on drafts. This is the mechanism to avoid burning budget on in-progress work."
   },
   {
    "prompt": "A developer posts `@claude review` as an inline comment on a specific diff line in a PR. Will this trigger a Code Review?",
    "options": [
     "Yes — inline comments on diff lines are the intended way to trigger targeted reviews",
     "No — manual triggers must be top-level PR comments with the command at the start of the comment; inline diff comments are not recognized",
     "Yes, but only if the developer has write access to the repository",
     "No — `@claude review` only works in the PR description, not in comments"
    ],
    "correct": 1,
    "sectionIndices": [
     1
    ],
    "explanation": "Manual triggers must be top-level PR comments (not inline on a diff line) with the command at the start of the comment. Inline diff comments are not recognized as trigger commands."
   },
   {
    "prompt": "You want Code Review to stop flagging auto-generated migration files and to report at most three nits per review. Where do you put these rules, and why?",
    "options": [
     "In CLAUDE.md at the repo root, because it is read as the highest-priority system prompt override",
     "In a repo-root REVIEW.md, because it is injected verbatim into every review agent's system prompt as highest-priority instructions and lands more reliably than rules buried in CLAUDE.md",
     "In a `.github/claude-review.yml` config file that the GitHub App reads before each review",
     "In the PR description using a `<!-- claude-config -->` HTML comment block"
    ],
    "correct": 1,
    "sectionIndices": [
     1
    ],
    "explanation": "REVIEW.md at the repo root is injected verbatim into every review agent's system prompt as highest-priority instructions. CLAUDE.md is also read as project context, but REVIEW.md is the right place for review-behavior changes and lands more reliably."
   },
   {
    "prompt": "Your organization's Anthropic bill shows Code Review charges even though you process all other Claude usage through Bedrock. Why?",
    "options": [
     "Bedrock does not support Code Review, so the service automatically falls back to direct Anthropic billing",
     "Code Review is billed via usage credits separately and appears on the Anthropic bill even if the org uses Bedrock or Vertex for other Claude usage",
     "This is a billing error — Code Review should route through whichever provider the org uses",
     "The GitHub App always bills the PR author's personal Anthropic account, not the org account"
    ],
    "correct": 1,
    "sectionIndices": [
     1
    ],
    "explanation": "Managed Code Review billing is separate from included plan usage and appears on the Anthropic bill regardless of whether the org routes other Claude traffic through Bedrock or Vertex."
   },
   {
    "prompt": "A team sets `--enable-auto-mode` in their CI script after upgrading to Claude Code v2.1.120. What happens?",
    "options": [
     "The flag enables automatic permission acceptance for all tool calls",
     "`--enable-auto-mode` was removed in v2.1.111; the correct flag is `--permission-mode auto`",
     "The flag is silently ignored and Claude defaults to `--permission-mode default`",
     "Claude starts in auto mode but logs a deprecation warning and continues normally"
    ],
    "correct": 1,
    "sectionIndices": [
     0
    ],
    "explanation": "`--enable-auto-mode` was removed in v2.1.111. The replacement is `--permission-mode auto`. Scripts using the old flag will break on v2.1.111+."
   },
   {
    "prompt": "You need to troubleshoot a CI failure where Claude is behaving unexpectedly. You suspect a hook or MCP server is the cause. Which flag lets you isolate user/project customizations while keeping auth, model, built-in tools, and permissions intact?",
    "options": [
     "--bare",
     "--safe-mode",
     "--permission-mode default",
     "--disallowedTools 'mcp__*'"
    ],
    "correct": 1,
    "sectionIndices": [
     0
    ],
    "explanation": "`--safe-mode` (v2.1.169+) disables all user/project customizations (hooks, skills, MCP, CLAUDE.md) while keeping auth, model, built-in tools, and permissions normal. `--bare` skips discovery of hooks/skills/plugins/MCP but does not have the same isolation guarantee for all customization sources."
   }
  ],
  "tasks": [
   {
    "id": "stage-7-task-ci-auth",
    "afterSectionIdx": 0,
    "title": "Set up non-interactive auth for CI",
    "instructions": "**Option A — Subscription billing (setup-token)**\n\n```bash\n# Run this on your local machine (interactive, requires a Claude subscription)\nclaude setup-token\n# Copy the printed token — it is never saved automatically\n\n# In your CI environment (GitHub Actions, GitLab, etc.) add the secret:\n# Secret name: CLAUDE_CODE_OAUTH_TOKEN\n# Value: <the token printed above>\n\n# In your workflow YAML:\nenv:\n  CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}\n```\n\n**Option B — Console API billing (auth login --console)**\n\n```bash\n# In CI, run:\nclaude auth login --console\n# Follow the printed URL to complete auth in the browser,\n# or use --email to pre-fill the Console account email.\n\n# Verify auth succeeded (exit 0 = logged in):\nclaude auth status\necho \"Exit code: $?\"\n```\n\n**Smoke test your CI setup:**\n\n```bash\nclaude -p 'Print the number 42 and nothing else' \\\n  --max-turns 1 \\\n  --output-format text\n# Expected output: 42\n```",
    "doneWhen": "Running `claude -p 'Print the number 42 and nothing else' --max-turns 1 --output-format text` in a non-interactive shell (or CI environment) exits 0 and prints `42`."
   },
   {
    "id": "stage-7-task-headless-flags",
    "afterSectionIdx": 0,
    "title": "Run a bounded headless audit with scoped tool permissions",
    "instructions": "Run a real headless invocation with cost controls, tool scoping, and machine-readable output.\n\n```bash\n# Create a small test file to audit\necho 'function add(a, b) { return a + b; }' > /tmp/test-audit.js\n\n# Run the audit with cost/loop caps, auto-approve only git reads, deny all MCP\nclaude -p 'List any issues you see in the file /tmp/test-audit.js. Be concise.' \\\n  --max-turns 3 \\\n  --max-budget-usd 0.10 \\\n  --output-format json \\\n  --allowedTools 'Read' \\\n  --disallowedTools 'mcp__*' \\\n  --no-session-persistence\n```\n\nInspect the JSON output structure:\n\n```bash\nclaude -p 'Print JSON: {\"status\": \"ok\"}' \\\n  --max-turns 1 \\\n  --output-format json \\\n  --no-session-persistence | python3 -m json.tool\n```\n\nThen verify that `--allowedTools` does NOT prevent other tools from being available by checking what tools Claude acknowledges it has:\n\n```bash\nclaude -p 'List the tool names available to you.' \\\n  --max-turns 1 \\\n  --output-format text \\\n  --no-session-persistence\n# You should see more than just Read — --allowedTools only skips prompts, it does not restrict\n```",
    "doneWhen": "The bounded invocation exits without error, returns JSON output, and you can observe that `--allowedTools Read` alone does not eliminate other tools from Claude's available set."
   },
   {
    "id": "stage-7-task-review-md",
    "afterSectionIdx": 1,
    "title": "Author a REVIEW.md to control managed Code Review behavior",
    "instructions": "If your repo has the managed Code Review GitHub App enabled, create a `REVIEW.md` at the repo root to control review behavior. If you do not have the app, you can still create and commit the file so it is ready.\n\n```bash\n# Navigate to your repo root\ncd ~/projects/YOUR_REPO  # replace with your actual repo path\n\n# Create REVIEW.md with focused, actionable rules\ncat > REVIEW.md << 'EOF'\n## Review guidelines\n\n### What counts as Important (red)\nOnly flag issues that would cause a production failure, data loss, or a security\nvulnerability. Style, readability, and performance micro-optimizations are not\nImportant.\n\n### Nit volume cap\nReport at most five nits per review. If there are more, mention the count only.\n\n### Skip these paths entirely\n- `**/generated/**` — machine-generated code\n- `**/*.lock` — lockfiles\n- `**/migrations/**` — database migrations\n- Files where the diff is only whitespace or comment changes\n\n### Verification bar\nEvery behavior claim in a finding must cite a specific file and line number.\nDo not infer behavior from naming conventions alone.\n\n### Re-review convergence\nAfter the first review on a PR, suppress new nit-level findings and post\nImportant findings only.\nEOF\n\n# Commit it\ngit add REVIEW.md\ngit commit -m 'Add REVIEW.md to control Claude Code Review behavior'\n```\n\nOptionally test the local `/code-review` command (no GitHub App required):\n\n```bash\n# In any Claude Code session with an active diff:\n# /code-review effort=low\n```",
    "doneWhen": "`REVIEW.md` exists at the repo root, is committed to git, and contains at least: a narrowed definition of Important findings, a nit cap, and a list of paths to skip."
   }
  ],
  "visualizations": [
   {
    "id": "stage-7-v",
    "kind": "comparison-table",
    "title": "Automation, CI & integrations",
    "textualSummary": "Key concepts of Automation, CI & integrations: headless (print) mode, CI authentication, cost and loop bounding.",
    "columns": [
     "Concept",
     "In this stage"
    ],
    "rows": [
     {
      "label": "headless (print) mode",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "CI authentication",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "cost and loop bounding",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "tool and MCP scoping",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     }
    ]
   }
  ],
  "confusions": [
   {
    "misconception": "Skipping Automation, CI & integrations.",
    "correction": "Each stage is load-bearing for the setup."
   },
   {
    "misconception": "These concepts are interchangeable.",
    "correction": "Each has a distinct role; see the definitions."
   }
  ],
  "quiz": [
   {
    "id": "stage-7-q1",
    "type": "multiple-choice",
    "prompt": "Your team's CI pipeline runs `claude -p` to auto-triage issues. After a few weeks, costs spike because a runaway agent opened hundreds of tool calls on a single job. Which two flags should you add to the invocation to bound this in the future?",
    "options": [
     "`--max-turns N` and `--max-budget-usd N`",
     "`--bare` and `--safe-mode`",
     "`--no-session-persistence` and `--output-format json`",
     "`--permission-mode plan` and `--allow-dangerously-skip-permissions`"
    ],
    "correct": 0,
    "explanation": "The content explicitly recommends `--max-turns` + `--max-budget-usd` together to bound runaway cost and loops in pipelines. `--bare`/`--safe-mode` affect customization loading, not cost caps. `--no-session-persistence` and `--output-format json` are useful but don't cap spend or turns. `--permission-mode plan` slows execution but doesn't cap it."
   },
   {
    "id": "stage-7-q2",
    "type": "multiple-choice",
    "prompt": "A teammate sets `--allowedTools 'Bash(git log *)'` in a CI script, expecting it to prevent Claude from running any other Bash commands. On the next run, Claude still executes `Bash(rm -rf temp/)` without prompting. What is the correct explanation?",
    "options": [
     "`--allowedTools` auto-approves matching tools without prompting but does NOT restrict which tools are available — other tools remain usable.",
     "`--allowedTools` is not supported in `-p` print mode; it is silently ignored.",
     "The glob pattern `git log *` does not match subcommands correctly; you need `Bash(git *)` for it to take effect at all.",
     "Bash is a built-in tool that cannot be scoped by either `--allowedTools` or `--disallowedTools`."
    ],
    "correct": 0,
    "explanation": "`--allowedTools` only controls whether Claude prompts for approval — it does not remove or deny tools. To prevent `rm -rf`, use `--disallowedTools 'Bash(rm *)'` (scoped rule: leaves Bash available but denies matching calls). The flag works in print mode. Pattern syntax is valid. Bash is fully scopeable."
   },
   {
    "id": "stage-7-q3",
    "type": "multiple-choice",
    "prompt": "You want to grant your CI runner a Claude token for headless use, billed to the team's subscription (not the API). Which command produces a suitable long-lived token that CI can consume?",
    "options": [
     "`claude setup-token`",
     "`claude auth login --console`",
     "`claude auth login --sso`",
     "`claude auth status --json`"
    ],
    "correct": 0,
    "explanation": "`claude setup-token` mints a long-lived OAuth token for CI, billed to subscription. `claude auth login --console` authenticates for API-usage billing (not subscription billing). `--sso` forces SSO during an interactive login — it doesn't produce a CI token. `claude auth status` only checks current auth state; it produces no token."
   },
   {
    "id": "stage-7-q4",
    "type": "multiple-choice",
    "prompt": "A senior engineer argues: 'We should set the Code Review check as a required status check so PRs can't merge if Claude finds Important issues.' What is wrong with this plan?",
    "options": [
     "Code Review never approves or blocks — the check always completes neutral, so it cannot gate merges via branch protection.",
     "The Code Review check is only available on Manual trigger mode; automatic modes don't create a status check at all.",
     "Required status checks only work with GitHub Actions, not with a GitHub App like the Code Review service.",
     "Code Review results are delivered as PR comments, not as a check run, so there is nothing to gate on."
    ],
    "correct": 0,
    "explanation": "The check always completes neutral regardless of findings, so branch protection on it never blocks a merge. The content instructs teams to build their own gate by parsing the machine-readable severity line. The check exists in all trigger modes. It is a proper check run (not only comments). GitHub App checks work with branch protection."
   },
   {
    "id": "stage-7-q5",
    "type": "multiple-choice",
    "prompt": "Your team has a busy monorepo where multiple small PRs are pushed throughout the day. The Code Review service is currently set to 'After every push'. A teammate suggests switching to 'Manual' mode for this repo. What is the strongest operational justification?",
    "options": [
     "Manual mode is recommended for high-traffic repos so only specific ready PRs opt in, avoiding per-push cost accrual on every PR.",
     "Manual mode is the only trigger that runs on draft PRs, which the team relies on for early feedback.",
     "After every push auto-resolves threads when issues are fixed, but Manual mode preserves the original comments for audit trail.",
     "Manual mode avoids the 20-minute average review delay, since it runs inline with the push event."
    ],
    "correct": 0,
    "explanation": "The content explicitly recommends Manual for high-traffic repos to avoid per-push cost on all PRs. Draft PRs are the opposite: Manual triggers run on drafts but automatic triggers do not — so Manual mode supports drafts, but that's not the operational justification for switching a busy repo away from per-push. Auto-resolve happens in 'After every push' mode, not Manual. Manual mode doesn't change the review latency (it's queue-based)."
   },
   {
    "id": "stage-7-q6",
    "type": "multiple-choice",
    "prompt": "You need to prevent the Code Review agent from flagging lint errors and import ordering in a new repo because CI already enforces those. Where should you put these suppression instructions, and why?",
    "options": [
     "In `REVIEW.md` at the repo root, because it is injected verbatim as highest-priority instructions into every review agent's system prompt.",
     "In `CLAUDE.md` at the repo root, because Code Review reads that file first and it takes precedence over any other configuration.",
     "As a comment in a `@claude review` trigger comment, because inline instructions override all file-based configuration.",
     "In the org admin settings at `claude.ai/admin-settings/claude-code`, because review behavior is a team-wide policy."
    ],
    "correct": 0,
    "explanation": "REVIEW.md is injected as highest-priority system prompt content and is explicitly recommended for skip rules on CI-enforced items. CLAUDE.md is read as project context but is lower priority than REVIEW.md and the content warns rules 'buried in a long CLAUDE.md' land less reliably. Inline trigger comments are not documented as a configuration mechanism for persistent skip rules. Admin settings control billing and the GitHub App install, not per-repo review focus."
   },
   {
    "id": "stage-7-q7",
    "type": "multi-select",
    "prompt": "A teammate is setting up a shared CI script that will run `claude -p` across hundreds of parallel jobs from different team members' machines. They want to ensure prompt-cache reuse across jobs to minimize latency and cost. Select ALL flags or practices that are necessary or directly relevant to achieving cross-machine prompt-cache reuse.",
    "options": [
     "Pass `--exclude-dynamic-system-prompt-sections` alongside `-p`",
     "Use the same model across all jobs via `--model`",
     "Use `--bare` to skip CLAUDE.md and plugin/hook discovery",
     "Move per-machine sections (cwd, env, memory paths, git-repo flag) into the first user message",
     "Set `CLAUDE_CODE_SKIP_PROMPT_HISTORY` to disable session persistence"
    ],
    "correct": [
     0,
     3
    ],
    "explanation": "`--exclude-dynamic-system-prompt-sections` is the flag and the mechanism it enables (moving per-machine sections into the first user message) together constitute the stated approach. Using the same model is good practice but not documented as necessary for prompt-cache reuse in this context. `--bare` skips CLAUDE.md/hooks but doesn't address cache variance from per-machine system prompt content. Disabling session persistence prevents cross-session state leakage but is unrelated to prompt-cache reuse across parallel jobs."
   },
   {
    "id": "stage-7-q8",
    "type": "multi-select",
    "prompt": "You need to lock a CI job so that it uses ONLY the team's curated MCP servers and no servers from any individual developer's user-level MCP config. Select ALL flags required to achieve this.",
    "options": [
     "`--mcp-config <team-config-file>`",
     "`--strict-mcp-config`",
     "`--disallowedTools 'mcp__*'`",
     "`--tools ''`",
     "`--bare`"
    ],
    "correct": [
     0,
     1
    ],
    "explanation": "`--mcp-config` supplies the team's curated server list, and `--strict-mcp-config` causes only those servers to load, ignoring all other (user-level) MCP config. Together they pin the MCP surface. `--disallowedTools 'mcp__*'` would deny all MCP tool calls but still loads them into context (and denies the team's own servers too). `--tools ''` only affects built-in tools and does not affect MCP tools. `--bare` skips plugin/hook/MCP discovery from CLAUDE.md but the content does not document it as preventing user-level MCP config from loading."
   },
   {
    "id": "stage-7-q9",
    "type": "multiple-choice",
    "prompt": "A team's Code Review costs jumped from ~$20 per PR to ~$100 when they enabled 'After every push' on an active PR with frequent small fixup commits. A developer then posts `@claude review once` on that PR. What does this command do?",
    "options": [
     "Triggers a single one-shot review of the PR without subscribing it to future push-triggered reviews.",
     "Triggers a review and changes the repo's global trigger mode from 'After every push' to 'Manual' for all future PRs.",
     "Triggers a review and subscribes this PR to all future push reviews, identical to `@claude review`.",
     "Triggers a review and then immediately unsubscribes the PR from the 'After every push' mode permanently."
    ],
    "correct": 0,
    "explanation": "`@claude review once` gives one review with no ongoing subscription — precisely the right tool for a long-running PR with frequent pushes. It does not change the repo's global trigger mode (that's a per-repo dropdown setting). Unlike plain `@claude review`, it does not subscribe the PR to future push-triggered reviews. It also doesn't retroactively unsubscribe an already-subscribed PR."
   },
   {
    "id": "stage-7-q10",
    "type": "multiple-choice",
    "prompt": "Your team's Code Review bill is billed to your Anthropic account even though your org uses Bedrock for all other Claude API calls. A finance team member asks why. What is the correct explanation?",
    "options": [
     "Code Review billing is separate: it uses usage credits billed directly on the Anthropic bill even if the org uses Bedrock or Vertex for other API calls.",
     "Code Review cannot be used with Bedrock at all; the separate billing is a sign that the integration is incorrectly configured.",
     "The Bedrock billing for Code Review is delayed by one billing cycle, so the charge appears on Anthropic first and is reconciled later.",
     "Code Review is included in the Team/Enterprise plan fee, so any Anthropic charges are from a different Claude Code feature."
    ],
    "correct": 0,
    "explanation": "The content explicitly states Code Review billing appears on the Anthropic bill regardless of whether the org routes other calls through Bedrock or Vertex. It is a separate usage-credit line item. There is no misconfiguration — this is by design. There is no reconciliation delay described. It is not included in the plan fee; the content notes it does not count against included plan usage, but is billed as usage credits on top."
   },
   {
    "id": "stage-7-q11",
    "type": "multiple-choice",
    "prompt": "A new engineer runs `claude -p 'summarize this repo'` with both `--system-prompt 'You are a helpful assistant.'` and `--append-system-prompt 'Always respond in bullet points.'`. What will happen?",
    "options": [
     "This combination is valid: `--append-system-prompt` layers rules on top of whatever replaces the default, so bullet-point formatting is added to the custom system prompt.",
     "Claude Code will error because `--system-prompt` and `--append-system-prompt` are mutually exclusive flags.",
     "The `--append-system-prompt` is silently ignored because `--system-prompt` replaces the entire system prompt; you cannot append to a replaced prompt.",
     "The default tool guidance and safety instructions are preserved alongside both the custom prompt and the append — `--system-prompt` only replaces the persona section."
    ],
    "correct": 0,
    "explanation": "The content explicitly says `--append-system-prompt` can be combined with either replace flag. When `--system-prompt` replaces the default, `--append-system-prompt` then layers on top of that replacement. They are not mutually exclusive. `--append-system-prompt` is not silently ignored. However, once `--system-prompt` is used, the default tool guidance and safety instructions are dropped (you own safety) — the append adds to the replacement, not to the original defaults."
   },
   {
    "id": "stage-7-q12",
    "type": "multiple-choice",
    "prompt": "When Claude Code's configuration is behaving unexpectedly and you suspect a project-level hook or skill is at fault, which flag should you use first to isolate whether user/project customizations are the cause — without losing managed policy enforcement?",
    "options": [
     "`--safe-mode`, which disables all customizations while keeping auth, model, built-in tools, permissions, and managed policy normal.",
     "`--bare`, which skips all customization loading including managed policy, giving a minimal baseline.",
     "`--permission-mode default`, which resets to default permissions and implicitly reverts hook behavior.",
     "`--settings '{}'`, which loads an empty settings object, clearing all user and project configuration."
    ],
    "correct": 0,
    "explanation": "`--safe-mode` is the documented first step for troubleshooting: it disables all customizations while leaving managed policy, auth, model, and built-in tools intact — meaning it isolates user/project customizations specifically. `--bare` skips discovery of hooks/skills/plugins but the content does not state it preserves managed policy; it's not the documented troubleshooting step. `--permission-mode default` resets the permission mode but doesn't affect hooks or skills. `--settings '{}'` overrides only keys you set; an empty object overrides nothing — it doesn't clear user or project config."
   }
  ],
  "masteryCheckpoint": "You can explain the concepts of Automation, CI & integrations."
 },
 {
  "id": "stage-8",
  "stage": 8,
  "title": "Optimal use, context & cost",
  "summary": "Optimal use, context & cost: Shared CLAUDE.md, Context as the fundamental constraint, Per-feature precedence and four config levels.",
  "prerequisites": [
   "stage-7"
  ],
  "objectives": [
   "Understand the concepts in Optimal use, context & cost."
  ],
  "definitions": [
   {
    "term": "Shared CLAUDE.md",
    "short": "A committed instruction file that loads into context every request and is shared with the whole team when placed at the repo root."
   },
   {
    "term": "Context as the fundamental constraint",
    "short": "Performance degrades and cost rises as the 200K-token window fills, and much of it is consumed before the first prompt by config that always loads."
   },
   {
    "term": "Per-feature precedence and four config levels",
    "short": "Config exists at user, project, plugin, and managed levels, but each feature type resolves conflicts by its own rule rather than a single uniform hierarchy."
   },
   {
    "term": "Build setup incrementally by trigger",
    "short": "Each kind of recurring friction maps to a specific config artifact, so the team's shared setup grows from observed pain rather than upfront design."
   },
   {
    "term": "Hooks as enforcement vs advisory instructions",
    "short": "CLAUDE.md is a request Claude may ignore, while hooks run deterministically at lifecycle events for anything that must happen every time."
   },
   {
    "term": "Context cost varies by feature",
    "short": "Different config features charge context differently (full content, descriptions only, deferred schemas, or zero), which determines what to load eagerly versus on demand."
   }
  ],
  "sections": [
   {
    "heading": "What You're Actually Optimizing",
    "body": "Every Claude Code session is a race against two resources: **tokens** (which determine cost) and **context quality** (which determines output quality). They are not independent — as the 200K-token window fills, not only does cost rise, but adherence to instructions measurably degrades. Configuration files are the biggest controllable variable in that budget. They load before your first prompt and are present for every turn.\n\nThe team lead's job is to set up a shared configuration surface that puts the right information in context at the right time — no sooner, no later — and enforces the non-negotiables through mechanisms that actually run, not just instructions that the model may choose to follow. This stage covers the entire configuration stack: where files live, what they cost, what enforces versus advises, and how to grow the setup from observed friction rather than up-front design.\n\nThe distinction between **advisory** and **enforced** configuration runs through everything here:\n\n- `CLAUDE.md` content is **advisory**: delivered as a user message, read by Claude, followed when clear and consistent, but with no enforcement guarantee.\n- Permission deny rules and hooks are **enforced**: they run as client-side logic or shell commands regardless of what Claude decides to do.\n\nDesigning a team setup means deciding which category each requirement belongs in, and using the right mechanism for each."
   },
   {
    "heading": "The Four Configuration Levels and Where Files Live",
    "body": "Claude Code resolves configuration from four scopes. For scalar settings, the highest-priority value wins. **Permission rules merge across all scopes** — deny beats allow when the same action appears in both. **CLAUDE.md files are concatenated**, not overridden.\n\n| Scope | Settings file | CLAUDE.md location | Shared with team? | Can be overridden? |\n|---|---|---|---|---|\n| **Managed** | `/etc/claude-code/managed-settings.json` (Linux/WSL); `/Library/Application Support/ClaudeCode/` (macOS) | `/etc/claude-code/CLAUDE.md` | All users on machine | No — enforced |\n| **User** | `~/.claude/settings.json` | `~/.claude/CLAUDE.md` | Only you | Yes |\n| **Project** | `.claude/settings.json` | `./CLAUDE.md` or `./.claude/CLAUDE.md` | Team (committed) | Yes |\n| **Local** | `.claude/settings.local.json` | `./CLAUDE.local.md` | Only you (gitignore it) | Yes |\n\n**Scope precedence for scalar settings**: managed → command-line flags → local → project → user.\n\n### CLAUDE.md load order\n\nClaude Code walks the directory tree from the filesystem root down to your working directory, collecting every `CLAUDE.md` and `CLAUDE.local.md` it finds. All discovered files are **concatenated in order**: broader-scope files load first, more-specific files load last. Within each directory, `CLAUDE.local.md` is appended after `CLAUDE.md`. Files in subdirectories below your working directory load on demand when Claude reads files in those subdirectories — they are not loaded at session start.\n\nBlock-level HTML comments (`<!-- maintainer note -->`) in CLAUDE.md are stripped before injection into Claude's context. Use them freely for human readers without spending tokens.\n\n### The Practical File Layout for a Team Repo\n\n```\n.gitignore              # include CLAUDE.local.md and .claude/settings.local.json\nCLAUDE.md               # team's primary instruction file; committed\nCLAUDE.local.md         # personal project overrides; gitignored\n.claude/\n  settings.json         # permissions, hooks, model defaults; committed\n  settings.local.json   # per-developer overrides; gitignored\n  rules/\n    testing.md          # path-scoped: loads only when *.test.ts files are touched\n    api-conventions.md  # path-scoped: loads only for src/api/**/*.ts\n  skills/\n    deploy/\n      SKILL.md          # committed; invoke with /deploy\n    research-module/\n      SKILL.md          # context:fork skill; Claude invokes automatically\n  agents/\n    code-reviewer.md    # committed subagent definition; team-shared\n```"
   },
   {
    "heading": "Shared CLAUDE.md: The Team's Standing Context",
    "body": "A CLAUDE.md committed to the repo root is the single most impactful configuration artifact a team can create. It loads into **every session for every developer** without any per-developer action. After `/compact`, Claude re-reads the project-root CLAUDE.md from disk and re-injects it — it survives compaction.\n\n### What Belongs Here\n\nCLAUDE.md is for facts Claude should hold every session: build commands, architectural constraints, naming conventions, test runner invocations, where things live. It is not a policy document — vague rules like \"write good code\" consume tokens without directing behavior. Specific, verifiable instructions work:\n\n```markdown\n## Build\nRun `npm run check` (tsc + content validator + build) before declaring any UI done.\nHeadless visual pass: `npm run screenshots` — mandatory before UI changes ship.\n\n## Repo layout\n- All API handlers live in `src/api/handlers/`\n- Every public function needs a docstring explaining *why*, not *what*\n- Use 2-space indentation; Prettier enforced by the PostToolUse hook\n\n## Running the app\n`npm run dev` — Vite dev server on localhost:5173\n`npm run backend` — FastAPI on :8000; requires `OPENAI_API_KEY` in `.env`\n```\n\n### Importing additional files\n\nUse `@path/to/file` syntax in CLAUDE.md to pull in additional files. Imported files are loaded at session start. **Imports do not reduce context cost** — they are expanded in full. Use a skill for on-demand loading of large reference material instead.\n\n```markdown\nSee @README for project overview and @package.json for available npm scripts.\n```\n\n### Size and Adherence\n\nTarget **under 200 lines** per CLAUDE.md file. Longer files consume more context and reduce adherence because the model's attention is a finite resource and the file is re-read as part of a large context. If your instructions are growing past 200 lines, the right move is to split into path-scoped rules (load only when matching files are touched) or skills (load only when invoked).\n\n### What Does Not Belong Here\n\n- Multi-step procedures → put in a skill\n- Instructions relevant only to `src/billing/` → put in `.claude/rules/billing.md` with `paths: [\"src/billing/**\"]`\n- Personal sandbox URLs or test credentials → `CLAUDE.local.md` (gitignored)\n- Anything that must happen every time without fail → a hook, not text\n- Cross-project personal preferences that should follow you across worktrees → import from `~/.claude/` in your user-level CLAUDE.md"
   },
   {
    "heading": "Context as the Fundamental Constraint",
    "body": "The 200K-token window is consumed before you type your first character. Here is the actual startup load order, with representative token costs from the official context window simulator at `code.claude.com/docs/en/context-window`:\n\n| Layer | Content | Approx tokens | Changes when |\n|---|---|---|---|\n| System prompt | Tool definitions, core instructions | ~4,200 | Claude Code upgrades, tool set changes |\n| Auto memory (MEMORY.md) | First 200 lines / 25KB of session notes | ~680 | Session start, /clear, /compact |\n| Environment info | CWD, platform, shell, git branch + recent commits | ~280 | Each session start |\n| MCP tools (deferred) | Names only; full schemas fetched per-tool on demand | ~120 | MCP servers connect/disconnect |\n| Skill descriptions | Descriptions of available skills (≤1,536 chars each, combined ~450 tokens) | ~450 | Session start (not re-injected after /compact) |\n| User CLAUDE.md | `~/.claude/CLAUDE.md` | ~320 | Session start, /clear, /compact |\n| Project CLAUDE.md | `./CLAUDE.md` or `./.claude/CLAUDE.md` | ~1,800 | Session start, /clear, /compact |\n\nThat is roughly **7,850 tokens consumed before your first prompt**. A 500-line CLAUDE.md easily doubles that overhead. Every file read, tool result, and conversation turn adds on top.\n\nThe key implication: **context consumed by config is always-on cost**. A 1,000-token CLAUDE.md adds 1,000 tokens to every API call for the entire session. Over 50 turns, that is 50,000 tokens you paid for without sending them. Path-scoped rules and skills are the escape hatch: they load only when triggered.\n\n### The Cache Amplifies Everything\n\nPrompt caching makes stable early layers nearly free on repeated turns. The cache matches by exact prefix:\n\n```\n[System prompt] → [Project context: CLAUDE.md, memory, unscoped rules] → [Conversation]\n     ^stable at session start         ^stable at session start                 ^changes every turn\n```\n\nCached tokens read at roughly **10% of the standard input rate**. If your CLAUDE.md is stable, it effectively costs 10% per turn after the first. If you edit it mid-session, it does **not** reload — CLAUDE.md is read once at session start and held in memory. The new content loads on the next `/clear`, `/compact`, or restart. This is also why mid-session edits are cache-safe: the prefix does not change and no cache invalidation occurs.\n\nActions that **do** invalidate the cache (causing the next turn to re-read and re-bill the full context):\n- Switching models (`/model`) — each model has its own cache\n- Changing effort level (`/effort`) — each effort level is a separate cache key; Claude Code shows a confirmation dialog before applying\n- Connecting or disconnecting an MCP server whose tools load into the prefix (tools are deferred by default, so this is avoidable)\n- Running `/compact` — by design; rebuilds the conversation layer from a summary\n- Claude Code auto-update — changes the system prompt on next launch\n- Denying an entire built-in tool mid-session (removes its definition from the system prompt layer)"
   },
   {
    "heading": "Per-Feature Precedence: It Is Not One Uniform Hierarchy",
    "body": "The naive mental model — managed > local > project > user — applies to most scalar settings, but several feature types have their own resolution rules.\n\n### Settings (scalar values)\nStandard precedence: managed → command-line flags → local → project → user. The highest scope wins. Example: if user settings set `model: sonnet` and project settings set `model: opus`, the project value applies (local would override project, managed overrides everything).\n\n### Permission rules (allow / ask / deny)\nPermission rules **merge across all scopes**. Deny beats allow when the same action appears in both. This means a team lead can add deny rules to the project settings that cannot be undone by a developer's user settings.\n\n```json\n// .claude/settings.json — committed to the repo\n{\n  \"permissions\": {\n    \"deny\": [\n      \"Bash(rm -rf *)\",\n      \"Read(./.env)\",\n      \"Read(./.env.*)\"\n    ],\n    \"allow\": [\n      \"Bash(npm run *)\",\n      \"Bash(git *)\"\n    ]\n  }\n}\n```\n\n### CLAUDE.md files\nNot overridden — **concatenated**. All CLAUDE.md files Claude Code discovers are appended in load order (root-down through the directory tree). Conflicting instructions in different files produce undefined behavior. Review them periodically with `/memory` to identify contradictions. In large monorepos, use `claudeMdExcludes` in `.claude/settings.local.json` to skip irrelevant ancestor CLAUDE.md files — managed CLAUDE.md files cannot be excluded.\n\n### Skills\nWhen skills share the same name across levels, enterprise overrides personal, personal overrides project. A project-level `code-review` skill replaces the bundled `/code-review`. Plugin skills are namespaced (`plugin-name:skill-name`) and cannot conflict.\n\n### Hooks\nHooks defined at multiple scopes **all run**. Unlike permission rules, there is no override — hooks accumulate. A managed hook that runs a security check and a project hook that runs a linter both fire on every matching event. Check for duplicate hooks with `/hooks`.\n\n### Model and effort\nSet in settings as initial defaults; user can override with `/model` or `/effort` mid-session. Managed settings can restrict available models with `availableModels` and `enforceAvailableModels` (the latter prevents the Default picker option from resolving outside the allowlist).\n\n```json\n// /etc/claude-code/managed-settings.json (Linux/WSL)\n{\n  \"availableModels\": [\"sonnet\", \"haiku\"],\n  \"enforceAvailableModels\": true\n}\n```"
   },
   {
    "heading": "Hooks as Enforcement vs. Advisory Instructions",
    "body": "This is the most operationally important distinction in the configuration system: **CLAUDE.md is a request; hooks are enforcement.**\n\nCLAUDE.md content is delivered as a user message after the system prompt. Claude reads it and tries to follow it, but compliance is not guaranteed — the official docs state: \"Claude Code treats them as context, not enforced configuration.\" Hooks are shell commands (or HTTP endpoints, MCP tool calls, prompt hooks, or agent hooks) that execute at defined lifecycle events, regardless of what Claude decides to do.\n\n### Lifecycle Events\n\nClaude Code supports 33 hook events across three cadences. The most operationally important:\n\n| Event | Cadence | Can block? | Notes |\n|---|---|---|---|\n| `PreToolUse` | Every tool call | Yes (exit 2) | Can also modify tool input via JSON response |\n| `PostToolUse` | Every tool call | Yes (blocks next model call) | Tool already ran; exit 2 blocks the subsequent loop turn |\n| `UserPromptSubmit` | Per turn | Yes (exit 2) | 30s default timeout |\n| `Stop` | Per turn | Yes (exit 2, forces continuation) | Use to enforce a checklist before Claude stops |\n| `SessionStart` | Once per session | No | Good for environment setup |\n| `PreCompact` | Around /compact | Yes (exit 2) | |\n| `PostCompact` | Around /compact | No | |\n| `SubagentStop` | When subagent finishes | Yes (exit 2) | |\n\n### Hook Configuration Format\n\nHooks are configured in settings files. The structure has three levels: event → matcher group → handler list.\n\n```json\n// .claude/settings.json\n{\n  \"hooks\": {\n    \"PostToolUse\": [\n      {\n        \"matcher\": \"Edit|Write\",\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"npx prettier --write ${tool_input.file_path}\",\n            \"statusMessage\": \"Formatting...\"\n          }\n        ]\n      }\n    ],\n    \"PreToolUse\": [\n      {\n        \"matcher\": \"Bash\",\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"if\": \"Bash(rm *)\",\n            \"command\": \"${CLAUDE_PROJECT_DIR}/.claude/hooks/block-destructive.sh\"\n          }\n        ]\n      }\n    ]\n  }\n}\n```\n\nThe `matcher` field filters by tool name. If it contains only letters, digits, `_`, or `|`, it is treated as an exact string match or list (e.g., `Edit|Write`). Otherwise it is a JavaScript regex. The `if` field inside a hook handler provides additional filtering using permission rule syntax.\n\nHook handler types: `command` (shell), `http` (POST to a URL), `mcp_tool` (call an existing MCP tool), `prompt` (single-turn LLM evaluation), `agent` (experimental subagent with tool access).\n\n### Exit Codes\n\n| Exit code | Meaning |\n|---|---|\n| **0** | Success; stdout JSON is processed if present |\n| **2** | Blocking signal; stderr becomes the error message; blocks the triggering event (for blocking-capable events) |\n| **Any other non-zero** | Non-blocking error; shown to user but execution continues |\n\n**Important**: JSON output is **ignored** at exit 2. To use JSON-based blocking (`permissionDecision: \"deny\"`), exit with 0 and include the decision in the JSON.\n\n### The Critical Distinction: Hooks vs. Deny Rules\n\nHooks are for **automation** (run this command deterministically at lifecycle events). Deny rules are for **capability restrictions** (this tool must never be called, full stop). The `if` field in a hook uses permission rule syntax that fails open — if the pattern cannot be parsed, the hook runs anyway. For hard enforcement, use `permissions.deny`, not a hook.\n\n### Hook Context Cost\n\nOnly output returned via the `additionalContext` field in a hook's JSON response enters Claude's context. Plain stdout on exit 0 goes only to the debug log. Keep `additionalContext` concise — it is injected without truncation.\n\n```json\n// Hook JSON response on exit 0 — injects context into Claude's next turn\n{\n  \"hookSpecificOutput\": {\n    \"hookEventName\": \"PostToolUse\",\n    \"additionalContext\": \"Prettier ran: 3 files reformatted.\"\n  }\n}\n```"
   },
   {
    "heading": "Context Cost Varies by Feature: What to Load Eagerly vs. On Demand",
    "body": "Different configuration features have very different context costs. The design decision of when to load something has compounding effects across sessions.\n\n| Feature | Context cost | When it loads |\n|---|---|---|\n| Project CLAUDE.md (200 lines) | Full content every session (~1,800 tokens) | Session start; re-injected after /compact |\n| User CLAUDE.md | Full content every session | Session start; re-injected after /compact |\n| `.claude/rules/*.md` (no paths frontmatter) | Full content every session | Session start |\n| `.claude/rules/*.md` (with `paths:` frontmatter) | Full content when matching file is touched | On-demand, mid-session |\n| Skill description (auto-invocable) | Combined description + when_to_use, truncated at 1,536 chars | Session start |\n| Skill body (invoked) | Full content | When Claude invokes or you type `/skill-name` |\n| Skill with `disable-model-invocation: true` | **Zero** until you invoke it | Only when you type `/skill-name` |\n| MCP tool names (deferred, default) | Names only (~120 tokens for all servers) | Session start |\n| MCP tool schemas (deferred) | Full schema for one tool | When Claude calls that tool |\n| Hook `additionalContext` output | Injected without truncation | When the hook fires |\n| Auto memory MEMORY.md | First 200 lines or 25KB | Session start |\n\n### The `disable-model-invocation` Trick\n\nFor skills with side effects — deploy, commit-and-push, send-slack-notification — set `disable-model-invocation: true`. The skill's description does not appear in the startup skill listing at all. Zero context cost until you invoke it with `/skill-name`.\n\n```yaml\n# .claude/skills/deploy-staging/SKILL.md\n---\nname: deploy-staging\ndescription: Deploy the current branch to the staging environment\ndisable-model-invocation: true\ncontext: fork\n---\n\n1. Run `npm run check` — abort if any errors\n2. Run `git push origin HEAD`\n3. Trigger the staging deploy via `./scripts/deploy.sh staging`\n4. Tail logs for 60 seconds and report any errors\n```\n\n### Path-Scoped Rules\n\n```markdown\n---\npaths:\n  - \"src/api/**/*.ts\"\n---\n\n# API Conventions\n\n- All endpoints must include request body validation with Zod\n- Error responses must use the standard `{ error: string, code: string }` shape\n- Include JSDoc `@openapi` annotations for each handler\n```\n\nThis rule loads 0 tokens until Claude reads a file under `src/api/`. A 400-token rule file applied only to API files costs nothing during a backend-only session. Use the same `paths:` frontmatter in skill files (`paths:` is also a valid skill frontmatter field) to restrict auto-invocation to relevant file contexts.\n\n### MCP Tool Deferral\n\nBy default, MCP tool names load at startup (~120 tokens total) and full schemas are fetched on demand when Claude calls a tool. Set `ENABLE_TOOL_SEARCH=false` to load all schemas upfront (increases startup token cost but removes per-tool fetch latency). Set `ENABLE_TOOL_SEARCH=auto` to load schemas upfront only when they fit within 10% of the context window."
   },
   {
    "heading": "What Survives Compaction",
    "body": "Auto-compaction fires when the conversation approaches the context limit. It replaces the message history with a structured summary. Understanding what does and does not survive is essential for writing durable instructions.\n\n### What Survives `/compact`\n\n- **Project-root CLAUDE.md and user-level CLAUDE.md**: re-read from disk and re-injected into the new session start.\n- **Unscoped rules** in `.claude/rules/` without `paths:` frontmatter: re-injected.\n- **Settings** (permissions, hooks, model): unchanged — they are not part of the conversation.\n- **Skills you actively invoked**: preserved in the summary as conversation history (you ran `/skill-name` so it appeared as a message).\n\n### What Does Not Survive `/compact`\n\n- **Skill descriptions**: the skill listing is **not** re-injected after `/compact`. Skills remain callable by explicit `/skill-name` invocation, but Claude no longer sees the listing of available skills for auto-invocation.\n- **Nested CLAUDE.md files** in subdirectories: not re-injected automatically; they reload the next time Claude reads a file in that subdirectory.\n- **Path-scoped rules** that were loaded mid-session: not re-injected; they reload when Claude next reads a matching file.\n- **Anything given only in conversation**: if an instruction was given as a chat message rather than written to a file, compaction removes it.\n\n### The Compaction Mechanism\n\nCompaction is efficient by design. Claude Code sends a one-off request with the same system prompt, tools, and full history as your conversation, plus a summarization instruction appended as a final message. Because it shares your prefix, that request reads the existing cache rather than reprocessing the full history. Most of compaction's time goes to generating the summary, not to a cache miss. The post-compaction turn rebuilds the conversation cache only for the much shorter summary.\n\nUse `/rewind` when you want to abandon a bad path: rewinding truncates back to an earlier turn that is already in the cache, rather than building a new prefix as compaction does. For context savings without summarizing, `/rewind` is cheaper.\n\n### Operational Implication\n\nIf a rule must hold for the entire session, including after compaction, it must live in the project-root CLAUDE.md or in an unscoped `.claude/rules/` file. Instructions given in chat are ephemeral. This is why \"add it to CLAUDE.md\" matters — it is the difference between an instruction that survives a long autonomous session and one that disappears after the first context wrap."
   },
   {
    "heading": "Subagent Context Isolation",
    "body": "When Claude delegates work to a subagent (via the Agent tool or a skill with `context: fork`), the subagent gets its own separate context window. This is the primary mechanism for keeping verbose operations from bloating the main session.\n\n### What a Subagent Gets\n\n- Its own system prompt (shorter than the main session's — the general-purpose subagent's is a brief prompt plus environment info)\n- Project CLAUDE.md (same file, counts against the subagent's context, not yours — Explore and Plan built-in subagents skip it to stay fast)\n- MCP servers and skills (same access, minus several tools that don't apply in nested context: plan-mode controls, background-task tools, and by default the Agent tool itself to prevent recursion)\n- A task prompt from the parent (not the full conversation history)\n- Its own persistent memory if configured with `memory:` in the agent frontmatter\n\n### What the Main Session Sees\n\nOnly the subagent's **final text response** returns to the main context, plus a small metadata trailer with token counts and duration. If the subagent read 6,100 tokens of files to research something and returned a 420-token summary, your main context paid 420 tokens, not 6,100.\n\n### When to Use Subagents for Context Control\n\n- Running a full test suite and summarizing failures\n- Fetching and parsing large log files or API responses\n- Exploring an unfamiliar part of the codebase to produce a briefing\n- Any task where the intermediate file reads are much larger than the useful output\n\n```yaml\n# .claude/agents/research-module.md\n---\nname: research-module\ndescription: Deep-read a module and produce a concise briefing. Use when asked to understand an unfamiliar subsystem.\nmodel: haiku\n---\n\nRead the module at $ARGUMENTS recursively. Produce a briefing of:\n1. Purpose and responsibilities of the module\n2. Key public interfaces and their signatures\n3. External dependencies\n4. Any obvious pitfalls or gotchas\n\nBe concise — this briefing will be injected into the parent session's context.\n```\n\nNote: subagent definitions live in `~/.claude/agents/` (personal) or `.claude/agents/` (project), not in `.claude/skills/`. Skills use `context: fork` to run in a forked subagent.\n\n### Subagent Cache Behavior\n\nA regular subagent (spawned via the Agent tool) builds its own cache, starting cold. It uses the **five-minute TTL** even on a subscription — the one-hour TTL applies only to the main conversation.\n\nA **forked subagent** (`context: fork` in a skill, or a \"fork\" invocation) inherits the parent's system prompt, tools, and conversation history, so its first call hits the parent's cache. This is the mechanism that makes compaction's summarization request efficient — it forks the conversation to share the prefix."
   },
   {
    "heading": "Model and Effort Selection as the Main Spend Lever",
    "body": "After context size, model and effort selection are the largest controllable cost variables.\n\n### Model Aliases\n\nAliases resolve to the recommended version for your provider and update over time. On the Anthropic API:\n\n| Alias | Resolves to (Anthropic API) | Default effort | When to use |\n|---|---|---|---|\n| `haiku` | Latest Haiku model | No effort support | Background tasks, simple lookups, cost-sensitive subagents |\n| `sonnet` | Claude Sonnet 4.6 | `high` | Daily coding tasks — default for most sessions |\n| `opus` | Claude Opus 4.8 | `high` | Complex architectural decisions, multi-step reasoning |\n| `fable` | Claude Fable 5 | `high` | Long autonomous sessions, root-cause investigations, ambiguous problems |\n| `best` | Fable 5 where available, otherwise latest Opus | — | Always-best, account-aware |\n| `opusplan` | Opus during plan mode, Sonnet during execution | — | Opus reasoning for planning, Sonnet speed for code |\n| `sonnet[1m]` | Sonnet 4.6 with 1M context window | `high` | Long sessions with large codebases (requires usage credits on Pro; included on Max/Team) |\n| `opus[1m]` | Opus 4.8 with 1M context window | `high` | Included on Max/Team/Enterprise; requires credits on Pro |\n\n**Model switches mid-session invalidate the prompt cache** — the next turn re-reads the entire conversation history with no cache hits. Pick the model at the top of a session. `opusplan` causes a cache invalidation on each plan-mode toggle, since entering and leaving plan mode switches the underlying model.\n\n### The Effort Level Decision\n\nEffort controls adaptive reasoning — how much the model thinks before responding. Supported models and levels:\n\n| Level | Fable 5 / Opus 4.8 / Opus 4.7 | Opus 4.6 / Sonnet 4.6 | When to use |\n|---|---|---|---|\n| `low` | Yes | Yes | Short, scoped, latency-sensitive tasks |\n| `medium` | Yes | Yes | Cost-sensitive work where some intelligence can be traded |\n| `high` | Yes (default on Fable 5, Opus 4.8, Sonnet 4.6, Opus 4.6) | Yes | Default; balances cost and capability |\n| `xhigh` | Yes (default on Opus 4.7) | No — falls back to `high` | Deeper reasoning at higher spend |\n| `max` | Yes | Yes | Session-only; deepest reasoning; can overthink simple tasks |\n\n`max` and `ultracode` are session-only and cannot be set in `settings.json`. Setting `effortLevel` in settings accepts only `low`, `medium`, `high`, or `xhigh`.\n\n**Effort changes mid-session also invalidate the cache.** Claude Code shows a confirmation dialog before applying the change.\n\n### Setting Defaults in settings.json\n\n```json\n// .claude/settings.json — committed to the repo\n{\n  \"model\": \"sonnet\",\n  \"effortLevel\": \"high\"\n}\n```\n\nSkill frontmatter can override the effort level for specific tasks:\n\n```yaml\n---\nname: architecture-review\ndescription: Deep architectural review of a module or PR. Use for significant design decisions.\neffort: xhigh\n---\n```\n\nSubagent frontmatter can pin a cheaper model for coordination tasks:\n\n```yaml\n---\nname: quick-lookup\nmodel: haiku\n---\n```\n\n### The `ultrathink` Keyword\n\nType `ultrathink` anywhere in a prompt to request deeper reasoning for that single turn without changing the session effort setting. Claude Code recognizes the keyword and adds an in-context instruction. The effort level sent to the API is **unchanged** — this is distinct from `/effort max`. Other phrases like \"think hard\" are passed through as ordinary text and are not recognized keywords.\n\n### The `ultracode` Setting\n\n`ultracode` is a Claude Code orchestration setting (not a model effort level): it sends `xhigh` to the model and additionally has Claude orchestrate dynamic workflows for substantive tasks. It applies to the current session only and cannot be persisted in `settings.json`. Enable it via `/effort` → ultracode, or pass `\"ultracode\": true` via `--settings`."
   },
   {
    "heading": "Prompt Caching by Stable Prefix",
    "body": "The API caches by exact prefix match. Each request re-sends the full history; the cache avoids reprocessing the part that is identical to the previous request. Cached tokens read at roughly **10% of the standard input rate**.\n\n### The Three-Layer Cache Structure\n\n```\n┌─────────────────────────────────────────────────────────┐\n│ Layer 1: System prompt (~4,200 tokens)                  │\n│ Changes: Claude Code upgrade, tool-set changes,         │\n│          output-style change (requires restart)         │\n├─────────────────────────────────────────────────────────┤\n│ Layer 2: Project context (~3,000–5,000 tokens)          │\n│ CLAUDE.md + auto memory + unscoped rules                │\n│ Changes: session start, /clear, /compact                │\n├─────────────────────────────────────────────────────────┤\n│ Layer 3: Conversation (grows per turn)                  │\n│ Messages, tool results, file reads                      │\n│ Changes: every turn                                     │\n└─────────────────────────────────────────────────────────┘\n```\n\nA change to Layer 3 leaves Layers 1 and 2 cached. A change to Layer 1 (model switch, Claude Code upgrade) invalidates everything below it.\n\n### Practical Rules for Maximum Cache Hit Rate\n\n1. **Pick model and effort at session start, not mid-task.** Model and effort are part of the cache key. Each switch recomputes the entire request on the next turn.\n2. **Do not edit CLAUDE.md mid-session.** The edit does not apply until the next `/clear` or restart (content is read once at session start), so you pay nothing but also gain nothing. Edit, then `/clear` or restart.\n3. **MCP tools are deferred by default** — tool names load as ~120 tokens; schemas are fetched per-tool when Claude calls them. Avoid `alwaysLoad` on MCP servers unless required, as it moves schemas into the cached prefix and invalidates the cache when any server connects or disconnects.\n4. **Save `/compact` for natural task boundaries.** The post-compaction turn is efficient (system prompt and CLAUDE.md cache-hit), but the conversation cache has to rebuild from the shorter summary.\n5. **Use `/rewind` to abandon a bad path cheaply.** Rewinding truncates to an earlier turn already in the cache, rather than building a new prefix as compaction does.\n\n### Cache Lifetime (TTL)\n\n| Authentication | Default TTL | How to change |\n|---|---|---|\n| Claude subscription (not over usage limit) | **1 hour** — automatic | Set `FORCE_PROMPT_CACHING_5M=1` to force 5 minutes |\n| Claude subscription (over usage limit, drawing on usage credits) | **5 minutes** — automatic | Set `ENABLE_PROMPT_CACHING_1H=1` to opt into 1 hour |\n| API key / Bedrock / Vertex / Foundry | **5 minutes** | Set `ENABLE_PROMPT_CACHING_1H=1` to opt into 1 hour |\n\nThe one-hour TTL bills cache writes at a higher rate than the five-minute TTL, but for subscription users it is included in the plan. For API key users, the write-rate premium is outweighed by reduced input token costs on long sessions.\n\nSubagents use the **5-minute TTL** even on a subscription — the automatic one-hour TTL applies only to the main conversation."
   },
   {
    "heading": "Build Setup Incrementally by Trigger",
    "body": "The worst way to set up a team's Claude Code configuration is to design it up front. The best way is to instrument friction as it occurs and add exactly the config artifact that addresses it. This gives you a setup where every line earns its place.\n\n### The Friction-to-Artifact Mapping\n\n| Observed friction | Appropriate artifact |\n|---|---|\n| Claude makes the same mistake twice | Add to CLAUDE.md (specific, verifiable instruction) |\n| Developer keeps typing the same multi-step procedure | Create a skill |\n| A CLAUDE.md section grew into a procedure, not facts | Move to a skill |\n| Rule only matters for one part of the codebase | Move to a path-scoped `.claude/rules/` file with `paths:` frontmatter |\n| Something must happen every time without relying on Claude | Write a hook |\n| A tool must never be called | Add to `permissions.deny` |\n| Claude keeps reading the wrong files to understand a subsystem | Write a subagent definition in `.claude/agents/` or a `context:fork` skill |\n| Developers override each other's model preferences | Lock `model` in `.claude/settings.json`; add `availableModels` + `enforceAvailableModels` in managed settings |\n| Long sessions lose instructions after compaction | Move to project-root CLAUDE.md or unscoped rules (not path-scoped, not skills) |\n| CI needs different permissions than interactive sessions | Use `--settings` flag with a CI-specific settings file |\n| A side task reads many files but produces a small summary | Delegate to a subagent or `context:fork` skill |\n\n### The Starting Point for a New Team Repo\n\nDo not start with a large CLAUDE.md. Start with three things:\n\n```bash\n# 1. Generate a starting CLAUDE.md from the codebase\n# (interactive multi-phase setup with CLAUDE_CODE_NEW_INIT=1)\nCLAUDE_CODE_NEW_INIT=1 claude\n# then run /init inside the session\n\n# 2. Commit a minimal settings.json with the most important deny rules\ncat > .claude/settings.json << 'EOF'\n{\n  \"permissions\": {\n    \"deny\": [\n      \"Read(./.env)\",\n      \"Read(./.env.*)\"\n    ],\n    \"allow\": [\n      \"Bash(npm run *)\",\n      \"Bash(git status)\",\n      \"Bash(git diff *)\"\n    ]\n  }\n}\nEOF\n\n# 3. Add personal override files to .gitignore\necho 'CLAUDE.local.md' >> .gitignore\necho '.claude/settings.local.json' >> .gitignore\n```\n\nThen add to the CLAUDE.md and settings.json as real friction appears. After two weeks you will have a configuration that reflects what your team actually needs rather than what you imagined they would need."
   },
   {
    "heading": "Common Pitfalls and Gotchas",
    "body": "### 1. Confusing Advisory Instructions with Enforcement\n\nThe most common mistake: writing \"never push directly to main\" in CLAUDE.md and believing it is enforced. It is not — it is a request. To enforce it, add a deny rule:\n\n```json\n{\n  \"permissions\": {\n    \"deny\": [\n      \"Bash(git push origin main)\",\n      \"Bash(git push --force *)\"\n    ]\n  }\n}\n```\n\n### 2. Model or Effort Switches Mid-Session\n\n`/model opus` or `/effort xhigh` can look free until you see the next turn is slow and expensive. Both invalidate the entire prompt cache. Claude Code shows a confirmation dialog before effort changes; model switches have a softer warning. If a task needs Opus or high effort, start the session with `claude --model opus --effort xhigh`, not mid-session.\n\n### 3. CLAUDE.md Edits Don't Apply Until Restart\n\nEditing CLAUDE.md mid-session has no effect on the running session — and also does not invalidate the cache. The content was read once at session start. If you make a critical fix, you must `/clear` or restart for it to apply.\n\n### 4. `@` Imports Don't Save Context\n\n`@path/to/file` imports in CLAUDE.md expand the referenced file into context at session start. They help with organization, not context savings. If you import a 500-line reference doc, all 500 lines load every session. Use a skill for on-demand loading instead.\n\n### 5. Skill Descriptions Don't Survive Compaction\n\nAfter `/compact`, the skill listing is not re-injected. Claude can still use skills you explicitly invoke with `/skill-name` (those appeared as messages and survive in the summary), but it no longer has the listing of available skills for auto-invocation. For skills that must be auto-invoked across long sessions, make their instructions an unscoped rule instead of a skill.\n\n### 6. Hooks Accumulate Across Scopes\n\nIf you define a `PostToolUse` hook in both `~/.claude/settings.json` and `.claude/settings.json`, both run. There is no override mechanism for hooks — they all fire. Managed hooks also cannot be disabled by `disableAllHooks` at a lower settings level.\n\n### 7. `CLAUDE.local.md` Doesn't Sync Across Worktrees\n\nA gitignored `CLAUDE.local.md` exists only in the worktree where you created it. For personal instructions that should follow you across all worktrees of the same repo, use an `@~/.claude/my-project-notes.md` import in your user-level `~/.claude/CLAUDE.md`.\n\n### 8. Extended Thinking Bills as Output Tokens\n\nThinking tokens are generated tokens and are billed at the output rate. At `xhigh` effort on Opus 4.7 or `high` on Opus 4.8, the thinking budget can be tens of thousands of tokens per request. For tasks that do not require deep reasoning — rename a variable, fix a typo, add a comment — run `/effort low` for that task. The difference in cost can be 5–10x per turn.\n\n### 9. PostToolUse Exit 2 Does Not Undo the Tool\n\nA `PostToolUse` hook that exits 2 does not reverse the tool that already ran — the file was already written, the command already executed. What exit 2 does in `PostToolUse` is **block the next model call** (block the loop), surfacing the stderr as an error. Use `PreToolUse` exit 2 to prevent a tool from running; use `PostToolUse` exit 2 to halt further Claude actions after an undesirable tool result.\n\n### 10. Subagent Definitions vs. Skills with `context: fork`\n\nCustom subagents (files in `.claude/agents/`) are persistent, reusable agent definitions with their own system prompt, model, and tool restrictions. A skill with `context: fork` runs its instructions in a forked subagent but inherits the parent's setup rather than defining a custom one. Use agent files for specialized workers you want Claude to auto-select; use `context: fork` in skills for one-off research or isolation of verbose operations."
   }
  ],
  "preQuiz": [
   {
    "prompt": "Your team commits a CLAUDE.md to the project root. One developer adds 450 lines of detailed API documentation, reasoning that more context helps Claude. After the next major model release, another developer wants to add a critical architecture rule but finds Claude ignoring it. What is the most likely root cause?",
    "options": [
     "The CLAUDE.md file needs to be placed in ~/.claude/ instead of the project root to be read reliably.",
     "A bloated file causes Claude to ignore real instructions, because important rules are lost in noise — and the full token cost loads on every request.",
     "CLAUDE.md only reads the first 200 lines or 25KB, so the architecture rule was likely truncated.",
     "Detailed API docs belong in CLAUDE.md so Claude can reference them without needing file reads."
    ],
    "correct": 1,
    "sectionIndices": [
     2
    ],
    "explanation": "CLAUDE.md loads its full content every request, inflating context cost everywhere. More critically, a bloated file causes Claude to ignore real instructions — the important rule is lost in noise. The 200-line/25KB cap applies to auto memory (MEMORY.md), not CLAUDE.md."
   },
   {
    "prompt": "A developer runs Claude from a package subdirectory (e.g., `packages/api/`). Which of the following accurately describes what loads?",
    "options": [
     "Only that subdirectory's CLAUDE.md loads; parent CLAUDE.md files are excluded to avoid conflicts.",
     "The subdirectory's CLAUDE.md plus every ancestor's CLAUDE.md load; sibling packages are excluded automatically.",
     "All CLAUDE.md files in the repo load, including sibling packages, because CLAUDE.md is additive.",
     "The repo-root CLAUDE.md takes precedence and the subdirectory CLAUDE.md is ignored unless the root imports it."
    ],
    "correct": 1,
    "sectionIndices": [
     7
    ],
    "explanation": "Launching from a subdirectory loads that directory's CLAUDE.md plus every ancestor's — giving a natural scope stack. Sibling package CLAUDE.md files are not loaded, which is the main reason to launch from a subdirectory when work is scoped there."
   },
   {
    "prompt": "Your team's `.claude/settings.json` defines a deny rule blocking edits to `.env`. A developer opens a git worktree at `/worktrees/feature-branch/`. They find that Claude readily edits `.env` in the worktree. Why?",
    "options": [
     "`settings.json` is only evaluated for PreToolUse hooks, not for permission deny rules.",
     "`settings.json` does NOT inherit from parent directories — it loads only from the starting directory; the worktree root needs its own copy.",
     "The deny rule syntax requires `paths:` frontmatter to apply in worktrees; a plain deny rule only applies to the main working tree.",
     "Deny rules in `settings.json` cover `Read` operations but not `Write` or `Edit` tool calls."
    ],
    "correct": 1,
    "sectionIndices": [
     6
    ],
    "explanation": "Unlike CLAUDE.md, `.claude/settings.json` does NOT inherit from parent directories — it loads only from the starting directory. A worktree session loads settings from the worktree root, so deny rules must also exist there."
   },
   {
    "prompt": "You have a skill that commits and pushes code. You notice it runs in the background and consumes context window space even when not explicitly invoked. What is the correct fix?",
    "options": [
     "Move the skill to `~/.claude/skills/` (user level) instead of the project `.claude/skills/` directory.",
     "Add `disable-model-invocation: true` to the skill's YAML frontmatter so it costs zero context until explicitly invoked with `/name`.",
     "Set the skill description to an empty string so the model does not auto-detect it as relevant.",
     "Use `claudeMdExcludes` in settings.json to suppress the skill from loading until needed."
    ],
    "correct": 1,
    "sectionIndices": [
     3
    ],
    "explanation": "`disable-model-invocation: true` in a skill's frontmatter prevents it from being auto-invoked by the model — it costs zero context until you call it explicitly with `/skill-name`. This is especially important for side-effecting skills like commit/deploy."
   },
   {
    "prompt": "A team member says: 'I added a rule to CLAUDE.md telling Claude never to edit .env, but it edited .env anyway.' What is the architecturally correct fix?",
    "options": [
     "Move the rule to the top of CLAUDE.md and add IMPORTANT: prefix so it gets higher priority.",
     "Add a `PreToolUse` hook in `.claude/settings.json` that blocks edits to `.env` — hooks are enforcement, CLAUDE.md is advisory.",
     "Add the rule to both the global `~/.claude/CLAUDE.md` and the project CLAUDE.md so it appears twice.",
     "Reduce the length of CLAUDE.md so Claude pays more attention to the .env rule."
    ],
    "correct": 1,
    "sectionIndices": [
     3
    ],
    "explanation": "Hooks run deterministically at lifecycle events — a PreToolUse hook can actually block a tool call. CLAUDE.md is advisory; Claude may still override it. Use hooks for anything that *must* happen every time."
   },
   {
    "prompt": "Your team installs an MCP server with 40 tools. After connecting it, you notice session startup is slower and your first-prompt context usage jumps significantly. What is the correct mental model for why this happens, and what should you check first?",
    "options": [
     "MCP servers load all tool schemas upfront at connection time; you should reduce the number of tools defined in the server.",
     "MCP tool names load at startup; full JSON schemas are deferred until a tool is used. But ENABLE_TOOL_SEARCH=false would load ALL schemas upfront — check that setting.",
     "MCP tool names load at startup and add constant overhead; running `/mcp` shows per-server token cost so you can disconnect unused servers.",
     "MCP schemas are compressed before loading; the overhead is from authentication handshakes, not token cost."
    ],
    "correct": 2,
    "sectionIndices": [
     11
    ],
    "explanation": "By default, only MCP tool names load at startup (schemas are deferred). Running `/mcp` shows per-server token cost and connection status — the right first step is to see which servers are expensive and disconnect ones you're not actively using."
   },
   {
    "prompt": "You're mid-session and Claude seems to have forgotten an instruction you gave earlier. You suspect auto-compaction fired. Which of the following correctly describes what was lost vs. what was preserved?",
    "options": [
     "Everything before the compaction point is lost; only the most recent 10 messages survive.",
     "paths:-frontmatter rules and nested subdirectory CLAUDE.md re-inject automatically after compaction; skill bodies are what gets lost.",
     "Project-root CLAUDE.md and auto memory re-inject from disk after compaction; paths:-frontmatter rules and nested subdirectory CLAUDE.md are NOT re-injected until a matching file is re-read.",
     "Auto-compaction preserves all invoked skill bodies in full; what's lost is the project-root CLAUDE.md which must be manually re-read."
    ],
    "correct": 2,
    "sectionIndices": [
     14
    ],
    "explanation": "After compaction, project-root CLAUDE.md, unscoped rules, and auto memory are re-injected from disk. What's lost until a matching file is read: paths:-frontmatter rules and nested subdirectory CLAUDE.md. Invoked skill bodies survive but are capped at 5,000 tokens/skill, truncated keeping the start."
   },
   {
    "prompt": "Your team has three repositories that all need the same set of hooks, a shared MCP server, and two shared skills. Currently each repo has its own copy. When should you package this as a plugin instead?",
    "options": [
     "Immediately — plugins are always preferable to per-repo copies because they eliminate drift.",
     "When a second repository needs the same setup — that is the explicit trigger for packaging as a plugin.",
     "When the number of repos exceeds five — smaller distributions are better managed as symlinks.",
     "Only when the setup includes MCP servers — hooks and skills alone don't justify a plugin."
    ],
    "correct": 1,
    "sectionIndices": [
     3,
     4
    ],
    "explanation": "The incremental trigger rule is explicit: 'Package as a plugin once a second repository needs it.' Below that threshold, per-repo copies are simpler. Plugins bundle skills, hooks, subagents, and MCP servers and are enabled via `enabledPlugins` rather than per-developer installs."
   },
   {
    "prompt": "A subagent is delegated to read 200 large log files and produce a summary. A colleague says: 'The subagent won't have access to our project's CLAUDE.md or the conversation history we built up — we need to pass those in.' Which part of this statement is accurate?",
    "options": [
     "Both parts are wrong — subagents fully inherit conversation history and CLAUDE.md from the parent session.",
     "Subagents do NOT inherit conversation history or auto memory, but they DO get their own copy of CLAUDE.md. The conversation history concern is correct; the CLAUDE.md concern is not.",
     "Subagents inherit both conversation history and CLAUDE.md, but not MCP connections — those must be passed explicitly.",
     "Subagents have their own shorter system prompt and do NOT inherit conversation history or auto memory. But they DO get their own CLAUDE.md copy. Only conversation history needs to be passed explicitly."
    ],
    "correct": 3,
    "sectionIndices": [
     12
    ],
    "explanation": "Subagents run in their own context window with their own shorter system prompt and their own CLAUDE.md copy. They do NOT inherit conversation history or auto memory. Context accumulated in the parent session must be passed explicitly in the subagent prompt."
   },
   {
    "prompt": "At session start, before your first prompt, the context window already has 8,000 tokens consumed. You have a global CLAUDE.md (300 lines), a project CLAUDE.md (150 lines), and 12 MCP servers connected with tool-search enabled. Which component is consuming the most tokens?",
    "options": [
     "The 12 MCP servers, because all tool schemas load at startup even with tool-search enabled.",
     "The global CLAUDE.md at 300 lines, because global files load in full and count against the window.",
     "The system prompt (~4.2K tokens) plus both CLAUDE.md files in full — the 12 MCP servers contribute only tool names with schemas deferred.",
     "Auto memory (MEMORY.md) at up to 25KB, because it loads before CLAUDE.md and is the largest component."
    ],
    "correct": 2,
    "sectionIndices": [
     9,
     10,
     11
    ],
    "explanation": "The system prompt is ~4.2K tokens; CLAUDE.md files load their full content every request. With tool-search enabled (the default), MCP servers contribute only tool names at startup — full JSON schemas are deferred. Auto memory loads up to 200 lines or 25KB, but both CLAUDE.md files together likely exceed that at 450 lines total."
   },
   {
    "prompt": "You notice that after using `/compact`, a skill that was auto-invoked earlier in the session is no longer influencing Claude's behavior. What explains this, and what is the correct architectural fix?",
    "options": [
     "Compaction deletes skills from `.claude/skills/` — you need to reinstall the skill after each compact.",
     "Skill descriptions are NOT re-injected after compaction — only skills you actually invoked persist (capped at 5,000 tokens). If the skill's body was too long, the relevant instructions may have been truncated. Move critical rules into project CLAUDE.md so they survive via disk re-injection.",
     "The skill is still active but compaction resets its priority — use `/skill-name` to re-invoke it explicitly.",
     "Compact strips all skills from context; you need to add `disable-model-invocation: false` to force re-injection."
    ],
    "correct": 1,
    "sectionIndices": [
     3,
     14
    ],
    "explanation": "After compaction, only skills that were actually invoked have their bodies re-injected — and only from the start of the file, capped at 5,000 tokens/skill. Skill descriptions are NOT re-injected. Critical always-needed rules belong in CLAUDE.md (re-injected from disk), not only in skills."
   },
   {
    "prompt": "You want Claude to avoid reading files in `./dist/` and `./vendor/` directories. You add these to `.gitignore`. Are they now excluded from Claude's file reads?",
    "options": [
     "Yes — Claude respects .gitignore for file access, the same way git does.",
     "No — you must add explicit `Read` deny rules in `permissions.deny` in `.claude/settings.json`, e.g. `\"Read(./**/dist/**)\"`. .gitignore does not control Claude's file access.",
     "Partially — .gitignore prevents Claude from reading via glob patterns, but not from direct path reads.",
     "No — you must add the paths to `claudeMdExcludes` in settings.json to block file reads."
    ],
    "correct": 1,
    "sectionIndices": [
     6
    ],
    "explanation": "`.gitignore` controls git tracking, not Claude's file access. To block Claude from reading generated or vendored directories, add explicit `Read` deny rules under `permissions.deny` in `.claude/settings.json`, e.g. `\"Read(./**/dist/**)\"`. Note: these cover built-in file tools and recognized Bash file commands, but don't filter denied paths from recursive-search output."
   },
   {
    "prompt": "Which precedence rule is DIFFERENT from what most people assume when first learning the four config levels (user, project, plugin, managed)?",
    "options": [
     "Skills follow managed > user > project, while CLAUDE.md is additive across all levels (conflicts resolved by Claude's judgment, not guaranteed more-specific wins).",
     "MCP servers follow project > user > local, so a project MCP server always overrides a user-level one.",
     "Hooks are exclusive — only the highest-precedence hook fires; lower-level hooks are suppressed.",
     "Subagents follow user > project > plugin, so user-defined agents always take priority over project agents."
    ],
    "correct": 0,
    "sectionIndices": [
     5
    ],
    "explanation": "The common mistake is assuming CLAUDE.md works like code precedence (more-specific wins). It is actually additive — all levels contribute, and conflicts are resolved by Claude's judgment with no guarantee. MCP servers follow local > project > user. Hooks MERGE — all matching hooks fire regardless of source."
   },
   {
    "prompt": "A developer keeps asking Claude the same multi-step deployment question every session by pasting a long procedure from Confluence. This has happened three times. According to the incremental setup model, what is the correct next action?",
    "options": [
     "Add the deployment procedure to CLAUDE.md so it loads automatically every session.",
     "Add a PreToolUse hook that injects the procedure when deployment-related commands are detected.",
     "Convert the repeated paste into a skill — a repeated prompt becomes a skill on the second time, a pasted multi-step playbook on the third time.",
     "Create a subagent with the procedure hardcoded in its system prompt."
    ],
    "correct": 2,
    "sectionIndices": [
     4
    ],
    "explanation": "The incremental trigger progression is: wrong twice → CLAUDE.md; repeated prompt → user-invocable skill; pasted multi-step playbook by the 3rd time → skill. Adding a 3x-repeated procedure to CLAUDE.md would bloat it with context that only matters for deployment sessions. A skill loads only when invoked."
   },
   {
    "prompt": "You grant `permissions.additionalDirectories` to `/data/shared/` so Claude can read large datasets there. A developer expects Claude to also pick up the CLAUDE.md in `/data/shared/.claude/CLAUDE.md` and auto-invoke skills from that directory. What actually happens?",
    "options": [
     "`additionalDirectories` grants full access: file read/write, CLAUDE.md loading, and skill discovery from that directory.",
     "`additionalDirectories` grants file read/write access only — it never loads that directory's CLAUDE.md, rules, or skills. Use `--add-dir` to also load skills and optionally CLAUDE.md.",
     "`additionalDirectories` loads CLAUDE.md from the added directory but not skills — skills require the directory to be set as the launch directory.",
     "`additionalDirectories` and `--add-dir` behave identically; the difference is only whether the flag is set at runtime or in config."
    ],
    "correct": 1,
    "sectionIndices": [
     6
    ],
    "explanation": "`permissions.additionalDirectories` grants file read/write access but NEVER loads that directory's CLAUDE.md, rules, or skills. To also load skills (and optionally CLAUDE.md via the environment variable `CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1`), use `--add-dir` or `/add-dir`."
   }
  ],
  "tasks": [
   {
    "id": "stage-8-task-claudemd",
    "afterSectionIdx": 2,
    "title": "Audit and tighten your project CLAUDE.md",
    "instructions": "1. Open (or create) your project's `CLAUDE.md` at the repo root:\n```bash\ncode ./CLAUDE.md\n# or: nano ./CLAUDE.md\n```\n2. For each line, ask: *Would removing this cause Claude to make mistakes on this project?* Delete lines that are:\n   - Inferable from the code itself\n   - Standard conventions (standard git commit format, etc.)\n   - Detailed API documentation (replace with a link)\n   - Never relevant to day-to-day Claude work in this repo\n3. Add `IMPORTANT:` or `YOU MUST` prefixes to any rule you've seen Claude skip.\n4. Check the line count:\n```bash\nwc -l CLAUDE.md\n```\n5. Commit the trimmed file:\n```bash\ngit add CLAUDE.md && git commit -m 'docs: trim CLAUDE.md to actionable instructions only'\n```\n6. Test by starting a new Claude Code session in the repo root and asking Claude to describe its instructions — verify the key rules appear in its answer.",
    "doneWhen": "Your CLAUDE.md is under 200 lines, committed to git, and a fresh Claude Code session can recite at least two of the key project rules you kept."
   },
   {
    "id": "stage-8-task-hooks-deny",
    "afterSectionIdx": 6,
    "title": "Add a PreToolUse hook to block .env edits",
    "instructions": "1. Open (or create) your project's `.claude/settings.json`:\n```bash\nmkdir -p .claude\ncode .claude/settings.json\n```\n2. Add a deny rule for `.env` and a PreToolUse hook. Paste this structure (merge with any existing content):\n```json\n{\n  \"permissions\": {\n    \"deny\": [\n      \"Edit(./.env)\",\n      \"Write(./.env)\"\n    ]\n  }\n}\n```\n3. Save the file, then start a fresh Claude Code session in the project root:\n```bash\nclaude\n```\n4. Ask Claude to edit `.env` (e.g., `\"add a line FOO=bar to .env\"`). Claude should refuse with a permission denial.\n5. Verify the deny rule is being read by running `/memory` inside the session and confirming the project settings loaded.\n6. Commit the settings file:\n```bash\ngit add .claude/settings.json && git commit -m 'config: deny .env edits via Claude Code permissions'  \n```",
    "doneWhen": "When asked to edit `.env`, Claude responds with a permission denial, not a modification — and the settings file is committed to git."
   },
   {
    "id": "stage-8-task-context-check",
    "afterSectionIdx": 13,
    "title": "Inspect your session context and disconnect unused MCP servers",
    "instructions": "1. Start a Claude Code session in a project you actively use:\n```bash\nclaude\n```\n2. Run the context breakdown command:\n```\n/context\n```\nRead the output: note which categories (CLAUDE.md, skills, MCP, conversation) are consuming the most tokens.\n3. Run the MCP status command to see per-server token cost:\n```\n/mcp\n```\nIdentify any servers listed that you have not used in this session (or rarely use).\n4. For each unused server, disconnect it:\n```\n/mcp disconnect <server-name>\n```\n5. Run `/context` again and note the reduction in MCP-attributed token usage.\n6. For servers you routinely don't need in a particular project, add them to the project's `.claude/settings.json` under `mcpServers` with `\"disabled\": true`, or remove them from the project-level MCP config altogether.",
    "doneWhen": "You have run `/context` and `/mcp`, identified at least one unused MCP server, disconnected it, and confirmed the context report shows reduced MCP token cost after disconnection."
   }
  ],
  "visualizations": [
   {
    "id": "stage-8-v",
    "kind": "comparison-table",
    "title": "Optimal use, context & cost",
    "textualSummary": "Key concepts of Optimal use, context & cost: Shared CLAUDE.md, Context as the fundamental constraint, Per-feature precedence and four config levels.",
    "columns": [
     "Concept",
     "In this stage"
    ],
    "rows": [
     {
      "label": "Shared CLAUDE.md",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "Context as the fundamental constraint",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "Per-feature precedence and four config levels",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "Build setup incrementally by trigger",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     }
    ]
   }
  ],
  "confusions": [
   {
    "misconception": "Skipping Optimal use, context & cost.",
    "correction": "Each stage is load-bearing for the setup."
   },
   {
    "misconception": "These concepts are interchangeable.",
    "correction": "Each has a distinct role; see the definitions."
   }
  ],
  "quiz": [
   {
    "id": "stage-8-q1",
    "type": "multiple-choice",
    "prompt": "Your team's CLAUDE.md has grown to 450 lines. Claude now occasionally ignores the 'never commit to main' rule buried on line 380. A teammate suggests adding 'IMPORTANT: NEVER COMMIT TO MAIN' to make it stick. What is the most likely root cause, and what should you do?",
    "options": [
     "Claude doesn't process lines beyond a certain token limit per request, so the rule is being truncated during loading.",
     "The file is too long — important rules are being lost in noise. The fix is to prune lines that don't cause mistakes if removed, not just add emphasis.",
     "The rule phrasing is ambiguous. You should rewrite it with clearer natural language rather than uppercase.",
     "CLAUDE.md reloads from disk only at session start, so a rule added mid-session won't apply until restart."
    ],
    "correct": 1,
    "explanation": "The content states: 'If Claude ignores a rule, the file is probably too long; if it asks questions the file answers, the phrasing is ambiguous.' A 450-line file far exceeds the recommended limit of 200 lines, and important rules get lost in noise. The teammate's instinct to add emphasis isn't wrong (emphasis does help adherence) but it doesn't fix the root cause — the bloat. Option A is wrong: CLAUDE.md full content loads every request, not truncated by line count. Option C mistakes the symptom — 'ignoring' maps to 'too long,' ambiguity maps to 'asking questions.' Option D is wrong: CLAUDE.md loads at session start by design, which is how it works correctly."
   },
   {
    "id": "stage-8-q2",
    "type": "multiple-choice",
    "prompt": "A teammate adds 'never edit .env files' to the project CLAUDE.md. Two days later, Claude edits a .env file while fixing a bug. The teammate escalates. What is the real fix?",
    "options": [
     "Move the rule to the global ~/.claude/CLAUDE.md so it takes precedence over the project file.",
     "Add IMPORTANT: YOU MUST NEVER EDIT .env to increase adherence weight.",
     "Create a PreToolUse hook that blocks writes to .env files — CLAUDE.md is advisory, not enforcement.",
     "Add .env to the permissions.deny list in .claude/settings.json so Claude's file tools skip it."
    ],
    "correct": 2,
    "explanation": "The content explicitly states: 'Use hooks — not CLAUDE.md — for anything that must happen every time: CLAUDE.md is advisory, hooks are enforcement. A PreToolUse hook can block editing .env or reject rm -rf /; \"never edit .env\" in CLAUDE.md is only a request.' A hook deterministically blocks the action; CLAUDE.md cannot guarantee it. Option A is wrong: CLAUDE.md at all levels is additive and conflicts are resolved by Claude's judgment, so global doesn't override project. Option B may increase adherence but still leaves it as advisory — not enforcement. Option D (deny rules) covers built-in file tools and recognized Bash file commands but is specifically for read/write permissions, not a general 'never edit' enforcement — and .env is a specific file, not a directory pattern; a hook is the right fit for this enforcement class."
   },
   {
    "id": "stage-8-q3",
    "type": "multiple-choice",
    "prompt": "Your monorepo has backend/ and frontend/ packages. Claude is launched from the repo root to fix a backend bug. Midway through, it reads a file in frontend/ and picks up a frontend-specific CLAUDE.md with React conventions that conflict with your backend style guide. What mechanism caused this, and how do you prevent it?",
    "options": [
     "Child directory CLAUDE.md files load automatically on session start — move frontend rules to a plugin instead.",
     "Child directory CLAUDE.md files are pulled in on demand when Claude reads a file there. Use claudeMdExcludes with an absolute-path glob to skip the frontend file when launching from root.",
     "Launching from root grants access to every subdirectory's CLAUDE.md at startup. The fix is to always launch from backend/ when doing backend work.",
     "CLAUDE.md files in child directories are never loaded — the conflict must come from a skill description bleeding into context."
    ],
    "correct": 1,
    "explanation": "The content states: 'Child directories — Pulled in on demand when Claude reads a file there.' This means the frontend CLAUDE.md was pulled in the moment Claude read a frontend file, not at startup. The content also describes claudeMdExcludes: 'claudeMdExcludes skips specific CLAUDE.md/rules by glob; patterns match absolute paths, so start relative-style patterns with **/' — this is the mechanism to suppress unwanted child CLAUDE.md files. Option A is wrong: child CLAUDE.md files don't load at startup. Option C is partially correct advice (launching from backend/ would scope access to that subtree) but misdiagnoses the mechanism — 'access to every subdirectory's CLAUDE.md at startup' is wrong. Option D is incorrect: child directory CLAUDE.md files are documented as loading on demand."
   },
   {
    "id": "stage-8-q4",
    "type": "multiple-choice",
    "prompt": "You run /context and see that MCP server tool schemas are consuming 40,000 tokens even though you only use one of the five connected MCP servers today. What is the most efficient fix?",
    "options": [
     "Set ENABLE_TOOL_SEARCH=false to defer all schema loading until a tool is actually called.",
     "Disconnect the servers you are not using — /mcp shows per-server token cost, and tool schemas for unused servers still consume context.",
     "Move MCP server connections from project settings to user settings so they load conditionally.",
     "Replace all MCP servers with CLI equivalents like gh, aws, or gcloud which add no per-tool listing to context."
    ],
    "correct": 1,
    "explanation": "The content states: '/mcp shows per-server token cost + connection status — disconnect servers you're not using.' Even with tool search on (schemas deferred until used), connected servers do incur some context cost and disconnecting unused ones is the direct fix for this session. Option A is wrong and backwards: ENABLE_TOOL_SEARCH=false loads ALL schemas upfront (the opposite of deferring); the default (tool search ON) already defers schemas. Option C is wrong: moving to user settings doesn't make loading conditional — connection status, not scope, determines cost. Option D is valid long-term advice but doesn't fix the immediate 40K token problem for this session where the MCP servers are already connected."
   },
   {
    "id": "stage-8-q5",
    "type": "multi-select",
    "prompt": "After a long session, Claude starts ignoring rules that were established in the first hour. Which of the following are valid explanations for this behavior, based on how compaction and context work? Select all that apply.",
    "options": [
     "Auto-compaction cleared early tool outputs and summarized older context, and rules that lived only in conversation history (not in CLAUDE.md) were silently lost.",
     "paths:-frontmatter rules that loaded during earlier file reads are not re-injected after compaction and must wait for a matching file to be re-read.",
     "Claude's context window is 200,000 tokens but rules stated early in the session are deprioritized by a recency weighting mechanism.",
     "The project CLAUDE.md survived compaction and was re-injected, but rules added inline to the chat did not persist.",
     "Skill descriptions are re-injected after compaction, which may have pushed earlier rules past the attention horizon."
    ],
    "correct": [
     0,
     1,
     3
    ],
    "explanation": "Three options are grounded in the content. A: 'Put persistent rules in CLAUDE.md, not conversation history — early instructions can silently vanish post-compaction.' Rules only in conversation history are lost. B: 'LOST until a matching file is re-read: paths:-frontmatter rules, nested subdirectory CLAUDE.md.' These are explicitly listed as not surviving compaction. D: Project-root CLAUDE.md is listed under what survives: 'Survives (re-injected from disk): system prompt, output style, project-root CLAUDE.md, unscoped rules, auto memory, invoked skill bodies.' So inline chat rules are lost while CLAUDE.md rules survive — D correctly identifies this contrast. Option C is wrong: there is no documented 'recency weighting mechanism' — the mechanism is compaction removing older content, not a weighting system. Option E is wrong: 'Skill descriptions are NOT re-injected — only skills you actually invoked persist.' Descriptions don't come back after compaction."
   },
   {
    "id": "stage-8-q6",
    "type": "multi-select",
    "prompt": "Your team wants to enforce that Claude never runs 'rm -rf' and also wants all engineers to get a recommended plugin when launching from the infra/ directory. Which mechanisms should you use? Select all that apply.",
    "options": [
     "Add 'never run rm -rf' to the project CLAUDE.md under a bold IMPORTANT header.",
     "Create a PreToolUse hook that inspects the command and blocks rm -rf executions.",
     "Create a SessionStart hook that detects the launch directory and recommends the infra plugin.",
     "Add rm -rf to permissions.deny in .claude/settings.json.",
     "Create a subagent with a tools: list that excludes the Bash tool so rm -rf is unreachable."
    ],
    "correct": [
     1,
     2
    ],
    "explanation": "B: The content explicitly cites this use case: 'A PreToolUse hook can block editing .env or reject rm -rf / — this is the enforcement mechanism for commands that must never run.' C: 'A SessionStart hook can recommend the right plugin per launch directory' — this exactly matches the requirement. Option A is wrong: CLAUDE.md is advisory and Claude has been shown to still violate it; hooks are enforcement. Option D is wrong: permissions.deny is for file read/write access patterns (e.g., Read(./**/dist/**)), not for blocking shell commands. Option E is wrong: excluding Bash entirely would break all shell commands and is a blunt instrument; the content describes subagents as context isolation tools (for research, large reads) not as a command-blocking mechanism."
   },
   {
    "id": "stage-8-q7",
    "type": "multiple-choice",
    "prompt": "A new engineer on your team installs a third-party plugin that includes a skill with side effects — it automatically sends a Slack notification whenever invoked. You want to ensure this skill doesn't fire during any normal prompt matching, only when explicitly invoked. What is the correct mechanism?",
    "options": [
     "Move the plugin from user settings to project settings so it only activates for project-scoped sessions.",
     "Use skillOverrides in settings to set disable-model-invocation: true for that skill, so it costs zero context and is only invoked explicitly.",
     "Delete the Slack MCP server the plugin depends on — without the server connection, the skill cannot execute its side effect.",
     "Add the skill name to claudeMdExcludes so it is excluded from context loading."
    ],
    "correct": 1,
    "explanation": "The content states: 'Set disable-model-invocation: true on skills with side effects (commit, deploy, send messages) so they cost zero context until invoked with /name. Use skillOverrides in settings to do the same for third-party skills you didn't author.' This is the exact mechanism for third-party skills you can't directly edit. Option A is wrong: moving between user and project settings changes scope/sharing, not invocation behavior. Option C is wrong: deleting the MCP server is destructive and could break other functionality; the correct fix is invocation control, not infrastructure removal. Option D is wrong: claudeMdExcludes skips CLAUDE.md/rules files by glob, not skills."
   },
   {
    "id": "stage-8-q8",
    "type": "multiple-choice",
    "prompt": "You have two git worktrees of the same repo: main/ and feature-branch/. You add a deny rule to main/.claude/settings.json that blocks reading files in vendor/. A teammate opens a Claude session from the feature-branch/ worktree and finds the deny rule is not active. Why?",
    "options": [
     "Deny rules in settings.json inherit from parent directories, so the rule in main/ should have applied — this is a bug in the worktree setup.",
     ".claude/settings.json does not inherit from parent directories and loads only from the starting directory. Settings in main/ are not inherited by the feature-branch/ worktree.",
     "Deny rules only apply when Claude is launched with the --deny flag; they don't load from settings files automatically.",
     "The deny rule was added to the wrong section — it should be in permissions.additionalDirectories, not permissions.deny."
    ],
    "correct": 1,
    "explanation": "The content states: '.claude/settings.json does NOT inherit from parent directories (unlike CLAUDE.md) — it loads only from the starting directory. Worktree sessions load settings from the worktree root, so deny rules/hooks needed in worktrees must also live in the repo-root .claude/settings.json (a second copy).' CLAUDE.md pulls from parent directories, but settings.json does not — this is a critical distinction. Option A gets the behavior backwards (settings.json doesn't inherit). Option C invents a --deny flag that does not exist in the content. Option D confuses two different config fields: permissions.deny is for deny rules; permissions.additionalDirectories grants access to additional directories."
   },
   {
    "id": "stage-8-q9",
    "type": "multiple-choice",
    "prompt": "A subagent is tasked with reading 500 log files and summarizing errors. A teammate asks: 'Will the subagent have access to the conversation history from our current session, and will the error summary it produces be added to our main context window?' What is the correct answer?",
    "options": [
     "Yes and yes — subagents share the parent session's context window and their full output is appended to the parent conversation.",
     "No and no — subagents run in their own context window and return only a summary; they do not inherit conversation history.",
     "Yes and no — subagents inherit conversation history as read-only context but return only a summary to the parent.",
     "No and yes — subagents don't inherit history but their full output including all file reads is passed back to the parent context."
    ],
    "correct": 1,
    "explanation": "The content states: 'Subagents run in their own context window and return only a summary. Subagents get their own context (own shorter system prompt, own CLAUDE.md copy, same MCP/skills) but do NOT inherit conversation history, auto memory, or already-invoked skills — pass needed context explicitly in the prompt.' This makes Option B correct: no inherited history, and only a summary (not full output) returns. Option A is wrong on both counts. Option C is wrong: subagents explicitly do NOT inherit conversation history. Option D is wrong: the summary (not full output) is what returns — which is the whole point of using subagents for context isolation."
   },
   {
    "id": "stage-8-q10",
    "type": "multiple-choice",
    "prompt": "Your team has a shared deployment skill that runs across three repos. It should load with its full body pre-loaded at subagent launch (not discovered on demand). How do you configure this?",
    "options": [
     "Add the skill to repo-root .claude/skills/ and set disable-model-invocation: false in the skill frontmatter.",
     "List the skill in the subagent's skills: frontmatter field — skills listed there are fully preloaded at launch.",
     "Add the skill to .claude/settings.json under enabledPlugins so it is always available globally.",
     "Place the skill in ~/.claude/skills/ so it loads at user scope before the subagent starts."
    ],
    "correct": 1,
    "explanation": "The content states: 'Skills in a subagent's skills: field are fully preloaded at launch.' This is the mechanism for ensuring a skill's full body is available in a subagent from the start, not discovered on demand. Option A is wrong: disable-model-invocation controls whether a skill can be auto-invoked; setting it to false (the default) doesn't pre-load the body in a subagent — that's what the skills: frontmatter does. Option C is wrong: enabledPlugins manages which plugins are enabled repo-wide; it doesn't control pre-loading within a subagent. Option D is wrong: user-scope placement determines discoverability, not subagent pre-loading — user-scope skills still follow the same discovery rules unless listed in the subagent's skills: field."
   },
   {
    "id": "stage-8-q11",
    "type": "multiple-choice",
    "prompt": "A teammate asks: 'I know skills follow managed > user > project precedence. Does the same precedence apply to MCP servers?' What is the correct answer?",
    "options": [
     "Yes — all Claude Code features follow managed > user > project precedence uniformly.",
     "No — MCP servers follow local > project > user precedence, which is the reverse of skills.",
     "No — MCP servers follow project > user > managed precedence, giving project-level config the highest priority.",
     "MCP servers have no precedence — all connected servers are always active regardless of scope."
    ],
    "correct": 1,
    "explanation": "The content explicitly lists different precedence orders for different features: 'Skills: managed > user > project. MCP servers: local > project > user.' These are deliberately different — MCP servers give local config the highest priority (the reverse of skills). Option A is wrong and the question is specifically designed to catch this misconception — precedence is NOT uniform across features. Option C invents a different order not in the content. Option D is wrong: precedence affects which server config wins when there are conflicts, not whether servers are active."
   },
   {
    "id": "stage-8-q12",
    "type": "multiple-choice",
    "prompt": "Your team uses permissions.additionalDirectories to grant Claude read/write access to a shared config directory outside the repo. A teammate notices that Claude isn't picking up the CLAUDE.md or custom skills stored in that directory. Why?",
    "options": [
     "CLAUDE.md in directories outside the repo root is never loaded regardless of how access is granted.",
     "permissions.additionalDirectories grants file read/write but never loads that directory's CLAUDE.md, rules, or skills. Use --add-dir or /add-dir to load those.",
     "Skills require being listed in .claude/settings.json before they load from any directory — the directory grant alone is insufficient.",
     "The CLAUDE.md in the additional directory has lower precedence than the project CLAUDE.md and is overridden silently."
    ],
    "correct": 1,
    "explanation": "The content states: 'permissions.additionalDirectories grants file read/write but never loads that dir's CLAUDE.md/rules/skills. --add-dir / /add-dir loads skills, and loads CLAUDE.md/rules only if CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1.' The distinction is intentional: additionalDirectories is about file access, not instruction/skill loading. --add-dir is the mechanism for loading instructions from an additional directory. Option A is wrong: CLAUDE.md outside the repo CAN load if accessed via --add-dir with the right env var. Option C invents a settings.json registration requirement for skills. Option D is wrong: the issue isn't precedence — the CLAUDE.md simply isn't loaded at all via additionalDirectories."
   },
   {
    "id": "stage-8-q13",
    "type": "multiple-choice",
    "prompt": "You notice that auto-memory (MEMORY.md) isn't fully loading — Claude seems unaware of entries beyond a certain point. What is the loading constraint on auto-memory?",
    "options": [
     "Auto-memory loads the most recently modified 200 entries, oldest entries are dropped.",
     "Auto-memory loads the first 200 lines or 25KB, whichever comes first.",
     "Auto-memory loads up to 50KB or the entire file if it fits within 10% of the context window.",
     "Auto-memory is limited to 5,000 tokens per file, matching the per-skill cap."
    ],
    "correct": 1,
    "explanation": "The content states: 'auto memory MEMORY.md (first 200 lines or 25KB, whichever first).' Both constraints apply and the first one hit wins. Option A invents a 'most recently modified entries' semantics — it's first lines, not recent entries. Option C invents a 50KB cap and a 10% window rule (that rule applies to ENABLE_TOOL_SEARCH=auto for MCP schemas, not auto-memory). Option D invents a 5,000-token cap — that's the per-skill cap for invoked skills post-compaction, not auto-memory."
   },
   {
    "id": "stage-8-q14",
    "type": "multiple-choice",
    "prompt": "When should a repeated team process graduate from a pasted multi-step playbook in chat to a skill? And when should a skill graduate to a plugin?",
    "options": [
     "Pasted playbooks should become skills after 5+ uses; skills become plugins when they include MCP servers.",
     "A pasted multi-step playbook used a 3rd time should become a skill; a skill should be packaged as a plugin once a second repository needs it.",
     "Both should be formalized after a sprint retrospective identifies them as high-frequency — the trigger is process-level, not use-count.",
     "Pasted playbooks become skills when they exceed 10 steps; skills become plugins when they need admin approval to install."
    ],
    "correct": 1,
    "explanation": "The content states the explicit trigger sequence: 'A pasted multi-step playbook (3rd time) → skill. A repo needing the same setup → package as a plugin.' These are concrete, countable triggers, not vague process gates. Option A invents '5+ uses' and an incorrect plugin trigger (MCP inclusion is not the criterion). Option C is wrong: the triggers are explicitly use-count and cross-repo need, not retrospective timing. Option D invents '10 steps' and 'admin approval' — neither appears in the content."
   }
  ],
  "masteryCheckpoint": "You can explain the concepts of Optimal use, context & cost."
 },
 {
  "id": "stage-9",
  "stage": 9,
  "title": "Surfaces & mobility",
  "summary": "Surfaces & mobility: surfaces share one engine and config, auth method determines available features, config mobility across surfaces.",
  "prerequisites": [
   "stage-8"
  ],
  "objectives": [
   "Understand the concepts in Surfaces & mobility."
  ],
  "definitions": [
   {
    "term": "surfaces share one engine and config",
    "short": "Different ways to run Claude Code (CLI, Desktop, IDE, web) vary in capabilities, but several share the same engine and read the same config files, so one change can affect multiple surfaces at once."
   },
   {
    "term": "auth method determines available features",
    "short": "How a team authenticates (claude.ai OAuth vs API key vs governed third-party provider) silently enables or disables whole feature sets, so auth is the first team decision to standardize."
   },
   {
    "term": "config mobility across surfaces",
    "short": "Each surface carries a different subset of configuration, so where you put a setting determines whether it reaches a given execution environment."
   },
   {
    "term": "managed (admin-governed) settings",
    "short": "Server-managed settings files override project and user settings and cannot be overridden by users, giving admins a central enforcement layer for org policy."
   },
   {
    "term": "managed-settings distribution gap",
    "short": "How a managed policy actually reaches a device differs by surface, so a centrally configured rule does not automatically apply everywhere."
   },
   {
    "term": "cloud session environments",
    "short": "Web/remote sessions run on isolated managed VMs whose network access and setup script are defined per named environment, controlling what the agent can reach and install."
   }
  ],
  "sections": [
   {
    "heading": "The surface taxonomy: one engine, multiple shells",
    "body": "Claude Code runs as a single engine — the same CLI binary, same settings hierarchy, same MCP protocol — wrapped in different presentation shells depending on where you launch it. Understanding this prevents the most common team configuration mistake: treating each surface as a separate product with its own config silo.\n\nThe five first-party surfaces are Terminal CLI, VS Code extension (also Cursor and other VS Code forks), JetBrains plugin, Desktop app (macOS and Windows only — not available on Linux), and Web (claude.ai/code). Beyond these, Claude Code integrates into GitHub Actions, GitLab CI, Slack, Chrome, Routines, and custom agents via the Agent SDK.\n\nThe statement that captures this:\n\n> Each surface connects to the same underlying Claude Code engine, so your CLAUDE.md files, settings, and MCP servers work across all of them.\n\nBut \"the same engine\" has a precise meaning that requires unpacking. It does not mean every surface reads every config file. It means the settings resolution logic and the permission/tool model are shared. What actually reaches a given execution environment depends on which config files that surface can physically access at runtime.\n\n| Surface | Reads `~/.claude/settings.json` | Reads `.claude/settings.json` | Reads managed-settings | IDE context injected | `apiKeyHelper` supported |\n|---------|--------------------------------|-------------------------------|------------------------|----------------------|-------------------------|\n| Terminal CLI | Yes | Yes | Yes | No | Yes |\n| VS Code extension | Yes (shared with CLI) | Yes | Yes | Yes (auto) | No |\n| JetBrains plugin | Yes (shared with CLI) | Yes | Yes | Yes (auto) | No |\n| Desktop app | Yes | Yes | Yes (MDM/plist/registry) | No | No |\n| Web / cloud session | No | Yes (from git clone) | Server-managed only | No | No |\n| CI (GitHub Actions) | No | Yes (from clone) | No | No | Via env vars |\n\nSeveral distinctions that save debugging time:\n\n**VS Code extension vs. CLI binary**: the extension bundles its own private copy of the CLI for its chat panel but does NOT add `claude` to your PATH. If a developer wants to run `claude` in a terminal inside VS Code, they must also install the standalone CLI separately. Both the extension and the standalone CLI read the same `~/.claude/settings.json` and `.mcp.json`, which is why a permission rule added via the CLI shows up in the extension's next session without any sync step.\n\n**Desktop app platform support**: the Desktop app runs on macOS (Intel and Apple Silicon universal build) and Windows (x64 and ARM64). There is no Linux Desktop app. Linux users use the CLI.\n\n**Web sessions and user config**: a cloud session clones your repository and has access only to what is in that clone. User-scoped files — `~/.claude/settings.json`, `~/.claude/CLAUDE.md`, `~/.claude/skills/`, MCP servers added with `claude mcp add` — are not available in cloud sessions. Commit what you need into `.claude/` in the repo."
   },
   {
    "heading": "Auth method as a feature gate: the decision that shapes everything else",
    "body": "Authentication is not just an identity concern. It is the primary selector for which features are available at all. Your team must align on an auth strategy before deploying settings, because the wrong choice silently disables capabilities you may be counting on.\n\n### The six auth methods in precedence order\n\nWhen Claude Code starts, it picks credentials in this order and stops at the first match:\n\n1. Cloud provider env vars (`CLAUDE_CODE_USE_BEDROCK`, `CLAUDE_CODE_USE_VERTEX`, `CLAUDE_CODE_USE_FOUNDRY`)\n2. `ANTHROPIC_AUTH_TOKEN` (bearer token for LLM gateway routing, sent as `Authorization: Bearer`)\n3. `ANTHROPIC_API_KEY` (direct Anthropic API key, sent as `X-Api-Key`)\n4. `apiKeyHelper` script output (dynamic/rotating credentials, called on startup then every 5 minutes or on HTTP 401)\n5. `CLAUDE_CODE_OAUTH_TOKEN` (long-lived OAuth token from `claude setup-token`)\n6. Subscription OAuth from `claude auth login` (claude.ai Pro/Max/Team/Enterprise)\n\nThe precedence matters operationally. If a developer has `ANTHROPIC_API_KEY` set in their shell and also has a Teams subscription, the API key wins once approved, and they will be billed against the Console workspace, not the subscription. If that key belongs to a disabled org, every call fails. This is a common foot-gun during migration. Fix: run `unset ANTHROPIC_API_KEY` and confirm the active method with `/status`.\n\n### Feature availability by auth method\n\n| Feature | claude.ai OAuth (Pro/Max/Team/Enterprise) | Console API key | Bedrock/Vertex/Foundry |\n|---------|------------------------------------------|-----------------|------------------------|\n| Claude Code on the web (cloud sessions) | Yes | No | No |\n| `--teleport` (web-to-terminal session handoff) | Yes | No | No |\n| Remote Control | Yes | No | No |\n| Server-managed settings delivery | Yes (Teams/Enterprise) | No | No |\n| Routines (Anthropic-managed cron) | Yes | No | No |\n| `claude setup-token` (CI OAuth token) | Yes (Pro/Max/Team/Enterprise) | No | No |\n| `apiKeyHelper` / `ANTHROPIC_API_KEY` override | No (CLI only) | Yes | N/A |\n| IAM-based access control | No | No | Yes |\n| Cloud provider billing (AWS Cost Explorer, etc.) | No | No | Yes |\n| VPC / private endpoint routing | No | No | Yes |\n\nThe most operationally significant row: server-managed settings delivery requires claude.ai subscription OAuth. If your team routes through Bedrock for data residency or IAM policy reasons, you lose the server-managed settings channel entirely and must fall back to endpoint-managed settings (MDM plist, Windows registry, or system `managed-settings.json`).\n\n### Standardizing team auth\n\nFor most teams, Claude for Teams or Enterprise is the right starting point. Members authenticate with claude.ai accounts, get centralized billing, web access, and the managed-settings channel. Enterprise adds SSO/domain capture and the compliance API.\n\nFor AWS-native teams with data residency requirements: Bedrock is appropriate, but you lose server-managed settings, web sessions, and teleport. You gain VPC routing and CloudTrail audit logs.\n\nFor CI-only usage with a Teams or Enterprise subscription: use `claude setup-token` to generate a one-year OAuth token per developer, set as `CLAUDE_CODE_OAUTH_TOKEN` in the CI environment. This is scoped to inference only and cannot establish Remote Control sessions.\n\n```bash\n# Run once per developer, copy output to CI secret manager\nclaude setup-token\nexport CLAUDE_CODE_OAUTH_TOKEN=<printed-token>\n```\n\n**Critical gotchas**:\n- `apiKeyHelper`, `ANTHROPIC_API_KEY`, and `ANTHROPIC_AUTH_TOKEN` apply to Terminal CLI sessions only. The Desktop app and cloud (web) sessions use OAuth exclusively and ignore these variables. If your team's credential rotation script works in the terminal but breaks in the Desktop app, this is why.\n- Bare mode (`--bare` flag) does NOT read `CLAUDE_CODE_OAUTH_TOKEN`. If your scripted call uses `--bare`, authenticate with `ANTHROPIC_API_KEY` or an `apiKeyHelper` instead.\n- `ANTHROPIC_API_KEY` in a cloud (web) session's environment variables does not override subscription credentials — the docs confirm cloud sessions always use subscription OAuth.\n- The `apiKeyHelper` slow-notice threshold is 10 seconds. If your vault call is slow, optimize the credential fetch script."
   },
   {
    "heading": "Config mobility: where you put it determines who gets it",
    "body": "The settings hierarchy has four scopes: Managed > Local > Project > User (in override order, highest first). But the mobility question — \"will this setting reach the execution environment I care about?\" — is different from the precedence question.\n\n### The four config scopes and their files\n\n```\n# Managed (highest precedence, cannot be overridden)\n/Library/Application Support/ClaudeCode/managed-settings.json  # macOS system\n/etc/claude-code/managed-settings.json                          # Linux/WSL\nC:\\Program Files\\ClaudeCode\\managed-settings.json               # Windows\n# Note: C:\\ProgramData\\ClaudeCode\\managed-settings.json is DEPRECATED as of v2.1.75\n~/.claude/remote-settings.json                                  # Server-delivered cache (read-only, internal)\n\n# Local (user + this repo only, gitignored)\n.claude/settings.local.json\nCLAUDE.local.md\n\n# Project (shared with team via git)\n.claude/settings.json\n.mcp.json\nCLAUDE.md\n.claude/CLAUDE.md\n\n# User (your personal defaults, all projects)\n~/.claude/settings.json\n~/.claude/CLAUDE.md\n~/.claude.json  # OAuth session, user-scoped MCP servers, per-project state\n```\n\n### The mobility table\n\nThis is the table to internalize before deploying any setting:\n\n| Config location | Terminal CLI | VS Code ext | Desktop app | Web/cloud session | CI clone |\n|----------------|-------------|-------------|-------------|-------------------|----------|\n| `~/.claude/settings.json` | Yes | Yes (shared) | Yes | **No** | **No** |\n| `.claude/settings.json` | Yes | Yes | Yes | Yes (from clone) | Yes (from clone) |\n| `.mcp.json` | Yes | Yes | Yes | Yes (from clone) | Yes (from clone) |\n| `~/.claude/CLAUDE.md` | Yes | Yes | Yes | **No** | **No** |\n| `CLAUDE.md` / `.claude/CLAUDE.md` (repo) | Yes | Yes | Yes | Yes | Yes |\n| `~/.claude/skills/`, `~/.claude/agents/` | Yes | Yes | Yes | **No** | **No** |\n| `.claude/skills/`, `.claude/agents/` (repo) | Yes | Yes | Yes | Yes | Yes |\n| System `managed-settings.json` | Yes | Yes | Yes | **No** | **No** |\n| Server-managed settings | Yes (OAuth only) | Yes (OAuth only) | Yes (OAuth only) | Yes | **No** |\n| User-scoped MCP (`claude mcp add`) | Yes | Yes | Yes | **No** | **No** |\n\nThe practical consequence: **anything you want in a cloud session must be committed to the repository**. User-scoped settings, personal CLAUDE.md, and locally-added MCP servers (`claude mcp add` writes to `~/.claude.json`) do not reach cloud VMs. The cloud session clones your repo and has access only to what is in the clone.\n\nFor MCP servers specifically: the project-scoped `.mcp.json` is the file that travels to cloud sessions. MCP servers added with `claude mcp add` at user scope are silently absent from cloud runs — this is a frequent source of confused debugging.\n\n### Setting location decision rule\n\n- **Team standard that applies everywhere**: `.claude/settings.json` (checked in) — this is the mobility workhorse\n- **Personal preference that should not affect teammates**: `~/.claude/settings.json`\n- **Local experiment that should not commit**: `.claude/settings.local.json` (gitignored by convention)\n- **Policy that must not be user-overridable**: managed scope only (server-managed or endpoint-managed)\n- **CI-only behavior**: `SessionStart` hook in `.claude/settings.json` gated on `CLAUDE_CODE_REMOTE=true`\n\nFor environment variables, `settings.json` `env` blocks carry through to all local surfaces but not cloud sessions. For cloud, set env vars in the environment configuration UI, not in `settings.json`."
   },
   {
    "heading": "Managed (admin-governed) settings: the enforcement layer",
    "body": "Managed settings are the only config scope that cannot be overridden by users or project settings. They are the org-policy enforcement layer.\n\n### Two delivery mechanisms\n\n**Server-managed settings** (Teams/Enterprise only): configured in the claude.ai admin console at Admin Settings > Claude Code > Managed settings. Requires Claude Code v2.1.38+ (Teams) or v2.1.30+ (Enterprise). Delivered as JSON at startup and polled hourly during active sessions. Cached internally on the client (read-only). Requires `api.anthropic.com` access and claude.ai OAuth auth — not available for Bedrock/Vertex/Foundry deployments.\n\n**Endpoint-managed settings**: deployed by IT to devices via MDM or file drop. Stronger security guarantees on managed devices because the OS protects the files from user modification. Three sub-mechanisms:\n- macOS managed preferences plist (`com.anthropic.claudecode` domain)\n- Windows registry (`HKLM\\SOFTWARE\\Policies\\ClaudeCode`, JSON in `Settings` REG_SZ)\n- System directory `managed-settings.json` files\n\nWithin the managed tier, the first source that delivers a non-empty configuration wins. If server-managed settings deliver any keys at all, endpoint-managed settings are ignored entirely for that session. If server-managed delivers nothing (empty config or fetch failed), endpoint-managed applies. They do not merge.\n\n### Drop-in directory for composable policies\n\nThe `managed-settings.d/` directory alongside `managed-settings.json` lets you compose policies from multiple files, following the systemd convention:\n\n```\n/etc/claude-code/\n  managed-settings.json          # base policy\n  managed-settings.d/\n    10-telemetry.json             # OTEL config\n    20-security.json              # deny rules\n    30-mcp-policy.json            # MCP allowlist\n```\n\nMerge rules: `managed-settings.json` applies first, then `*.json` files in the drop-in directory sorted alphabetically. Scalars: later files win. Arrays: concatenate and deduplicate. Objects: deep-merge. Hidden files starting with `.` are ignored. Use numeric prefixes to control order explicitly.\n\n### Managed-only keys (cannot be set elsewhere)\n\nSome keys are only enforced when present in the managed scope. Placing them in project or user settings has no effect. The full list from the official docs includes:\n\n```json\n{\n  \"allowAllClaudeAiMcps\": true,\n  \"allowedChannelPlugins\": [\"...\"],\n  \"allowManagedHooksOnly\": true,\n  \"allowManagedMcpServersOnly\": true,\n  \"allowManagedPermissionRulesOnly\": true,\n  \"blockedMarketplaces\": [\"...\"],\n  \"channelsEnabled\": true,\n  \"forceRemoteSettingsRefresh\": true,\n  \"pluginTrustMessage\": \"...\",\n  \"sandbox.filesystem.allowManagedReadPathsOnly\": true,\n  \"sandbox.network.allowManagedDomainsOnly\": true,\n  \"strictKnownMarketplaces\": [\"...\"],\n  \"strictPluginOnlyCustomization\": true,\n  \"wslInheritsWindowsSettings\": true\n}\n```\n\nNote: `forceLoginMethod`, `forceLoginOrgUUID`, `claudeMd`, and `policyHelper` are NOT in the managed-only list — they work from any settings scope but are most useful in managed settings where they cannot be overridden. `policyHelper` and `wslInheritsWindowsSettings` are restricted to OS-level policy sources and cannot be delivered via server-managed settings.\n\n### A minimal team security baseline\n\n```json\n// Server-managed console or /etc/claude-code/managed-settings.json\n{\n  \"permissions\": {\n    \"deny\": [\n      \"Bash(curl *)\",\n      \"Bash(wget *)\",\n      \"Read(./.env)\",\n      \"Read(./.env.*)\",\n      \"Read(./secrets/**)\"\n    ],\n    \"disableBypassPermissionsMode\": \"disable\"\n  },\n  \"allowManagedPermissionRulesOnly\": true,\n  \"requiredMinimumVersion\": \"2.1.100\",\n  \"autoUpdatesChannel\": \"stable\"\n}\n```\n\n`allowManagedPermissionRulesOnly` means user and project `permissions` blocks are ignored entirely — only the managed deny list applies. `disableBypassPermissionsMode` prevents users from entering bypass mode; note this key works from any settings scope but is most meaningful in managed settings.\n\n### Fail-safe parsing\n\nManaged settings parse tolerantly (requires v2.1.169+): a single malformed entry is stripped with a warning, not rejected. The salvaged (valid-only) payload is what gets cached and presented in the security approval dialog. Security-enforcement fields have special failure modes:\n- `allowedMcpServers`: invalid entry → enforced as empty allowlist (nothing allowed)\n- `allowManagedMcpServersOnly`: invalid → treated as `true` (locks down MCP)\n\nThis means a typo in `allowedMcpServers` can silently block all MCP access across your org. Validate with `claude --debug-file /tmp/debug.log` and inspect for `Remote settings` log lines on a test machine before rolling out."
   },
   {
    "heading": "The managed-settings distribution gap",
    "body": "Configuring policy in the admin console does not automatically enforce it everywhere. The gap between \"policy is configured\" and \"policy reaches every execution environment\" is the most important operational reality for a team lead deploying managed settings.\n\n### Why the gap exists\n\nDifferent surfaces receive managed settings through different channels:\n\n| Surface | Delivery channel | When policy applies | Gap risk |\n|---------|-----------------|---------------------|----------|\n| Terminal CLI (macOS/Linux) | System `managed-settings.json` or server pull | At startup + hourly | Low (if MDM deployed) |\n| Desktop app | MDM plist / registry / server pull | At startup | Low (same as CLI) |\n| VS Code extension | Same as CLI (shared config) | At startup | Low |\n| Web / cloud session | Server-managed only (per-session fetch) | At session start | Zero for cloud-native; N/A for endpoint |\n| CI pipelines | **Nothing — no managed channel reaches CI** | Never | **High** |\n| Bedrock/Vertex users | Endpoint-managed only (server-managed blocked) | At startup (if deployed) | Medium |\n\nCI is the critical gap. Cloud sessions (web) benefit from server-managed settings because they authenticate with claude.ai OAuth and fetch settings at session start. But a CI job running `claude -p \"...\"` with an API key or OAuth token against the Anthropic API does not pull server-managed settings, and there is no MDM to deploy endpoint-managed settings to ephemeral CI runners.\n\nFor CI policy enforcement, your options are:\n1. Commit permission rules and hooks in `.claude/settings.json` (project scope, readable by all)\n2. Use explicit `--allowedTools` / `--disallowedTools` flags in the CI invocation script\n3. Run CI through a self-hosted runner that has endpoint-managed settings pre-deployed\n4. Use the Agent SDK with custom permission logic wrapping the claude invocation\n\n### The server-managed settings window\n\nAt first launch with no cached settings, Claude Code fetches settings asynchronously. There is a brief window at startup where restrictions are not yet enforced. With `forceRemoteSettingsRefresh: true`, the CLI blocks at startup until the fetch succeeds — or exits if the fetch fails. This is the fail-closed option:\n\n```json\n{ \"forceRemoteSettingsRefresh\": true }\n```\n\nImportant nuance (v2.1.139+): `claude auth` subcommands (e.g., `claude auth login`) are exempt from the fail-closed check. This prevents a locked-out user who needs to re-authenticate from being stuck in a catch-22 where expired credentials prevent the settings fetch needed to run.\n\nBefore enabling `forceRemoteSettingsRefresh`, ensure `api.anthropic.com` is reachable on your network. If the endpoint is unreachable, the CLI exits and users cannot start Claude Code at all — a hard dependency on Anthropic's infrastructure.\n\n### The security approval dialog\n\nServer-managed settings that include hooks, custom environment variables, or shell commands require explicit user approval on first delivery. A dialog describes what is being configured. If the user rejects it, the CLI exits. In non-interactive (headless) mode with `-p`, the dialog is skipped and settings apply without approval. This creates an asymmetry: interactive sessions get a consent gate; automated pipelines do not.\n\nThe security approval dialog evaluates the salvaged payload (invalid entries already stripped), so a stripped invalid entry is never presented for approval and never executes.\n\n### The bypass via third-party provider\n\nA developer who sets `CLAUDE_CODE_USE_BEDROCK=1` causes Claude Code to route through Bedrock. Server-managed settings are silently bypassed — Bedrock deployments cannot receive server-managed settings at all. Endpoint-managed settings still apply if deployed to the device. To prevent this bypass, deploy endpoint-managed settings containing a `forceLoginMethod: \"claudeai\"` value through MDM, which prevents Claude Code from authenticating via third-party providers."
   },
   {
    "heading": "Cloud session environments: isolated managed VMs",
    "body": "Cloud sessions run on Anthropic-managed VMs provisioned on demand, running Ubuntu 24.04. Each session gets a fresh environment. Understanding what that environment contains — and what it does not — is prerequisite to writing tasks that actually work.\n\n### What the cloud environment provides\n\nEach VM starts with a substantial pre-installed toolchain:\n\n| Category | Included |\n|----------|----------|\n| Python | 3.x, pip, poetry, uv, black, mypy, pytest, ruff |\n| Node.js | 20, 21, 22 via nvm; npm, yarn, pnpm, bun¹, eslint, prettier, chromedriver |\n| Ruby | 3.1, 3.2, 3.3 with gem, bundler, rbenv |\n| PHP | 8.4 with Composer |\n| Java | OpenJDK 21, Maven, Gradle |\n| Go | Latest stable with module support |\n| Rust | rustc, cargo |\n| C/C++ | GCC, Clang, cmake, ninja, conan |\n| Docker | docker, dockerd, docker compose |\n| Databases | PostgreSQL 16, Redis 7.0 (not running by default) |\n| Utilities | git, jq, yq, ripgrep, tmux, vim, nano |\n\n¹ Bun has known proxy compatibility issues for package fetching in cloud sessions.\n\nFor exact versions: ask Claude to run `check-tools` in a cloud session (this command only exists in cloud sessions). PostgreSQL and Redis are pre-installed but not started; ask Claude to run `service postgresql start` per session if needed.\n\nResource ceilings (approximate, may change): 4 vCPUs, 16 GB RAM, 30 GB disk. Memory-intensive builds may fail. For workloads beyond these limits, use Remote Control to run Claude Code on your own hardware.\n\n### What the cloud environment does NOT have\n\nThis is the more operationally important list:\n\n| Missing | Why |\n|---------|-----|\n| `~/.claude/CLAUDE.md`, `~/.claude/skills/`, `~/.claude/agents/` | Live on your machine, not in the repo |\n| User-scoped MCP servers (`claude mcp add`) | Those write to `~/.claude.json`, not the repo |\n| Plugins enabled only in user settings | `enabledPlugins` in `~/.claude/settings.json` not available; put in repo's `.claude/settings.json` |\n| Dedicated secrets store | None yet. Env vars are stored in environment config visible to anyone who can edit the environment |\n| Interactive auth (AWS SSO, browser OAuth) | Not supported — SSO requires browser-based login |\n| The `gh` CLI | Not pre-installed. Install in setup script + provide `GH_TOKEN` env var |\n| `@-mention` files in prompts | Not available in cloud sessions |\n\n### Named environments: the config unit\n\nAn environment is a named configuration bundle with: network access level, environment variables, and setup script. You can have multiple environments (e.g., `default`, `restricted-network`, `data-science`). The default environment for `--remote` sessions is set with `/remote-env` in your terminal. Add, edit, and archive environments from the web interface.\n\nEnvironment variables use `.env` format — one `KEY=value` pair per line without quotes (quotes are stored as part of the value):\n\n```text\nNODE_ENV=production\nLOG_LEVEL=warn\nDATABASE_URL=postgres://localhost:5432/myapp\n```\n\n### Environment caching\n\nThe setup script runs once, then Anthropic snapshots the filesystem. Subsequent sessions in the same environment skip the setup script and start from the snapshot. The cache captures files, not running processes. The cache is rebuilt when the setup script or allowed network hosts change, and expires after roughly 7 days. Setup scripts run as root on Ubuntu 24.04; `apt install` and most language package managers work directly.\n\n### Setup script vs. SessionStart hook: the split\n\n| | Setup script | SessionStart hook |\n|--|-------------|-------------------|\n| Attached to | Cloud environment config (UI) | Repo `.claude/settings.json` |\n| Runs | Before Claude launches, cached after first run | After Claude launches, every session including resumed |\n| Scope | Cloud only | Both local and cloud |\n| Use for | Install tooling the cloud lacks but your laptop has | Project bootstrap that should run everywhere |\n\nFor cloud-only install steps that should not run locally:\n\n```json\n// .claude/settings.json\n{\n  \"hooks\": {\n    \"SessionStart\": [{\n      \"matcher\": \"startup|resume\",\n      \"hooks\": [{\n        \"type\": \"command\",\n        \"command\": \"\\\"$CLAUDE_PROJECT_DIR\\\"/scripts/cloud-init.sh\"\n      }]\n    }]\n  }\n}\n```\n\n```bash\n#!/bin/bash\n# scripts/cloud-init.sh\nif [ \"$CLAUDE_CODE_REMOTE\" != \"true\" ]; then\n  exit 0\nfi\nnpm ci\npip install -r requirements.txt\n```\n\nNote: SessionStart hooks have a limitation — they run on every session start and resume, unlike setup scripts which benefit from caching. Keep install scripts fast by checking whether dependencies are already present.\n\n### Network access levels\n\n| Level | Outbound access |\n|-------|----------------|\n| None | No outbound (Claude API still works via Anthropic-bound channel) |\n| Trusted | Default: common package registries, GitHub, cloud SDKs, container registries, and 100+ domains |\n| Full | Any domain |\n| Custom | Your explicit allowlist, optionally including Trusted defaults |\n\nGitHub operations go through a separate dedicated proxy regardless of this setting, which handles authentication via scoped credentials. The actual GitHub token never enters the container. All other outbound traffic passes through a security proxy for abuse prevention and DNS-level audit logging.\n\n### Link cloud sessions back to their work\n\nEach cloud session exposes `CLAUDE_CODE_REMOTE_SESSION_ID` (prefixed `cse_`). To generate the transcript URL for inclusion in PR bodies or reports:\n\n```bash\necho \"https://claude.ai/code/${CLAUDE_CODE_REMOTE_SESSION_ID/#cse_/session_}\"\n```\n\nFrom v2.1.182, Claude automatically adds a `Claude-Session: <url>` git trailer to commits and a session URL line to PR bodies."
   },
   {
    "heading": "Remote/cloud execution and teleport: the one-directional flow",
    "body": "Session mobility between local and cloud follows a specific topology that you must internalize before designing team workflows around it.\n\n### The one-directional rule from the CLI\n\nFrom the CLI, session handoff is one-way:\n- **Terminal → Cloud**: `claude --remote \"task description\"` creates a new cloud session\n- **Cloud → Terminal**: `claude --teleport` or `claude --teleport <session-id>` pulls a cloud session to your terminal\n- **Terminal → Cloud (existing session)**: not directly possible from the CLI alone. The Desktop app's \"Continue in\" menu (accessible from the VS Code icon in the bottom right of the session toolbar) is the first-party way to send an existing local session to the web — it pushes your branch, generates a conversation summary, and creates a new cloud session with full context.\n\n### `--remote`: starting cloud sessions from the terminal\n\n```bash\n# Start a cloud session for the current repo\nclaude --remote \"Fix the authentication bug in src/auth/login.ts\"\n\n# Start multiple parallel cloud sessions\nclaude --remote \"Fix the flaky test in auth.spec.ts\"\nclaude --remote \"Update the API documentation\"\nclaude --remote \"Refactor the logger to use structured output\"\n```\n\nKey mechanics:\n- The VM clones your current directory's GitHub remote at your **current branch**. If you have local commits that are not pushed, the cloud session will not see them. **Push first.**\n- `--remote` is not `--remote-control`. The latter (`claude --remote-control` or alias `--rc`) exposes a local interactive session for web monitoring — it does not create a cloud session.\n- Without GitHub access, Claude Code bundles your local repository and uploads it directly. The bundle includes full git history across all branches plus uncommitted changes to tracked files. The bundled repository must be under 100 MB; larger repos fall back to bundling only the current branch, then to a single squashed snapshot. Untracked files are not included.\n\nMonitor running cloud sessions: `/tasks` in any CLI session, or `claude.ai/code` in a browser.\n\n### `--teleport`: pulling cloud sessions to the terminal\n\nTeleport checks prerequisites before proceeding:\n\n| Requirement | What fails if missing |\n|------------|----------------------|\n| Clean git state | Prompted to stash; cannot proceed with uncommitted changes |\n| Correct repository | Must run from a checkout of the same repo (not a fork) |\n| Branch pushed to remote | Teleport auto-fetches and checks out the cloud branch |\n| Same claude.ai account | Auth mismatch → error |\n| claude.ai OAuth (not API key/Bedrock/Vertex/Foundry) | `--teleport` unavailable — run `/login` to switch auth method |\n\n```bash\n# Interactive session picker\nclaude --teleport\n\n# Direct by session ID\nclaude --teleport cse_xxxxxxxx\n\n# From inside an existing session\n/teleport    # or /tp\n\n# From /tasks view\n/tasks       # then press 't' on a session\n```\n\n`--teleport` is distinct from `--resume`. `--resume` (`-r`) opens a conversation from local history on this machine only. `--teleport` pulls a cloud session and its branch from Anthropic's infrastructure. They are not interchangeable.\n\n### Rate limits are shared across surfaces\n\nCloud sessions draw from the same account rate limits as local sessions. Running five parallel cloud sessions with `--remote` consumes the same quota as five local sessions running simultaneously. This is a per-account (and per-plan) limit, not a per-surface limit. Teams on shared subscription seats should account for this when designing parallel agent workflows.\n\n### The plan-then-execute pattern\n\nFor complex tasks, a productive pattern is to plan locally and execute remotely:\n\n```bash\n# Step 1: explore and plan locally (no edits)\nclaude --permission-mode plan\n# Claude reads files and produces a plan document\n# You review, annotate inline with comments in the diff view, and refine\n\n# Step 2: commit the plan and push\ngit add docs/migration-plan.md && git commit -m \"Add migration plan\"\ngit push\n\n# Step 3: execute autonomously in the cloud\nclaude --remote \"Execute the migration plan in docs/migration-plan.md\"\n```\n\nFor drafting and reviewing the plan itself in a web session, the `ultraplan` feature lets Claude generate the plan on Claude Code on the web while you continue working locally, then you comment on sections in your browser."
   },
   {
    "heading": "IDE context exposure and the hidden MCP server",
    "body": "IDE integration surfaces (VS Code, JetBrains) inject context into every prompt that you may not realize is being sent. For teams with sensitive codebases or strict data handling requirements, this is not a cosmetic concern.\n\n### The built-in IDE MCP server\n\nWhen the VS Code extension is active, it runs a local MCP server bound to `127.0.0.1` on a random high port. The CLI connects to this server automatically when running inside VS Code. The server is named `ide` and is intentionally hidden from `/mcp` — there is nothing user-configurable about it.\n\n**Authentication**: each extension activation generates a fresh random auth token, written to a lock file under `~/.claude/ide/` with `0600` permissions in a `0700` directory. Only the user running VS Code can read it. The server is not reachable from other machines.\n\nThe server hosts several internal RPC tools (for opening diffs, reading selections, saving files), but only two are visible to the model:\n\n| Tool name (canonical, for hooks) | What it does | Writes? |\n|-----------|-------------|--------|\n| `mcp__ide__getDiagnostics` | Returns language-server diagnostics from VS Code's Problems panel (optionally scoped to one file) | No |\n| `mcp__ide__executeCode` | Runs Python code in the active Jupyter notebook's kernel — always requires explicit Quick Pick confirmation | Yes |\n\nIf your organization uses a `PreToolUse` hook to allowlist MCP tools, you need explicit entries for these even though they do not appear in `/mcp`:\n\n```json\n// .claude/settings.json\n{\n  \"hooks\": {\n    \"PreToolUse\": [{\n      \"matcher\": \"mcp__ide__getDiagnostics|mcp__ide__executeCode\",\n      \"hooks\": [{\n        \"type\": \"command\",\n        \"command\": \"echo allowed\"\n      }]\n    }]\n  }\n}\n```\n\nFor a deny-by-default MCP policy, `mcp__ide__getDiagnostics` and `mcp__ide__executeCode` must be explicitly allowed or explicitly denied.\n\n### Automatic selection and file context\n\nWhile the CLI is connected to VS Code, it automatically includes your current editor selection and the active file path as context on each prompt. The transcript shows `⧉ Selected N lines from <file>` when this occurs.\n\nThis is silent and automatic — developers may not realize it is happening. For sensitive files (`.env`, credential files, private keys), add a `Read` deny rule:\n\n```json\n// .claude/settings.json\n{\n  \"permissions\": {\n    \"deny\": [\n      \"Read(./.env)\",\n      \"Read(./.env.*)\",\n      \"Read(./secrets/**)\",\n      \"Read(./.aws/credentials)\"\n    ]\n  }\n}\n```\n\nPer the official docs: a matching `Read` deny rule prevents both the explicit read access AND the automatic IDE context injection for that file. The deny rule has to match precisely (including path format — note that `Read(.env)` and `Read(**/.env)` are equivalent due to gitignore-style matching, but `Read(./.env)` matches only in the current directory).\n\n### Jupyter execution confirmation flow\n\n`mcp__ide__executeCode` cannot run anything silently. Each invocation inserts code as a new cell at the notebook's end, VS Code scrolls it into view, and a native Quick Pick asks you to **Execute** or **Cancel**. Cancelling (or pressing `Esc`) returns an error to Claude. The tool also refuses when:\n- No Jupyter notebook is active\n- The `ms-toolsai.jupyter` extension is not installed\n- The kernel is not Python\n\nThis means a `PreToolUse` allowlist entry for `mcp__ide__executeCode` permits Claude to propose running a cell; the Quick Pick is the actual execution gate. These are two separate mechanisms — both must be satisfied.\n\n### JetBrains plugin specifics\n\nThe JetBrains plugin (IntelliJ IDEA, PyCharm, WebStorm, and others) shares the context-injection behavior — it auto-sends editor context and selection with each prompt. The plugin requires the Claude Code CLI installed separately; the plugin is a GUI wrapper, not a standalone binary.\n\nInstallation: JetBrains Marketplace, plugin ID `claude-code-beta`. After restarting the IDE, the plugin connects automatically to the running CLI."
   },
   {
    "heading": "The Desktop app: visual diffing, scheduled tasks, and session handoff",
    "body": "The Desktop app (macOS universal, Windows x64 and ARM64 — **not available on Linux**) is a standalone GUI for use outside the terminal or IDE. It reads the same `~/.claude/settings.json` and managed-settings (MDM plist on macOS, registry on Windows) as the CLI, so config is shared.\n\n### What Desktop adds over the CLI\n\n- Visual side-by-side diff review with per-line inline comment capability\n- Multiple sessions running in parallel with Git worktree isolation per session\n- Integrated terminal pane and file editor pane (drag-and-drop layout)\n- App preview pane with embedded browser for front-end dev server verification\n- PR monitoring with auto-fix and auto-merge toggles (requires `gh` CLI installed)\n- Side questions via `Cmd+;` that use session context without derailing the main thread\n- Scheduled tasks running on the local machine\n- Dispatch: receive task assignments on your phone, Desktop creates and manages the session\n- Continue in menu: the only first-party way to send an existing local session to the web (pushes branch, generates conversation summary, creates new cloud session with full context)\n\n### Auth behavior specific to Desktop\n\nThe Desktop app uses OAuth exclusively. It does not read `ANTHROPIC_API_KEY`, `apiKeyHelper`, or `ANTHROPIC_AUTH_TOKEN`. If your team routes through environment variables for API key access, Desktop will not pick that up — it will prompt for OAuth login. For teams standardizing on Bedrock or Vertex, the Desktop Code tab is not appropriate; use the CLI instead.\n\n**Dispatch is Pro/Max only**: Dispatch (the phone-to-Desktop session creation feature) requires a Pro or Max plan. It is NOT available on Team or Enterprise plans.\n\n### Desktop scheduled tasks vs. Routines\n\n| | Desktop scheduled tasks | Routines |\n|--|------------------------|----------|\n| Runs on | Your local machine | Anthropic-managed cloud infra |\n| Requires machine on | Yes | No |\n| Has local file access | Yes | No (clones from GitHub) |\n| Configured via | Desktop app UI | Web, Desktop, or `/schedule` CLI |\n| Auth requirement | claude.ai OAuth | claude.ai OAuth |\n\nUse Desktop scheduled tasks for workflows that need local file access (reading from local paths, writing to local dirs). Use Routines for anything that should run unattended when your machine is off, or for overnight automation against GitHub repos.\n\n### Config shared between Desktop and CLI\n\nThe Desktop app and CLI share `~/.claude/settings.json`. A `permissions.deny` rule added via the CLI persists for Desktop sessions. An MCP server added with `claude mcp add` at user scope appears in both. This is convenient but requires awareness: a project-local setting that accidentally got promoted to user scope will silently affect Desktop sessions on unrelated projects.\n\nSession worktrees (for parallel sessions) are stored in `<project-root>/.claude/worktrees/` by default. The worktree location can be changed in Desktop Settings → Claude Code. Each session gets an isolated Git worktree so changes in one session do not affect others until committed."
   },
   {
    "heading": "Deployment patterns: choosing the right surface for each team use case",
    "body": "With the mechanics established, here is the decision framework for surface selection across common team scenarios.\n\n### The surface selection decision table\n\n| Scenario | Recommended surface | Key reason |\n|----------|--------------------|-----------|\n| Interactive coding with visual diffs | VS Code extension or Desktop | Editor context, native diff UI |\n| Long-running autonomous task (can close laptop) | Web / `--remote` | Cloud VM persists after disconnect |\n| CI/CD automation | CLI with `-p` + `CLAUDE_CODE_OAUTH_TOKEN` | Scriptable, non-interactive |\n| Code review on every PR | GitHub Actions integration | Event-driven, no human in loop |\n| PR monitoring + auto-fix | Web cloud session + auto-fix toggle (requires GitHub App) | Requires GitHub App webhooks |\n| Multiple parallel tasks | `--remote` (multiple calls) or Desktop parallel sessions | Separate sessions per task, Git worktree isolation |\n| Mobile oversight of running task | Web or Claude iOS app | Remote monitoring without laptop |\n| Local file access on a schedule | Desktop scheduled tasks | Machine-local file system |\n| Unattended overnight automation | Routines | Runs without machine being on |\n| Team with strict data residency | Bedrock/Vertex | VPC routing, no Anthropic storage |\n| Phone-to-Desktop task dispatch | Dispatch (Pro/Max only) | Not available on Team/Enterprise |\n\n### Common configuration pitfalls\n\n**The API key collision**: developer has `ANTHROPIC_API_KEY` in `.bashrc` for personal projects, joins a Teams org, their CLI sessions charge the old Console org (which may be disabled) instead of the Teams subscription. Fix: `unset ANTHROPIC_API_KEY`, verify with `claude auth status --text`.\n\n**The user-scope MCP leak**: developer adds an MCP server at user scope (`claude mcp add`). It appears in every project's CLI sessions including those with project-level deny rules. The server name will not appear in `.mcp.json` but will load anyway from `~/.claude.json`. Fix: audit `~/.claude.json` for user-scoped MCP entries and move project-specific servers to `.mcp.json`.\n\n**Cloud session credential gap**: team writes a `SessionStart` hook that calls `aws sts get-caller-identity`. Works locally (developer has AWS SSO). Fails in cloud sessions (interactive SSO not supported — browser-based OAuth flows cannot run in a cloud VM). Fix: add static AWS credentials as environment variables in the cloud environment config, and gate the hook on `CLAUDE_CODE_REMOTE`.\n\n**Managed settings bypass via third-party provider**: a developer sets `CLAUDE_CODE_USE_BEDROCK=1` and the server-managed settings are silently bypassed — Bedrock deployments cannot receive server-managed settings. The security constraint the team thought was enforced is not enforced for that session. Fix: deploy endpoint-managed settings to devices via MDM; those apply regardless of provider. There is no `forceLoginMethod` in managed-only keys — prevent Bedrock use at the IAM/network policy level.\n\n**The `--remote` branch gap**: developer runs `claude --remote \"fix the bug\"` but has three local commits not yet pushed. The cloud VM clones from GitHub and sees none of those commits. The task runs on stale code. Fix: always push before `--remote`. Add a pre-task reminder or `git push` step to your workflow.\n\n**Desktop ignoring env-var credentials**: team standardizes on `apiKeyHelper` for credential rotation. It works in the terminal. Developers using the Desktop app see login prompts because Desktop uses OAuth only. Fix: ensure all Desktop users have claude.ai subscription OAuth set up; treat credential rotation as a Console workspace concern rather than a local script.\n\n**Cloud session missing `.gh` or `gh` CLI**: the `gh` CLI is NOT pre-installed in cloud sessions. If a `SessionStart` hook or task script calls `gh`, install it in the environment setup script (`apt update && apt install -y gh`) and provide `GH_TOKEN` as an environment variable. The cloud session's built-in GitHub tools cover most PR/issue operations without `gh`."
   },
   {
    "heading": "Worked example: a minimal team settings deployment",
    "body": "This brings everything together in a concrete deployment sequence for a team of 20 engineers, Teams plan, mix of local CLI and cloud sessions.\n\n### Step 1: Standardize auth\n\nAll engineers authenticate with their claude.ai accounts (Teams subscription). No `ANTHROPIC_API_KEY` in any shared profile or dotfile. CI uses `claude setup-token` per developer:\n\n```bash\n# Run once per developer, output stored in CI secret vault\nclaude setup-token\n# Copy the printed token to your CI secret manager as CLAUDE_CODE_OAUTH_TOKEN\n# Note: --bare sessions do not read CLAUDE_CODE_OAUTH_TOKEN;\n# use ANTHROPIC_API_KEY for those.\n```\n\n### Step 2: Configure server-managed policy\n\nIn the claude.ai admin console (Admin Settings > Claude Code > Managed settings), requires Teams or Enterprise plan and Claude Code v2.1.38+ (Teams) or v2.1.30+ (Enterprise):\n\n```json\n{\n  \"permissions\": {\n    \"deny\": [\n      \"Bash(curl *)\",\n      \"Bash(wget *)\",\n      \"Read(./.env)\",\n      \"Read(./.env.*)\",\n      \"Read(./secrets/**)\"\n    ],\n    \"disableBypassPermissionsMode\": \"disable\"\n  },\n  \"allowManagedPermissionRulesOnly\": true,\n  \"requiredMinimumVersion\": \"2.1.100\",\n  \"autoUpdatesChannel\": \"stable\"\n}\n```\n\nNote: `forceLoginMethod` and `forceLoginOrgUUID` are NOT managed-only keys — they work in any settings scope but are most useful here. If needed, add them:\n\n```json\n{\n  \"forceLoginMethod\": \"claudeai\",\n  \"forceLoginOrgUUID\": \"org_your_uuid_here\"\n}\n```\n\n### Step 3: Project settings (checked into every repo)\n\n```json\n// .claude/settings.json\n{\n  \"$schema\": \"https://json.schemastore.org/claude-code-settings.json\",\n  \"permissions\": {\n    \"allow\": [\n      \"Bash(npm run *)\",\n      \"Bash(pytest *)\",\n      \"Bash(make *)\"\n    ]\n  },\n  \"hooks\": {\n    \"SessionStart\": [{\n      \"matcher\": \"startup|resume\",\n      \"hooks\": [{\n        \"type\": \"command\",\n        \"command\": \"\\\"$CLAUDE_PROJECT_DIR\\\"/scripts/cloud-init.sh\"\n      }]\n    }]\n  }\n}\n```\n\n```bash\n#!/bin/bash\n# scripts/cloud-init.sh -- runs on cloud sessions only\n# Must be executable: chmod +x scripts/cloud-init.sh\nif [ \"$CLAUDE_CODE_REMOTE\" != \"true\" ]; then exit 0; fi\nnpm ci 2>&1 | tail -5  # keep hook output short\n```\n\n### Step 4: MCP configuration for cloud sessions\n\n```json\n// .mcp.json  (project root, checked in)\n{\n  \"mcpServers\": {\n    \"github\": {\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"@modelcontextprotocol/server-github\"],\n      \"env\": {\n        \"GITHUB_PERSONAL_ACCESS_TOKEN\": \"${GITHUB_TOKEN}\"\n      }\n    }\n  }\n}\n```\n\n`GITHUB_TOKEN` must be set as an environment variable in the cloud environment config (UI), not in `settings.json env` (which does not reach cloud sessions). Note: user-scoped MCP servers added with `claude mcp add` do NOT appear in cloud sessions — only `.mcp.json` in the repo does.\n\n### Step 5: Verify the deployment\n\n```bash\n# Each developer runs after setup\nclaude --debug-file /tmp/claude-debug.log &\nclaude doctor                      # check managed settings validity\nclaude /permissions                # inspect effective permission rules\nclaude auth status --text          # confirm auth method and org\n\n# Test that deny rules work\nclaude -p 'run: curl https://example.com'   # should be denied\n\n# Test cloud mobility\ngit push\nclaude --remote 'List the test files in this repo and report back'\nclaude --teleport                  # pull the session back to terminal\n```\n\nThis deployment gives you: org-enforced security baseline via server-managed settings, project-level allow-list in git, cloud session compatibility via `.mcp.json` and `SessionStart` hook, and verified mobility between local and cloud execution. The key invariant: everything a cloud session needs (MCP servers, hooks, CLAUDE.md, skills, agents) must be in the repository under `.claude/` or in the cloud environment configuration UI."
   }
  ],
  "preQuiz": [
   {
    "prompt": "Your team wants to use Remote Control so engineers can monitor Claude Code sessions from mobile. You set ANTHROPIC_API_KEY in each developer's shell profile. What actually happens?",
    "options": [
     "Remote Control works normally — API keys and OAuth are interchangeable for Remote Control.",
     "Remote Control is silently blocked because a set ANTHROPIC_API_KEY prevents the OAuth session that Remote Control requires.",
     "Remote Control requires an additional CLAUDE_CODE_OAUTH_TOKEN env var on top of ANTHROPIC_API_KEY.",
     "Remote Control only fails if the API key is a project key rather than a personal key."
    ],
    "correct": 1,
    "sectionIndices": [
     1
    ],
    "explanation": "A set ANTHROPIC_API_KEY silently blocks Remote Control. Remote Control requires claude.ai OAuth established via `/login`. Tokens from `claude setup-token` or CLAUDE_CODE_OAUTH_TOKEN are inference-only and also cannot establish Remote Control sessions."
   },
   {
    "prompt": "Your team standardizes on AWS Bedrock for all LLM calls. A developer notices that the channels feature, voice dictation, and `--teleport` never work. What is the root cause?",
    "options": [
     "Bedrock requires a separate `channelsEnabled: true` entry in managed settings that most teams forget to add.",
     "Third-party providers (Bedrock, Vertex, Foundry) and raw API keys disable channels, computer use, voice dictation, --teleport, and Remote Control.",
     "Channels work on Bedrock but require the AWS region to be us-east-1; the other features are unrelated.",
     "These features are disabled only when using Foundry, not Bedrock or Vertex."
    ],
    "correct": 1,
    "sectionIndices": [
     1
    ],
    "explanation": "Third-party providers (Bedrock, Vertex, Foundry) and raw API keys disable a wide set of features including channels, computer use, voice dictation, --teleport, /web-setup, /desktop migration, and Remote Control. Teams using these providers should not document those features as available."
   },
   {
    "prompt": "A team lead adds an MCP server config to the VS Code extension settings UI (not to ~/.claude/settings.json). A developer using the CLI on the same machine doesn't see the server. What went wrong?",
    "options": [
     "MCP servers added via the VS Code UI are stored in extension-native settings, which are not shared with the CLI; they must go in ~/.claude/settings.json to be shared.",
     "MCP servers added in VS Code are stored per-workspace; the developer must open the same workspace folder in CLI too.",
     "The VS Code extension bundles a private CLI that maintains its own MCP registry, separate from the system CLI's registry.",
     "The CLI requires a manual `claude mcp sync` command to pull from VS Code's extension settings."
    ],
    "correct": 0,
    "sectionIndices": [
     2
    ],
    "explanation": "~/.claude/settings.json is shared between CLI and the VS Code/JetBrains extensions. Extension-native settings (VS Code Extensions → Claude Code panel) are per-developer UI preferences only. MCP configs belong in ~/.claude/settings.json to be shared with the CLI."
   },
   {
    "prompt": "Your security team wants to prevent users from enabling Bypass permissions mode org-wide. They set `permissions.disableBypassPermissionsMode: 'disable'` in the admin console and push it remotely. Two weeks later, Desktop users report they can still enable Bypass. What did the team miss?",
    "options": [
     "The key name changed — it must be `disableBypassPermissions` without the `Mode` suffix in managed settings.",
     "Remotely pushed admin-console managed settings reach CLI and IDE only, not Desktop. For Desktop, the managed file must be deployed via MDM (Jamf/Kandji or Windows registry).",
     "Bypass mode is controlled by a separate Desktop-specific toggle in the admin console under Code in the desktop, not via managed settings keys.",
     "Desktop honors `disableBypassPermissionsMode` only when the user is enrolled in the Team plan, not Pro or Max."
    ],
    "correct": 1,
    "sectionIndices": [
     3
    ],
    "explanation": "Admin-console remotely pushed managed settings reach CLI and IDE only — NOT Desktop. To govern Desktop, you must deploy the managed settings file to disk via MDM (macOS: com.anthropic.claudefordesktop / Jamf / Kandji; Windows: registry SOFTWARE\\Policies\\Claude)."
   },
   {
    "prompt": "Your team distributes a plugin by setting it in `Install for this project` scope in the VS Code UI. A developer using the CLI on the same repo says the plugin doesn't appear. Why?",
    "options": [
     "Project-scope plugin installs from the VS Code UI are written to the repo's .vscode/ folder, which the CLI doesn't read.",
     "Project-scope plugin installs are shared between extension and CLI via the repo's .claude/settings.json, so the CLI should see them — the developer likely hasn't restarted the CLI after the change.",
     "The CLI requires a separate `claude plugin install` command; VS Code UI installs don't propagate.",
     "Plugin/marketplace config is shared extension↔CLI, so this should work; but a CLI restart is required to apply plugin changes after the install."
    ],
    "correct": 3,
    "sectionIndices": [
     2
    ],
    "explanation": "Plugin/marketplace config IS shared between the extension and CLI. However, a restart is required to apply plugin changes. The developer needs to restart their CLI session to pick up the newly installed plugin."
   },
   {
    "prompt": "You run `claude --remote 'Run the full test suite'` from a local repo that is not hosted on GitHub (it's on a private GitLab). What actually happens?",
    "options": [
     "The command fails immediately with an error — --remote requires a GitHub-hosted repo.",
     "Claude uploads the repo as a bundle (history + uncommitted tracked changes), creates a cloud session, and runs the task — but the session cannot push results back unless GitHub auth is configured.",
     "The command works identically to a GitHub repo because the web session just clones from the remote URL you have configured.",
     "Claude uploads only the uncommitted changes as a diff and applies them to a fresh Ubuntu VM without any history."
    ],
    "correct": 1,
    "sectionIndices": [
     5
    ],
    "explanation": "For non-GitHub repos, `claude --remote` uses the bundle path: it uploads history + uncommitted tracked changes (needs ≥1 commit, <100MB; untracked files excluded). Bundled sessions can't push back unless GitHub auth is separately configured."
   },
   {
    "prompt": "A cloud session's setup script installs several heavy dependencies. On the 8th session of the day, a developer notices the setup script runs again from scratch and takes 5 minutes. What is the most likely explanation?",
    "options": [
     "Cloud session caches are per-user; the 8th session exceeded the daily cache-use quota and triggered a rebuild.",
     "Setup scripts re-run on every resume by design — only SessionStart hooks are cached.",
     "The ~7-day cache expired, or the setup script itself changed, or the allowed-hosts list changed — any of these invalidates the cache and forces a rebuild.",
     "The cache only persists for 4 hours; after that all sessions rebuild from scratch regardless of changes."
    ],
    "correct": 2,
    "sectionIndices": [
     5
    ],
    "explanation": "Setup scripts re-run only on script change, allowed-hosts change, or ~7-day cache expiry. Any of these three conditions invalidates the cache. Resuming a session never re-runs the setup script — but a new session after cache expiry will."
   },
   {
    "prompt": "You want to restrict Desktop SSH connections to only hosts matching *.internal.company.com. You add sshHostAllowlist to managed settings. A developer then uses the CLI to SSH into an external server and it works fine. What is happening?",
    "options": [
     "sshHostAllowlist is honored by Desktop only — CLI, IDE extensions, and Bash-tool SSH calls all ignore it. It does not restrict egress.",
     "sshHostAllowlist blocks outbound SSH at the OS network layer, so CLI SSH also should be blocked — this suggests the managed settings file wasn't deployed correctly.",
     "sshHostAllowlist applies to both Desktop and CLI but only for sessions started via claude, not for raw ssh commands spawned as subprocesses.",
     "The allowlist only applies when the user is in a Remote Control session; direct CLI use bypasses it by design."
    ],
    "correct": 0,
    "sectionIndices": [
     3
    ],
    "explanation": "sshHostAllowlist is honored ONLY by Desktop. CLI, IDE extensions, and Bash-tool ssh commands all ignore it. It does not restrict network egress at the OS level — it only controls which hosts appear in Desktop's SSH connection dropdown."
   },
   {
    "prompt": "Your DevOps team enables Auto-fix PRs via the GitHub App for a repo that uses Atlantis for Terraform plan/apply. A day later, Atlantis unexpectedly runs a Terraform apply on production. What caused this?",
    "options": [
     "Auto-fix PRs bypass GitHub branch protection rules, allowing direct pushes that trigger Atlantis apply webhooks.",
     "Claude Code's Auto-fix PR replies post under your GitHub account, which can trigger issue_comment automation like Atlantis/Terraform/Actions running privileged operations.",
     "The GitHub App for Auto-fix PRs was granted org-admin permissions during setup, which elevated the session's capability to trigger Atlantis.",
     "Auto-fix PR comments use a special GitHub event type that Atlantis misinterprets as a manual apply trigger."
    ],
    "correct": 1,
    "sectionIndices": [
     5
    ],
    "explanation": "Auto-fix PR replies post under your GitHub account. This means they can trigger issue_comment automation (like Atlantis, Terraform, or GitHub Actions) that runs privileged operations. The lesson explicitly warns to audit existing automations before enabling Auto-fix PRs."
   },
   {
    "prompt": "A cloud session developer sets a DATABASE_URL environment variable in the environment config with the value `\"postgres://user:pass@host/db\"` (including the quotes). The app fails to connect. What is wrong?",
    "options": [
     "DATABASE_URL cannot contain special characters like @ or : in cloud environment variables — they must be percent-encoded.",
     "The environment variable value was stored with the literal quote characters because cloud env vars use .env format and values must NOT be quoted — quotes are stored literally.",
     "Cloud sessions require secrets to be injected via the setup script, not the environment variable UI.",
     "The variable name DATABASE_URL is reserved by the cloud VM's PostgreSQL installation and cannot be overridden."
    ],
    "correct": 1,
    "sectionIndices": [
     5
    ],
    "explanation": "Cloud environment variables use .env format and values must NOT be quoted — quotes are stored literally, not stripped. So `\"postgres://...\"` stores the quotes as part of the value, causing connection failures. The correct entry is DATABASE_URL=postgres://user:pass@host/db with no surrounding quotes."
   },
   {
    "prompt": "Your org is on ZDR (Zero Data Retention) with the Enterprise plan. A developer tries to open claude.ai/code to start a cloud session. What happens?",
    "options": [
     "ZDR orgs get cloud sessions but with reduced logging — session content is not retained after the session ends.",
     "ZDR orgs cannot use cloud sessions or /web-setup at all.",
     "ZDR orgs can use cloud sessions but only with the GitHub App auth method, not /web-setup.",
     "ZDR orgs must use --teleport from the CLI instead of the web UI to initiate cloud sessions."
    ],
    "correct": 1,
    "sectionIndices": [
     5
    ],
    "explanation": "ZDR (Zero Data Retention) orgs cannot use cloud sessions or /web-setup at all. This is a hard blocker — not a degraded mode."
   },
   {
    "prompt": "A developer uses `claude --teleport` to move their local session to cloud. The command fails. Their working tree has uncommitted changes to three files. What is the most likely cause?",
    "options": [
     "Teleport requires the branch to have an open pull request — it cannot teleport from a local-only branch.",
     "Teleport requires a clean git tree; uncommitted changes block the teleport.",
     "Teleport is disabled when using VS Code because the VS Code extension maintains its own session state.",
     "Teleport fails if the session has been running for more than 2 hours due to session token expiry."
    ],
    "correct": 1,
    "sectionIndices": [
     5
    ],
    "explanation": "Teleport requires a clean git tree (among other prerequisites: same non-fork repo, branch pushed to remote, claude.ai auth). Uncommitted changes block the teleport operation."
   },
   {
    "prompt": "Your team wants the Auto mode setting to be enforced only via managed settings and not allow a cloned repo's .claude/settings.json to inject autoMode classifier rules. Is this possible, and why?",
    "options": [
     "No — autoMode classifier rules are always read from .claude/settings.json in the current project, whether managed or not.",
     "Yes — autoMode classifier rules are deliberately not read from checked-in .claude/settings.json; they must come from managed settings so a cloned repo cannot inject trust rules.",
     "Yes, but only when disableAutoMode is also set to 'disable' in managed settings.",
     "No — classifier rules in managed settings are overridden by project-level .claude/settings.json because project settings have higher precedence."
    ],
    "correct": 1,
    "sectionIndices": [
     3
    ],
    "explanation": "autoMode classifier rules are deliberately NOT read from checked-in .claude/settings.json. This is a security boundary: a cloned repo cannot inject trust rules via project settings. Classifier rules must come from managed settings deployed via MDM or admin console."
   }
  ],
  "tasks": [
   {
    "id": "stage-9-task-auth-standardization",
    "afterSectionIdx": 1,
    "title": "Audit and standardize auth for Remote Control readiness",
    "instructions": "**Goal:** Verify your machine is using claude.ai OAuth (not an API key) and confirm Remote Control is reachable.\n\n1. Check whether ANTHROPIC_API_KEY is set in your current shell:\n```bash\necho ${ANTHROPIC_API_KEY:-'(not set)'}\n```\n\n2. If it is set, check whether it is defined in your shell profile and note the source:\n```bash\ngrep -r 'ANTHROPIC_API_KEY' ~/.bashrc ~/.zshrc ~/.profile ~/.bash_profile 2>/dev/null\n```\n\n3. Verify your current auth method:\n```bash\nclaude auth status\n```\nYou should see an OAuth token from claude.ai, NOT an API key auth.\n\n4. If you're using an API key, switch to OAuth:\n```bash\nclaude auth login\n```\nFollow the browser prompt. Unset the env var for this session:\n```bash\nunset ANTHROPIC_API_KEY\n```\nThen comment out or remove the export line from your shell profile.\n\n5. Confirm Remote Control is available after the switch by opening Claude Code and checking that the Remote Control option appears in the session menu (or run `/status` inside a Claude Code session and look for `Remote Control: available`).",
    "doneWhen": "`claude auth status` shows claude.ai OAuth and ANTHROPIC_API_KEY is not set in your shell environment."
   },
   {
    "id": "stage-9-task-managed-settings",
    "afterSectionIdx": 3,
    "title": "Add team-governed settings to ~/.claude/settings.json",
    "instructions": "**Goal:** Configure shared team settings in `~/.claude/settings.json` so they apply to both the CLI and VS Code/JetBrains extensions.\n\n1. Open (or create) your user-level settings file:\n```bash\nmkdir -p ~/.claude\n${EDITOR:-nano} ~/.claude/settings.json\n```\n\n2. Add the schema reference and a `channelsEnabled` key (if your team uses channels) and an initial permission default. Merge with any existing content — do not overwrite existing keys:\n```jsonc\n{\n  \"$schema\": \"https://json.schemastore.org/claude-code-settings.json\",\n  \"channelsEnabled\": true,\n  \"permissions\": {\n    \"defaultMode\": \"default\"\n  }\n}\n```\n\n3. Save the file, then verify it is valid JSON:\n```bash\npython3 -m json.tool ~/.claude/settings.json\n```\n\n4. Open a new Claude Code CLI session and run `/config` to confirm the permission mode reflects `default` (Ask permissions), and that no error appears about the settings file.\n\n5. If you have the VS Code extension installed, open the Claude Code panel — it should reflect the same permission mode without any extra configuration, confirming the shared settings file is being read.",
    "doneWhen": "`python3 -m json.tool ~/.claude/settings.json` exits 0 (valid JSON) and a new Claude Code session starts without settings-file errors."
   },
   {
    "id": "stage-9-task-cloud-session",
    "afterSectionIdx": 5,
    "title": "Run a task in a cloud session and teleport back",
    "instructions": "**Goal:** Launch a cloud session from a GitHub-hosted repo, observe what config travels, and practice teleporting to local.\n\n**Prerequisites:** A GitHub repo with at least one commit, claude.ai OAuth auth, Pro/Max/Team subscription.\n\n1. Ensure your branch is pushed and your working tree is clean:\n```bash\ngit status\ngit push\n```\n\n2. Launch a cloud session with a concrete, bounded task:\n```bash\nclaude --remote \"List the files in the repo root and show the content of any CLAUDE.md found. Then stop.\"\n```\n\n3. While it runs, open `claude.ai/code` in your browser to view the session in the web UI.\n\n4. Note which config traveled: look for CLAUDE.md being read (repo-level), and confirm that any `~/.claude/settings.json` additions you made in Task 2 are NOT visible in the cloud session (user-level config does not travel).\n\n5. When the task completes, practice teleporting: in a separate terminal with a clean working tree, run:\n```bash\nclaude --teleport\n```\nIf you have no active session to teleport, instead run `/tasks` inside a local Claude Code session and observe the session list.\n\n6. Check the session URL appears in git history if v2.1.179+ is installed:\n```bash\ngit log --format='%B' -1 | grep -i 'claude-session' || echo 'No Claude-Session trailer found (may need a commit from the cloud session)'\n```",
    "doneWhen": "A `claude --remote` task completes in a cloud session and you can view it at claude.ai/code, confirming the repo CLAUDE.md was read but ~/.claude/settings.json was not."
   }
  ],
  "visualizations": [
   {
    "id": "stage-9-v",
    "kind": "comparison-table",
    "title": "Surfaces & mobility",
    "textualSummary": "Key concepts of Surfaces & mobility: surfaces share one engine and config, auth method determines available features, config mobility across surfaces.",
    "columns": [
     "Concept",
     "In this stage"
    ],
    "rows": [
     {
      "label": "surfaces share one engine and config",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "auth method determines available features",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "config mobility across surfaces",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "managed (admin-governed) settings",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     }
    ]
   }
  ],
  "confusions": [
   {
    "misconception": "Skipping Surfaces & mobility.",
    "correction": "Each stage is load-bearing for the setup."
   },
   {
    "misconception": "These concepts are interchangeable.",
    "correction": "Each has a distinct role; see the definitions."
   }
  ],
  "quiz": [
   {
    "id": "stage-9-q1",
    "type": "multiple-choice",
    "prompt": "Your team standardizes on AWS Bedrock as the Claude provider. A developer asks why they can't use Remote Control from their VS Code extension. What is the correct explanation?",
    "options": [
     "Remote Control requires VS Code version 1.90+ and needs an extension update.",
     "Third-party providers like Bedrock disable Remote Control along with channels, computer use, voice dictation, and teleport.",
     "Remote Control is only available on macOS and Windows Desktop, not in VS Code.",
     "The developer needs to run `claude mcp add` first to register the remote control server."
    ],
    "correct": 1,
    "explanation": "The content states that 'Third-party providers (Bedrock / Vertex / Foundry) and raw API keys disable a wide set of features: channels, computer use, voice dictation, --teleport, /web-setup, /desktop migration, and Remote Control.' This is a provider-level restriction, not a version or OS issue. The macOS/Windows constraint applies to Desktop as a surface, not to Remote Control itself. Remote Control being blocked has nothing to do with MCP server registration."
   },
   {
    "id": "stage-9-q2",
    "type": "multiple-choice",
    "prompt": "A developer sets `ANTHROPIC_API_KEY` in their shell environment and then tries to start a Remote Control session. They report it simply does not work. What is the most direct cause?",
    "options": [
     "API keys are not supported for Remote Control — they must use claude.ai OAuth via `/login`.",
     "`ANTHROPIC_API_KEY` set in the environment silently blocks Remote Control; it must be unset.",
     "Remote Control requires a Team or Enterprise plan, not an individual API key.",
     "The key works but the session times out because API keys have lower rate limits than OAuth tokens."
    ],
    "correct": 1,
    "explanation": "The content explicitly states '`ANTHROPIC_API_KEY` set in the environment silently blocks Remote Control — it must be unset.' The word 'silently' is important — it doesn't throw an obvious error. The plan tier matters for some features but the immediate cause here is the env var conflict. The rate-limit distractor conflates a different failure mode."
   },
   {
    "id": "stage-9-q3",
    "type": "multiple-choice",
    "prompt": "You want all team developers to use the same MCP server configs, permission hooks, and allowed commands. Where should this configuration live?",
    "options": [
     "In the VS Code Extensions → Claude Code settings panel, since that is where extension behavior is governed.",
     "In a `.claude/settings.json` checked into each project repo, since that is where project-scoped config lives.",
     "In `~/.claude/settings.json`, because it is shared between the CLI and VS Code/JetBrains extensions.",
     "In managed settings pushed from the admin console, because only managed settings can enforce hooks."
    ],
    "correct": 2,
    "explanation": "The content says 'Govern team-wide behavior through `~/.claude/settings.json` (allowed commands, env vars, hooks, MCP servers) — it is shared between the CLI and the VS Code/JetBrains extensions.' The VS Code settings panel is reserved for per-developer UI preferences. Project `.claude/settings.json` is project-scoped and doesn't share state globally across all developers' machines. Managed settings are for admin-override governance, not the primary channel for team config distribution."
   },
   {
    "id": "stage-9-q4",
    "type": "multiple-choice",
    "prompt": "A security-conscious admin wants to block all developers from using Bypass permissions mode org-wide. They set `permissions.disableBypassPermissionsMode = \"disable\"` in managed settings. A developer later runs Claude with `--dangerously-skip-permissions` and finds explicit `ask` rules still fire. Is this the expected behavior?",
    "options": [
     "No — Bypass permissions should disable all permission checks including explicit `ask` rules.",
     "Yes — Bypass permissions is not a total bypass; explicit `ask` rules still fire even in Bypass mode.",
     "No — if the admin set `disableBypassPermissionsMode`, the CLI flag should be completely blocked at the OS level.",
     "Yes — but only because the developer is using the CLI flag rather than the settings key; the settings key would be a total bypass."
    ],
    "correct": 1,
    "explanation": "The content states 'Bypass only in sandboxed containers/VMs; admins should disable org-wide. It is NOT a total bypass (explicit `ask` rules still fire).' Bypass mode and the CLI `--dangerously-skip-permissions` flag are equivalent but neither suppresses explicit `ask` rules. The admin setting blocks users from entering Bypass mode at all, but the note about `ask` rules is a separate invariant about what Bypass does when it is active."
   },
   {
    "id": "stage-9-q5",
    "type": "multiple-choice",
    "prompt": "Your team uses Claude Code on the web (cloud sessions). A developer adds a custom MCP server using `claude mcp add` on their local machine and expects it to be available in cloud sessions. It isn't. Why?",
    "options": [
     "`claude mcp add` servers require restart of the cloud VM to take effect.",
     "Cloud sessions only support MCP servers declared in the admin console managed settings.",
     "Cloud sessions cannot access local config — `claude mcp add` servers do not travel to cloud; only `.mcp.json` in the project repo does.",
     "The developer must use `/web-setup` first to sync local MCP config to their Claude account."
    ],
    "correct": 2,
    "explanation": "The content lists what does and does not carry over to cloud sessions. Under 'Does NOT carry over' it explicitly lists '`claude mcp add` servers'. What does carry over includes '.mcp.json (project MCP)' — meaning MCP servers must be declared in the repo's `.mcp.json`, not added via the local CLI command."
   },
   {
    "id": "stage-9-q6",
    "type": "multiple-choice",
    "prompt": "An admin pushes managed settings from the admin console to enforce `sshHostAllowlist`. A developer using the CLI tries to SSH to a host not on the list and succeeds. Is this expected?",
    "options": [
     "No — managed settings always override all surfaces including CLI.",
     "Yes — `sshHostAllowlist` is honored only by Desktop; CLI, IDE, and Bash-tool `ssh` ignore it.",
     "No — this indicates the managed settings were not deployed correctly and need re-push.",
     "Yes — `sshHostAllowlist` only applies when using the Remote Control feature, not direct SSH."
    ],
    "correct": 1,
    "explanation": "The content explicitly states '`sshHostAllowlist` is honored **only by Desktop** — CLI, IDE, and Bash-tool `ssh` ignore it, and it does not restrict egress.' This is a named gotcha about the scope of this particular managed setting. Admins who want SSH restriction must understand it only governs Desktop's dropdown, not all Claude Code surfaces."
   },
   {
    "id": "stage-9-q7",
    "type": "multiple-choice",
    "prompt": "A Linux developer tries to install Claude Desktop to use as their primary daily driver. They find it is unavailable. What is the constraint?",
    "options": [
     "Desktop requires a Team or Enterprise subscription, not available on individual Pro/Max plans.",
     "Desktop is macOS and Windows only — it does not run on Linux.",
     "Desktop on Linux requires an additional kernel module that is not yet released.",
     "Desktop is available on Linux but only via the Snap store, not the main download page."
    ],
    "correct": 1,
    "explanation": "The surface map in the content states Desktop runs on 'macOS + Windows only (**not Linux**)'. This is a hard platform constraint, not a subscription or packaging issue."
   },
   {
    "id": "stage-9-q8",
    "type": "multiple-choice",
    "prompt": "A developer installs the VS Code extension and expects to call `claude` from their terminal directly. After install, `claude` is not found in PATH. Why?",
    "options": [
     "The VS Code extension bundles a CLI that is private to the panel and does NOT add `claude` to PATH.",
     "The developer needs to run the extension's 'Add to PATH' command from the VS Code command palette.",
     "The PATH entry requires a shell restart; the developer should open a new terminal.",
     "VS Code extensions can only modify PATH on macOS, not Windows or Linux."
    ],
    "correct": 0,
    "explanation": "The content states the VS Code extension's 'Bundled CLI is private to the panel — does NOT add `claude` to PATH.' Installing the extension does not install or expose the CLI globally. The developer must install the CLI separately if they want to call `claude` from a terminal."
   },
   {
    "id": "stage-9-q9",
    "type": "multi-select",
    "prompt": "Your team is setting up Claude Code on the web (cloud sessions). Select ALL statements that are true about setup scripts in cloud environments.",
    "options": [
     "A non-zero exit from the setup script causes session start to fail.",
     "Setup scripts run on every session resume to ensure a fresh environment.",
     "Setup scripts run as root on the cloud VM.",
     "PostgreSQL and Redis are pre-installed but not running by default; you must start them explicitly.",
     "Large downloads like Docker images should go in the setup script body to maximize the cache build."
    ],
    "correct": [
     0,
     2,
     3
    ],
    "explanation": "True: (A) 'Non-zero exit fails session start — append `|| true` to non-critical commands.' (C) 'setup scripts run as root.' (D) 'PostgreSQL 16, Redis 7 (both **not running by default** — `service postgresql start`).' False: (B) Setup scripts re-run only on script change, allowed-hosts change, or ~7-day cache expiry — resuming never re-runs them; SessionStart hooks run on every resume, not setup scripts. (E) The content says to 'move large downloads (Docker images, model weights) into a background `SessionStart` hook' — not the setup script body — because the setup script has a ~5-minute cache-build budget."
   },
   {
    "id": "stage-9-q10",
    "type": "multi-select",
    "prompt": "Which of the following are true about using `claude --remote` with a non-GitHub repository (bundle mode)?",
    "options": [
     "Untracked files are included in the bundle automatically.",
     "The repository must have at least one commit.",
     "Bundled sessions can push changes back to the origin remote without any extra configuration.",
     "The bundle must be under 100MB.",
     "The bundle includes uncommitted tracked changes along with history."
    ],
    "correct": [
     1,
     3,
     4
    ],
    "explanation": "True: (B) 'Needs ≥1 commit' (C is false — 'bundled sessions can't push back unless GitHub auth is configured'). (D) '<100MB'. (E) 'history + uncommitted tracked changes' are included. False: (A) 'untracked files excluded (`git add` first)' — untracked files are excluded. (C) bundled sessions cannot push back unless GitHub auth is separately configured."
   },
   {
    "id": "stage-9-q11",
    "type": "multiple-choice",
    "prompt": "An admin wants to deploy managed settings to Claude Desktop machines. They push the settings from the admin console. A week later, Desktop users still see their personal settings. What did the admin miss?",
    "options": [
     "Managed settings pushed from the admin console require users to manually run `claude sync` to pull them.",
     "Admin-console remotely pushed managed settings reach CLI and IDE only — NOT Desktop. Desktop requires MDM deployment of the managed file to disk.",
     "Desktop managed settings only apply to the Remote Control feature, not general permissions.",
     "Managed settings only apply to Enterprise plans; the team must upgrade first."
    ],
    "correct": 1,
    "explanation": "The content states 'admin-console *remotely pushed* managed settings reach **CLI and IDE only — NOT Desktop**. For Desktop, deploy the managed file to disk via **MDM** (macOS `com.anthropic.claudefordesktop`/Jamf/Kandji; Windows registry `SOFTWARE\\Policies\\Claude`).' This is a named distribution gotcha. Remote push and Desktop are separate distribution paths."
   },
   {
    "id": "stage-9-q12",
    "type": "multiple-choice",
    "prompt": "A developer uses `claude --teleport` to move a session from CLI to the cloud. They run it on a branch that has local commits not yet pushed to the remote. The command fails. What is the most likely cause?",
    "options": [
     "Teleport is one-way only (cloud→CLI), not CLI→cloud.",
     "Teleport requires the branch to be pushed to the remote — a branch with unpushed commits cannot be teleported.",
     "Teleport only works when invoked from Desktop, not the CLI.",
     "The developer's plan does not include cloud session access."
    ],
    "correct": 1,
    "explanation": "The content lists teleport requirements including 'branch pushed to remote'. If the branch has local commits not pushed, this precondition is violated. The content also clarifies directionality: 'You cannot push terminal→web from CLI (only Desktop's \"Continue in\")' — but that applies to the opposite direction. Teleport IS available from CLI. Plan tier is not the issue described."
   },
   {
    "id": "stage-9-q13",
    "type": "multiple-choice",
    "prompt": "You enable Auto-fix PRs for your GitHub repositories. A teammate warns that auto-fix replies could trigger Atlantis Terraform plan runs on your infrastructure. Is this a real concern, and why?",
    "options": [
     "No — Auto-fix PR replies are posted by the Anthropic bot account, which GitHub automation tools typically exclude.",
     "Yes — Auto-fix PR replies post under your GitHub account, which can trigger `issue_comment` automation like Atlantis running privileged ops.",
     "No — Auto-fix only comments on PRs, not issues, so `issue_comment` webhooks do not fire.",
     "Yes — but only if the repo's `.github/workflows` files explicitly allow automated comments to trigger plans."
    ],
    "correct": 1,
    "explanation": "The content states 'Replies post under your GitHub account → can trigger `issue_comment` automation (Atlantis/Terraform/Actions) running privileged ops — audit before enabling.' The replies do NOT come from a bot account — they come from the user's own account, which automation tools treat as a human-initiated comment. This is a genuine security concern explicitly called out."
   },
   {
    "id": "stage-9-q14",
    "type": "multiple-choice",
    "prompt": "A team distributes a plugin via the VS Code extension using `Install for this project`. A developer using the CLI on the same project does not see the plugin. Is this expected?",
    "options": [
     "Yes — `Install for this project` is IDE-only and never affects the CLI.",
     "No — the content says plugin and marketplace config is shared between the extension and CLI. The developer may need to restart.",
     "Yes — CLI does not support plugins; only extensions do.",
     "No — the developer must separately run `claude mcp add` to register the plugin in the CLI."
    ],
    "correct": 1,
    "explanation": "The content states 'Distribute team plugins via project scope (`Install for this project`) so they ship through the repo and also work in the CLI' and 'Plugin/marketplace config is shared extension↔CLI. Restart to apply plugin changes.' The developer likely just needs to restart. The CLI does support plugins when distributed via project scope."
   }
  ],
  "masteryCheckpoint": "You can explain the concepts of Surfaces & mobility."
 },
 {
  "id": "stage-10",
  "stage": 10,
  "title": "Monitoring, analytics & troubleshooting",
  "summary": "Monitoring, analytics & troubleshooting: Adoption & ROI analytics dashboards, GitHub contribution attribution, OpenTelemetry cost & usage export.",
  "prerequisites": [
   "stage-9"
  ],
  "objectives": [
   "Understand the concepts in Monitoring, analytics & troubleshooting."
  ],
  "definitions": [
   {
    "term": "Adoption & ROI analytics dashboards",
    "short": "Hosted dashboards that surface team adoption, accept rates, and PR/line contribution so a lead can measure rollout and ROI rather than per-user cost."
   },
   {
    "term": "GitHub contribution attribution",
    "short": "The rules by which Claude Code credits code to itself — a matching window around PR merges, exclusions for rewrites and generated files, and a deliberate undercount."
   },
   {
    "term": "OpenTelemetry cost & usage export",
    "short": "Fleet-wide telemetry pushed to your own backend that the dashboards can't give you: per-user token/cost breakdowns and a SIEM audit stream."
   },
   {
    "term": "Multi-team cost attribution",
    "short": "Tagging telemetry with custom resource attributes and injecting a user identity so spend can be sliced by team, department, or person on every datapoint."
   },
   {
    "term": "Audit logging & SIEM integration",
    "short": "Opt-in OTLP log events capturing tool calls, bash commands, permission changes, and optionally prompt/tool content, sent only to your endpoint for the SIEM to alert on."
   },
   {
    "term": "In-session cost & limit governance",
    "short": "Slash commands that attribute plan-limit and token consumption to skills, subagents, plugins, and MCP servers so users can self-diagnose what is driving their costs."
   }
  ],
  "sections": [
   {
    "heading": "What This Stage Covers",
    "body": "Once Claude Code is rolling across a team, the hard work is *visibility*: knowing who is actually using it, what it costs you, whether the investment is paying off, and — when something breaks — knowing enough to fix it in minutes rather than hours. This stage covers the full operational stack:\n\n- The two hosted analytics dashboards and what each gives you\n- The GitHub contribution attribution algorithm: mechanics, exclusions, and why the numbers are deliberately low\n- OpenTelemetry export: the full metric/event/trace schema, multi-team tagging, and SIEM-ready audit logs\n- Multi-team cost attribution using `OTEL_RESOURCE_ATTRIBUTES`\n- In-session cost governance with `/usage` and how it attributes spend to skills, subagents, and MCP servers\n- Fallback model chains and retry tuning for resilience through overload events\n- Auth precedence: why stale environment keys cause confusing 400/403 errors and org-disabled errors\n- Context window exhaustion and corrupted-transcript recovery"
   },
   {
    "heading": "Adoption & ROI Analytics Dashboards",
    "body": "Claude Code ships two separate hosted dashboards for measuring rollout. They serve different principals and surface different data:\n\n| Plan | Dashboard URL | Who can view | What it shows |\n|------|--------------|-------------|---------------|\n| Teams / Enterprise | `claude.ai/analytics/claude-code` | Admins and Owners | Usage metrics, contribution metrics (requires GitHub integration), leaderboard, CSV export |\n| API (Console) | `platform.claude.com/claude-code` | UsageView permission (Developer, Billing, Admin, Owner, Primary Owner) | Lines accepted, accept rate, daily active users/sessions, per-user spend and lines |\n\n### Teams/Enterprise metrics\n\nThe Teams dashboard exposes:\n- **PRs with CC**: merged pull requests containing at least one CC-assisted line\n- **Lines of code with CC**: total lines across all tagged PRs counted as \"effective lines\" — more than 3 characters after normalization, excluding empty lines and lines with only brackets or trivial punctuation\n- **PRs with Claude Code (%)**: share of all merged PRs that contain CC-assisted code\n- **Suggestion accept rate**: percentage of Edit, Write, and NotebookEdit tool invocations the user accepted\n- **Lines of code accepted**: CC-written lines accepted in-session; does not track post-acceptance deletions\n\nThe **Adoption** chart tracks daily active users and sessions. The **Pull requests** chart shows a daily breakdown of PRs with vs. without CC (toggle to Lines view). The **Leaderboard** ranks top-10 users by CC contribution volume; the **Export all users** button downloads complete data as CSV for all users, not just the top 10.\n\n### API Console metrics\n\nThe Console dashboard shows: accept rate, lines accepted, an Activity chart (daily users and sessions), a Spend chart (daily API cost), and a team insights table with per-user spend and lines for the current month. API-key users display by key identifier; OAuth users by email. Spend figures are **estimates for analytics purposes**; for actual billing, use the billing page. Per-user token counts are not in either dashboard — for that you need OpenTelemetry export (see next section).\n\n### Enabling GitHub contribution metrics\n\nContribution metrics require the Claude GitHub App installed on your GitHub org and are in public beta. They are **not available for orgs with Zero Data Retention enabled** — those orgs see usage metrics only.\n\n1. A GitHub admin installs the Claude GitHub App at `github.com/apps/claude`\n2. A Claude Owner navigates to `claude.ai/admin-settings/claude-code` and enables \"Claude Code analytics\"\n3. Enable the \"GitHub analytics\" toggle on the same page\n4. Complete GitHub OAuth and select which GitHub organizations to include\n\nData typically appears within 24 hours; updates are daily. If you see \"Data processing in progress\" after a few days, verify the GitHub App is installed on the correct GitHub organization (not a personal account)."
   },
   {
    "heading": "GitHub Contribution Attribution: Mechanics and Gotchas",
    "body": "The attribution algorithm is intentionally conservative. The docs say explicitly: *these metrics are deliberately conservative and represent an underestimate of Claude Code's actual impact*. Understanding the mechanics lets you interpret low numbers correctly rather than treating them as a tooling failure.\n\n### Attribution process\n\nWhen a PR is merged, the system:\n1. Extracts added lines from the PR diff\n2. Identifies Claude Code sessions that edited matching files within the attribution time window\n3. Matches PR lines against Claude Code output using multiple string-matching strategies\n4. Before comparison, normalizes lines: whitespace trimming, collapsing multiple spaces, standardizing quotes, converting to lowercase\n\n### Time window\n\nSessions from **21 days before to 2 days after** the PR merge date are considered. This broad backward window covers long-lived feature branches, but the 2-day forward window means a PR merged on Monday can still pick up Friday's session work.\n\n### Excluded content\n\nThese files are automatically excluded from the diff analysis:\n- Lock files: `package-lock.json`, `yarn.lock`, `Cargo.lock`, and similar\n- Generated code: protobuf outputs, build artifacts, minified files\n- Build directories: `dist/`, `build/`, `node_modules/`, `target/`\n- Test fixtures: snapshots, cassettes, mock data\n- Lines over 1,000 characters (likely minified or generated)\n\n### Attribution rules that reduce the count further\n\n- Code **substantially rewritten** by developers (more than 20% difference from CC output) is not attributed to Claude Code\n- Sessions outside the 21-day window are excluded regardless of file overlap\n- Branch source/destination is ignored; only file content matching matters\n\n### The GitHub label\n\nMerged PRs that contain CC-assisted lines are labeled `claude-code-assisted` in GitHub. You can query these programmatically:\n\n```bash\ngh pr list --search 'label:claude-code-assisted is:merged' --json number,title,mergedAt\n```\n\n### Practical interpretation\n\nIf your attribution percentage seems low, the most common causes are:\n- Large codebases with many regenerated or auto-formatted files (the exclusion list is broad)\n- PRs that landed more than 21 days after CC sessions that produced the underlying work\n- Developers heavily editing CC output before committing (the 20% rewrite threshold)\n- GitHub App installed on a personal account rather than the org\n\nDo not tune the algorithm — it is hosted. Frame the dashboard numbers as a *floor*, not the ceiling, of actual CC impact."
   },
   {
    "heading": "OpenTelemetry Cost & Usage Export",
    "body": "The dashboards give adoption-level visibility. For per-user token and cost breakdowns, SIEM audit streams, and custom alerting, you need to run your own OTel collector. Claude Code exports via standard OTLP.\n\n### Quickstart configuration\n\n```bash\nexport CLAUDE_CODE_ENABLE_TELEMETRY=1\nexport OTEL_METRICS_EXPORTER=otlp       # options: otlp, prometheus, console, none\nexport OTEL_LOGS_EXPORTER=otlp          # options: otlp, console, none\nexport OTEL_EXPORTER_OTLP_PROTOCOL=grpc\nexport OTEL_EXPORTER_OTLP_ENDPOINT=http://collector.example.com:4317\nexport OTEL_EXPORTER_OTLP_HEADERS=\"Authorization=Bearer your-token\"\n# Default intervals: metrics 60 000 ms, logs 5 000 ms. For debugging:\nexport OTEL_METRIC_EXPORT_INTERVAL=10000\nexport OTEL_LOGS_EXPORT_INTERVAL=5000\n```\n\nDeploy via managed settings so it is centrally controlled and users cannot override it. On Linux/WSL the managed settings file lives at `/etc/claude-code/managed-settings.json` (on macOS: `/Library/Application Support/ClaudeCode/managed-settings.json`):\n\n```json\n{\n  \"env\": {\n    \"CLAUDE_CODE_ENABLE_TELEMETRY\": \"1\",\n    \"OTEL_METRICS_EXPORTER\": \"otlp\",\n    \"OTEL_LOGS_EXPORTER\": \"otlp\",\n    \"OTEL_EXPORTER_OTLP_PROTOCOL\": \"grpc\",\n    \"OTEL_EXPORTER_OTLP_ENDPOINT\": \"http://collector.internal:4317\",\n    \"OTEL_EXPORTER_OTLP_HEADERS\": \"Authorization=Bearer your-token\"\n  }\n}\n```\n\n**Critical**: Claude Code does NOT pass `OTEL_*` variables to subprocesses (Bash tool, hooks, MCP servers, language servers). If you instrument something Claude Code *runs*, set those variables directly in the command.\n\n### Available metrics\n\n| Metric | Unit | Key attributes |\n|--------|------|----------------|\n| `claude_code.session.count` | count | `start_type` (fresh/resume/continue) |\n| `claude_code.cost.usage` | USD | `model`, `query_source`, `speed`, `effort`, `agent.name`, `skill.name`, `plugin.name`, `mcp_server.name`, `mcp_tool.name` |\n| `claude_code.token.usage` | tokens | `type` (input/output/cacheRead/cacheCreation), `model`, `query_source`, same attribution attributes |\n| `claude_code.lines_of_code.count` | count | `type` (added/removed), `model` |\n| `claude_code.pull_request.count` | count | standard attributes |\n| `claude_code.commit.count` | count | standard attributes |\n| `claude_code.code_edit_tool.decision` | count | `tool_name`, `decision`, `source`, `language` |\n| `claude_code.active_time.total` | s | `type` (user/cli) |\n\nThe `query_source` on `cost.usage` and `token.usage` distinguishes `main`, `subagent`, and `auxiliary` — you can see at a glance how much of a user's spend is agent-spawned work versus direct conversation.\n\n### Standard attributes on every metric and event\n\n| Attribute | Notes |\n|-----------|-------|\n| `session.id` | Unique per session; controlled by `OTEL_METRICS_INCLUDE_SESSION_ID` (default: true) |\n| `user.account_uuid` / `user.account_id` | Authenticated account; controlled by `OTEL_METRICS_INCLUDE_ACCOUNT_UUID` (default: true) |\n| `user.email` | Present when authenticated via OAuth |\n| `organization.id` | Org UUID (when authenticated) |\n| `user.id` | Anonymous stable ID from `~/.claude.json`; always present, no PII |\n| `terminal.type` | iTerm.app, vscode, cursor, tmux, etc. |\n| `app.version` | Claude Code version; controlled by `OTEL_METRICS_INCLUDE_VERSION` (default: false) |\n| `app.entrypoint` | How the session launched (cli, sdk-cli, sdk-ts, sdk-py, claude-vscode); controlled by `OTEL_METRICS_INCLUDE_ENTRYPOINT` (default: false) |\n\nEvents also include `prompt.id` (UUID v4, correlates all events from one user prompt) — intentionally excluded from metrics because each prompt generates a unique ID that would produce unbounded cardinality.\n\n### Key events for cost and audit\n\n- `claude_code.user_prompt` — every prompt submitted; `prompt` content redacted by default\n- `claude_code.api_request` — every API call; includes `cost_usd`, `input_tokens`, `output_tokens`, `cache_read_tokens`, `model`, `request_id`, skill/agent/MCP attribution\n- `claude_code.api_error` — failure tracking with `status_code`, `error`, `attempt` count\n- `claude_code.api_retries_exhausted` — emitted once when all retries are spent; includes `total_attempts` and `total_retry_duration_ms`\n- `claude_code.api_request_body` / `claude_code.api_response_body` — full Messages API JSON (requires `OTEL_LOG_RAW_API_BODIES`); inline bodies truncated at 60 KB, or untruncated on disk with `file:<dir>` mode\n- `claude_code.tool_decision` — accept/reject per tool call, with `source` (`config`, `hook`, `user_permanent`, `user_temporary`, `user_abort`, `user_reject`)\n- `claude_code.tool_result` — completed tool executions with `duration_ms`, `success`, `tool_use_id`\n- `claude_code.permission_mode_changed` — Shift+Tab mode cycling events with `from_mode` and `to_mode`\n- `claude_code.auth` — login/logout events\n- `claude_code.mcp_server_connection` — connect/fail/disconnect events\n\n### Distributed traces (beta)\n\nFor a full request timeline linking prompt → API calls → tool executions:\n\n```bash\nexport CLAUDE_CODE_ENHANCED_TELEMETRY_BETA=1\nexport OTEL_TRACES_EXPORTER=otlp\n```\n\nSpan hierarchy:\n\n```\nclaude_code.interaction\n├── claude_code.llm_request\n├── claude_code.hook           (requires ENABLE_BETA_TRACING_DETAILED=1)\n└── claude_code.tool\n    ├── claude_code.tool.blocked_on_user\n    ├── claude_code.tool.execution\n    └── (Agent tool) subagent llm_request / tool spans\n```\n\nBash and PowerShell subprocesses automatically inherit a `TRACEPARENT` environment variable (W3C trace context) of the active tool span, enabling end-to-end tracing through scripts Claude runs. The `tool_use_id` attribute on spans, tool_result events, and tool_decision events is a join key across all three.\n\n**Important**: `TRACEPARENT` propagation to model requests and HTTP MCP requests is only active when `ANTHROPIC_BASE_URL` is unset or points at `api.anthropic.com`. If you run through a custom proxy that rejects unrecognized headers, set `CLAUDE_CODE_PROPAGATE_TRACEPARENT=1` to force propagation.\n\n### Dynamic auth headers\n\nFor short-lived collector tokens, use `otelHeadersHelper` instead of a static header string:\n\n```json\n// .claude/settings.json (managed scope)\n{\n  \"otelHeadersHelper\": \"/usr/local/bin/generate_otel_headers.sh\"\n}\n```\n\n```bash\n#!/bin/bash\n# /usr/local/bin/generate_otel_headers.sh\n# Must output valid JSON with string key-value pairs\necho \"{\\\"Authorization\\\": \\\"Bearer $(aws secretsmanager get-secret-value \\\n  --secret-id otel-token --query SecretString --output text)\\\"}\"\n```\n\nThe script runs at startup and every **29 minutes** (1 740 000 ms, configurable via `CLAUDE_CODE_OTEL_HEADERS_HELPER_DEBOUNCE_MS`). It only works with `http/protobuf` and `http/json` protocols — the `grpc` exporter uses only the static `OTEL_EXPORTER_OTLP_HEADERS` value and silently ignores the helper.\n\nIf the helper fails or returns non-JSON, the error appears in `/doctor` output and `--debug` logs (and on stderr in non-interactive `-p` sessions)."
   },
   {
    "heading": "Multi-Team Cost Attribution",
    "body": "The OTel integration's multi-team attribution mechanism is `OTEL_RESOURCE_ATTRIBUTES`. Custom key-value pairs you set here are attached to every metric datapoint and event record, making them queryable labels in your metrics backend.\n\n```bash\nexport OTEL_RESOURCE_ATTRIBUTES=\"department=engineering,team.id=platform,cost_center=CC-1042\"\n```\n\nIn managed settings, inject this per team or per host:\n\n```json\n{\n  \"env\": {\n    \"CLAUDE_CODE_ENABLE_TELEMETRY\": \"1\",\n    \"OTEL_METRICS_EXPORTER\": \"otlp\",\n    \"OTEL_EXPORTER_OTLP_ENDPOINT\": \"http://collector.internal:4317\",\n    \"OTEL_RESOURCE_ATTRIBUTES\": \"department=engineering,team.id=platform,cost_center=CC-1042\"\n  }\n}\n```\n\nClaude Code attaches these values as attributes on every metric datapoint and event record, in addition to sending them in the OTLP resource block. Custom keys never override the built-in standard attributes: when a key collides, Claude Code keeps the built-in value.\n\nFor user-level tagging, combine resource attributes with the built-in `user.account_id` attribute (already on every datapoint when authenticated) to slice spend by both team *and* individual:\n\n```promql\n# Grafana: cost by team over 7 days\nsum by (team_id) (\n  increase(claude_code_cost_usage_total[7d])\n)\n```\n\n### Formatting constraints\n\nThe `OTEL_RESOURCE_ATTRIBUTES` format is strict:\n- Comma-separated `key=value` pairs, no spaces anywhere\n- Allowed characters: US-ASCII, no control chars, no whitespace, no double-quotes, no commas, no semicolons, no backslashes\n- Special characters must be percent-encoded\n\n```bash\n# WRONG — contains spaces in value\nexport OTEL_RESOURCE_ATTRIBUTES=\"org.name=My Company\"\n\n# CORRECT — use underscores, camelCase, or percent-encoding\nexport OTEL_RESOURCE_ATTRIBUTES=\"org.name=MyCompany\"\nexport OTEL_RESOURCE_ATTRIBUTES=\"org.name=John%27s%20Organization\"\n```\n\nWrapping a value in quotes does NOT escape spaces — `org.name=\"My Company\"` sets the literal value `\"My Company\"` (with quotes). Test your attributes with `OTEL_METRICS_EXPORTER=console` before pointing at your production collector so you can see exactly what is being emitted.\n\n### Cardinality management\n\nEach custom key becomes a label on every metric series. High-cardinality values (per-user IDs, request IDs) will explode storage in backends like Prometheus/Thanos. Mitigation strategies:\n\n- Use `cost_center`, `team.id`, `department` — coarse stable identifiers\n- To send custom attributes in the resource block only (omitting them from datapoint labels), set:\n  ```bash\n  export OTEL_METRICS_INCLUDE_RESOURCE_ATTRIBUTES=false\n  ```\n- The built-in `user.account_uuid` and `session.id` are already on datapoints; disable with:\n  ```bash\n  export OTEL_METRICS_INCLUDE_ACCOUNT_UUID=false\n  export OTEL_METRICS_INCLUDE_SESSION_ID=false\n  ```\n\nAdditionally, `app.version` (default: off) and `app.entrypoint` (default: off) can be enabled via `OTEL_METRICS_INCLUDE_VERSION` and `OTEL_METRICS_INCLUDE_ENTRYPOINT` when you need them for debugging — just be aware they add cardinality."
   },
   {
    "heading": "Audit Logging & SIEM Integration",
    "body": "The OTel logs/events channel is Claude Code's audit stream. Configure `OTEL_LOGS_EXPORTER=otlp` and point the collector at your SIEM (Splunk, Elastic, Datadog Logs, etc.). By default, content is redacted — you opt in to each level of detail.\n\n### Verbosity tiers\n\n| Variable | What it enables | Privacy implication |\n|----------|----------------|--------------------|\n| (default) | Tool names, session IDs, cost, accept/reject decisions | No content exposed |\n| `OTEL_LOG_TOOL_DETAILS=1` | Bash commands (`full_command`), file paths, skill names, MCP server/tool names, subagent types, error messages; also enables custom/plugin/MCP command names on `user_prompt` events | Reveals what code Claude ran |\n| `OTEL_LOG_USER_PROMPTS=1` | User prompt text on `user_prompt` events and span attributes | Reveals developer intent and queries |\n| `OTEL_LOG_TOOL_CONTENT=1` | Tool input and output bodies on trace spans (requires tracing beta); truncated at 60 KB | Full code content |\n| `OTEL_LOG_RAW_API_BODIES=1` | Full Anthropic Messages API request/response as `api_request_body` / `api_response_body` events; inline bodies truncated at 60 KB; implies all above | Entire conversation history |\n| `OTEL_LOG_RAW_API_BODIES=file:<dir>` | Same but untruncated, written to disk as `<uuid>.request.json` / `<request_id>.response.json`; event carries a `body_ref` pointer | Full bodies untruncated |\n\n### Event correlation: `prompt.id`\n\nEvery event produced by a single user prompt shares a `prompt.id` (UUID v4). This lets you trace all API calls, tool results, and decisions that flow from one user action — essential for audit reconstruction:\n\n```sql\n-- Find all activity from one user action (Splunk / SQL pseudocode)\nSELECT * FROM events\nWHERE prompt_id = '3f2a1b4c-...'\nORDER BY event_timestamp\n```\n\n`prompt.id` is intentionally excluded from metrics to avoid unbounded cardinality; use it only at the event level.\n\n### SIEM alert patterns\n\nWith `OTEL_LOG_TOOL_DETAILS=1`, the following event-based alerts are practical:\n\n**Bash commands running outside the repo:**\n```sql\n-- event.name = claude_code.tool_result, tool_name = Bash\n-- requires OTEL_LOG_TOOL_DETAILS=1 for tool_parameters.full_command\nevent.name=\"claude_code.tool_result\" AND tool_name=\"Bash\"\nAND NOT tool_parameters.full_command LIKE \"%/project/%\"\n```\n\n**Permission mode escalations to bypassPermissions:**\n```\nevent.name=\"claude_code.permission_mode_changed\" AND to_mode=\"bypassPermissions\"\n```\n\n**Tool rejections (user said No):**\n```\nevent.name=\"claude_code.tool_decision\" AND decision=\"reject\" AND source=\"user_reject\"\n```\n\n**MCP server connection failures:**\n```\nevent.name=\"claude_code.mcp_server_connection\" AND status=\"failed\"\n```\n\n### Configuring hook endpoint allowlists\n\nTo prevent hooks from exfiltrating data, lock their target URLs via managed settings:\n\n```json\n{\n  \"allowedHttpHookUrls\": [\"https://hooks.internal.example.com/*\"],\n  \"httpHookAllowedEnvVars\": [\"HOOK_SECRET\"],\n  \"allowManagedHooksOnly\": true\n}\n```"
   },
   {
    "heading": "In-Session Cost & Limit Governance",
    "body": "Individual engineers can interrogate their own consumption without needing admin dashboard access. This is the self-service layer for cost debugging.\n\n### `/usage` breakdown\n\nThe `/usage` command shows a session cost summary. For Pro/Max/Team/Enterprise subscribers it also shows a breakdown of what counts against plan limits:\n\n```\nTotal cost:            $0.55\nTotal duration (API):  6m 19.7s\nTotal duration (wall): 6h 33m 10.2s\nTotal code changes:    0 lines added, 0 lines removed\n```\n\nThe figures are computed from local session history on the current machine; usage from other devices or claude.ai is not included. If you hit a session or weekly limit, `/usage` shows the reset time alongside the limit message.\n\n### The cost attribution on `claude_code.cost.usage`\n\nThe same decomposition is available in OTel at the metric level on the `cost.usage` metric. The attribution attributes follow these redaction rules:\n\n| Attribute | What appears verbatim | What is redacted |\n|-----------|----------------------|------------------|\n| `agent.name` | Built-in and official-marketplace agent names | Other user-defined names → `\"custom\"` |\n| `skill.name` | Built-in, bundled, user-defined, official-marketplace | Third-party plugin skill names → `\"third-party\"` |\n| `plugin.name` | Official-marketplace plugin names | Third-party plugin names → `\"third-party\"` |\n| `mcp_server.name` | Built-in, claude.ai-proxied, official-registry servers | User-configured server names → `\"custom\"` |\n| `mcp_tool.name` | Same redaction as `mcp_server.name` | User-configured tool names → `\"custom\"` |\n| `query_source` | `main` / `subagent` / `auxiliary` | (no redaction) |\n\nThis means you can build a Grafana dashboard showing exactly how much of the fleet's spend came from a specific plugin or MCP server.\n\n### Workspace-level spend limits (API customers)\n\nSet on the Console at `platform.claude.com` under workspace limits. This caps total Claude Code workspace spend independently of individual per-user behavior. A \"Claude Code\" workspace is auto-created on first Console authentication — you cannot create API keys for it directly. See `platform.claude.com/settings/billing` to set workspace caps and enable auto-reload.\n\n### Rate limit sizing\n\nThe Anthropic Console shows your current TPM and RPM limits. Rate limits are **workspace-level** on the API; individual users within a workspace share the pool. When you hit a 429 it is your workspace limit. When you hit a 529 it is global API capacity — two entirely different root causes with different remedies (see the Retries section). Request a higher TPM tier from the Console if 429s are frequent during normal team usage."
   },
   {
    "heading": "Retries, Capacity & Fallback Resilience",
    "body": "During overload incidents, the right configuration keeps a team productive. Claude Code's retry and fallback behavior is tunable.\n\n### Automatic retry behavior\n\nClaude Code retries transient failures — 5xx, 529 overloaded, temporary 429 throttles, request timeouts, and dropped connections — **up to 10 times** with exponential backoff. The spinner shows `Retrying in Ns · attempt x/y`. When you *see* an error on-screen, those retries have already been exhausted.\n\nTunable variables:\n\n| Variable | Default | Purpose |\n|----------|---------|--------|\n| `CLAUDE_CODE_MAX_RETRIES` | 10 | Max retry attempts (capped at 15 as of v2.1.186). Lower it to surface failures faster in scripts. |\n| `CLAUDE_CODE_RETRY_WATCHDOG` | unset | Set to `1` in CI/automation to retry `429`/`529` capacity errors **indefinitely** instead of giving up after `CLAUDE_CODE_MAX_RETRIES`. |\n| `API_TIMEOUT_MS` | 600000 | Per-request timeout in milliseconds (10 minutes). Raise for slow networks or proxies. |\n\nFor unattended CI jobs, always set `CLAUDE_CODE_RETRY_WATCHDOG=1` — without it, capacity blips can fail jobs that would have succeeded if they had waited.\n\n### Fallback model chains\n\nWhen the primary model is overloaded, unavailable, or returns a non-retryable server error, `fallbackModel` specifies an ordered list of alternatives. Authentication, billing, rate-limit, request-size, and transport errors **never** trigger a switch; those follow normal retry and error handling.\n\n```json\n// .claude/settings.json\n{\n  \"fallbackModel\": [\"claude-sonnet-4-6\", \"claude-haiku-4-5\"]\n}\n```\n\nFor a single session, override with `--fallback-model` (comma-separated):\n\n```bash\nclaude --fallback-model sonnet,haiku\n```\n\n**Key behavior:**\n\n- The switch lasts for the current turn only; the next message tries the primary model first again\n- Chains are **capped at three models** after duplicate removal; extra entries are ignored\n- `fallbackModel` does **not** merge across settings files. The highest-precedence file that defines the key supplies the entire chain. Managed settings override user settings, so a chain in managed settings cannot be locally augmented.\n- An element not in `availableModels` is dropped from the chain when the chain is read, not when it is triggered\n- Claude Code also prompts users in-session: `Opus is experiencing high load, please use /model to switch to Sonnet`\n\n### When to use `/model` mid-session versus fallback chains\n\n| Scenario | Use |\n|----------|-----|\n| Anticipated long-term model unavailability | `fallbackModel` chain in managed settings |\n| User wants a cheaper model for a specific task | `/model` slash command mid-session |\n| CI job that must not fail on capacity events | `CLAUDE_CODE_RETRY_WATCHDOG=1` + `fallbackModel` chain |\n| Restricting which models users can select | `availableModels` + `enforceAvailableModels: true` |\n\n### 429 versus 529 — critical distinction\n\n- **429** = *your workspace's* rate limit on your API key or Console workspace. Fix: request a higher TPM tier in the Console or spread load across workspaces.\n- **529** = global API capacity across all users. Fix: wait, switch models with `/model`, or set `CLAUDE_CODE_RETRY_WATCHDOG=1` and let retries ride it out.\n\nThese require completely different interventions. Running `/status` tells you which credential (and therefore which workspace limit) is active."
   },
   {
    "heading": "Auth Precedence & Credential Failures",
    "body": "The single most common cause of confusing errors on team rollouts is credential precedence. Claude Code resolves credentials in this exact order (highest wins):\n\n### Authentication precedence (highest to lowest)\n\n1. Cloud provider credentials — when `CLAUDE_CODE_USE_BEDROCK`, `CLAUDE_CODE_USE_VERTEX`, or `CLAUDE_CODE_USE_FOUNDRY` is set\n2. `ANTHROPIC_AUTH_TOKEN` environment variable (bearer token; used for LLM gateway routing)\n3. `ANTHROPIC_API_KEY` environment variable (direct Anthropic API key)\n4. `apiKeyHelper` script output\n5. `CLAUDE_CODE_OAUTH_TOKEN` environment variable (long-lived token from `claude setup-token`)\n6. OAuth subscription credentials from `/login`\n\n**Important note on `ANTHROPIC_API_KEY`**: in interactive mode, Claude Code prompts you once to approve or decline the key; your choice is remembered. In non-interactive mode (`-p`), the key is *always* used when present — no prompt.\n\n### The \"org disabled\" failure pattern\n\nSymptom:\n```\nYour ANTHROPIC_API_KEY belongs to a disabled organization · Unset the environment variable to use your other credentials\nAPI Error: 400 ... This organization has been disabled.\n```\n\nCause: a developer has `ANTHROPIC_API_KEY` set in their shell profile — possibly from a previous employer or an old project. When Claude Code launches, it uses that key (precedence #3) instead of their active Teams/Enterprise subscription login (precedence #6). If the key belongs to a disabled Console organization, every API call fails.\n\nDiagnosis:\n```bash\nenv | grep ANTHROPIC\n# Also check shell profiles:\ngrep -r 'ANTHROPIC_API_KEY' ~/.zshrc ~/.bashrc ~/.profile 2>/dev/null\n# Inside a running session:\n/status\n```\n\nFix:\n```bash\nunset ANTHROPIC_API_KEY\n# Remove from shell profile permanently, then:\nclaude\n# Verify:\n/status\n```\n\n### The \"org disabled API key auth\" pattern\n\nIf your org admin has disabled API key authentication at the Console level, the error varies by which source supplied the key:\n\n```\nYour organization has disabled API key authentication · Run /login to sign in with your claude.ai account\nYour organization has disabled API key authentication · Unset ANTHROPIC_API_KEY to use your claude.ai account instead\nYour organization has disabled API key authentication · Unset the apiKeyHelper setting and run /login\n```\n\nJust running `/login` does not fix this if `ANTHROPIC_API_KEY` (or `apiKeyHelper`) is still set — the environment variable is evaluated before OAuth. You must remove the key source, *then* run `/login`.\n\n### `forceLoginMethod` and `forceLoginOrgUUID`\n\nFor enterprises that want to prevent API key auth entirely and enforce SSO:\n\n```json\n// Managed settings\n{\n  \"forceLoginMethod\": \"claudeai\",\n  \"forceLoginOrgUUID\": \"xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx\"\n}\n```\n\n`forceLoginMethod` accepts `\"claudeai\"` (restrict to Claude.ai accounts) or `\"console\"` (restrict to Claude Console accounts). It blocks `ANTHROPIC_API_KEY`, `ANTHROPIC_AUTH_TOKEN`, and `apiKeyHelper` auth at startup — but does **not** block Bedrock/Vertex/Foundry sessions.\n\n`forceLoginOrgUUID` can be a single UUID string (which also pre-selects that org during login) or an array of UUIDs. Login fails at startup if the authenticated account does not belong to a listed org. An empty array fails closed — all logins are blocked with a misconfiguration message. Sessions using `apiKeyHelper` or `ANTHROPIC_API_KEY` are blocked because org membership cannot be verified.\n\n### `apiKeyHelper` for dynamic credentials\n\nFor automation that cannot use `/login`:\n\n```json\n{\n  \"apiKeyHelper\": \"/usr/local/bin/generate_temp_api_key.sh\"\n}\n```\n\nThe script is executed in `/bin/sh` and its stdout is sent as both `X-Api-Key` and `Authorization: Bearer` headers. By default, the helper is called again after **5 minutes** or on any HTTP 401 response. Set `CLAUDE_CODE_API_KEY_HELPER_TTL_MS` for a custom refresh interval. If the helper takes longer than 10 seconds to return, Claude Code shows a warning in the prompt bar.\n\n`apiKeyHelper`, `ANTHROPIC_API_KEY`, and `ANTHROPIC_AUTH_TOKEN` apply only to terminal CLI sessions. Claude Desktop and cloud sessions use OAuth exclusively and ignore them.\n\n### `/status` is always the first diagnostic step\n\nAny time auth behavior is unexpected, `/status` tells you which credential source is active without any guessing."
   },
   {
    "heading": "Context-Window & Corrupted-Turn Recovery",
    "body": "Context window problems manifest in two distinct failure modes that require different recovery actions. Confusing them wastes tokens on the wrong fix.\n\n### Failure mode 1: Context exhaustion (the common case)\n\n**Symptoms**:\n- `Prompt is too long`\n- `Error during compaction: Conversation too long · Press esc twice to go up a few messages and try again`\n- Auto-compact fires repeatedly but context refills immediately\n- `Autocompact is thrashing: the context refilled to the limit...`\n\nThe thrashing error means compaction succeeded but a file read or tool output immediately refilled the window again — several times in a row. Claude Code stops retrying to avoid burning API calls in a loop.\n\n**Recovery options in order of preference**:\n\n1. Ask Claude to read oversized files in smaller chunks (specific line ranges or functions) rather than whole-file reads\n2. Run `/compact` with a focus directive: `/compact keep only the plan and the diff`\n3. Press Esc twice to open the message list, step back several turns to drop recent large outputs, then run `/compact` again\n4. Delegate large-file work to a subagent (separate context window)\n5. Run `/clear` if prior conversation is no longer needed (the session is preserved on disk and accessible via `claude --resume`)\n\nFor ongoing context hygiene, add compaction instructions to `CLAUDE.md`:\n\n```markdown\n# Compact instructions\nWhen using compact, focus on test output and code changes.\n```\n\nMonitor context usage via the `/context` command, which shows a breakdown of system prompt, tools, memory files, and messages. Auto-compact is on by default; re-enable it if `DISABLE_AUTO_COMPACT` was set.\n\n**The 1M context edge case**: If a user accidentally selected the `opus[1m]` or `sonnet[1m]` model variant and their plan requires usage credits for 1M context, they will see:\n```\nAPI Error: Usage credits required for 1M context · run /usage-credits to turn them on, or /model to switch to standard context\n```\nRun `/model` and select the variant without the `[1m]` suffix, or set `CLAUDE_CODE_DISABLE_1M_CONTEXT=1` to remove 1M variants from the picker entirely.\n\n### Failure mode 2: Corrupted transcript\n\n**Symptoms**:\n- `API Error: 400 due to tool use concurrency issues. Run /rewind to recover the conversation.`\n- `API Error: 400 ... unexpected tool_use_id found in tool_result blocks`\n- `API Error: 400 ... thinking blocks ... cannot be modified`\n\nAll three mean the same thing: the sequence of `tool_use`, `tool_result`, and `thinking` blocks in conversation history no longer matches what the API expects — typically because a tool call was interrupted mid-stream or a turn was edited.\n\n**Note**: `thinking.type.enabled is not supported for this model` is a *different* error — it means your Claude Code version is too old for Opus 4.7 or Opus 4.8. Fix with `claude update`, not `/rewind`.\n\n**Recovery**:\n- Run `/rewind`, or press Esc twice, to step back to a checkpoint before the corrupted turn. This is the primary recovery path.\n- If on Claude Code before v2.1.156, upgrade first — older versions can trigger this error during normal Opus 4.7/4.8 tool use and `/rewind` will not clear it.\n- If `/rewind` fails or the corruption is deep, `/clear` abandons the session entirely (the prior session is preserved and accessible via `claude --resume`)\n- Do not attempt to continue working in the corrupted session; every new message inherits the bad state\n\n### Decision table\n\n| Symptom | Root cause | Recovery |\n|---------|-----------|----------|\n| `Prompt is too long` | Single message or total context exceeds window | `/compact`, `/context`, shorten message |\n| `Autocompact is thrashing` | Tool output/file exceeds context after compaction | Chunked reads, subagent delegation, `/compact` with focus |\n| `400 tool use concurrency issues` | Corrupted `tool_use`/`tool_result` blocks | `/rewind` or `/clear` |\n| `thinking.type.enabled not supported` | Claude Code version too old for Opus 4.7/4.8 | `claude update` |\n| `Usage credits required for 1M context` | 1M model selected but plan requires credits | `/model` (switch to standard) or `/usage-credits` |\n| Quality degraded, wrong answers | Context too old / compacted too aggressively | `/clear`, re-provide key context |\n| Model gives answers from prior task | Stale context from previous task | `/clear`, use `/rename` + `/resume` to manage sessions |\n\n### Auto-compaction behavior\n\nClaude Code auto-compacts by default when approaching context limits. The compaction is triggered in the background; the result is a summarized conversation that preserves code state. Configure what to preserve globally in `CLAUDE.md` or per-session with `/compact <focus>`. Run `/context` at any point to see an interactive breakdown of what is currently filling the window."
   },
   {
    "heading": "Common Pitfalls & Operational Gotchas",
    "body": "Lessons learned from operating Claude Code at team scale that do not fit neatly into a single section:\n\n### OTel variables are not inherited by subprocesses\n\nIf you are running an OTel-instrumented application via the Bash tool and expecting it to report to the same collector, it will not. Claude Code deliberately does not pass `OTEL_*` to its subprocesses (Bash tool, hooks, MCP servers, language servers). Set these in the Bash command itself:\n\n```bash\nOTEL_EXPORTER_OTLP_ENDPOINT=http://collector:4317 ./my-instrumented-app\n```\n\n### Metrics temporality defaults to `delta`\n\nMost metrics backends (Prometheus, Thanos, VictoriaMetrics) expect cumulative temporality. If your queries look wrong or counters seem to reset:\n\n```bash\nexport OTEL_EXPORTER_OTLP_METRICS_TEMPORALITY_PREFERENCE=cumulative\n```\n\n### `OTEL_RESOURCE_ATTRIBUTES` silently rejects spaces\n\nSpaces in values cause the attribute to be malformed or silently dropped. Test with `OTEL_METRICS_EXPORTER=console` first to see exactly what attributes are being emitted before pointing at your production collector.\n\n### The `otelHeadersHelper` only works with HTTP protocols\n\nIf you switch from `http/protobuf` to `grpc` for lower latency, the dynamic header helper is silently ignored. The `grpc` exporter reads only the static `OTEL_EXPORTER_OTLP_HEADERS` value. This is documented behavior, not a bug.\n\n### `fallbackModel` does not merge across settings files\n\nUnlike permission arrays (which concatenate), the fallback model chain is taken wholesale from the highest-precedence file that defines it. If managed settings define a chain, user settings cannot extend it — the managed chain is used entirely. Document which file owns the chain to avoid confusion.\n\n### `apiKeyHelper` has a default 5-minute TTL\n\nUnlike the `otelHeadersHelper` which requires explicit `CLAUDE_CODE_OTEL_HEADERS_HELPER_DEBOUNCE_MS` to refresh, `apiKeyHelper` automatically refreshes every **5 minutes** and on any HTTP 401 response, even without setting `CLAUDE_CODE_API_KEY_HELPER_TTL_MS`. This means credential rotation works out of the box without any TTL configuration.\n\n### Auth precedence surprises on non-interactive sessions\n\nIn `claude -p` (non-interactive) mode, `ANTHROPIC_API_KEY` is **always** used when present — there is no approval prompt. If a CI environment has a stale API key in its environment, it will always use that key, even if the org uses managed settings with `forceLoginMethod`. Audit your CI environment variables as part of any Teams rollout.\n\n### Contribution metrics need time and the right GitHub org\n\nData takes up to 24 hours to appear after enabling GitHub analytics. If contribution metrics show zeros after 24 hours, verify the GitHub App is installed on the correct GitHub *organization*, not a personal account. The dashboard message \"GitHub app required\" appears if the app is missing; \"Data processing in progress\" appears if it is present but still indexing.\n\n### Zero Data Retention and contribution metrics are mutually exclusive\n\nOrganizations with Zero Data Retention enabled will never see contribution metrics — the dashboard shows usage metrics only. This is a product constraint, not a bug.\n\n### `/doctor` is your first stop for unexplained failures\n\nBefore escalating any configuration problem, run `/doctor` inside a session (or `claude doctor` from the shell if the session will not start). It checks installation health, settings validity, MCP server connectivity, context usage, and the OTel headers helper status in one pass and prints a structured report."
   }
  ],
  "preQuiz": [
   {
    "prompt": "Your organization has 45 developers using Claude Code. You open the Console analytics leaderboard and see only 10 names. What is the correct way to get a complete per-user breakdown?",
    "options": [
     "Switch to the Owner view at claude.ai/analytics/claude-code — the leaderboard there shows all users",
     "Use the Export all users button to download a CSV containing all users, not just the top 10",
     "Enable the GitHub analytics toggle, which unlocks full user listings",
     "Grant all users the UsageView permission so each can see their own data, then aggregate manually"
    ],
    "correct": 1,
    "sectionIndices": [
     0
    ],
    "explanation": "The leaderboard intentionally shows only the top 10 users. The 'Export all users' button provides a complete per-user CSV for the full team."
   },
   {
    "prompt": "A developer on your team merged a PR that included 900 lines of code she wrote. Claude Code suggested a 400-line refactor during the session, but she rewrote most of it herself. The GitHub analytics dashboard shows zero Claude-assisted lines for that PR. What is the most likely correct explanation?",
    "options": [
     "The GitHub app was not installed before the PR was merged, so no data was captured for that session",
     "The developer's code was rewritten more than 20% from the Claude suggestion, so those lines are not credited to Claude Code",
     "The PR fell outside the 21-day attribution window because the session started too early",
     "Lock files and generated code were filtered out, which excluded the majority of the counted lines"
    ],
    "correct": 1,
    "sectionIndices": [
     0
    ],
    "explanation": "Code rewritten more than 20% by the developer is not credited to Claude Code. This is a deliberate design choice to count only high-confidence attributions, which means reported numbers are intentional underestimates."
   },
   {
    "prompt": "You set up Claude Code analytics for your GitHub organization. It has been 6 hours and the dashboard shows no contribution data at all. What is the most likely root cause?",
    "options": [
     "Contribution data takes up to 24 hours (sometimes a few days) to appear — this is expected latency",
     "The GitHub analytics toggle was enabled before the Claude GitHub app was installed, so the integration is broken",
     "The analytics feature is only available on Enterprise plans, not Teams",
     "You need to re-authenticate GitHub every 6 hours for data to sync"
    ],
    "correct": 0,
    "sectionIndices": [
     0
    ],
    "explanation": "Contribution data typically appears within 24 hours and sometimes takes a few days to process. An empty dashboard after only 6 hours is within normal expected latency, not a configuration error."
   },
   {
    "prompt": "You want per-user token costs broken down by model for your 30-person team. You check the Team analytics dashboard at claude.ai/analytics/claude-code but find no per-user token cost data. What is the correct approach?",
    "options": [
     "Grant developers the UsageView permission so per-user token data appears in the Console insights view",
     "Configure OpenTelemetry export with CLAUDE_CODE_ENABLE_TELEMETRY=1 — the dashboard does not surface per-user token counts or cost estimates",
     "Enable the GitHub analytics toggle, which unlocks the token cost breakdown tab",
     "Use the /cost command in each developer's session and aggregate the outputs centrally"
    ],
    "correct": 1,
    "sectionIndices": [
     1
    ],
    "explanation": "The analytics dashboard does not surface per-user token counts or cost estimates. OpenTelemetry export is the correct mechanism for per-user cost and token breakdowns, using claude_code.cost.usage and claude_code.token.usage metrics."
   },
   {
    "prompt": "Your team runs Claude Code on AWS Bedrock. You configure OTEL_RESOURCE_ATTRIBUTES=user.email=jdoe@example.com in the managed settings file. After deploying, the OTel metrics show no user.email attribute. What explains this?",
    "options": [
     "user.email is an OAuth-only standard attribute — on Bedrock there is no Claude account identity, so only user.id and session.id populate",
     "Resource attributes set via OTEL_RESOURCE_ATTRIBUTES are ignored on Bedrock; use OTEL_EXPORTER_OTLP_HEADERS instead",
     "The attribute name must be enduser.email, not user.email, to match the OTel semantic convention",
     "OTEL_METRICS_INCLUDE_RESOURCE_ATTRIBUTES defaults to false on Bedrock deployments"
    ],
    "correct": 0,
    "sectionIndices": [
     1
    ],
    "explanation": "On Bedrock/Vertex/Foundry/direct-API there is no Claude account identity — only user.id (anonymous) and session.id populate. user.email is only available for OAuth users. The workaround is to inject enduser.id via OTEL_RESOURCE_ATTRIBUTES manually."
   },
   {
    "prompt": "You configure OTEL_METRIC_EXPORT_INTERVAL=1000 to debug a metrics pipeline issue. The problem is resolved. What must you do before returning to production?",
    "options": [
     "Set OTEL_METRIC_EXPORT_INTERVAL=60000 to restore the 60-second default, because leaving 1s intervals floods the exporter",
     "Delete the variable entirely — any explicit value overrides the safe default and must be removed",
     "Set it to 30000 (30s) as a compromise between freshness and exporter load",
     "No action needed — the 1s interval is rate-limited server-side and won't cause issues"
    ],
    "correct": 0,
    "sectionIndices": [
     1
    ],
    "explanation": "The documentation explicitly warns to only lower export intervals for debugging, then reset them. The production default for metrics is 60000ms (60 seconds). Leaving 1s debug intervals in production floods the exporter."
   },
   {
    "prompt": "You need to audit which Bash commands Claude Code runs for each user. You enable OTEL_LOGS_EXPORTER=otlp and point it at your SIEM. Bash commands are not appearing in the logs. What is missing?",
    "options": [
     "You also need OTEL_LOG_TOOL_DETAILS=1 to capture Bash commands and call arguments on tool_result events",
     "Bash command logging requires OTEL_LOG_RAW_API_BODIES=1 because commands are embedded in the Messages API payload",
     "The grpc exporter protocol is required for tool logging; http/protobuf does not support it",
     "You need CLAUDE_CODE_ENHANCED_TELEMETRY_BETA=1 because Bash command logging is a beta-only feature"
    ],
    "correct": 0,
    "sectionIndices": [
     1
    ],
    "explanation": "OTEL_LOG_TOOL_DETAILS=1 is required to capture MCP server/tool names, Bash commands, and call arguments on tool_result, tool_decision, and mcp_server_connection events. Enabling the logs exporter alone only captures event structure, not tool parameters."
   },
   {
    "prompt": "You need hook execution spans in your distributed traces to debug a pre-tool hook that is occasionally timing out. You set CLAUDE_CODE_ENABLE_TELEMETRY=1 and CLAUDE_CODE_ENHANCED_TELEMETRY_BETA=1 with OTEL_TRACES_EXPORTER configured. Hook spans do not appear. What additional configuration is required?",
    "options": [
     "Set ENABLE_BETA_TRACING_DETAILED=1 and BETA_TRACING_ENDPOINT, and ensure your org is allowlisted for interactive CLI tracing",
     "Set OTEL_LOG_TOOL_DETAILS=1 — hook spans are emitted as log events, not trace spans",
     "Set CLAUDE_CODE_PROPAGATE_TRACEPARENT=1 to enable context propagation into hook subprocesses",
     "Hook spans only appear when using the Agent SDK or headless -p mode, not interactive CLI"
    ],
    "correct": 0,
    "sectionIndices": [
     1
    ],
    "explanation": "Detailed hook spans require ENABLE_BETA_TRACING_DETAILED=1 plus BETA_TRACING_ENDPOINT, and org allowlisting for interactive CLI. Setting ENHANCED_TELEMETRY_BETA alone without these additional flags will not emit hook spans."
   },
   {
    "prompt": "A CI pipeline uses Claude Code for automated code review. Long-running jobs occasionally fail because the API returns 529 Overloaded errors and the process exits after 10 retries. You want the job to wait out capacity constraints rather than fail. What is the correct setting?",
    "options": [
     "Set CLAUDE_CODE_MAX_RETRIES=50 to give the job enough retry budget to outlast capacity constraints",
     "Set CLAUDE_CODE_RETRY_WATCHDOG=1 to make unattended sessions retry 429/529 errors indefinitely instead of failing",
     "Set CLAUDE_CODE_MAX_RETRIES=15 — this is the maximum allowed value and gives the most retries before failure",
     "Set API_TIMEOUT_MS=3600000 to extend the per-request timeout, which prevents 529 errors on slow API responses"
    ],
    "correct": 1,
    "sectionIndices": [
     2
    ],
    "explanation": "CLAUDE_CODE_RETRY_WATCHDOG=1 makes unattended/CI sessions retry 429/529 capacity errors indefinitely. The documentation explicitly recommends this over raising MAX_RETRIES for long runs. MAX_RETRIES is capped at 15 regardless of the value set."
   },
   {
    "prompt": "You set CLAUDE_CODE_MAX_RETRIES=25 in your team's managed settings. How many retries will actually occur before Claude Code gives up on a failing request?",
    "options": [
     "25 — the configured value is used exactly as set",
     "15 — values above 15 are clamped to the maximum",
     "10 — the default is always used when an invalid value is set",
     "Unlimited — MAX_RETRIES only applies to non-capacity errors; 529s always retry indefinitely"
    ],
    "correct": 1,
    "sectionIndices": [
     2
    ],
    "explanation": "CLAUDE_CODE_MAX_RETRIES is capped at 15. Values above 15 are clamped to 15. To get indefinite retries for capacity errors in CI, use CLAUDE_CODE_RETRY_WATCHDOG=1 instead."
   },
   {
    "prompt": "You query your OTel backend and see a spike in claude_code.api_error events. A developer reports that Claude Code showed 'Retrying in 8s · attempt 3/10' before eventually succeeding. How many api_error events should you expect to see in your backend for that session?",
    "options": [
     "3 — one event per retry attempt so you can track the retry progression",
     "1 — api_error fires only after retries are exhausted; intermediate retries are not separate events",
     "0 — the session succeeded, so no api_error is emitted even if retries occurred",
     "10 — all retry slots are counted as errors even if the request ultimately succeeds"
    ],
    "correct": 1,
    "sectionIndices": [
     1
    ],
    "explanation": "claude_code.api_error fires only after retries are exhausted (terminal). Intermediate retries are not separate events — you track retry count via the attempt attribute on the final error event. A session that eventually succeeds emits no api_error."
   },
   {
    "prompt": "You want to slice your OTel cost metrics by engineering department and cost center without inflating per-datapoint cardinality. Which configuration achieves this?",
    "options": [
     "Set OTEL_RESOURCE_ATTRIBUTES=department=engineering,cost_center=eng-123 and OTEL_METRICS_INCLUDE_RESOURCE_ATTRIBUTES=false so attributes appear only in the OTLP resource block, not as per-datapoint labels",
     "Set OTEL_RESOURCE_ATTRIBUTES=department=engineering,cost_center=eng-123 and leave OTEL_METRICS_INCLUDE_RESOURCE_ATTRIBUTES at its default (true) so they appear as queryable labels on every datapoint",
     "Add department and cost_center as standard OTel semantic attributes via OTEL_EXPORTER_OTLP_HEADERS",
     "Use OTEL_METRICS_INCLUDE_SESSION_ID=false and OTEL_METRICS_INCLUDE_ACCOUNT_UUID=false to free up cardinality budget for department labels"
    ],
    "correct": 0,
    "sectionIndices": [
     1
    ],
    "explanation": "Setting OTEL_METRICS_INCLUDE_RESOURCE_ATTRIBUTES=false moves custom attributes to the OTLP resource block only (not per-datapoint labels), which reduces cardinality while still allowing slicing at the resource level. The default (true) puts them as per-datapoint labels, which enables finer slicing but increases cardinality."
   },
   {
    "prompt": "You use a custom ANTHROPIC_BASE_URL proxy for your team. You have distributed tracing configured but parent spans from upstream services are not propagating into Claude Code traces. What setting enables context propagation through the proxy?",
    "options": [
     "Set CLAUDE_CODE_PROPAGATE_TRACEPARENT=1 — propagation through a custom base URL proxy is off by default",
     "Set OTEL_PROPAGATORS=tracecontext,baggage — the default propagator list excludes W3C TraceContext when a custom endpoint is used",
     "Propagation through custom proxies is not supported; you must use the native Anthropic API endpoint for distributed tracing",
     "Set ENABLE_BETA_TRACING_DETAILED=1 — traceparent propagation requires the detailed tracing beta flag"
    ],
    "correct": 0,
    "sectionIndices": [
     1
    ],
    "explanation": "When using a custom ANTHROPIC_BASE_URL proxy, traceparent propagation is off by default. Setting CLAUDE_CODE_PROPAGATE_TRACEPARENT=1 enables W3C traceparent context propagation through the proxy to connect Claude Code spans to upstream traces."
   }
  ],
  "tasks": [
   {
    "id": "stage-10-task-otel-local",
    "afterSectionIdx": 4,
    "title": "Wire up a local OpenTelemetry collector and verify cost metrics are flowing",
    "instructions": "**Prerequisites:** Docker installed, Claude Code v2.1+ installed.\n\n1. Clone the monitoring guide repo:\n```bash\ngit clone https://github.com/anthropics/claude-code-monitoring-guide /tmp/cc-monitoring\ncd /tmp/cc-monitoring\n```\n\n2. Start the Docker Compose stack (Prometheus + OTel Collector + Grafana):\n```bash\ndocker compose up -d\n```\n\n3. Add telemetry config to your **managed settings file** so it applies fleet-wide (not user-overridable). Edit `~/.claude/settings.json`:\n```json\n{\n  \"env\": {\n    \"CLAUDE_CODE_ENABLE_TELEMETRY\": \"1\",\n    \"OTEL_METRICS_EXPORTER\": \"otlp\",\n    \"OTEL_LOGS_EXPORTER\": \"otlp\",\n    \"OTEL_EXPORTER_OTLP_PROTOCOL\": \"http/protobuf\",\n    \"OTEL_EXPORTER_OTLP_ENDPOINT\": \"http://localhost:4318\",\n    \"OTEL_LOG_TOOL_DETAILS\": \"1\"\n  }\n}\n```\n\n4. Run a short Claude Code session to generate telemetry:\n```bash\nclaude -p \"What is 2+2? Answer in one word.\"\n```\n\n5. Verify metrics are arriving at the Prometheus endpoint:\n```bash\ncurl -s http://localhost:9090/api/v1/query?query=claude_code_cost_usage_total | python3 -m json.tool\n```\n\n6. Open Grafana at http://localhost:3000 (admin/admin) and check the Claude Code dashboard for cost and token data.",
    "doneWhen": "The Prometheus query for claude_code_cost_usage_total returns a non-empty result array, confirming that cost metrics from your Claude Code session are reaching the local OTel collector."
   },
   {
    "id": "stage-10-task-multi-team-attribution",
    "afterSectionIdx": 4,
    "title": "Configure per-team cost attribution labels in OTel resource attributes",
    "instructions": "This task sets up custom dimensions so metrics can be sliced by team or cost center in your OTel backend.\n\n1. Open your managed settings file:\n```bash\nnano ~/.claude/settings.json\n```\n\n2. Add `OTEL_RESOURCE_ATTRIBUTES` to the `env` block (adjust values to match your team):\n```json\n{\n  \"env\": {\n    \"CLAUDE_CODE_ENABLE_TELEMETRY\": \"1\",\n    \"OTEL_METRICS_EXPORTER\": \"otlp\",\n    \"OTEL_EXPORTER_OTLP_ENDPOINT\": \"http://localhost:4318\",\n    \"OTEL_RESOURCE_ATTRIBUTES\": \"department=engineering,team.id=platform,cost_center=eng-123\"\n  }\n}\n```\n\n3. Generate a session to emit labeled metrics:\n```bash\nclaude -p \"List three sorting algorithms.\"\n```\n\n4. Query Prometheus to confirm the custom label is attached to the cost metric:\n```bash\ncurl -s 'http://localhost:9090/api/v1/query?query=claude_code_cost_usage_total' | python3 -c \"\nimport sys, json\ndata = json.load(sys.stdin)\nfor r in data.get('data', {}).get('result', []):\n    print(r.get('metric', {}))\n\"\n```\n\nYou should see `department`, `team_id`, and `cost_center` labels in the metric output.\n\n5. To reduce per-datapoint cardinality while keeping team-level slicing, add:\n```json\n\"OTEL_METRICS_INCLUDE_RESOURCE_ATTRIBUTES\": \"false\"\n```\nThis moves the custom attrs to the OTLP resource block only (not per-datapoint labels). Re-run the query to confirm the labels no longer appear per-datapoint.",
    "doneWhen": "The Prometheus label query shows your custom department, team.id, and cost_center attributes attached to claude_code_cost_usage_total metric results."
   },
   {
    "id": "stage-10-task-ci-watchdog",
    "afterSectionIdx": 8,
    "title": "Configure retry resilience for a CI pipeline Claude Code job",
    "instructions": "This task hardens a headless Claude Code invocation against API capacity errors.\n\n1. Create a test CI wrapper script at `/tmp/cc-ci-wrapper.sh`:\n```bash\ncat > /tmp/cc-ci-wrapper.sh << 'EOF'\n#!/usr/bin/env bash\nset -euo pipefail\n\n# Retry indefinitely on 429/529 capacity errors (do NOT raise MAX_RETRIES instead)\nexport CLAUDE_CODE_RETRY_WATCHDOG=1\n\n# Keep MAX_RETRIES at default (10) or lower for fast failure on non-capacity errors\nexport CLAUDE_CODE_MAX_RETRIES=10\n\n# Extend per-request timeout for slow CI networks (default 600000ms = 10min)\nexport API_TIMEOUT_MS=900000\n\necho \"Starting Claude Code CI job with watchdog retry enabled...\"\nclaude -p \"$1\"\nEOF\nchmod +x /tmp/cc-ci-wrapper.sh\n```\n\n2. Test the wrapper runs a headless prompt successfully:\n```bash\n/tmp/cc-ci-wrapper.sh \"Output only the word DONE.\"\n```\n\n3. Verify the retry cap behavior: confirm that MAX_RETRIES=25 is clamped to 15 by checking the Claude Code source or docs. In your settings, never set it above 15 — use WATCHDOG instead for indefinite capacity retry.\n\n4. For a real CI integration (GitHub Actions example), add to your workflow `.github/workflows/claude-review.yml`:\n```yaml\n- name: Claude Code review\n  env:\n    ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}\n    CLAUDE_CODE_RETRY_WATCHDOG: \"1\"\n    CLAUDE_CODE_MAX_RETRIES: \"10\"\n    API_TIMEOUT_MS: \"900000\"\n  run: |\n    claude -p \"Review the diff and output a brief summary.\"\n```",
    "doneWhen": "Running `/tmp/cc-ci-wrapper.sh 'Output only the word DONE.'` completes successfully and prints DONE, confirming the watchdog-enabled wrapper executes headless Claude Code sessions correctly."
   }
  ],
  "visualizations": [
   {
    "id": "stage-10-v",
    "kind": "comparison-table",
    "title": "Monitoring, analytics & troubleshooting",
    "textualSummary": "Key concepts of Monitoring, analytics & troubleshooting: Adoption & ROI analytics dashboards, GitHub contribution attribution, OpenTelemetry cost & usage export.",
    "columns": [
     "Concept",
     "In this stage"
    ],
    "rows": [
     {
      "label": "Adoption & ROI analytics dashboards",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "GitHub contribution attribution",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "OpenTelemetry cost & usage export",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "Multi-team cost attribution",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     }
    ]
   }
  ],
  "confusions": [
   {
    "misconception": "Skipping Monitoring, analytics & troubleshooting.",
    "correction": "Each stage is load-bearing for the setup."
   },
   {
    "misconception": "These concepts are interchangeable.",
    "correction": "Each has a distinct role; see the definitions."
   }
  ],
  "quiz": [
   {
    "id": "stage-10-q1",
    "type": "multiple-choice",
    "prompt": "Your team has GitHub analytics enabled and the dashboard is completely empty after two days. A teammate says the toggle must be misconfigured. What is the most likely actual cause?",
    "options": [
     "The GitHub analytics toggle was enabled before the Claude GitHub app was installed",
     "The plan does not support GitHub analytics — it requires Enterprise only",
     "Contribution data always takes at least a week to appear for new setups",
     "The Owner role was not used to enable the analytics toggle"
    ],
    "correct": 0,
    "explanation": "The content states that 'An empty dashboard usually means the GitHub app isn't installed yet.' The setup must be sequenced: GitHub admin installs the app first, then a Claude Owner enables analytics and the GitHub toggle. If the app was never installed, no data flows regardless of toggle state. Option B is wrong — Teams plans are supported, not just Enterprise. Option C is wrong — the content says data typically appears within 24 hours, sometimes 'a few days,' not a week. Option D is wrong — the Owner role IS required for the toggle, but the question states analytics is 'enabled' meaning that step succeeded; the empty dashboard points to the missing app installation."
   },
   {
    "id": "stage-10-q2",
    "type": "multiple-choice",
    "prompt": "A developer rewrote 25% of a Claude-suggested function before merging the PR. Will those lines appear in the 'Lines of code with CC' metric?",
    "options": [
     "No — code rewritten more than 20% by the developer is not credited to Claude Code",
     "Yes — any PR merged within the attribution window counts all lines regardless of edits",
     "Yes — only the original Claude-suggested lines are counted, not the rewritten portion",
     "No — the PR falls outside the 21-day attribution window because of the rewrite time"
    ],
    "correct": 0,
    "explanation": "The content states explicitly: 'Code rewritten >20% by the developer is not credited to Claude Code.' A 25% rewrite exceeds this threshold, so those lines are excluded. Option B is wrong — the attribution window determines whether a session is matched to a PR, but rewrite percentage is a separate exclusion criterion applied on top. Option C is a plausible partial-credit intuition but the rule is a binary threshold on the whole block, not a partial deduction. Option D conflates rewrite time with the PR merge attribution window, which is a separate concept entirely."
   },
   {
    "id": "stage-10-q3",
    "type": "multiple-choice",
    "prompt": "Your team uses Bedrock to access Claude Code. After enabling OpenTelemetry, you notice that user.email is blank for every event. A teammate says this is a Bedrock configuration error you can fix in the console. What is actually happening?",
    "options": [
     "On Bedrock there is no Claude account identity, so only user.id and session.id populate; you must inject user identity yourself via OTEL_RESOURCE_ATTRIBUTES",
     "The OTEL_LOG_USER_PROMPTS flag must be set to 1 to surface user email in events",
     "user.email only populates after the user has authenticated with GitHub for contribution metrics",
     "The Bedrock IAM role lacks the permissions needed to pass user identity to the OTel exporter"
    ],
    "correct": 0,
    "explanation": "The content states: 'On Bedrock/Vertex/Foundry/direct-API there is no Claude account identity (only user.id and session.id populate) — inject enduser.id=jdoe@example.com yourself via OTEL_RESOURCE_ATTRIBUTES, or events can't map to real users.' This is a structural limitation of non-OAuth deployments, not a misconfiguration. Option B is wrong — OTEL_LOG_USER_PROMPTS logs prompt content, not user identity. Option C confuses GitHub OAuth for contribution metrics with the OTel user.email attribute, which comes from Claude account OAuth. Option D is wrong — IAM permissions govern API access, not OTel attribute population."
   },
   {
    "id": "stage-10-q4",
    "type": "multiple-choice",
    "prompt": "An SRE sets CLAUDE_CODE_MAX_RETRIES=20 for a long-running CI job to avoid transient 429 failures. What actually happens?",
    "options": [
     "The value is clamped to 15 — values above the cap are silently reduced to the maximum allowed",
     "The setting is rejected and the default of 10 is used instead",
     "The CI job will retry up to 20 times, but only for 5xx errors, not 429s",
     "The setting works as intended; there is no upper cap on CLAUDE_CODE_MAX_RETRIES"
    ],
    "correct": 0,
    "explanation": "The content states MAX_RETRIES is 'Capped at 15 (values above clamped).' Setting it to 20 silently results in 15 retries. The content also recommends CLAUDE_CODE_RETRY_WATCHDOG=1 for unattended/CI sessions that need to handle capacity errors indefinitely, rather than raising MAX_RETRIES. Option B is wrong — the value is clamped, not rejected. Option C is wrong — 429 throttles are explicitly listed among retry-eligible errors. Option D is wrong — the cap exists and the content is explicit about it."
   },
   {
    "id": "stage-10-q5",
    "type": "multiple-choice",
    "prompt": "You want to find every PR in your GitHub org that was Claude-assisted, outside of the analytics dashboard. What approach does the content recommend?",
    "options": [
     "Search GitHub for the 'claude-code-assisted' label, which is applied to merged PRs with Claude-assisted lines",
     "Query the OpenTelemetry pull_request.count metric, which includes PR URLs as attributes",
     "Export the dashboard CSV and cross-reference with your GitHub PR list",
     "Use the GitHub REST API to query Claude Code's internal PR tracking database"
    ],
    "correct": 0,
    "explanation": "The content states: 'Merged PRs with Claude-assisted lines are labeled claude-code-assisted in GitHub — query adoption programmatically by searching GitHub for that label rather than relying only on the dashboard.' This is the recommended programmatic approach. Option B is wrong — the OTel metric counts PRs but does not include PR URLs or enough metadata to enumerate specific PRs. Option C works only for the top 10 users unless you use 'Export all users,' and still requires a manual cross-reference — it is not the recommended programmatic approach. Option D is a fabrication; no such internal database is mentioned."
   },
   {
    "id": "stage-10-q6",
    "type": "multiple-choice",
    "prompt": "A security engineer wants to capture the exact Bash commands Claude Code ran during a session for SIEM analysis. Which configuration is required?",
    "options": [
     "Enable OTEL_LOG_TOOL_DETAILS=1 along with the OTLP logs exporter, which captures Bash commands and call arguments on tool_result events",
     "Set OTEL_LOG_RAW_API_BODIES=1, which includes Bash commands in the full Messages API JSON",
     "Enable OTEL_LOG_TOOL_CONTENT=1, which is the only flag that captures tool input in span events",
     "Bash commands are captured automatically by the audit log without any additional flags"
    ],
    "correct": 0,
    "explanation": "The content states: 'Enable the OTLP logs exporter and OTEL_LOG_TOOL_DETAILS=1 to capture MCP server/tool names, Bash commands, and call arguments on tool_result, tool_decision, mcp_server_connection events.' This is the correct combination for SIEM Bash command capture. Option B is wrong — OTEL_LOG_RAW_API_BODIES captures full Messages API JSON including conversation history, which is broader than needed and may include sensitive content; it is not the targeted mechanism for tool/Bash command logging. Option C is wrong — OTEL_LOG_TOOL_CONTENT captures tool input/output in span events and requires tracing to be enabled; OTEL_LOG_TOOL_DETAILS is the flag for Bash commands in log events. Option D is wrong — the audit event list (tool_decision, permission_mode_changed, etc.) does not include Bash command arguments without OTEL_LOG_TOOL_DETAILS."
   },
   {
    "id": "stage-10-q7",
    "type": "multiple-choice",
    "prompt": "You configure OTEL_METRIC_EXPORT_INTERVAL=1000 (1 second) to debug a flaky metric. The issue is resolved. What should you do and why?",
    "options": [
     "Reset the interval back to the default 60000ms — leaving 1-second debug intervals in production floods the exporter",
     "Leave it at 1 second to get the most granular production data possible",
     "Change it to 5000ms to match the logs and traces intervals, which are production defaults",
     "Remove the variable entirely so it inherits from the OTel Collector's configured scrape interval"
    ],
    "correct": 0,
    "explanation": "The content explicitly warns: 'Only lower [export intervals] for debugging, then reset' and 'Leaving 1s debug intervals in production floods the exporter.' The default metric export interval is 60000ms (60 seconds). Option B ignores the explicit warning about flooding. Option C is wrong — 5000ms is the default for logs and traces, not metrics; using it for metrics still floods the exporter relative to the intended 60-second cadence. Option D is wrong — the variable controls how often the client-side SDK batches and pushes metrics, not how often a Prometheus scraper pulls; removing it returns to the 60-second default, which is correct, but the reasoning about inheriting from the Collector is wrong."
   },
   {
    "id": "stage-10-q8",
    "type": "multi-select",
    "prompt": "Which of the following files or directories are AUTO-EXCLUDED from the 'Lines of code with CC' metric? Select all that apply.",
    "options": [
     "package-lock.json",
     "dist/ build artifacts",
     "Unit test files (e.g., foo.test.ts)",
     "Test fixture snapshots and mock data",
     "Lines longer than 1,000 characters"
    ],
    "correct": [
     0,
     1,
     3,
     4
    ],
    "explanation": "The content lists auto-excluded files as: 'lock files (package-lock.json, yarn.lock, Cargo.lock), generated/minified/protobuf code, build dirs (dist/, build/, node_modules/, target/), test fixtures (snapshots, cassettes, mock data), and any line >1,000 chars.' So package-lock.json (lock file), dist/ (build dir), test fixture snapshots/mock data, and lines >1000 chars are all excluded. Unit test files themselves (e.g., foo.test.ts) are NOT listed as auto-excluded — only test fixtures (snapshots, cassettes, mock data) are excluded. This is a critical distinction: the test code you write is counted; the snapshot/cassette/mock data files are not."
   },
   {
    "id": "stage-10-q9",
    "type": "multi-select",
    "prompt": "A teammate wants to attribute OTel cost metrics to specific teams and cost centers across a fleet deployment. Which approaches does the content support? Select all that apply.",
    "options": [
     "Set OTEL_RESOURCE_ATTRIBUTES per group in managed settings or a launch wrapper with values like department=engineering,team.id=platform",
     "Use the user.email attribute, which is automatically populated on all deployment types including Bedrock",
     "Set OTEL_RESOURCE_ATTRIBUTES=enduser.id=jdoe@example.com on Bedrock deployments where no Claude account identity exists",
     "Query the Console analytics dashboard's per-user cost column filtered by team",
     "Use the skill.name, plugin.name, and agent.name attributes on cost.usage and token.usage metrics to attribute spend to components"
    ],
    "correct": [
     0,
     2,
     4
    ],
    "explanation": "Option A is correct: the content explicitly describes setting OTEL_RESOURCE_ATTRIBUTES per group (managed settings or launch wrapper) with department/team/cost_center values to slice dashboards. Option C is correct: for Bedrock where no Claude account identity exists, the content recommends injecting enduser.id via OTEL_RESOURCE_ATTRIBUTES. Option E is correct: the content states 'attribute spend to skills/plugins/subagents via skill.name, plugin.name, agent.name on cost.usage/token.usage.' Option B is wrong — user.email only populates for OAuth users; on Bedrock/Vertex/direct-API it is absent. Option D is wrong — the dashboard does NOT surface per-user token counts and cost estimates; the content explicitly states 'the dashboard does NOT surface them — configure OpenTelemetry export.'"
   },
   {
    "id": "stage-10-q10",
    "type": "multiple-choice",
    "prompt": "An OTel query shows claude_code.api_error firing once for a request. Does this mean Claude Code retried the request once before giving up?",
    "options": [
     "No — api_error fires only after retries are exhausted; intermediate retries are not separate events. The attempt attribute shows how many retries occurred.",
     "Yes — each api_error event corresponds to one retry attempt, so one event means one retry",
     "No — api_error fires on the first failure; subsequent retries fire api_retry events instead",
     "It depends on whether CLAUDE_CODE_MAX_RETRIES was set above 1 for this session"
    ],
    "correct": 0,
    "explanation": "The content states: 'claude_code.api_error fires only after retries are exhausted (terminal); intermediate retries are not separate events — count retries via the attempt attribute.' So one api_error event means the entire retry sequence failed, not that one retry occurred. Option B has the relationship backwards — the event is terminal, not per-attempt. Option C invents an api_retry event that is not mentioned in the content. Option D is wrong — the semantics of api_error are fixed (terminal, post-exhaustion) regardless of the retry limit setting."
   },
   {
    "id": "stage-10-q11",
    "type": "multiple-choice",
    "prompt": "You want to see a complete per-user breakdown of activity, not just the top performers. Where do you get this data?",
    "options": [
     "Use 'Export all users' in the Console team insights dashboard to get a full per-user CSV",
     "The Leaderboard view shows all users, not just the top 10",
     "Run a query against the OpenTelemetry metrics using user.account_uuid to enumerate all users",
     "The Admin analytics page at claude.ai/analytics/claude-code shows all users in a paginated table"
    ],
    "correct": 0,
    "explanation": "The content states: 'Leaderboard shows top 10 users; use Export all users for a complete per-user CSV (all users, not just top 10).' The Export all users feature is the correct mechanism for complete data. Option B is directly contradicted by the content — the Leaderboard shows only top 10. Option C is technically possible via OTel but is not the recommended or described approach for this use case; the content describes Export all users for this purpose. Option D is wrong — the Admin analytics page is for admins/owners to view aggregate metrics, not a paginated per-user table."
   },
   {
    "id": "stage-10-q12",
    "type": "multiple-choice",
    "prompt": "A long-running overnight agent job is failing on 529 'Server is temporarily limiting requests' errors after 10 retries. A teammate suggests setting MAX_RETRIES=50. What does the content recommend instead, and why?",
    "options": [
     "Set CLAUDE_CODE_RETRY_WATCHDOG=1, which makes unattended sessions retry 429/529 capacity errors indefinitely instead of failing — this is preferred over raising MAX_RETRIES for long runs",
     "Set MAX_RETRIES=15, which is the effective maximum; 529 errors will then be retried up to 15 times",
     "Add a cron job that restarts the agent on failure, since no in-process retry mechanism handles 529s indefinitely",
     "529 errors count against quota and will block retries regardless of MAX_RETRIES; contact Anthropic support"
    ],
    "correct": 0,
    "explanation": "The content states: 'CLAUDE_CODE_RETRY_WATCHDOG | unset | =1 makes unattended/CI sessions retry 429/529 capacity errors indefinitely instead of failing — prefer this over raising MAX_RETRIES for long runs.' This is the explicit recommendation for this exact scenario. Option B is wrong — MAX_RETRIES is capped at 15, so even the suggested 50 only gives 15 retries, which isn't indefinite. The watchdog is the right tool for indefinite retry on capacity errors. Option C invents an external workaround when a built-in mechanism exists. Option D is directly contradicted by the content: '529 Overloaded / Server is temporarily limiting requests do not count against quota.'"
   },
   {
    "id": "stage-10-q13",
    "type": "multiple-choice",
    "prompt": "You use the /usage command in session. A teammate says /cost and /stats still work. What relationship do these commands have?",
    "options": [
     "/usage subsumes /cost and /stats — the old names route to the right tab in /usage, so all three effectively work",
     "/cost and /stats are deprecated and will error; only /usage is supported",
     "/usage, /cost, and /stats are completely independent commands with different data sources",
     "/cost works for subscription users; /usage is for API key users; /stats is for Enterprise only"
    ],
    "correct": 0,
    "explanation": "The content states: '/usage (subsumes /cost and /stats; old names route to the right tab).' The old command names still work as aliases that route to the appropriate tab within /usage. Option B is wrong — they still work as aliases, they don't error. Option C is wrong — they share the same underlying system, with /cost and /stats routing into /usage. Option D invents a user-tier distinction that the content does not describe."
   },
   {
    "id": "stage-10-q14",
    "type": "multi-select",
    "prompt": "Which of the following are among the audit events that Claude Code emits to the OTLP logs exporter? Select all that apply.",
    "options": [
     "tool_decision",
     "permission_mode_changed",
     "mcp_server_connection",
     "api_request",
     "plugin_installed"
    ],
    "correct": [
     0,
     1,
     2,
     4
    ],
    "explanation": "The content lists audit events as: 'tool_decision, permission_mode_changed, hook_execution_complete, auth, mcp_server_connection, plugin_installed/plugin_loaded, tool_result.' So tool_decision, permission_mode_changed, mcp_server_connection, and plugin_installed are all audit events. api_request is NOT listed as an audit event — it is a separate event type used for correlation (the content mentions using prompt.id to correlate api_request/tool_result events, but api_request itself is not in the audit event list)."
   },
   {
    "id": "stage-10-q15",
    "type": "multiple-choice",
    "prompt": "A manager sees the 'Lines of code with CC' metric is low for the past sprint and concludes the team barely used Claude Code. What does the content say about interpreting this reading?",
    "options": [
     "Treat reported numbers as a deliberate underestimate — only high-confidence attributions count, so low numbers should not be read as low usage",
     "The metric is accurate for all usage; low numbers indicate the team did not use Claude Code for code generation",
     "Low numbers typically indicate the GitHub app attribution window is misconfigured",
     "The metric only counts accepted suggestions, so low numbers mean the team rejected most Claude suggestions"
    ],
    "correct": 0,
    "explanation": "The content explicitly states: 'Treat reported numbers as a deliberate underestimate — only high-confidence attributions count. Don't read low numbers as low usage.' The metric has multiple exclusions (rewrites >20%, lock files, build dirs, lines >1000 chars, etc.) that intentionally omit uncertain attributions. Option B is the exact misconception the content warns against. Option C invents a specific diagnosis not supported by the content. Option D partially describes 'Lines of code accepted' (which excludes rejected suggestions) but the question is about overall line counts and the general underestimate principle."
   }
  ],
  "masteryCheckpoint": "You can explain the concepts of Monitoring, analytics & troubleshooting."
 },
 {
  "id": "stage-11",
  "stage": 11,
  "title": "Enterprise providers & gateways",
  "summary": "Enterprise providers & gateways: Model pinning, Small/fast model cost trap, Provider enable & routing precedence.",
  "prerequisites": [
   "stage-10"
  ],
  "objectives": [
   "Understand the concepts in Enterprise providers & gateways."
  ],
  "definitions": [
   {
    "term": "Model pinning",
    "short": "Explicitly setting each model-tier env var to a concrete model ID/deployment so the team runs a known version instead of a drifting built-in alias default."
   },
   {
    "term": "Small/fast model cost trap",
    "short": "If the background/small-fast model isn't pinned to a real Haiku deployment, it silently defaults to the PRIMARY model, so cheap tasks run at primary rates."
   },
   {
    "term": "Provider enable & routing precedence",
    "short": "Which enterprise backend a request hits is decided by env-var flags with a fixed precedence order, so leftover flags silently route to the wrong provider."
   },
   {
    "term": "Centralized config distribution",
    "short": "Pushing provider/model/auth config through the settings.json env block or enterprise managed settings rather than per-developer shell exports, treating keys as production credentials."
   },
   {
    "term": "Provider auth & credential refresh",
    "short": "Each backend authenticates via its cloud SDK's credential chain, with settings hooks that re-run or export credentials when they expire mid-session."
   },
   {
    "term": "Region & project resolution order",
    "short": "Region (and GCP project) are resolved through a precedence chain of env vars, profiles, and config files, so a stale value silently targets the wrong region/account."
   }
  ],
  "sections": [
   {
    "heading": "What this stage covers and why it matters",
    "body": "Claude Code supports five third-party backend providers alongside its own Anthropic API: Amazon Bedrock (which includes the Mantle sub-endpoint), Claude Platform on AWS, Google Vertex AI, and Microsoft Foundry. Each has its own model catalog, credential chain, region resolution rules, and billing surface. The Mantle endpoint is a distinct request path inside the Bedrock integration — it serves Claude models through the native Anthropic API shape rather than the Bedrock Invoke API — but it shares the same AWS credentials and IAM permissions as standard Bedrock.\n\nFor an individual developer the defaults are usually fine. For a team of twenty engineers, a misconfigured provider silently runs all background tasks at primary-model rates, costs accumulate invisibly, credentials expire mid-session and are never refreshed, and a gateway that strips one header breaks cost attribution across the fleet.\n\nThis stage treats the full stack: which env vars enable which provider, why the small/fast model must be pinned explicitly (and what it costs not to), how to distribute config through `settings.json` instead of shell profiles, how each cloud's credential chain resolves and how to hook refresh scripts into it, where region and project ID come from, what IAM permissions are needed before a model can be invoked, what LiteLLM gives you and what you must configure to make it work, and exactly which headers a gateway must forward or functionality degrades silently.\n\nEverything in this stage is derived from the official `code.claude.com` documentation fetched at authoring time. Env-var names, defaults, and precedence rules are exact."
   },
   {
    "heading": "Provider enable flags and routing",
    "body": "Claude Code selects a backend through boolean enable flags. Set exactly one per deployment. The flags and their companion env vars are:\n\n| Provider | Enable flag | Base URL override | Auth-skip flag |\n|---|---|---|---|\n| Anthropic API | (none — default) | `ANTHROPIC_BASE_URL` | — |\n| Bedrock | `CLAUDE_CODE_USE_BEDROCK=1` | `ANTHROPIC_BEDROCK_BASE_URL` | `CLAUDE_CODE_SKIP_BEDROCK_AUTH=1` |\n| Bedrock Mantle | `CLAUDE_CODE_USE_MANTLE=1` | `ANTHROPIC_BEDROCK_MANTLE_BASE_URL` | `CLAUDE_CODE_SKIP_MANTLE_AUTH=1` |\n| Vertex AI | `CLAUDE_CODE_USE_VERTEX=1` | `ANTHROPIC_VERTEX_BASE_URL` | `CLAUDE_CODE_SKIP_VERTEX_AUTH=1` |\n| Foundry | `CLAUDE_CODE_USE_FOUNDRY=1` | `ANTHROPIC_FOUNDRY_BASE_URL` | `CLAUDE_CODE_SKIP_FOUNDRY_AUTH=1` |\n| Claude Platform on AWS | `CLAUDE_CODE_USE_ANTHROPIC_AWS=1` | `ANTHROPIC_AWS_BASE_URL` | `CLAUDE_CODE_SKIP_ANTHROPIC_AWS_AUTH=1` |\n\nThe auth-skip flags tell Claude Code not to generate a signed request or attach a cloud SDK token. Use them when a gateway injects credentials server-side.\n\n### Routing priority when multiple flags coexist\n\nThe official docs document one conflict explicitly: **Bedrock and Foundry take precedence over Claude Platform on AWS**. The docs state: \"unset `CLAUDE_CODE_USE_BEDROCK` and `CLAUDE_CODE_USE_FOUNDRY` if they're set\" when targeting Claude Platform on AWS. No full multi-flag precedence ladder is documented for other combinations — set exactly one flag to avoid ambiguity.\n\n### Running Bedrock and Mantle simultaneously\n\n`CLAUDE_CODE_USE_BEDROCK=1` and `CLAUDE_CODE_USE_MANTLE=1` can coexist. Claude Code routes each request based on the model ID format: IDs prefixed with `anthropic.` (the Mantle format, e.g., `anthropic.claude-haiku-4-5`) route to Mantle; all other IDs route to the Bedrock Invoke API. `/status` inside a running session shows the active provider; when both are active it shows `Amazon Bedrock + Amazon Bedrock (Mantle)`.\n\n### Base URL overrides change destination, not model\n\n`ANTHROPIC_BASE_URL` (and the provider-specific equivalents) point requests at a different host. They do **not** change which model Claude Code selects. When a gateway changes the reachable model set, always pair the base URL override with model pinning variables (described next) and optionally `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1`.\n\n### What Mantle is and when to use it\n\nMantle is a Bedrock endpoint that accepts the native Anthropic Messages API shape. This means your gateway can use the same Anthropic-format request parsing for Bedrock traffic that it uses for direct Anthropic API traffic. Mantle has its own model lineup (IDs like `anthropic.claude-haiku-4-5`) that is separate from the standard Bedrock catalog and requires separate account access — contact your AWS account team. Requires Claude Code v2.1.94 or later."
   },
   {
    "heading": "Model pinning: the exact variables and what they control",
    "body": "Claude Code resolves models through aliases — `fable`, `opus`, `sonnet`, `haiku` — that expand to a concrete model ID at runtime. The expansion depends on provider:\n\n- On **Anthropic API**: `opus` → Opus 4.8, `sonnet` → Sonnet 4.6\n- On **Claude Platform on AWS**: `opus` → Opus 4.7, `sonnet` → Sonnet 4.6\n- On **Bedrock, Vertex, and Foundry**: `opus` → Opus 4.6, `sonnet` → Sonnet 4.5 (the built-in default lags the newest release deliberately — the model may not yet be enabled in every account)\n- On **Foundry**: there is no startup fallback check; a missing model causes errors at first request\n\nFive env vars override alias resolution:\n\n| Variable | Alias it controls | Also controls |\n|---|---|---|\n| `ANTHROPIC_DEFAULT_FABLE_MODEL` | `fable` | Identity check for automatic fallback from Fable 5 on 3rd-party providers |\n| `ANTHROPIC_DEFAULT_OPUS_MODEL` | `opus` | `opusplan` during plan mode |\n| `ANTHROPIC_DEFAULT_SONNET_MODEL` | `sonnet` | `opusplan` during execution mode |\n| `ANTHROPIC_DEFAULT_HAIKU_MODEL` | `haiku` | Background/small-fast tasks (title generation, context compression) |\n| `CLAUDE_CODE_SUBAGENT_MODEL` | all subagents and agent-team members | Overrides per-invocation model parameter |\n\n`ANTHROPIC_SMALL_FAST_MODEL` is deprecated. Use `ANTHROPIC_DEFAULT_HAIKU_MODEL`.\n\n### Model ID format by provider\n\n```bash\n# Bedrock — cross-region inference profile IDs (with regional prefix)\nexport ANTHROPIC_DEFAULT_OPUS_MODEL='us.anthropic.claude-opus-4-8'\nexport ANTHROPIC_DEFAULT_SONNET_MODEL='us.anthropic.claude-sonnet-4-6'\nexport ANTHROPIC_DEFAULT_HAIKU_MODEL='us.anthropic.claude-haiku-4-5-20251001-v1:0'\n\n# GovCloud regions use the us-gov. prefix\nexport ANTHROPIC_DEFAULT_OPUS_MODEL='us-gov.anthropic.claude-opus-4-8'\n\n# Application inference profile ARN\nexport ANTHROPIC_DEFAULT_OPUS_MODEL='arn:aws:bedrock:us-east-2:123456789012:application-inference-profile/opus-prod'\n\n# Vertex AI — bare model IDs (no regional prefix), snapshot with @ separator\nexport ANTHROPIC_DEFAULT_OPUS_MODEL='claude-opus-4-8'\nexport ANTHROPIC_DEFAULT_SONNET_MODEL='claude-sonnet-4-6'\nexport ANTHROPIC_DEFAULT_HAIKU_MODEL='claude-haiku-4-5@20251001'\n\n# Foundry — deployment name you created in the Azure portal\nexport ANTHROPIC_DEFAULT_OPUS_MODEL='claude-opus-4-8'\n```\n\n### 1M context window pinning\n\nAppend `[1m]` to the model ID in any `ANTHROPIC_DEFAULT_*_MODEL` variable. Claude Code strips the suffix before sending to the provider:\n\n```bash\nexport ANTHROPIC_DEFAULT_OPUS_MODEL='claude-opus-4-8[1m]'\n```\n\nThe suffix is read per variable: a model ID without `[1m]` in its variable uses the 200K window even if another variable pins the same model with `[1m]`. Only append `[1m]` to models that actually support 1M context (Opus 4.6+, Sonnet 4.6, Fable 5).\n\n### Declaring capabilities for provider-specific IDs\n\nBedrock ARNs and Foundry deployment names do not match Claude Code's pattern matching for features like adaptive thinking and effort levels. Pair each `ANTHROPIC_DEFAULT_*_MODEL` with companion variables:\n\n```bash\nexport ANTHROPIC_DEFAULT_OPUS_MODEL='arn:aws:bedrock:us-east-1:123456789012:application-inference-profile/opus-prod'\nexport ANTHROPIC_DEFAULT_OPUS_MODEL_NAME='Opus via Bedrock'\nexport ANTHROPIC_DEFAULT_OPUS_MODEL_DESCRIPTION='Opus 4.8 routed through Bedrock application inference profile'\nexport ANTHROPIC_DEFAULT_OPUS_MODEL_SUPPORTED_CAPABILITIES='effort,xhigh_effort,max_effort,thinking,adaptive_thinking,interleaved_thinking'\n```\n\nThe same `_NAME`, `_DESCRIPTION`, and `_SUPPORTED_CAPABILITIES` suffixes are available for `SONNET`, `HAIKU`, `FABLE`, and `ANTHROPIC_CUSTOM_MODEL_OPTION`. When `_SUPPORTED_CAPABILITIES` is unset, Claude Code falls back to built-in detection from the model ID string — which fails for ARNs and deployment names."
   },
   {
    "heading": "The small/fast model cost trap",
    "body": "This is the most common and most expensive misconfiguration in enterprise Bedrock and Vertex deployments.\n\nClaude Code uses two model tiers. The **primary model** handles interactive coding sessions. The **small/fast model** (Haiku tier) handles background work: session title generation, summary pruning, context compression between turns. On a busy team, background calls can account for 20-40% of total token spend.\n\nThe trap: on Bedrock, Vertex, and Foundry, when `ANTHROPIC_DEFAULT_HAIKU_MODEL` is not set, the small/fast model defaults to the **same model as the primary**. The official docs are explicit:\n\n> Background tasks such as session title generation use the small/fast model, normally a Haiku-class model. On Bedrock, Claude Code defaults this to the primary model because Haiku may not be enabled in every account or region.\n\nThe same behavior applies on Vertex AI and Foundry. The provider defaults:\n\n| Provider | Primary model default | Small/fast model default (no pin) |\n|---|---|---|\n| Bedrock | `us.anthropic.claude-sonnet-4-5-20250929-v1:0` | Same as primary |\n| Vertex AI | `claude-sonnet-4-5@20250929` | Same as primary |\n| Foundry | `claude-sonnet-4-5` | Same as primary |\n\nHaiku 4.5 is roughly 5x cheaper per token than Sonnet 4.6 and 25x cheaper than Opus 4.8. An Opus 4.8 primary deployment with no Haiku pin runs all title generation and context summarization at Opus 4.8 rates.\n\nThe fix is explicit and mandatory:\n\n```bash\n# Bedrock\nexport ANTHROPIC_DEFAULT_HAIKU_MODEL='us.anthropic.claude-haiku-4-5-20251001-v1:0'\n\n# Vertex AI\nexport ANTHROPIC_DEFAULT_HAIKU_MODEL='claude-haiku-4-5@20251001'\n\n# Foundry\nexport ANTHROPIC_DEFAULT_HAIKU_MODEL='claude-haiku-4-5'\n```\n\nBefore setting this, confirm the model is enabled in your account. On Bedrock: check the Model catalog in the console and submit the use case form for Haiku if not already done (model family approvals are per-family — enabling Sonnet does not enable Haiku). On Vertex: confirm Haiku is separately approved in Model Garden. An unset variable costs money; an incorrectly set variable targeting a disabled model causes background-task failures that surface as empty session titles, unexpected context-window exhaustion, or silent summarization errors.\n\n### Verification\n\nAfter enabling the team for a day, run the model distribution query:\n\n```bash\nsqlite3 ~/projects/data/llm_observability.db \\\n  \"SELECT model, COUNT(*) as calls, SUM(input_tokens+output_tokens) as tokens \\\n   FROM llm_calls \\\n   WHERE project='myproject' AND date > date('now', '-1 day') \\\n   GROUP BY model ORDER BY tokens DESC;\"\n```\n\nExpected: Haiku-class model for 30%+ of call count (background tasks), Opus/Sonnet for the remainder. A distribution that is 100% Opus or 100% Sonnet is the diagnostic signature of a missing Haiku pin."
   },
   {
    "heading": "Centralized config distribution via settings.json",
    "body": "Shell profiles (`.bashrc`, `.zshrc`) are per-user, per-machine, and invisible to version control. They are the wrong layer for team config. Claude Code has a structured settings system with five scopes:\n\n| Scope | File | Git-tracked | Priority |\n|---|---|---|---|\n| Managed | See paths below | No | 1st (highest; cannot override) |\n| Command-line args | `--model`, `--settings` flags | — | 2nd |\n| Local | `.claude/settings.local.json` | No (gitignored) | 3rd |\n| Project | `.claude/settings.json` | Yes | 4th |\n| User | `~/.claude/settings.json` | No | 5th (lowest) |\n\nNote: the Local scope (`.claude/settings.local.json`) has **higher** priority than the Project scope. Permission rules are an exception — they merge across scopes rather than override.\n\nManaged settings paths by OS:\n\n| OS | File path | Drop-in directory |\n|---|---|---|\n| macOS | `/Library/Application Support/ClaudeCode/managed-settings.json` | `/Library/Application Support/ClaudeCode/managed-settings.d/` |\n| Linux / WSL | `/etc/claude-code/managed-settings.json` | `/etc/claude-code/managed-settings.d/` |\n| Windows | `C:\\Program Files\\ClaudeCode\\managed-settings.json` | `C:\\Program Files\\ClaudeCode\\managed-settings.d\\` |\n| Windows (registry) | `HKLM\\SOFTWARE\\Policies\\ClaudeCode` (REG_SZ or REG_EXPAND_SZ, JSON value) | — |\n\nThe `env` block inside any settings file injects environment variables into every Claude Code session and its subprocess environment:\n\n```json\n// .claude/settings.json (project scope, committed to the repo)\n{\n  \"$schema\": \"https://json.schemastore.org/claude-code-settings.json\",\n  \"model\": \"opus\",\n  \"env\": {\n    \"CLAUDE_CODE_USE_BEDROCK\": \"1\",\n    \"AWS_REGION\": \"us-east-1\",\n    \"AWS_PROFILE\": \"claude-code-prod\",\n    \"ANTHROPIC_DEFAULT_OPUS_MODEL\": \"us.anthropic.claude-opus-4-8\",\n    \"ANTHROPIC_DEFAULT_SONNET_MODEL\": \"us.anthropic.claude-sonnet-4-6\",\n    \"ANTHROPIC_DEFAULT_HAIKU_MODEL\": \"us.anthropic.claude-haiku-4-5-20251001-v1:0\"\n  },\n  \"availableModels\": [\"opus\", \"sonnet\", \"haiku\"],\n  \"awsAuthRefresh\": \"aws sso login --profile claude-code-prod\"\n}\n```\n\nThis file goes in the repo root, gets committed, and every developer who clones the repo gets the correct provider, region, and model pins automatically — with no shell-profile editing.\n\n### Managed settings for fleet-wide enforcement\n\nFor policies that must not be overridable by end users, use the managed layer:\n\n- **macOS**: deploy via Jamf or Kandji to `/Library/Application Support/ClaudeCode/managed-settings.json`, or use the `com.anthropic.claudecode` MDM/Plist preferences domain\n- **Windows**: deploy via Intune or Group Policy to `HKLM\\SOFTWARE\\Policies\\ClaudeCode` (REG_SZ or REG_EXPAND_SZ value containing JSON), or to `C:\\Program Files\\ClaudeCode\\managed-settings.json`\n- **Linux/WSL**: deploy via Ansible, Chef, or file copy to `/etc/claude-code/managed-settings.json`\n\nFor multi-policy environments, the `managed-settings.d/` drop-in directory merges JSON fragments alphabetically (hidden files ignored):\n\n```\n/etc/claude-code/\n  managed-settings.json          # base policy\n  managed-settings.d/\n    10-provider.json             # Bedrock provider + model pins\n    20-security.json             # permission deny-lists\n    30-models.json               # availableModels enforcement\n```\n\nMerge rules: scalar values in later fragments override earlier ones; arrays are concatenated and deduplicated; objects are deep-merged. When `availableModels` is set in managed settings, the managed value **replaces** (does not merge with) lower-precedence entries.\n\nAs of v2.1.169, invalid managed entries are stripped with a warning — a single typo cannot disable the entire policy. Run `claude doctor` after deploying to validate before fleet rollout.\n\n### What belongs in which layer\n\n- **Project `settings.json`**: provider choice, model pins, `availableModels` restriction, `awsAuthRefresh` script path — anything that is team-standard for this repo.\n- **Managed settings**: `forceLoginMethod`, `forceLoginOrgUUID`, `requiredMinimumVersion`, hard `enforceAvailableModels`, `allowManagedMcpServersOnly`, `claudeMd` (org-wide CLAUDE.md injection).\n- **User `settings.json`**: personal model preferences, personal AWS profile override.\n- **Shell profile**: nothing related to Claude Code in a multi-user enterprise setup."
   },
   {
    "heading": "Provider auth and credential refresh",
    "body": "### Anthropic API\n\nThree mechanisms, in priority order:\n\n1. `ANTHROPIC_AUTH_TOKEN` — sent as `Authorization: Bearer <token>`. Use for LiteLLM virtual keys or vault-issued tokens.\n2. `ANTHROPIC_API_KEY` — sent as `x-api-key` header. Standard first-party key. Takes precedence over subscription login.\n3. `apiKeyHelper` in settings — a shell script (run by `/bin/sh`) whose stdout is sent as both the `X-Api-Key` header and the `Authorization: Bearer` header. Lower precedence than both env vars above.\n\n```json\n{\n  \"apiKeyHelper\": \"~/bin/get-anthropic-key.sh\",\n  \"env\": {\n    \"CLAUDE_CODE_API_KEY_HELPER_TTL_MS\": \"3600000\"\n  }\n}\n```\n\nThe helper runs at session start and then again after `TTL_MS` elapses. The script can call a vault, generate a JWT, or issue a short-lived token from your IdP.\n\n### Amazon Bedrock\n\nClaude Code uses the standard AWS SDK credential chain. The documented options are:\n\n- `AWS_ACCESS_KEY_ID` + `AWS_SECRET_ACCESS_KEY` (+ optional `AWS_SESSION_TOKEN`) — static/session credentials\n- `AWS_BEARER_TOKEN_BEDROCK` — a Bedrock API key; simpler than full IAM credentials, useful when the token's permission scope is narrower than a full IAM role\n- `AWS_PROFILE` — named profile from `~/.aws/credentials` / `~/.aws/config`\n- AWS SSO cache (populated by `aws sso login`)\n- IAM instance/task/workload identity (EC2 instance role, ECS task role, EKS service account)\n\nFor SSO, the recommended pattern in settings:\n\n```json\n{\n  \"awsAuthRefresh\": \"aws sso login --profile claude-code-prod\",\n  \"env\": {\n    \"AWS_PROFILE\": \"claude-code-prod\"\n  }\n}\n```\n\n`awsAuthRefresh` triggers when Bedrock returns a credential error **or** Claude Code detects the credentials have expired based on their timestamp. It displays its output to the user (SSO flows open a browser tab). After the command succeeds, Claude Code retries the failed request.\n\nFor cross-account credentials that differ from the default chain, use `awsCredentialExport` instead. This script runs at session start and on each credential reload (not only when credentials are expired). Its stdout must be JSON:\n\n```json\n{\n  \"Credentials\": {\n    \"AccessKeyId\": \"ASIA...\",\n    \"SecretAccessKey\": \"...\",\n    \"SessionToken\": \"...\",\n    \"Expiration\": \"2026-06-24T00:00:00Z\"\n  }\n}\n```\n\nAs of v2.1.181, the flat output format from `aws configure export-credentials --format process` is also accepted (same keys at the top level, not nested under `Credentials`). With a valid ISO 8601 `Expiration` field (v2.1.176+), credentials are cached until 5 minutes before expiry. Without it, they are cached for one hour.\n\n### Google Vertex AI\n\nClaude Code uses Application Default Credentials (ADC). Sources in precedence order per the docs:\n\n1. `GCLOUD_PROJECT` or `GOOGLE_CLOUD_PROJECT` — project ID (these take precedence over `ANTHROPIC_VERTEX_PROJECT_ID`)\n2. `GOOGLE_APPLICATION_CREDENTIALS` — path to a service account JSON key file or Workload Identity Federation credential config (v2.1.121+)\n3. `gcloud auth application-default login` credentials at `~/.config/gcloud/application_default_credentials.json`\n4. `ANTHROPIC_VERTEX_PROJECT_ID` — Claude Code's own project ID variable\n5. Active `gcloud` configuration (`gcloud config get-value project`)\n6. Attached service account (GCE, GKE, Cloud Run metadata)\n\nFor teams using workstation-based auth:\n\n```json\n{\n  \"gcpAuthRefresh\": \"gcloud auth application-default login\",\n  \"env\": {\n    \"ANTHROPIC_VERTEX_PROJECT_ID\": \"my-project-prod\"\n  }\n}\n```\n\n`gcpAuthRefresh` triggers when Claude Code detects expired or unloadable GCP credentials and retries after the command succeeds. The command times out after 3 minutes. If set in project settings (`.claude/settings.json`), it only runs after you accept the workspace trust prompt.\n\n### Common pitfall: `GCLOUD_PROJECT` silently overrides `ANTHROPIC_VERTEX_PROJECT_ID`\n\nA developer who has `GCLOUD_PROJECT` set for an unrelated workflow will have their Claude Code sessions target the wrong GCP project even if `ANTHROPIC_VERTEX_PROJECT_ID` is correctly set in `.claude/settings.json`. Check `/status` to confirm the resolved project ID.\n\n### Common pitfall: interactive commands in refresh hooks\n\nNone of the refresh hooks (`awsAuthRefresh`, `awsCredentialExport`, `gcpAuthRefresh`, `apiKeyHelper`) support interactive stdin. Commands that prompt for a password or MFA code hang silently. Use only browser-based flows (SSO redirects, `az login`, `gcloud auth` browser flows) or non-interactive credential export scripts. The symptom of a broken refresh is a session that stalls for the hook's timeout period then fails with an auth error."
   },
   {
    "heading": "Region and project ID resolution order",
    "body": "Stale region or project values are the second most common cause of \"model not found\" and \"no access\" errors — especially after new models are enabled in a different region than the one hardcoded in a shell profile months ago.\n\n### AWS region resolution (Bedrock and Mantle)\n\nAs of Claude Code v2.1.172, resolution order from highest to lowest precedence:\n\n1. `AWS_REGION`\n2. `AWS_DEFAULT_REGION`\n3. `region` key in the active AWS profile, read from `~/.aws/credentials` first then `~/.aws/config` (matching AWS SDK precedence); use `AWS_SHARED_CREDENTIALS_FILE` or `AWS_CONFIG_FILE` to override file paths\n4. Hardcoded fallback: `us-east-1`\n\nThe active profile is `AWS_PROFILE` if set, otherwise `default`. On versions before v2.1.172, Claude Code did not read AWS config files — `AWS_REGION` was required explicitly.\n\nTo use a separate region for the small/fast (Haiku) model:\n\n```bash\nexport ANTHROPIC_SMALL_FAST_MODEL_AWS_REGION=us-west-2\n# Note: has no effect unless ANTHROPIC_DEFAULT_HAIKU_MODEL is also set\n```\n\nRun `/status` inside Claude Code to see the resolved region and which source it came from (env var, profile, or default fallback).\n\n### GCP region and project resolution (Vertex AI)\n\nProject ID resolution order:\n\n1. `GCLOUD_PROJECT` or `GOOGLE_CLOUD_PROJECT` (env vars, highest precedence)\n2. Project ID in the credential file referenced by `GOOGLE_APPLICATION_CREDENTIALS`\n3. `ANTHROPIC_VERTEX_PROJECT_ID`\n4. Active `gcloud` configuration (`gcloud config get-value project`)\n5. Attached service account's project (GCE/GKE metadata)\n\nThe non-obvious fact: `GCLOUD_PROJECT` takes precedence over `ANTHROPIC_VERTEX_PROJECT_ID`. If a developer has `GCLOUD_PROJECT` set for a different workflow, their Claude Code sessions target the wrong project even if `ANTHROPIC_VERTEX_PROJECT_ID` is correctly set in `.claude/settings.json`.\n\nRegion is set with `CLOUD_ML_REGION`. Valid values: `global`, multi-region codes (`us`, `eu`), or specific regions (`us-east5`, `europe-west1`). Claude Code selects the correct Vertex AI hostname for each form automatically. For models that do not support global endpoints, override per-model:\n\n```bash\nexport CLOUD_ML_REGION=global\nexport VERTEX_REGION_CLAUDE_HAIKU_4_5=us-east5\nexport VERTEX_REGION_CLAUDE_4_6_SONNET=europe-west1\n```\n\nMost model versions have a corresponding `VERTEX_REGION_CLAUDE_*` variable — see the env-vars reference for the full list.\n\n### Diagnosing stale region values\n\nExact error symptoms by provider:\n- **Bedrock**: `ValidationException: Invocation of model ID ... with on-demand throughput isn't supported` — the model is not available in the resolved region. Fix: specify the model as an [inference profile ID](https://docs.aws.amazon.com/bedrock/latest/userguide/inference-profiles-support.html), or use a region where it is available.\n- **Vertex**: HTTP 404 with `model not found` — the model is not available in the resolved region or project, or was not enabled in Model Garden.\n\nAlways check `/status` first to confirm the resolved region and project before investigating the model access layer."
   },
   {
    "heading": "Model access provisioning: IAM, RBAC, and catalog approvals",
    "body": "Provider flags and credentials are necessary but not sufficient. Each cloud requires a separate model access provisioning step before an invocation succeeds.\n\n### Amazon Bedrock\n\n**Step 1 — Use case form.** First-time Anthropic model access on Bedrock requires submitting a use case form once per AWS account. Navigate to the Bedrock console → Model catalog → select an Anthropic model → submit the form. Access is granted immediately after submission.\n\nFor AWS Organizations, submit from the management account using the `PutUseCaseForModelAccess` API, which requires the `bedrock:PutUseCaseForModelAccess` IAM permission. Approval propagates to child accounts automatically.\n\n**Step 2 — IAM policy.** Attach a policy with these permissions to the IAM role or user running Claude Code:\n\n```json\n{\n  \"Version\": \"2012-10-17\",\n  \"Statement\": [\n    {\n      \"Sid\": \"AllowModelAndInferenceProfileAccess\",\n      \"Effect\": \"Allow\",\n      \"Action\": [\n        \"bedrock:InvokeModel\",\n        \"bedrock:InvokeModelWithResponseStream\",\n        \"bedrock:ListInferenceProfiles\",\n        \"bedrock:GetInferenceProfile\"\n      ],\n      \"Resource\": [\n        \"arn:aws:bedrock:*:*:inference-profile/*\",\n        \"arn:aws:bedrock:*:*:application-inference-profile/*\",\n        \"arn:aws:bedrock:*:*:foundation-model/*\"\n      ]\n    },\n    {\n      \"Sid\": \"AllowMarketplaceSubscription\",\n      \"Effect\": \"Allow\",\n      \"Action\": [\n        \"aws-marketplace:ViewSubscriptions\",\n        \"aws-marketplace:Subscribe\"\n      ],\n      \"Resource\": \"*\",\n      \"Condition\": {\n        \"StringEquals\": {\n          \"aws:CalledViaLast\": \"bedrock.amazonaws.com\"\n        }\n      }\n    }\n  ]\n}\n```\n\n`bedrock:GetInferenceProfile` lets Claude Code resolve an application inference profile ARN to its backing foundation model, selecting the correct request shape. Without it, Claude Code recovers automatically by retrying once with the alternate shape — requests still succeed, but each new model adds an extra round-trip. This applies most often to `AWS_BEARER_TOKEN_BEDROCK` deployments where the token policy is narrower than a full IAM role.\n\n### Google Vertex AI\n\n**Step 1 — Enable the API.**\n```bash\ngcloud services enable aiplatform.googleapis.com\n```\n\n**Step 2 — Model Garden approval.** Navigate to Vertex AI Model Garden in the GCP console, search for \"Claude\", and request access to each model family. Approval takes 24-48 hours. Haiku requires a separate approval from Sonnet and Opus — they are distinct catalog entries. This is the most common source of `ANTHROPIC_DEFAULT_HAIKU_MODEL` failures where Haiku was pinned but not approved.\n\n**Step 3 — IAM.** Assign `roles/aiplatform.user` to the service account or developer identity. The single required permission for model invocation is `aiplatform.endpoints.predict`. For more restrictive permissions, create a custom role with only that permission.\n\n### Microsoft Foundry\n\nAssign the `Azure AI User` or `Cognitive Services User` default role. For more restrictive permissions, create a custom role with the data action `Microsoft.CognitiveServices/accounts/providers/*`.\n\n### Common pitfall: model family approvals are per-family, not per-tier\n\nOn both Bedrock and Vertex, approvals are model-family-scoped. Enabling Claude Sonnet does not enable Claude Haiku. On Bedrock, a separate use case form submission is needed for each Anthropic model family. On Vertex, each model family in Model Garden is a separate approval request with its own 24-48h window.\n\nA deployment that pins `ANTHROPIC_DEFAULT_HAIKU_MODEL` before Haiku is approved produces a silent failure mode: primary model sessions work, background tasks fail quietly, session titles remain empty, context is never summarized, and context windows fill faster than expected."
   },
   {
    "heading": "LiteLLM proxy: architecture and when to use it",
    "body": "An LLM gateway is a service that sits between Claude Code and the cloud provider. Primary motivations:\n\n- **Centralized auth** — developers authenticate to the gateway, not directly to AWS/GCP. One secret to rotate instead of N.\n- **Cost attribution** — query the gateway's logs instead of parsing AWS Cost Explorer or GCP Billing per developer.\n- **Rate limiting and budgets** — per-team or per-user spend caps at the gateway layer.\n- **Audit logging** — every prompt and response logged for compliance before reaching the cloud.\n- **Multi-provider fallback** — route to Bedrock when Anthropic API is rate-limited, or vice versa.\n\nLiteLLM is the most common open-source choice. It exposes an OpenAI-compatible endpoint that rewrites requests to Anthropic, Bedrock, Vertex, Azure, and others.\n\n### When a gateway adds complexity without benefit\n\n| Use case | Gateway needed? |\n|---|---|\n| Single team, direct Anthropic API | No — `ANTHROPIC_API_KEY` in managed settings is sufficient |\n| Multi-team on Bedrock, shared AWS account, need per-team spend | Yes — gateway adds team-level cost attribution |\n| Strict network policy: outbound only to single internal endpoint | Yes — gateway is the egress point |\n| Want to switch providers without redeploying all developer configs | Yes — change gateway backend, client config unchanged |\n| Simple Bedrock deployment, each dev has own IAM role | No — Bedrock's CloudTrail + Cost Explorer handle attribution natively |\n| Team size < 5 engineers | Probably not worth the ops overhead |\n| Team size > 20 with separate billing or compliance needs | Worth the ops overhead |\n\n### Basic LiteLLM setup\n\n```bash\npip install 'litellm[proxy]'\n```\n\n```yaml\n# litellm_config.yaml\nmodel_list:\n  - model_name: claude-opus-4-8\n    litellm_params:\n      model: bedrock/us.anthropic.claude-opus-4-8\n      aws_region_name: us-east-1\n  - model_name: claude-sonnet-4-6\n    litellm_params:\n      model: bedrock/us.anthropic.claude-sonnet-4-6\n      aws_region_name: us-east-1\n  - model_name: claude-haiku-4-5\n    litellm_params:\n      model: bedrock/us.anthropic.claude-haiku-4-5-20251001-v1:0\n      aws_region_name: us-east-1\n\nlitellm_settings:\n  pass_through_unrecognized_params: true  # preserves anthropic-beta body fields\n  drop_params: false\n```\n\n```bash\nlitellm --config litellm_config.yaml --port 4000\n```\n\nClaude Code configuration to route through the unified Anthropic-format endpoint (recommended over pass-through):\n\n```bash\nexport ANTHROPIC_BASE_URL=https://litellm.internal:4000\nexport ANTHROPIC_AUTH_TOKEN=sk-litellm-virtual-key\n\n# Model pins must match LiteLLM's model_name values exactly\nexport ANTHROPIC_DEFAULT_OPUS_MODEL='claude-opus-4-8'\nexport ANTHROPIC_DEFAULT_SONNET_MODEL='claude-sonnet-4-6'\nexport ANTHROPIC_DEFAULT_HAIKU_MODEL='claude-haiku-4-5'\n```\n\nProvider-native pass-through endpoints (alternative — use when you need the Bedrock or Vertex billing surface directly):\n\n```bash\n# Bedrock pass-through\nexport CLAUDE_CODE_USE_BEDROCK=1\nexport ANTHROPIC_BEDROCK_BASE_URL=https://litellm.internal:4000/bedrock\nexport CLAUDE_CODE_SKIP_BEDROCK_AUTH=1  # gateway injects AWS auth\n\n# Vertex AI pass-through\nexport CLAUDE_CODE_USE_VERTEX=1\nexport ANTHROPIC_VERTEX_BASE_URL=https://litellm.internal:4000/vertex_ai/v1\nexport ANTHROPIC_VERTEX_PROJECT_ID=my-gcp-project\nexport CLAUDE_CODE_SKIP_VERTEX_AUTH=1\nexport CLOUD_ML_REGION=us-east5\n```\n\n`CLAUDE_CODE_SKIP_*_AUTH=1` prevents Claude Code from generating SigV4 signatures or GCP OAuth tokens — the gateway injects them server-side.\n\n### Security warning\n\nLiteLLM PyPI versions 1.82.7 and 1.82.8 were compromised with credential-stealing malware. Remove those versions, rotate all credentials on affected systems, and follow the remediation in [BerriAI/litellm#24518](https://github.com/BerriAI/litellm/issues/24518). Pin to a specific known-good version in your deployment pipeline. LiteLLM is a third-party service; Anthropic does not endorse or audit it."
   },
   {
    "heading": "Gateway header contract: what must be forwarded and why",
    "body": "Claude Code sends headers that enable features and cost attribution. A gateway that strips any of these causes silent degradation — no error is returned, features just stop working or costs become unattributable.\n\n### Required headers by API format\n\n| API format | What the gateway must preserve |\n|---|---|\n| Anthropic Messages (`/v1/messages`) | Forward request headers: `anthropic-beta`, `anthropic-version` |\n| Bedrock InvokeModel (`/invoke`, `/invoke-with-response-stream`) | Preserve request body fields: `anthropic_beta`, `anthropic_version` |\n| Vertex rawPredict (`:rawPredict`, `:streamRawPredict`) | Forward request headers: `anthropic-beta`, `anthropic-version` |\n\n`anthropic-beta` carries the beta feature flags Claude Code needs (extended thinking, interleaved thinking, extended output, tool use schemas). If the gateway drops it, Claude Code sessions fall back to base API behavior, silently losing features that depend on betas. `anthropic-version` controls the API version Claude Code targets; stripping it applies the provider's default version which may not support all schema shapes Claude Code sends.\n\n### Attribution and cost-tracking headers\n\nClaude Code includes these headers on every API request:\n\n| Header | Purpose |\n|---|---|\n| `X-Claude-Code-Session-Id` | Unique identifier for the current Claude Code session. Aggregate all API calls from one session without parsing request bodies. |\n| `X-Claude-Code-Agent-Id` | Identifier of the subagent or teammate that issued the request. Use for per-agent cost attribution in parallel agent runs. Present only for in-process subagent or teammate requests. |\n| `X-Claude-Code-Parent-Agent-Id` | Identifier of the spawning agent. Use with `X-Claude-Code-Agent-Id` to trace nested agent cost attribution. Present only when the requesting agent was itself spawned by another agent. |\n\nBoth agent ID headers are ephemeral per-spawn identifiers, not persistent user or device IDs. Log them alongside request cost; do not use them as stable user keys.\n\n### The attribution block in the system prompt\n\nClaude Code prepends a short attribution block (client version + conversation fingerprint) to the system prompt. The Anthropic API strips this block before processing, so it does not break first-party prompt caching. If your gateway implements its own prompt cache keyed on the full request body, this block causes a cache miss on every new Claude Code client version:\n\n```bash\n# Disable the attribution block to stabilize gateway-side prompt cache keys\nexport CLAUDE_CODE_ATTRIBUTION_HEADER=0\n```\n\nThis is only relevant when the gateway — not the Anthropic API — is doing the prompt caching.\n\n### Suppressing experimental beta headers\n\nWhen pointing Claude Code at a gateway using the Anthropic Messages format (`ANTHROPIC_BASE_URL`) over a Bedrock or Vertex backend, Claude Code may send experimental beta headers that the provider's API does not accept:\n\n```bash\nexport CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS=1\n```\n\nThis strips Anthropic-specific `anthropic-beta` header values and beta tool-schema fields (`defer_loading`, `eager_input_streaming`). Standard fields are preserved. Use when the proxy rejects beta headers.\n\n### Detecting header stripping\n\nSymptoms of missing `anthropic-beta`:\n- Extended thinking stops appearing\n- Tool use schema validation errors\n- Features that work on direct API calls fail through the gateway\n\nSymptoms of missing attribution headers in gateway logs:\n- All requests show identical or absent session IDs\n- Per-agent cost attribution is unavailable\n- Cost is attributed to the gateway's shared service account, not individual teams\n\nTo diagnose: enable request logging on the gateway and compare the raw headers against a direct `curl` to `api.anthropic.com` for the same Claude Code operation. The difference is what the gateway is dropping."
   },
   {
    "heading": "Decision table: direct provider vs. gateway",
    "body": "| Criterion | Direct provider (Bedrock/Vertex) | LiteLLM gateway |\n|---|---|---|\n| Per-developer IAM roles exist | Yes — each dev authenticates directly with their own role | No benefit — gateway would still need to issue per-user tokens |\n| Multi-team, shared cloud account, need per-team cost | No — CloudTrail/Cost Explorer are per-account | Yes — gateway aggregates across accounts |\n| Want to switch providers without client config changes | No — each provider needs different env vars on every machine | Yes — change gateway backend, client config unchanged |\n| Compliance requirement for prompt logging | Difficult — provider logs token counts, not content | Yes — gateway can log full request/response |\n| Strict egress policy (all outbound to single internal host) | No — requests go directly to cloud provider IPs | Yes — gateway is the single egress point |\n| Network latency budget is tight | Better — one fewer network hop | Worse — gateway adds latency per hop (varies by deployment) |\n| Team size < 5 engineers | Probably not worth the ops overhead | — |\n| Team size > 20 engineers with separate billing or compliance | — | Worth the ops overhead |\n| Need fallback when Anthropic API is overloaded | No — provider-native only | Yes — LiteLLM load balancing + fallback chains |\n| Gateway model discovery wanted | Set `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1` (v2.1.129+) | Required for dynamic model picker from gateway `/v1/models` |\n\n### Deployment checklist for LiteLLM\n\n```\n[ ] LiteLLM version pinned in requirements.txt (avoid 1.82.7, 1.82.8)\n[ ] All three model tiers defined in litellm_config.yaml (opus, sonnet, haiku)\n[ ] pass_through_unrecognized_params: true set (preserves anthropic-beta body fields)\n[ ] drop_params: false set\n[ ] anthropic-beta and anthropic-version headers forwarded (for Anthropic Messages format)\n[ ] X-Claude-Code-Session-Id and X-Claude-Code-Agent-Id logged per request\n[ ] CLAUDE_CODE_ATTRIBUTION_HEADER=0 set if gateway implements prompt caching\n[ ] CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS=1 set if using Anthropic-format over Bedrock/Vertex\n[ ] CLAUDE_CODE_SKIP_*_AUTH=1 set to prevent double-signing on pass-through endpoints\n[ ] ANTHROPIC_AUTH_TOKEN or apiKeyHelper provisioned (not both set — env var wins)\n[ ] ANTHROPIC_DEFAULT_*_MODEL vars match gateway model_name values exactly\n[ ] ANTHROPIC_DEFAULT_HAIKU_MODEL set (prevents Opus/Sonnet running background tasks)\n```"
   },
   {
    "heading": "Putting it all together: a team Bedrock deployment",
    "body": "End-to-end configuration for a team using Claude Code through Bedrock with a LiteLLM gateway, model pins, credential refresh, and managed settings distribution.\n\n### 1. AWS account setup (once)\n\n```bash\n# Submit use case form for each model family\n# (management account — propagates to child accounts via Organizations)\naws bedrock put-use-case-for-model-access \\\n  --model-id anthropic.claude-opus-4-8 \\\n  --use-case-details '{\"useCase\": \"Software development assistant\"}'\n\n# Attach IAM policy to the developer role\naws iam put-role-policy \\\n  --role-name ClaudeCodeDeveloperRole \\\n  --policy-name BedrockClaudeAccess \\\n  --policy-document file://bedrock-policy.json\n```\n\n### 2. LiteLLM proxy config\n\n```yaml\n# litellm_config.yaml\nmodel_list:\n  - model_name: claude-opus-4-8\n    litellm_params:\n      model: bedrock/us.anthropic.claude-opus-4-8\n      aws_region_name: us-east-1\n  - model_name: claude-sonnet-4-6\n    litellm_params:\n      model: bedrock/us.anthropic.claude-sonnet-4-6\n      aws_region_name: us-east-1\n  - model_name: claude-haiku-4-5\n    litellm_params:\n      model: bedrock/us.anthropic.claude-haiku-4-5-20251001-v1:0\n      aws_region_name: us-east-1\n\ngeneral_settings:\n  master_key: \"sk-litellm-master\"  # rotate via vault\n  store_model_in_db: true\n  database_url: \"postgresql://...\"\n\nlitellm_settings:\n  pass_through_unrecognized_params: true  # preserves anthropic-beta body fields\n  drop_params: false\n```\n\n### 3. Project settings.json (committed to the team repo)\n\n```json\n{\n  \"$schema\": \"https://json.schemastore.org/claude-code-settings.json\",\n  \"model\": \"opus\",\n  \"env\": {\n    \"ANTHROPIC_BASE_URL\": \"https://litellm.internal.example.com:4000\",\n    \"ANTHROPIC_DEFAULT_OPUS_MODEL\": \"claude-opus-4-8\",\n    \"ANTHROPIC_DEFAULT_SONNET_MODEL\": \"claude-sonnet-4-6\",\n    \"ANTHROPIC_DEFAULT_HAIKU_MODEL\": \"claude-haiku-4-5\"\n  },\n  \"apiKeyHelper\": \"~/bin/get-litellm-key.sh\",\n  \"availableModels\": [\"opus\", \"sonnet\", \"haiku\"]\n}\n```\n\nDo not set `ANTHROPIC_AUTH_TOKEN` in the `env` block alongside `apiKeyHelper`. `ANTHROPIC_AUTH_TOKEN` has higher precedence than `apiKeyHelper` — if set (even to an empty string that your shell doesn't interpret as unset), it overrides the helper output.\n\n### 4. Key rotation script\n\n```bash\n#!/bin/bash\n# ~/bin/get-litellm-key.sh\n# Output: single token on stdout; sent as both X-Api-Key and Authorization: Bearer\nvault kv get -field=claude_code_key secret/litellm/engineering-team\n```\n\nSet the TTL in the `env` block of settings:\n\n```json\n{\n  \"env\": {\n    \"CLAUDE_CODE_API_KEY_HELPER_TTL_MS\": \"3600000\"\n  }\n}\n```\n\n### 5. Managed settings for fleet enforcement\n\nDeploy to all developer machines via Ansible or similar:\n\n```json\n// /etc/claude-code/managed-settings.d/10-provider.json\n{\n  \"requiredMinimumVersion\": \"2.1.172\",\n  \"availableModels\": [\"opus\", \"sonnet\", \"haiku\"],\n  \"enforceAvailableModels\": true,\n  \"env\": {\n    \"ANTHROPIC_DEFAULT_HAIKU_MODEL\": \"claude-haiku-4-5\"\n  }\n}\n```\n\nThis enforces the Haiku pin even if a developer's local settings try to override it, prevents access to models outside the approved list, and requires a minimum client version that reads AWS config files for region resolution (v2.1.172+).\n\n### 6. Verification\n\n```bash\n# Inside Claude Code — confirm provider, model tiers, and region\n/status\n\n# Validate managed settings deployment\nclaude doctor\n\n# After one day of team usage — confirm model distribution\nsqlite3 ~/projects/data/llm_observability.db \\\n  \"SELECT model, COUNT(*) as calls, SUM(input_tokens+output_tokens) as tokens \\\n   FROM llm_calls \\\n   WHERE project='myproject' AND date > date('now', '-1 day') \\\n   GROUP BY model ORDER BY tokens DESC;\"\n```\n\nExpected distribution: Haiku-class model for >30% of call count (background tasks), Opus/Sonnet for interactive turns. A 100%-Opus or 100%-Sonnet distribution means the Haiku pin is not reaching the process — check that the managed settings file is deployed and that `ANTHROPIC_DEFAULT_HAIKU_MODEL` is not being overridden by a local `env` block with higher precedence."
   }
  ],
  "preQuiz": [
   {
    "prompt": "Your team deploys Claude Code on AWS Bedrock without pinning any model IDs. A month later, Anthropic releases a new Sonnet version and your provider enables it. What actually happens to your team's sessions?",
    "options": [
     "Nothing changes — Claude Code always uses the latest model the provider has enabled.",
     "Claude Code continues using the same model version it used at install time, since the binary locks the version.",
     "The sonnet/opus/haiku aliases resolve to Claude Code's built-in per-provider default, which may lag behind the newest release and may not be enabled in your account — your team stays on the old default, not the new release.",
     "Claude Code automatically prompts every user to approve the new model before switching."
    ],
    "correct": 2,
    "sectionIndices": [
     0
    ],
    "explanation": "Unpinned aliases resolve to Claude Code's built-in per-provider defaults, which lag new releases and may not be enabled in your account. Pinning gives you control over upgrade timing."
   },
   {
    "prompt": "You set up Claude Code on Bedrock for your team and explicitly pin ANTHROPIC_DEFAULT_OPUS_MODEL and ANTHROPIC_DEFAULT_SONNET_MODEL, but leave ANTHROPIC_DEFAULT_HAIKU_MODEL unset. What is the most likely unintended consequence?",
    "options": [
     "Background tasks like session titling silently run at primary-model rates, inflating costs.",
     "Claude Code refuses to start without all three model IDs pinned.",
     "The Haiku alias falls back to the last Haiku version in the Bedrock catalog, which is cheaper than the primary.",
     "Haiku tasks queue behind Opus/Sonnet requests, causing latency for background work."
    ],
    "correct": 0,
    "sectionIndices": [
     0
    ],
    "explanation": "On all four providers, the small/fast model defaults to the PRIMARY model when HAIKU is unpinned. Session titles and other background tasks then run at primary rates — silent cost inflation."
   },
   {
    "prompt": "Your org uses both Claude Platform on AWS and AWS Bedrock. You have CLAUDE_CODE_USE_BEDROCK=1 and CLAUDE_CODE_USE_ANTHROPIC_AWS=1 set simultaneously in your environment. Where do requests actually go?",
    "options": [
     "Requests go to Claude Platform on AWS, because ANTHROPIC_AWS takes precedence.",
     "Claude Code errors and refuses to start with both flags set.",
     "Requests go to AWS Bedrock, because BEDROCK takes precedence over Claude Platform on AWS.",
     "Requests are load-balanced between Bedrock and Claude Platform on AWS."
    ],
    "correct": 2,
    "sectionIndices": [
     1
    ],
    "explanation": "CLAUDE_CODE_USE_BEDROCK takes precedence over CLAUDE_CODE_USE_ANTHROPIC_AWS. To use Claude Platform on AWS, you must unset BEDROCK (and FOUNDRY) — otherwise requests silently go to Bedrock."
   },
   {
    "prompt": "You're on Claude Code v2.1.165 (before v2.1.172). Your AWS profile's config file sets region = eu-west-1, but you have not set AWS_REGION or AWS_DEFAULT_REGION. Which region does Claude Code use?",
    "options": [
     "eu-west-1, read from the active profile's config file.",
     "us-east-1, because on v2.1.171 and earlier the AWS config file's region is NOT read.",
     "The region is auto-detected from the nearest AWS endpoint.",
     "Claude Code prompts the user to confirm the region before making any requests."
    ],
    "correct": 1,
    "sectionIndices": [
     2
    ],
    "explanation": "On v2.1.171 and earlier, the AWS config file's region field is not read. Without AWS_REGION or AWS_DEFAULT_REGION set, it falls back to us-east-1 regardless of the profile config."
   },
   {
    "prompt": "Your team runs a credential script that needs to provide cross-account AWS credentials to Claude Code every session. You configure awsCredentialExport with a script path. What constraint must your script satisfy that awsAuthRefresh does NOT have?",
    "options": [
     "The script must complete within 3 minutes, while awsAuthRefresh has no timeout.",
     "The script must return credentials as JSON with keys at the top level (or nested under Credentials), and it runs every session start and each credential reload — it cannot modify .aws files.",
     "The script must write credentials to ~/.aws/credentials and exit with code 0.",
     "The script must accept --profile as an argument matching the AWS_PROFILE env var."
    ],
    "correct": 1,
    "sectionIndices": [
     2
    ],
    "explanation": "awsCredentialExport runs every session start and each credential reload, captures output silently, and must emit credential JSON directly — it cannot modify .aws files. awsAuthRefresh only runs when creds are detected expired and IS allowed to modify .aws files (e.g., aws sso login)."
   },
   {
    "prompt": "You enable Bedrock Mantle (CLAUDE_CODE_USE_MANTLE=1) alongside CLAUDE_CODE_USE_BEDROCK=1. You pin a model with ID us.anthropic.claude-sonnet-4-6 and another with anthropic.claude-haiku-4-5. What routing behavior should you expect?",
    "options": [
     "All requests go to Mantle because MANTLE takes precedence over BEDROCK.",
     "The us.anthropic.claude-sonnet-4-6 ID routes to the Bedrock Invoke API; the anthropic.claude-haiku-4-5 ID routes to Mantle.",
     "Both IDs route to Mantle because both start with anthropic.",
     "Claude Code errors on startup because both flags cannot be active simultaneously."
    ],
    "correct": 1,
    "sectionIndices": [
     3
    ],
    "explanation": "Setting both USE_BEDROCK=1 and USE_MANTLE=1 causes Mantle-format IDs (prefix 'anthropic.' without version suffix) to route to Mantle, while all other IDs (like cross-region inference profile IDs with 'us.') route to the Bedrock Invoke API."
   },
   {
    "prompt": "You set up Claude Platform on AWS for your team. Authentication is configured using AWS credentials and SigV4. A team member also sets ANTHROPIC_AWS_API_KEY in their shell. What happens to their requests?",
    "options": [
     "Requests use SigV4, because SigV4 is the enterprise-grade auth method and takes precedence.",
     "Claude Code errors because you cannot have both auth methods configured.",
     "Requests use the workspace API key sent as x-api-key; AWS credentials are ignored.",
     "Requests use SigV4 for the first session, then switch to the API key after a credential refresh."
    ],
    "correct": 2,
    "sectionIndices": [
     4
    ],
    "explanation": "ANTHROPIC_AWS_API_KEY takes precedence over SigV4. When it is set, AWS credentials are ignored entirely for that user's requests."
   },
   {
    "prompt": "Your team subscribes to Claude via AWS Marketplace and gets provisioned access. A developer tries to use their existing Anthropic Console API key to authenticate with the Claude Platform on AWS endpoint. What happens?",
    "options": [
     "It works — Anthropic Console keys are universally valid across all enterprise providers.",
     "It works only if the developer adds their Console workspace ID as ANTHROPIC_AWS_WORKSPACE_ID.",
     "The Marketplace subscription provisions a NEW Anthropic org tied to the AWS account; Console org credentials do not transfer — they must use the AWS-linked org's workspace ID and keys.",
     "The request succeeds but is billed to the Console org, not the AWS account."
    ],
    "correct": 2,
    "sectionIndices": [
     4
    ],
    "explanation": "AWS Marketplace subscriptions provision a new Anthropic org tied to the AWS account, separate from any pre-existing Console org. Credentials and keys do not transfer between orgs."
   },
   {
    "prompt": "You run Claude Code on Google Vertex AI with CLOUD_ML_REGION=global. You need claude-haiku-4-5 for background tasks but it lacks a global endpoint. What is the correct approach?",
    "options": [
     "Switch CLOUD_ML_REGION to a specific region like us-east5 so all models use that region.",
     "Set VERTEX_REGION_CLAUDE_HAIKU_4_5=us-east5 to override the region for Haiku specifically, leaving other models on global.",
     "Use ANTHROPIC_VERTEX_BASE_URL to point to a different project that has Haiku at the global endpoint.",
     "Enable ENABLE_TOOL_SEARCH=true to allow Claude Code to discover the Haiku endpoint at runtime."
    ],
    "correct": 1,
    "sectionIndices": [
     5
    ],
    "explanation": "The correct approach is per-model region overrides (VERTEX_REGION_CLAUDE_HAIKU_4_5=us-east5) rather than switching everything away from global. This keeps other models on the preferred global endpoint."
   },
   {
    "prompt": "A Vertex AI admin sets GOOGLE_CLOUD_PROJECT=project-A in their environment and also has ANTHROPIC_VERTEX_PROJECT_ID=project-B in settings.json. Which project does Claude Code use, and why?",
    "options": [
     "project-B, because settings.json values always override shell environment variables.",
     "project-A, because GOOGLE_CLOUD_PROJECT overrides ANTHROPIC_VERTEX_PROJECT_ID — the latter is the lowest-precedence source.",
     "Claude Code merges both and tries project-A first, then falls back to project-B.",
     "Whichever project was set most recently in the shell session wins."
    ],
    "correct": 1,
    "sectionIndices": [
     5
    ],
    "explanation": "GCLOUD_PROJECT, GOOGLE_CLOUD_PROJECT, and the file at GOOGLE_APPLICATION_CREDENTIALS all override ANTHROPIC_VERTEX_PROJECT_ID, which is the lowest-precedence source. A stale env var silently targets the wrong project."
   },
   {
    "prompt": "You're enabling MCP tool search (ENABLE_TOOL_SEARCH=true) for a team using Google Vertex AI. Some team members use claude-sonnet-4-5 and others use an older claude-sonnet-4-0 deployment. What is the risk?",
    "options": [
     "MCP tool search costs more on older models, so the team will see higher bills.",
     "Older models reject the beta header sent with tool search enabled, causing requests to FAIL — the feature only works on Sonnet 4.5+ / Opus 4.5+.",
     "MCP tools load lazily on older models, so tool search works but is slower.",
     "ENABLE_TOOL_SEARCH=true is ignored on models older than Sonnet 4.5, falling back to loading all tools upfront."
    ],
    "correct": 1,
    "sectionIndices": [
     5
    ],
    "explanation": "ENABLE_TOOL_SEARCH=true only works on Sonnet 4.5+ / Opus 4.5+. Older models reject the beta header and requests FAIL — they do not silently fall back."
   },
   {
    "prompt": "You configure Microsoft Foundry as your provider. After setting all env vars correctly, you launch Claude Code and get a runtime request failure — no early config error, no model-unavailability warning at startup. Which Foundry behavior explains this?",
    "options": [
     "Foundry has a 30-second startup health check that occasionally times out, producing a runtime error.",
     "Foundry has no interactive wizard and no startup model check — a missing or unavailable default surfaces as a runtime request failure, not an early config error.",
     "Foundry requires a manual /model command to select a model before the first request can succeed.",
     "The Azure SDK DefaultAzureCredential takes longer to resolve than the request timeout, causing the first request to fail."
    ],
    "correct": 1,
    "sectionIndices": [
     6
    ],
    "explanation": "Unlike Bedrock and Vertex, Foundry has no interactive wizard and no startup model check. A missing or unavailable model default surfaces only as a runtime request failure."
   },
   {
    "prompt": "Your enterprise Foundry deployment uses Microsoft Entra ID for authentication. A new developer sets ANTHROPIC_FOUNDRY_API_KEY in their shell from a static key they found in a shared doc, despite your Entra setup. What happens?",
    "options": [
     "Entra ID takes precedence over a static key, so requests still use Entra credentials.",
     "Claude Code uses the static API key and bypasses Entra ID entirely, since ANTHROPIC_FOUNDRY_API_KEY takes precedence when set.",
     "The request fails because both auth methods are configured and Foundry requires exactly one.",
     "Claude Code uses the API key for the first session and Entra ID for subsequent sessions after a credential refresh."
    ],
    "correct": 1,
    "sectionIndices": [
     6
    ],
    "explanation": "ANTHROPIC_FOUNDRY_API_KEY takes precedence when set. The Azure SDK DefaultAzureCredential (Entra ID) is only used when no API key is set. A developer with a static key in their env bypasses Entra entirely."
   }
  ],
  "tasks": [
   {
    "id": "stage-11-task-pin-models",
    "afterSectionIdx": 0,
    "title": "Pin all model IDs in your team settings and verify with /status",
    "instructions": "Open (or create) your project-level Claude Code settings file:\n\n```bash\nmkdir -p .claude\ncat ~/.claude/settings.json  # check your user-level settings first\n```\n\nAdd or merge an `env` block that pins all three model aliases. For Bedrock, use cross-region inference profile IDs; adjust the values for your actual provider:\n\n```json\n{\n  \"env\": {\n    \"ANTHROPIC_DEFAULT_OPUS_MODEL\": \"us.anthropic.claude-opus-4-8\",\n    \"ANTHROPIC_DEFAULT_SONNET_MODEL\": \"us.anthropic.claude-sonnet-4-6\",\n    \"ANTHROPIC_DEFAULT_HAIKU_MODEL\": \"us.anthropic.claude-haiku-4-5-20251001-v1:0\"\n  }\n}\n```\n\nSave to `.claude/settings.json` in your project directory (project-scoped) or `~/.claude/settings.json` (user-scoped for all projects).\n\nThen launch Claude Code and run:\n```\n/status\n```\n\nConfirm the output shows your pinned model IDs and the correct provider.",
    "doneWhen": "The /status command shows your three pinned model IDs (not the alias defaults) and lists the correct provider and region."
   },
   {
    "id": "stage-11-task-bedrock-sso-auth",
    "afterSectionIdx": 2,
    "title": "Configure SSO credential refresh for AWS Bedrock and test mid-session expiry recovery",
    "instructions": "1. Ensure you have an AWS SSO profile configured:\n```bash\naws configure sso --profile my-claude-profile\n# follow the prompts, then:\naws sso login --profile my-claude-profile\nexport AWS_PROFILE=my-claude-profile\n```\n\n2. Add `awsAuthRefresh` to your `.claude/settings.json`:\n```json\n{\n  \"env\": {\n    \"CLAUDE_CODE_USE_BEDROCK\": \"1\",\n    \"AWS_PROFILE\": \"my-claude-profile\",\n    \"AWS_REGION\": \"us-east-1\"\n  },\n  \"awsAuthRefresh\": \"aws sso login --profile my-claude-profile\"\n}\n```\n\n3. Launch Claude Code and run `/status` to confirm Bedrock is the active provider and the region is resolved from AWS_REGION (not the config file).\n\n4. To verify the refresh hook is wired up, run `/login` inside Claude Code — you should see a \"refresh credentials\" option listed alongside any other login options.",
    "doneWhen": "Running /login inside Claude Code shows a 'refresh credentials' option, and /status confirms Bedrock is active with the correct region."
   },
   {
    "id": "stage-11-task-vertex-region-override",
    "afterSectionIdx": 5,
    "title": "Set up Vertex AI with global region and a per-model region override for Haiku",
    "instructions": "1. Authenticate to GCP if you haven't already:\n```bash\ngcloud auth application-default login\n```\n\n2. Add Vertex AI configuration to `~/.claude/settings.json`:\n```json\n{\n  \"env\": {\n    \"CLAUDE_CODE_USE_VERTEX\": \"1\",\n    \"CLOUD_ML_REGION\": \"global\",\n    \"ANTHROPIC_VERTEX_PROJECT_ID\": \"your-gcp-project-id\",\n    \"ANTHROPIC_DEFAULT_SONNET_MODEL\": \"claude-sonnet-4-6\",\n    \"ANTHROPIC_DEFAULT_HAIKU_MODEL\": \"claude-haiku-4-5@20251001\",\n    \"VERTEX_REGION_CLAUDE_HAIKU_4_5\": \"us-east5\"\n  }\n}\n```\nReplace `your-gcp-project-id` with your actual GCP project ID.\n\n3. Launch Claude Code and run `/status`. Confirm it shows Vertex as the provider and `global` as the primary region.\n\n4. To confirm the GOOGLE_CLOUD_PROJECT precedence rule, temporarily run:\n```bash\nexport GOOGLE_CLOUD_PROJECT=some-other-project\nclaude\n```\nThen run `/status` inside — you should see it targeting `some-other-project`, not the value from settings.json. Exit and unset the variable:\n```bash\nunset GOOGLE_CLOUD_PROJECT\n```",
    "doneWhen": "Running /status with GOOGLE_CLOUD_PROJECT unset shows your settings.json project ID; running it with GOOGLE_CLOUD_PROJECT set shows the env var value instead, confirming precedence."
   }
  ],
  "visualizations": [
   {
    "id": "stage-11-v",
    "kind": "comparison-table",
    "title": "Enterprise providers & gateways",
    "textualSummary": "Key concepts of Enterprise providers & gateways: Model pinning, Small/fast model cost trap, Provider enable & routing precedence.",
    "columns": [
     "Concept",
     "In this stage"
    ],
    "rows": [
     {
      "label": "Model pinning",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "Small/fast model cost trap",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "Provider enable & routing precedence",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     },
     {
      "label": "Centralized config distribution",
      "cells": {
       "Concept": {
        "value": "yes"
       },
       "In this stage": {
        "value": "yes"
       }
      }
     }
    ]
   }
  ],
  "confusions": [
   {
    "misconception": "Skipping Enterprise providers & gateways.",
    "correction": "Each stage is load-bearing for the setup."
   },
   {
    "misconception": "These concepts are interchangeable.",
    "correction": "Each has a distinct role; see the definitions."
   }
  ],
  "quiz": [
   {
    "id": "stage-11-q1",
    "type": "multiple-choice",
    "prompt": "Your team just enabled Claude Code on AWS Bedrock and didn't pin any model IDs. After a month, Anthropic releases a new Sonnet version and Claude Code's built-in default is updated. What happens to your team's sessions without any action on your part?",
    "options": [
     "Nothing changes — unpinned aliases stay on the model version active at first use until you explicitly upgrade.",
     "The `sonnet` alias silently resolves to the new built-in per-provider default, potentially switching the whole team to a model not yet enabled in your account.",
     "Claude Code refuses to start and prompts you to pin a model ID before continuing.",
     "Claude Code sends a team notification via /status that the default model has changed."
    ],
    "correct": 1,
    "explanation": "The content states: 'Unpinned, the `sonnet`/`opus`/`haiku` aliases resolve to Claude Code's built-in per-provider default, which lags the newest release and may not be enabled in your account. Pinning also gives you control over upgrade timing so a new model release doesn't move the whole team at once.' So an unpinned alias will silently shift when the built-in default updates — the whole team moves without warning. Option A is wrong: aliases are not frozen at first use; they always resolve to the current built-in default. Option C is wrong: there is no refusal-to-start behavior described. Option D is wrong: /status shows current state but does not send proactive notifications about upstream default changes."
   },
   {
    "id": "stage-11-q2",
    "type": "multiple-choice",
    "prompt": "A teammate sets up Claude Code on Bedrock and doesn't pin `ANTHROPIC_DEFAULT_HAIKU_MODEL`. They notice their AWS bill is far higher than expected — session title generation and background tasks are costing as much as primary completions. What is the most likely cause?",
    "options": [
     "The Bedrock service tier defaulted to `priority` mode, which has lower latency but higher per-token cost.",
     "Background tasks like session titles default to the PRIMARY model when `ANTHROPIC_DEFAULT_HAIKU_MODEL` is not pinned, incurring primary-model rates.",
     "Cross-region inference was enabled automatically, adding surcharges on every Haiku call.",
     "The IAM role is missing `bedrock:InvokeModelWithResponseStream`, causing expensive retry storms."
    ],
    "correct": 1,
    "explanation": "The content explicitly warns: 'On all four providers the small/fast (background-task) model defaults to the PRIMARY model — silent cost inflation, since session titles etc. run at primary rates.' Not pinning Haiku means every cheap background call runs at the primary (Sonnet/Opus) rate. Option A is wrong: the default service tier is `default`, not `priority`. Option C is wrong: cross-region inference prefix usage is about routing, not automatic surcharges from unpinned Haiku. Option D is wrong: missing streaming IAM permission would cause errors/failures, not silent cost inflation."
   },
   {
    "id": "stage-11-q3",
    "type": "multiple-choice",
    "prompt": "Your team uses Claude Platform on AWS. A developer exports `ANTHROPIC_AWS_API_KEY` in their shell and also has valid AWS SigV4 credentials configured. Which auth method does Claude Code use, and what happens to the other?",
    "options": [
     "SigV4 takes precedence because it is more secure; the API key is ignored.",
     "Claude Code uses both simultaneously for redundancy — SigV4 for the request signing and the API key for workspace identification.",
     "`ANTHROPIC_AWS_API_KEY` takes precedence over SigV4; when set, AWS credentials are ignored.",
     "Claude Code raises an authentication conflict error and requires you to unset one before proceeding."
    ],
    "correct": 2,
    "explanation": "The content states: '`ANTHROPIC_AWS_API_KEY` takes precedence over SigV4 — when set, AWS creds are ignored.' The API key wins, not SigV4. Option A is wrong: it reverses the documented precedence. Option B is wrong: the two methods are mutually exclusive, not complementary. Option D is wrong: there is no documented conflict error; the resolution is silent and deterministic."
   },
   {
    "id": "stage-11-q4",
    "type": "multiple-choice",
    "prompt": "A team is running Claude Code v2.1.170 on Bedrock. Their AWS shared config file sets `region = eu-west-1` in the `[default]` profile. They have not set `AWS_REGION` or `AWS_DEFAULT_REGION`. Which region does Claude Code use?",
    "options": [
     "`eu-west-1`, read from the AWS shared config file.",
     "`us-east-1`, because on v2.1.171 and earlier the AWS config file's region is NOT read.",
     "`us-west-2`, the hardcoded Bedrock default when no region env var is present.",
     "Claude Code prompts the user to specify the region at startup since it cannot determine it automatically."
    ],
    "correct": 1,
    "explanation": "The content states: 'On v2.1.171 and earlier the AWS config file's `region` is NOT read — set `AWS_REGION` explicitly or it falls back to `us-east-1`.' v2.1.170 is earlier than v2.1.171, so the config file is ignored and the fallback is `us-east-1`. Option A is wrong: the config file region is only read in v2.1.172+. Option C is wrong: the documented fallback is `us-east-1`, not `us-west-2`. Option D is wrong: there is no interactive region prompt described."
   },
   {
    "id": "stage-11-q5",
    "type": "multiple-choice",
    "prompt": "A team migrates from an existing Anthropic Console organization to Claude Platform on AWS via an AWS Marketplace subscription. They try to use their existing Console workspace API keys with the new setup. The keys are rejected on every request. Why?",
    "options": [
     "AWS Marketplace subscriptions require IAM-based auth; API keys are not supported on this provider.",
     "AWS Marketplace subscription provisions a NEW Anthropic org tied to the AWS account, separate from any pre-existing Console org; credentials and keys do not transfer.",
     "The keys must be re-issued by rotating them through the AWS Secrets Manager before they are valid on the AWS-linked org.",
     "The `ANTHROPIC_AWS_WORKSPACE_ID` env var must be set to the old Console workspace ID to bridge the two orgs."
    ],
    "correct": 1,
    "explanation": "The content states: 'AWS Marketplace subscription provisions a NEW Anthropic org tied to the AWS account, separate from any pre-existing Console org; credentials/keys do not transfer — use the AWS-linked org's workspace ID + keys.' The two orgs are completely separate. Option A is wrong: API keys are supported on Claude Platform on AWS via `ANTHROPIC_AWS_API_KEY`. Option C is wrong: there is no key-rotation bridge through AWS Secrets Manager described. Option D is wrong: using the old workspace ID will fail because the orgs are separate; a new workspace ID from the AWS-linked org is needed."
   },
   {
    "id": "stage-11-q6",
    "type": "multiple-choice",
    "prompt": "You're setting up MCP tool search on Vertex AI. Your team pins the model to `claude-sonnet-4-0` (an older Sonnet release) and sets `ENABLE_TOOL_SEARCH=true`. When the agent makes a request that triggers tool search, what happens?",
    "options": [
     "Tool search works normally — the beta header is ignored on older models and the request succeeds without it.",
     "The request fails because MCP tool search on Vertex is only supported on Sonnet 4.5+ and Opus 4.5+; older models reject the beta header.",
     "Tool search silently falls back to loading all tool definitions upfront, the same behavior as when `ENABLE_TOOL_SEARCH` is not set.",
     "Vertex AI upgrades the model alias to the nearest supported version automatically."
    ],
    "correct": 1,
    "explanation": "The content states: 'Opt in with `ENABLE_TOOL_SEARCH=true` — only on Sonnet 4.5+ / Opus 4.5+ (older models reject the beta header and requests FAIL).' Requests on older models do not silently degrade; they fail. Option A is wrong: the beta header is not ignored; it actively causes failures on unsupported models. Option C is wrong: the content says requests FAIL, not that they silently fall back. Option D is wrong: Vertex AI does not perform automatic model alias upgrades."
   },
   {
    "id": "stage-11-q7",
    "type": "multiple-choice",
    "prompt": "A Vertex AI deployment sets both `ANTHROPIC_VERTEX_PROJECT_ID=my-project` and `GOOGLE_CLOUD_PROJECT=other-project` in the environment. Which project receives the API requests?",
    "options": [
     "`my-project`, because `ANTHROPIC_VERTEX_PROJECT_ID` is the Claude Code-specific variable and takes precedence over generic GCP variables.",
     "`other-project`, because `GCLOUD_PROJECT`, `GOOGLE_CLOUD_PROJECT`, and the file at `GOOGLE_APPLICATION_CREDENTIALS` all override `ANTHROPIC_VERTEX_PROJECT_ID`, which is the lowest-precedence source.",
     "Claude Code merges the two project IDs and routes to whichever project has available quota first.",
     "Whichever env var was exported last in the shell wins."
    ],
    "correct": 1,
    "explanation": "The content states: '`GCLOUD_PROJECT`, `GOOGLE_CLOUD_PROJECT`, and the file at `GOOGLE_APPLICATION_CREDENTIALS` all override `ANTHROPIC_VERTEX_PROJECT_ID` (the lowest-precedence source). A stale env/credential file silently targets the wrong project.' `GOOGLE_CLOUD_PROJECT` overrides `ANTHROPIC_VERTEX_PROJECT_ID`. Option A is wrong: despite being Claude Code-specific, `ANTHROPIC_VERTEX_PROJECT_ID` is the lowest-precedence source. Option C is wrong: there is no quota-based merging behavior. Option D is wrong: shell export order is irrelevant; the documented precedence hierarchy determines the winner."
   },
   {
    "id": "stage-11-q8",
    "type": "multiple-choice",
    "prompt": "Your team uses Claude Platform on AWS (`CLAUDE_CODE_USE_ANTHROPIC_AWS=1`) with an SSO profile. Mid-session, the SSO token expires. The team has configured `awsAuthRefresh` with a login command. What does `awsAuthRefresh` do, and what does it NOT do?",
    "options": [
     "`awsAuthRefresh` runs at every session start to pre-warm credentials; it does not handle mid-session expiry.",
     "`awsAuthRefresh` re-runs the login command when mid-session SSO expiry occurs, but it cannot modify `.aws` files and must return credentials directly in JSON format.",
     "`awsAuthRefresh` re-runs the login command on mid-session SSO expiry; it can modify `.aws` files and adds a 'refresh credentials' option under `/login`.",
     "`awsAuthRefresh` and `awsCredentialExport` are interchangeable; either can be used for mid-session refresh."
    ],
    "correct": 2,
    "explanation": "The content says: '`awsAuthRefresh`: Only when creds detected expired…then retries. SSO login commands that modify `.aws`; adds a \"refresh credentials\" option under `/login`.' This is distinct from `awsCredentialExport` which runs at every session start and cannot modify `.aws`. On Claude Platform on AWS, `awsAuthRefresh` re-runs the login command on mid-session SSO expiry. Option A is wrong: that describes `awsCredentialExport`'s every-session-start behavior, not `awsAuthRefresh`. Option B is wrong: it conflates `awsAuthRefresh` constraints with `awsCredentialExport` constraints — `awsCredentialExport` is the one that cannot modify `.aws` and must return JSON directly. Option D is wrong: they are explicitly separate settings with different trigger conditions and capabilities."
   },
   {
    "id": "stage-11-q9",
    "type": "multiple-choice",
    "prompt": "A team sets `CLAUDE_CODE_USE_BEDROCK=1` and also `CLAUDE_CODE_USE_ANTHROPIC_AWS=1` in their environment, intending to use Claude Platform on AWS. Where do their requests actually go?",
    "options": [
     "Claude Platform on AWS, because `CLAUDE_CODE_USE_ANTHROPIC_AWS=1` explicitly names the intended destination.",
     "AWS Bedrock, because `CLAUDE_CODE_USE_BEDROCK` takes precedence over Claude Platform on AWS; to use Platform-on-AWS, `CLAUDE_CODE_USE_BEDROCK` must be unset.",
     "Requests are load-balanced between Bedrock and Claude Platform on AWS based on model availability.",
     "Claude Code raises a configuration conflict error since both providers target AWS infrastructure."
    ],
    "correct": 1,
    "explanation": "The content states: '`CLAUDE_CODE_USE_BEDROCK` and `CLAUDE_CODE_USE_FOUNDRY` take precedence over Claude Platform on AWS. If Platform-on-AWS should be used, unset both — otherwise requests silently go to the wrong provider.' Having `CLAUDE_CODE_USE_BEDROCK=1` set will silently route to Bedrock even if `CLAUDE_CODE_USE_ANTHROPIC_AWS=1` is also set. Option A is wrong: explicit intent in the variable name does not override the documented routing precedence. Option C is wrong: there is no load-balancing behavior. Option D is wrong: the conflict is resolved silently by precedence, not by an error."
   },
   {
    "id": "stage-11-q10",
    "type": "multi-select",
    "prompt": "Your team is rolling out Claude Code on Bedrock to 50 developers. Select ALL practices that the documentation explicitly recommends for this enterprise rollout.",
    "options": [
     "Use the manual env-var setup path for CI/scripted enterprise rollouts, not the interactive wizard.",
     "Pin `ANTHROPIC_DEFAULT_HAIKU_MODEL` to a real Haiku model deployment to prevent background tasks from running at primary-model rates.",
     "Distribute workspace API keys and `AWS_PROFILE` values via `settings.json` env blocks rather than per-developer shell exports.",
     "Run `/status` first to confirm resolved provider, region, workspace ID, and auth settings before making any changes.",
     "Run the interactive `/setup-bedrock` wizard for each developer's machine to ensure per-user settings are correctly scoped."
    ],
    "correct": [
     0,
     1,
     2,
     3
    ],
    "explanation": "Options A, B, C, and D are all explicitly recommended. A: 'Use the manual env-var setup path for CI/scripted enterprise rollouts, not the interactive wizard.' B: 'Pin `ANTHROPIC_DEFAULT_HAIKU_MODEL` to a real Haiku model/deployment.' C: 'Distribute config via the `env` block of `settings.json`…not per-developer shell exports. Treat workspace API keys and `AWS_PROFILE` as production credentials kept in settings files.' D: 'Run `/status` first — confirms resolved provider, region…before you change anything.' Option E is wrong: the docs explicitly say to use the manual env-var path for enterprise rollouts, not the interactive wizard."
   },
   {
    "id": "stage-11-q11",
    "type": "multi-select",
    "prompt": "Which of the following statements about Microsoft Foundry are TRUE according to the documentation? Select all that apply.",
    "options": [
     "There is no interactive wizard for Foundry; env vars are the only config path.",
     "Missing or unavailable models surface as an early startup config error, similar to Bedrock's startup model check.",
     "Microsoft Entra ID via `DefaultAzureCredential` is the preferred auth method for enterprise rollouts over static API keys.",
     "Model alias env vars must match the Azure deployment names created in the Foundry portal.",
     "Foundry supports the same `awsAuthRefresh` hook for handling mid-session credential expiry as Bedrock."
    ],
    "correct": [
     0,
     2,
     3
    ],
    "explanation": "A is true: 'No interactive wizard (unlike Bedrock/Vertex) — env vars are the only config path.' B is false: 'No startup model check — a missing/unavailable default surfaces as a runtime request failure, not an early config error.' C is true: 'Prefer Entra ID for enterprise rollouts over static keys.' D is true: 'Model alias env vars must match the Azure deployment names created in the Foundry portal.' E is false: `awsAuthRefresh` is documented for AWS providers (Bedrock and Claude Platform on AWS); there is no mention of it for Foundry."
   },
   {
    "id": "stage-11-q12",
    "type": "multiple-choice",
    "prompt": "A team wants to enable Bedrock Guardrails with cross-region inference profiles. They set the guardrail identifier and version via `ANTHROPIC_CUSTOM_HEADERS`. After deploying, requests are routed across multiple AWS regions but guardrail evaluation is inconsistently applied. What step did they most likely miss?",
    "options": [
     "They need to set `ANTHROPIC_BEDROCK_SERVICE_TIER=priority` to ensure guardrails are applied consistently at all latency levels.",
     "They need to enable Cross-Region inference on the Guardrail itself when using cross-region inference profiles.",
     "They should switch from `ANTHROPIC_CUSTOM_HEADERS` to a dedicated guardrail env var, since custom headers bypass guardrail enforcement on cross-region calls.",
     "They must grant the IAM action `bedrock:ApplyGuardrail` separately for each region."
    ],
    "correct": 1,
    "explanation": "The content states: 'Enable Cross-Region inference on the Guardrail when using cross-region inference profiles.' Guardrails are set via `ANTHROPIC_CUSTOM_HEADERS` correctly, but cross-region inference profiles require the Guardrail to have Cross-Region inference enabled at the Guardrail configuration level. Option A is wrong: service tier affects cost/latency, not guardrail consistency. Option C is wrong: `ANTHROPIC_CUSTOM_HEADERS` is the documented mechanism for setting guardrails; there is no separate dedicated env var mentioned, and custom headers do not bypass guardrails. Option D is wrong: there is no `bedrock:ApplyGuardrail` per-region grant mentioned; the documented IAM actions are InvokeModel, ListInferenceProfiles, etc."
   },
   {
    "id": "stage-11-q13",
    "type": "multiple-choice",
    "prompt": "A teammate configures `awsCredentialExport` with a script that calls a secrets manager to return cross-account credentials. After the first session, they modify the shared AWS config file to add a new default profile. The next day they notice `awsCredentialExport` is still called even though they have valid, non-expired credentials. They expected it would be skipped. Why is it still running?",
    "options": [
     "The teammate introduced a config file syntax error that invalidates the stored credentials, causing the export hook to be re-triggered.",
     "`awsCredentialExport` runs at every session start and on each credential reload, even when current credentials are valid — it is not skipped for non-expired credentials.",
     "`awsCredentialExport` only runs when `AWS_SHARED_CREDENTIALS_FILE` is set to a non-default path, and that env var was inadvertently set.",
     "`awsCredentialExport` replaces `awsAuthRefresh`, so it now handles both expiry refreshes and session starts, causing the extra runs."
    ],
    "correct": 1,
    "explanation": "The content states: '`awsCredentialExport`: Every session start + each credential reload (even if valid); output captured silently.' It always runs regardless of whether credentials are valid or expired. This is a key behavioral difference from `awsAuthRefresh`, which only runs when credentials are detected as expired. Option A is wrong: config file errors do not trigger the export hook. Option C is wrong: `awsCredentialExport` trigger is not conditioned on `AWS_SHARED_CREDENTIALS_FILE` being set. Option D is wrong: the two hooks are separate settings with distinct trigger conditions, not substitutes for each other."
   },
   {
    "id": "stage-11-q14",
    "type": "multiple-choice",
    "prompt": "A team wants to enable 1M context on Vertex AI for Opus. They have manually pinned a model ID and set `CLOUD_ML_REGION=global`. What additional step is required to enable 1M context?",
    "options": [
     "Set `ANTHROPIC_VERTEX_LARGE_CONTEXT=1` to opt the session into the extended context window.",
     "Switch `CLOUD_ML_REGION` from `global` to a specific region that supports 1M context, since the global endpoint does not support extended windows.",
     "Append `[1m]` to the manually pinned model ID, since 1M context is auto-enabled only for 1M variants and must be requested explicitly for manually pinned IDs.",
     "Request a quota increase from Google Cloud support, as 1M context requires provisioned capacity that is not included in the default Vertex AI plan."
    ],
    "correct": 2,
    "explanation": "The content states: '1M context: Opus 4.6+ / Sonnet 4.6; auto-enabled for 1M variants, else append `[1m]` to a manually pinned ID.' When the model is manually pinned (not an auto-selected 1M variant), the `[1m]` suffix must be appended to the model ID. Option A is wrong: there is no `ANTHROPIC_VERTEX_LARGE_CONTEXT` env var documented. Option B is wrong: the `global` region is actually preferred and the documentation does not say 1M context requires a non-global region. Option D is wrong: while higher rate limits may require Google Cloud support, the mechanism for enabling 1M context itself is the model ID suffix, not a quota form."
   }
  ],
  "masteryCheckpoint": "You can explain the concepts of Enterprise providers & gateways."
 }
];
export function lessonById(id: string): Lesson | undefined { return LESSONS.find((l) => l.id === id); }
