{
 "stage-0": [
  {
   "heading": "Why the names blur — and why that's good news",
   "body": "You've been asked to stand up \"Claude\" for your team, and the vocabulary already fights you. There's a **Desktop app** with a tab called *Code*, a **terminal command** called `claude`, a cloud thing called **Cowork**, and a separate vendor's tool called **Codex**. People in your channel use these words interchangeably and inconsistently, and the docs don't always disambiguate them either.\n\nHere is the load-bearing fact that makes the rest of this course tractable:\n\n> **Claude Code's terminal CLI and the Desktop app's *Code* tab are the same engine reading the same config** — the same `CLAUDE.md`, MCP servers, hooks, skills, and settings. (Source: https://code.claude.com/docs/en/desktop-quickstart)\n\nThe official quickstart puts it plainly: *\"Desktop runs the same engine as the CLI with a graphical interface. You can run both simultaneously on the same project, and they share configuration (CLAUDE.md files, MCP servers, hooks, skills, and settings).\"*\n\nThat is the whole insight of stage 0. You are not configuring four products. You are configuring **one product** — Claude Code — and the front-ends are interchangeable windows onto it. When your teammate on macOS uses the GUI and your teammate on Linux uses the terminal, they drive the same engine off the same files in the repo. That is what makes a *team* setup possible: you write the config once, commit it, and every face of the tool obeys it.\n\nThe rest of this stage names each face precisely, shows you the config files they all share (with real, copy-able formats and exact paths), and draws the one genuine boundary that *isn't* just a UI difference: Codex, a different vendor's agent that does not read Claude's config and is a deliberate complement, not a substitute."
  },
  {
   "heading": "What \"Claude Code\" actually is",
   "body": "**Claude Code is Anthropic's agentic coding tool**: a long-running agent that reads your repository, plans, edits files, runs commands, and iterates — driven either from a terminal or a GUI. \"Agentic\" is the operative word. It is not a chat box that emits code for you to paste. It holds a session, takes tools (read file, write file, run bash, call an MCP server), and works in a loop until the task is done or it needs you.\n\n### One engine, multiple front-ends\n\nMentally, separate two layers:\n\n- **The engine** — the agent loop, the tool-use machinery, the model calls, and the *config resolver* that loads your instructions, permissions, MCP servers, hooks, and skills at session start.\n- **The front-end** — the surface you sit in front of: a terminal TUI, a Desktop GUI pane, an IDE side panel, or a web/cloud session.\n\nThe engine is the same across front-ends. The front-end only changes ergonomics. The terminal gives you keyboard-driven speed and easy scripting; the Desktop GUI adds a sidebar of parallel sessions (each in its own git worktree), drag-and-drop panes (chat, diff, terminal, file editor, preview), visual diff review with line-level comments, live app preview, and GitHub PR monitoring with auto-merge. Neither changes *what the agent can do* or *which config it reads*.\n\n### Why this matters for a team lead\n\nIf the engine and config are shared, then your job — \"set up Claude for the team\" — collapses to **authoring shared config files and committing them**. You do not onboard people per-surface. A `CLAUDE.md` you commit today is read identically whether a teammate runs `claude` in a tmux pane or clicks the Code tab in the Desktop app. Get the files right once and the surface choice becomes a personal-preference detail, not an infrastructure decision.\n\nOne practical note from the quickstart: the Desktop app *bundles* Claude Code — you don't need to install Node.js or the CLI separately to use the Code tab. If you also want the `claude` terminal command, you install the CLI separately. Same engine either way."
  },
  {
   "heading": "The five faces, mapped",
   "body": "Here are the names you'll hear, what each actually *is*, and whether it shares Claude Code's config.\n\n| Name | What it is | Engine | Reads shared Claude config? | Runs where |\n|------|-----------|--------|------------------------------|-----------|\n| **Claude Code CLI** (`claude`) | Terminal TUI; the canonical surface | Claude Code | Yes | Local (your machine) |\n| **Desktop → Code tab** | GUI front-end on the *same* engine | Claude Code | Yes | Local, Remote (Anthropic cloud), or SSH |\n| **Desktop → Cowork tab** | Autonomous background agent in a cloud VM | Claude Code (remote) | Yes (same infra as Claude Code on the web) | Cloud VM |\n| **Desktop → Chat tab** | claude.ai-style chat, **no file access** | Not the coding agent | No | n/a |\n| **OpenAI Codex** | A *different vendor's* agentic coding tool | Codex (OpenAI) | **No** — reads `AGENTS.md` + `~/.codex/config.toml` | Local CLI, IDE ext, Codex Cloud |\n\nThe Desktop app, per the quickstart, has exactly three tabs: *\"**Chat**: General conversation with no file access, similar to claude.ai. **Cowork**: An autonomous background agent that works on tasks in a cloud VM with its own environment. **Code**: An interactive coding assistant with direct access to your local files.\"* (Source: https://code.claude.com/docs/en/desktop-quickstart)\n\n### Reading the table\n\nThe first three rows are *the same tool*. Within the Code tab itself you pick where it runs — **Local** (your files directly), **Remote** (Anthropic's cloud infrastructure, sessions continue even if you close the app, same infra as Claude Code on the web), or **SSH** (your own server / VM / dev container; Desktop installs Claude Code on the remote machine automatically the first time). The **Chat** tab is the odd one out within Desktop — it is the claude.ai chat experience bolted into the same window, with no repo access; it is not the coding agent and won't read your `CLAUDE.md`. **Codex** is the genuine other product, covered at the end of this stage and throughout the course as a deliberate second agent — not a synonym for Claude Code."
  },
  {
   "heading": "The shared config: what makes them one tool",
   "body": "**Shared config** is the set of files Claude Code reads at session start — instructions, settings, MCP servers, hooks, and skills — that *every* Claude Code front-end resolves the same way. This is the literal mechanism behind \"one tool, many front-ends.\" When the docs say the CLI and the Code tab \"share configuration,\" they mean these files on disk.\n\nThere are five categories. You'll meet each in depth later in the course; here is the map so the words stop blurring.\n\n| Category | What it controls | Canonical location(s) |\n|----------|------------------|------------------------|\n| **Instructions** | Persistent natural-language guidance to the agent | `./CLAUDE.md` or `./.claude/CLAUDE.md` (project), `~/.claude/CLAUDE.md` (user), `.claude/rules/*.md` (path-scoped) |\n| **Settings** | Permissions, env vars, model, behavior toggles | `.claude/settings.json`, `.claude/settings.local.json`, `~/.claude/settings.json`, `managed-settings.json` (policy) |\n| **MCP servers** | External tools/data the agent can call | `.mcp.json` (project root), `~/.claude.json` (user/local) |\n| **Hooks** | Shell commands that fire at lifecycle events (deterministic enforcement) | `.claude/settings.json` or a plugin's `hooks/hooks.json` |\n| **Skills** | On-demand procedures invokable as `/name` | `.claude/skills/<name>/SKILL.md`, `~/.claude/skills/...`, plugins |\n\n### The recurring two-axis pattern\n\nNearly every config type follows the same **scope × shared-ness** layout, which is worth internalizing now because it repeats:\n\n- **Project scope** lives in `.claude/` *in the repo* and is committed — this is your **team** config.\n- **User scope** lives in `~/.claude/` — personal, applies across all your projects.\n- **Local scope** is `*.local.*` in the repo, gitignored — personal overrides for *this* project. (MCP is the one exception: its \"local\" scope lives in `~/.claude.json`, not in the repo — covered in the MCP section.)\n- **Managed/policy** scope is IT-deployed and cannot be overridden by individuals.\n\nGet this layout in your head and every config file in the course slots into a known grid: *who does it apply to* (this project / all my projects / everyone in the org) and *is it committed* (team-shared / personal)."
  },
  {
   "heading": "Instructions: CLAUDE.md (the team's shared brain)",
   "body": "`CLAUDE.md` is the file you'll author first and touch most. It's plain markdown, loaded into context in full at the start of *every* session, on *every* front-end. Treat it as the thing you'd otherwise re-explain to a new teammate: build commands, conventions, architecture, \"always do X\" rules.\n\n### Locations and load order\n\nThe docs list them broadest-to-most-specific; later (more specific) files appear later in context. (Source: https://code.claude.com/docs/en/memory)\n\n| Scope | Location | Shared with |\n|-------|----------|-------------|\n| Managed policy | `/etc/claude-code/CLAUDE.md` (Linux/WSL), `/Library/Application Support/ClaudeCode/CLAUDE.md` (macOS), `C:\\Program Files\\ClaudeCode\\CLAUDE.md` (Windows) | All users in org (cannot be excluded) |\n| User | `~/.claude/CLAUDE.md` | Just you, all projects |\n| Project | `./CLAUDE.md` or `./.claude/CLAUDE.md` | **Team, via source control** |\n| Local | `./CLAUDE.local.md` (gitignore it) | Just you, this project |\n\nClaude walks *up* the directory tree from your cwd and concatenates every `CLAUDE.md` (and `CLAUDE.local.md`) it finds, ordered filesystem-root-down so instructions closest to where you launched are read last. Nested `CLAUDE.md` files in subdirectories load *on demand* when Claude reads files there — useful in monorepos.\n\n### Generating and importing\n\nRun `/init` to have Claude analyze the repo and draft a starting `CLAUDE.md` (if one exists, it suggests improvements rather than overwriting). You can also pull in other files with `@`-imports — resolved relative to the importing file, recursive to a max depth of four hops, and still loaded in full into context at launch:\n\n```markdown\n@AGENTS.md\n\n## Claude Code\nUse plan mode for changes under `src/billing/`.\n```\n\nThat `@AGENTS.md` import is the canonical bridge to Codex: the docs state *\"Claude Code reads `CLAUDE.md`, not `AGENTS.md`\"*, so importing it lets both agents share one instruction source. (Source: https://code.claude.com/docs/en/memory)\n\n### Gotchas\n\n- **It's context, not enforcement.** The docs are explicit: CLAUDE.md is *\"delivered as a user message after the system prompt\"* and *\"there's no guarantee of strict compliance.\"* To *block* an action regardless of what the model decides, use a **PreToolUse hook** or `permissions.deny`, not a CLAUDE.md sentence.\n- **Keep it under ~200 lines.** The docs recommend targeting under 200 lines; longer files consume more context and *reduce* adherence. Move procedures to skills and path-specific guidance to `.claude/rules/` (which can be scoped to globs via `paths:` frontmatter so they only load for matching files).\n- **Conflicts are silent.** If two CLAUDE.md files contradict, *\"Claude may pick one arbitrarily.\"* Audit periodically; in monorepos use the `claudeMdExcludes` setting to skip other teams' files.\n- **Don't confuse it with auto memory.** Claude also keeps a separate machine-local auto-memory at `~/.claude/projects/<project>/memory/MEMORY.md` that *Claude* writes; `CLAUDE.md` is the file *you* write and commit. Only `CLAUDE.md` is team-shared via git."
  },
  {
   "heading": "Settings & permissions: settings.json",
   "body": "`settings.json` is the JSON config for behavior — most importantly **permissions**, which decide what the agent may do without asking. Same scope grid as CLAUDE.md, with a precise precedence.\n\n### Locations and precedence (highest wins)\n\n```\n1. Managed policy   (managed-settings.json — cannot be overridden)\n2. Command-line args\n3. .claude/settings.local.json   (local — personal, gitignored)\n4. .claude/settings.json         (project — committed, TEAM config)\n5. ~/.claude/settings.json        (user — all projects)\n```\n\nThe managed policy file is literally named `managed-settings.json` and lives in the OS policy directory: `/etc/claude-code/` (Linux/WSL), `/Library/Application Support/ClaudeCode/` (macOS), `C:\\Program Files\\ClaudeCode\\` (Windows). (Source: https://code.claude.com/docs/en/settings)\n\n### Example\n\n```json\n{\n  \"$schema\": \"https://json.schemastore.org/claude-code-settings.json\",\n  \"permissions\": {\n    \"allow\": [\n      \"Bash(npm run lint)\",\n      \"Bash(npm run test:*)\",\n      \"Read(~/.zshrc)\"\n    ],\n    \"ask\": [\n      \"Bash(git push:*)\"\n    ],\n    \"deny\": [\n      \"Read(./.env)\",\n      \"Read(./.env.*)\",\n      \"Read(./secrets/**)\"\n    ]\n  },\n  \"env\": { \"CLAUDE_CODE_ENABLE_TELEMETRY\": \"1\" },\n  \"model\": \"claude-sonnet-4-6\"\n}\n```\n\n### Permission rule syntax\n\nRules are `Tool(pattern)`. `Bash(npm run test:*)` matches a command prefix; `Read`/`Write` take glob paths (`*` = one segment, `**` = recursive). The three lists are `allow` (run silently), `ask` (prompt), `deny` (block). **Permission rules behave differently from other settings: they *merge* across scopes rather than override, and deny wins on conflict.** So a managed `deny` can't be unlocked by a project `allow`. (For technical enforcement the docs steer you to managed settings — `permissions.deny`, `sandbox.enabled`, `env` — and reserve CLAUDE.md for behavioral guidance.)\n\n### Why this is the heart of a team setup\n\nPermissions are the difference between \"the agent is a productivity tool\" and \"the agent `rm -rf`'d something at 2am.\" Because the project `settings.json` is committed, you author the team's safety envelope *once* and every front-end — terminal, Code tab, Cowork — enforces it identically. Personal experiments go in the gitignored `settings.local.json` so nobody has to touch the shared file to try something. (Source: https://code.claude.com/docs/en/settings)"
  },
  {
   "heading": "External tools: .mcp.json",
   "body": "MCP (Model Context Protocol) is how Claude Code talks to tools and data outside itself — a GitHub server, a database, an internal API, the codebase knowledge graph this very project uses. **Project-scoped** servers are declared in **`.mcp.json`** at the project root (committed, team-shared). **User-** and **local-scoped** servers live in `~/.claude.json` in your home directory — note that MCP's \"local\" scope is *not* a file in the repo, unlike `settings.local.json`.\n\n```json\n{\n  \"mcpServers\": {\n    \"github\": {\n      \"command\": \"/usr/local/bin/github-mcp-server\",\n      \"env\": { \"GITHUB_TOKEN\": \"${GITHUB_TOKEN}\" }\n    },\n    \"filesystem\": {\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"@modelcontextprotocol/server-filesystem\", \"/srv/allowed-dir\"]\n    }\n  }\n}\n```\n\n### The three scopes (per the docs)\n\n| Scope | Available in | Shared via VCS | Stored in |\n|-------|--------------|----------------|-----------|\n| Local (default) | Current project only | No | `~/.claude.json` |\n| Project | Current project only | **Yes** | `.mcp.json` in project root |\n| User | All your projects | No | `~/.claude.json` |\n\n### Notes that bite people\n\n- **Secrets via `${VAR}` interpolation, never literals.** Claude Code supports `${VAR}` and `${VAR:-default}` expansion in `.mcp.json`. Since the file is committed, put `${GITHUB_TOKEN}` in the file and the real token in the environment. A token pasted into `.mcp.json` is a token pushed to the remote.\n- **Project `.mcp.json` is shared; trust is gated.** The docs: *\"For security reasons, Claude Code prompts for approval before using project-scoped servers from `.mcp.json` files.\"* Reset choices with `claude mcp reset-project-choices`. Review what a repo wants to run before approving — a committed MCP server is executable config arriving from the repo.\n- **MCP is a presentation layer, not a requirement.** Per this ecosystem's tooling policy, prefer importing shared Python libraries directly in orchestration code; reach for MCP when an *interactive* agent needs to discover and choose tools mid-conversation. Don't bolt on a large always-on MCP surface just because you can.\n\nThe payoff for the stage-0 thesis: declare a server in `.mcp.json`, commit it, and it's available identically in every front-end — the GUI doesn't have its own separate tool list. (Sources: https://code.claude.com/docs/en/mcp, https://code.claude.com/docs/en/settings)"
  },
  {
   "heading": "On-demand procedures: skills (SKILL.md)",
   "body": "A **skill** is a packaged procedure the agent loads *only when used*, invokable as `/name`. Skills are how you keep CLAUDE.md small: *\"a skill's body loads only when it's used, so long reference material costs almost nothing until you need it.\"* They follow the open **Agent Skills** standard (agentskills.io), which other AI tools also read; Claude Code extends it with invocation control, subagent execution, and dynamic context injection.\n\n### Format\n\nA skill is a directory whose entrypoint is `SKILL.md` — YAML frontmatter plus markdown instructions. Only `description` is strictly needed (the *directory name* becomes the command); set `name` to override the invocation name:\n\n```yaml\n---\nname: deploy\ndescription: Deploy the application to production. Use when the user asks to ship or release.\ndisable-model-invocation: true\n---\n\nDeploy the application:\n1. Run the test suite\n2. Build the application\n3. Push the release tag\n```\n\nThe `description` is what lets Claude decide to auto-load the skill; `disable-model-invocation: true` makes it `/`-only (no autonomous triggering). You can also inject live context with a `` !`command` `` line, whose output is inlined before Claude sees the skill. A skill folder can carry supporting files Claude reads on demand:\n\n```text\nmy-skill/\n├── SKILL.md          # required entrypoint\n├── reference.md      # loaded only when needed\n└── scripts/validate.sh\n```\n\n### Locations (same scope grid)\n\n| Location | Path | Applies to |\n|----------|------|-----------|\n| Enterprise | via managed settings | All users in your org |\n| Personal | `~/.claude/skills/<name>/SKILL.md` | All your projects |\n| Project | `.claude/skills/<name>/SKILL.md` | **This project (team-shared)** |\n| Plugin | `<plugin>/skills/<name>/SKILL.md` | Where the plugin is enabled |\n\nName collisions resolve **enterprise > personal > project**, and any of these overrides a bundled skill of the same name (e.g. a project `code-review` skill replaces the built-in `/code-review`). Plugin skills use a `plugin-name:skill-name` namespace so they can't conflict. Note that **custom commands have merged into skills**: a file at `.claude/commands/deploy.md` and a skill at `.claude/skills/deploy/SKILL.md` both create `/deploy`; if both exist, the skill wins. (Source: https://code.claude.com/docs/en/skills)"
  },
  {
   "heading": "Bundling it all: plugins and marketplaces",
   "body": "Once your team has several skills, hooks, and MCP servers, you don't want each teammate to copy files by hand. A **plugin** is a self-contained directory bundling those components (skills, agents, hooks, MCP servers, LSP servers, monitors); a **marketplace** is a git-hosted catalog of plugins your team installs from. This is the distribution mechanism for shared config.\n\n### Plugin manifest\n\n```json\n// .claude-plugin/plugin.json\n{\n  \"name\": \"quality-review-plugin\",\n  \"description\": \"Adds a quality-review skill for quick code reviews\",\n  \"version\": \"1.0.0\"\n}\n```\n\nIf you include a manifest, **`name` is the only required field** (`description` and `version` are optional but recommended). Per the docs, *\"the `.claude-plugin/` directory contains the `plugin.json` file. All other directories (`commands/`, `agents/`, `skills/`, `output-styles/`, `themes/`, `monitors/`, `hooks/`) must be at the plugin root, not inside `.claude-plugin/`.\"*\n\n### Marketplace catalog\n\n```json\n// .claude-plugin/marketplace.json\n{\n  \"name\": \"my-plugins\",\n  \"owner\": { \"name\": \"Your Name\" },\n  \"plugins\": [\n    {\n      \"name\": \"quality-review-plugin\",\n      \"source\": \"./plugins/quality-review-plugin\",\n      \"description\": \"Adds a quality-review skill for quick code reviews\"\n    }\n  ]\n}\n```\n\n### Install flow\n\n```shell\n/plugin marketplace add ./my-marketplace      # or a GitHub repo\n/plugin install quality-review-plugin@my-plugins\n```\n\nPush the marketplace repo, teammates run those two commands, and everyone has the same skills/hooks/MCP servers. **Versioning gotcha (verbatim from the reference):** *\"Setting `version` pins the plugin to that version string, so users only receive updates when you bump it. If omitted, Claude Code falls back to the git commit SHA, so every commit is treated as a new version.\"* So: set `version` and you must bump it on every release or teammates keep the cached copy; omit `version` and each commit auto-counts as new (good for internal, fast-moving plugins). Marketplace plugins are copied into a local cache (`~/.claude/plugins/cache`), and the old version is cleaned up ~7 days after an update. (Sources: https://code.claude.com/docs/en/plugin-marketplaces, https://code.claude.com/docs/en/plugins-reference)"
  },
  {
   "heading": "The faces in practice: when to use what",
   "body": "All Claude Code front-ends share config, so the choice is about *ergonomics and where the work runs*, not capability.\n\n| You want to... | Use | Why |\n|----------------|-----|-----|\n| Script the agent, pipe output, run in CI/tmux, work over SSH on a server | **CLI** (`claude`) | Keyboard-driven, composable, the canonical surface; the only option on Linux (no Desktop on Linux) |\n| Run several tasks in parallel, review diffs visually, preview a running app | **Desktop → Code** | GUI adds parallel sessions (each in its own git worktree), line-level diff review, live preview, PR auto-merge |\n| Kick off long autonomous work that survives closing your laptop | **Code tab → Remote**, **Cowork**, or CLI/Desktop \"send to cloud\" | Runs in a cloud VM on the same infra as Claude Code on the web |\n| Just chat, no repo access | **Desktop → Chat** | claude.ai in the same window; *not* the coding agent |\n| A second, cheaper agent for high-volume keystroke-level iteration | **Codex** (next section) | Different vendor, different cost/limit profile |\n\n### Practical guidance for a team\n\nDon't standardize the *surface*; standardize the *config*. Let people use the terminal or the GUI by taste — they're driving the same engine off the same committed `.claude/` directory. What you enforce as a lead is the contents of `CLAUDE.md`, `.claude/settings.json` (especially permissions), `.mcp.json`, and your plugin marketplace. And note the hard constraint: per the quickstart, *\"the desktop app is not available on Linux; use the CLI instead\"* — so any Linux teammates are on the CLI regardless. That's one more reason the *config*, not the surface, is the unit of standardization."
  },
  {
   "heading": "The one real boundary: Codex is a different tool",
   "body": "Everything above is one product. **OpenAI Codex is not.** It's a separate vendor's agentic coding tool with its *own* engine, config, and surfaces (CLI TUI, IDE extension, and Codex Cloud). Critically for this stage's thesis: **Codex does not read Claude Code's config.** It reads its own.\n\n### Codex's config files\n\n```toml\n# ~/.codex/config.toml   (project override: .codex/config.toml, loaded when you trust the project)\nmodel = \"gpt-5.5\"\napproval_policy = \"on-request\"   # untrusted | on-request | never\nsandbox_mode = \"workspace-write\" # read-only | workspace-write | danger-full-access\nmodel_reasoning_effort = \"high\"\n```\n\nThose `approval_policy` and `sandbox_mode` value sets are the documented valid options; `model` is illustrative (use whatever model id your account exposes). (Source: https://developers.openai.com/codex/config-basic)\n\n### The shared-instruction bridge\n\nThe two ecosystems meet at **`AGENTS.md`**, the cross-tool open standard (agents.md). Codex reads `AGENTS.md` natively for instructions; it discovers them from your Codex home (`~/.codex/AGENTS.md`) down through the git root to your cwd, and per its docs *\"concatenates files from the root down... files closer to your current directory override earlier guidance because they appear later in the combined prompt\"* (capped at `project_doc_max_bytes`, 32 KiB default). Keep your durable instructions in `AGENTS.md` and have `CLAUDE.md` import it:\n\n```markdown\n@AGENTS.md\n\n## Claude Code-specific\nUse plan mode for changes under `src/billing/`.\n```\n\nA symlink (`ln -s AGENTS.md CLAUDE.md`) also works when you have nothing Claude-specific to add (on Windows the import is preferred, since symlinks need admin/Developer Mode). Now one instruction source feeds both agents. Permissions, MCP, hooks, and skills still differ — only the prose instructions are shared.\n\n### Two distinct config worlds\n\n| | Claude Code | Codex |\n|--|-------------|-------|\n| Instructions | `CLAUDE.md` (imports/symlinks `AGENTS.md`) | `AGENTS.md` (read directly) |\n| Settings/permissions | `.claude/settings.json` | `~/.codex/config.toml` |\n| Approval model | `permissions` allow/ask/deny | `approval_policy` + `sandbox_mode` |\n\n(Sources: https://developers.openai.com/codex/config-basic, https://developers.openai.com/codex/guides/agents-md)"
  },
  {
   "heading": "Both, not either: the Claude Code + Codex pattern",
   "body": "Among practitioners, **Claude Code vs Codex is contested — and the productive resolution is to run both**, routing work by their different strengths rather than picking a winner. (Source: https://dev.to/_46ea277e677b888e0cd13/claude-code-vs-codex-2026-what-500-reddit-developers-really-think-31pb)\n\nThe two positions in that survey of ~500 developers:\n\n- **Claude-quality:** Claude Code is rated higher quality (~67% blind-comparison win rate), leads SWE-bench, and has full MCP support — but ships with tighter usage limits.\n- **Codex-efficiency:** Codex is cheaper (~4x fewer tokens), leads Terminal-Bench, and the community shorthand is *\"Codex for keystroke, Claude Code for commits.\"*\n\n### What \"Codex for keystroke, Claude Code for commits\" means operationally\n\nUse the cheaper, token-efficient agent (Codex) for high-volume, low-stakes iteration — the keystroke-level churn where you'd burn through Claude's tighter limits fast. Bring in the higher-quality agent (Claude Code) for the consequential, commit-worthy work: the refactor, the tricky bug, the change you'll actually ship. This is a *routing* decision, and because both read a shared `AGENTS.md`, you can move a task between them without rewriting your project's ground rules.\n\n### What this means for your team setup\n\nDesign for two agents from the start. Put durable project knowledge in `AGENTS.md` so both read it; keep Claude-specific enforcement (permissions, hooks, skills) in `.claude/`; and treat the two agents' differing cost/limit profiles as a *budget tool*, not a loyalty test. This is contested terrain — present it to your team as a tradeoff table, not a mandate. (These quality/cost figures are community-survey claims, not vendor benchmarks; treat them as directional, and re-check against your own usage.)"
  },
  {
   "heading": "Common pitfalls and the mental model to keep",
   "body": "### Pitfalls\n\n- **Treating Desktop's tabs as one thing.** *Code* is the agent; *Chat* has no file access and ignores your repo config; *Cowork* runs remotely. Conflating them leads to \"why didn't it see my CLAUDE.md?\" — because you were in the Chat tab.\n- **Expecting CLAUDE.md to *enforce* anything.** It's delivered as a user message — context, not a guardrail, with no compliance guarantee. Anything that must *always* happen (block a path, run a lint before commit) belongs in a **PreToolUse hook** or `permissions.deny`, which the client enforces regardless of model behavior.\n- **Committing secrets in `.mcp.json` or settings.** These files are team-shared. Use `${ENV_VAR}` (or `${VAR:-default}`) interpolation; keep real secrets in the environment.\n- **Putting personal tweaks in the committed file.** Use `settings.local.json` and `CLAUDE.local.md` (gitignored) so experiments don't churn the team's shared config or leak your sandbox URLs.\n- **Confusing MCP's \"local\" scope with `settings.local.json`.** MCP local-scoped servers live in `~/.claude.json` (home), not in the repo. Only *project*-scoped MCP (`.mcp.json`) is committed and team-shared.\n- **Assuming Codex reads `CLAUDE.md`.** It does not — it reads `AGENTS.md`. Bridge via an `@AGENTS.md` import or symlink, or maintain two instruction sources knowingly.\n- **Plugin updates that don't update.** A pinned `version` in `plugin.json` must be bumped every release; otherwise teammates keep the cached copy. Omit it and each commit SHA counts as a new version.\n\n### The mental model to carry into the rest of the course\n\nHold two sentences:\n\n1. *Claude Code is one engine; the CLI, the Code tab, and Cowork are interchangeable front-ends reading the same `.claude/` config — so I configure the **files**, not the surface.*\n2. *Codex is a separate agent with its own config; the only shared seam is `AGENTS.md`, and the smart pattern is to run both and route by strength.*\n\nEvery later stage — permissions, hooks, skills, MCP, plugins, multi-agent orchestration — is just depth on one of the config categories you met here. The blur is gone: there is one tool with many faces, one genuine sibling tool, and a small grid of files that ties the whole thing to your repo."
  }
 ],
 "stage-1": [
  {
   "heading": "The question behind the question",
   "body": "When you stand up shared agentic-coding infrastructure, the first thing your team will ask is deceptively simple: *\"Which one do I open?\"* They mean: terminal, desktop app, the thing my non-engineer PM is using, the OpenAI tool half the team prefers. The wrong answer wastes weeks. A backend dev who only knows the GUI will hand-edit JSON that should have been a `claude -p` one-liner in CI. An ops teammate who never hears about Cowork will keep doing by hand the cross-app drudgery that surface was built to absorb. A reviewer who assumes Codex and Claude Code share config will wonder why their carefully written `CLAUDE.md` is invisible to half the team.\n\nThe honest answer is **not one tool — it's a menu**, and most items on the menu are the *same capability projected onto a different surface*, with one critical exception (Codex, which is a different vendor entirely). This stage maps the menu precisely: what each surface is, what it can and cannot do, where its config lives, and the failure modes you hit when you pick wrong.\n\n### The mental model: one engine, several surfaces, one foreign agent\n\nThree of the five things on this menu — the terminal CLI, the Desktop **Code** tab, and headless mode — are the *same Claude Code engine*. The official desktop docs say it plainly: \"Desktop runs the same engine as the CLI with a graphical interface... they share configuration (CLAUDE.md files, MCP servers, hooks, skills, and settings)\" ([desktop quickstart](https://code.claude.com/docs/en/desktop-quickstart)). Cowork is a fourth surface of Anthropic's agentic capability aimed at non-developers. Codex is a *separate product from OpenAI* with its own engine and its own config tree. Hold that shape in your head: **don't reason about \"Claude Code the GUI vs Claude Code the CLI\" as two products** — they are one product reading one config, shown two ways."
  },
  {
   "heading": "Surface 1 — the CLI (the terminal): the scriptable core",
   "body": "The CLI is `claude` in your terminal. It is the canonical, lowest-level surface: everything the other Claude Code surfaces do, this can do, plus the things *only* a terminal can do (pipe stdin, redirect stdout, exit codes, run inside CI with no display).\n\n### What it is\n\nAn interactive REPL when you run `claude` bare, and a one-shot tool when you add `-p`. It reads the full configuration stack at startup: `CLAUDE.md` files up the directory tree, `~/.claude/` user config, MCP servers from `.mcp.json`, hooks, skills, and `settings.json`.\n\n### Why a team standardizes on it as the base layer\n\n- **Scriptable and automatable.** It composes with the rest of your shell. A `package.json` script can call it; a Makefile target can call it; a git hook can call it.\n- **Reproducible.** With the right flags (see headless, below) you get identical behavior across machines.\n- **Universal.** It is the only surface available on Linux — the docs state \"The desktop app is not available on Linux; use the CLI instead\" ([desktop quickstart](https://code.claude.com/docs/en/desktop-quickstart)). Your CI runners and remote boxes are almost always Linux, so the CLI is the only surface that runs *everywhere your code runs*.\n\n### Concrete: starting an interactive session\n\n```bash\ncd ~/projects/godel\nclaude                          # interactive REPL in this repo\nclaude --model claude-opus-4-8  # pin a model\nclaude --add-dir ../shared-lib  # grant access to a sibling dir\n```\n\nInside the REPL, `/memory` lists which `CLAUDE.md`, `CLAUDE.local.md`, and rules files loaded (and toggles auto memory), `/config` changes settings, and `/skill-name` invokes a skill. The point of the CLI is that *all* of these are also reachable non-interactively — which is the next surface.\n\n### One menu, one table\n\n| Surface | Engine | OS | Headless? | Config read |\n|---|---|---|---|---|\n| CLI (`claude`) | Claude Code | macOS / Windows / **Linux** | yes (`-p`) | full Claude Code stack |\n| Headless (`claude -p`) | Claude Code | same as CLI | yes (it *is* the mode) | full stack, or none with `--bare` |\n| Desktop **Code** tab | Claude Code | macOS / Windows only | no (interactive) | same stack as CLI |\n| Cowork | Anthropic agent (cloud VM) | macOS / Windows desktop | n/a (autonomous) | connectors, not CLAUDE.md |\n| Codex | **OpenAI** | macOS / Windows / Linux | yes (`codex exec`) | `~/.codex` (TOML) |"
  },
  {
   "heading": "Surface 2 — headless mode: the same engine, no human in the loop",
   "body": "Headless mode is not a separate program. It is the CLI run **non-interactively** with `-p` (alias `--print`): one prompt in, output out, then exit. This is what you put in scripts, git hooks, and CI. Per the docs, this non-interactive mode is a **terminal-CLI capability — the Desktop GUI is interactive only** ([headless docs](https://code.claude.com/docs/en/headless)).\n\n### The shape of a headless call\n\n```bash\nclaude -p \"What does the auth module do?\"\n```\n\nIt reads stdin, so it composes like any Unix filter:\n\n```bash\ncat build-error.txt | claude -p 'concisely explain the root cause of this build error' > output.txt\n```\n\nAs of Claude Code v2.1.128, piped stdin is capped at 10MB; beyond that, Claude Code exits with a clear error and a non-zero status, so write to a file and reference the path in the prompt instead.\n\n### Structured output for scripts\n\n`--output-format` controls the response shape — `text` (default), `json` (result + session id + cost metadata), or `stream-json` (newline-delimited events for live streaming). For schema-constrained output, combine `--output-format json` with `--json-schema`; the structured payload lands in the `structured_output` field:\n\n```bash\nclaude -p \"Extract the main function names from auth.py\" \\\n  --output-format json \\\n  --json-schema '{\"type\":\"object\",\"properties\":{\"functions\":{\"type\":\"array\",\"items\":{\"type\":\"string\"}}},\"required\":[\"functions\"]}' \\\n  | jq '.structured_output'\n```\n\nThe `json` payload includes `total_cost_usd` and a per-model cost breakdown, so a scripted caller can track spend per invocation without opening a dashboard. For live streaming, pair `--output-format stream-json` with `--verbose --include-partial-messages` and filter `text_delta` events with `jq`.\n\n### Auto-approving tools (the part everyone gets wrong first)\n\nIn an interactive session you approve each tool use. In headless mode there is no human to approve, so an un-pre-approved tool **aborts the run**. You grant permission up front:\n\n```bash\nclaude -p \"Run the test suite and fix any failures\" \\\n  --allowedTools \"Bash,Read,Edit\"\n```\n\n`--allowedTools` uses [permission-rule syntax](https://code.claude.com/docs/en/settings#permission-rule-syntax), so you can scope Bash narrowly. The trailing space-then-`*` is load-bearing:\n\n```bash\nclaude -p \"Look at my staged changes and create an appropriate commit\" \\\n  --allowedTools \"Bash(git diff *),Bash(git log *),Bash(git status *),Bash(git commit *)\"\n```\n\nThe docs are explicit: `Bash(git diff *)` allows any command starting with `git diff`, and \"the space before `*` is important: without it, `Bash(git diff*)` would also match `git diff-index`.\" For a whole-session baseline instead of listing tools, pass a [permission mode](https://code.claude.com/docs/en/permission-modes): `--permission-mode acceptEdits` (auto-writes files and auto-approves common fs commands like `mkdir`, `touch`, `mv`, `cp` — other shell/network calls still need an `--allowedTools` entry) or `--permission-mode dontAsk` (denies anything not in `permissions.allow` or the read-only command set — the locked-down CI choice).\n\n### `--bare`: reproducibility for CI\n\n`claude --bare -p ...` skips auto-discovery of hooks, skills, plugins, MCP servers, auto memory, and `CLAUDE.md`. The docs call this \"the recommended mode for scripted and SDK calls\" and say it \"will become the default for `-p` in a future release.\" The motivation is sharp: *a hook in a teammate's `~/.claude` or an MCP server in the repo's `.mcp.json` won't silently change your CI result, because bare mode never reads them.* In bare mode Claude still has Bash, file-read, and file-edit tools; you pass everything else explicitly:\n\n```bash\nclaude --bare -p \"Summarize this file\" --allowedTools \"Read\" \\\n  --settings ./ci-settings.json \\\n  --mcp-config ./ci-mcp.json\n```\n\nNote bare mode skips OAuth/keychain reads — Anthropic auth must come from `ANTHROPIC_API_KEY` or an `apiKeyHelper` in the JSON passed to `--settings` (Bedrock/Vertex/Foundry use their usual provider credentials).\n\n### Use it as a project linter / reviewer\n\n```json\n{\n  \"scripts\": {\n    \"lint:claude\": \"git diff main | claude -p \\\"you are a typo linter. for each typo in this diff, report filename:line on one line and the issue on the next. return nothing else.\\\"\"\n  }\n}\n```\n\nPiping the diff means Claude doesn't even need Bash permission to read it. Add `--bare` for CI so the runner's ambient config can't change the result."
  },
  {
   "heading": "Surface 3 — the Desktop Code tab: the same engine with a GUI",
   "body": "The Desktop app's **Code** tab is Claude Code with a graphical interface. The single most important fact to drill into your team, straight from the docs: \"Desktop runs the same engine as the CLI with a graphical interface. You can run both simultaneously on the same project, and they share configuration (CLAUDE.md files, MCP servers, hooks, skills, and settings)\" ([desktop quickstart](https://code.claude.com/docs/en/desktop-quickstart)). They do not have divergent behavior. Note the app is macOS/Windows only (universal macOS build; Windows x64 and ARM64) — there is no Linux desktop, and Claude Code requires a Pro, Max, Team, or Enterprise subscription.\n\n### What the GUI adds over the terminal\n\nThe Desktop app is described as \"built for running multiple sessions side by side\" with a visual review loop:\n\n- **Visual diff review.** Claude proposes a change; you see a file-by-file diff with Accept/Reject buttons and can comment on specific lines, which Claude reads and revises. Clicking **Review code** has Claude evaluate its own diffs and leave inline suggestions.\n- **Drag-and-drop panes.** Chat, diff, integrated terminal (open with **Ctrl+`**), file editor, and live app **Preview** (run your dev server inside the app; Claude can view the running app, test endpoints, and inspect logs).\n- **Parallel sessions, each in its own git worktree.** This is the headline capability: open multiple sessions from the sidebar, \"each in its own Git worktree,\" so concurrent tasks don't stomp on each other's working tree. A tasks pane shows the subagents and background commands a session is running; a *side chat* lets you ask a question without derailing the main thread.\n- **PR monitoring + auto-merge, scheduled tasks, remote/SSH environments.** It monitors CI check results and \"can automatically fix failures or merge the PR once all checks pass\"; it runs scheduled tasks (daily review, weekly dep audit, briefings); and sessions run **Local**, **Remote** (Anthropic cloud — \"continue even if you close the app\"), or over **SSH** (it installs Claude Code on the remote machine the first time you connect).\n\n### The permission modes mirror the CLI\n\n| Desktop mode | CLI equivalent | Behavior |\n|---|---|---|\n| Ask permissions (default) | (interactive default) | Approve before every edit |\n| Auto accept edits | `--permission-mode acceptEdits` | Auto-applies file edits |\n| Plan mode | `--permission-mode plan` | Maps an approach, touches no files |\n\n### The gotcha that costs teams a day: Code tab ≠ legacy Claude desktop chat app\n\nThe desktop app has three tabs — **Chat** (\"general conversation with no file access, similar to claude.ai\"), **Cowork** (\"an autonomous background agent that works on tasks in a cloud VM,\" covered below), and **Code**. The **Code** tab is distinct from the *legacy Claude desktop chat app*, whose MCP configuration lives in a separate `claude_desktop_config.json` that **does not appear in the Code tab** ([desktop quickstart](https://code.claude.com/docs/en/desktop-quickstart), [issue #33732](https://github.com/anthropics/claude-code/issues/33732)). So if a teammate added an MCP server to the old chat app's `claude_desktop_config.json` and wonders why the Code tab can't see it — that's why. Code-tab MCP comes from Claude Code's own config (`.mcp.json`, user/project settings, or the Code tab's **+ → Plugins** UI), the *same* config the CLI reads, not from `claude_desktop_config.json`."
  },
  {
   "heading": "Surface 4 — Cowork: the same agentic capability for non-developers",
   "body": "Cowork is the same agentic loop — give it a goal, it plans, it acts across your stuff — pointed at **non-coding knowledge work**. It is the surface you point your PMs, ops, analysts, and support leads at. The docs describe the Cowork tab as \"an autonomous background agent that works on tasks in a cloud VM with its own environment\" that \"can run independently while you do other work\"; the product page frames it as agentic non-coding knowledge work that shows its plan before acting, available on paid plans including Team and Enterprise ([Cowork product page](https://claude.com/product/cowork), [support article](https://support.claude.com/en/articles/13345190)).\n\n### What it does\n\nHand off a task, get a polished deliverable: organizing file systems, building spreadsheets, generating reports, synthesizing scattered notes, scheduling recurring tasks. The defining property for trust: **before Claude acts, it shows you the plan and waits for your approval** — you can redirect, refine, or change approach at any step. This is the difference between \"an agent that ran amok in my Drive\" and \"an agent I supervised.\"\n\n### How it reaches your tools\n\nCowork works across apps using a **connector for Slack, Chrome for web research, or your screen** to drive apps that have no direct integration. You keep access controls over which folders and connectors it may use. It is one of the three desktop tabs and, because it runs as an autonomous background agent in a cloud VM, it continues while you do other work; mobile can initiate tasks via phone pairing.\n\n### Why this belongs on the menu even for an engineering org\n\nYour infra is not only for developers. The exact same capability that fixes a failing test can reconcile a vendor spreadsheet or compile a weekly status report from Slack threads — and if your non-engineers never learn that Cowork exists, they keep doing that work by hand while paying for the agent that would do it. Enterprise admins can manage feature access, control spend, and track Cowork usage across the org, so it slots into the same governance story as Code.\n\n### Adjacent preview: Dispatch + computer use (know the boundary)\n\nComputer use (Claude points/clicks/navigates your screen) and phone **Dispatch** (assign tasks from your phone) are a **research preview available for Claude Pro and Max** on macOS and Windows ([Dispatch + computer use](https://claude.com/blog/dispatch-and-computer-use), [support](https://support.claude.com/en/articles/12138966)). The docs name Pro and Max; they do **not** document Team/Enterprise inclusion or exclusion — so the accurate statement to your org is *\"computer use/Dispatch is not part of the documented Team plan,\"* an inference, not a stated exclusion. Treat it as experimental: it \"won't always work perfectly\" and is slower than a direct integration. Don't build a team workflow on it yet — prefer a real connector where one exists."
  },
  {
   "heading": "Surface 5 — Codex: the second coding agent (different vendor, different tree)",
   "body": "Codex is **OpenAI's coding agent** — a genuinely separate product some of your devs will prefer and run alongside Claude Code. This is the one menu item that is *not* the same engine, and the most important thing to internalize is the boundary: **Codex has its own config tree (`~/.codex`); nothing syncs automatically between Codex and Claude Code** ([Codex config reference](https://developers.openai.com/codex/config-reference), [Claude `.claude` directory](https://code.claude.com/docs/en/claude-directory)).\n\n### The config tree\n\n```\n~/.codex/                         # = $CODEX_HOME\n├── config.toml                   # user-level config (TOML, not JSON)\n├── auth.json                     # CLI credentials\n├── AGENTS.md                     # global agent instructions (or AGENTS.override.md)\n├── log/                          # logs (log_dir)\n└── history.jsonl                 # session history (history.persistence)\n```\n\n(The config reference documents `config.toml`, `auth.json`, the `log/` directory, and `history.jsonl`; treat any other files you see in `~/.codex` as implementation detail, not contract.) Project-scoped overrides go in a `.codex/config.toml` in the repo (these cannot override machine-local provider/auth). The user-level config lives at `~/.codex/config.toml`; keys look like:\n\n```toml\nmodel = \"gpt-5.5\"\nmodel_provider = \"openai\"           # provider ID from model_providers\napproval_policy = \"on-request\"      # untrusted | on-request | never\nsandbox_mode = \"workspace-write\"    # read-only | workspace-write | danger-full-access\n\n[mcp_servers.my_server]\ncommand = \"python -m my_mcp_server\"\n```\n\nContrast with Claude Code: JSON `settings.json`, `.mcp.json`, `~/.claude/`. **Two different formats, two different trees.** A teammate who configured an MCP server for Claude Code has done *nothing* for Codex, and vice versa.\n\n### Headless Codex\n\nCodex's non-interactive entrypoint is `codex exec` (short form `codex e`) — per the docs, \"for scripted or CI-style runs that should finish without human interaction.\" The current guidance: prefer explicit `--sandbox workspace-write` over the deprecated `--full-auto` (Codex prints a warning when `--full-auto` is used), and use `approval_policy = \"never\"` for non-interactive runs (`on-request` for interactive) ([Codex CLI reference](https://developers.openai.com/codex/cli/reference), [config reference](https://developers.openai.com/codex/config-reference)).\n\n```bash\ncodex exec --sandbox workspace-write \"run the tests and fix failures\"\n```\n\n### The instruction-file split that bites every mixed team\n\nThis is the single most consequential interop fact:\n\n- **Codex reads `AGENTS.md`** for project instructions, building a chain from `~/.codex/AGENTS.md` (global) down through the git root to your cwd, concatenated root-down so closer files win ([Codex AGENTS.md](https://developers.openai.com/codex/guides/agents-md)).\n- **Claude Code reads `CLAUDE.md`, NOT `AGENTS.md`** — the memory docs state verbatim: \"Claude Code reads `CLAUDE.md`, not `AGENTS.md`\" ([Claude memory](https://code.claude.com/docs/en/memory)).\n\nSo a repo with only `AGENTS.md` is invisible to Claude Code, and a repo with only `CLAUDE.md` is invisible to Codex. You **bridge** them (next section)."
  },
  {
   "heading": "Bridging CLAUDE.md and AGENTS.md (the interop you must set up)",
   "body": "If any part of your team runs Codex, you must make one set of instructions feed both agents — otherwise the two drift and reviewers can't tell which agent saw what. There are two sanctioned bridges; both come straight from the official memory docs, which include this exact guidance: \"If your repository already uses `AGENTS.md` for other coding agents, create a `CLAUDE.md` that imports it so both tools read the same instructions without duplicating them.\"\n\n### Option A — import (recommended, cross-platform)\n\nMake `CLAUDE.md` import `AGENTS.md` with the `@path` import syntax, then add any Claude-specific notes below. Claude loads the imported file at session start, then appends the rest:\n\n```markdown\n@AGENTS.md\n\n## Claude Code\nUse plan mode for changes under `src/billing/`.\n```\n\nThis keeps `AGENTS.md` as the shared source of truth and works on Windows (no symlink privileges needed). Imports can recurse up to four hops; to *mention* a path without importing it, wrap it in backticks.\n\n### Option B — symlink (only if you need no Claude-specific content)\n\nThe docs say \"A symlink also works if you don't need to add Claude-specific content\":\n\n```bash\nln -s AGENTS.md CLAUDE.md\n```\n\nOn Windows a symlink \"requires Administrator privileges or Developer Mode, so use the `@AGENTS.md` import instead.\"\n\n### Option C — generated mirror (what mature teams do)\n\nKeep `CLAUDE.md` canonical and **generate** `AGENTS.md` from it (or vice versa) with a script in CI, so neither drifts. This repo's parent ecosystem does exactly this: per the project rules, *\"`CLAUDE.md` is canonical per scope. `AGENTS.md` is the Codex-facing mirror... generated from CLAUDE.md via `scripts/meta/render_agents_md.py`.\"* The tradeoff vs. the import: a generated mirror lets you transform/filter content per-agent and gives both files real on-disk presence (some tooling greps for one or the other), at the cost of a build step that must run before commit.\n\n### Decision\n\n| Situation | Bridge |\n|---|---|\n| Mixed Codex+Claude team, want one source, Windows present | `@AGENTS.md` import in `CLAUDE.md` |\n| Identical instructions, no per-agent content, macOS/Linux | symlink |\n| Want per-agent transforms / both files materialized / enforce in CI | generated mirror |\n\nA useful bonus, confirmed in the docs: running Claude Code's `/init` in a repo that already has `AGENTS.md` \"reads it and incorporates the relevant parts into the generated `CLAUDE.md`. It also reads other tool configs like `.cursorrules`, `.devin/rules/`, and `.windsurfrules`.\""
  },
  {
   "heading": "Claude Code vs Codex — when to reach for which",
   "body": "This is **not** either/or. The productive pattern reported across hundreds of developers is to run both, and assign work by their respective strengths ([Claude Code vs Codex, 2026](https://dev.to/_46ea277e677b888e0cd13/claude-code-vs-codex-2026-what-500-reddit-developers-really-think-31pb)).\n\n### The two positions, stated fairly\n\n- **Claude-quality.** Claude Code wins blind quality comparisons (~67% win rate cited), leads SWE-bench, and has full MCP support — but ships tighter usage limits.\n- **Codex-efficiency.** Codex is cited as cheaper (~4x fewer tokens), leads Terminal-Bench, and the community shorthand is *\"Codex for keystroke, Claude Code for commits.\"*\n\nRead that shorthand literally as a workflow: use the cheaper/faster agent for high-frequency, low-stakes keystroke-level help (quick edits, exploration where token cost dominates), and bring the higher-quality agent in for the consequential unit of work — the commit, the refactor, the PR you'll actually land.\n\n### Decision table\n\n| Need | Reach for |\n|---|---|\n| Highest single-shot quality on a hard change | Claude Code |\n| Full MCP tool ecosystem in the agent loop | Claude Code |\n| Lowest token cost for high-volume edits | Codex |\n| Terminal-task-heavy automation | Codex |\n| Visual diff review + parallel worktrees + PR auto-merge | Claude Code Desktop |\n| Non-developer cross-app knowledge work | Cowork |\n| Runs on Linux CI with no display | Claude Code CLI (`-p`) or `codex exec` |\n\n### What this means for your infra\n\nBudget for both. Keep instructions bridged so the two agents share project context. Don't try to standardize the org onto one coding agent for ideological reasons — let devs pick per-task, and make sure your `AGENTS.md`/`CLAUDE.md` bridge means *the choice is free* (no re-documenting per agent). Treat the cost/win-rate figures as perishable community benchmarks, not contracts — re-check before you set hard policy on them."
  },
  {
   "heading": "\"Which one do I open?\" — the routing answers",
   "body": "Give your team this routing table verbatim. It answers the literal question that opened this stage.\n\n| If you are... and you want to... | Open |\n|---|---|\n| A dev, iterating with full keyboard control, scripting | **CLI** (`claude`) |\n| A dev, want visual diffs / parallel worktrees / live preview / PR auto-merge | **Desktop Code tab** |\n| A dev or platform eng, wiring into CI, git hooks, a linter, a cron | **Headless** (`claude --bare -p ...`) |\n| A PM/ops/analyst, want a deliverable across files + Slack + web | **Cowork** |\n| A dev who prefers OpenAI's agent or wants cheap high-volume edits | **Codex** (`codex` / `codex exec`) |\n| On Linux (laptop, server, CI runner) | **CLI / headless** only (no Desktop) |\n\n### Cross-surface continuity\n\nBecause the three Claude Code surfaces share an engine and config, you can **start in one and continue in another**. A specific CLI session can be resumed by session id with `--resume`, and the docs note the lookup \"is scoped to the current project directory and its git worktrees.\" The Desktop app can send long-running work to the cloud or continue a session on the web/IDE. The portability is real *because* the config is shared — which is the whole reason to model these as surfaces, not separate tools.\n\n### Headless continuation example\n\n```bash\nsession_id=$(claude -p \"Start a review\" --output-format json | jq -r '.session_id')\nclaude -p \"Continue that review\" --resume \"$session_id\"\n```\n\nRun both commands from the same directory — session-id lookup is scoped to the current project directory and its git worktrees. (To just pick up the most recent conversation without capturing an id, use `--continue` instead of `--resume`.)"
  },
  {
   "heading": "Config surface map — where each thing actually reads from",
   "body": "Picking the right surface also means knowing *which files it reads*, so you don't edit config that the surface in front of you ignores.\n\n### Claude Code (CLI, Desktop Code tab, headless) — shared stack\n\nLoad order is broad→specific (managed → user → project → local); files closer to your cwd are read last and win:\n\n```\n/etc/claude-code/CLAUDE.md             # managed policy (Linux/WSL)\n/Library/Application Support/ClaudeCode/CLAUDE.md   # managed policy (macOS)\nC:\\Program Files\\ClaudeCode\\CLAUDE.md  # managed policy (Windows)\n~/.claude/CLAUDE.md                    # user-level instructions (all projects)\n~/.claude/settings.json                # user settings\n~/.claude/rules/*.md                   # user-level rules\n<repo>/CLAUDE.md  or  .claude/CLAUDE.md # project instructions (committed)\n<repo>/CLAUDE.local.md                  # personal project prefs (gitignore)\n<repo>/.claude/rules/*.md               # project rules (optional paths: frontmatter)\n<repo>/.mcp.json                        # project MCP servers\n<repo>/.claude/settings.json            # project settings\n<repo>/.claude/settings.local.json      # personal/local settings\n```\n\n`CLAUDE.md` and `CLAUDE.local.md` files in the directory hierarchy load *in full* at launch; within each directory `CLAUDE.local.md` is appended after `CLAUDE.md`. Auto memory is separate: it lives at `~/.claude/projects/<project>/memory/MEMORY.md`, and only its first 200 lines / 25KB load at session start. Path-scoped rules use frontmatter:\n\n```markdown\n---\npaths:\n  - \"src/api/**/*.ts\"\n---\n# API rules (load only when touching matching files)\n```\n\n### Codex — separate stack\n\n```\n~/.codex/config.toml      # user config (TOML)\n~/.codex/AGENTS.md        # global instructions (or AGENTS.override.md)\n<repo>/.codex/config.toml # project config override\n<repo>/AGENTS.md          # project instructions (chained git-root → cwd, closer wins)\n```\n\n### Legacy Claude desktop chat app — a third, unrelated stack\n\n```\nclaude_desktop_config.json   # MCP for the OLD chat app ONLY\n                             # NOT read by the Code tab or the CLI\n```\n\nKeep this triple-tree picture posted. Most \"my config isn't working\" tickets are someone editing a file the surface in front of them doesn't read."
  },
  {
   "heading": "Common pitfalls and gotchas",
   "body": "### 1. Editing `claude_desktop_config.json` and expecting the Code tab to see it\nThat file is the *legacy chat app's* MCP config. The Code tab (and CLI) read Claude Code's own config (`.mcp.json`, settings). Symptom: \"I added the MCP server but the agent can't use it.\" Fix: add it to `.mcp.json` or via the Code tab's **+ → Plugins** UI ([issue #33732](https://github.com/anthropics/claude-code/issues/33732)).\n\n### 2. Expecting headless in the Desktop GUI\nThere is no `-p` in the GUI; the Desktop app is interactive only. Anything that must run unattended (CI, hooks, cron-without-the-app) is CLI/headless territory ([headless docs](https://code.claude.com/docs/en/headless)). The Desktop *can* run scheduled tasks, but that's the app driving an interactive session, not a Unix-pipe headless call.\n\n### 3. Headless run aborts on a tool prompt\nNo human to approve → an un-pre-approved tool kills the run. Pre-grant with `--allowedTools` or a `--permission-mode`. And mind the space in `Bash(git diff *)` — drop it and you over-match (`git diff-index`). Note `--permission-mode acceptEdits` auto-approves edits and common fs commands but *not* arbitrary shell/network — those still need an `--allowedTools` entry.\n\n### 4. `CLAUDE.md` ↔ `AGENTS.md` drift\nClaude Code does **not** read `AGENTS.md`; Codex does **not** read `CLAUDE.md`. A repo with only one is half-blind to the team. Bridge with `@AGENTS.md` import, a symlink, or a generated mirror — and pick *one* canonical file so they can't disagree.\n\n### 5. Assuming Codex inherits your Claude config\nNothing syncs. Different tree (`~/.codex`), different format (TOML vs JSON), different MCP block (`[mcp_servers.<id>]` vs `.mcp.json`). Configure both, or you'll have one dev whose MCP tools \"don't exist\" in the other agent.\n\n### 6. Non-reproducible CI because of ambient config\nWithout `--bare`, `claude -p` loads *whatever* is in the runner's `~/.claude` and the repo's `.mcp.json`/hooks — so a teammate's hook can change your build. Use `--bare` plus explicit `--settings`/`--mcp-config` for deterministic CI. Remember bare mode needs `ANTHROPIC_API_KEY` or an `apiKeyHelper` (no keychain/OAuth).\n\n### 7. Treating computer use / Dispatch as production\nResearch preview, Pro/Max-documented, macOS/Windows, \"won't always work perfectly,\" slower than a direct integration. Don't put it on a team-critical path; prefer a real connector/integration where one exists.\n\n### 8. Pointing non-developers at the Code tab\nThe Code-tab review loop is built around *diffs and git*. For an analyst reconciling spreadsheets, that's the wrong surface — Cowork (goal in, plan shown, deliverable out, works across Slack/Chrome/screen) is the one built for them. Get this wrong and your ops people either bounce off a developer tool or never adopt anything at all."
  }
 ],
 "stage-2": [
  {
   "heading": "The bug isn't in the prompt — it's in the room",
   "body": "Two engineers paste the same prompt — \"add input validation to the billing endpoint\" — into the same repo. One gets code with Pydantic models, structured logging, and a test. The other gets a bare `if not x: return`. Same model, same prompt, same files. The difference is everything the agent loaded *before* the prompt: one teammate has a `CLAUDE.md` that says \"API handlers validate with Pydantic and log all state changes,\" a `.claude/settings.json` that denies `Bash(git push:*)` so the agent doesn't surprise-push, and a skill that knows the house test command. The other teammate is running bare.\n\nThis is the central insight of this stage: **an agent's behavior is a function of its configuration, and configuration is usually invisible.** Nobody reviews it, nobody versions it, nobody diffs it. When the prompt is the only artifact you share, you're sharing the smallest, least important part of the setup. The fix is to treat the *setup itself* — instructions, permissions, hooks, skills — as code: one canonical source, committed, reviewed in PRs, and projected to every engine your team uses (Claude Code and Codex) so switching tools doesn't mean re-teaching each one.\n\nThe rest of this stage is the concrete machinery: what `CLAUDE.md` and `AGENTS.md` are and how to keep them from diverging, what lives in `.claude/`, how `settings.json` permissions and precedence actually evaluate, and the failure modes that bite teams at scale. Throughout, keep one line from the official docs in mind, because it draws the sharpest boundary in the whole stage: \"Settings rules are enforced by the client regardless of what Claude decides to do. CLAUDE.md instructions shape Claude's behavior but are not a hard enforcement layer.\""
  },
  {
   "heading": "CLAUDE.md: the always-loaded instruction file",
   "body": "`CLAUDE.md` is a plain-markdown file Claude Code loads into context at the **start of every session**. It is not a system prompt and not enforced configuration — the docs are explicit: \"CLAUDE.md content is delivered as a user message after the system prompt, not as part of the system prompt itself. Claude reads it and tries to follow it, but there's no guarantee of strict compliance.\" It *shapes* what the agent tries to do; it does not *guarantee* anything. (For hard guarantees you need hooks and permission rules — covered below.)\n\n### What goes in it\nThe load-bearing content is the stuff you'd otherwise re-explain every session. The docs frame the trigger as: add to `CLAUDE.md` when \"Claude makes the same mistake a second time,\" \"a code review catches something Claude should have known,\" or \"a new teammate would need the same context.\"\n- Build/test commands (`npm run check`, `make test`)\n- Project layout (\"API handlers live in `src/api/handlers/`\")\n- Conventions and \"always do X\" rules\n- Architecture decisions and gotchas a new teammate would need\n\nIf an entry is a multi-step *procedure* or only matters for one part of the tree, the docs say to move it to a skill or a path-scoped rule instead — not to grow `CLAUDE.md`.\n\n### Where it can live (load order, broad → specific)\n| Scope | Location | Shared with |\n|---|---|---|\n| Managed policy | `/Library/Application Support/ClaudeCode/CLAUDE.md` (macOS), `/etc/claude-code/CLAUDE.md` (Linux/WSL), `C:\\Program Files\\ClaudeCode\\CLAUDE.md` (Windows) | All users on machine (IT-deployed; cannot be excluded) |\n| User | `~/.claude/CLAUDE.md` | Just you, every project |\n| Project | `./CLAUDE.md` or `./.claude/CLAUDE.md` | The team, via git |\n| Local | `./CLAUDE.local.md` (gitignore it) | Just you, this repo |\n\nClaude walks **up** the directory tree from your cwd and concatenates every `CLAUDE.md` and `CLAUDE.local.md` it finds. Content is ordered root-first (\"from the filesystem root down to your working directory\"), so instructions closest to where you launched are read **last**; within a directory, `CLAUDE.local.md` is appended after `CLAUDE.md`. Subdirectory `CLAUDE.md` files load **on demand** when Claude reads files there — this is the \"context cascade\" that makes per-package instructions work without bloating every session.\n\n### Specificity is the whole game\nThe docs measure adherence by specificity. \"Use 2-space indentation\" beats \"Format code properly\"; \"Run `npm test` before committing\" beats \"Test your changes\"; \"API handlers live in `src/api/handlers/`\" beats \"Keep files organized.\" Write instructions concrete enough to *verify*. Vague rules and — worse — *contradictory* rules across nested files cause Claude to \"pick one arbitrarily.\""
  },
  {
   "heading": "Keep CLAUDE.md short, owned, and reviewed",
   "body": "The dominant anti-pattern at scale is the **over-stuffed, unowned `CLAUDE.md`** — a 600-line file nobody owns, everyone appends to, and that quietly contradicts itself. Two reasons this is corrosive:\n\n1. **Context cost.** `CLAUDE.md` loads *in full* every session regardless of length (the docs: \"CLAUDE.md files are loaded in full regardless of length, though shorter files produce better adherence\"). The docs set an explicit target: **target under 200 lines per file**. Longer files \"consume more context and reduce adherence\" — the irony being that the more you cram in to control the agent, the *less* reliably it follows any of it.\n2. **Rot.** An unowned file accumulates stale rules. When two rules conflict, behavior becomes nondeterministic — \"Claude may pick one arbitrarily.\"\n\n### The three disciplines\n- **Give it an owner.** Someone is responsible for pruning. The test from the docs: add a line when \"Claude makes the same mistake a second time\" or \"a new teammate would need the same context\" — not speculatively.\n- **Review it like code.** Changes to `CLAUDE.md` go through PR review. A new \"always do X\" rule is a behavioral change to every agent session on the team; it deserves the same scrutiny as a code change.\n- **Cascade instead of cramming.** Use a root `CLAUDE.md` for project-wide facts and **`.claude/rules/`** (or per-package files) for the local stuff. Rules go in `.claude/rules/<topic>.md`; a rule *without* a `paths:` frontmatter loads every session (same priority as `.claude/CLAUDE.md`), while a **path-scoped** rule loads only when Claude touches matching files:\n\n```markdown\n---\npaths:\n  - \"src/api/**/*.ts\"\n---\n# API rules\n- All API endpoints must include input validation\n- Use the standard error response format\n```\n\nA path-scoped rule costs zero context until the agent opens a matching file. That's how you keep the always-loaded surface small while still encoding deep, local knowledge. (`paths` accepts glob patterns and brace expansion: `\"src/**/*.{ts,tsx}\"`.)\n\n### Imports for organization\n`CLAUDE.md` can pull in other files with `@path/to/file` syntax. Relative paths resolve against the *importing* file (not cwd); imports recurse to a **maximum depth of four hops**. Caveat from the docs: imported files \"are expanded and loaded into context at launch,\" so imports buy *organization*, not context savings — \"Splitting into `@path` imports helps organization but does not reduce context.\" To mention a path without importing it, wrap it in backticks (`` `@README` ``). The first time a project uses external imports, Claude shows a one-time approval dialog listing the files.\n\n### One thing the docs preserve\nBlock-level HTML comments (`<!-- maintainer notes -->`) are stripped before injection, so you can leave human-only notes that cost zero context. Project-root `CLAUDE.md` is also re-read and re-injected after `/compact` — nested subdirectory files are not, so anything that must survive compaction belongs at the root (or in a hook)."
  },
  {
   "heading": "AGENTS.md: the Codex-facing mirror",
   "body": "`AGENTS.md` is the same idea — a markdown file giving AI coding agents project context — but it's the **open standard** the rest of the ecosystem reads. Per agents.md it is \"stewarded by the Agentic AI Foundation under the Linux Foundation,\" \"used by over 60k open-source projects,\" and read by **25+ tools** including OpenAI Codex, Google Jules, Aider, goose, Cursor, Gemini CLI, VS Code, Devin, and GitHub Copilot's coding agent. The format is deliberately minimal: \"AGENTS.md is just standard Markdown. Use any headings you like; the agent simply parses the text you provide.\" There are no required fields.\n\nFor monorepos, AGENTS.md uses **closest-file-wins**: \"The closest AGENTS.md to the edited file wins; explicit user chat prompts override everything.\" Each subproject ships tailored instructions — the same cascade idea as nested `CLAUDE.md`.\n\n### How Codex actually reads it\nCodex's discovery is concrete and worth knowing, because it shapes how you place files. Per the Codex docs, Codex reads, in order, from your Codex home (`~/.codex` unless `CODEX_HOME` is set) then from the project root down to cwd, checking each directory for `AGENTS.override.md`, then `AGENTS.md`, then fallback names. It \"concatenates files from the root down, joining them with blank lines,\" and \"files closer to your current directory override earlier guidance because they appear later in the combined prompt.\" Instructions are capped at `project_doc_max_bytes` (**32 KiB by default**) before truncation.\n\n### The asymmetry that creates the whole problem\nHere is the fact that drives this stage's design, and it's easy to get wrong:\n\n> **Claude Code reads ONLY `CLAUDE.md`. It does not read `AGENTS.md` — not even as a fallback.** The docs say it directly: \"Claude Code reads `CLAUDE.md`, not `AGENTS.md`.\"\n\nCodex reads `AGENTS.md`. Claude reads `CLAUDE.md`. They do not natively share a file. So a team that uses both engines — or even a single dev who switches between Claude Code and Codex in VS Code — has two instruction files that *will* drift unless you do something about it. (Note: the agents.md tool list does **not** include Claude Code; trust Claude's own docs here — the native reader is `CLAUDE.md`, and the bridge below is how Claude participates in the standard.)\n\nThat \"something\" is the config mirror."
  },
  {
   "heading": "The config mirror: one canonical source, projected",
   "body": "The principle: **keep ONE canonical config and project it to the other engine** rather than hand-maintaining two files that silently fall out of sync. There are two mechanically different ways to bridge `CLAUDE.md` ↔ `AGENTS.md`, and the choice matters for cross-platform teams.\n\n### Option A — the `@AGENTS.md` import (preferred, portable)\nMake `AGENTS.md` the canonical file (it's the open standard, the broader-reach format) and have `CLAUDE.md` import it. This is the exact pattern the Claude docs recommend:\n\n```markdown CLAUDE.md\n@AGENTS.md\n\n## Claude Code\nUse plan mode for changes under `src/billing/`.\n```\n\nClaude \"loads the imported file at session start, then appends the rest.\" This works on **Windows with no admin rights**, recurses up to four hops, and lets you add engine-specific notes below the import. This is the recommended bridge.\n\n### Option B — the symlink\nIf you don't need Claude-specific content, symlink the two — also shown in the Claude docs:\n\n```bash\nln -s AGENTS.md CLAUDE.md\n```\n\nNow both engines read the same bytes. The catch, stated in the docs: \"On Windows, creating a symlink requires Administrator privileges or Developer Mode, so use the `@AGENTS.md` import instead.\" (Mixed-OS teams hit a second snag: git only materializes the symlink as a real link when `core.symlinks` is enabled on the Windows clone; otherwise it checks out as a plain text file containing the path string.) Use the symlink only on all-POSIX teams.\n\n### Comparison\n| | `@AGENTS.md` import | `ln -s AGENTS.md CLAUDE.md` symlink |\n|---|---|---|\n| Canonical file | `AGENTS.md` | `AGENTS.md` |\n| Windows without admin | Works | Needs Admin / Developer Mode |\n| Add Claude-only content | Yes, below the import | No (identical bytes) |\n| Recursion / nesting | Up to 4 hops | N/A |\n| Risk of silent drift | Low (single source) | None (literally one file) |\n\n### The generated-projection variant\nA third pattern, used in this very ecosystem, treats `CLAUDE.md` (or a `relationships.yaml`) as canonical and **deterministically renders** `AGENTS.md` from it — see `project-meta/scripts/meta/render_agents_md.py`, which extracts a fixed set of sections \"without making `AGENTS.md` a second hand-maintained authority.\" Use this when the two files need different *shapes* (e.g., Codex wants a flattened projection, or you want only some sections mirrored), not just different bytes. Whatever you pick, the rule is the same: **exactly one file is authored by hand; the other is derived.**"
  },
  {
   "heading": "The drift check: fail CI when the mirror diverges",
   "body": "A mirror is only as good as the thing that catches it breaking. The discipline that makes \"configure once\" real is a **drift check in CI** that fails the build when the canonical source and its projection diverge. Without it, the import or symlink is a *convention*, and conventions rot.\n\nThis ecosystem ships exactly such a check: `project-meta/scripts/check_agent_surface_sync.py`. It is \"intentionally read-only\" and \"reports whether the workspace root instruction files resolve to the same content and whether Claude/Codex skill discovery directories have obvious mirror drift.\" The mechanism is a content hash: render the projection, hash it, compare to the committed file.\n\n### What a drift gate looks like in CI\n```yaml\n# .github/workflows/agent-config.yml\nname: agent-config\non: [push, pull_request]\njobs:\n  surface-sync:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-python@v5\n        with: { python-version: \"3.12\" }\n      - name: Re-render AGENTS.md and fail on diff\n        run: python scripts/meta/render_agents_md.py --check\n      - name: Hash CLAUDE.md vs AGENTS.md + skill dirs\n        run: python scripts/check_agent_surface_sync.py\n```\n\nThe `--check` mode re-renders the projection and exits non-zero if it differs from what's committed — the same pattern as `black --check` or `prettier --check`. A reviewer who edits `AGENTS.md` but forgets to regenerate (or edits the wrong file) gets a red build, not a teammate three days later wondering why Codex and Claude disagree.\n\n### Why this is the linchpin\nThe payoff is concrete: a dev can switch between Claude Code and Codex in VS Code and get **identical rules**, because CI guarantees the two surfaces can't silently diverge. The drift check is what turns \"we have two instruction files\" from a liability into a single logical source with two physical projections. Note one real limit: if you use the bare `@AGENTS.md` import, `CLAUDE.md` legitimately carries *extra* Claude-only lines below the import — so the check must compare the *resolved/imported* content, not byte-for-byte, or scope the hash to the shared region. A pure symlink sidesteps this (one file, nothing to diff)."
  },
  {
   "heading": "The .claude/ directory: configuration as committed code",
   "body": "`CLAUDE.md` is instructions. The `.claude/` directory is everything *else* an agent needs, stored as **versioned code in the repo**. The decisive fact:\n\n> A committed `.claude/` directory **travels with the repo**. Cloning it (and accepting the workspace-trust dialog) gives a teammate the project's skills, agents, hooks, and `settings.json`. (`code.claude.com/docs/en/skills`, `/settings`)\n\nThat single sentence is what makes setup shareable. The teammate who clones doesn't configure anything — they accept one trust prompt and inherit the whole team's agent setup.\n\n### Layout\n```text\nyour-project/\n├── CLAUDE.md                    # always-loaded instructions (or @AGENTS.md import)\n├── AGENTS.md                    # canonical, Codex-facing\n└── .claude/\n    ├── settings.json            # committed: permissions, hooks, env (team)\n    ├── settings.local.json      # gitignored: personal overrides\n    ├── rules/                   # instruction files (path-scoped or always-on)\n    │   ├── testing.md\n    │   └── api-design.md\n    ├── skills/                  # reusable procedures (SKILL.md per skill)\n    │   └── run-migrations/\n    │       └── SKILL.md\n    └── agents/                  # subagent definitions\n        └── reviewer.md\n```\n\n### What's shared vs. personal\n- **Committed** (`.claude/settings.json`, `rules/`, `skills/`, `agents/`): the team contract. Reviewed in PRs.\n- **Gitignored** (`.claude/settings.local.json`, `CLAUDE.local.md`): personal overrides — your sandbox URLs, your extra allow-rules, your preferred model. These layer *on top* of the shared config without forcing your preferences on the team.\n\nThe trust dialog matters and is load-bearing: because `.claude/` can contain hooks (arbitrary shell commands) and tool-granting skills, Claude Code gates a freshly-cloned `.claude/` behind a one-time workspace-trust prompt. The docs tie multiple capabilities to that gate — a project skill's `allowed-tools` \"takes effect after you accept the workspace trust dialog for that folder, the same as permission rules in `.claude/settings.json`,\" and `autoMemoryDirectory` set in project settings is \"honored only after you accept the workspace trust dialog.\" That's the deliberate friction that keeps a malicious repo from auto-executing a hook or self-granting broad tool access on clone — accept it knowingly, the same way you'd review a `Makefile` before running `make`."
  },
  {
   "heading": "settings.json: permissions, hooks, and env",
   "body": "`.claude/settings.json` is where the *enforceable* configuration lives — the part that, unlike `CLAUDE.md`, does not depend on the model deciding to cooperate. It has three load-bearing sections.\n\n```json\n{\n  \"$schema\": \"https://json.schemastore.org/claude-code-settings.json\",\n  \"permissions\": {\n    \"allow\": [\"Bash(npm run test:*)\", \"Bash(npm run lint)\"],\n    \"ask\":   [\"Bash(git push:*)\"],\n    \"deny\":  [\"Read(./.env)\", \"Read(.env)\", \"Bash(curl:*)\"]\n  },\n  \"env\": {\n    \"NODE_ENV\": \"test\"\n  },\n  \"hooks\": {\n    \"PreToolUse\": [\n      { \"matcher\": \"Edit\", \"hooks\": [{ \"type\": \"command\", \"command\": \".claude/hooks/protect.sh\" }] }\n    ]\n  }\n}\n```\n\n- **`permissions`** — the allow/ask/deny rules controlling which tools the agent may use and which files/domains it may touch. The detail of how these evaluate is the next section; it's the most consequential and most misunderstood part.\n- **`env`** — environment variables injected into the session. Use this for things every teammate's agent needs set identically (telemetry flags, a test-mode env). Don't put secrets here — `settings.json` is committed.\n- **`hooks`** — shell commands fired at lifecycle events (e.g. `PreToolUse`, `SessionStart`). Hooks are the *enforcement* layer: a `PreToolUse` hook that exits with code 2 \"stops the tool call before permission rules are evaluated,\" so the block applies even when an allow rule would otherwise let the call through. This is how you make \"never edit `migrations/` directly\" a hard wall instead of a hope. (Most settings hot-reload — the docs note `permissions`, `hooks`, and credential helpers reload without a restart; `model` and `outputStyle` are read once at session start.)\n\n### Why both CLAUDE.md and settings.json\nThey're complementary, and the docs draw the line sharply in a table: use **CLAUDE.md for behavioral guidance** (\"Code style and quality guidelines\") and **managed/settings for technical enforcement** (\"Block specific tools, commands, or file paths\"). `CLAUDE.md` asks; `settings.json` deny-rules and hooks *enforce*. If you find yourself writing \"NEVER push to main\" in `CLAUDE.md` and hoping, you want an `ask`/`deny` rule like `Bash(git push origin main)` or a `PreToolUse` hook instead."
  },
  {
   "heading": "Permission rules: syntax and the deny→ask→allow order",
   "body": "Permission rules are the safety surface, so the exact semantics matter. A rule is `Tool` or `Tool(specifier)`.\n\n### Specifier syntax\n| Rule | Effect |\n|---|---|\n| `Bash` (bare) | Matches *all* Bash; as a deny rule it \"removes the tool from Claude's context entirely\" |\n| `Bash(npm run build)` | Exact command match |\n| `Bash(npm run test:*)` | Trailing wildcard — `:*` at the end is \"equivalent to a trailing ` *`\" (enforces a word boundary) |\n| `Read(.env)` / `Read(**/.env)` | Equivalent; any `.env` at or under cwd (gitignore semantics, matches at any depth) |\n| `Read(/src/**)` | Project-root-relative — note `/path` is the **project root**, NOT the filesystem root (`//path` is absolute) |\n| `WebFetch(domain:example.com)` | Fetch requests to that hostname |\n| `mcp__puppeteer__*` | All tools from the `puppeteer` MCP server |\n\nThe space before `*` is load-bearing: `Bash(ls *)` matches `ls -la` but **not** `lsof`; `Bash(ls*)` matches both. Claude Code is shell-aware — `Bash(safe-cmd *)` does **not** authorize `safe-cmd && rm -rf .`, because \"a rule must match each subcommand independently\" (recognized separators: `&&`, `||`, `;`, `|`, `|&`, `&`, newlines). It also strips a fixed set of process wrappers before matching, so `Bash(npm test *)` also matches `timeout 30 npm test` (wrappers: `timeout`, `time`, `nice`, `nohup`, `stdbuf`, and bare `xargs`).\n\n### Evaluation order (memorize this)\nFrom the docs verbatim: \"Rules are evaluated in order: deny, then ask, then allow. The first match in that order determines the outcome, and rule specificity does not change the order.\" Consequences:\n- A broad `deny` like `Bash(aws *)` blocks *everything* matching, \"including calls that also match a narrower allow rule like `Bash(aws s3 ls)`, so a deny rule cannot carry allowlist exceptions.\"\n- \"A matching ask rule prompts even when a more specific allow rule also matches.\"\n- Permission rules are \"enforced by Claude Code, not by the model\" — so unlike `CLAUDE.md`, a deny rule is a real wall.\n\n### Fragile-pattern gotcha\nDon't try to constrain command *arguments* with Bash patterns — the docs flag this as fragile by design: `Bash(curl http://github.com/ *)` is trivially bypassed by `curl -X GET …`, `https://`, redirects, or a shell variable. There's a hard version of this too: a rule like `Bash(command:rm *)` (param-matching on `command`) is \"bypassable by a compound command, so Claude Code ignores it and emits a startup warning.\" For URL control the docs prescribe: **deny `curl`/`wget` entirely and use `WebFetch(domain:…)` allow-rules**, or enforce with a `PreToolUse` hook. Note `WebFetch` alone does not block the network — if Bash is allowed, Claude can still curl anywhere. Watch one more env-runner trap: `Bash(devbox run *)` (or `npx`, `docker exec`) matches *whatever comes after `run`*, including `devbox run rm -rf .`, because those runners aren't on the stripped-wrapper list — write `Bash(devbox run npm test)` instead."
  },
  {
   "heading": "Settings precedence: merge vs. override, and the deny floor",
   "body": "Settings come from multiple scopes, and the merge rules differ by setting type — this trips up teams constantly.\n\n### Precedence for most settings (override, highest wins per key)\nFrom the docs, highest to lowest:\n1. **Managed settings** — \"cannot be overridden by any other level, including command line arguments\"\n2. **Command-line arguments** — temporary session overrides\n3. **Local** (`.claude/settings.local.json`)\n4. **Project** (`.claude/settings.json`)\n5. **User** (`~/.claude/settings.json`)\n\nSo your personal `settings.local.json` overrides the committed project `settings.json` for scalar keys like `model` — which is exactly what you want for personal overrides that don't touch the team. (Managed `settings.json` lives in `managed-settings.json` at `/Library/Application Support/ClaudeCode/`, `/etc/claude-code/`, or `C:\\Program Files\\ClaudeCode\\` — a *different* filename from the managed `CLAUDE.md`.)\n\n### Permission rules are the exception (merge)\nThe `allow`/`ask`/`deny` arrays **concatenate across all scopes** rather than override. The critical consequence, stated verbatim in the docs:\n\n> \"If a tool is denied at any level, no other level can allow it. For example, a managed settings deny cannot be overridden by `--allowedTools`… a user-level deny blocks a project-level allow, because deny rules from any scope are evaluated before allow rules.\"\n\nA higher-scope **deny is a floor no lower scope can relax.** This is a feature: an org can deploy a managed `deny` for `Read(./.env)` and *no* engineer's local settings can re-enable it. You build security *up* by adding deny rules at higher scopes, and you can't accidentally weaken them from below. (To go further, managed settings can set `allowManagedPermissionRulesOnly: true`, which makes *only* managed allow/ask/deny rules apply at all.)\n\n### Worked example\n| Scope | Rule | Result for `git push origin main` |\n|---|---|---|\n| Project `settings.json` | `allow: [\"Bash(git push:*)\"]` | would allow… |\n| User `settings.json` | `deny: [\"Bash(git push * main)\"]` | …but deny is evaluated first → **blocked** |\n\nDeny-first + merge means the user's deny beats the project's allow. The agent cannot push to main no matter what the repo's committed settings say."
  },
  {
   "heading": "Hooks and skills: the rest of what travels in .claude/",
   "body": "Two more `.claude/` surfaces round out the shared setup, because committing them is what makes the *whole* workflow reproducible — not just the rules.\n\n### Hooks — enforcement and automation\nHooks are shell commands wired to lifecycle events in `settings.json`. They run *as the harness*, not as the model, so they're how you enforce things the model can't be trusted to do every time:\n- `PreToolUse` — runs before a tool call; **exit code 2 blocks it**, and the docs are explicit that this \"stops the tool call before permission rules are evaluated,\" overriding even an allow rule. This is the \"block edits to protected files\" pattern.\n- `SessionStart` / `SessionEnd` — bootstrap env, emit telemetry.\n\nOne subtlety worth knowing: a `PreToolUse` hook can also *return* `\"allow\"`/`\"ask\"` decisions, but those \"do not bypass permission rules\" — a matching deny or ask rule still fires. So hooks can tighten or auto-approve, but they cannot loosen a deny. Because hooks execute arbitrary commands, they're exactly why the clone trust-dialog exists.\n\n### Skills — reusable procedures, loaded on demand\nA skill is a folder under `.claude/skills/<name>/` with a `SKILL.md` whose YAML frontmatter declares when it should fire. Per the docs, \"Every skill needs a `SKILL.md` file with two parts: YAML frontmatter between `---` markers that tells Claude when to use the skill, and markdown content with the instructions.\" Unlike `CLAUDE.md` (always loaded in full), a skill's *description* sits in context but its **body loads only when invoked or judged relevant**, so it costs almost no baseline context:\n\n```markdown\n---\nname: run-migrations\ndescription: Run and verify DB migrations. Use when the user adds a model field or mentions Alembic/schema changes.\nallowed-tools: Bash(make migrate-gen *) Bash(make migrate-up)\n---\n# Run migrations\n1. `make migrate-gen MSG=\"<change>\"`\n2. Review the generated migration\n3. `make migrate-up` and confirm the schema diff\n```\n\nThe directory name (`run-migrations/`) becomes the `/run-migrations` command; `description` is what Claude reads to decide when to auto-load (and is truncated at 1,536 characters in the listing, so put the key use case first). Only `description` is recommended; all other frontmatter fields are optional. Skills follow the Agent Skills open standard (agentskills.io). Committing skills means the procedure your senior engineer figured out once is available to every teammate's agent automatically. To mirror skills onto Codex's surface, the ecosystem pattern is a **portable source** (e.g. `~/.agents/skills`) symlinked into both `.claude/skills/` and Codex's directory — with the same drift check guarding it. This is the skill-level analogue of the `CLAUDE.md` ↔ `AGENTS.md` mirror: one source, projected to each engine, CI-verified."
  },
  {
   "heading": "When to use what",
   "body": "A decision table for \"where does this piece of setup belong\":\n\n| You want to… | Put it in | Why |\n|---|---|---|\n| State a convention every session should know | `CLAUDE.md` / `AGENTS.md` | Always-loaded behavioral guidance |\n| Encode a deep rule for one package only | `.claude/rules/*.md` with `paths:` | Loads only when matching files are touched — saves context |\n| **Block** a command/path no matter what | `settings.json` `permissions.deny` (or managed) | Enforced by the client, not the model; deny is evaluated first |\n| Require confirmation for risky ops | `settings.json` `permissions.ask` | e.g. `Bash(git push:*)` |\n| Make a routine check automatic and unbypassable | `settings.json` `hooks` (`PreToolUse`) | Runs as the harness; exit 2 blocks before rules evaluate |\n| Package a multi-step procedure | `.claude/skills/<name>/SKILL.md` | Loads on demand, near-zero baseline cost |\n| Personal override (model, sandbox URL, extra allow) | `settings.local.json` / `CLAUDE.local.md` (gitignored) | Layers on top without touching the team |\n| Org-wide non-negotiable | Managed settings (`managed-settings.json`) / managed `CLAUDE.md` | Can't be relaxed or excluded by any lower scope |\n\n### The cross-engine layer (orthogonal to the above)\nFor *any* of the instruction/skill surfaces: author **one** canonical file, project it to the other engine (import, symlink, or generated render), and gate it with a **CI drift check**. That's what makes the whole table true for both Claude Code and Codex simultaneously."
  },
  {
   "heading": "Common pitfalls and gotchas",
   "body": "### 1. The 600-line CLAUDE.md\nThe most common failure. It loads in full every session, costs context, and *reduces* adherence. Symptom: the agent ignores rules buried mid-file. Fix: cap at ~200 lines (the docs' explicit target), push depth into `.claude/rules/` (path-scoped) and skills, give the file an owner.\n\n### 2. Putting enforcement in CLAUDE.md\n\"NEVER push to main\" in `CLAUDE.md` is a *suggestion* — it's \"delivered as a user message,\" not a guarantee, with \"no guarantee of strict compliance.\" The agent will mostly comply and occasionally won't. Real enforcement is `permissions.deny` or a `PreToolUse` hook. The docs say it plainly: \"To block an action regardless of what Claude decides, use a PreToolUse hook instead.\"\n\n### 3. Assuming Claude reads AGENTS.md\nIt doesn't — *ever*, not even as a fallback (\"Claude Code reads `CLAUDE.md`, not `AGENTS.md`\"). A repo with only `AGENTS.md` gives Claude Code **zero** project instructions. You must bridge with `@AGENTS.md` import or a symlink, or Claude runs blind while Codex runs informed. This is the silent version of the two-teammates bug. (Also: agents.md's own tool list doesn't name Claude Code, so don't expect it to \"just work.\")\n\n### 4. Symlink on Windows\n`ln -s AGENTS.md CLAUDE.md` requires Admin/Developer Mode on Windows (per the docs), and git needs `core.symlinks` enabled on the clone or the link checks out as a plain text file containing the path string. On a mixed-OS team, use the `@AGENTS.md` import.\n\n### 5. Expecting a deny exception\n`deny: [\"Bash(aws *)\"]` + `allow: [\"Bash(aws s3 ls)\"]` does **not** carve out an exception — \"a deny rule cannot carry allowlist exceptions,\" so `aws s3 ls` is blocked. Model your deny rules as absolute floors, and remember a higher-scope deny can't be relaxed from below.\n\n### 6. Committing secrets in settings.json env\n`settings.json` is committed. `env` values land in git. Keep secrets out; use `settings.local.json` (gitignored) or real secret management.\n\n### 7. Trying to constrain arguments with Bash patterns\n`Bash(curl … *)` is fragile (bypassable by flags/protocol/redirects/variables), and the param form `Bash(command:rm *)` is *ignored* with a startup warning. Env-runner rules like `Bash(devbox run *)` or `Bash(npx *)` silently authorize anything after `run`. Constrain at the tool level (`WebFetch(domain:…)`, deny `curl`/`wget`) or with a hook — not with argument globs.\n\n### 8. No drift check\nA mirror with no CI gate *will* diverge — someone edits the wrong file, regenerates nothing, and three days later the two engines disagree. The drift check (`render --check` + `check_agent_surface_sync.py`) is not optional polish; it's the thing that makes \"configure once\" hold under a team's edit pressure.\n\n### 9. Forgetting the clone trust dialog in onboarding\nA teammate clones, the `.claude/` config is right there, but until they accept the workspace-trust prompt, hooks, project-skill `allowed-tools`, and trusted local settings (`autoMemoryDirectory`) don't activate. Document this one-time step in onboarding so new hires don't run with half the setup silently disabled."
  }
 ],
 "stage-3": [
  {
   "heading": "The itch this stage scratches",
   "body": "You notice it the tenth time you paste the same five-step deploy checklist into chat. Or when a teammate force-pushes to `main` *again* despite the line in `CLAUDE.md` that says not to. Or when you realize half your context window is gone before you've typed a word, eaten by an MCP server you wired up last week and forgot about.\n\nThose are distinct itches, and they map to distinct mechanisms. Getting the mapping right is the whole game:\n\n- **\"I keep re-describing a *procedure*\"** → a **skill**. On-demand, model-invoked, loaded only when relevant.\n- **\"A rule must *always* hold, no matter what the model decides\"** → a **hook**. Deterministic, event-driven, can hard-block.\n- **\"I want a *bounded* sub-task done without polluting my main context\"** → a **sub-agent**. Own window, least-privilege tools, returns a summary.\n- **\"I need Claude to *reach into* an external system\"** → an **MCP server**. A typed gateway to GitHub, a database, a tracker.\n\nThe failure mode is reaching for the wrong one: encoding a non-negotiable policy as advisory `CLAUDE.md` prose (it gets ignored under load), or bolting a 40-tool MCP server onto every session when a CLI call would do. This stage goes deep on each primitive, its real file format, and the decision boundary between them. Everything below is the actual on-disk format as of mid-2026 — verified against `code.claude.com/docs`, `developers.openai.com/codex`, and `agentskills.io`, not from memory."
  },
  {
   "heading": "Skills: a capability whose description is its trigger",
   "body": "A **skill** is an on-demand capability packaged as a directory with a `SKILL.md` entrypoint. The defining property: **it is model-invoked via its description.** At session start Claude loads only each skill's `name` + `description` (cheap). When your request matches a description, Claude loads the full `SKILL.md` body and follows it. You can also fire it explicitly with `/skill-name`.\n\nThe docs give a one-line decision rule (code.claude.com/docs/en/skills): create a skill *\"when you keep pasting the same instructions, checklist, or multi-step procedure into chat, or when a section of CLAUDE.md has grown into a procedure rather than a fact.\"* A fact belongs in `CLAUDE.md`; a *procedure* belongs in a skill, because the skill's body loads only when used, so long reference material costs almost nothing until you need it.\n\nNote: in Claude Code, **custom commands have been merged into skills.** A file at `.claude/commands/deploy.md` and a skill at `.claude/skills/deploy/SKILL.md` both create `/deploy`; the skill form just adds a directory for supporting files, frontmatter for invocation control, and automatic model invocation.\n\n### File layout\n\n```text\n.claude/skills/\n└── summarize-changes/\n    ├── SKILL.md          # required entrypoint\n    ├── reference.md      # optional, loaded only when referenced\n    └── scripts/\n        └── validate.sh    # optional, executed not loaded\n```\n\nThe **directory name** becomes the `/command` you type. The frontmatter `name` is only a display label shown in listings — *except* at a plugin root, where there is no skill directory and `name` does set the command.\n\n### SKILL.md frontmatter\n\nThe docs' canonical first example carries only a `description` — every other field is optional:\n\n```yaml\n---\ndescription: Summarizes uncommitted changes and flags anything risky. Use when the user asks what changed, wants a commit message, or asks to review their diff.\n---\n\n## Current changes\n\n!`git diff HEAD`\n\n## Instructions\n\nSummarize the changes above in two or three bullet points, then list any risks.\n```\n\nThe `` !`git diff HEAD` `` line is **dynamic context injection**: Claude Code runs the command *before* the model sees the body and inlines the output (this is preprocessing — Claude never executes it). For multi-line, use a ` ```! ` fenced block. The inline form only fires when `!` starts a line or follows whitespace; `KEY=!`cmd`` is left literal."
  },
  {
   "heading": "Skill frontmatter fields that actually matter",
   "body": "Every frontmatter field is optional; only `description` is *recommended* (without it Claude has nothing to match on, and falls back to the first paragraph of the body). Here are the fields you will actually reach for (code.claude.com/docs/en/skills, frontmatter reference):\n\n| Field | Effect |\n|-------|--------|\n| `description` | The trigger. Put the key use case first. Combined `description` + `when_to_use` is truncated at **1,536 chars** in the listing (configurable via `maxSkillDescriptionChars`). |\n| `when_to_use` | Extra trigger phrases / example requests, appended to `description`; counts toward the 1,536-char cap. |\n| `disable-model-invocation: true` | Only *you* can invoke (`/name`); description is not even loaded into context. Use for side-effecting workflows — `/deploy`, `/commit` — you don't want Claude auto-running. |\n| `user-invocable: false` | Hide from the `/` menu; only *Claude* invokes. Use for background knowledge that isn't a meaningful user command. |\n| `allowed-tools` | Pre-approve tools while the skill is active (e.g. `Bash(git add *) Bash(git commit *)`). Does **not** restrict the pool — it just skips the permission prompt. |\n| `disallowed-tools` | *Removes* tools from the pool while the skill is active (e.g. `AskUserQuestion` for a background loop). Clears on your next message. |\n| `context: fork` + `agent` | Run the skill in a sub-agent's isolated context (see the sub-agent section). |\n| `model` / `effort` | Override the model or effort level for the rest of the turn while the skill is active. |\n| `argument-hint` / `arguments` | Autocomplete hint and named `$arg` substitution. |\n| `paths` | Glob patterns; auto-activate the skill only when working on matching files. |\n\n### Arguments\n\nArguments arrive via `$ARGUMENTS` (full string), `$ARGUMENTS[0]` / shorthand `$0` (0-based positional), or named `$issue` when declared in `arguments:`. Indexed args use shell-style quoting — `/my-skill \"hello world\" second` makes `$0` = `hello world`. If you pass args but the body has no placeholder, Claude Code appends `ARGUMENTS: <value>` so the model still sees them. Other useful substitutions: `${CLAUDE_SKILL_DIR}` (resolves bundled-script paths regardless of cwd), `${CLAUDE_SESSION_ID}`, `${CLAUDE_EFFORT}`.\n\n### Pitfall: malformed frontmatter\n\nIf the YAML is malformed, Claude Code loads the body with **empty metadata** — `/name` still works, but there's no `description` to match, so model invocation silently never fires. Run `claude --debug` to surface the parse error."
  },
  {
   "heading": "Progressive disclosure: why a lean skill stays cheap",
   "body": "**Progressive disclosure** is the mechanism that lets you have dozens of skills without drowning context. It works in three tiers (code.claude.com/docs/en/skills, docs.claude.com agent-skills overview):\n\n1. **Discovery** — only `name` + `description` are in context for every skill. This is what the model matches against.\n2. **Activation** — when a skill is selected, the full `SKILL.md` content enters the conversation as a single message and *stays for the rest of the session*. Claude Code does **not** re-read the file on later turns.\n3. **Bundled references** — files like `reference.md` load only when Claude actually opens them, because you pointed at them from `SKILL.md`.\n\n### The two design consequences\n\n**Keep the body lean.** Once activated, the body is a recurring per-turn token cost — it just sits in context. The docs cap the recommendation at **500 lines** for `SKILL.md` and tell you to push detail into separate files:\n\n```markdown\n## Additional resources\n- For the full API surface, see [reference.md](reference.md)\n- For worked examples, see [examples.md](examples.md)\n```\n\nClaude loads `reference.md` only if the task needs it. A 4,000-line API spec next to a 60-line `SKILL.md` costs nothing until the moment it's relevant. (One subtlety: after auto-compaction, Claude Code re-attaches only the most recent invocation of each skill, keeping the first 5,000 tokens, under a shared 25,000-token budget — so a huge body can be silently truncated post-compaction. Re-invoke to restore it.)\n\n**Mind the listing budget.** All skill *names* are always listed, but descriptions share a budget that scales at **~1% of the model's context window**. Raise it with the `skillListingBudgetFraction` setting (e.g. `0.02` = 2%) or the `SLASH_COMMAND_TOOL_CHAR_BUDGET` env var (fixed char count). When it overflows, the descriptions of the *least-used* skills get dropped first — silently stripping the keywords Claude needs to match them. Run `/doctor` to see what's being shortened or dropped. The lesson: front-load the trigger phrase in every `description`.\n\n### Gotcha: triggering vs. working\n\nSeeing a skill fire only tells you the description matched — not that the body did its job. If a skill seems to stop influencing behavior, the content is usually still present and the model is just choosing other tools; strengthen the `description`/body, or escalate to a hook for true enforcement. The docs prescribe a **baseline comparison**: run the same realistic prompts in a fresh session with the skill enabled and disabled, and compare. The official `skill-creator` plugin automates this A/B loop with per-case sub-agent isolation, writing pass-rate vs. token-overhead to `benchmark.json`."
  },
  {
   "heading": "Portable skills: author once, mirror into each tool",
   "body": "Your team runs both Claude Code and Codex in VS Code. The good news: Agent Skills use the **open `SKILL.md` standard** that both tools build on (agentskills.io, developers.openai.com/codex/skills, code.claude.com/docs/en/skills). The catch: **each tool reads only its own skills directory.** There is no shared resolver.\n\n| Tool | Skills directory (project) | Skills directory (user) | Invocation |\n|------|---------------------------|------------------------|------------|\n| Claude Code | `.claude/skills/<name>/SKILL.md` | `~/.claude/skills/` | `/name` or model-matched on `description` |\n| Codex | `.agents/skills/<name>/SKILL.md` | `~/.agents/skills/` | `/skills`, `$name`, or model-matched on `description` |\n\nCodex's search order is: repo-level `.agents/skills` (current dir walking up to repo root) → `$HOME/.agents/skills` → `/etc/codex/skills` (admin) → built-ins. It uses the same progressive-disclosure model: name+description first (the initial skills list is capped at **~2% of context, or 8,000 characters**), full body on selection. Both tools require `name` + `description` frontmatter; same-named skills are *not* merged across scopes — both can appear in selectors.\n\n### Portability = author once, mirror into each dir\n\nThe core `SKILL.md` (frontmatter `name` + `description` + body) is identical across both tools. A **portable skill** is one written in that common subset and made discoverable in each tool's directory. The robust pattern: keep a single canonical source and project it.\n\n```text\n~/.agents/skills/             # canonical source of truth\n└── summarize-changes/\n    └── SKILL.md\n\n# Project it into each tool's discovery path (symlink or copy):\nln -s ~/.agents/skills/summarize-changes .claude/skills/summarize-changes\nln -s ~/.agents/skills/summarize-changes .agents/skills/summarize-changes\n```\n\n### Gotchas\n\n- **Claude-only frontmatter doesn't port.** `context: fork`, `disable-model-invocation`, `allowed-tools`, `disallowed-tools`, `argument-hint`, `paths` are Claude Code extensions to the standard. Codex ignores them. Keep portable skills to the common subset, or accept tool-specific behavior.\n- **A new top-level skills directory needs a restart.** Claude Code live-watches *existing* skill dirs (`~/.claude/skills/`, project `.claude/skills/`) for edits, but creating a *new* top-level dir mid-session requires restarting so it can be watched.\n- **Drift is invisible until it bites.** If you copy rather than symlink, the two dirs silently diverge. Gate it in CI — see the last section."
  },
  {
   "heading": "Sub-agents: a bounded mission in its own context",
   "body": "A **sub-agent** *\"runs in its own context window with a custom system prompt, specific tool access, and independent permissions,\"* and *\"returns only the summary to the main context\"* (code.claude.com/docs/en/sub-agents). That last clause is the point: the sub-agent can read 40 files, run 12 greps, and burn 30k tokens — and your main conversation gets back three paragraphs, not the transcript.\n\nUse one when a side task would flood your main window with search results, logs, or file contents you won't reference again. Define a *reusable* one when you keep spawning the same kind of worker with the same instructions. (Claude Code also ships built-ins: `Explore` — Haiku, read-only, for codebase search; `Plan` — read-only research; `general-purpose` — all tools. `Explore`/`Plan` skip your CLAUDE.md and git status to stay cheap.)\n\n### File format\n\nMarkdown with YAML frontmatter, in `.claude/agents/` (project) or `~/.claude/agents/` (user). Only `name` and `description` are required:\n\n```yaml\n---\nname: safe-researcher\ndescription: Research agent with restricted, read-only capabilities. Use for codebase exploration that should not write files.\ntools: Read, Grep, Glob, Bash\nmodel: haiku\n---\n\nYou are a read-only research agent. Find relevant files, read them,\nand return a summary with specific file:line references. Never edit.\n```\n\nThe **markdown body becomes the sub-agent's entire system prompt** — it does not inherit the main Claude Code system prompt, only basic environment details.\n\n### The three levers\n\n| Lever | Field | Why it matters |\n|-------|-------|----------------|\n| **Least-privilege tools** | `tools:` (allowlist) or `disallowedTools:` (denylist) | Omit `tools` and the sub-agent inherits *all* tools. Set `tools: Read, Grep, Glob` to lock it to exactly the mission — never `Write` or `Edit`. Or invert with `disallowedTools: Write, Edit` to keep everything else. This is enforcement, not a suggestion. |\n| **Cheaper model** | `model:` | `sonnet` / `opus` / `haiku` / `fable` / a full ID / `inherit` (default). Route bounded grunt work to `haiku`; reserve the strong model for the main thread. |\n| **Context isolation** | (inherent) | The sub-agent's reads never enter your main window. Only its return summary does. |\n\n### Two ways skills and sub-agents compose\n\n- **Skill with `context: fork`** — the skill *body* becomes the task, and `agent: Explore` (or a custom agent) provides the execution environment. The forked context sees only the SKILL.md content, not your conversation history. If omitted, `agent` defaults to `general-purpose`.\n- **Sub-agent with a `skills:` field** — a custom sub-agent that *preloads* skills as reference material at startup. Per the docs, the **full skill content is injected, not just the description** (the inverse of a normal session).\n\n### Pitfalls\n\n- **Don't fork a skill that has no task.** `context: fork` on a pure-guidelines skill (\"use these API conventions\") gives the sub-agent rules but no actionable prompt — it returns nothing useful. (This warning is verbatim in the docs.)\n- **The implementer anti-pattern.** Sub-agents shine as *researchers* (read, summarize, return). Handing a sub-agent a large *implementation* task is risky: it can't ask you clarifying questions mid-flight, and its edits land without your review of the reasoning. Prefer: sub-agent gathers context → returns summary → main thread implements. (See the `/design-subagents` skill.)\n- **No conversation memory.** Each invocation starts cold. Pass everything it needs in the delegation, or via files on disk."
  },
  {
   "heading": "Hooks: enforcement that fires no matter what",
   "body": "Here is the single most important distinction in this stage. `CLAUDE.md` is **advisory** — the model reads it and *usually* complies, but under load or distraction it can drift. A **hook** is a command (or other handler) that runs on a lifecycle event and **fires no matter what the model decides** (code.claude.com/docs/en/hooks). If a rule must be true 100% of the time, it is a hook, not a sentence in a markdown file. Hooks fire deterministically on event + matcher conditions and can hard-block.\n\n### Configuration shape (settings.json)\n\n```json\n{\n  \"hooks\": {\n    \"PreToolUse\": [\n      {\n        \"matcher\": \"Bash\",\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"if\": \"Bash(git push --force *)\",\n            \"command\": \"${CLAUDE_PROJECT_DIR}/.claude/hooks/block-force-push.sh\",\n            \"timeout\": 10\n          }\n        ]\n      }\n    ]\n  }\n}\n```\n\nThree nesting levels: `EventName` → `matcher` (which tool/source) → `hooks[]` (the handlers). Settings locations, in resolution order **managed policy → project → local → user → plugin → skill/agent**: `~/.claude/settings.json` (user), `.claude/settings.json` (shareable, checked in), `.claude/settings.local.json` (gitignored), plugin `hooks/hooks.json`, or skill/agent frontmatter.\n\n### Matchers and the `if` field\n\nFor tool events the matcher is the tool name: `\"Bash\"`, `\"Edit|Write\"` (pipe-separated list), or a JS regex like `\"mcp__github__.*\"`. `\"*\"`, `\"\"`, or omitting it matches all. MCP tools follow `mcp__<server>__<tool>`. Non-tool events match other things — `SessionStart` matches `startup`/`resume`/`clear`/`compact`; `SubagentStart` matches the agent type. The per-handler **`if`** field narrows further using permission-rule syntax (`\"Bash(rm *)\"`, `\"Edit(*.ts)\"`); it strips leading `VAR=value` assignments and inspects `$()`/backtick subcommands, and is only evaluated on tool events.\n\n### How a hook hard-blocks\n\nTwo mechanisms. **Exit code 2** blocks the action and feeds stderr back to Claude. Or, on exit 0, emit structured JSON:\n\n```bash\n#!/bin/bash\nCOMMAND=$(jq -r '.tool_input.command')\nif echo \"$COMMAND\" | grep -qE 'push .*(--force|-f)\\b'; then\n  jq -n '{\n    hookSpecificOutput: {\n      hookEventName: \"PreToolUse\",\n      permissionDecision: \"deny\",\n      permissionDecisionReason: \"Force-push to remote is blocked by policy.\"\n    }\n  }'\nelse\n  exit 0   # no decision; normal permission flow applies\nfi\n```\n\n| Exit code | Meaning |\n|-----------|---------|\n| `0` | Success. stdout parsed for JSON control output. |\n| `2` | **Blocking error.** stderr fed to Claude; action blocked. JSON ignored. |\n| other | Non-blocking error. stderr shown to user; execution continues. |\n\nNote that conventional Unix `exit 1` is *non-blocking* here — only `2` blocks. The `PreToolUse` `permissionDecision` accepts `allow|deny|ask|defer`, and can even rewrite the call via `updatedInput`. Top-level-decision events (`UserPromptSubmit`, `Stop`, `PostToolBatch`) use `{\"decision\": \"block\", \"reason\": \"...\"}` instead."
  },
  {
   "heading": "Hooks: events, handler types, and the patterns that pay off",
   "body": "Most people know `PreToolUse` / `PostToolUse`, but the event surface is far wider (code.claude.com/docs/en/hooks):\n\n- **Session**: `SessionStart`, `Setup`, `SessionEnd`, `InstructionsLoaded`\n- **Per-turn**: `UserPromptSubmit`, `UserPromptExpansion`, `Stop`, `StopFailure`\n- **Tool loop**: `PreToolUse`, `PermissionRequest`, `PermissionDenied`, `PostToolUse`, `PostToolUseFailure`, `PostToolBatch`\n- **Async/env**: `FileChanged`, `CwdChanged`, `ConfigChange`, `Notification`, `MessageDisplay`, `WorktreeCreate`, `WorktreeRemove`\n- **Sub-agent/task**: `SubagentStart`, `SubagentStop`, `TaskCreated`, `TaskCompleted`, `TeammateIdle`\n- **MCP**: `Elicitation`, `ElicitationResult`\n- **Compaction**: `PreCompact`, `PostCompact`\n\nAnd handlers are not limited to shell commands. The `type` can be:\n\n| `type` | What it does |\n|--------|--------------|\n| `command` | Shell command; JSON on stdin, control via exit code / stdout JSON |\n| `http` | POST the event JSON to a URL; response drives the decision |\n| `mcp_tool` | Call a tool on a configured MCP server |\n| `prompt` | Single-turn LLM evaluation for a yes/no decision |\n| `agent` | Spawn a sub-agent with tool access (**experimental**) |\n\nThat means a lifecycle event can fire an LLM prompt or even a sub-agent, not just a script.\n\n### High-value patterns\n\n- **Format/lint on write** — `PostToolUse` matching `Edit|Write` runs `prettier`/`ruff` so the model never sees unformatted code it then has to re-fix.\n- **Block dangerous commands** — `PreToolUse` matching `Bash` with `if: \"Bash(rm -rf *)\"` denies destructively.\n- **Inject context at prompt time** — `UserPromptSubmit` returns `hookSpecificOutput.additionalContext` to prepend live state (current branch, failing tests) to every prompt.\n- **Enforce \"never end with uncommitted changes\"** — a `Stop` hook with `{\"decision\": \"block\"}` if `git status --short` is non-empty.\n\n### Skill vs. hook — the decision\n\n| | Skill | Hook |\n|---|-------|------|\n| Invocation | Model decides (description match) or `/name` | Deterministic on event+matcher |\n| Can it be skipped? | Yes — model may not invoke it | No — fires regardless |\n| Good for | *Procedures* you want applied when relevant | *Policies* that must always hold |\n| Failure if misused | Advisory rule silently ignored | Over-blocking that frustrates real work |\n\nRule of thumb: if the consequence of skipping it is \"a bit less convenient,\" use a skill. If it's \"a broken build, a leaked secret, or a force-pushed `main`,\" use a hook.\n\n### Gotcha\n\nHooks in skills/agents are scoped to that component's lifecycle, but **plugin sub-agents cannot use the `hooks`, `mcpServers`, or `permissionMode` frontmatter** — those fields are silently ignored when an agent loads from a plugin (security). If you need them, copy the agent into `.claude/agents/`."
  },
  {
   "heading": "MCP servers: typed gateways to external systems",
   "body": "An **MCP server** is an external tool Claude calls over the Model Context Protocol — GitHub, Postgres, a tracker. Connect one *\"when you find yourself copying data into chat from another tool\"* (code.claude.com/docs/en/mcp).\n\n### Transports\n\n| Transport | When | Add command |\n|-----------|------|-------------|\n| **http** (alias `streamable-http`) | Remote cloud services; supports OAuth | `claude mcp add --transport http notion https://mcp.notion.com/mcp` |\n| **stdio** | Local processes, custom scripts, direct system access | `claude mcp add --env KEY=val --transport stdio airtable -- npx -y airtable-mcp-server` |\n| **sse** | **Deprecated** — use http | `claude mcp add --transport sse ...` |\n| **ws** | Persistent bidirectional push; no OAuth, no `--transport` flag (JSON only) | via `claude mcp add-json` |\n\nFor stdio, everything after `--` is passed to the server untouched; `--` separates Claude's own flags (`--transport`, `--env`, `--scope`) from the server command. In `.mcp.json`, the `type` field accepts `streamable-http` as an alias for `http` so configs copied from server docs work unchanged.\n\n### Scopes\n\n| Scope (`--scope`) | Stored in | Shared? |\n|-------------------|-----------|---------|\n| `local` (default) | `~/.claude.json`, keyed to *this project's path* — private to you | No |\n| `project` | `.mcp.json` in repo root | **Yes — check it in** |\n| `user` | `~/.claude.json` | Across your projects |\n\nProject scope is the team mechanism. Claude Code prompts each member for approval before using project-scoped servers (a prompt-injection guardrail; they show as `⏸ Pending approval` in `claude mcp list`); reset with `claude mcp reset-project-choices`.\n\n### .mcp.json with secrets kept out of the file\n\nThe critical pattern: **commit the config, never the secret.** Claude Code expands `${VAR}` and `${VAR:-default}` in the `command`, `args`, `env`, `url`, and `headers` fields:\n\n```json\n{\n  \"mcpServers\": {\n    \"github\": {\n      \"type\": \"http\",\n      \"url\": \"https://api.githubcopilot.com/mcp/\",\n      \"headers\": {\n        \"Authorization\": \"Bearer ${GITHUB_PAT}\"\n      }\n    },\n    \"local-db\": {\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"@modelcontextprotocol/server-postgres\", \"${DATABASE_URL}\"],\n      \"env\": {}\n    }\n  }\n}\n```\n\n`${GITHUB_PAT}` and `${DATABASE_URL}` resolve from each developer's environment. The repo holds the *shape*; the secret lives in the shell/`.env`. **If a required var is unset with no default, Claude Code fails to parse the config** — fail-loud, by design.\n\n### Operational guardrails\n\n- Claude Code warns when an MCP tool returns **>10,000 tokens**; raise with `MAX_MCP_OUTPUT_TOKENS` (e.g. `=50000`). This warning is your early signal that a server is a context hog.\n- Per-server `timeout` (ms) in the `.mcp.json` entry caps each tool call (e.g. `\"timeout\": 600000`); values below 1000 fall through to `MCP_TOOL_TIMEOUT`. Startup timeout is `MCP_TIMEOUT`.\n- `/mcp` shows status and tool counts; `claude mcp list` / `get <name>` from the CLI."
  },
  {
   "heading": "The MCP context-cost problem (contested)",
   "body": "MCP is the primitive most likely to quietly wreck your setup, because **loading many MCP tools consumes a large share of the context window before work begins.** Each always-on server pays its tool-schema tax every single session. This is a genuine open debate, and your team should pick a side deliberately.\n\n### The two camps\n\n**Thin-gateway / CLI-first** (blog.sshh.io): an MCP server should be a thin, secure gateway exposing a *few high-level* tools. Migrate stateless integrations — Jira, GitHub, AWS — to plain CLIs that Claude calls via `Bash`, and reserve MCP for genuinely *stateful* clients like Playwright (a live browser session). A CLI call costs tokens only when invoked; an always-on MCP server pays up front.\n\n**Compress-don't-restrict** (mksg.lu/blog/context-mode): keep the tool richness, but route MCP outputs through a compression middleware for roughly **98% context reduction**. Don't amputate capability; shrink the payload.\n\n| | Thin-gateway / CLI-first | Compress-don't-restrict |\n|---|--------------------------|-------------------------|\n| Core move | Fewer tools; CLIs for stateless services | Keep tools; compress outputs |\n| Best when | Stateless integrations (GitHub, Jira, AWS) | You genuinely need many tools |\n| Reserve MCP for | Stateful clients (Playwright, live DB sessions) | — |\n| Risk | Lose discoverability of niche tools | Middleware complexity; compression can drop signal |\n\nThis ecosystem's own tool-interface policy (the project ADR on CLI-first / MCP-adapters) lands on the thin-gateway side: *core library first, CLI by default, Skill for procedure, MCP only for a justified integration boundary.* A defensible default — but the contest is live; note it, don't pretend it's settled.\n\n### Mitigations available today\n\n- **Tool search** defers MCP tool schemas behind a `ToolSearch` call instead of loading all of them up front — directly attacks the up-front cost. Control with the `ENABLE_TOOL_SEARCH` env var.\n- **`alwaysLoad`** per-server forces a server's tools to load eagerly when you truly need them present at first prompt.\n- **Per-tool overrides** like `anthropic/maxResultSizeChars` let a single chatty tool declare its own output cap independent of `MAX_MCP_OUTPUT_TOKENS`.\n- **Approval gating / `disabledMcpjsonServers`** lets a team ship a rich `.mcp.json` while individuals opt into only what they use."
  },
  {
   "heading": "When to use what: the decision table",
   "body": "The four primitives are not interchangeable. Misclassifying a need is the most common and most expensive mistake in this stage. Here is the full decision surface:\n\n| You want… | Reach for | Because |\n|-----------|-----------|---------|\n| A fact / convention always in context | `CLAUDE.md` (or `user-invocable: false` skill) | Cheap passive knowledge; no procedure |\n| A *procedure* applied when relevant | **Skill** | Model-invoked on description; body loads on demand |\n| A workflow *you* trigger with side effects | **Skill** + `disable-model-invocation: true` | You control timing (`/deploy`) |\n| A rule that must hold 100% of the time | **Hook** | Deterministic, enforced, can hard-block |\n| Format/lint/validate after every edit | **Hook** (`PostToolUse`) | Fires regardless of model choice |\n| A bounded sub-task without context pollution | **Sub-agent** | Own window, returns only a summary |\n| The same worker spawned repeatedly | **Reusable sub-agent** in `.claude/agents/` | Encodes system prompt + tool restrictions once |\n| Read/act on an external system | **MCP server** | Typed protocol gateway |\n| A *stateless* external call (GitHub, Jira) | **CLI via Bash** (often, not MCP) | Avoids always-on tool-schema tax |\n| Bundle several of the above to ship to a team | **Plugin + marketplace** | One install delivers skills+agents+hooks+MCP |\n\n### Worked disambiguations\n\n- *\"Always run our linter\"* → hook, not skill. A skill can be skipped; a `PostToolUse` hook cannot.\n- *\"Summarize a PR\"* → skill (procedure), optionally `context: fork` to keep the diff out of your main window.\n- *\"Never let the agent push to main\"* → `PreToolUse` hook with `deny`. Putting it in `CLAUDE.md` is the classic mistake — it's advisory and will eventually be ignored.\n- *\"Query our prod database\"* → MCP (stateful connection) or a read-only CLI wrapper, depending on which camp above.\n- *\"Explore an unfamiliar module\"* → sub-agent (read-heavy, returns a map, spares your context)."
  },
  {
   "heading": "The contested case: passive docs vs. model-invoked skills",
   "body": "Before you skill-ify everything, sit with the strongest counter-evidence — because it changes *what you build first*.\n\n### The finding\n\nIn Vercel's published agent evals, an **`AGENTS.md` docs-index scored 100% while Skills scored 53–79%**, and *the skill was never invoked in 56% of cases* (vercel.com/blog/agents-md-outperforms-skills-in-our-agent-evals). For supplying **always-true knowledge**, a static docs-index currently outperforms model-invoked skills.\n\n### The two readings\n\n- **passive-context-wins-now**: results matter today. If knowledge should *always* be available, put it in passive context (`AGENTS.md` / `CLAUDE.md`), where it can't fail to trigger. The 53–79% reflects a real, current cost: a skill that doesn't fire does nothing.\n- **skills-for-actions**: skills win for *vertical, explicitly-triggered action workflows* — the place where you *want* on-demand loading, not always-on knowledge. The invocation gap is a current tool-use limitation that will close.\n\nBoth can be right because they're about different jobs. The synthesis your team should adopt:\n\n| Need | Put it in | Rationale |\n|------|-----------|-----------|\n| Always-true knowledge (conventions, architecture facts) | Passive context (`CLAUDE.md` / `AGENTS.md`) | Can't fail to trigger; eval-backed |\n| Always-true *enforcement* | Hook | Deterministic |\n| On-demand *procedure / action* | Skill | This is exactly the skill sweet spot |\n\nThe practical implication: **don't convert reference knowledge into a skill just because skills are new.** The very first `code.claude.com/docs/en/skills` guidance agrees — *a fact belongs in CLAUDE.md, a procedure belongs in a skill.* Skill-ify procedures; keep facts passive."
  },
  {
   "heading": "Packaging: plugins and marketplaces for the whole team",
   "body": "Skills, agents, hooks, and MCP servers are individually useful, but a team wants to *ship a bundle and have everyone get it with one command.* That's a **plugin**, distributed through a **marketplace**.\n\n### Plugin layout\n\n```text\nmy-plugin/\n├── .claude-plugin/\n│   └── plugin.json        # ONLY the manifest lives here\n├── skills/                # all components at the plugin ROOT\n│   └── pr-summary/SKILL.md\n├── agents/\n│   └── safe-researcher.md\n├── hooks/\n│   └── hooks.json\n└── .mcp.json              # bundled MCP servers\n```\n\nA critical and easy-to-miss rule: **only `plugin.json` goes inside `.claude-plugin/`.** Every component directory (`skills/`, `agents/`, `hooks/`, `commands/`) must be at the plugin *root*. Putting them inside `.claude-plugin/` is the #1 \"skills not appearing\" bug. Remember too that plugin components are **namespaced** — an agent `reviewer` in plugin `team-infra` appears as `team-infra:reviewer`, and plugin sub-agents silently drop `hooks`/`mcpServers`/`permissionMode` frontmatter (security).\n\n### plugin.json\n\n```json\n{\n  \"name\": \"team-infra\",\n  \"version\": \"1.2.0\",\n  \"description\": \"Shared skills, hooks, and MCP for the platform team\",\n  \"author\": { \"name\": \"Platform Team\", \"email\": \"infra@example.com\" }\n}\n```\n\n`name` is the only required field; omit the manifest entirely and components auto-discover with the name derived from the directory.\n\n### marketplace.json\n\nIn `.claude-plugin/marketplace.json` at the catalog repo root:\n\n```json\n{\n  \"name\": \"company-tools\",\n  \"owner\": { \"name\": \"DevTools Team\", \"email\": \"devtools@example.com\" },\n  \"plugins\": [\n    {\n      \"name\": \"team-infra\",\n      \"source\": \"./plugins/team-infra\",\n      \"description\": \"Shared platform skills, hooks, and MCP\",\n      \"version\": \"1.2.0\"\n    },\n    {\n      \"name\": \"deployment-tools\",\n      \"source\": { \"source\": \"github\", \"repo\": \"company/deploy-plugin\" }\n    }\n  ]\n}\n```\n\n### Install flow\n\n```text\n/plugin marketplace add company/tools-marketplace   # or a local path\n/plugin install team-infra@company-tools\n/reload-plugins                                      # activate in current session\n```\n\nThis is how *\"describe it again\"* becomes *\"it's built in\"* for a whole team: one marketplace entry delivers the skills, the enforcement hooks, the restricted sub-agents, and the MCP wiring in a single versioned install. Pin `version` so members only update when you bump it."
  },
  {
   "heading": "Configure once, run in both Claude Code and Codex",
   "body": "The lead's real goal is *configure once, get identical rules whether a dev opens Claude Code or Codex in VS Code.* Nothing makes this automatic — both tools read their *own* directories. The plan (project-meta `render_agents_md.py` + `check_agent_surface_sync.py`) is to **keep one canonical config and project it**, with a CI drift check.\n\n### The projection\n\n1. **Instructions.** `CLAUDE.md` is canonical; `AGENTS.md` is the Codex-facing mirror (agents.md, developers.openai.com/codex/guides/agents-md). Generate or symlink `AGENTS.md` from `CLAUDE.md` rather than maintaining two copies — in this ecosystem via `render_agents_md.py`.\n2. **Skills.** Keep a portable source (e.g. `~/.agents/skills/`) and mirror it into each tool's discovery dir — `.claude/skills/` for Claude, `.agents/skills/` for Codex — by symlink where possible.\n3. **Drift check in CI.** `check_agent_surface_sync.py` is read-only: it verifies the root instruction files resolve to the same content and flags mirror drift between the skill directories (it also enforces the 500-line `SKILL.md` body limit). Wire it so CI *fails* on divergence — that turns \"the two tools quietly disagree\" from a silent bug into a blocked merge.\n\n### Layout\n\n```text\nrepo/\n├── CLAUDE.md                    # canonical instructions\n├── AGENTS.md                    # generated/symlinked from CLAUDE.md (Codex)\n├── .claude/\n│   ├── skills/  -> mirror of portable source\n│   ├── agents/  *.md             # sub-agents (Claude-specific format)\n│   ├── hooks/                    # hook scripts\n│   └── settings.json             # hooks + permissions (shareable)\n├── .agents/\n│   └── skills/  -> mirror of portable source\n└── .mcp.json                    # project MCP servers (secrets via ${VAR})\n```\n\n### What ports cleanly vs. what doesn't\n\n| Layer | Portable across both tools? |\n|-------|-----------------------------|\n| Instructions | Yes — `CLAUDE.md` ⇄ `AGENTS.md` projection |\n| Skill core (`name`/`description`/body) | Yes — open `SKILL.md` standard |\n| Claude skill extensions (`context: fork`, `allowed-tools`, `paths`) | No — Claude-only |\n| Hooks / `settings.json` | No — Claude Code mechanism |\n| Sub-agent files | No — Claude Code format |\n| `.mcp.json` MCP servers | Partially — both speak MCP, but config files and scoping differ |\n\nThe honest takeaway: **instructions and skill bodies project cleanly; enforcement (hooks) and orchestration (sub-agents) are currently Claude-Code-specific.** Standardize what's standardizable, gate it with the drift check, and keep the tool-specific enforcement layer in each tool's native config — don't fake a portability that the formats don't yet support."
  },
  {
   "heading": "Common pitfalls and failure modes",
   "body": "A consolidated list of the traps, since each primitive fails differently.\n\n### Skills\n- **Description doesn't trigger.** Missing the keywords users actually say. Fix: front-load the trigger phrase; verify with `What skills are available?`; check `/doctor` for dropped descriptions.\n- **Triggers but doesn't work.** The body loaded; the model chose other tools. Strengthen the body, or escalate to a hook for true enforcement. Validate with a fresh-session baseline A/B, not a same-session spot check.\n- **Bloated body.** Over 500 lines and full of narration. It's a recurring per-turn cost (and can be truncated to 5,000 tokens after compaction). Move detail to `reference.md` (progressive disclosure).\n- **Malformed frontmatter.** Claude loads the body with *empty metadata* — `/name` still works but there's no description to match. Run `claude --debug` to see the parse error.\n\n### Hooks\n- **Used `exit 1` to block.** Only `exit 2` (or a JSON `deny`/`block`) blocks; `1` is non-blocking. Silent failure to enforce.\n- **Policy written in `CLAUDE.md` instead of a hook.** The category error of this whole stage. Advisory ≠ enforced.\n- **Over-broad matcher.** `\"*\"` on `PreToolUse` running a slow script taxes every tool call. Scope with `matcher` + `if`.\n- **Plugin sub-agent hooks ignored.** `hooks`/`mcpServers`/`permissionMode` are dropped from plugin-loaded agents (security). Copy into `.claude/agents/` if needed.\n\n### Sub-agents\n- **Forking a task-less skill.** `context: fork` with only guidelines → no actionable prompt → empty return.\n- **Using a sub-agent as an implementer.** It can't ask clarifying questions and its edits skip your review. Prefer researcher → summary → main-thread implements.\n- **Forgetting it has no memory.** Each call starts cold; pass context in the delegation or via files.\n- **Forgot `tools:` entirely.** Omitting it inherits *all* tools — the opposite of least-privilege. Set `tools:` or `disallowedTools:`.\n\n### MCP\n- **Secret committed in `.mcp.json`.** Use `${VAR}`/`${VAR:-default}` expansion (works in `command`/`args`/`env`/`url`/`headers`); keep the token in the environment. The repo holds shape, never secrets.\n- **Context blown before work starts.** Too many always-on tools. Lean on tool-search, prune to a thin gateway, or compress outputs — and treat the >10k-token MCP-output warning as your signal.\n- **Forgot project-scope approval.** Teammates must approve project-scoped servers; `claude mcp reset-project-choices` to redo.\n\n### Portability / cross-tool\n- **Copied skills drift.** Symlink from one canonical source, and fail CI on divergence with the surface-sync check.\n- **Assumed hooks/agents port to Codex.** They don't. Only instructions and skill cores project; keep enforcement in each tool's native config."
  }
 ],
 "stage-4": [
  {
   "heading": "The shelf nobody built",
   "body": "Three people on your team have written a code-review skill. One lives in Maya's `~/.claude/skills/review/`, one is copy-pasted into the `.claude/commands/` of the payments repo, and the third is a half-remembered prompt Devon keeps in a Notion doc. They are 80% the same and 20% subtly divergent — different severity ladders, different ideas about what \"done\" means. The waste was never the writing. Writing a good review skill once is cheap. The waste is that there is **no shelf everyone pulls from**, so the skill gets re-derived, drifts, and rots in three places at once.\n\nThis stage is about building that shelf. Claude Code's answer has two parts: a **plugin** (the unit you put *on* the shelf — a versioned bundle of skills, sub-agents, hooks, MCP servers, and LSP servers) and a **plugin marketplace** (the shelf itself — a catalog repo the team adds once and pulls updates from). By the end you will be able to take that scattered review skill, package it as a plugin, publish it in a private team marketplace, and have everyone install it with two commands — plus understand exactly what you are trusting when you do, because installing a plugin is one consent click over a lot of executable surface.\n\nThis is depth, not a tour. Every format below is copy-able and checked against the official docs at `code.claude.com/docs` (`/plugins`, `/plugin-marketplaces`, `/discover-plugins`, `/plugins-reference`)."
  },
  {
   "heading": "What a plugin actually is",
   "body": "A **plugin** is a versioned bundle of extensions — slash commands, sub-agents, skills, hooks, MCP servers, and LSP servers — packaged into one installable unit (source: `code.claude.com/docs/en/plugins`). It is the difference between \"here's a file, copy it into your `.claude/`\" and \"install `code-review@team-tools` and you have the same thing I have, with updates.\"\n\nThe docs frame the standalone-vs-plugin choice as a tradeoff over skill *names* and *sharing*: standalone `.claude/` skills get short names (`/review`); plugin skills are always namespaced (`/plugin-name:review`). Use standalone for personal/project-only iteration; reach for a plugin when you want versioned releases reusable across projects and teammates.\n\n### The minimum: a manifest\n\nEvery plugin is a directory. The defining file is the manifest at `.claude-plugin/plugin.json`:\n\n```json\n{\n  \"name\": \"code-review\",\n  \"description\": \"Team code-review skill: severity ladder, security pass, test-coverage check\",\n  \"version\": \"1.2.0\",\n  \"author\": { \"name\": \"DevTools Team\", \"email\": \"devtools@acme.com\" }\n}\n```\n\nFields that matter (from the official manifest table):\n\n| Field | Required | Why it matters |\n|---|---|---|\n| `name` | yes | Unique id **and** skill namespace. Skills become `/code-review:<skill>`. kebab-case, no spaces. |\n| `description` | recommended | Shown in the plugin manager when teammates browse. |\n| `version` | optional | If set, users only get updates when you **bump** it. If omitted on a git-hosted plugin, the commit SHA is used and every commit counts as a new version. |\n| `author` | optional | Attribution (`name` required, `email` optional). |\n\nThe manifest also accepts `homepage`, `repository`, `license`, and `keywords` (see the full manifest schema in `/plugins-reference`). The namespacing is deliberate: a plugin skill is *always* invoked as `/plugin-name:skill-name` (e.g. `/code-review:review`) so two plugins can both ship a `review` skill without colliding. You change the prefix by changing `name`.\n\nNote a subtlety from the docs: the manifest is technically *optional* if every component sits in its default location — a directory with just `skills/` can load. But for anything you intend to version and share, write the `plugin.json`; it is what the plugin manager displays and what drives version-based updates."
  },
  {
   "heading": "Plugin directory layout — and the one mistake everyone makes",
   "body": "Components live in fixed directories at the **plugin root**, not inside `.claude-plugin/`. This is the single most common authoring error, and the docs flag it explicitly with a Warning.\n\n```text\ncode-review/\n├── .claude-plugin/\n│   └── plugin.json          # ONLY this goes in .claude-plugin/\n├── skills/\n│   └── review/\n│       └── SKILL.md         # → /code-review:review\n├── agents/\n│   └── security-reviewer.md  # a sub-agent\n├── hooks/\n│   └── hooks.json            # event handlers\n├── monitors/\n│   └── monitors.json         # background watchers (tail logs, files, status)\n├── .mcp.json                 # MCP server configs\n├── .lsp.json                 # language-server configs\n├── commands/                 # legacy: flat .md skills (use skills/ for new work)\n├── bin/                      # executables added to PATH while enabled\n└── settings.json             # default settings when the plugin is enabled\n```\n\n| Directory / file | Location | Purpose |\n|---|---|---|\n| `.claude-plugin/plugin.json` | inside `.claude-plugin/` | the manifest — **the only thing** in here |\n| `skills/<name>/SKILL.md` | root | model-invoked skills |\n| `commands/*.md` | root | legacy flat-Markdown skills; docs say use `skills/` for new plugins |\n| `agents/*.md` | root | sub-agent definitions |\n| `hooks/hooks.json` | root | event handlers (PreToolUse, PostToolUse, …) |\n| `monitors/monitors.json` | root | background monitors; each `command`'s stdout lines become notifications |\n| `.mcp.json` | root | MCP server configs |\n| `.lsp.json` | root | LSP servers for code intelligence |\n| `bin/` | root | executables added to the Bash tool's `$PATH` while the plugin is enabled |\n| `settings.json` | root | default settings (only `agent` and `subagentStatusLine` honored; unknown keys silently ignored) |\n\nFrom the docs (verbatim Warning): *\"Don't put `commands/`, `agents/`, `skills/`, or `hooks/` inside the `.claude-plugin/` directory. Only `plugin.json` goes inside `.claude-plugin/`. All other directories must be at the plugin root level.\"* If your skills don't show up after install, this is the first thing to check.\n\nA plugin shipping exactly one skill may put `SKILL.md` at the plugin root and skip the `skills/` directory entirely (Claude Code uses the frontmatter `name` for the invocation); use the `skills/` layout once you expect more than one."
  },
  {
   "heading": "The components: what you can bundle",
   "body": "### Skills\n\nA skill is a folder under `skills/` containing a `SKILL.md` with YAML frontmatter. The `description` is load-bearing — it's how Claude decides when to auto-invoke:\n\n```markdown\n---\ndescription: Reviews code for bugs, security, and performance. Use when reviewing code, checking PRs, or analyzing code quality.\n---\n\nWhen reviewing code, check for:\n1. Code organization and structure\n2. Error handling and edge cases\n3. Security concerns (injection, secrets, authz)\n4. Test coverage\n\nReport findings as severity-ranked, actionable items. Be concise.\n```\n\nAdd `disable-model-invocation: true` to the frontmatter (the docs use exactly this in their quickstart) if you want the skill to be *only* explicitly invokable (`/code-review:review`) and never auto-fired by the model. `$ARGUMENTS` in the body captures text the user types after the skill name (e.g. `/code-review:review src/auth.ts`).\n\n### Sub-agents\n\nDrop agent definitions in `agents/*.md`. A `settings.json` at the plugin root with `{\"agent\": \"security-reviewer\"}` can even make one of the plugin's agents the **main thread** when the plugin is enabled — applying its system prompt, tool restrictions, and model. That's a plugin that changes how Claude Code behaves by default. (Only `agent` and `subagentStatusLine` are honored in plugin `settings.json`; other keys are silently ignored.)\n\n### Hooks\n\nHooks live in `hooks/hooks.json` and use the exact same format as the `hooks` block in `settings.json` — so migration is copy-paste. The command receives hook input as JSON on stdin; the docs use `jq` to pull the file path out:\n\n```json\n{\n  \"hooks\": {\n    \"PostToolUse\": [\n      {\n        \"matcher\": \"Write|Edit\",\n        \"hooks\": [\n          { \"type\": \"command\", \"command\": \"${CLAUDE_PLUGIN_ROOT}/scripts/lint.sh\" }\n        ]\n      }\n    ]\n  }\n}\n```\n\n`${CLAUDE_PLUGIN_ROOT}` resolves to the plugin's installed (cached) location — use it for any path into the plugin, because plugins are **copied to a cache**, so `../shared-utils` and absolute author-machine paths will not resolve. For state that must *survive plugin updates* (a SQLite db, a token cache), use `${CLAUDE_PLUGIN_DATA}` instead — the cache directory is replaced on update, the data directory is not.\n\n### MCP servers\n\nBundle an MCP server config in `.mcp.json` (or inline `mcpServers` in the marketplace entry). This is how the official `github`, `linear`, `sentry`, `slack`, `notion`, `figma`, `vercel`, and `supabase` plugins ship pre-configured MCP integrations — install the plugin, get the server, no manual `.mcp.json` editing.\n\n### LSP servers\n\n`.lsp.json` wires a language server for real-time diagnostics and code navigation. The official marketplace already ships `pyright-lsp`, `gopls-lsp`, `typescript-lsp`, `rust-analyzer-lsp`, and more; author your own only for languages not covered. The user must have the language-server **binary** on `$PATH` — if it's missing you'll see `Executable not found in $PATH` in the `/plugin` Errors tab. Minimal `.lsp.json`:\n\n```json\n{ \"go\": { \"command\": \"gopls\", \"args\": [\"serve\"], \"extensionToLanguage\": { \".go\": \"go\" } } }\n```\n\n### Background monitors\n\n`monitors/monitors.json` lets a plugin watch a log, file, or external status in the background; each stdout line from the monitor's `command` is delivered to Claude as a notification during the session — no instruction to \"start watching\" needed."
  },
  {
   "heading": "Develop and test before you publish",
   "body": "You do not need a marketplace to iterate. Load a plugin straight off disk:\n\n```bash\nclaude --plugin-dir ./code-review\n```\n\nThen inside the session, after edits, pick up changes without restarting:\n\n```text\n/reload-plugins\n```\n\n`/reload-plugins` reloads plugins, skills, agents, hooks, and plugin MCP/LSP servers, and reports counts for each. Verify the three surfaces individually: run `/code-review:review`, confirm agents appear in `/agents`, and confirm hooks fire on a real edit.\n\nLoad several at once by repeating the flag: `claude --plugin-dir ./a --plugin-dir ./b`. When a `--plugin-dir` plugin shares a name with an installed marketplace plugin, the local copy wins for that session — so you can test a patch to something already installed without uninstalling it (exception: plugins force-enabled/disabled by managed settings can't be overridden this way). `--plugin-dir` also accepts a `.zip` archive (Claude Code v2.1.128+), and `--plugin-url https://…/plugin.zip` fetches and loads an archive for that session only.\n\nValidate the structure before you ship:\n\n```bash\nclaude plugin validate ./code-review     # or /plugin validate ./code-review in-session\n```\n\nPointed at a **plugin** directory, the validator checks `plugin.json` plus the frontmatter of every skill, agent, and command, and the JSON of `hooks/hooks.json`. Pointed at a **marketplace** directory, it checks `marketplace.json` only (schema, duplicate plugin names, `..` path traversal, version mismatches against each `plugin.json`). A malformed `hooks.json` prevents the **entire plugin** from loading, so this catches a real failure mode.\n\n### Converting your scattered skill\n\nMigrating the existing `.claude/` review skill is mechanical (these are the docs' own migration steps):\n\n```bash\nmkdir -p code-review/.claude-plugin\n# write plugin.json (above), then:\ncp -r .claude/skills/review code-review/skills/\n```\n\nHooks from a `settings.json` move verbatim into `hooks/hooks.json` (same format). Afterward, **delete the originals** from `.claude/` — project/user `.claude/agents/` definitions override same-named plugin agents, so leaving them creates silent shadowing.\n\nIf you'd rather skip `--plugin-dir` on every launch while iterating, `claude plugin init my-tool` scaffolds a plugin under `~/.claude/skills/my-tool/` that auto-loads next session as `my-tool@skills-dir`."
  },
  {
   "heading": "What a marketplace is",
   "body": "A **plugin marketplace** is a catalog repo defined by a single file, `.claude-plugin/marketplace.json`, that lists plugins and where to fetch each one. Teams **add it once**, install the plugins they need, and pull updates by refreshing the catalog (sources: `code.claude.com/docs/en/plugin-marketplaces`, `/discover-plugins`).\n\nThe mental model from the docs: adding a marketplace is *\"like adding an app store\"* — it gives you the catalog to browse, but you still choose which plugins to install individually. Adding the marketplace installs **nothing**.\n\n### The catalog file\n\n```json\n{\n  \"name\": \"acme-tools\",\n  \"owner\": { \"name\": \"DevTools Team\", \"email\": \"devtools@acme.com\" },\n  \"plugins\": [\n    {\n      \"name\": \"code-review\",\n      \"source\": \"./plugins/code-review\",\n      \"description\": \"Team code-review skill\",\n      \"version\": \"1.2.0\"\n    },\n    {\n      \"name\": \"deploy-tools\",\n      \"source\": { \"source\": \"github\", \"repo\": \"acme/deploy-plugin\" },\n      \"description\": \"Deployment automation\"\n    }\n  ]\n}\n```\n\nRequired top-level fields are `name` (kebab-case, public-facing — it's the `@acme-tools` in `/plugin install x@acme-tools`), `owner` (object, `name` required, `email` optional), and `plugins` (array). Each plugin entry needs at minimum `name` and `source`. Useful optionals: `metadata.pluginRoot: \"./plugins\"` lets you write `\"source\": \"code-review\"` instead of `\"./plugins/code-review\"`; a plugin entry may also carry `displayName` (a spaced/cased human label, v2.1.143+) and `defaultEnabled: false` (install disabled until the user opts in, v2.1.154+).\n\nTwo constraints worth knowing up front: each user can register only **one marketplace per name** — adding a second with the same `name` replaces the first, so list all your plugins in one `marketplace.json`. And a set of names (`claude-plugins-official`, `claude-community`, `anthropic-*`, etc.) is **reserved** for Anthropic and rejected for third-party marketplaces, along with names that impersonate official ones."
  },
  {
   "heading": "Plugin sources: where each plugin is fetched from",
   "body": "The `source` field on a plugin entry is the most important authoring decision in a marketplace, because it determines pinning, bandwidth, and whether relative paths even work.\n\n| Source | Form | Fields | Notes |\n|---|---|---|---|\n| Relative path | `\"./plugins/x\"` | — | Must start with `./`; resolved from marketplace **root** (the dir containing `.claude-plugin/`), never `..`. Only works when the marketplace is added via git, not via a bare URL to `marketplace.json`. |\n| `github` | object | `repo`, `ref?`, `sha?` | `owner/repo` shorthand. |\n| `url` | object | `url`, `ref?`, `sha?` | Any git URL (GitLab, Bitbucket, self-hosted); `.git` suffix optional. |\n| `git-subdir` | object | `url`, `path`, `ref?`, `sha?` | Sparse partial clone — for a plugin inside a monorepo. `url` also takes `owner/repo` or SSH form. |\n| `npm` | object | `package`, `version?`, `registry?` | Installed via `npm install`; supports private registries. |\n\nPinning, for the three git-based sources (`github`, `url`, `git-subdir`):\n\n```json\n{\n  \"name\": \"code-review\",\n  \"source\": {\n    \"source\": \"github\",\n    \"repo\": \"acme/code-review\",\n    \"ref\": \"v1.2.0\",\n    \"sha\": \"a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0\"\n  }\n}\n```\n\nWhen both `ref` and `sha` are set, **`sha` is the effective pin** — Claude Code checks out that exact commit (a full 40-character SHA), and on GitHub/GitLab/Bitbucket the install still succeeds even if the branch named by `ref` was later deleted, as long as the commit is reachable. (Exception: servers like AWS CodeCommit can't fetch by SHA, so there the `ref` must still exist.) For a security-reviewed internal marketplace, pinning `sha` is how you guarantee everyone runs the exact bytes you audited.\n\n**Marketplace source vs plugin source** are distinct and pinned independently: the marketplace source (passed to `/plugin marketplace add`) is where the *catalog* lives and supports `ref` but **not** `sha`; each plugin's `source` is where that *plugin* lives and supports both. A catalog at `acme/plugin-catalog` can list a plugin fetched from `acme/code-review` — different repos."
  },
  {
   "heading": "The full lifecycle: add, install, update, remove",
   "body": "Every `/plugin` command has a non-interactive `claude plugin …` twin for scripting and CI. Shortcuts: `/plugin market` for `marketplace`, `rm` for `remove`.\n\n### Add the marketplace (once per teammate)\n\n```text\n/plugin marketplace add acme/claude-plugins          # GitHub owner/repo\n/plugin marketplace add acme/claude-plugins@v2.0     # pin a branch/tag (GitHub shorthand: @ref)\n/plugin marketplace add https://gitlab.com/acme/plugins.git\n/plugin marketplace add https://gitlab.com/acme/plugins.git#v1.0.0   # git URL: #ref\n/plugin marketplace add git@gitlab.com:acme/plugins.git             # SSH\n/plugin marketplace add ./my-marketplace             # local, for testing\n```\n\n### Install plugins (each person picks what they need)\n\n```text\n/plugin install code-review@acme-tools     # installs to USER scope by default\n```\n\nTo choose a different scope from the CLI you can pass `--scope project|local|user`; from the UI, press Enter on the plugin and pick a scope.\n\n### Update — the whole point of the shelf\n\n```text\n/plugin marketplace update acme-tools   # refresh the catalog from its source\n```\n\nWhen you ship `code-review` v1.3.0, you push to the marketplace repo; teammates run `marketplace update` (or get it via auto-update at startup) and `/reload-plugins`. No more drift across three copies.\n\n### Manage installed plugins\n\n```text\n/plugin list                              # add --enabled / --disabled\n/plugin disable code-review@acme-tools    # turn off without uninstalling\n/plugin enable  code-review@acme-tools\n/plugin uninstall code-review@acme-tools\n/reload-plugins                           # apply changes mid-session\n```\n\n`/plugin` with no args opens a four-tab UI you cycle with **Tab**: **Discover**, **Installed**, **Marketplaces**, **Errors**. In the Discover detail pane, recent versions show a **Context cost** estimate (tokens added per turn, v2.1.143+), a **Last updated** date (v2.1.144+), and a **Will install** list enumerating every command, agent, skill, hook, and MCP/LSP server (v2.1.145+) — review this before you install. The same component inventory is available non-interactively via `claude plugin details`.\n\n### Remove\n\n```text\n/plugin marketplace remove acme-tools\n```\n\n**Gotcha (verbatim Warning):** *\"Removing a marketplace will uninstall any plugins you installed from it.\"* (Precisely: removing it from its *last remaining scope* uninstalls them.) To merely refresh, use `marketplace update`, never `remove` + re-add."
  },
  {
   "heading": "Making it automatic for the team",
   "body": "Telling every new hire to run two `/plugin` commands is the manual-coordination failure mode all over again. Push the marketplace and the default plugin set into the repo's `.claude/settings.json` so trusting the project folder is enough.\n\n```json\n{\n  \"extraKnownMarketplaces\": {\n    \"acme-tools\": {\n      \"source\": { \"source\": \"github\", \"repo\": \"acme/claude-plugins\" },\n      \"autoUpdate\": true\n    }\n  },\n  \"enabledPlugins\": {\n    \"code-review@acme-tools\": true,\n    \"deploy-tools@acme-tools\": true\n  }\n}\n```\n\nWhen a teammate trusts the repository folder, Claude Code prompts them to install these marketplaces and plugins. `extraKnownMarketplaces` *registers* the catalog; `enabledPlugins` says which plugins should be on by default. `autoUpdate: true` (admin-settable per entry) keeps the org marketplace current without each person toggling it. Note the defaults: official Anthropic marketplaces auto-update; third-party and local-dev marketplaces do **not** unless you opt in.\n\n### CLI declares scope, too\n\n```bash\nclaude plugin marketplace add acme/claude-plugins --scope project\nclaude plugin install code-review@acme-tools --scope project\n```\n\nScopes: **user** (you, everywhere — the install default), **project** (shared via `.claude/settings.json`), **local** (you, this repo only). For a team shelf you want **project** scope so the config is committed and shared. You may also see **managed** scope — admin-installed via managed settings, not user-modifiable.\n\n### Locking it down (managed settings)\n\nAdministrators can restrict which marketplaces anyone may add via `strictKnownMarketplaces` in **managed** settings (which users and project config cannot override):\n\n```json\n{\n  \"strictKnownMarketplaces\": [\n    { \"source\": \"github\", \"repo\": \"acme/approved-plugins\" },\n    { \"source\": \"hostPattern\", \"hostPattern\": \"^github\\\\.acme\\\\.com$\" }\n  ]\n}\n```\n\n`[]` = total lockdown (no new marketplaces); a list = allowlist only; undefined = no restriction. Source types include exact `github`/`url`, plus regex `hostPattern` and `pathPattern`. Pair it with `extraKnownMarketplaces` so the approved catalog is also auto-registered (`strictKnownMarketplaces` restricts but does not register). For CI/containers, `CLAUDE_CODE_PLUGIN_SEED_DIR` pre-populates plugins at image-build time so nothing clones at runtime (the seed is read-only and its entries take precedence)."
  },
  {
   "heading": "Version management — the silent-no-op trap",
   "body": "Versions decide cache paths and whether `/plugin update` does anything. If the resolved version matches what a user already has, both `/plugin update` and auto-update skip the plugin. Claude Code resolves a plugin's version from the first of these that's set:\n\n1. `version` in the plugin's `plugin.json`\n2. `version` in the marketplace entry\n3. the git commit SHA of the plugin's source\n\nTwo strategies, and you must pick one consciously:\n\n| Strategy | How | Behavior |\n|---|---|---|\n| **Explicit version** | set `version` in `plugin.json`, bump every release | Users update only when the string changes. Predictable; requires discipline. |\n| **SHA-as-version** | omit `version` on a git-hosted plugin (`github`, `url`, `git-subdir`, or a relative path in a git-hosted marketplace) | Every commit is a new version. Zero ceremony; good for internal, fast-moving plugins. |\n\nThe trap, straight from the docs Warning: if `plugin.json` says `\"version\": \"1.0.0\"` and you push new commits **without changing that string**, existing users get nothing — Claude Code sees the same version and keeps the cached copy. The fix is to bump on every release, or omit `version` entirely.\n\nAlso (same Warning): **don't set `version` in both** `plugin.json` and the marketplace entry. The `plugin.json` value wins *silently*, so a stale manifest can mask the version you set in `marketplace.json`. Pick one home for the version number.\n\n### Release channels\n\nChannels fall out of this: point two marketplaces (`stable-tools`, `latest-tools`) at different `ref`s of the same repo and assign each to a user group via managed settings. The docs' caveat (verbatim spirit): **each channel must resolve to a *different* version** — if two refs resolve to the same `version` string, Claude Code treats them as identical and skips the update. With omitted `version`, the distinct commit SHAs already distinguish the channels."
  },
  {
   "heading": "The trust problem: one click, a lot of code",
   "body": "Here is the part the convenience hides. Installing a plugin activates its skills, hooks, and MCP servers as **code running under one trust decision**, with your user privileges. The docs say it plainly: *\"Plugins and marketplaces are highly trusted components that can execute arbitrary code on your machine with your user privileges. Only install plugins and add marketplaces from sources you trust.\"* The install UI repeats it: *\"Anthropic does not control what MCP servers, files, or other software are included in plugins and cannot verify that they work as intended.\"*\n\nSecurity researchers call this the **bundled trust radius**: *\"A single plugin installation activates skills, MCP servers, hooks, and arbitrary binaries under a single, opaque click of consent\"* (Pluto Security). The `plugin.json` manifest carries no signed authorship and no capability statement — approving the apex of the plugin endorses every layer beneath it without enumeration.\n\nThis is not theoretical. Snyk's *ToxicSkills* audit of **3,984** third-party skills found **13.4% (534) had a critical security issue**, **76 contained confirmed malicious payloads**, **10.9% exposed secrets**, and **17.7% fetched untrusted third-party content** (Pluto Security, citing Snyk). And the runtime matters: per Pluto, *\"The same `SKILL.md` is safely sandboxed on the Claude API but inherits full local host privileges when executed via Claude Code\"* — full filesystem and network. *\"The threat is not in the file; it is in where it runs.\"*\n\n### A demonstrated plugin-injection chain\n\nPromptArmor demonstrated a full hijack via an injected marketplace plugin (source: promptarmor.com/resources/hijacking-claude-code-via-injected-marketplace-plugins):\n\n1. **Install** — a user adds a malicious marketplace from a public registry, often impersonating Anthropic branding.\n2. **Permission bypass** — a malicious **hook** fires and either overwrites `settings.local.json` to permanently allow dangerous commands like `curl`, or auto-approves command types *before the human-in-the-loop dialog appears*.\n3. **Manipulation** — the user runs an innocent-looking command (e.g. \"Docs\") that carries hidden **prompt-injection** instructions.\n4. **Exfiltration** — Claude, believing it needs a \"RAG service,\" `curl`s the codebase context to the attacker's server — now automatic, because the guardrails were already disabled in step 2.\n\nHooks are the primary weapon precisely because they run shell code on events, before tool-use prompts. A plugin is not a passive config bundle; it's an executable supply-chain dependency. (Note: Anthropic's own `claude-community` marketplace screens submissions with automated safety checks and pins each plugin to a reviewed commit SHA — but that protection does **not** extend to arbitrary third-party marketplaces you add yourself.)"
  },
  {
   "heading": "Governing the shelf: review, pin, restrict",
   "body": "The mitigation is not \"don't use plugins\" — it's to treat a plugin like any other dependency you'd pull into your build, and to put the team shelf where you control it.\n\n### Pre-install review (the non-negotiable)\n\nBefore anyone clicks Trust, **read the code**: `plugin.json`, every `SKILL.md`, `hooks/hooks.json`, and any MCP server implementation. The **Will install** pane and `claude plugin details` enumerate every component — use them. Hooks and MCP servers deserve the hardest look because they're where arbitrary execution lives. The **Context cost** line in the same pane is a second signal: a \"review skill\" that wants to add thousands of tokens per turn is worth a second look.\n\n### Minimize the trust radius\n\nPrefer the smallest primitive that does the job: a plain skill over a full plugin, a local script over a remote MCP server, where feasible. Smaller surface, smaller blast radius. `defaultEnabled: false` on a marketplace entry lets you ship something installed-but-off until a user deliberately opts in.\n\n### Pin to reviewed commits\n\nUse `sha` pins in the marketplace's plugin `source` so everyone runs exactly the bytes you audited. An unpinned git source means \"every push is auto-deployed to the team\" — fine for your own actively-developed plugins, dangerous for anything you didn't write.\n\n### Run a private, allowlisted marketplace\n\nHost the team marketplace in a **private** GitHub repo (the docs and researchers both recommend this over public distribution; Claude Code installs from private repos using your existing git credential helpers). Then close the door with managed settings:\n\n```json\n{\n  \"strictKnownMarketplaces\": [\n    { \"source\": \"github\", \"repo\": \"acme/approved-plugins\" }\n  ],\n  \"extraKnownMarketplaces\": {\n    \"approved-plugins\": { \"source\": { \"source\": \"github\", \"repo\": \"acme/approved-plugins\" } }\n  }\n}\n```\n\nNow the only shelf anyone can pull from is the one you review. The docs are explicit that `strictKnownMarketplaces` is checked *before* any network or filesystem operation, on add **and** on every install/update/refresh/auto-update, and it cannot be overridden by user or project settings. Note one matching gotcha: it uses **exact** matching (a trailing slash, `.git` suffix, or `ssh://` vs `https://` are different values), so prefer a `hostPattern` entry over a literal URL when a repo can be cloned by more than one URL form."
  },
  {
   "heading": "When to use what",
   "body": "Not everything should be a plugin, and not every plugin needs a marketplace. Match the mechanism to the sharing need.\n\n| Situation | Use | Why |\n|---|---|---|\n| Personal workflow, one machine | standalone `.claude/skills/` | Short names (`/review`), zero packaging, instant iteration. |\n| One project's quirk, not shared | project `.claude/` (committed) | Travels with the repo; no namespace ceremony. |\n| Reusable across *your* projects | plugin via `--plugin-dir`, or `claude plugin init` in `~/.claude/skills/` | Versioning + one source of truth, no marketplace overhead yet. |\n| Shared with the team, kept in sync | **plugin in a private marketplace** | The shelf: add once, install, pull updates by pushing to the repo. |\n| Distributed to the community | `claude-community` marketplace (submitted + screened) | Discovery + automated safety screening + SHA-pinned catalog. |\n\nThe official docs' rule of thumb (verbatim Tip): *\"Start with standalone configuration in `.claude/` for quick iteration, then convert to a plugin when you're ready to share.\"* Don't reach for a marketplace on day one of a skill — reach for it when \"three people rebuilt the same thing\" becomes true.\n\n### Common pitfalls / gotchas (consolidated)\n\n- **Wrong directory for components.** `commands/`, `skills/`, `agents/`, `hooks/` go at the **plugin root**, never inside `.claude-plugin/`. Only `plugin.json` lives in `.claude-plugin/`.\n- **Version string never bumped** → `/plugin update` is a silent no-op. Bump `version` every release, or omit it to use the commit SHA. Never set it in both `plugin.json` and the marketplace entry.\n- **`../` and absolute paths break after install.** Plugins are copied to `~/.claude/plugins/cache`; reference internal files with `${CLAUDE_PLUGIN_ROOT}`, persist update-surviving state under `${CLAUDE_PLUGIN_DATA}`, and use symlinks if you must share files across plugins.\n- **Relative-path sources fail on URL-added marketplaces.** A bare URL to `marketplace.json` downloads only that file, not the plugin dirs. Use git-hosted marketplaces (or `github`/`npm`/`url` plugin sources) when you need `./` paths.\n- **`marketplace remove` uninstalls its plugins.** To refresh without losing installs, use `marketplace update`.\n- **Reserved / impersonating marketplace names are rejected.** Don't name yours `anthropic-*` or anything that looks official.\n- **Plugin skills not appearing?** Try `/reload-plugins`; check the `/plugin` Errors tab; as a last resort clear the cache: `rm -rf ~/.claude/plugins/cache`, restart, reinstall.\n- **Trust is cumulative and opaque.** One install endorses every skill, hook, and MCP server in the bundle, transitively. Review before Trust; pin SHAs; keep the team marketplace private and `strictKnownMarketplaces`-allowlisted."
  }
 ],
 "stage-5": [
  {
   "heading": "Why a CLAUDE.md note is not a control",
   "body": "Stage 4 gave the team a shared `CLAUDE.md`, skills, and a plugin or two. That file shapes what Claude *tries* to do. It does not change what Claude Code *allows*. The distinction is load-bearing, and the docs are blunt about it:\n\n> Permission rules are enforced by Claude Code, not by the model. Instructions in your prompt or `CLAUDE.md` shape what Claude tries to do, but they don't change what Claude Code allows. ([permissions docs](https://code.claude.com/docs/en/permissions))\n\nThis means every must-not you wrote in prose — \"never read `.env`\", \"don't `git push` to main\", \"don't install plugins from random marketplaces\" — is a *suggestion to a probabilistic system under adversarial pressure* (a prompt injection in a fetched web page, a malicious skill, a confused autocomplete). The model can be talked out of a CLAUDE.md rule. It cannot be talked out of a deny rule, because the deny rule lives in the harness and is evaluated before the tool call runs.\n\nGovernance is the act of moving the must-nots out of prose and into three enforcement layers:\n\n- **Permission rules** — allow/ask/deny glob patterns the harness checks on every tool call.\n- **Permission modes** — the autonomy dial that sets the baseline for how often it even asks.\n- **Managed settings** — org-level policy that sits *above* everything a developer can change, so a lower scope cannot relax it.\n\nThe second half of this stage is the money side of governance: **seats and plan gating** — making sure you are not budgeting for access you already have, or assuming a feature is in your plan when it is a research preview. Both halves answer the same question: what does the *system* guarantee, independent of any individual's good intentions?"
  },
  {
   "heading": "Settings precedence: the resolution order that makes governance possible",
   "body": "Claude Code reads configuration from five scopes and resolves them in a fixed order. Highest wins:\n\n| Rank | Scope | File | Who controls it |\n|------|-------|------|-----------------|\n| 1 (highest) | **Managed** | `/etc/claude-code/managed-settings.json` (Linux/WSL), `/Library/Application Support/ClaudeCode/managed-settings.json` (macOS), `C:\\Program Files\\ClaudeCode\\managed-settings.json` (Windows) | Org admin / MDM |\n| 2 | **Command-line args** | `--permission-mode`, `--allowedTools`, `--disallowedTools`, etc. | Whoever launches the session |\n| 3 | **Local project** | `.claude/settings.local.json` (gitignored) | Individual dev, this machine |\n| 4 | **Shared project** | `.claude/settings.json` (committed) | The team, via the repo |\n| 5 (lowest) | **User** | `~/.claude/settings.json` | Individual dev, all projects |\n\nSource: [settings docs](https://code.claude.com/docs/en/settings). The legacy Windows path `C:\\ProgramData\\ClaudeCode\\managed-settings.json` is **no longer supported as of v2.1.75** — if you wrote MDM tooling against it, it silently stops applying. Migrate deployments to `C:\\Program Files\\ClaudeCode\\managed-settings.json`.\n\n### Two different merge behaviors — do not conflate them\n\nFor *most* scalar settings (`model`, `outputStyle`, `defaultMode`), higher precedence **overrides** lower: managed `model` beats user `model`, full stop.\n\nBut **permission rules do not override — they merge (concatenate)** across all five scopes. This is the single most important mechanical fact in this stage and the reason governance is enforceable. It gets its own section below.\n\n### The governance lever\n\nBecause managed settings sit at rank 1 and *cannot be overridden by a CLI flag* (rank 2), a managed deny rule is a hard floor. A developer cannot pass `--allowedTools 'Read(./.env.*)'` to climb over a managed `deny`. The docs are explicit: \"a managed settings deny cannot be overridden by `--allowedTools`, and `--disallowedTools` can add restrictions beyond what managed settings define.\" That is what makes managed settings a *control* and not a *default*."
  },
  {
   "heading": "Permission rules: the access-control primitive",
   "body": "A permission rule is a tool-name glob pattern living in the `permissions` object of a settings file. Three arrays:\n\n```json\n{\n  \"permissions\": {\n    \"allow\": [\n      \"Bash(npm run *)\",\n      \"Bash(git commit *)\",\n      \"Read(~/.zshrc)\"\n    ],\n    \"ask\": [\n      \"Bash(git push *)\"\n    ],\n    \"deny\": [\n      \"Read(./.env)\",\n      \"Read(./.env.*)\",\n      \"Read(./secrets/**)\",\n      \"Bash(curl *)\"\n    ]\n  }\n}\n```\n\nManage them live with `/permissions`, which lists every rule and the settings file it came from.\n\n### Rule shape\n\nA rule is `Tool` or `Tool(specifier)`. A bare tool name matches every use:\n\n- `Bash` — every shell command. `Bash(*)` is equivalent. As a **deny** rule, a bare name *removes the tool from Claude's context entirely* — Claude never even sees it. A scoped rule like `Bash(rm *)` leaves Bash available and blocks only matching calls.\n- `Bash(git push *)` — commands starting with `git push`. The space before `*` enforces a word boundary: `Bash(ls *)` matches `ls -la` but **not** `lsof`; `Bash(ls*)` matches both. `Bash(ls:*)` is an equivalent trailing-wildcard form (the `:*` suffix is only recognized at the *end* of a pattern). A single `*` spans multiple arguments, so `Bash(git * main)` matches `git push origin main` and `git merge main`.\n- `Read(./.env.*)` — file reads follow [gitignore semantics](https://code.claude.com/docs/en/permissions). A bare filename matches at any depth: `Read(.env)` == `Read(**/.env)`. Anchors matter: `/path` is **relative to project root**, `//path` is a true absolute path, `~/path` is home, and a bare `path` or `./path` is **relative to the current directory**. `Read(/Users/alice/x)` is NOT absolute — it is `<project>/Users/alice/x`; use `Read(//Users/alice/x)` for the real absolute path.\n- `WebFetch(domain:example.com)` — matches the hostname (case-insensitive). `WebFetch(domain:*.example.com)` matches subdomains at any depth but **not** the apex `example.com`.\n- `mcp__github` / `mcp__github__*` / `mcp__github__get_*` — MCP tools. The allow-side server segment must be glob-free. `mcp__*` (a tool-name glob) in a **deny** array kills every MCP tool across every server; as an *allow* glob, `mcp__*` is skipped with a warning and auto-approves nothing.\n\n### Compound commands and process wrappers\n\nClaude Code is shell-aware: a rule like `Bash(safe-cmd *)` does **not** authorize `safe-cmd && other-cmd` — each subcommand (split on `&&`, `||`, `;`, `|`, `&`, newlines) must match independently. A fixed set of wrappers is stripped before matching, so `Bash(npm test *)` also matches `timeout 30 npm test` (recognized wrappers: `timeout`, `time`, `nice`, `nohup`, `stdbuf`, and bare `xargs`). Environment-runner wrappers like `npx`, `docker exec`, `devbox run`, and `mise exec` are **not** stripped — `Bash(devbox run *)` would match `devbox run rm -rf .`, so write an explicit inner rule like `Bash(devbox run npm test)`.\n\n### Match by input parameter (deny/ask only)\n\nA newer form, `Tool(param:value)`, matches a top-level scalar input parameter — useful for autonomy gating: `Agent(model:opus)`, `Agent(isolation:worktree)`, `Bash(run_in_background:true)`. This is **deny/ask only** — an allow rule for one parameter wouldn't establish the whole call is safe. The value supports `*` as a wildcard; a parameter the model omits is never matched (so `Agent(model:*)` does not match a call that leaves `model` unset). Canonicalized fields (`command` for Bash, `file_path` for Read/Edit/Write, `url` for WebFetch, `path` for Grep/Glob) are *not* matchable this way — `Bash(command:rm *)` is ignored with a startup warning; use `Bash(rm *)`.\n\nAll of the above is verified against the [permissions docs](https://code.claude.com/docs/en/permissions)."
  },
  {
   "heading": "How rules evaluate: deny → ask → allow, first-match-wins, arrays merge",
   "body": "This is the algorithm. Memorize it; it is the whole enforcement model.\n\n### Evaluation order\n\nRules are checked in the order **deny, then ask, then allow**. The first match in that order wins, and **specificity does not change the order**:\n\n> A broad deny rule like `Bash(aws *)` blocks every matching call, including calls that also match a narrower allow rule like `Bash(aws s3 ls)`, so a deny rule cannot carry allowlist exceptions. ([permissions docs](https://code.claude.com/docs/en/permissions))\n\nSo you cannot write `deny: Bash(aws *)` + `allow: Bash(aws s3 ls)` and expect the narrow allow to punch through. Deny is absolute for anything it matches. The same precedence holds between ask and allow: a matching `ask` rule prompts even when a more specific `allow` also matches. If you need exceptions, you invert: allow the safe specifics, and use a `PreToolUse` hook or a narrower deny for the dangerous ones.\n\n### Arrays merge across scopes — the floor property\n\nThe `allow`/`ask`/`deny` arrays from **all five scopes are concatenated** before evaluation. Combined with deny-first, this gives the property that makes org governance real:\n\n> If a tool is denied at any level, no other level can allow it. ([permissions docs](https://code.claude.com/docs/en/permissions))\n\nWork the direction both ways:\n\n- A **managed** `deny: Read(./.env.*)` is a floor. No user, project, local, or CLI allow can lift it. This is the mechanism behind \"deny secret reads\" org-wide.\n- A **user** `deny` also floors a **project** `allow`. If a dev personally denies `Bash(git push *)` in `~/.claude/settings.json`, a repo's committed `allow: Bash(git push *)` cannot override it — deny from any scope is evaluated before any allow.\n\n### A worked trace\n\nManaged: `deny: [\"Read(./.env.*)\"]`. Project: `allow: [\"Read(./**)\"]`. Claude tries to read `./.env.production`:\n\n1. Merged deny array contains `Read(./.env.*)` → matches `./.env.production` → **denied**. Allow is never consulted. The project's broad allow is irrelevant.\n\nThis is why you put the secret-read denials and the destructive-command denials in **managed** settings, not the repo: the repo is editable by anyone with commit access; managed settings are not."
  },
  {
   "heading": "Permission modes: the orthogonal autonomy dial",
   "body": "Permission *rules* answer *what is allowed*. Permission *modes* answer *how aggressively Claude acts on what's allowed* — how often it pauses to ask at all. They are orthogonal: a mode sets the baseline, rules layer on top, and **deny rules and explicit `ask` rules apply in every mode, including `bypassPermissions`** (allow rules have no effect in `bypassPermissions` because everything is already approved).\n\n| Mode | Runs without asking | Best for | Set via |\n|------|---------------------|----------|---------|\n| `default` | Reads only | Sensitive work, getting started | baseline |\n| `plan` | Reads + read-only shell (no edits at all; proposes a plan) | Exploring before changing | `Shift+Tab`, `/plan`, `--permission-mode plan` |\n| `acceptEdits` | Reads + file edits + common fs commands (`mkdir`, `touch`, `rm`, `rmdir`, `mv`, `cp`, `sed`) in-scope | Iterating on code you'll review via `git diff` | `Shift+Tab`, flag, `defaultMode` |\n| `auto` | Everything, with a classifier model checking each call | Long tasks, reducing prompt fatigue | `--permission-mode auto`, admin-gated |\n| `dontAsk` | Only pre-approved (allow rules + read-only Bash); everything else auto-**denied** | Locked-down CI | `--permission-mode dontAsk` |\n| `bypassPermissions` | Everything, no prompts | Isolated containers/VMs only | `--permission-mode bypassPermissions` / `--dangerously-skip-permissions` |\n\nSource: [permission-modes docs](https://code.claude.com/docs/en/permission-modes).\n\n### Setting and cycling\n\n```json\n{ \"permissions\": { \"defaultMode\": \"plan\" } }\n```\n\nIn-session, `Shift+Tab` cycles `default → acceptEdits → plan`. The other modes are deliberately *not* in the default cycle: `auto` slots in only when your account qualifies and you opt in; `bypassPermissions` appears only after you started with an enabling flag (`--permission-mode bypassPermissions`, `--dangerously-skip-permissions`, or `--allow-dangerously-skip-permissions`); `dontAsk` never appears in the cycle at all and must be set with `--permission-mode dontAsk`.\n\n### Protected paths — never auto-approved except in bypass\n\nWrites to a fixed set of protected paths are **never auto-approved in any mode except `bypassPermissions`** — and crucially, a `permissions.allow` rule does **not** pre-approve them (the safety check runs *before* allow rules). The set is broader than just `.git` and `.claude`: it includes `.config/git`, `.vscode`, `.idea`, `.husky`, `.cargo`, `.devcontainer`, `.yarn`, `.mvn`, plus files like `.gitconfig`, `.zshrc`, `.npmrc`, `.envrc`, `.mcp.json`, and `.claude.json`. In `auto` mode these route to the classifier; in `dontAsk` they are denied; in prompting modes you get a prompt.\n\n### The two notes that bite teams\n\n- **`defaultMode: \"auto\"` is ignored from project/local settings.** As of v2.1.142, Claude Code ignores `auto` set in `.claude/settings.json` or `.claude/settings.local.json` so a repository cannot grant itself auto mode. It must live in `~/.claude/settings.json` or managed settings.\n- **`bypassPermissions` got more dangerous in v2.1.126.** It now executes writes to protected paths like `.git/` and `.claude/` that earlier versions still prompted for. Only `rm -rf /`, `rm -rf ~`, and explicit `ask` rules still stop it."
  },
  {
   "heading": "auto mode: a classifier, not a free pass",
   "body": "`auto` mode is worth its own section because teams reach for it to \"reduce prompts\" and misjudge what it guarantees. It is a **research preview** and requires **Claude Code v2.1.83 or later** ([permission-modes docs](https://code.claude.com/docs/en/permission-modes)).\n\n### What it actually does\n\nInstead of prompting you, a **separate classifier model** reviews each non-trivial action before it runs and blocks anything that \"escalates beyond your request, targets unrecognized infrastructure, or appears driven by hostile content Claude read.\" Reads and working-directory edits (outside protected paths) skip the classifier; shell commands and network calls go through it — adding a round-trip and counting toward your token usage. The classifier sees user messages, tool calls, and your `CLAUDE.md`, but **tool results are stripped**, so hostile content in a fetched page can't manipulate it directly.\n\n### Blocked by default (a sample of the real list)\n\n- Downloading and executing code, like `curl | bash`; sending sensitive data to external endpoints\n- Production deploys and migrations; mass cloud-storage deletion; granting IAM or repo permissions\n- **Force push, or pushing directly to `main`**\n- `git reset --hard`, `git checkout -- .`, `git restore .`, `git clean -fd`, `git stash drop`/`clear`, and `git commit --amend` on a HEAD not created this session (presumed to discard work)\n- `terraform`/`pulumi`/`cdk`/`terragrunt destroy`, and applying a plan that destroys resources\n\nAllowed by default: local file ops in the working dir, installing declared dependencies, reading `.env` and sending credentials to their matching API, read-only HTTP, and pushing to the branch you started on or one Claude created. Run `claude auto-mode defaults` to print the full lists.\n\n### Three things to know before you trust it\n\n1. **It drops your dangerous broad allow rules on entry.** Blanket `Bash(*)`/`PowerShell(*)`, wildcarded interpreters like `Bash(python*)`, package-manager run commands, and `Agent` allow rules are dropped while in auto mode (restored on exit). Narrow rules like `Bash(npm test)` carry over. So a repo that allow-listed `Bash(*)` does not get a blank check.\n2. **Conversational boundaries are honored but fragile.** Telling Claude \"don't deploy until I review\" makes the classifier block matching actions — but boundaries are not stored as rules; the classifier re-reads them from the transcript each check, so **context compaction can lose the message that stated it**. For a hard guarantee, use a deny rule, not a sentence.\n3. **Admin-gated and model-gated.** On Team/Enterprise an admin must enable it in [Claude Code admin settings](https://claude.ai/admin-settings/claude-code), and can lock it off with `permissions.disableAutoMode: \"disable\"`. Model gating: Opus 4.6+ or Sonnet 4.6 on the Anthropic API; on Bedrock/Vertex/Foundry only **Opus 4.7 and Opus 4.8**, and off until `CLAUDE_CODE_ENABLE_AUTO_MODE=1` is set (v2.1.158+)."
  },
  {
   "heading": "A team baseline: the three files you actually deploy",
   "body": "Here is a concrete, copyable governance baseline. Three files, three scopes, each doing the job its scope is good at.\n\n### 1. Managed settings (admin-deployed, un-overridable floor)\n\n`/etc/claude-code/managed-settings.json` (Linux/WSL; adjust path per OS):\n\n```json\n{\n  \"permissions\": {\n    \"deny\": [\n      \"Read(./.env)\",\n      \"Read(./.env.*)\",\n      \"Read(./secrets/**)\",\n      \"Read(~/.ssh/**)\",\n      \"Read(~/.aws/**)\",\n      \"Bash(curl *)\",\n      \"Bash(wget *)\"\n    ],\n    \"disableBypassPermissionsMode\": \"disable\",\n    \"disableAutoMode\": \"disable\"\n  },\n  \"strictKnownMarketplaces\": [\n    { \"source\": \"github\", \"repo\": \"acme-corp/approved-plugins\" }\n  ],\n  \"allowManagedMcpServersOnly\": true,\n  \"requiredMinimumVersion\": \"2.1.150\"\n}\n```\n\nEverything here is a floor: secret reads are denied org-wide and no dev can lift them; bypass/auto modes are off; only the approved marketplace can be added; only managed MCP servers run; and `requiredMinimumVersion` makes clients below 2.1.150 **refuse to start** (so a known-bad release can't be used). Note `requiredMinimumVersion` is the *hard* floor — the related, non-managed `minimumVersion` only blocks auto-update/`claude update` from going below it and never blocks startup.\n\n### 2. Shared project settings (committed, team conventions)\n\n`.claude/settings.json`:\n\n```json\n{\n  \"permissions\": {\n    \"allow\": [\n      \"Bash(npm run *)\",\n      \"Bash(git commit *)\",\n      \"Bash(git status)\",\n      \"Bash(git diff *)\"\n    ],\n    \"ask\": [\n      \"Bash(git push *)\"\n    ],\n    \"defaultMode\": \"plan\"\n  }\n}\n```\n\nThe repo grants the routine commands and forces a confirmation prompt on `git push`. `defaultMode: \"plan\"` means a fresh session starts read-only and proposes before it edits.\n\n### 3. Local settings (gitignored, per-dev convenience)\n\n`.claude/settings.local.json` — a dev's personal allow-list additions for their own machine, e.g. `\"allow\": [\"Bash(docker compose up *)\"]`. It can loosen *only within* what managed/project allow; it can never override a higher-scope deny, because deny from any scope is evaluated first."
  },
  {
   "heading": "Managed-only keys: the org control surface",
   "body": "A specific set of keys are read **only** from managed settings; placing them in user or project files does nothing. These are the levers an admin actually pulls. Verified against the [permissions](https://code.claude.com/docs/en/permissions) and [settings](https://code.claude.com/docs/en/settings) docs.\n\n| Key | Effect |\n|-----|--------|\n| `strictKnownMarketplaces` | `[]` = no marketplaces addable at all; a list = only those exact sources (with `hostPattern`/`pathPattern` regex forms). Checked *before* any download. |\n| `blockedMarketplaces` | Blocklist of marketplace sources; blocked sources are checked before download and never touch the filesystem. |\n| `allowedMcpServers` / `deniedMcpServers` | Allow/deny specific MCP servers by name (`deniedMcpServers` still merges from all sources). |\n| `allowManagedMcpServersOnly` | Only managed `allowedMcpServers` are respected (deny still merges from all sources). |\n| `allowManagedPermissionRulesOnly` | User/project **cannot define any** allow/ask/deny rules — only managed rules apply. (Does not affect the MCP allowlist.) |\n| `allowManagedHooksOnly` | Only managed/SDK/force-enabled-plugin hooks load; all user/project hooks blocked. |\n| `strictPluginOnlyCustomization` | `true` blocks skills, agents, hooks, and MCP from user/project sources — they must come from plugins or managed settings. Or an array like `[\"skills\",\"hooks\"]` to lock only those surfaces. |\n| `requiredMinimumVersion` / `requiredMaximumVersion` | Refuse to start outside a version window (hard floor/ceiling). |\n| `wslInheritsWindowsSettings` | WSL reads the Windows policy chain in addition to `/etc/claude-code`. |\n| `forceLoginMethod` / `forceLoginOrgUUID` | Pin auth to a method / org. |\n| `forceRemoteSettingsRefresh` | Fail-closed: block startup until remote managed settings are freshly fetched, exit if the fetch fails. |\n| `blockedMarketplaces`, `pluginTrustMessage`, `allowAllClaudeAiMcps`, `channelsEnabled`, `allowedChannelPlugins` | Additional managed-only plugin/MCP/channel controls. |\n\nNote the version-key trap: `requiredMinimumVersion` (managed-only, **blocks startup**) is distinct from `minimumVersion` (any scope, never blocks startup — only gates auto-update). Use the former for enforcement.\n\n### Drop-in directory\n\nManaged settings can be split into `managed-settings.d/*.json`, merged **alphabetically** (`10-telemetry.json` → `20-security.json`): scalars override, **arrays concatenate and de-dupe**, objects deep-merge, dotfiles ignored. This lets MDM tooling ship security policy and telemetry policy as separate files without coordinating edits to one file.\n\n### The escalating lockdown ladder\n\n`strictPluginOnlyCustomization` and the `allowManaged*Only` family form a ratchet. Default: anyone can add skills/hooks/MCP locally. One notch: only plugins or managed settings can. Full: `allowManagedPermissionRulesOnly` + `allowManagedHooksOnly` + `allowManagedMcpServersOnly` means the *entire* extension surface is admin-defined and a developer's machine cannot introduce a single rule, hook, or server you didn't ship."
  },
  {
   "heading": "Plugin governance: why this is the highest-leverage deny",
   "body": "Stage 4 introduced plugins as a convenience. This stage names the risk: **installing a plugin is one trust decision that activates its skills, hooks, and MCP servers as code**. A hook is an arbitrary shell command that runs on tool events. A skill is instructions the model will follow. An MCP server is a process with tools. One `/plugin install` turns all of that on at once.\n\nThis is not theoretical. An audit of open third-party marketplaces found **76 confirmed malicious payloads among ~3,984 skills (13.4% rated critical)**, and plugin-injection attacks against Claude Code have been demonstrated ([Pluto Security](https://pluto.security/blog/claude-extension-ecosystem-security-practitioner-guide/), [PromptArmor](https://www.promptarmor.com/resources/hijacking-claude-code-via-injected-marketplace-plugins)).\n\n### The marketplace format you are gating\n\nA marketplace is a `.claude-plugin/marketplace.json` catalog at the repo root:\n\n```json\n{\n  \"name\": \"my-plugins\",\n  \"owner\": { \"name\": \"Your Name\" },\n  \"plugins\": [\n    {\n      \"name\": \"quality-review-plugin\",\n      \"source\": \"./plugins/quality-review-plugin\",\n      \"description\": \"Adds a quality-review skill for quick code reviews\"\n    }\n  ]\n}\n```\n\nUsers add and install with:\n\n```shell\n/plugin marketplace add ./my-marketplace      # or owner/repo for a GitHub-hosted catalog\n/plugin install quality-review-plugin@my-plugins\n```\n\nThey refresh a local copy with `/plugin marketplace update`.\n\n### The governance move\n\nDon't tell people \"only install from the approved marketplace.\" Enforce it. In managed settings:\n\n```json\n{\n  \"strictKnownMarketplaces\": [\n    { \"source\": \"github\", \"repo\": \"acme-corp/approved-plugins\" },\n    { \"source\": \"hostPattern\", \"hostPattern\": \"^github\\\\.acme\\\\.com$\" }\n  ]\n}\n```\n\n`strictKnownMarketplaces: []` is complete lockdown — no marketplace addable. A list restricts to exact matches, with `hostPattern`/`pathPattern` regex forms for self-hosted git (use `pathPattern: \".*\"` to allow any filesystem path while still gating network sources). The check runs **before any network/filesystem operation**, and **on marketplace add and on plugin install, update, refresh, and auto-update** — so a marketplace added before the policy existed gets frozen out at update time too. Exact matching does not normalize URLs (a trailing slash, `.git` suffix, or `ssh://` vs `https://` are distinct), so prefer a `hostPattern` over a literal URL. Pair it with `extraKnownMarketplaces` (a non-managed key) to *pre-register* the approved marketplace so devs don't even run `/plugin marketplace add`. Source: [plugin-marketplaces docs](https://code.claude.com/docs/en/plugin-marketplaces)."
  },
  {
   "heading": "Defense in depth: rules, hooks, and the sandbox",
   "body": "Permission rules are necessary but not sufficient, and the docs are careful about their boundaries. Three failure modes worth internalizing.\n\n### Read/Edit deny rules don't stop subprocesses\n\n> Read and Edit deny rules apply to Claude's built-in file tools and to file commands Claude Code recognizes in Bash, such as `cat`, `head`, `tail`, and `sed`. They do not apply to arbitrary subprocesses that read or write files indirectly, like a Python or Node script that opens files itself. ([permissions docs](https://code.claude.com/docs/en/permissions))\n\nSo `deny: Read(./.env.*)` stops `cat .env` and the Read tool, but a `python -c 'print(open(\".env\").read())'` slips past the rule layer. For OS-level enforcement that blocks *all* processes, you need [sandboxing](https://code.claude.com/docs/en/sandboxing) — filesystem/network isolation for Bash and its children. Rules + sandbox are explicitly designed as complementary layers; the sandbox's filesystem boundary *merges* Read/Edit deny rules with the `sandbox.filesystem` settings, and its network boundary merges `WebFetch(domain:...)` rules with the sandbox `allowedDomains`/`deniedDomains`.\n\n### Bash argument constraints are fragile\n\n`Bash(curl http://github.com/ *)` does not constrain curl to GitHub — `-X GET` before the URL, `https://`, a `-L` redirect, or `URL=... && curl $URL` all evade it. The doc's own recommendation: deny `curl`/`wget` wholesale and route allowed fetches through `WebFetch(domain:...)`, or validate in a `PreToolUse` hook. (Note: WebFetch alone does not block network access — if Bash is allowed, Claude can still reach any URL via `curl`.)\n\n### Hooks as runtime permission logic\n\nA `PreToolUse` hook runs before the permission prompt and can deny, force a prompt, or allow. But the precedence is one-directional: a hook **cannot bypass a deny or ask rule** (those still apply regardless of what the hook returns, preserving the managed-deny floor), while a hook that exits **code 2 does** override an allow rule — it stops the call before permission rules are even evaluated. The idiom \"allow all Bash, block a few\" is: put `Bash` in allow, then register a hook that rejects the specific dangerous commands. The hook is where you put logic too contextual for a glob."
  },
  {
   "heading": "Seats & plan gating: stop paying for access you already have",
   "body": "The other half of governance is not paying — in money or in wrong assumptions — for what your plan does or doesn't include. The single most common procurement mistake here is treating the Premium seat as the access gate for Claude Code. It is not.\n\n### The fact that resets the budget\n\n> Claude Code is included with **every** Team plan seat — Standard *and* Premium. Premium only buys more usage for heavier workloads; it is **not** the access gate. ([support 11845131](https://support.claude.com/en/articles/11845131), [support 9266767](https://support.claude.com/en/articles/9266767))\n\nA Premium/Code-seat *requirement* applies only to **legacy Enterprise**; on new or self-serve Enterprise, Claude Code is included with every seat too. So if you are on a Team plan, you do not need to buy Premium seats to give engineers Claude Code; you buy Premium only for the people who will hit usage ceilings. Sizing the rollout as \"N engineers × Premium\" when most of them are light users is a straightforward overspend.\n\n### Decision frame\n\n| Question | Answer |\n|----------|--------|\n| Does every Team seat get Claude Code? | Yes — Standard and Premium both. |\n| What does Premium buy? | More usage, not access. |\n| When is a Code/Premium seat *required*? | Legacy Enterprise only (not new/self-serve Enterprise). |\n| Who toggles auto mode, Remote Control, web sessions org-wide? | An admin, in [Claude Code admin settings](https://claude.ai/admin-settings/claude-code). |\n\n### The governance read\n\nPlan gating *is* a control surface, just a commercial one. Admin-level toggles (auto mode on/off, Remote Control, web sessions, channels) live in the admin console and behave like managed settings: org-wide, not per-dev. Pair the seat decision with the managed-settings decision — they're the two halves of \"what is this org allowed to do.\""
  },
  {
   "heading": "Plan-gating the autonomous extras: don't budget a research preview as a feature",
   "body": "The flip side of \"don't overpay for access you have\" is \"don't assume access you don't have.\" Two headline-grabbing capabilities are easy to write into a team plan that doesn't actually include them.\n\n### Computer use and phone Dispatch\n\n**Computer use** (Claude driving a desktop, enabled in the desktop-app settings) and **phone Dispatch** (Claude acting via a phone) are a **Pro/Max research preview on macOS and Windows** ([Anthropic blog](https://claude.com/blog/dispatch-and-computer-use), [support 12138966](https://support.claude.com/en/articles/12138966)).\n\nThe careful claim — and the reason to state it carefully — is this: the docs do **not** document Team/Enterprise *exclusion*. \"Not on Team\" is an *inference*, not a documented fact. So the accurate way to write it in a plan is:\n\n> Computer use and phone Dispatch are **not part of the documented Team plan** — they are a Pro/Max research preview. Do not budget a Team rollout around them.\n\nThe distinction matters because a research preview is unstable scope: it can change, move plans, or be withdrawn. Building a team workflow on top of it is building on sand, and assuming it's in your Team seat because you saw the announcement is exactly the recency-bias trap — the announcement is salient, the plan-scoping footnote is not.\n\n### The general rule\n\nWhen a capability is described as a \"research preview,\" treat it as **off the governance map**: don't deny-rule around it (it may not exist for your users), don't budget for it, and don't write runbooks that depend on it. Revisit at GA. The verified-claims discipline here — separating *documented fact* from *reasonable inference* — is itself the skill: governance decisions made on inferences should be labeled as such, so the next person knows what to recheck when the docs update."
  },
  {
   "heading": "When to use what: a governance decision table",
   "body": "Put the right control at the right scope. The recurring mistake is reaching for the model (CLAUDE.md) when the situation calls for the harness (a rule), or reaching for a rule when it calls for OS-level isolation (the sandbox).\n\n| Goal | Wrong tool | Right tool | Scope |\n|------|-----------|-----------|-------|\n| Never read secrets | CLAUDE.md \"don't read .env\" | `deny: Read(./.env.*)` | **Managed** (floor) |\n| Block secrets even from a `python` subprocess | deny rule alone | Sandbox filesystem isolation | Managed + sandbox |\n| Confirm before pushing | CLAUDE.md note | `ask: Bash(git push *)` | Project |\n| Only approved plugins | \"please use our marketplace\" | `strictKnownMarketplaces` | **Managed** |\n| No untrusted MCP servers | trust | `allowManagedMcpServersOnly` | **Managed** |\n| Read-only exploration by default | habit | `defaultMode: \"plan\"` | Project |\n| Unattended CI run, locked down | `bypassPermissions` | `dontAsk` + explicit allow-list | CLI / project |\n| Long autonomous task, some oversight | `bypassPermissions` | `auto` mode | User / CLI |\n| Fully autonomous in a throwaway container | nothing | `bypassPermissions` | CLI, container only |\n| Dev can't introduce *any* local rule | hope | `allowManagedPermissionRulesOnly` | **Managed** |\n\n### Common pitfalls / gotchas, consolidated\n\n- **Permission rules MERGE; other settings OVERRIDE.** Forgetting this makes a managed deny look like it \"isn't applying\" when in fact it's working — the lower-scope allow is simply being out-prioritized.\n- **Deny carries no exceptions.** `deny: Bash(aws *)` + `allow: Bash(aws s3 ls)` blocks `aws s3 ls`. Invert to a narrow allow + hook instead.\n- **`/path` is project-relative, not absolute.** Use `//path` for true absolute. The single most common Read-rule bug.\n- **Allow rules don't pre-approve protected-path writes** (`.git`, `.claude`, `.vscode`, `.npmrc`, …) — the safety check runs before allow rules in every mode but `bypassPermissions`.\n- **`defaultMode: \"auto\"` is ignored from repo settings** (v2.1.142+). Set it in `~/.claude` or managed.\n- **`bypassPermissions` writes to `.git`/`.claude`** as of v2.1.126 — containers/VMs only, and lock it off org-wide with `disableBypassPermissionsMode: \"disable\"`.\n- **Bash argument filters are evadable** — deny the binary, allow via `WebFetch(domain:...)` or a hook.\n- **A plugin install is one trust decision for skills+hooks+MCP.** Gate the marketplace, don't gate intentions.\n- **`requiredMinimumVersion` (managed, blocks startup) ≠ `minimumVersion` (any scope, gates auto-update only).** Use the former to refuse known-bad releases.\n- **Premium ≠ access to Claude Code on Team.** Buy Premium for usage headroom, not to unlock the tool.\n- **Computer use / Dispatch are a Pro/Max research preview**, not documented Team features — don't plan around them."
  }
 ],
 "stage-6": [
  {
   "heading": "The decision you're actually making",
   "body": "By now you know the parts: skills, subagents, hooks, MCP servers, permission rules, `AGENTS.md`. That was the *vocabulary*. This stage is the *grammar* — the rule that tells you where each part goes so a five-person team (or fifty) shares one coherent setup instead of fifty divergent ones.\n\nThe question your team is waiting on is not \"what is a skill?\" It is: **when someone writes a useful skill, where does it live so everyone gets it, it stays versioned, and it works whether they drive Claude Code or Codex?** Get this wrong and you hit the two classic failure modes:\n\n- **Sprawl** — every engineer hand-copies skills into their own `~/.claude/`, they drift, and \"works on my machine\" infects your agent tooling.\n- **Lock-in** — everything lives in one person's local config, nobody can review it, and onboarding a new hire is a 40-minute Slack thread.\n\nThe fix is a single mental model: **the four-tier model**. Decide the tier for each shared thing *once*, write it down, and the rest is mechanics. The verified team-setup guidance ([Anthropic, *Steering Claude Code*](https://claude.com/blog/steering-claude-code-skills-hooks-rules-subagents-and-more); [*Settings*](https://code.claude.com/docs/en/settings)) is exactly this: shared setup falls into four tiers by **where each thing lives**."
  },
  {
   "heading": "The four-tier model",
   "body": "Every shared artifact belongs to exactly one of four tiers, distinguished by **scope of ownership** and **distribution mechanism**:\n\n| Tier | What lives here | Mechanism | Who owns it | Committed to git? |\n|------|----------------|-----------|-------------|-------------------|\n| **1. Project config** | Rules, project-specific skills/agents, hooks, permission allowlist, marketplace declaration | `.claude/` dir + `CLAUDE.md`/`AGENTS.md` in the repo | The repo / its maintainers | Yes — in the repo |\n| **2. Cross-project tools** | Skills, agents, MCP servers, hooks reused across many repos | A **plugin marketplace** (a git repo of plugins) | A platform/devtools team | Yes — in the marketplace repo |\n| **3. Org policy** | Mandatory guardrails: which MCP servers are allowed, version floors, forced login org, marketplace allowlist | **Managed settings** (`managed-settings.json`, MDM) | IT / security | Deployed by IT, not editable by users |\n| **4. Personal config** | Individual preferences: model choice, theme, personal experimental skills | `~/.claude/settings.json`, `~/.codex/config.toml` | Each engineer | No — local only |\n\n### The decision rule\n\nAsk these questions in order; the first \"yes\" picks the tier:\n\n1. **Is it a non-negotiable the org must enforce?** → Tier 3 (managed settings — it sits at the top of precedence and can't be overridden).\n2. **Is it reused across repos by the team?** → Tier 2 (marketplace plugin).\n3. **Is it specific to one repo?** → Tier 1 (project config in `.claude/`).\n4. Otherwise → Tier 4 (personal).\n\n### Precedence at runtime\n\nThe tiers also map onto a **precedence order** when settings collide. Per [the settings docs](https://code.claude.com/docs/en/settings), Claude Code resolves settings highest-to-lowest:\n\n**Managed → command-line args → Local → Project → User.**\n\nManaged (Tier 3) always wins; personal *user-scope* defaults (Tier 4) lose to everything. Note one wrinkle: a repo's `.claude/settings.local.json` is the **Local** scope, which *outranks* the committed Project `.claude/settings.json` — so a personal per-repo override beats the team's committed project setting, but still loses to managed. This precedence is deliberate: org policy can't be overridden by a clever engineer, and a personal experiment in one repo doesn't silently leak into the team's shared config.\n\nOne exception runs through everything below: **permission rules merge, they don't override** (covered in Tier 1)."
  },
  {
   "heading": "Tier 1 — Project config in the repo",
   "body": "This is the tier most teams already half-have. Everything that is *true about this repo specifically* lives in `.claude/` and is committed, so cloning the repo gives you a fully-configured agent.\n\n### Layout\n\n```text\nmy-service/\n├── CLAUDE.md                 # rules/context for Claude Code\n├── AGENTS.md                 # same role for Codex & other agents (see portability)\n└── .claude/\n    ├── settings.json         # committed: team-shared settings (Project scope)\n    ├── settings.local.json   # gitignored: personal per-repo overrides (Local scope)\n    ├── skills/\n    │   └── run-migrations/SKILL.md\n    ├── agents/\n    │   └── db-reviewer.md\n    └── hooks/                # hook scripts; the hook *declarations* live in settings.json\n```\n\n### Committed vs local\n\nThe single most important split inside Tier 1: **`.claude/settings.json` is committed and shared (Project scope); `.claude/settings.local.json` is gitignored and personal (Local scope)** ([Settings docs](https://code.claude.com/docs/en/settings)). Put the team's permission allowlist, env vars, and hooks in `settings.json`. Put your personal one-off allows in `settings.local.json` — and remember Local outranks Project, so your local file wins any conflict with the committed one.\n\n```json\n// .claude/settings.json — committed, the whole team gets this\n{\n  \"$schema\": \"https://json.schemastore.org/claude-code-settings.json\",\n  \"permissions\": {\n    \"allow\": [\"Bash(npm run test:*)\", \"Bash(npm run lint)\"],\n    \"deny\": [\"Read(./.env)\", \"Read(./.env.*)\", \"Read(./secrets/**)\"],\n    \"ask\": [\"Bash(git push:*)\"]\n  },\n  \"env\": { \"NODE_ENV\": \"development\" }\n}\n```\n\n### Permissions merge — they don't override\n\nThe one setting that breaks the precedence-override rule: **permission rules (`allow`/`ask`/`deny`) merge across every scope rather than override** ([Settings docs](https://code.claude.com/docs/en/settings)). A project `deny` and a managed `deny` both apply; arrays from all tiers combine. That's why a project can safely *add* `deny` rules on top of org policy without weakening it — and why a project **cannot remove** an org-level `deny`. Layered, additive guardrails are the design intent."
  },
  {
   "heading": "Tier 2 — Cross-project tools in a marketplace",
   "body": "This is the tier that turns \"I wrote a nice skill\" into \"the team has it.\" When a skill, agent, or MCP integration is useful across repos, you do **not** copy it into each `.claude/`. You package it as a **plugin** and distribute it through a **plugin marketplace** ([Create plugins](https://code.claude.com/docs/en/plugins); [Marketplaces](https://code.claude.com/docs/en/plugin-marketplaces)).\n\n### Why a marketplace instead of copying\n\n| Use standalone `.claude/` (Tier 1) | Use a plugin/marketplace (Tier 2) |\n|------------------------------------|-----------------------------------|\n| Single project | Reused across multiple projects |\n| Personal / experimental | Shared with the team or community |\n| Short skill names (`/deploy`) | Versioned, namespaced (`/my-plugin:deploy`) |\n| Manual copy to share | `/plugin install`, central updates |\n\nA marketplace gives you **centralized discovery, version tracking, automatic updates, and one source of truth.** Bump the version in one repo; every engineer's next `/plugin update` (or background auto-update) pulls it. No drift.\n\n### Anatomy of a plugin\n\nA plugin is a directory with a manifest in `.claude-plugin/` plus component dirs *at the plugin root* — **not** inside `.claude-plugin/`:\n\n```text\nquality-tools/\n├── .claude-plugin/\n│   └── plugin.json          # ONLY the manifest goes here\n├── skills/\n│   └── code-review/SKILL.md\n├── agents/\n│   └── security-reviewer.md\n├── hooks/\n│   └── hooks.json\n└── .mcp.json                # MCP servers the plugin ships\n```\n\n```json\n// .claude-plugin/plugin.json\n{\n  \"name\": \"quality-tools\",\n  \"description\": \"Shared code-review skill + security agent\",\n  \"version\": \"1.2.0\",\n  \"author\": { \"name\": \"DevTools Team\" }\n}\n```\n\nThe `name` becomes the namespace: this plugin's `code-review` skill is invoked `/quality-tools:code-review`. Namespacing is what keeps two teams' `deploy` skills from colliding. Inside hooks and MCP configs, reference files with `${CLAUDE_PLUGIN_ROOT}` — the plugin is copied to a cache on install, so relative-to-cwd paths won't resolve (more in Pitfalls)."
  },
  {
   "heading": "Tier 2 mechanics — the marketplace file",
   "body": "A **marketplace** is itself just a git repo containing a catalog at `.claude-plugin/marketplace.json` that lists plugins and where to fetch each one ([Marketplaces](https://code.claude.com/docs/en/plugin-marketplaces)). Required top-level fields are `name`, `owner` (with a required `owner.name`), and `plugins`.\n\n```json\n// .claude-plugin/marketplace.json\n{\n  \"name\": \"acme-tools\",\n  \"owner\": { \"name\": \"DevTools Team\", \"email\": \"devtools@acme.com\" },\n  \"plugins\": [\n    {\n      \"name\": \"quality-tools\",\n      \"source\": \"./plugins/quality-tools\",\n      \"description\": \"Code review + security agent\"\n    },\n    {\n      \"name\": \"deploy-tools\",\n      \"source\": { \"source\": \"github\", \"repo\": \"acme-corp/deploy-plugin\" }\n    }\n  ]\n}\n```\n\n### Plugin source types (the load-bearing field)\n\nEach plugin's `source` says where to fetch *that plugin*. (Distinct from the *marketplace* source — where the catalog itself lives — which supports `ref` but not `sha`.) The choices:\n\n| `source` | Form | Use when |\n|----------|------|----------|\n| Relative path | `\"./plugins/x\"` (must start with `./`, resolved from the marketplace root) | Plugin lives in the same marketplace repo |\n| `github` | `{ \"source\": \"github\", \"repo\": \"org/repo\", \"ref\": \"v2\", \"sha\": \"<40-char>\" }` | Plugin in a separate GitHub repo |\n| `url` | `{ \"source\": \"url\", \"url\": \"https://gitlab.com/...git\", \"ref\": \"...\", \"sha\": \"...\" }` | Any git host (`.git` suffix optional) |\n| `git-subdir` | `{ \"source\": \"git-subdir\", \"url\": \"...\", \"path\": \"tools/plugin\" }` | Plugin in a **monorepo** subdir (sparse clone) |\n| `npm` | `{ \"source\": \"npm\", \"package\": \"@acme/plugin\", \"version\": \"^2.0\", \"registry\": \"https://npm.example.com\" }` | Distribute via npm (public or private registry) |\n\n### Versioning — the part teams get wrong\n\nClaude Code resolves a plugin's version from the first of these that is set: (1) `version` in `plugin.json`, (2) `version` in the marketplace entry, (3) the git commit SHA of the source.\n\n- **For internal, fast-iterating plugins, omit `version` entirely** on git-based sources (`github`/`url`/`git-subdir`/relative-in-git). Every commit then counts as a new version — the simplest setup.\n- **For stable releases, set `version` in `plugin.json` and bump it on every release.** If `plugin.json` says `\"1.0.0\"` and you push new commits *without* changing it, existing users get **nothing** — Claude Code sees the same version and keeps the cache.\n- **Never set `version` in both `plugin.json` and the marketplace entry** — the `plugin.json` value wins *silently* and can mask the marketplace one.\n- **Pin with `sha` for reproducibility.** When both `ref` and `sha` are set, the `sha` is the effective pin; on GitHub/GitLab/Bitbucket the install survives even if the branch named by `ref` is later deleted (some hosts like AWS CodeCommit can't fetch by SHA, so there the `ref` must still exist).\n\n### Installing\n\n```shell\n/plugin marketplace add acme-corp/claude-plugins   # GitHub owner/repo shorthand (append @ref to pin)\n/plugin install quality-tools@acme-tools           # plugin@marketplace\n/plugin marketplace update                          # pull new versions\n```\n\nNon-interactive equivalents exist for CI and scripting:\n\n```bash\nclaude plugin marketplace add acme-corp/claude-plugins --scope project\nclaude plugin install quality-tools@acme-tools\nclaude plugin validate .   # run this in CI before publishing\n```\n\n`--scope project` writes the declaration into the committed `.claude/settings.json` (the Tier-1/Tier-2 bridge below), rather than the per-user default."
  },
  {
   "heading": "Tier 2 — making the marketplace mandatory for the team",
   "body": "Discovery is good, but you don't want onboarding to depend on each engineer remembering to run `/plugin marketplace add`. You make the marketplace **part of the repo** so that trusting the project folder prompts installation automatically ([Marketplaces → Require for your team](https://code.claude.com/docs/en/plugin-marketplaces)).\n\nThis is the bridge between Tier 1 and Tier 2: the marketplace *lives* in Tier 2, but the *declaration that this repo uses it* is committed Tier-1 project config in `.claude/settings.json`:\n\n```json\n// .claude/settings.json  (committed)\n{\n  \"extraKnownMarketplaces\": {\n    \"acme-tools\": {\n      \"source\": { \"source\": \"github\", \"repo\": \"acme-corp/claude-plugins\" }\n    }\n  },\n  \"enabledPlugins\": {\n    \"quality-tools@acme-tools\": true,\n    \"deploy-tools@acme-tools\": true\n  }\n}\n```\n\nNow a new hire clones the repo, trusts the folder, and Claude Code prompts them to install exactly the plugins the team relies on. `enabledPlugins` decides which are on by default.\n\n### Gotcha: marketplace state is per-user, not per-project\n\nMarketplace registrations live once per user in `~/.claude/plugins/known_marketplaces.json`, and each plugin is copied into a versioned cache under `~/.claude/plugins/cache`. Consequences:\n\n- A plugin **cannot** reference files outside its own directory (no `../shared-utils`) — they aren't copied. Use `${CLAUDE_PLUGIN_ROOT}` for in-plugin paths, or symlinks for genuinely shared assets.\n- For containers/CI, pre-seed with `CLAUDE_CODE_PLUGIN_SEED_DIR`. Build the seed once (run Claude Code, install the plugins, copy `~/.claude/plugins`), then point the variable at it. The seed mirrors `~/.claude/plugins`:\n\n```text\n$CLAUDE_CODE_PLUGIN_SEED_DIR/\n  known_marketplaces.json\n  marketplaces/<name>/...\n  cache/<marketplace>/<plugin>/<version>/...\n```\n\nThe seed is **read-only** (auto-updates are disabled for it) and its entries take precedence over the user's config on each startup, so the image starts fully provisioned and clones nothing at runtime."
  },
  {
   "heading": "Tier 3 — Org policy in managed settings",
   "body": "Tiers 1 and 2 are *opt-in conveniences*. Tier 3 is *enforcement*. Managed settings are deployed by IT (MDM, or a file dropped by config management) and **cannot be overridden** by user, project, or local config — they sit at the top of the precedence order ([Settings](https://code.claude.com/docs/en/settings)).\n\n### Where the file lives\n\n| OS | Managed settings path |\n|----|----------------------|\n| macOS | `/Library/Application Support/ClaudeCode/managed-settings.json` |\n| Linux / WSL | `/etc/claude-code/managed-settings.json` |\n| Windows | `C:\\Program Files\\ClaudeCode\\managed-settings.json` |\n\nNote: the legacy Windows path `C:\\ProgramData\\ClaudeCode\\managed-settings.json` is **no longer supported** as of v2.1.75 — use the `Program Files` path. File-based managed settings also merge with drop-ins under `managed-settings.d/*.json` (merged alphabetically after the base file; later files override scalars, arrays concatenate and de-dupe, objects deep-merge). This lets independent teams (telemetry, security) ship separate policy fragments — e.g. `10-telemetry.json`, `20-security.json` — without editing one shared file.\n\n### What belongs here\n\nOnly true non-negotiables — the things security/compliance must guarantee regardless of what any engineer does:\n\n```json\n// managed-settings.json — deployed by IT, wins over everything\n{\n  \"forceLoginOrgUUID\": \"xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx\",\n  \"requiredMinimumVersion\": \"2.1.150\",\n  \"allowManagedMcpServersOnly\": true,\n  \"allowedMcpServers\": [{ \"serverName\": \"github\" }],\n  \"deniedMcpServers\": [{ \"serverName\": \"filesystem\" }],\n  \"permissions\": { \"deny\": [\"Bash(curl *)\", \"Read(/etc/**)\"] },\n  \"strictKnownMarketplaces\": [\n    { \"source\": \"github\", \"repo\": \"acme-corp/approved-plugins\" }\n  ]\n}\n```\n\nKey semantics: `requiredMinimumVersion` blocks startup below the floor (but `claude update` still runs). `deniedMcpServers` takes precedence over `allowedMcpServers`. `forceLoginOrgUUID` accepts a single UUID or an array (an empty array fails closed). `strictKnownMarketplaces` is the Tier-3 control over Tier 2: an empty array `[]` is a full lockdown (no marketplaces can be added); a list is an allowlist. It only *restricts* — it does not register anything, so pair it with `extraKnownMarketplaces` in the same managed file to *also* auto-register the approved one. Matching is **exact** and does not normalize URLs, so a trailing slash or `.git` suffix breaks the match — use a `hostPattern` entry (regex on the host) instead of a literal URL when your git host is reachable by multiple URL forms.\n\n### When to reach for Tier 3\n\nManaged settings are heavyweight (IT deployment, no per-repo flexibility). Use them only for: forced auth/org binding, version floors, MCP allow/deny-listing, hard `deny` rules, and marketplace allowlisting. Everything else stays in Tier 1/2. Don't put convenience here \"to be safe\" — you lose all per-repo flexibility and gain nothing."
  },
  {
   "heading": "Tier 4 — Personal config, kept out of everyone's way",
   "body": "The last tier is the pressure-release valve. Engineers have preferences and experiments that **should not** leak into shared config. These live in user-scope files that aren't committed and lose to every higher tier.\n\n```text\n~/.claude/settings.json     # personal Claude Code defaults (model, theme, env)\n~/.claude/skills/           # personal experimental skills\n~/.codex/config.toml        # personal Codex config (see portability section)\n```\n\nThe `.claude/settings.local.json` inside a repo is the *project-scoped* personal override — same idea, but scoped to one repo and gitignored. It's the **Local** scope, so it outranks the committed Project `settings.json` but still loses to managed. Use it for \"I want to allow `Bash(docker:*)` in *this* repo only\" without committing it to the team.\n\n### The anti-pattern this prevents\n\nThe whole reason Tier 4 exists as a named tier is to give people a *legitimate* place to put personal stuff so they stop polluting Tier 1. If an engineer's pet skill is genuinely useful, the move is to **promote it to Tier 2** (package as a plugin, PR to the marketplace) — not to quietly commit it to a project's `.claude/skills/`. \"Promote personal → marketplace\" is the healthy lifecycle; \"sneak personal into project config\" is the rot."
  },
  {
   "heading": "The shared library — the assembled output of the four tiers",
   "body": "Now the second concept. A **shared library** is not a fifth tier or a new file format — it is the **assembled output** of the four tiers: *one* set of skills, agents, and tools the whole team uses, versioned and distributed, working across both Claude Code and Codex ([Steering Claude Code](https://claude.com/blog/steering-claude-code-skills-hooks-rules-subagents-and-more); [Skills](https://code.claude.com/docs/en/skills)).\n\nConcretely, the team's shared library = **a plugin marketplace (Tier 2) + repo `.claude/` and `AGENTS.md` (Tier 1) + portability so it runs on Codex too.**\n\n### What's in it\n\n| Component | Authored as | Distributed via |\n|-----------|-------------|-----------------|\n| Cross-project skills | `skills/<name>/SKILL.md` | Marketplace plugin |\n| Cross-project agents | `agents/<name>.md` | Marketplace plugin |\n| Shared MCP servers | `.mcp.json` in plugin / `config.toml` | Marketplace plugin / Codex config |\n| Shared hooks | `hooks/hooks.json` | Marketplace plugin |\n| Per-repo rules | `CLAUDE.md` + `AGENTS.md` | Committed in each repo |\n\nThe defining property is **single-sourcing**: the `code-review` skill exists in *one* place (the marketplace repo). Claude Code installs it via `/plugin install`. Codex picks it up because the same `SKILL.md` format is portable. Nobody maintains two copies.\n\n### SKILL.md is the shared atom\n\nThe reason one library can span both tools is that the **`SKILL.md` format is nearly identical** across them: YAML frontmatter (`name` is implicit from the directory in Claude Code; `description` is required by both) plus a markdown body.\n\n```markdown\n---\nname: code-review\ndescription: Reviews a diff for bugs, security, and performance. Use when reviewing PRs or recent changes.\n---\n\nWhen reviewing code, check for: bugs/edge cases, security concerns,\nperformance issues, and test coverage. Be concise and actionable.\n```\n\nThe `description` is load-bearing — it's what both Claude and Codex use to decide *when* to auto-invoke the skill (progressive disclosure loads only names + descriptions first, then the body on demand). Write it as \"what it does + when to use it,\" not just a label."
  },
  {
   "heading": "Cross-tool portability — making the library work on Codex",
   "body": "\"Works across both Claude and Codex\" is a real engineering requirement, not a slogan. Here's the concrete mapping, verified against the [Codex skills](https://developers.openai.com/codex/skills), [config](https://developers.openai.com/codex/config-basic), [MCP](https://developers.openai.com/codex/mcp), and [AGENTS.md](https://developers.openai.com/codex/guides/agents-md) docs.\n\n### The portability table\n\n| Concept | Claude Code | Codex |\n|---------|-------------|-------|\n| Per-repo rules | `CLAUDE.md` | `AGENTS.md` (root→cwd, concatenated; closest wins) |\n| Project skills | `.claude/skills/<name>/SKILL.md` | `.agents/skills/<name>/SKILL.md` |\n| User skills | `~/.claude/skills/` | `~/.agents/skills/` |\n| Admin skills | (managed settings) | `/etc/codex/skills/` |\n| MCP servers | `.mcp.json` / settings | `[mcp_servers.<name>]` in `config.toml` |\n| Settings file | `settings.json` (JSON) | `config.toml` (TOML) |\n\n### Two practical portability moves\n\n**1. Author rules once, mirror to both filenames.** Codex reads `AGENTS.md` and walks the tree from repo root down to cwd, **concatenating** each file it finds (joined with blank lines); because the closest file appears last in the combined prompt, **the closest file wins** on conflicts, and an explicit chat prompt overrides everything. (Codex also checks `AGENTS.override.md` ahead of `AGENTS.md` in the same directory.) Claude reads `CLAUDE.md`. Keep one canonical source and generate or symlink the other so they never drift — this repo's own convention is `CLAUDE.md` canonical, `AGENTS.md` the Codex-facing mirror.\n\n**2. Codex skills live under `.agents/skills`, not `.codex/skills`.** Per the [Codex skills doc](https://developers.openai.com/codex/skills), Codex scans `.agents/skills` at the cwd, walking up through parent dirs to the repo root, plus `$HOME/.agents/skills` and `/etc/codex/skills`. The `SKILL.md` body is portable; only the directory and the per-tool config files differ.\n\n### Codex MCP config\n\nMCP servers are the most portable component — same servers, different config syntax. For a stdio server:\n\n```toml\n# ~/.codex/config.toml  (or .codex/config.toml in a trusted project)\n[mcp_servers.github]\ncommand = \"npx\"\nargs = [\"-y\", \"@modelcontextprotocol/server-github\"]\n\n[mcp_servers.github.env]\nGITHUB_TOKEN = \"...\"\n```\n\nOr non-interactively: `codex mcp add github --env GITHUB_TOKEN=... -- npx -y @modelcontextprotocol/server-github`. (Codex also supports `env_vars = [...]` to forward named host variables instead of inlining secrets, and `url`-based HTTP servers in place of `command`/`args`.) Disable a skill without deleting it via `[[skills.config]]` with its `path` and `enabled = false`, then restart Codex:\n\n```toml\n[[skills.config]]\npath = \"/path/to/skill/SKILL.md\"\nenabled = false\n```"
  },
  {
   "heading": "Worked example — a real team setup",
   "body": "Putting all four tiers together for a 6-person team that ships a Python service and a TS frontend, drives both Claude Code and Codex.\n\n### The marketplace repo (Tier 2 — the shared library's home)\n\n```text\nacme-corp/claude-plugins/          # one repo, the source of truth\n├── .claude-plugin/marketplace.json\n└── plugins/\n    ├── quality-tools/             # code-review skill + security agent + lint hook\n    │   ├── .claude-plugin/plugin.json\n    │   ├── skills/code-review/SKILL.md\n    │   ├── agents/security-reviewer.md\n    │   └── hooks/hooks.json\n    └── data-tools/                # shared MCP servers (db, internal API)\n        ├── .claude-plugin/plugin.json\n        └── .mcp.json\n```\n\n### Each service repo (Tier 1)\n\n```text\nacme-corp/payments-service/\n├── CLAUDE.md            # ← canonical rules\n├── AGENTS.md            # ← mirror for Codex\n├── .agents/skills/      # ← Codex picks up project skills here\n└── .claude/\n    └── settings.json    # declares the marketplace + enables plugins + repo permissions\n```\n\n```json\n// payments-service/.claude/settings.json\n{\n  \"extraKnownMarketplaces\": {\n    \"acme-tools\": { \"source\": { \"source\": \"github\", \"repo\": \"acme-corp/claude-plugins\" } }\n  },\n  \"enabledPlugins\": { \"quality-tools@acme-tools\": true, \"data-tools@acme-tools\": true },\n  \"permissions\": { \"allow\": [\"Bash(pytest:*)\"], \"deny\": [\"Read(./.env)\"] }\n}\n```\n\n### IT deployment (Tier 3)\n\n`/etc/claude-code/managed-settings.json` sets `requiredMinimumVersion`, `allowManagedMcpServersOnly: true`, and `strictKnownMarketplaces` allowlisting only `acme-corp/claude-plugins` — paired with `extraKnownMarketplaces` so the approved marketplace also auto-registers.\n\n### Each engineer (Tier 4)\n\n`~/.claude/settings.json` picks their model and theme; `~/.codex/config.toml` carries their personal Codex prefs. Nothing team-relevant lives here.\n\n**Result:** clone any repo → trust folder → prompted to install the exact shared library → identical skills/agents/MCP whether you open Claude Code or Codex. Bump `quality-tools` to `1.3.0`, everyone gets it on next `/plugin update`."
  },
  {
   "heading": "When to use what — the decision table",
   "body": "The full lookup for \"I have a thing — which tier?\"\n\n| You have... | Tier | Where it goes |\n|-------------|------|---------------|\n| Rules about *this repo* | 1 | `CLAUDE.md` + `AGENTS.md` |\n| A repo-only helper skill | 1 | `.claude/skills/` (+ `.agents/skills/` for Codex) |\n| The team's permission allowlist for a repo | 1 | `.claude/settings.json` (committed, Project scope) |\n| A personal allow for one repo | 4 | `.claude/settings.local.json` (gitignored, Local scope) |\n| A skill/agent used in 2+ repos | 2 | Marketplace plugin |\n| A shared MCP server (internal DB, API) | 2 | Plugin `.mcp.json` / Codex `config.toml` |\n| \"You may only add *our* marketplace\" | 3 | `strictKnownMarketplaces` (managed) |\n| \"Everyone must be on ≥ v2.1.150\" | 3 | `requiredMinimumVersion` (managed) |\n| \"Block `curl`, force org login\" | 3 | `permissions.deny` / `forceLoginOrgUUID` (managed) |\n| Your model/theme preference | 4 | `~/.claude/settings.json` |\n| An experimental skill you're iterating on | 4 → 2 | `~/.claude/skills/`, then promote to marketplace |\n\n### Heuristics\n\n- **Reuse count ≥ 2 repos → promote from Tier 1 to Tier 2.** Copying is the smell that says \"make it a plugin.\"\n- **Tier 3 is for guarantees, not conveniences.** If you can't articulate it as a security/compliance must, it doesn't belong in managed settings.\n- **Tier 4 is a staging area, not a graveyard.** Personal-useful things should graduate to Tier 2."
  },
  {
   "heading": "Common pitfalls and gotchas",
   "body": "The failure modes that bite teams, with the fix:\n\n### Plugin structure\n- **Putting `skills/`, `agents/`, `hooks/` *inside* `.claude-plugin/`.** Only `plugin.json` goes in `.claude-plugin/`; all component dirs sit at the plugin root. This is the #1 \"my plugin doesn't load\" cause.\n- **Referencing `../shared-utils` from a plugin.** Plugins are copied to `~/.claude/plugins/cache`; outside files aren't copied (the validator flags `..` as path traversal). Use `${CLAUDE_PLUGIN_ROOT}` for in-plugin paths, or symlinks for shared assets.\n\n### Versioning\n- **Setting `version` in both `plugin.json` and the marketplace entry.** The `plugin.json` value wins *silently*, masking the marketplace one. Pick one place.\n- **Forgetting to bump `version`.** If `plugin.json` says `\"1.0.0\"` and you push new commits without changing it, existing users get **nothing** — Claude Code sees the same version and keeps the cache. Either bump on every release, or omit `version` on a git-based source so each commit SHA counts as a new version.\n\n### Settings & precedence\n- **Expecting a project setting to override managed.** It can't — managed (Tier 3) is the top of precedence.\n- **Forgetting Local beats Project.** A stray `.claude/settings.local.json` silently overrides the committed team `settings.json` (it's the higher-precedence Local scope). When a teammate's behavior diverges, check their local file first.\n- **Confusing override vs merge.** Most settings *override* by precedence; **permission rules merge** across tiers. A project can add `deny` rules on top of org policy but can't remove an org `deny`.\n- **`strictKnownMarketplaces` exact-matching.** A trailing slash or `.git` suffix makes the URL not match (no normalization). Use `hostPattern` (regex on host) for robustness, and remember it only restricts — pair with `extraKnownMarketplaces` to actually register the approved marketplace.\n\n### Portability\n- **Assuming Codex reads `.claude/skills`.** It reads `.agents/skills` (repo, walking up to root) and `~/.agents/skills`, plus `/etc/codex/skills`. The `SKILL.md` body is portable; the directory and config file (`config.toml` vs `settings.json`) are not. Mirror, don't assume.\n- **Letting `CLAUDE.md` and `AGENTS.md` drift.** Make one canonical and generate/symlink the other, or the two tools will behave differently. Note Codex caps the concatenated instruction chain at `project_doc_max_bytes` (32 KiB default) — keep root rules lean.\n\n### Process\n- **No CI validation.** Run `claude plugin validate .` in the marketplace repo's CI — pointed at the marketplace dir it checks `marketplace.json` (schema, duplicate plugin names, source path traversal, version mismatches); pointed at a plugin dir (`claude plugin validate ./plugins/my-plugin`) it also checks each skill/agent/hook's frontmatter and `hooks.json` syntax. Run both before changes reach the team."
  },
  {
   "heading": "What you commit to — the team charter",
   "body": "This stage is a *decision* stage, so end with the commitments your team writes down. A one-page charter that answers, concretely:\n\n1. **Where is our shared library?** → one marketplace repo (e.g. `acme-corp/claude-plugins`). One source of truth.\n2. **How does a repo opt in?** → `extraKnownMarketplaces` + `enabledPlugins` committed in `.claude/settings.json`, so cloning a repo provisions the agent.\n3. **What's mandatory vs optional?** → mandatory guardrails in managed settings (Tier 3); everything else is opt-in plugins (Tier 2). If you lock down Tier 2, pair `strictKnownMarketplaces` with `extraKnownMarketplaces`.\n4. **How do we stay portable?** → canonical `CLAUDE.md` mirrored to `AGENTS.md`; portable `SKILL.md`; project skills in both `.claude/skills` and `.agents/skills`; MCP servers expressed in both `.mcp.json` and `config.toml`.\n5. **How does a personal skill become a team skill?** → PR it into the marketplace, bump the version. Tier 4 → Tier 2 is the only legitimate promotion path.\n6. **How do we ship changes?** → bump `version` (or omit it and rely on commit SHA), push, `claude plugin validate .` in CI; engineers pick it up on `/plugin update` or background auto-update.\n\nThe four-tier model isn't bureaucracy — it's the smallest set of decisions that makes a shared setup *reproducible, reviewable, and portable*. Decide each thing's tier once, write the charter, and the mechanics in this stage are all you need to execute it.\n\n### Sources\n- [Steering Claude Code: skills, hooks, rules, subagents](https://claude.com/blog/steering-claude-code-skills-hooks-rules-subagents-and-more)\n- [Claude Code Settings](https://code.claude.com/docs/en/settings)\n- [Create plugins](https://code.claude.com/docs/en/plugins) · [Plugin marketplaces](https://code.claude.com/docs/en/plugin-marketplaces) · [Skills](https://code.claude.com/docs/en/skills)\n- [Codex skills](https://developers.openai.com/codex/skills) · [Codex config](https://developers.openai.com/codex/config-basic) · [Codex MCP](https://developers.openai.com/codex/mcp) · [Codex AGENTS.md](https://developers.openai.com/codex/guides/agents-md) · [AGENTS.md spec](https://agents.md)"
  }
 ]
}