# Research-loop audit — 2026-06-23

Run `wf_95f824fd-afd` on the team-infra domain, audit mode (baseline 25 concepts). **81 agents, 3 rounds, did NOT converge** (hit maxRounds=3 before drying out) — the loop kept finding fresh gaps, i.e. the domain is materially larger than the 25-concept curriculum captures. This is the methodology working: it tells you what you don't have.

- **79 distinct new concept candidates** (from 92 raw, deduped)
- **151 gaps** by class: coverage 38, closure 30, perishability 25, completeness 25, contestation 19, correctness 14
- **5 corrections** (all refine the audit's OWN new candidates; none hit the live 28-claim ledger)

## Reconciliation status
- **Folded this pass:** the permissions model — `permission-rule` (allow/ask/deny, deny-first, managed-ceiling/user-floor) and `permission-mode` (default/plan/acceptEdits/auto/bypass) — into stage-5, plus the `config-mirror` @import refinement. Backing claims added to the ledger.
- **Captured as backlog (NOT yet folded):** the rest below. Surfaced, not silently dropped. Next highest-value clusters: CLAUDE.md mechanics (@import / .claude/rules/ / subtree lazy-load / /memory), slash-commands-merged-into-skills (2026), supply-chain security (lethal trifecta, OWASP Agentic Skills Top 10), and Codex specifics (config.toml, trusted project, managed config).

## All distinct new concept candidates
- **$ARGUMENTS substitution** — Skill/command placeholders: $ARGUMENTS (full string), $ARGUMENTS[N] or $N (0-based positional), $name (declared in arguments frontmatter), plus ${CLAUDE_SKILL_DIR} and ${CLAUDE_SESSION_ID}.
- **.claude/rules/** — Directory of modular topic markdown files (testing, style) loaded as project memory at launch like CLAUDE.md, optionally scoped via a paths: glob frontmatter so they load only when matching files are 
- **/memory command** — Lists/opens every CLAUDE.md, CLAUDE.local.md, and rules file loaded in the current session, toggles auto memory, and opens files for editing — a diagnostic to verify what actually loaded.
- **@path import (CLAUDE.md ↔ AGENTS.md)** — CLAUDE.md syntax @path/to/file that expands and loads files into context at launch; recursive up to a maximum depth of four hops, skips backtick-wrapped paths. A one-line @AGENTS.md import is the docu
- **@path import syntax** — CLAUDE.md syntax to inline another file (@path/to/file) at session start; relative paths resolve from the importing file, recursive up to max depth (docs cite four/five hops), turning memory into a co
- **acceptEdits mode** — Auto-approves file edits so you review after the fact via editor/git diff instead of inline per-edit prompts.
- **Agent Skills open standard** — SKILL.md spec (agentskills.io, Apache-2.0 code / CC-BY-4.0 docs; folder = YAML frontmatter name/description + Markdown body + scripts/references/assets dirs + progressive disclosure), announced by Ant
- **agent team** — Experimental multi-session orchestration (CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1; shipped with Opus 4.6 on 2026-02-05): one lead session plus teammates, each with its own context window, sharing a tas
- **agent team / teammate** — Experimental multi-agent feature (Opus 4.6, env-flag-gated) where peer Claude instances each hold their own context window and coordinate via a shared file-locked task list and mailbox, vs subagents' 
- **Agent tool** — The tool that spawns subagents and teammates; teammates can reference a subagent definition's tools/model, but the definition's skills and mcpServers frontmatter are NOT applied to teammates (they loa
- **Agent(model:opus) permission syntax** — Tool(param:value) permission form, e.g. constraining the Agent tool's model; teammates inherit the lead's permission mode at spawn and cannot be set per-teammate at spawn time.
- **AGENTS.md interop via import** — Claude Code reads CLAUDE.md, not AGENTS.md; the documented single-source fix is a CLAUDE.md that does @AGENTS.md (or a symlink), so both Claude Code and Codex read one file.
- **AGENTS.md↔CLAUDE.md interop** — Claude Code reads ONLY CLAUDE.md, NOT AGENTS.md (not even as a fallback; tracked in issues #6235/#34235); single-source-of-truth via a first-line @AGENTS.md import (works on Windows, no admin) or `ln 
- **allowManagedPermissionRulesOnly** — Managed-only setting that prevents user/project settings from defining any allow/ask/deny rules, so only managed rules apply (with siblings allowManagedHooksOnly, allowManagedMcpServersOnly).
- **auto memory** — Per-repo notes (v2.1.59+) stored under ~/.claude/projects/<project>/memory/MEMORY.md, of which only the first 200 lines / 25KB load each session; machine-local, not shared across machines.
- **Auto memory (MEMORY.md)** — Claude-written per-repo notes at ~/.claude/projects/<project>/memory/MEMORY.md (v2.1.59+, on by default); machine-local and NOT synced across machines or the team; only the first 200 lines or 25KB loa
- **auto mode** — Executes without routine prompts but a separate classifier model reviews each action, blocking escalation beyond your request, unrecognized infrastructure, or actions driven by hostile content Claude 
- **auto mode (classifier, research preview)** — A research-preview mode that auto-approves actions while a separate server-configured classifier model (independent of the user's /model choice) reviews each tool call, auto-approving safe ones and ro
- **bypassPermissions mode** — Skips prompts and safety checks including writes to protected paths (.git, .claude, .vscode, .idea, etc., as of v2.1.126); the only forced prompts are explicit ask rules and a root/home removal circui
- **claude plugin prune** — CLI command (v2.1.121+) / --prune flag that removes auto-installed plugin dependencies no other plugin still requires.
- **CLAUDE.md load order** — Files are discovered by walking up the directory tree and concatenated root-down (broadest scope first, working-directory file last); subdirectory CLAUDE.md / CLAUDE.local.md load lazily when Claude r
- **CLI+Skill-first selection heuristic** — Build capabilities as CLI commands wrapped in a skill first; reach for MCP only when useful state lives inside someone else's running system — the dominant 2026 cost-driven design rule, since MCP tool
- **CLI-first / MCP-last heuristic** — Selection rule favoring CLI then progressive-disclosure skills over always-on MCP, because MCP tool schemas load tens of thousands of tokens upfront regardless of use, taxing the context window before
- **Codex config.toml** — Codex's settings analogue in TOML: ~/.codex/config.toml (user) plus project-scoped .codex/config.toml (trusted projects only, root-down, closest wins), holding model/provider, [mcp_servers.<name>] tab
- **Codex managed configuration** — Enterprise admin-pushed Codex config (managed layer) — Codex's analogue to Claude Code managed-settings for org policy.
- **Codex trusted project** — Untrusted Codex projects skip all project-scoped .codex/ layers (project config, hooks, rules) — a workspace-trust gate analogous to Claude Code's.
- **Compliance API** — Enterprise-only API giving security teams programmatic real-time access to Claude activity logs, chat histories, and file content (audit logs for SIEM) — a governance gate, not a headcount gate.
- **cross-tool skill portability** — A single SKILL.md folder works identically in Claude Code, Codex CLI (~/.codex/skills, --enable skills), Gemini CLI, GitHub Copilot, and Cursor — portability backed by the open spec.
- **Deny floor / array merge across scopes** — allow/ask/deny arrays deep-merge (concatenate+dedupe) across managed→CLI→local→project→user scopes rather than replace; if a tool is denied at any level no other level can allow it, making any-scope d
- **deny-first evaluation order** — Permission rules evaluate deny → ask → allow with first-match-wins regardless of specificity; a broad deny cannot carry allowlist exceptions, and a matching ask prompts even if a narrower allow also m
- **disableBypassPermissionsMode / disableAutoMode** — Settings (value 'disable') that gate the bypass/auto modes; most useful in managed settings where they cannot be overridden — the real lockout mechanism, not a protected-path list.
- **disableSkillShellExecution** — Setting (most useful in managed scope, un-overridable there) that disables the !`command` dynamic-context shell execution in user/project/plugin skills and commands, replacing each with '[shell comman
- **dontAsk mode** — Auto-denies every tool call that would otherwise prompt; only permissions.allow matches and read-only Bash execute — fully non-interactive for locked-down CI. Never appears in the Shift+Tab cycle; set
- **enabledPlugins persistence** — Once a plugin's state is written to enabledPlugins at any settings scope it persists across plugin updates and reinstalls and takes precedence over the plugin's defaultEnabled; --scope project writes 
- **Enterprise governance surface** — What Enterprise (vs Team) adds: SSO, SCIM, role-based permissions, audit logs, and the Compliance API — the actual Team-vs-Enterprise gate is identity/compliance/administration, not Claude Code access
- **five extension mechanisms** — Claude Code's customization layers: CLAUDE.md (always-on context), skills (on-demand knowledge/workflows), MCP (external connections), subagents (isolation), hooks (deterministic automation) — distrib
- **hooks: request vs guarantee** — Framing that a prompt is a request (~70-90% compliance) while a hook is a deterministic guarantee (~100%) firing on lifecycle/tool-call events (PreToolUse/PostToolUse/SessionStart/Stop).
- **lethal trifecta** — Willison's supply-chain threat frame for agent skills/plugins: combine (1) access to private data/secrets, (2) exposure to untrusted content, and (3) ability to exfiltrate externally in one agent — th
- **Managed ceiling vs user floor** — Managed settings are the policy ceiling and cannot be loosened by lower scopes; SDK embedders with parentSettingsBehavior=merge can tighten but not loosen.
- **Managed marketplace controls** — blockedMarketplaces (a blocklist checked before download so blocked sources never touch the filesystem), strictKnownMarketplaces, and pluginTrustMessage — managed-settings keys governing which plugin 
- **managed marketplace restriction** — Admins can constrain plugin sources via strictKnownMarketplaces and blockedMarketplaces in managed settings to forbid third-party/skills-dir marketplaces (including the @skills-dir source).
- **managed MCP** — Enterprise admin-controlled MCP server configuration pushed to seats — bundled with admin spend caps and seat allocation on Team/Enterprise.
- **managed settings** — Org policy floor at OS-specific paths (/etc/claude-code/managed-settings.json, /Library/Application Support/ClaudeCode/, C:\Program Files\ClaudeCode\), deployable via MDM/Intune/GPO; un-bypassable, ov
- **managed-settings.d/** — A directory whose .json files merge alphabetically (later overrides earlier); within the managed tier precedence is server > MDM/OS policy > file-based > HKCU registry (Windows). Some keys (availableM
- **Model-invoked skill / progressive disclosure** — Skills are loaded autonomously by the model from a short description (~30-50 tokens, listing truncated at 1,536 chars) only when relevant; full content loads only on invocation — the premise the contr
- **OWASP Agentic Skills Top 10** — Emerging standardized threat taxonomy for agent skills (AST01 Malicious Skills, etc.) — a supply-chain governance reference for vetting third-party skills/plugins.
- **OWASP Agentic Skills Top 10 (AST)** — OWASP threat taxonomy (published 2026-04-27, AST01 Malicious Skills, etc.) for the agentic skills/behavior layer between the LLM and the MCP tool layer, giving shared vocabulary and mitigations for th
- **permission mode** — A runtime-autonomy dial orthogonal to rules (how aggressively to act, not what's allowed): default, acceptEdits, plan (read-only), bypassPermissions, dontAsk, and auto (a model classifier approves/den
- **permission rule (allow/ask/deny)** — The core access-control primitive: rule objects in settings.json permissions{} with allow/ask/deny arrays of tool-name glob patterns (e.g. Bash(git push:*), Read(./.env.*), mcp__*) that govern each to
- **permission-rule merge (floor)** — Unlike most settings which override, allow/ask/deny arrays are concatenated and deduplicated across scopes/layers, so a higher-scope (user/managed) deny is a floor no lower scope can relax.
- **plan mode** — Read-only mode: Claude researches and proposes changes (reads/explores files) but makes no edits; file-edit and shell-write tools route to the permission callback regardless of allow rules, so writes 
- **plugin** — A self-contained git-distributable directory bundling skills/commands, agents (subagents), hooks, and MCP servers (plus LSP servers/monitors) into one installable, version-controlled unit for team dis
- **premium seat** — Team/Enterprise add-on (~$100/seat/mo annual, $125 monthly) granting ~5x a standard $20 seat's usage with pooled usage across the apps and Code terminal/IDE; no longer gates Claude Code access, which 
- **PreToolUse hook bypass (enforcement gap)** — Counter-position (issue #45427): subagents dispatched via the Agent tool execute tool calls without triggering the parent's PreToolUse hooks; hooks also fail silently, can be model-rewritten, and fail
- **PreToolUse hook enforcement gap** — PreToolUse hooks are the model-facing enforcement layer but are a request not a guarantee: only exit code 2 blocks (an uncaught exception exits 1 and fails silently), the exit code is ignored for Agen
- **Production Class Ladder** — A four-tier framework (Personal → Team Beta → Supported Internal → Customer-Facing) for classifying AI-built software's required rigor before shipping — the nearest external 'four-tier' model to the c
- **project_doc_fallback_filenames** — Codex config key listing alternate instruction filenames; Codex checks AGENTS.override.md, then AGENTS.md, then the fallbacks in each directory — the mechanism letting Codex read a CLAUDE.md-style fil
- **prompt injection (indirect)** — Attacker-controlled text or images reaching the model and overriding its instructions; the dominant technique in confirmed malicious skills.
- **protected paths** — Paths like .git, .claude, and shell rc files still prompt even under bypassPermissions; writes there are never silently auto-approved.
- **Sandbox (OS-level enforcement)** — OS-level filesystem/network isolation for the Bash tool and its children; complementary to permissions and the defense that holds even if a prompt injection bypasses Claude's decision-making.
- **settings precedence** — Highest→lowest: managed settings → command-line args → .claude/settings.local.json → .claude/settings.json → ~/.claude/settings.json; managed scope overrides even CLI flags and cannot be bypassed by a
- **skill / SKILL.md** — A SKILL.md file (YAML frontmatter + markdown body), optionally bundled in a directory with scripts/templates, that extends Claude; the description is always in context but the body loads only when inv
- **Skill scope precedence** — For skill name clashes across scopes, precedence is enterprise > personal > project > bundled; a same-named skill at any level overrides a bundled skill.
- **skill scope tiers** — Where a skill lives sets its audience: Enterprise (managed) > Personal (~/.claude/skills) > Project (.claude/skills) > Plugin (namespaced plugin:skill); enterprise overrides personal overrides project
- **SKILL.md frontmatter** — YAML metadata atop a skill file: description (recommended; drives auto-triggering, capped 1,536 chars combined with when_to_use), allowed-tools, disallowed-tools, model, disable-model-invocation, user
- **Skill/plugin supply-chain trust (ToxicSkills)** — The trust decision for installing third-party skills/plugins/marketplaces; Snyk's ToxicSkills audit of ClawHub makes it a first-class governance choice — private/vendored skills and managed marketplac
- **slash command (merged into skills)** — A /name invocation defined by a markdown file (.claude/commands/*.md with $ARGUMENTS); as of 2026 custom commands merged into skills, so .claude/commands/deploy.md and .claude/skills/deploy/SKILL.md b
- **Slash command merged into skills** — .claude/commands/deploy.md and .claude/skills/deploy/SKILL.md both create /deploy with $ARGUMENTS substitution; legacy commands keep working but skills are recommended, and on a name clash the skill t
- **slash-command vs skill** — Slash command = reusable prompt/checklist (prompt repetition; 'how Claude thinks'); skill = loadable instructions that can use tools, take args, spawn subagents (real workflow logic; 'what Claude can 
- **spec-driven development (SDD)** — Writing a versioned, durable spec with goals/constraints/acceptance criteria and driving agents against it (plan mode, Spec Kit, Kiro, OpenSpec) — Karpathy's 'agentic engineering' replacing casual vib
- **strictPluginOnlyCustomization** — Managed-only setting that blocks skills/agents/hooks/MCP from user and project sources so they come only from plugins or managed settings; true locks all four, an array locks named ones — the lever fo
- **sub-agent** — A named, isolated helper Claude instance with its own context window, tools, and permission mode that does focused work and reports results back only to its caller; stateless (returns output then clos
- **Subagent model field / CLAUDE_CODE_SUBAGENT_MODEL** — Per-subagent model assignment (sonnet/opus/haiku/inherit) enabling cost-tier routing; setting a lighter subagent model is the single biggest subagent cost lever.
- **subtree CLAUDE.md (lazy load)** — CLAUDE.md files in subdirectories below cwd are NOT loaded at launch; they load lazily only when Claude reads files in that subdirectory, whereas the upward directory-tree crawl loads eagerly (concate
- **supply-chain trust (marketplace skills/plugins)** — Third-party skill/plugin marketplaces are an untrusted software supply chain: installed skills can carry malware, prompt injection, or exposed secrets, so teams must vendor/review or restrict to priva
- **teammate vs subagent** — A subagent reports results only back to its caller; a teammate is a full independent session that messages other teammates and self-claims shared tasks. Teammates ignore a subagent definition's skills
- **tool-name glob pattern** — Patterns match the full tool name: '*' = every tool, 'mcp__*' = all MCP tools, 'mcp__github__get_*' = a server's get_ tools, 'Bash(aws *)' = command-scoped Bash; MCP globs honored only after a literal
- **ToolName(specifier) / ToolName(param:value) syntax** — Permission match form: a bare tool name (e.g. Bash) as a deny removes the tool from context entirely while a scoped Bash(rm *) leaves it available; deny/ask rules can also match a top-level scalar inp
- **Two-layer enterprise stack** — The 2026 pattern: Copilot broad as autocomplete baseline + Claude Code adopted bottom-up by senior engineers for agentic work (+ Codex for parallel/cloud isolation) — a governed multi-tool portfolio, 

## Corrections (refine the new candidates)
- ~~In plan mode, file-edit and shell-write tools are routed to the permission callback regardless of allow rules, so write operations cannot be~~ → The end state is right but the stated mechanism is not how the official docs describe plan mode. In plan mode Claude is restricted to reads and read-only shell commands and "does not edit your source files"; it does not 
- ~~Under bypassPermissions all prompts and safety checks are disabled except writes to protected paths (.git, .claude, shell rc files), which s~~ → Under bypassPermissions, permission prompts and safety checks are disabled and tool calls execute immediately — and as of v2.1.126 this INCLUDES writes to protected paths (.git, .claude, shell rc files), which are auto-a
- ~~CLAUDE.md @path imports are recursive (docs cite a maximum depth of four to five hops; relative paths resolve from the importing file); impo~~ → CLAUDE.md @path imports are recursive — imported files can recursively import other files, with a maximum depth of four hops (the official docs state exactly "four," not a "four to five" range). Relative paths resolve re
- ~~Anthropic released Agent Skills as an open standard on 2025-12-18 (spec + SDK at agentskills.io, Apache-2.0 code / CC-BY-4.0 docs); by 2026 ~~ → Anthropic released Agent Skills as an open standard on 2025-12-18 (spec + reference/validation library at agentskills.io and github.com/agentskills/agentskills, Apache-2.0 for code / CC-BY-4.0 for docs). By 2026, Codex C
- ~~There are six permission modes: default (reads only), acceptEdits (reads + file edits + common filesystem commands in working dir), plan (re~~ → There are six permission modes: default (reads only), acceptEdits (reads + file edits + common filesystem commands like mkdir/touch/mv/cp/sed for paths in the working directory or additionalDirectories), plan (reads only
