/**
 * Author comprehensive, technically-deep lesson content per stage, grounded in the
 * stage's concepts + verified claims (claims.jsonl) and official docs. Pipeline:
 * author → verify-and-deepen, no barrier. Returns { [stageId]: Section[] } for
 * integration into src/content/lesson-content.json.
 *
 *   Workflow({ scriptPath: "scripts/author-lessons.workflow.js", args: <stages array> })
 *   stages = [{ id, title, summary, concepts:[{term,def}], claims:[{statement,kind,sources,positions}] }]
 */
export const meta = {
  name: "author-lessons",
  description: "Author deep, grounded lesson content per stage (author → verify/deepen)",
  phases: [{ title: "Author" }, { title: "Verify" }],
};

const STAGES = [{"id":"stage-0","title":"Start Here: One Tool, Many Front-ends","summary":"You're about to set up Claude for your team and the names already blur together — Desktop, Code tab, CLI, Cowork, Codex. Before any of that: most of the confusion dissolves once you see that it's mostly one tool wearing different faces.","concepts":[{"term":"Claude Code","def":"Anthropic's agentic coding tool — one underlying engine you drive from the terminal or a GUI."},{"term":"shared config","def":"The configuration (instructions, tools, hooks, skills, settings) that every Claude Code front-end reads the same way — what makes them one tool."}],"claims":[{"statement":"Claude Code's terminal CLI and the Desktop app 'Code' tab are the same engine reading the same config (CLAUDE.md, MCP, hooks, skills, settings).","kind":"fact","sources":["https://code.claude.com/docs/en/desktop-quickstart"]},{"statement":"Claude Code vs Codex is not either/or; the productive pattern is to run both.","kind":"contested","sources":["https://dev.to/_46ea277e677b888e0cd13/claude-code-vs-codex-2026-what-500-reddit-developers-really-think-31pb"],"positions":[{"name":"claude-quality","claim":"Claude Code higher quality (~67% blind win), leads SWE-bench, full MCP; but tighter usage limits.","sourceUrls":["https://dev.to/_46ea277e677b888e0cd13/claude-code-vs-codex-2026-what-500-reddit-developers-really-think-31pb"]},{"name":"codex-efficiency","claim":"Codex cheaper (~4x fewer tokens), leads Terminal-Bench; 'Codex for keystroke, Claude Code for commits'.","sourceUrls":["https://dev.to/_46ea277e677b888e0cd13/claude-code-vs-codex-2026-what-500-reddit-developers-really-think-31pb"]}]}]},{"id":"stage-1","title":"The Surfaces","summary":"Your people will ask 'which one do I open?' — and the honest answer isn't one tool, it's a menu. Get the menu wrong and a dev fights a GUI to script something, or an ops teammate never discovers the surface built for them.","concepts":[{"term":"the CLI","def":"Claude Code in the terminal: scriptable and automatable."},{"term":"the Desktop Code tab","def":"Claude Code with a graphical interface: visual diff review, panes, and parallel sessions each in its own git worktree."},{"term":"headless mode","def":"Running non-interactively (one prompt in, output out) for scripts and CI — available only in the terminal."},{"term":"Cowork","def":"The same agentic capability applied to non-developer knowledge work: give it a goal and it works across your files and apps, showing its plan before acting."},{"term":"Codex","def":"A second coding agent (from OpenAI) some of your devs use alongside Claude Code."}],"claims":[{"statement":"Claude Code's terminal CLI and the Desktop app 'Code' tab are the same engine reading the same config (CLAUDE.md, MCP, hooks, skills, settings).","kind":"fact","sources":["https://code.claude.com/docs/en/desktop-quickstart"]},{"statement":"The Desktop 'Code' tab is distinct from the legacy Claude desktop chat app, whose MCP config (claude_desktop_config.json) is separate and does not appear in the Code tab.","kind":"fact","sources":["https://code.claude.com/docs/en/desktop-quickstart","https://github.com/anthropics/claude-code/issues/33732"]},{"statement":"Headless / non-interactive mode (-p/--print, scripting, CI) is a terminal-CLI capability; the Desktop GUI is interactive only.","kind":"fact","sources":["https://code.claude.com/docs/en/headless"]},{"statement":"Claude Cowork is agentic non-coding knowledge work that shows its plan before acting; available on paid plans including Team and Enterprise.","kind":"fact","sources":["https://claude.com/product/cowork","https://support.claude.com/en/articles/13345190"]},{"statement":"Computer use and phone Dispatch are a Pro/Max research preview (macOS and Windows); the docs do NOT document Team/Enterprise exclusion, so 'not on Team' is an inference, restated as 'not part of the documented Team plan'.","kind":"perishable","sources":["https://claude.com/blog/dispatch-and-computer-use","https://support.claude.com/en/articles/12138966"]},{"statement":"OpenAI Codex is a separate coding agent with its own config tree (~/.codex); nothing syncs automatically between Codex and Claude Code.","kind":"fact","sources":["https://developers.openai.com/codex/config-reference","https://code.claude.com/docs/en/claude-directory"]},{"statement":"Codex reads AGENTS.md for project instructions; Claude Code natively reads CLAUDE.md, NOT AGENTS.md, so teams bridge the two (symlink or generated mirror).","kind":"fact","sources":["https://developers.openai.com/codex/guides/agents-md","https://code.claude.com/docs/en/memory"]},{"statement":"Claude Code vs Codex is not either/or; the productive pattern is to run both.","kind":"contested","sources":["https://dev.to/_46ea277e677b888e0cd13/claude-code-vs-codex-2026-what-500-reddit-developers-really-think-31pb"],"positions":[{"name":"claude-quality","claim":"Claude Code higher quality (~67% blind win), leads SWE-bench, full MCP; but tighter usage limits.","sourceUrls":["https://dev.to/_46ea277e677b888e0cd13/claude-code-vs-codex-2026-what-500-reddit-developers-really-think-31pb"]},{"name":"codex-efficiency","claim":"Codex cheaper (~4x fewer tokens), leads Terminal-Bench; 'Codex for keystroke, Claude Code for commits'.","sourceUrls":["https://dev.to/_46ea277e677b888e0cd13/claude-code-vs-codex-2026-what-500-reddit-developers-really-think-31pb"]}]}]},{"id":"stage-2","title":"Config as Code","summary":"Two teammates get different results from the same prompt, and nobody knows why. The fix isn't a better prompt — it's making the setup itself shared, versioned, and reviewed like code.","concepts":[{"term":"CLAUDE.md","def":"Always-loaded project instructions: conventions, how-to-build, and gotchas Claude reads every session."},{"term":"AGENTS.md","def":"The Codex-facing instruction file — the mirror of CLAUDE.md so the same guidance reaches Codex."},{"term":"the config mirror","def":"Keep ONE canonical config and project it to the other engine instead of hand-maintaining two. Claude Code reads only CLAUDE.md (never AGENTS.md), so the portable bridge is a first-line `@AGENTS.md` import inside CLAUDE.md (works cross-platform, no admin) — preferred over an `ln -s AGENTS.md CLAUDE.md` symlink (Windows needs admin/Developer Mode). A drift check fails CI if the two diverge, so you can switch between Claude Code and Codex (e.g. in VS Code) without re-teaching each one."},{"term":"the .claude directory","def":"The .claude/ folder where skills, sub-agents, hooks, and settings live as versioned code in the repo."},{"term":"settings.json","def":"Permissions, hooks, and environment config — committed for the team, with personal overrides kept local."}],"claims":[{"statement":"A committed .claude/ directory travels with the repo: cloning it (and accepting the trust dialog) gives a teammate the project skills, agents, hooks, and settings.json.","kind":"fact","sources":["https://code.claude.com/docs/en/skills","https://code.claude.com/docs/en/settings"]},{"statement":"Codex reads AGENTS.md for project instructions; Claude Code natively reads CLAUDE.md, NOT AGENTS.md, so teams bridge the two (symlink or generated mirror).","kind":"fact","sources":["https://developers.openai.com/codex/guides/agents-md","https://code.claude.com/docs/en/memory"]},{"statement":"AGENTS.md is an open standard stewarded by the Agentic AI Foundation (Linux Foundation), used by ~60k projects and 20+ tools.","kind":"fact","sources":["https://agents.md"]},{"statement":"The dominant CLAUDE.md anti-pattern at scale is the over-stuffed, unowned file; keep it short (~<200 lines), give it an owner, review it like code, and use a root + per-package Context Cascade.","kind":"plan","sources":["https://generativeprogrammer.com/p/how-teams-scale-claude-code-across","https://blog.sshh.io/p/how-i-use-every-claude-code-feature"]},{"statement":"To make 'configure once' span Claude Code and Codex, keep one canonical config and project it: generate-or-symlink AGENTS.md from CLAUDE.md, and mirror skills into each tool's dir (a portable source like ~/.agents/skills symlinked into .claude/skills and Codex's surface), with a drift check that fails CI on divergence. This lets a dev switch between the two tools in VS Code and get identical rules.","kind":"plan","sources":["https://agents.md","https://developers.openai.com/codex/guides/agents-md","project-meta/scripts/meta/render_agents_md.py","project-meta/scripts/check_agent_surface_sync.py"]},{"statement":"Claude Code settings (permissions, hooks, env) live in .claude/settings.json (committed for the team) with personal overrides in .claude/settings.local.json; settings merge across scopes with the higher scope winning per key.","kind":"fact","sources":["https://code.claude.com/docs/en/settings"]},{"statement":"Claude Code access control is allow/ask/deny permission rules — tool-name glob patterns (e.g. Bash(git push:*), Read(./.env.*), mcp__*) in settings.json permissions{}. Rules evaluate deny → ask → allow, first-match-wins; the allow/ask/deny arrays MERGE (concatenate) across scopes, so a higher-scope (managed or user) deny is a floor no lower scope can relax.","kind":"fact","sources":["https://code.claude.com/docs/en/permissions","https://code.claude.com/docs/en/settings"]},{"statement":"Claude Code reads ONLY CLAUDE.md, not AGENTS.md (not even as a fallback). The cross-platform single-source bridge is a first-line `@AGENTS.md` import in CLAUDE.md (recursive up to 4 hops; works on Windows without admin), preferred over an `ln -s AGENTS.md CLAUDE.md` symlink (Windows needs core.symlinks + admin/Developer Mode).","kind":"fact","sources":["https://code.claude.com/docs/en/memory","https://github.com/anthropics/claude-code/issues/34235"]}]},{"id":"stage-3","title":"Capabilities","summary":"You catch yourself pasting the same multi-step instructions for the tenth time, or wishing a rule would just always run. That itch is the signal to reach for skills, agents, hooks, and MCP — the things that turn 'describe it again' into 'it's built in.'","concepts":[{"term":"a skill","def":"An on-demand capability whose description is its trigger — invoked when the description matches the task."},{"term":"progressive disclosure","def":"Keeping a skill lean: a short body plus references loaded only when needed, so it doesn't waste context."},{"term":"a sub-agent","def":"A bounded mission run in its own context with only the tools it needs, returning a summary rather than a transcript."},{"term":"a hook","def":"A command that runs on a lifecycle event and is enforced — it fires no matter what, unlike the advisory CLAUDE.md."},{"term":"an MCP server","def":"An external tool Claude can call (GitHub, a database) via the Model Context Protocol; shared in the repo with secrets kept out of the file."},{"term":"a portable skill","def":"A skill written once in the open SKILL.md format (an open standard both tools support) and made discoverable in each tool's own skills directory — Claude's .claude/skills and Codex's .agents/skills."}],"claims":[{"statement":"Agent Skills use the open SKILL.md format supported by both Claude Code and Codex, but each tool reads only its OWN skills directory (Claude .claude/skills, Codex .agents/skills); portability = author once, mirror into each tool's dir.","kind":"fact","sources":["https://agentskills.io/home","https://developers.openai.com/codex/skills","https://code.claude.com/docs/en/skills"]},{"statement":"Skills are model-invoked via their description (progressive disclosure: discovery loads name+description, activation loads SKILL.md, bundled references load only when accessed).","kind":"fact","sources":["https://code.claude.com/docs/en/skills","https://docs.claude.com/en/docs/agents-and-tools/agent-skills/overview"]},{"statement":"Hooks fire deterministically on event+matcher conditions, independent of the model's choice, and can hard-block (exit 2 / deny); handlers can be shell commands, HTTP endpoints, MCP tool calls, LLM prompts, or subagents, across many events beyond PreToolUse/PostToolUse.","kind":"fact","sources":["https://code.claude.com/docs/en/hooks"]},{"statement":"Each subagent runs in its own context window with a custom system prompt, restricted (least-privilege) tool access, and returns only a summary to the main context.","kind":"fact","sources":["https://code.claude.com/docs/en/sub-agents"]},{"statement":"For supplying always-true knowledge, a static docs-index (AGENTS.md/CLAUDE.md) currently outperforms model-invoked Skills in published evals.","kind":"contested","sources":["https://vercel.com/blog/agents-md-outperforms-skills-in-our-agent-evals"],"positions":[{"name":"passive-context-wins-now","claim":"Vercel eval: AGENTS.md docs-index 100% vs Skills 53-79% (skill never invoked in 56% of cases) - results matter now.","sourceUrls":["https://vercel.com/blog/agents-md-outperforms-skills-in-our-agent-evals"]},{"name":"skills-for-actions","claim":"Skills win for vertical, explicitly-triggered action workflows; the gap is a current model tool-use limitation that will close.","sourceUrls":["https://news.ycombinator.com/item?id=46809708"]}]},{"statement":"Loading many MCP tools consumes a large share of the context window before work begins; the open question is whether to restrict tools or compress outputs.","kind":"contested","sources":["https://blog.sshh.io/p/how-i-use-every-claude-code-feature","https://mksg.lu/blog/context-mode"],"positions":[{"name":"thin-gateway-cli-first","claim":"An MCP should be a thin secure gateway with a few high-level tools; migrate stateless tools (Jira/GitHub/AWS) to CLIs, reserve MCP for stateful (Playwright).","sourceUrls":["https://blog.sshh.io/p/how-i-use-every-claude-code-feature"]},{"name":"compress-dont-restrict","claim":"Keep tool richness; route outputs through a compression middleware for ~98% context reduction.","sourceUrls":["https://mksg.lu/blog/context-mode"]}]},{"statement":"To make 'configure once' span Claude Code and Codex, keep one canonical config and project it: generate-or-symlink AGENTS.md from CLAUDE.md, and mirror skills into each tool's dir (a portable source like ~/.agents/skills symlinked into .claude/skills and Codex's surface), with a drift check that fails CI on divergence. This lets a dev switch between the two tools in VS Code and get identical rules.","kind":"plan","sources":["https://agents.md","https://developers.openai.com/codex/guides/agents-md","project-meta/scripts/meta/render_agents_md.py","project-meta/scripts/check_agent_surface_sync.py"]}]},{"id":"stage-4","title":"Sharing It: Plugins & Marketplace","summary":"Three people on the team have each rebuilt the same review skill, slightly differently. The waste isn't the building — it's that there's no shelf everyone pulls from. That shelf is a plugin marketplace.","concepts":[{"term":"a plugin","def":"A versioned bundle of skills, sub-agents, hooks, and MCP servers — the unit you share."},{"term":"a plugin marketplace","def":"A catalog repo of plugins: the team adds it once, installs the plugins it needs, and gets updates by pushing to the repo."}],"claims":[{"statement":"A Claude Code plugin can bundle slash commands, subagents, skills, hooks, MCP servers, and LSP servers into one installable unit.","kind":"fact","sources":["https://code.claude.com/docs/en/plugins"]},{"statement":"A plugin marketplace is a catalog repo defined by .claude-plugin/marketplace.json; teams add it once (/plugin marketplace add), install plugins (/plugin install), and pull updates (/plugin marketplace update).","kind":"fact","sources":["https://code.claude.com/docs/en/plugin-marketplaces","https://code.claude.com/docs/en/discover-plugins"]},{"statement":"Installing a plugin activates its skills, hooks, and MCP servers as code under one trust decision; audits of open third-party marketplaces found malicious skills (Snyk: 76 confirmed malicious payloads among ~3,984 skills; 13.4% critical), and plugin-injection attacks are demonstrated.","kind":"fact","sources":["https://pluto.security/blog/claude-extension-ecosystem-security-practitioner-guide/","https://www.promptarmor.com/resources/hijacking-claude-code-via-injected-marketplace-plugins"]}]},{"id":"stage-5","title":"Governance & Plan","summary":"Someone is one keystroke from committing a secret, and a polite note in CLAUDE.md won't stop them. Governance is how you turn the must-nots into things the system enforces — and how you avoid paying for features your plan doesn't include.","concepts":[{"term":"managed settings","def":"Organization policy that overrides local config, delivered by an admin from the console or a policy file."},{"term":"settings precedence","def":"The order settings resolve in: managed (org) wins, then CLI, then local, then project, then user."},{"term":"seats & plan gating","def":"Per-seat plans decide access. Claude Code is included on EVERY Team seat (Standard and Premium) — Premium just buys more usage, it is not an access gate. A few autonomous extras (computer use, phone Dispatch) are a Pro/Max research preview and are not part of the documented Team plan."},{"term":"a permission rule","def":"The actual access-control primitive: allow / ask / deny lists of tool-name glob patterns (e.g. Bash(git push:*), Read(./.env.*), mcp__*) in settings.json. Rules evaluate deny → ask → allow, first-match-wins, and the arrays MERGE across scopes — so a higher-scope (managed or user) deny is a floor no lower scope can relax. This is the mechanism behind 'deny secret reads'."},{"term":"permission mode","def":"An autonomy dial orthogonal to the rules — how aggressively Claude acts, not what is allowed: default (ask), plan (read-only, proposes changes), acceptEdits (auto-approve edits), auto (a classifier model approves/denies each call), and bypassPermissions (no prompts at all). Set via defaultMode; cycle in-session with Shift+Tab."}],"claims":[{"statement":"Computer use and phone Dispatch are a Pro/Max research preview (macOS and Windows); the docs do NOT document Team/Enterprise exclusion, so 'not on Team' is an inference, restated as 'not part of the documented Team plan'.","kind":"perishable","sources":["https://claude.com/blog/dispatch-and-computer-use","https://support.claude.com/en/articles/12138966"]},{"statement":"Claude Code is included on every Team seat (Standard and Premium); Premium only buys more usage and is NOT the access gate. (A Premium/Code-seat requirement applies only to legacy Enterprise.)","kind":"fact","sources":["https://support.claude.com/en/articles/11845131","https://support.claude.com/en/articles/9266767"]},{"statement":"Settings precedence, highest to lowest: managed (org) > command-line args > local project (.claude/settings.local.json) > shared project (.claude/settings.json) > user (~/.claude/settings.json).","kind":"fact","sources":["https://code.claude.com/docs/en/settings"]},{"statement":"Managed (enterprise-policy) settings sit at the top of precedence and cannot be overridden by a user, CLI flag, or project file.","kind":"fact","sources":["https://code.claude.com/docs/en/settings"]},{"statement":"The managed-settings keys strictKnownMarketplaces, blockedMarketplaces, strictPluginOnlyCustomization, allowedMcpServers, wslInheritsWindowsSettings, and minimumVersion all exist and are enforced (plus allowManagedHooksOnly / allowManagedMcpServersOnly / allowManagedPermissionRulesOnly).","kind":"fact","sources":["https://code.claude.com/docs/en/settings","https://github.com/anthropics/claude-code/tree/main/examples/settings"]},{"statement":"Installing a plugin activates its skills, hooks, and MCP servers as code under one trust decision; audits of open third-party marketplaces found malicious skills (Snyk: 76 confirmed malicious payloads among ~3,984 skills; 13.4% critical), and plugin-injection attacks are demonstrated.","kind":"fact","sources":["https://pluto.security/blog/claude-extension-ecosystem-security-practitioner-guide/","https://www.promptarmor.com/resources/hijacking-claude-code-via-injected-marketplace-plugins"]},{"statement":"Claude Code access control is allow/ask/deny permission rules — tool-name glob patterns (e.g. Bash(git push:*), Read(./.env.*), mcp__*) in settings.json permissions{}. Rules evaluate deny → ask → allow, first-match-wins; the allow/ask/deny arrays MERGE (concatenate) across scopes, so a higher-scope (managed or user) deny is a floor no lower scope can relax.","kind":"fact","sources":["https://code.claude.com/docs/en/permissions","https://code.claude.com/docs/en/settings"]},{"statement":"Permission modes are an autonomy dial orthogonal to the rules: default (ask), plan (restricted to reads and read-only shell, proposes changes without editing), acceptEdits (auto-approve file edits + common fs commands in the working dir), auto (a classifier model approves/denies each call), and bypassPermissions (no prompts — and as of v2.1.126 even protected paths like .git/.claude execute). Set via defaultMode; cycle in-session with Shift+Tab.","kind":"fact","sources":["https://code.claude.com/docs/en/permission-modes","https://code.claude.com/docs/en/settings"]}]},{"id":"stage-6","title":"Decide: Your Team's Shared Setup","summary":"You now know the parts — but the real question your team is waiting on is simpler and harder: where does each shared thing actually live, and what do we commit to? This is where you decide.","concepts":[{"term":"the four-tier model","def":"Where each shared thing belongs: project config in the repo, cross-project tools in a marketplace, org policy in managed settings, and personal config local."},{"term":"a shared library","def":"One set of skills, agents, and tools the whole team uses — versioned, distributed, and working across both Claude and Codex."}],"claims":[{"statement":"Shared team setup falls into four tiers by where each thing lives: project config in the repo (.claude/), cross-project tools in a plugin marketplace, org policy in managed settings, and personal config local to each user.","kind":"plan","sources":["https://claude.com/blog/steering-claude-code-skills-hooks-rules-subagents-and-more","https://code.claude.com/docs/en/settings"]},{"statement":"A team's shared library is one set of skills/agents/tools distributed via a plugin marketplace plus repo .claude/ + AGENTS.md, made portable across Claude Code and Codex — the assembled output of the four tiers.","kind":"plan","sources":["https://claude.com/blog/steering-claude-code-skills-hooks-rules-subagents-and-more","https://code.claude.com/docs/en/skills"]}]}];

// __STAGES_DATA__ (injected)
const stages = STAGES;
log(`authoring ${stages.length} stages`);

const SECTIONS = {
  type: "object", additionalProperties: false, required: ["sections"],
  properties: {
    sections: {
      type: "array",
      description: "8-16 substantive lesson sections in reading order",
      items: {
        type: "object", additionalProperties: false, required: ["heading", "body"],
        properties: {
          heading: { type: "string", description: "section heading" },
          body: { type: "string", description: "markdown-lite body: paragraphs, - bullets, | tables |, ``` code fences ```, and ## / ### sub-headings. Use real commands and file layouts in code fences." },
          targetConceptsCovered: { type: "array", items: { type: "string" }, description: "list of target concept TERMS (from the stage definition) that this section directly teaches. At least one section must cover every listed target concept." },
          unverifiedClaims: { type: "array", items: { type: "string" }, description: "REQUIRED: list any specific technical details in this section (version numbers, env var names, file paths, setting key names, exact CLI flags) that you could NOT trace verbatim to the provided verified claims or official docs. If you are confident everything is sourced, return an empty array." },
        },
      },
    },
  },
};

const A = { agentType: "general-purpose", effort: "high" };

const brief = (s) =>
  `STAGE: ${s.title} (${s.id})\nOne-liner: ${s.summary}\n\n` +
  `CONCEPTS this stage introduces (define and go deep on each):\n` +
  s.concepts.map((c) => `- ${c.term}: ${c.def}`).join("\n") +
  `\n\nVERIFIED CLAIMS (authoritative — treat as fact, cite their sources; do NOT contradict):\n` +
  s.claims.map((c) => `- [${c.kind}] ${c.statement}` + (c.positions ? `\n    positions: ${c.positions.map((p) => p.name + ": " + p.claim).join(" | ")}` : "") + (c.sources ? `\n    sources: ${c.sources.join(", ")}` : "")).join("\n");

const results = await pipeline(
  stages,
  // 1. Author — comprehensive, deep, concrete
  (s) => agent(
    `You are writing ONE stage of a serious technical course for a lead setting up their team's shared Claude Code + OpenAI Codex infrastructure. The reader can read code and wants DEPTH, not hand-holding — prior drafts were criticized as "one page of text written for a five-year-old." Write a comprehensive treatment.\n\n${brief(s)}\n\n` +
    `REQUIREMENTS:\n` +
    `- Substantial: 8-16 sections, ~2000-3500 words total. Real substance, not filler.\n` +
    `- CONCRETE mechanics: actual file layouts, config, and commands in \`\`\` code fences; real examples a reader can copy. Show the formats (SKILL.md frontmatter, settings.json, .mcp.json, marketplace.json, /plugin commands, permission rules, etc.) where relevant to this stage.\n` +
    `- At least one markdown comparison/decision TABLE (| col | col |) where it helps.\n` +
    `- Include "When to use what", tradeoffs, and "Common pitfalls / gotchas" — the WHY, edge cases, and failure modes, not just the what.\n` +
    `- Use ## and ### sub-headings inside section bodies for structure.\n` +
    `- ACCURACY IS PARAMOUNT: the verified claims above are ground truth. For any command/format/flag you are not certain of, use WebSearch/WebFetch against official docs (code.claude.com/docs, developers.openai.com/codex, agents.md, agentskills.io) before writing it. Never invent a command or flag.\n` +
    `- GROUNDING RULE: Before including any specific technical detail — version numbers (v2.x.x), environment variable names (ALL_CAPS), exact file paths, exact CLI flag names, exact setting key names, exact behavioral rules — verify it appears verbatim or paraphrase-equivalent in the provided VERIFIED CLAIMS or official docs. If you cannot trace it to a source, OMIT IT or use a generic description. Do not add plausible-sounding specifics that are not in the provided material.\n` +
    `- CONCEPT COVERAGE: The stage defines specific target concepts. Every listed target concept MUST have at least one lesson section that directly explains it. If you finish drafting and a concept has no coverage, ADD A SECTION. List covered concept terms in each section's targetConceptsCovered field.\n` +
    `- COMMITTED-VS-PERSONAL TABLE: For any section covering configuration files, include an explicit table mapping each artifact to: committed-to-VCS | gitignored | never-commit. This prevents the recurring omission of the personal-config boundary.\n` +
    `- A short engaging opener is fine, but spend your words on depth.\n` +
    `Return the sections.`,
    { ...A, schema: SECTIONS, label: `author:${s.id}`, phase: "Author" },
  ),
  // 2. Verify and deepen — fact-check against docs, fill thin spots
  (authored, s) => agent(
    `You are a senior technical reviewer hardening one stage of a Claude Code + Codex team-setup course. Below are draft sections and the stage's verified claims. Your job:\n` +
    `1. FACT-CHECK every command, file format, flag, and claim against OFFICIAL docs (use WebFetch on code.claude.com/docs, developers.openai.com/codex, agents.md). Fix anything wrong or vague; make commands/paths exact.\n` +
    `2. REMOVE HALLUCINATED SPECIFICS: Look at each section's unverifiedClaims list. For any claim flagged there, either (a) fetch the official doc and confirm the exact string, or (b) remove or generalize it. Do not publish specific version numbers, env var names, or file paths that cannot be traced to a source.\n` +
    `3. CONCEPT COVERAGE: Check that the draft covers ALL target concepts. For any concept not yet covered, add a new section.\n` +
    `4. DEEPEN any thin section — add the missing mechanics, an example, a pitfall, or a tradeoff. Ensure there is real substance and at least one concrete code fence and one table.\n` +
    `5. Keep it accurate to the verified claims (do not contradict them).\n\n` +
    `STAGE: ${s.title} (${s.id})\n\nVERIFIED CLAIMS:\n` +
    s.claims.map((c) => `- ${c.statement}` + (c.sources ? ` [${c.sources.join(", ")}]` : "")).join("\n") +
    `\n\nDRAFT SECTIONS (JSON):\n${JSON.stringify(authored?.sections ?? []).slice(0, 40000)}\n\n` +
    `Return the corrected, deepened sections (same shape).`,
    { ...A, schema: SECTIONS, label: `verify:${s.id}`, phase: "Verify" },
  ),
);

// assemble { stageId: sections }
const out = {};
stages.forEach((s, i) => {
  const r = results[i];
  if (r && Array.isArray(r.sections)) out[s.id] = r.sections;
});
const counts = Object.entries(out).map(([k, v]) => `${k}:${v.length}`).join(" ");
log(`authored sections — ${counts}`);
return { stageContent: out, stageCount: Object.keys(out).length };
